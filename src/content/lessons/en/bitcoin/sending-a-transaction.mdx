---
title: "Sending a Transaction and Its Fee"
description: "Follow a bitcoin payment from your screen to a confirmed block: inputs and outputs, the change you send back to yourself, the mempool fee market in sat/vByte, confirmations, and why a fee is paid to the miner — not to a company."
topic: "en/bitcoin"
order: 6
minutes: 9
updated: 2026-06-02
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, Categorize, MatchConcepts, MindMap, MempoolFeeMarket, UtxoVsAccount } from '@/components/react';

You tap **Send**, pick an amount, and your wallet does something that looks suspiciously
simple. Under the hood, it's anything but. To pay you don't "withdraw" a number — you
reach into a pile of **coins** you already hold, hand whole ones over, get change back,
quietly tip a miner, and then broadcast the whole bundle into a global waiting room to be
picked up. This lesson follows one bitcoin payment along that entire journey, and finally
demystifies the part everyone grumbles about and almost nobody understands: **the fee** —
where it comes from, who gets it, and why it has nothing to do with how much you're
sending.

## Your Balance Is a Pile of Coins (UTXOs)

<MCQ
  client:visible
  pretest
  question="Guess before reading: when your wallet shows '0.8 BTC', what is actually stored on the Bitcoin blockchain?"
  options={[
    { text: "A set of discrete unspent chunks (UTXOs) addressed to your keys — your wallet adds them up", correct: true },
    { text: "A single field that literally reads 'your balance: 0.8 BTC'", correct: false },
    { text: "Your coins locked in a vault run by the Bitcoin Foundation", correct: false },
    { text: "Nothing — the balance exists only inside your phone's app", correct: false },
  ]}
  explanation="Bitcoin has no 'balance' field. The chain stores a pile of UTXOs — Unspent Transaction Outputs — that your keys control. Your wallet scans for every unspent chunk that's yours and sums them. That set of coins is what you actually spend."
/>

You met UTXOs back in crypto-basics; here's the deepened version, because everything in
this lesson hangs off it. **UTXO** stands for **Unspent Transaction Output**. Every time
someone pays you, the payment shows up as an *output* of their transaction. Until you
spend it, it sits on the chain *unspent* — like an uncashed cheque made out to your keys.
So your "balance" is really a **wallet full of discrete coins of various sizes**: maybe a
0.5 BTC chunk, a 0.2 BTC chunk, and a 0.1 BTC chunk, adding up to the 0.8 BTC your app
shows.

The crucial rule — the one that drives change, fees, and coin selection — is that **you
spend UTXOs whole.** Just as you can't tear a 20-dollar bill in half, you can't spend
"part of" a UTXO. To pay, you consume one or more whole coins as **inputs** and create
brand-new coins as **outputs**. The chain's record of what you own is always just the set
of outputs nobody has spent yet.

Watch the difference between this coin-like model and a plain bank balance. Hit **"Send a
payment"** and keep your eye on the left (UTXO) side: a whole coin lifts out toward the
recipient, and a smaller **change** chip floats right back to you.

<UtxoVsAccount client:visible />

<MCQ
  client:visible
  question="Your wallet holds three UTXOs: 0.5, 0.2 and 0.1 BTC. Which statement is true?"
  options={[
    { text: "Your balance is 0.8 BTC, and to pay you must spend one or more of those coins whole", correct: true },
    { text: "There's a single 0.8 BTC coin on the chain that you slice as needed", correct: false },
    { text: "You can spend exactly 0.05 BTC out of the 0.1 BTC coin and leave the rest in place", correct: false },
    { text: "The chain stores '0.8' in your account's balance field", correct: false },
  ]}
  explanation="Balance is the sum of your UTXOs (0.5 + 0.2 + 0.1 = 0.8). Each coin is spent whole as an input — you can't carve 0.05 off a 0.1 coin and leave the remainder untouched. There's no single coin and no balance field."
/>

## Inputs, Outputs, and Change

<MCQ
  client:visible
  pretest
  question="Guess before reading: you want to send 0.3 BTC but your smallest coin is 0.5 BTC. What does your wallet do?"
  options={[
    { text: "Spends the whole 0.5 BTC coin and sends the leftover back to you as a change output", correct: true },
    { text: "Refuses the payment until you own a coin worth exactly 0.3 BTC", correct: false },
    { text: "Splits the 0.5 coin in two and only moves half", correct: false },
    { text: "Sends 0.5 BTC and asks the recipient to refund 0.2", correct: false },
  ]}
  explanation="UTXOs are spent whole. Your wallet consumes the 0.5 BTC coin as an input, creates a 0.3 BTC output to the recipient, and creates a change output back to an address it controls for the leftover (minus the fee)."
/>

A bitcoin transaction has two sides:

- **Inputs** — references to UTXOs you own, each one **unlocked by your signature**
  (lesson 5). An input says "I'm spending this specific coin, and here's the proof I'm
  allowed to." The inputs must add up to **at least** the amount you're sending plus the
  fee.
- **Outputs** — the new coins this transaction creates: one (or more) to the recipient,
  and usually one back to yourself.

That last one is **change**. Because you spend whole coins, your inputs almost always
exceed what you owe — so the leftover is sent back to you as a new UTXO at a **change
address** your wallet controls. It's the digital twin of paying a 7-dollar coffee with a
20-dollar bill and getting 13 dollars back. The twist: in Bitcoin **you create the change
output yourself**, as part of the very same transaction. There's no cashier handing it
over; your wallet writes the change output in for you.

<Callout variant="info" title="Change is your own money — not a cost">
Seeing a "change" output is completely normal and means the system is working. It is
**your leftover money returning to you**, not a charge. The only value that actually
leaves your control is the fee. If you spend a 0.5 BTC coin to send 0.3 BTC, roughly 0.2
BTC comes straight back to you as change.
</Callout>

<MatchConcepts
  client:visible
  question="Connect every term to its correct meaning."
  pairs={[
    { term: "Input", definition: "A reference to a UTXO you own, unlocked by your signature" },
    { term: "Output", definition: "A new coin this transaction creates for a recipient" },
    { term: "Change output", definition: "Leftover value sent back to an address you control" },
    { term: "UTXO", definition: "An unspent chunk of money your keys can spend whole" },
  ]}
/>

## Where the Fee Actually Comes From

<MCQ
  client:visible
  pretest
  question="Guess before reading: how does a Bitcoin transaction specify its fee?"
  options={[
    { text: "There's no fee field — the fee is whatever the inputs exceed the outputs by", correct: true },
    { text: "You type a fee into a dedicated 'fee' field that the chain reads", correct: false },
    { text: "Bitcoin charges a flat percentage of the amount sent", correct: false },
    { text: "The fee is billed to your card separately after the payment clears", correct: false },
  ]}
  explanation="A transaction has no explicit fee field. The fee is the implicit remainder: total inputs minus total outputs. Whatever value you don't assign to an output, the miner who includes your transaction claims as the fee."
/>

Here's the part that surprises everyone. A Bitcoin transaction has **no fee field at
all.** Instead:

<Reveal client:visible buttonLabel="So where does the fee come from?">
**Fee = total inputs − total outputs.** Whatever value you put into the transaction (your
inputs) but *don't* assign to any output (recipient + change) is left over — and the
miner who includes your transaction simply **claims that leftover** as the fee. To pay a
bigger fee, you make your change output a little smaller. To pay a tiny fee, you make it a
little bigger. The "fee" is just the gap you choose to leave.
</Reveal>

Let's make it concrete. Say you spend a single 0.5 BTC coin to send 0.3 BTC, and you set
your change output to 0.1999 BTC:

| Part of the transaction | Amount (BTC) |
|---|---|
| **Input** (one UTXO you own) | 0.5000 |
| Output → recipient | 0.3000 |
| Output → change (back to you) | 0.1999 |
| **Fee** (inputs − outputs, claimed by the miner) | **0.0001** |

Inputs total 0.5; outputs total 0.4999; the 0.0001 BTC gap is the fee. Nobody wrote
"0.0001" into a fee box — it's just the slice you chose not to send anywhere, and the
miner who packs your transaction into a block scoops it up.

<Callout variant="warning" title="Myth: 'The fee goes to Bitcoin / a company / the network treasury'">
There is no Bitcoin company, no head office, and no treasury to pay. The fee goes to the
**miner** who includes your transaction in a block — the same proof-of-work miner from
lessons 2–3 who spent real electricity to build that block. You're tipping the person who
seats your transaction, alongside the block subsidy they already earn for mining it.
</Callout>

<MCQ
  client:visible
  question="In the worked example above, what would happen if you set your change output to 0.2000 BTC instead of 0.1999 BTC?"
  options={[
    { text: "The fee drops to 0 BTC (inputs 0.5 − outputs 0.5), so a miner has little reason to include it", correct: true },
    { text: "The fee doubles, because larger change means a larger fee", correct: false },
    { text: "The transaction is rejected for having too much change", correct: false },
    { text: "The extra 0.0001 BTC is paid to the recipient automatically", correct: false },
  ]}
  explanation="Outputs would then total 0.5 BTC, exactly matching the input, leaving a 0 BTC gap — a zero fee. A bigger change output means a smaller fee, not a bigger one. And a zero-fee transaction gives miners no incentive to pick it."
/>

## Fee Rate: Paying by Size, Not Amount

<MCQ
  client:visible
  pretest
  question="Guess before reading: a Bitcoin transaction's fee mostly depends on…"
  options={[
    { text: "Its size in (virtual) bytes and how congested the mempool is — not the amount sent", correct: true },
    { text: "A fixed percentage of the BTC amount you're moving", correct: false },
    { text: "How far away the recipient lives", correct: false },
    { text: "How long your private key is", correct: false },
  ]}
  explanation="Fees are priced per unit of transaction size (sat/vByte), and the going rate rises with congestion. A small payment can cost more in fees than a huge one if it bundles more inputs and therefore takes up more space."
/>

If the fee isn't a percentage of the amount, what *does* set it? **Size.** Miners care
about one thing: getting the most fee revenue into a block that has a fixed capacity. So
they value a transaction by its **fee rate** — fee divided by size — measured in
**satoshis per virtual byte (sat/vByte)**. (A *satoshi* is the smallest unit of bitcoin,
one hundred-millionth of a BTC. A *virtual byte* is Bitcoin's way of measuring how much
block space a transaction occupies.)

You don't really choose a total fee; you choose a **fee rate**, and the total falls out
of *rate × size*. The size, in turn, depends mostly on **how many inputs** you bundle. A
payment that gathers ten little coins is physically bigger — ten signatures to include —
than one that spends a single fat coin, even if the second moves a hundred times more
money.

<Callout variant="info" title="Why a small payment can cost MORE than a huge one">
Send 0.001 BTC by sweeping together 20 tiny "dust" UTXOs and your transaction is large —
20 inputs, 20 signatures — so at a given sat/vByte it costs a lot. Send 50 BTC from one
clean UTXO and it's tiny — one input — so it's cheap. Fees track **bytes, not value**.
That's why letting your wallet fill up with dust is a quiet way to overpay later.
</Callout>

Miners fill each block **highest-fee-rate first**, so a higher sat/vByte jumps the queue.
We'll watch that auction in the next section.

<Categorize
  client:visible
  question="Sort each change into whether it tends to raise your total fee or leave it unaffected."
  buckets={["Raises your fee", "Doesn't affect your fee"]}
  items={[
    { text: "Bundling many small UTXOs as inputs (a bigger transaction)", bucket: "Raises your fee" },
    { text: "Bidding a higher sat/vByte fee rate", bucket: "Raises your fee" },
    { text: "Sending during heavy mempool congestion", bucket: "Raises your fee" },
    { text: "Doubling the BTC amount you send (same one input)", bucket: "Doesn't affect your fee" },
    { text: "The recipient living on another continent", bucket: "Doesn't affect your fee" },
    { text: "Using a longer or shorter wallet password", bucket: "Doesn't affect your fee" },
  ]}
/>

## From Broadcast to Confirmation

<MCQ
  client:visible
  pretest
  question="Guess before reading: right after your wallet finishes building and signing a payment, the transaction is…"
  options={[
    { text: "Broadcast to nodes and held in the mempool until a miner includes it in a block", correct: true },
    { text: "Instantly final the moment your screen says 'sent'", correct: false },
    { text: "Written into the blockchain directly by your own phone", correct: false },
    { text: "Mailed to the recipient's bank to clear overnight", correct: false },
  ]}
  explanation="Building and signing happen on your device, but then the signed transaction is broadcast to the network and waits, unconfirmed, in the mempool. Only when a miner packs it into a block does it start to confirm."
/>

Every Bitcoin payment runs the same relay. Here's the whole trip:

| Stage | What happens |
|---|---|
| **1. Build** | Your wallet selects which UTXOs to spend and writes the inputs, outputs, and change |
| **2. Sign** | You authorize each input with your **signature** (lesson 5) — the proof you control those coins |
| **3. Broadcast** | The signed transaction is sent out to the network's **nodes** |
| **4. Mempool** | It sits, valid but unconfirmed, in the pool of pending transactions |
| **5. Block inclusion** | A **miner** selects it (highest fee rate first) and packs it into a block |
| **6. Confirmations** | The block is mined onto the chain; each later block adds another confirmation |

A **confirmation** is just *a block stacked on top of the block holding your
transaction*. **One confirmation** = your transaction is in the latest block.
**Six confirmations** = five more blocks have piled on top. Why count them? Because
reversing your payment would mean re-mining your block **and every block above it** faster
than the rest of the network mines new ones — exactly the proof-of-work tug-of-war from
lessons 2–3. The deeper your transaction is buried, the more work an attacker must redo.
So **more confirmations = harder to reverse = safer**, and ~6 is the common "very safe"
threshold for large amounts.

<Callout variant="warning" title="Myth: 'One confirmation means it's irreversible'">
One confirmation is *good* but not *final*. A single block can occasionally be displaced
by a competing one (a brief "reorg"). Each extra confirmation makes that exponentially
less likely, which is why high-value payments wait for several. Deep enough = effectively
irreversible; just-included = not quite there yet.
</Callout>

<Categorize
  client:visible
  question="Sort each event into the stage of a transaction's life where it belongs."
  buckets={["Before broadcast", "In the mempool", "After a block includes it"]}
  items={[
    { text: "Your wallet signs each input with your private key", bucket: "Before broadcast" },
    { text: "Your wallet picks which UTXOs to spend and writes the change output", bucket: "Before broadcast" },
    { text: "The transaction sits unconfirmed, waiting to be picked", bucket: "In the mempool" },
    { text: "It competes against other pending transactions on fee rate", bucket: "In the mempool" },
    { text: "A miner packs it into a block", bucket: "After a block includes it" },
    { text: "More blocks stack on top, adding confirmations", bucket: "After a block includes it" },
  ]}
/>

## When the Mempool Gets Crowded

<MCQ
  client:visible
  pretest
  question="Guess before reading: your transaction has been stuck pending for hours. The most likely reason is…"
  options={[
    { text: "Your fee rate is too low — higher-paying transactions keep filling the limited block space ahead of you", correct: true },
    { text: "The recipient hasn't 'accepted' it yet", correct: false },
    { text: "Bitcoin shuts down overnight and resumes in the morning", correct: false },
    { text: "Your signature expired and must be renewed", correct: false },
  ]}
  explanation="Block space is scarce, and miners pick the highest fee rates first. A stuck transaction is almost always one being outbid. Recipients don't 'accept' pending payments, and the network never pauses."
/>

Each block holds only so many virtual bytes, and a new one arrives roughly every ten
minutes. When more people want to transact than fit in the next few blocks, the mempool
**fills up** and a bidding war breaks out: the going **fee rate** climbs as everyone
tries to outbid everyone else for the limited seats. In quiet times the mempool nearly
empties and a few sat/vByte sails through; in a frenzy the clearing rate can spike
dramatically. Nothing about your transaction changed — the **competition** did.

Drag **your fee** up and down below, hit *produce next block*, and watch whether your
transaction grabs a slot or gets left in the mempool. There's no magic "correct" fee —
it's whatever beats the others bidding for the same block.

<MempoolFeeMarket client:visible />

What if you already broadcast a transaction with too low a fee and it's stuck? It is
**not lost forever**. You have options:

- **Replace-by-Fee (RBF):** re-broadcast the *same* transaction with a higher fee rate,
  replacing the stuck one and jumping the queue. (Your wallet must have flagged it as
  replaceable.)
- **Child-Pays-For-Parent (CPFP):** spend the stuck transaction's change output in a new,
  high-fee child transaction. A miner who wants the juicy child fee must include the
  cheap parent too, dragging both into a block.
- **Just wait:** if congestion eases, even a low-fee transaction can confirm later. And if
  it never confirms, it eventually drops out of mempools and the coins are simply spendable
  again — they were never gone.

<Callout variant="warning" title="Myth: 'A stuck transaction is lost money'">
A pending transaction hasn't moved any coins yet — it's still just an offer sitting in the
mempool. It can confirm later when fees drop, be bumped via RBF, be rescued via CPFP, or
quietly expire from the mempool, leaving your UTXOs fully spendable. Stuck ≠ gone.
</Callout>

<FillBlank
  client:visible
  question="Pick the right word for each blank."
  text="A signed transaction is broadcast to {{nodes|banks|miners}} and waits in the {{mempool|wallet|ledger}}. Its fee is the gap between {{inputs|amounts|senders}} and outputs, claimed by the {{miner|bank|recipient}} who includes it. Fees are priced per {{vByte|dollar|coin}}, and each block stacked on top adds a {{confirmation|reconfirmation|signature}}."
  explanation="Transactions broadcast to nodes and wait in the mempool; the fee is inputs minus outputs, claimed by the miner; fees are priced per virtual byte (sat/vByte); and each block on top is one more confirmation."
/>

<MCQ
  client:visible
  question="Recall from earlier lessons: what is it about Bitcoin's design that makes deep confirmations expensive to reverse?"
  options={[
    { text: "Reversing requires redoing the proof-of-work for your block and every block above it, faster than the network", correct: true },
    { text: "Each confirmation re-checks your signature, and signatures get harder to forge over time", correct: false },
    { text: "Confirmations lock your private key, which can't be unlocked once buried", correct: false },
    { text: "Older blocks are stored by more companies, who refuse to change them", correct: false },
  ]}
  explanation="From lessons 2–3: blocks are secured by proof-of-work. To rewrite a buried transaction, an attacker must re-mine its block and out-pace the honest network on every block since — costing more energy the deeper it sits. Signatures (lesson 5) prove ownership but aren't what makes reversal costly."
/>

## The Big Picture

One payment: built from coins you hold, signed, broadcast, parked in the mempool, and
bid into a block whose miner pockets the leftover as a fee. Chunk the whole journey into
one picture:

<MindMap
  client:visible
  title="The life of a bitcoin transaction"
  caption="Spend whole UTXOs as inputs, create outputs plus change, leave a gap as the fee, then broadcast into the mempool where transactions bid by fee rate for scarce block space until a miner confirms them."
  root={{
    label: 'A bitcoin transaction',
    children: [
      {
        label: 'Built from coins',
        children: [
          { label: 'Balance = a pile of UTXOs' },
          { label: 'Spend whole coins as inputs' },
          { label: 'Create outputs + change back to you' },
        ],
      },
      {
        label: 'The fee',
        children: [
          { label: 'Fee = inputs − outputs (no fee field)' },
          { label: 'Goes to the miner, not a company' },
          { label: 'Priced by size in sat/vByte, not amount' },
        ],
      },
      {
        label: 'The journey',
        children: [
          { label: 'Sign each input (lesson 5)' },
          { label: 'Broadcast to nodes → mempool' },
          { label: 'Miner includes it (highest fee rate first)' },
          { label: 'Confirmations stack → safer' },
        ],
      },
      {
        label: 'Congestion',
        children: [
          { label: 'Full mempool → fee rates rise' },
          { label: 'Stuck? RBF or CPFP to unstick' },
          { label: 'Pending ≠ lost' },
        ],
      },
    ],
  }}
/>

A mixed recap — it pulls from everything above:

<Quiz
  client:visible
  questions={[
    {
      question: "How is a Bitcoin transaction's fee determined?",
      options: [
        { text: "It's the leftover: total inputs minus total outputs, claimed by the including miner", correct: true },
        { text: "A flat percentage of the amount you send, set by the network", correct: false },
        { text: "A fixed price typed into a dedicated fee field the chain reads", correct: false },
        { text: "A monthly subscription charged by your wallet provider", correct: false },
      ],
      explanation: "There is no fee field. The fee is the gap between what you put in (inputs) and what you assign to outputs (recipient + change). The miner who includes the transaction takes that gap.",
    },
    {
      question: "You spend one 0.5 BTC UTXO to send 0.4 BTC and set your change output to 0.0998 BTC. What is the fee?",
      options: [
        { text: "0.0002 BTC", correct: true },
        { text: "0.0998 BTC", correct: false },
        { text: "0.4 BTC", correct: false },
        { text: "0 BTC — the change covers it", correct: false },
      ],
      explanation: "Fee = inputs − outputs = 0.5 − (0.4 + 0.0998) = 0.5 − 0.4998 = 0.0002 BTC. The recipient gets 0.4, you get 0.0998 back as change, and the 0.0002 gap is the miner's fee.",
    },
    {
      question: "Why can a tiny payment sometimes cost more in fees than a huge one?",
      options: [
        { text: "Fees track transaction size in vBytes; bundling many small inputs makes a bigger, costlier transaction", correct: true },
        { text: "Small payments are taxed at a higher percentage", correct: false },
        { text: "Miners dislike small amounts and punish them", correct: false },
        { text: "Huge payments are always exempt from fees", correct: false },
      ],
      explanation: "Fee rate is sat/vByte. A small payment that sweeps 20 dust UTXOs is large (20 inputs, 20 signatures), while a 50 BTC payment from one UTXO is tiny. Bytes drive the fee, not the BTC value moved.",
    },
    {
      question: "Who receives the fee on your transaction?",
      options: [
        { text: "The miner who includes it in a block", correct: true },
        { text: "The Bitcoin Foundation, as a network toll", correct: false },
        { text: "The recipient, on top of the amount sent", correct: false },
        { text: "Nobody — it's burned and destroyed", correct: false },
      ],
      explanation: "The fee is the miner's reward for seating your transaction in their block, on top of the block subsidy. There is no company or treasury, and the recipient never gets the fee.",
    },
    {
      question: "Your low-fee transaction is stuck in the mempool. Which is a real way to deal with it?",
      options: [
        { text: "Use Replace-by-Fee (RBF) to re-broadcast it with a higher fee rate", correct: true },
        { text: "Call Bitcoin support to manually push it through", correct: false },
        { text: "Nothing — the coins are now permanently lost", correct: false },
        { text: "Ask the miner to lower the going fee rate for you", correct: false },
      ],
      explanation: "RBF replaces a stuck, replaceable transaction with a higher-fee version; CPFP is the other rescue. It can also just confirm later or drop from the mempool, freeing your UTXOs. There's no support line and the coins aren't lost.",
    },
    {
      question: "What does 'six confirmations' tell you?",
      options: [
        { text: "Five blocks have been mined on top of the block holding your transaction, making reversal very costly", correct: true },
        { text: "Six different miners each personally approved your payment", correct: false },
        { text: "Your transaction was broadcast six times for safety", correct: false },
        { text: "You paid six separate fees", correct: false },
      ],
      explanation: "A confirmation is a block stacked on top. Six means your block plus five more above it — reversing it would mean re-mining all of them faster than the network, so deeper = safer.",
    },
  ]}
/>

## Key Takeaways

<Callout variant="success" title="What to remember">
- **Your balance is a pile of UTXOs** — discrete unspent coins your keys control. To pay, you spend whole coins as **inputs** and create **outputs**; the leftover comes back as a **change output** to an address you control.
- **The fee has no field: fee = inputs − outputs.** Whatever value you don't assign to an output, the **miner** who includes your transaction claims. The fee goes to that miner — not to a Bitcoin company, the recipient, or a treasury.
- **Fees are priced by size, not amount.** You bid a **fee rate in sat/vByte**, and miners fill blocks highest-rate-first. A small payment bundling many inputs can cost more than a huge one with a single input.
- **The journey:** build → **sign** each input → **broadcast** to nodes → wait in the **mempool** → a miner includes it → **confirmations** stack. More confirmations = harder to reverse = safer (~6 is the common "very safe" mark).
- **Congestion drives a fee auction.** When the mempool is full, the going rate rises and low-fee transactions stall. A stuck transaction isn't lost — bump it with **RBF**, rescue it with **CPFP**, or just wait.
</Callout>

You've now followed a payment from your screen into a block — but who actually *checks*
that your transaction is valid before relaying or mining it? Next: **nodes and full
verification**, the unsung machines that independently enforce every rule of Bitcoin so
no one has to be trusted.
