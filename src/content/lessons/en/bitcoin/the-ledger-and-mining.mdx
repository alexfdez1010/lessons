---
title: "The Ledger and Mining"
description: "How Bitcoin's blockchain grows: the mempool of pending payments, miners batching them into blocks every ~10 minutes, the block reward and fees that pay them, and the difficulty retarget that keeps the clock steady."
topic: "en/bitcoin"
order: 2
minutes: 9
updated: 2026-06-02
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, Categorize, MatchConcepts, MindMap, BlockchainChain } from '@/components/react';

In the last lesson you met Bitcoin itself — a fixed-supply money with no central issuer,
living on a public, append-only ledger. But a ledger that nobody updates is just a dusty
notebook. Bitcoin's notebook is *alive*: roughly every ten minutes a fresh page gets
added, full of new payments. Who writes that page? Who decides which payments make the
cut? And why on earth would anyone volunteer to do the work? That's the story of **mining**
— the engine that grows the chain — and by the end of this lesson you'll know exactly what
miners do, what's inside the pages they write, who pays them, and how the network keeps the
whole thing ticking at a steady pace.

## The Mempool: A Waiting Room for Payments

<MCQ
  client:visible
  pretest
  question="Guess before reading: when you send a Bitcoin payment, what happens to it in the first few seconds?"
  options={[
    { text: "It's broadcast to the network and waits, unconfirmed, in a holding area until a miner picks it up", correct: true },
    { text: "It's instantly carved into the blockchain the moment you hit send", correct: false },
    { text: "A bank approves it and forwards the money", correct: false },
    { text: "It disappears until the next day's batch run", correct: false },
  ]}
  explanation="A freshly sent transaction is broadcast across the network and lands in the mempool — a waiting room of pending, unconfirmed payments. It isn't in a block yet; it sits there until a miner selects it for the next block."
/>

When you send bitcoin, your payment doesn't teleport straight into the ledger. First it's
**broadcast** out across the network, where it lands in a kind of waiting room called the
**mempool** (short for *memory pool*). The mempool is simply the pile of transactions that
have been announced but **not yet confirmed** — they're valid and signed, but no block has
scooped them up yet.

Think of it like a post office at closing time. Letters keep dropping into the bin all day
(transactions broadcast to the network). They're real letters with real addresses, but
they haven't gone anywhere yet. Every so often a courier comes by, grabs an armful, and
sends them off together. The mempool is that bin; the courier is a **miner**; the armful
they grab becomes the next **block**.

Miners don't have to take *every* waiting transaction, and there's usually more in the bin
than fits in one armful. So they choose — and they tend to grab the ones offering the
highest **fee** first, because the fee is part of their pay (we'll dig into how fees are set
in the transactions lesson). For now, the headline is simple: **broadcast → mempool → wait
→ picked into a block.**

<Callout variant="info" title="“Unconfirmed” has a precise meaning">
A transaction sitting in the mempool is **unconfirmed** — it might still be dropped, replaced,
or simply outbid by others. It only becomes *confirmed* once a miner includes it in a block
that gets added to the chain. Until then, treating it as final money is a rookie mistake.
</Callout>

<MCQ
  client:visible
  question="A transaction is sitting in the mempool. Which statement is accurate?"
  options={[
    { text: "It's a valid, broadcast payment that no block has included yet — still unconfirmed", correct: true },
    { text: "It has already been permanently written into the blockchain", correct: false },
    { text: "It was rejected by the network for being invalid", correct: false },
    { text: "It is being secretly created by a miner on the sender's behalf", correct: false },
  ]}
  explanation="The mempool holds valid, broadcast-but-unconfirmed transactions waiting to be selected. Being in the mempool is not the same as being in a block — it's the 'pending' stage."
/>

## What Miners Actually Do

<MCQ
  client:visible
  pretest
  question="Before reading: what is a miner's core job?"
  options={[
    { text: "To assemble a candidate block of waiting transactions and compete for the right to add it to the chain", correct: true },
    { text: "To invent and sign the transactions that users send", correct: false },
    { text: "To approve loans and manage account balances", correct: false },
    { text: "To store the only copy of the blockchain", correct: false },
  ]}
  explanation="A miner gathers transactions from the mempool into a candidate block, adds a special transaction that pays themselves, and competes to be the one who appends that block. They don't create users' payments — they batch and order existing ones."
/>

Here's the single most important thing to get straight, because it's the source of endless
confusion: **mining is the act of proposing the next block.** A miner does roughly this:

1. **Scoops transactions** out of the mempool — usually the highest-fee ones — into a
   **candidate block**.
2. **Adds one special transaction** of their own, called the **coinbase transaction**, which
   pays the miner (more on this below). It's always the first transaction in the block.
3. **Competes** with every other miner on the planet for the right to be the one who appends
   this block to the chain. Whoever wins gets their block added; everyone else throws their
   candidate away and starts fresh on the next one.

That "competes" step is the heart of Bitcoin, and it's deliberately the *one piece we're
saving for next lesson*. The mechanism that decides the winner is called **proof-of-work**,
and it's a genuinely clever contest. For today, just hold the shape of it: many miners race,
exactly one wins per block, the winner writes the page and gets paid.

<Callout variant="warning" title="Myth: “Miners create the transactions”">
They don't. **Users** create and **sign** their own transactions — that signature is what
proves you own the coins you're spending. Miners only **select, order, and include**
transactions that already exist in the mempool. A miner can choose to ignore your payment,
but they can never forge it, redirect it, or pay from your wallet. The one transaction a
miner *does* author is the coinbase, which pays themselves their reward.
</Callout>

<Reveal summary="If a miner can't forge your payment, what power do they actually have?">
Real but limited power: a miner decides **which** waiting transactions go into *their*
candidate block and **what order** they appear in. They can prioritize high fees, or even
refuse to include a particular transaction. What they **cannot** do is change the amounts,
the senders, or the recipients — those are locked by the user's cryptographic signature. So
miners are gatekeepers of *inclusion and ordering*, not authors of payments.
</Reveal>

## Inside a Block

<MCQ
  client:visible
  pretest
  question="Guess: a Bitcoin block is split into two main parts. What are they?"
  options={[
    { text: "A header (compact summary data) and the list of transactions", correct: true },
    { text: "A username and a password", correct: false },
    { text: "A public copy and a private encrypted copy", correct: false },
    { text: "The sender's balance and the receiver's balance", correct: false },
  ]}
  explanation="Every block has a header — a small bundle of summary fields like the previous block's hash, a Merkle root, a timestamp, a nonce, and the difficulty target — plus the body: the actual list of transactions."
/>

A block isn't a shapeless blob of payments. It has two parts: a small **header** and the
**body** (the list of transactions). The header is the block's identity card — a compact
bundle of fields that the rest of the network reads at a glance:

| Header field | What it is |
|---|---|
| **Previous block's hash** | The fingerprint of the block before it — this is the link that forms the chain. |
| **Merkle root** | A single fingerprint that summarizes *all* the transactions in the block. |
| **Timestamp** | Roughly when the block was assembled. |
| **Nonce** | A number miners tweak while competing (its role is the proof-of-work story, next lesson). |
| **Difficulty target** | How hard the winning contest currently is (more on this below). |

The **Merkle root** deserves a moment, because it's quietly brilliant. All the transactions
in the block get hashed together, in pairs, again and again, until they collapse into one
final fingerprint — the Merkle root — which gets stamped into the header. The payoff:
**anyone can prove a specific transaction is in a block without downloading the whole
block.** You only need that one transaction plus a short trail of fingerprints leading up to
the root. For a phone wallet that can't store hundreds of gigabytes, that's the difference
between "works" and "doesn't."

<Callout variant="info" title="The header is the part that gets chained">
Notice the **previous block's hash** lives in the *header*. That's exactly the linking trick
from crypto-basics: each block points back at the one before it, so editing an old block
breaks every link after it. The header is small on purpose — it's the part the network
fingerprints and links together, while the bulky transaction list hangs off the Merkle root.
</Callout>

<Categorize
  client:visible
  question="Sort each item by where it lives: in the block header, in the transaction list (body), or whether it describes the network as a whole."
  buckets={["In the block header", "In the transaction list", "Describes the network"]}
  items={[
    { text: "The previous block's hash", bucket: "In the block header" },
    { text: "The Merkle root", bucket: "In the block header" },
    { text: "The difficulty target", bucket: "In the block header" },
    { text: "Alice's signed payment to Bob", bucket: "In the transaction list" },
    { text: "The coinbase transaction paying the miner", bucket: "In the transaction list" },
    { text: "The total hashrate of all miners combined", bucket: "Describes the network" },
  ]}
/>

Now revisit the linked-chain idea from crypto-basics with Bitcoin's blocks in mind. Tamper
with an early block below and watch the break cascade through every block built on top of it
— then **re-mine** to repair the chain. That re-mining is, in miniature, the work miners do
to propose valid blocks:

<BlockchainChain client:visible />

<MCQ
  client:visible
  question="What does the Merkle root in a block header let you do?"
  options={[
    { text: "Verify that a specific transaction is included in the block without downloading every transaction", correct: true },
    { text: "Decrypt the transactions so only you can read them", correct: false },
    { text: "Reverse a payment after it's confirmed", correct: false },
    { text: "Store the miner's password for the block", correct: false },
  ]}
  explanation="The Merkle root is one fingerprint summarizing all the block's transactions. With it, a lightweight wallet can prove a given transaction is in the block using only a short trail of hashes — no full download required."
/>

## The Block Reward: Why Anyone Bothers

<MCQ
  client:visible
  pretest
  question="Before reading: why would a miner spend real money on electricity and hardware to add a block?"
  options={[
    { text: "The miner who adds a block collects newly created bitcoin plus the fees of every transaction in it", correct: true },
    { text: "They're required to by law", correct: false },
    { text: "A bank pays them a salary", correct: false },
    { text: "They get to keep the senders' coins", correct: false },
  ]}
  explanation="The winning miner earns the block reward: a block subsidy of freshly minted bitcoin (created in the coinbase transaction) plus all the transaction fees in the block. That payout is the incentive that funds the network's security."
/>

Mining is expensive — real hardware, real electricity. Nobody does it for fun. The **block
reward** is the paycheck, and it has two pieces:

| Part of the reward | Where it comes from | Notes |
|---|---|---|
| **Block subsidy** | Brand-new bitcoin, minted in the coinbase transaction | This is the *only* way new coins enter circulation. |
| **Transaction fees** | The fees attached to every transaction the miner included | Paid by users; the more (and higher-fee) transactions, the bigger this slice. |

So the **block reward = subsidy + fees.** The subsidy is genuinely new money — it's created
out of nothing by the coinbase transaction, which is why miners always put it first in the
block. This is the answer to "where do new bitcoins come from?": not from a mint or a
treasury, but from each new block's coinbase, on a fixed, public schedule that everyone can
verify.

<Callout variant="warning" title="Myth: “New coins appear by magic / from nowhere”">
New bitcoin isn't conjured arbitrarily — it's issued **on a strict schedule** through the
coinbase transaction of each block, and only the miner who legitimately wins the block can
claim it. The amount is fixed by the network's rules, not chosen by the miner. And that
subsidy isn't forever: it **halves** at regular intervals (every 210,000 blocks, roughly
every four years), shrinking the new-coin flow over time. We'll trace the full halving
schedule and the 21-million-coin cap in the supply lesson.
</Callout>

<Callout variant="info" title="Why this *is* the security model">
The block reward isn't just a perk — it's the reason Bitcoin is hard to attack. To rewrite
history a cheater would have to out-compete every honest miner, all of whom are racing
because there's money in it. The reward ties block creation to a real, ongoing cost, which
makes the chain expensive to forge. *How* that cost is enforced is the proof-of-work story —
next lesson.
</Callout>

<FillBlank
  client:visible
  question="Fill in the blanks about the block reward."
  text="The block reward paid to the winning miner is made of two parts: the block {{subsidy|salary|deposit}}, which is newly minted bitcoin created in the {{coinbase|merkle|nonce}} transaction, plus the transaction {{fees|hashes|nodes}} of every payment included. The subsidy is the only source of {{new coins|merkle roots|timestamps}} entering circulation, and it {{halves|doubles|freezes}} on a fixed schedule."
  explanation="Block reward = subsidy + fees. The subsidy is fresh bitcoin minted via the coinbase transaction — the sole way new coins are created — and it halves roughly every four years. Fees come from the included transactions."
/>

Quick callback to lesson 1 — make sure the issuance picture lines up with the bigger story:

<MCQ
  client:visible
  question="In lesson 1 you learned Bitcoin has no central issuer and a capped supply. How does that square with miners minting new coins?"
  options={[
    { text: "New coins are minted only via the coinbase reward, on a fixed halving schedule that's coded into the rules — so issuance is automatic, not controlled by any authority", correct: true },
    { text: "It doesn't — miners can print unlimited coins whenever they like", correct: false },
    { text: "A central bank secretly approves each new coin", correct: false },
    { text: "New coins come from users' existing balances being reduced", correct: false },
  ]}
  explanation="There's no issuer because issuance is a rule, not a decision. Each block's coinbase mints a fixed, schedule-determined amount that halves over time, marching toward the 21-million cap — no person or bank chooses the amount."
/>

## Keeping the 10-Minute Clock: Difficulty

<MCQ
  client:visible
  pretest
  question="Guess: Bitcoin aims for a new block about every ten minutes. As more miners join and the network gets more powerful, what keeps that pace from speeding up?"
  options={[
    { text: "The network automatically raises the difficulty of the contest so blocks keep arriving about every ten minutes", correct: true },
    { text: "Nothing — more miners means many more blocks per minute", correct: false },
    { text: "A committee manually slows the miners down each morning", correct: false },
    { text: "Older miners are forced to switch off", correct: false },
  ]}
  explanation="Bitcoin targets ~10 minutes per block on average. Every 2016 blocks (about two weeks) it retargets the difficulty: if blocks came too fast, difficulty rises; too slow, it falls — keeping the long-run pace steady regardless of how much mining power joins or leaves."
/>

Bitcoin aims to add a new block **about every ten minutes on average.** Note *on average*:
it's a target, not a metronome. Real blocks vary a lot — some arrive a minute apart, others
forty minutes later. Over the long run, though, the average hugs ten minutes. Why ten? It's
a deliberate trade-off: long enough for blocks to spread across the global network before the
next one appears, short enough that payments confirm in a reasonable time.

But here's the puzzle. The winning contest gets *easier to win* the more total computing
power — called **hashrate** — is racing. If mountains of new miners plug in, blocks would
start popping out every five minutes, then every two. So Bitcoin has an automatic governor:
the **difficulty adjustment**, or **retarget**.

**Every 2016 blocks** — roughly every two weeks — the network looks at how long those blocks
actually took and recalibrates:

- Blocks came **faster** than ten minutes on average (lots of hashrate joined)? → **Difficulty
  rises**, making the contest harder, slowing the pace back toward ten minutes.
- Blocks came **slower** (miners left, hashrate dropped)? → **Difficulty falls**, making the
  contest easier, speeding the pace back up.

The genius is that this happens **automatically**, by rule, with no committee and no vote.
No matter how much mining power floods in or storms out, the 2016-block retarget keeps blocks
landing at roughly ten-minute intervals — the heartbeat stays steady.

<Callout variant="warning" title="Myth: “Blocks come exactly every ten minutes”">
Ten minutes is a **statistical average**, not a clock. Individual blocks scatter widely — you
might see two in three minutes, then nothing for half an hour. The difficulty retarget only
steers the *long-run average* back to ten minutes; it can't make any single block punctual.
</Callout>

<MatchConcepts
  client:visible
  question="Connect each term to its correct description."
  pairs={[
    { term: "Mempool", definition: "The waiting room of broadcast, unconfirmed transactions" },
    { term: "Coinbase transaction", definition: "The block's first transaction, which mints the subsidy and pays the miner" },
    { term: "Merkle root", definition: "One fingerprint summarizing all of a block's transactions" },
    { term: "Block subsidy", definition: "Newly created bitcoin paid to the winning miner" },
    { term: "Hashrate", definition: "The total computing power competing across the network" },
    { term: "Difficulty retarget", definition: "The every-2016-blocks recalibration keeping blocks near ten minutes" },
  ]}
/>

<MCQ
  client:visible
  allowMultiple
  question="Select EVERY statement that is true about Bitcoin's difficulty adjustment. (More than one is correct.)"
  options={[
    { text: "It happens automatically every 2016 blocks (about two weeks)", correct: true },
    { text: "Its goal is to keep the average time between blocks near ten minutes", correct: true },
    { text: "If hashrate rises, difficulty rises to slow blocks back down", correct: true },
    { text: "It is set by a vote among the largest mining companies each week", correct: false },
    { text: "It guarantees every single block arrives exactly ten minutes apart", correct: false },
  ]}
  explanation="The retarget is automatic, runs every 2016 blocks, and aims to hold the long-run average near ten minutes — raising difficulty when hashrate climbs and lowering it when hashrate falls. It's rule-driven (no vote), and it only steers the average, never individual blocks."
/>

## The Big Picture

Five moving parts, one growing ledger. Payments wait in the **mempool**; a **miner** scoops
them into a **candidate block** with a **coinbase** transaction that pays them; the block has
a compact **header** (previous hash, Merkle root, timestamp, nonce, difficulty) plus the
transaction list; the winner collects the **block reward** (subsidy + fees); and the
**difficulty retarget** every 2016 blocks keeps new blocks arriving about every ten minutes.
Chunk it into one picture:

<MindMap
  client:visible
  title="The ledger and mining"
  caption="How Bitcoin's chain grows — from the mempool of pending payments, through the miner who batches and proposes a block, to the reward that pays for it and the difficulty clock that keeps the pace steady."
  root={{
    label: 'The ledger and mining',
    children: [
      {
        label: 'Pending payments',
        children: [
          { label: 'Mempool — broadcast, unconfirmed transactions waiting' },
          { label: 'Users create & sign their own payments' },
          { label: 'Miners pick from it (high fees first)' },
        ],
      },
      {
        label: 'Mining = proposing a block',
        children: [
          { label: 'Assemble a candidate block from the mempool' },
          { label: 'Add the coinbase transaction (pays the miner)' },
          { label: 'Compete to append it — winner takes all' },
          { label: 'How the winner is chosen = proof-of-work (next)' },
        ],
      },
      {
        label: 'Inside a block',
        children: [
          { label: 'Header — prev hash, Merkle root, timestamp, nonce, target' },
          { label: 'Body — the list of transactions' },
          { label: 'Merkle root proves a tx is included without the full block' },
        ],
      },
      {
        label: 'Reward & pace',
        children: [
          { label: 'Block reward = subsidy + fees' },
          { label: 'Subsidy = new coins, halves every 210,000 blocks' },
          { label: 'Difficulty retargets every 2016 blocks (~2 weeks)' },
          { label: 'Keeps blocks near ten minutes on average' },
        ],
      },
    ],
  }}
/>

A mixed recap — it pulls from everything above:

<Quiz
  client:visible
  questions={[
    {
      question: "What is the mempool?",
      options: [
        { text: "The holding area of broadcast but unconfirmed transactions waiting to be put in a block", correct: true },
        { text: "The folder where confirmed blocks are permanently stored", correct: false },
        { text: "A miner's private wallet", correct: false },
        { text: "The list of all banks on the network", correct: false },
      ],
      explanation: "The mempool is the waiting room for valid, broadcast transactions that no block has included yet. Miners select from it — usually the highest-fee ones — to build the next block.",
    },
    {
      question: "Which best describes what mining is?",
      options: [
        { text: "Assembling a candidate block of mempool transactions and competing to be the one who appends it", correct: true },
        { text: "Creating and signing the payments that users send", correct: false },
        { text: "Solving useful scientific problems for universities", correct: false },
        { text: "Approving each transaction the way a bank teller would", correct: false },
      ],
      explanation: "Mining means proposing the next block: batching existing transactions, adding a coinbase to pay yourself, and competing for the right to add the block. Miners don't author users' payments.",
    },
    {
      question: "The block reward earned by the winning miner consists of:",
      options: [
        { text: "The block subsidy (newly minted bitcoin) plus the transaction fees of every included transaction", correct: true },
        { text: "A fixed salary paid by a central authority", correct: false },
        { text: "The combined balances of every sender in the block", correct: false },
        { text: "Only the fees, with no new coins ever created", correct: false },
      ],
      explanation: "Block reward = subsidy + fees. The subsidy is fresh bitcoin minted in the coinbase transaction — the only way new coins enter circulation — and the fees come from the included transactions.",
    },
    {
      question: "Where new bitcoins come from is best described as:",
      options: [
        { text: "The coinbase transaction of each block mints them on a fixed, halving schedule", correct: true },
        { text: "A central bank prints them when demand rises", correct: false },
        { text: "Miners take them from users' existing balances", correct: false },
        { text: "They are created at random with no rule", correct: false },
      ],
      explanation: "New coins are issued only through each block's coinbase subsidy, on a schedule coded into the rules that halves roughly every four years — no issuer chooses the amount.",
    },
    {
      question: "Why does the network adjust mining difficulty every 2016 blocks?",
      options: [
        { text: "To keep the average time between blocks near ten minutes as hashrate rises or falls", correct: true },
        { text: "To let the biggest miners vote on the coin supply", correct: false },
        { text: "To make sure every block arrives exactly ten minutes apart", correct: false },
        { text: "To encrypt the transactions in each block", correct: false },
      ],
      explanation: "The retarget automatically raises difficulty when blocks come too fast and lowers it when they come too slowly, steering the long-run average back to ten minutes regardless of how much mining power joins or leaves.",
    },
    {
      question: "Which statement is TRUE about a Bitcoin block header?",
      options: [
        { text: "It stores the previous block's hash and a Merkle root summarizing all the block's transactions", correct: true },
        { text: "It contains every transaction's full details, with no summary", correct: false },
        { text: "It hides the transactions so only the miner can read them", correct: false },
        { text: "It is created and signed by the users who sent the payments", correct: false },
      ],
      explanation: "The header is a compact summary: previous hash (the chain link), Merkle root (one fingerprint for all transactions), timestamp, nonce, and difficulty target. The full transactions live in the body, not the header.",
    },
  ]}
/>

## Key Takeaways

<Callout variant="success" title="What to remember">
- **The mempool is the waiting room.** Broadcast transactions sit there *unconfirmed* until a miner selects them — usually highest-fee first — for the next block. In the mempool ≠ in a block.
- **Mining means proposing the next block.** A miner batches mempool transactions into a candidate block, adds a coinbase transaction that pays themselves, and competes to append it. The winner writes the page; everyone else starts over.
- **Users author payments, not miners.** You create and sign your own transactions; miners only *select and order* them. They can ignore a payment but never forge, redirect, or alter one.
- **A block = header + transaction list.** The header holds the previous block's hash (the chain link), a Merkle root (one fingerprint for all transactions), a timestamp, a nonce, and the difficulty target. The Merkle root lets anyone prove a transaction is in a block without downloading the whole thing.
- **Block reward = subsidy + fees.** The subsidy is newly minted bitcoin from the coinbase — the only source of new coins — and it halves on a fixed schedule (every 210,000 blocks). Fees come from the included transactions. This reward is what pays for security.
- **Difficulty retargets every 2016 blocks (~2 weeks)** so blocks keep arriving about every ten minutes *on average*, no matter how much hashrate joins or leaves. More miners → difficulty rises; fewer → it falls.
- Myths busted: miners don't create transactions, mining isn't useful science, blocks aren't punctual (it's an average), and new coins aren't magic — they're scheduled coinbase issuance that halves over time.
</Callout>

You now know *who* writes each page, *what's* on it, and *why* they bother. But we kept
dodging the most important word: **compete.** How exactly does one miner *win* the right to
add a block? That contest is **proof-of-work** — a deliberately hard guessing game that ties
the whole system to real-world cost — and it's exactly where we head next.
