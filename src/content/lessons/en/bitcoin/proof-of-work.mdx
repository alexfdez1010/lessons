---
title: "Proof-of-Work: How the Winner Is Chosen"
description: "The mining lottery explained: grinding nonces to find a hash below target, why work is hard to do but easy to verify, the longest-chain rule, hashrate, and what a 51% attack can and cannot do."
topic: "en/bitcoin"
order: 3
minutes: 9
updated: 2026-06-02
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, Categorize, MatchConcepts, MindMap, ProofOfWork } from '@/components/react';

Last lesson left a juicy cliffhanger. We said miners batch transactions into blocks,
earn a block reward, and that difficulty quietly adjusts so a new block lands roughly
every ten minutes — but we hand-waved the most important part: **how does the network
actually pick who gets to add the next block?** No boss hands out turns. No vote is
taken. Instead, Bitcoin runs a giant, planet-scale **lottery** — and the price of a
ticket is electricity. The mechanism is called **proof-of-work**, and by the end of this
lesson you'll know exactly how it crowns a winner, why that winner can't cheat, and why
the scariest-sounding attack on Bitcoin can do far less than the headlines claim.

## The Guessing Game

<MCQ
  client:visible
  pretest
  question="Guess before reading: to 'win' the right to add the next block, what does a miner actually do?"
  options={[
    { text: "Repeatedly tweak a number and re-hash the block header until the resulting hash falls below a target value", correct: true },
    { text: "Solve a useful math problem, like factoring large primes or folding proteins", correct: false },
    { text: "Pay a fee to the Bitcoin Foundation for a turn", correct: false },
    { text: "Win a vote among all the other miners", correct: false },
  ]}
  explanation="Mining is brute-force guessing: change a number called the nonce, re-hash the header, and check whether the hash is below the target. There's no shortcut and no useful side-product — just guess, hash, check, repeat, billions of times."
/>

Recall from crypto-basics that a **hash** is a fixed-size fingerprint of data, and that it
has the **avalanche effect** — change the input by one bit and the entire fingerprint
scrambles unpredictably. Bitcoin leans hard on that. To add a block, a miner assembles a
**block header** (which includes the previous block's hash, a summary of the block's
transactions, a timestamp, and a number called the **nonce**) and runs it through a hash
function. Bitcoin specifically uses **double SHA-256** — it hashes, then hashes the result
again.

Here's the game. The network sets a **target** — a big number. A miner wins only if the
header's hash comes out **below that target.** Equivalently, the hash has to **start with
a certain number of leading zeros.** Because hashing is one-way and avalanche-y, there is
no clever way to design a header that hashes low. The miner's only move is to change the
**nonce**, re-hash, look at the result, and if it's too big, change the nonce again. And
again. Trillions of times a second, across the whole network.

That repeated tweak-and-rehash is **proof-of-work**: the "work" is the staggering pile of
failed guesses you had to burn through before stumbling onto a winning one.

Play with it. Hit mine and watch the miner grind through nonces until the fingerprint
finally lands below the target. Then drag the difficulty slider up and watch the expected
number of attempts explode:

<ProofOfWork
  client:visible
  caption="Each attempt is one guess: a new nonce, a fresh fingerprint, a check against the target. Crank the difficulty and the work needed multiplies — fast."
/>

<Callout variant="warning" title="Myth: “miners solve useful math problems”">
A stubborn legend says mining computes something valuable — protein folding, prime
numbers, curing cancer. It does not. The "problem" is to find a nonce whose header hashes
below the target, and the answer is useful for *exactly one thing*: proving you spent the
work. It's deliberately arbitrary. The point isn't the answer; it's that finding it was
provably expensive.
</Callout>

<FillBlank
  client:visible
  question="Fill in the core mechanics of a single mining attempt."
  text="The miner changes a number called the {{nonce|target|ledger}}, re-runs the block header through {{double SHA-256|a vote|encryption}}, and checks whether the result is {{below the target|the longest|a prime}}. If not, it changes the number and tries again."
  explanation="One attempt = pick a nonce, double-SHA-256 the header, compare to the target. A hash below the target wins; otherwise tweak the nonce and repeat. No shortcuts, just volume."
/>

## A Lottery Weighted by Hashrate

<MCQ
  client:visible
  pretest
  question="Two miners are racing for the next block. Miner A can compute twice as many hashes per second as Miner B. Who finds the next block?"
  options={[
    { text: "Probably A, but not certainly — A just has roughly twice the chance per unit time; either could win any given block", correct: true },
    { text: "A always, with certainty — the faster machine always wins", correct: false },
    { text: "Whoever started hashing first", correct: false },
    { text: "Neither — they take turns by agreement", correct: false },
  ]}
  explanation="It's probabilistic, not a deterministic race. Each hash is an independent lottery ticket, so more hashes means more tickets and a proportionally higher chance — but any single block can go to the underdog. Speed buys odds, not certainty."
/>

Because every guess is independent and equally (un)likely to win, mining is best pictured
as a **lottery**. Each hash you compute is one lottery ticket. The more hashes per second
you can churn out, the more tickets you hold, and the higher your odds of finding the next
block. The total guessing speed of the whole network is called the **hashrate** — hashes
per second — and your probability of mining the next block is roughly **your share of the
total hashrate.**

Control 10% of the network's hashrate? You'll find roughly 10% of blocks over time.
Control 0.001%? You'll win occasionally, on a timescale that may stretch into years. The
network's collective difficulty is tuned so that, summed across everyone, a winning ticket
turns up about once every **ten minutes** — that's the retargeting we met last lesson,
quietly nudging the target up or down to hold that pace as hashrate rises and falls.

<Callout variant="info" title="Independent tickets, no “due” numbers">
A miner who has hashed for an hour with no luck is **no more likely** to win on the next
guess than a miner who just switched on. Past failures don't build up credit — each hash
is a fresh, independent draw. There's no such thing as being "due" for a block.
</Callout>

<Reveal summary="If it's random, why do big mining pools win so consistently?">
Because the law of large numbers smooths randomness out. A single hobbyist with a tiny
share sees wild, lumpy luck — long droughts, occasional jackpots. A pool combining
millions of machines holds a huge, steady share of the tickets, so its results hug the
average closely: it wins close to its hashrate share, block after block. The dice are
still fair; there are just enough rolls to make the odds visible.
</Reveal>

## Hard to Do, Easy to Check

<MCQ
  client:visible
  pretest
  question="A miner announces a new block. How much work does everyone else do to confirm the block's proof-of-work is valid?"
  options={[
    { text: "Almost none — they hash the header once and check that the result is below the target", correct: true },
    { text: "The same enormous amount of work the miner did to find it", correct: false },
    { text: "Roughly half the work, by re-guessing some of the nonces", correct: false },
    { text: "They have to trust the miner; there's no way to check", correct: false },
  ]}
  explanation="This asymmetry is the whole point. Finding a valid nonce takes trillions of guesses; verifying one takes a single hash. Anyone can instantly confirm the block is legitimate without redoing the miner's labor."
/>

Here is the elegant asymmetry at the heart of proof-of-work: it is **astronomically hard
to produce, but trivial to verify.** Finding a nonce that makes the header hash below the
target might take the whole network trillions upon trillions of attempts. But once a miner
announces, *"here's my block, and the winning nonce is 487,221,003,"* every other node can
check it by hashing the header **just once** and confirming the result really is below the
target. One hash. Done.

The everyday picture: a fiendish jigsaw puzzle. Assembling it takes hours of grinding; but
glancing at a finished puzzle to confirm it's solved takes a second. Proof-of-work lets a
miner shout "I did the work!" and lets everyone else verify that claim almost for free —
without trusting the miner one bit.

<Categorize
  client:visible
  question="Sort each statement by whether it describes producing a valid block or verifying one."
  buckets={["Producing (mining)", "Verifying (any node)"]}
  items={[
    { text: "Try trillions of nonces until one hashes below target", bucket: "Producing (mining)" },
    { text: "Hash the announced header a single time", bucket: "Verifying (any node)" },
    { text: "Burn enormous amounts of electricity", bucket: "Producing (mining)" },
    { text: "Confirm the result is below the target", bucket: "Verifying (any node)" },
    { text: "Takes about ten minutes for the whole network", bucket: "Producing (mining)" },
    { text: "Takes a fraction of a millisecond per node", bucket: "Verifying (any node)" },
  ]}
/>

## Why Work = Security

<MCQ
  client:visible
  pretest
  question="Why does spending real work make old transactions hard to reverse?"
  options={[
    { text: "Rewriting an old block means redoing its proof-of-work plus every block after it, faster than the honest network is extending the real chain", correct: true },
    { text: "Old blocks are encrypted with a key only the network owns", correct: false },
    { text: "A central server backs up the chain and restores it if tampered", correct: false },
    { text: "Once mined, a block is physically destroyed so it can't be edited", correct: false },
  ]}
  explanation="To undo a buried transaction you'd have to re-mine its block and re-mine every block stacked on top of it — and do it faster than the honest network keeps adding new blocks. The deeper the block, the more cumulative work that takes."
/>

In crypto-basics we saw that editing one old block breaks every block after it because
each stores the previous block's hash. Proof-of-work supplies the missing ingredient: it
makes re-creating those broken blocks **ruinously expensive.** Each block isn't just
linked to the last — it's *sealed with work.* Rewriting an old block means redoing its
proof-of-work **and** redoing the proof-of-work of every block built on top of it, **and**
doing all of that faster than the honest network is busy extending the genuine chain.

This is why we talk about **confirmations.** When your transaction is in the latest block,
it has one confirmation; each new block stacked on top adds another. The deeper your
transaction is buried, the more cumulative work an attacker would have to out-pace to undo
it. A transaction six blocks deep would require re-mining six blocks faster than thousands
of honest machines mine new ones — practically, it's not happening. That's what people
mean when they call the chain **immutable**: not magically un-editable, but protected by a
mountain of accumulated work that grows higher every ten minutes.

<Callout variant="info" title="“Longest” really means “most work”">
You'll hear "longest chain," but the precise rule is **most cumulative proof-of-work.**
Length is just a convenient stand-in, since more blocks usually means more total work
poured in. When honest nodes choose between competing histories, they pick the one with
the heaviest pile of work behind it — that's the chain everyone agrees is real.
</Callout>

## Forks and the Longest Chain

<MCQ
  client:visible
  pretest
  question="Two miners on opposite sides of the world find a valid block at almost the same moment. What happens?"
  options={[
    { text: "The chain temporarily forks into two branches; whichever branch gets the next block (more work) wins, and the losing block's transactions go back to the mempool", correct: true },
    { text: "Both blocks are permanently added side by side", correct: false },
    { text: "Bitcoin crashes and the network restarts", correct: false },
    { text: "A referee node decides which one to keep", correct: false },
  ]}
  explanation="A brief fork is normal. Nodes follow whichever branch first gets extended by the next block, because that branch now carries more cumulative work. The orphaned block is discarded and its transactions return to the mempool to be mined again."
/>

Sometimes two miners find a valid block at nearly the same instant, before the news of
either has spread across the globe. Now the network briefly disagrees: some nodes heard
about block X first, others heard about block Y. The chain has temporarily **forked** into
two competing branches of equal length.

This isn't a crisis — it's expected, and the rule resolves it automatically. Nodes keep
mining on whichever branch they saw first, but the tie breaks the moment **the next block**
lands on one of the branches. That branch now has more cumulative work, so every honest
node switches to it. The losing block becomes an **orphan** (or "stale") block: it's
discarded, and any transactions it contained that aren't already in the winning chain drop
back into the **mempool** to be picked up by a future block. No transactions are lost; the
network simply converges on the heaviest chain.

<MatchConcepts
  client:visible
  question="Match each proof-of-work term to its meaning."
  pairs={[
    { term: "Nonce", definition: "The number a miner tweaks and re-hashes while searching" },
    { term: "Target", definition: "The value a header's hash must fall below to win" },
    { term: "Hashrate", definition: "How many hashes per second a miner or the network computes" },
    { term: "Confirmation", definition: "Each additional block stacked on top of yours" },
    { term: "Orphan block", definition: "A valid block discarded because a heavier branch won" },
  ]}
/>

## Target ↔ Difficulty

<MCQ
  client:visible
  pretest
  question="The network adjusts the target so that requiring one MORE leading zero in the winning hash makes mining...?"
  options={[
    { text: "Roughly an order of magnitude harder — each extra leading-zero requirement multiplies the expected work, exponentially", correct: true },
    { text: "A little bit harder, in a small linear step", correct: false },
    { text: "Easier, since zeros are common", correct: false },
    { text: "Exactly twice as hard, every time, forever", correct: false },
  ]}
  explanation="Difficulty scales exponentially. Each extra required leading zero shrinks the pool of winning hashes by a big factor, so the expected number of guesses roughly multiplies. Small-looking changes to the target mean huge changes in work."
/>

**Target** and **difficulty** are two views of the same dial. A **lower target** means
fewer hashes qualify as winners (you need more leading zeros), which makes mining
**harder**. A **higher target** loosens the bar and makes it **easier**. "Difficulty" is
just the human-friendly number that goes *up* as the target goes *down*.

The crucial, often-missed point is that this scaling is **exponential**, not linear. Each
additional leading zero you demand shrinks the set of acceptable hashes by a large factor,
so the expected work to find a winner roughly multiplies with every step. That's why
difficulty can climb to mind-bending heights: tiny-looking tightenings of the target
translate into colossal jumps in the number of guesses required. The slider on the miner
island above shows exactly this — bump the difficulty by one and watch the expected
attempts leap, not creep.

<Categorize
  client:visible
  question="Sort each change by whether it makes mining a block harder or easier."
  buckets={["Makes it harder", "Makes it easier"]}
  items={[
    { text: "Lowering the target", bucket: "Makes it harder" },
    { text: "Requiring more leading zeros", bucket: "Makes it harder" },
    { text: "Raising the difficulty number", bucket: "Makes it harder" },
    { text: "Raising the target", bucket: "Makes it easier" },
    { text: "Requiring fewer leading zeros", bucket: "Makes it easier" },
  ]}
/>

<Reveal summary="Spaced recall: so how does Bitcoin keep blocks landing every ~10 minutes?">
From last lesson: roughly every two weeks, the network looks at how fast the recent blocks
actually arrived and **retargets** — if blocks came too fast (hashrate rose), it lowers the
target to make mining harder; if too slow (hashrate fell), it raises the target. That
self-correcting feedback loop is what holds the average block time near ten minutes even as
the global hashrate swings wildly. The difficulty chases the hashrate; the ten-minute
heartbeat stays steady.
</Reveal>

## The 51% Attack (and Its Limits)

<MCQ
  client:visible
  pretest
  question="An attacker somehow controls a MAJORITY of the network's hashrate. What can they actually do?"
  options={[
    { text: "Out-mine the honest chain to reorder, censor, or double-spend their OWN recent coins — but not steal from others", correct: true },
    { text: "Drain any wallet on the network by forging signatures", correct: false },
    { text: "Print extra bitcoins beyond the supply schedule", correct: false },
    { text: "Instantly reverse a years-old, deeply buried transaction for free", correct: false },
  ]}
  explanation="A 51% attacker can out-mine everyone to rewrite RECENT history: censor transactions and double-spend coins they themselves control. They cannot forge signatures, steal arbitrary coins, mint new supply, or cheaply undo deeply buried blocks."
/>

The scariest phrase in Bitcoin is the **51% attack** — and it's worth being precise,
because the reality is both real and far narrower than the panic implies. If an attacker
controls a **majority of the hashrate**, they can build blocks faster than everyone else
combined. With that power they could **reorder or exclude (censor) transactions** and pull
off a **double-spend of their own recent coins**: pay you in a transaction, let it confirm,
then secretly mine a longer competing chain in which that payment never happened, and
publish it to override the chain you saw. The coins they sent you effectively snap back to
them.

That's genuinely dangerous. But look hard at what it does **not** allow.

| A 51% attacker CAN | A 51% attacker CANNOT |
| --- | --- |
| Double-spend their **own** recent coins | Steal coins from arbitrary addresses |
| Censor or delay transactions | Forge anyone else's signature |
| Reorder very recent blocks | Create coins beyond the supply schedule |
| Reverse a few shallow confirmations | Cheaply reverse old, deeply buried blocks |

The line everything hinges on: **the attacker has no one's private keys.** Moving coins
requires a valid **signature** from the owner's key, and proof-of-work does nothing to
break that. So an attacker can rearrange *which valid transactions* appear and in *what
order* — they cannot conjure a transaction that spends *your* coins, because they can't
sign for you. Nor can they mint extra supply or rewrite a transaction buried under
thousands of blocks, because out-pacing all that accumulated honest work is astronomically
expensive.

<Callout variant="warning" title="Myth: “a 51% attacker can drain anyone's wallet”">
**False.** Hashrate buys you control over block *ordering*, not over *ownership*. Spending
coins still demands the owner's private-key signature, which an attacker doesn't have. The
worst they can do is unwind their *own* recent payments — not reach into your wallet.
</Callout>

And there's a poetic catch: a successful 51% attack is **self-destructive.** Amassing a
majority of hashrate costs a fortune in hardware and power, and the moment the world sees
the chain being rewritten, confidence — and the price — would crater. The attacker would
be torching the value of the very asset they spent a fortune attacking. The economics, as
much as the cryptography, are part of the defense.

<MCQ
  client:visible
  allowMultiple
  question="Select EVERY action a 51% attacker could realistically pull off. (More than one is correct.)"
  options={[
    { text: "Double-spend coins they themselves recently sent", correct: true },
    { text: "Censor specific transactions by refusing to include them", correct: true },
    { text: "Reorder or reverse the most recent few blocks", correct: true },
    { text: "Forge another user's signature to spend that user's coins", correct: false },
    { text: "Create brand-new bitcoins outside the issuance schedule", correct: false },
    { text: "Cheaply reverse a transaction buried thousands of blocks deep", correct: false },
  ]}
  explanation="A majority of hashrate grants power over the ORDERING of valid transactions — double-spending one's own coins, censorship, and shallow reorgs. It grants zero power over ownership: no forged signatures, no extra supply, and no cheap rewriting of deeply buried history."
/>

## The Big Picture

Proof-of-work is one machine with a handful of interlocking gears: a guessing game, a
weighted lottery, a verify-cheaply asymmetry, and a heaviest-chain rule that together turn
spent electricity into tamper-resistance. Here's the whole thing in one frame:

<MindMap
  client:visible
  title="Proof-of-work at a glance"
  caption="From the nonce-grinding lottery, through the easy-to-verify asymmetry and the heaviest-chain rule, to what a 51% attacker can and can't touch."
  root={{
    label: 'Proof-of-work',
    children: [
      {
        label: 'The guessing game',
        children: [
          { label: 'Tweak the nonce, double-SHA-256 the header' },
          { label: 'Win if the hash is below the target' },
          { label: 'No shortcut — just guess, hash, check, repeat' },
        ],
      },
      {
        label: 'A weighted lottery',
        children: [
          { label: 'Each hash is one independent ticket' },
          { label: 'Win odds ≈ your share of the hashrate' },
          { label: 'Whole network: a block about every 10 minutes' },
        ],
      },
      {
        label: 'Why it secures the chain',
        children: [
          { label: 'Hard to produce, trivial to verify (one hash)' },
          { label: 'Rewriting old blocks means redoing all the work since' },
          { label: 'Heaviest-chain (most-work) rule resolves forks' },
        ],
      },
      {
        label: '51% attack — limits',
        children: [
          { label: 'CAN: double-spend own coins, censor, shallow reorg' },
          { label: 'CANNOT: forge signatures, mint supply, undo old blocks' },
          { label: 'Expensive and self-destructive to attempt' },
        ],
      },
    ],
  }}
/>

A mixed recap pulling from the whole lesson:

<Quiz
  client:visible
  questions={[
    {
      question: "What is a miner physically doing when mining a block?",
      options: [
        { text: "Repeatedly changing the nonce and re-hashing the header until the hash is below the target", correct: true },
        { text: "Solving a useful scientific problem the network needs answered", correct: false },
        { text: "Voting with other miners on the next block", correct: false },
        { text: "Decrypting the previous block to read its contents", correct: false },
      ],
      explanation: "Mining is brute-force guessing: tweak the nonce, double-SHA-256 the header, check against the target, repeat. The 'work' is the mountain of failed guesses behind a single winning one.",
    },
    {
      question: "Your chance of mining the next block is roughly equal to...",
      options: [
        { text: "Your share of the network's total hashrate", correct: true },
        { text: "How long you've been mining without a win", correct: false },
        { text: "How early you joined the network", correct: false },
        { text: "The size of the transactions you choose to include", correct: false },
      ],
      explanation: "Each hash is an independent ticket, so your odds track your fraction of total hashrate. Past dry spells don't make you 'due' — every guess is a fresh, independent draw.",
    },
    {
      question: "Why is proof-of-work described as asymmetric?",
      options: [
        { text: "Finding a valid nonce takes enormous work, but verifying one takes a single hash", correct: true },
        { text: "Mining is easy but verifying is the hard part", correct: false },
        { text: "Only the miner who found a block can ever check it", correct: false },
        { text: "Both producing and verifying take the same effort", correct: false },
      ],
      explanation: "Production is astronomically hard; verification is one hash. Any node can instantly confirm a block is valid without redoing the miner's labor — and without trusting them.",
    },
    {
      question: "Two valid blocks appear at the same time, forking the chain. How is it resolved?",
      options: [
        { text: "The branch that receives the next block has more cumulative work and wins; the orphan's transactions return to the mempool", correct: true },
        { text: "Both blocks stay in the chain permanently", correct: false },
        { text: "A central authority chooses the winner", correct: false },
        { text: "The fork is permanent and splits Bitcoin in two", correct: false },
      ],
      explanation: "Nodes follow the heaviest (most-work) chain. The next block breaks the tie; the losing block becomes an orphan and its un-included transactions drop back to the mempool.",
    },
    {
      question: "Requiring one additional leading zero in the winning hash makes mining...",
      options: [
        { text: "Dramatically harder — the expected work multiplies (exponential difficulty)", correct: true },
        { text: "Only slightly harder, in a small linear step", correct: false },
        { text: "Easier, because zeros are common", correct: false },
        { text: "Unchanged — leading zeros don't affect difficulty", correct: false },
      ],
      explanation: "Difficulty scales exponentially. Each extra required leading zero shrinks the pool of winning hashes by a big factor, so the expected number of guesses roughly multiplies.",
    },
    {
      question: "Which is something a 51% attacker genuinely CANNOT do?",
      options: [
        { text: "Forge another person's signature to steal their coins", correct: true },
        { text: "Double-spend coins they recently sent", correct: false },
        { text: "Censor transactions by excluding them from blocks", correct: false },
        { text: "Reorder the most recent few blocks", correct: false },
      ],
      explanation: "Majority hashrate controls transaction ORDERING, not ownership. Spending coins needs the owner's private-key signature, which the attacker doesn't have — so they can rearrange valid transactions but can't steal from arbitrary wallets.",
    },
  ]}
/>

## Key Takeaways

<Callout variant="success" title="What to remember">
- **Mining is a guessing game.** A miner tweaks the **nonce** in the block header and re-runs it through **double SHA-256**, hunting for a hash **below the target** (enough leading zeros). There's no shortcut — just guess, hash, check, repeat, trillions of times.
- **It's a lottery weighted by hashrate.** Each hash is one independent ticket; your odds of the next block ≈ your share of total **hashrate**. The network is tuned so a winning ticket appears about every **ten minutes**.
- **Hard to produce, trivial to verify.** Finding a valid nonce is astronomically expensive; checking it is a single hash. That asymmetry lets anyone confirm a block without trusting the miner.
- **Work = security.** Rewriting an old block means redoing its proof-of-work and every block after it, faster than the honest network adds new ones. More confirmations = more buried work = effectively immutable.
- **Heaviest-chain rule.** Honest nodes follow the chain with the **most cumulative work**. Simultaneous blocks cause a temporary fork; the next block decides it, and the orphan's transactions return to the mempool.
- **A 51% attacker** can double-spend their **own** recent coins, censor, and reorder recent blocks — but **cannot** forge signatures, steal arbitrary coins, mint extra supply, or cheaply undo old transactions. And it's expensive and self-destructive.
- **The energy is the point.** Proof-of-work deliberately burns real electricity. Critics call it wasteful; proponents argue the cost *is* the security — the dollar value of the work is exactly what makes rewriting history uneconomic. A fair trade-off to weigh, not a settled verdict.
</Callout>

Proof-of-work answers *who adds the next block and why we trust it.* But it also quietly
hands the winner freshly minted coins — and that raises the next question: where do new
bitcoins come from, and why will there only ever be 21 million of them? Next up: **the
21-million cap and halvings.**
