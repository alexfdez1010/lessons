---
title: "Optimal Execution & the Almgren–Chriss Frontier"
description: "Trade market impact against timing risk: how the Almgren–Chriss efficient frontier, your risk aversion, and a decaying alpha signal jointly set the optimal speed to execute a large order."
topic: "en/algorithmic-trading-and-execution"
order: 5
minutes: 13
updated: 2026-06-13
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, MatchConcepts, MindMap, ExecutionFrontier } from '@/components/react';

You have ten million dollars of stock to sell. Slam it into the market in one second and you'll move the price against yourself — a brutal, certain cost. Dribble it out over a week and you'll barely nudge the tape — but for a whole week the price is free to wander off a cliff before you finish. There is **no order size at which selling is both cheap and certain.** Optimal execution is the art of picking *where* on that brutal trade-off you want to stand. This lesson hands you the map practitioners actually use: the Almgren–Chriss efficient frontier.

<MCQ
  client:visible
  pretest
  question="You must liquidate a large position. As you stretch the execution out over a longer horizon, what happens to your two costs?"
  options={[
    { text: "Expected impact cost falls, but timing risk (variance of cost) rises", correct: true },
    { text: "Both expected cost and timing risk fall — slower is strictly better" },
    { text: "Expected cost rises and timing risk falls" },
    { text: "Both stay constant; only the average price you get changes" }
  ]}
  explanation="Trading slower means smaller slices, so you push the price less — expected impact cost drops. But you're exposed to the market's random drift for longer, so the variance of your final cost grows. That tension is the entire subject of this lesson."
/>

## The two costs you are trading off

**The analogy.** Crossing a fast river, you choose a speed. **Sprint** across and you make a huge *splash* — you spend energy fighting the water (that's market impact), but you're only exposed to the current for a heartbeat. **Wade slowly** and you barely ripple the surface (tiny impact), but now the current has minutes to shove you downstream to who-knows-where (that's timing risk). Same river, two completely different ways to get wet.

**The precise version.** Splitting a parent order into child slices over a horizon $T$ produces two distinct costs:

- **Expected cost — market impact.** Each slice you trade consumes liquidity and walks the price away from you. Trade in bigger, faster chunks and the impact per share grows. So the *expected* (average) cost of execution **falls as you slow down**: smaller slices, gentler footprint.
- **Timing risk — the variance of cost.** While you still hold un-executed shares, the price drifts randomly with volatility $\sigma$. The longer the horizon, the more the price can wander before you're done, so the **variance of your realized cost rises as you slow down.** Over a horizon $T$ the standard deviation of cost scales roughly with $\sigma\sqrt{T}$ on the remaining inventory.

The headline: expected cost and timing risk pull in **opposite directions** as you change speed. You cannot minimize both at once.

<Callout variant="info" title="Impact vs. risk, in one line">
**Impact** is what *you* do to the price by trading. **Timing risk** is what the *market* does to the price while you wait. Faster trading trades more of the second for more of the first.
</Callout>

### When to lean which way

If volatility is low and the spread/depth is thin (impact-dominated, calm tape), patience pays — slow down. If volatility is high and liquidity is deep (risk-dominated, jumpy tape), get it done — speed up. The whole point of a framework is to make that lean quantitative instead of a gut call.

<FillBlank
  client:visible
  question="Complete the trade-off."
  text="As you execute MORE slowly, expected market-impact cost {{falls|rises|stays flat}}, while the timing risk — the {{variance|skew|mean}} of your execution cost — {{rises|falls|disappears}}."
  explanation="Slower = smaller slices = less impact (expected cost falls), but longer exposure to random price drift = more variance of cost (timing risk rises)."
/>

## The Almgren–Chriss efficient frontier

**The analogy.** Think of every possible execution schedule as a different restaurant on a map, plotted by *price* (expected cost) and *unpredictability* (risk). Lots of restaurants are simply bad — overpriced *and* unreliable. Throw those out and you're left with the **efficient frontier**: the menu of options where you can only get cheaper by accepting more uncertainty. Almgren and Chriss (2000) showed that for large-order execution this frontier is a clean, well-behaved curve.

**The precise version.** Each feasible trading speed maps to one $(\,\text{expected cost},\ \text{risk}\,)$ pair. The non-dominated pairs form the frontier. A trader collapses that whole curve to a *single* chosen point by minimizing a **mean–variance objective**:

$$\min_{\text{schedule}}\; E[\text{cost}] + \lambda\,\mathrm{Var}[\text{cost}]$$

where $\lambda \ge 0$ is the **risk-aversion** coefficient — your personal price on uncertainty.

- **High $\lambda$ (risk-averse)** → variance is expensive → trade **FAST**. You land at the **top-left** of the frontier: high expected cost, low risk.
- **Low $\lambda$ (risk-tolerant)** → you barely penalize variance → trade **SLOW**. You land at the **bottom-right**: low expected cost, high risk.
- **$\lambda = 0$ (risk-neutral)** → minimize expected cost only → trade **as slowly as the horizon allows**, since impact is all that's left.
- **$\lambda \to \infty$ (infinitely risk-averse)** → variance dominates everything → **trade immediately**, accepting maximal impact to kill all timing risk.

Crucially, the optimal *trajectory* is **not a flat (linear) liquidation**. For positive $\lambda$ it's a smooth, roughly **exponentially decaying** schedule — sell faster early (when you have the most inventory exposed to risk), then taper. The decay rate is governed by $\lambda$, $\sigma$, and the impact coefficients.

<ExecutionFrontier client:visible caption="Drag the risk-aversion slider: high λ snaps the chosen point to the fast, low-risk corner; low λ slides it to the slow, low-cost corner. Every point on the curve is 'optimal' for *some* λ." />

<Callout variant="warning" title="A frontier is a menu, not an answer">
Almgren–Chriss does **not** tell you the single 'right' speed — that would require knowing your λ. It tells you which speeds are *defensible* (on the frontier) versus dumb (inside it). Picking the point is a preference, not a calculation.
</Callout>

<MatchConcepts
  client:visible
  question="Match each risk-aversion regime to its place on the frontier."
  pairs={[
    { term: "λ = 0 (risk-neutral)", definition: "Minimize expected impact only → trade as slowly as the horizon permits (bottom-right)" },
    { term: "Low but positive λ", definition: "Mostly patient, slightly front-loaded exponential schedule (lower-right of the curve)" },
    { term: "High λ (risk-averse)", definition: "Variance is costly → trade fast, accept high impact (top-left)" },
    { term: "λ → ∞", definition: "Trade the whole order immediately to eliminate timing risk entirely" }
  ]}
  explanation="The single objective E[cost] + λ·Var[cost] sweeps the whole frontier as λ runs from 0 to infinity. Larger λ always pushes you toward faster, costlier-but-safer execution."
/>

## A worked trade-off

Suppose you must liquidate the same block and you've estimated, for three candidate horizons, the **expected impact cost** (in basis points) and the **timing-risk standard deviation** (also in bps). Slower horizons cut impact but balloon risk:

| Horizon | Expected cost $E$ (bps) | Timing-risk std $\sigma_c$ (bps) | Variance $\mathrm{Var} = \sigma_c^{\,2}$ (bps²) |
|---|---|---|---|
| 1 hour  | 24 | 6  | 36   |
| 1 day   | 12 | 18 | 324  |
| 1 week  | 6  | 40 | 1600 |

Now score each with the mean–variance objective $E + \lambda\,\mathrm{Var}$ for two different traders. To keep units sane we measure variance in bps² and use a small $\lambda$ (in 1/bps²).

**Risk-tolerant trader, $\lambda = 0.005$:**

| Horizon | $E$ | $\lambda\,\mathrm{Var}$ | Score $E + \lambda\,\mathrm{Var}$ |
|---|---|---|---|
| 1 hour  | 24 | $0.005 \times 36 = 0.18$   | **24.18** |
| 1 day   | 12 | $0.005 \times 324 = 1.62$  | **13.62** |
| 1 week  | 6  | $0.005 \times 1600 = 8.00$ | **14.00** |

Lowest score wins → the **1-day** schedule (13.62) edges out the week (14.00) and crushes the hour (24.18). A patient trader leans slow but not *maximally* slow.

**Risk-averse trader, $\lambda = 0.02$:**

| Horizon | $E$ | $\lambda\,\mathrm{Var}$ | Score $E + \lambda\,\mathrm{Var}$ |
|---|---|---|---|
| 1 hour  | 24 | $0.02 \times 36 = 0.72$   | **24.72** |
| 1 day   | 12 | $0.02 \times 324 = 6.48$  | **18.48** |
| 1 week  | 6  | $0.02 \times 1600 = 32.0$ | **38.00** |

Now the **1-day** schedule (18.48) still wins, the week is punished hardest (38.00), and the hour is in between. Quadruple $\lambda$ again to $0.08$ and the hour's score becomes $24 + 0.08\times36 = 26.88$ while the day becomes $12 + 0.08\times324 = 37.92$ — the **fast 1-hour** plan finally takes over. Same three options, **different optimal choice purely because of $\lambda$.**

<Callout variant="tip" title="Why variance, not std, in the objective">
The penalty is $\lambda\cdot\mathrm{Var}$, not $\lambda\cdot\text{std}$. Squaring the risk makes the objective quadratic, which is what gives the optimal schedule its clean closed form and its smooth exponential shape. It also means doubling the timing-risk std *quadruples* its penalty — risk gets expensive fast.
</Callout>

<Reveal client:visible>
<details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
<summary className="cursor-pointer font-medium">If a fourth option had E = 14 bps and σ_c = 5 bps, would any trader ever pick the 1-hour plan over it?</summary>

No. That fourth option has **lower expected cost (14 < 24) and lower risk (5 < 6)** than the 1-hour plan — it *dominates* it on both axes. The 1-hour schedule would sit strictly **inside** the frontier, never on it, so for *no* value of λ does $E+\lambda\mathrm{Var}$ favor it over the fourth option. Dominated points are simply removed before you ever pick λ.

</details>
</Reveal>

## Alpha decay sets the urgency

**The analogy.** Two people hold melting ice-cream cones. One has gelato in July (melts in minutes); the other has a cone in a freezer aisle (basically stable). "Eat slowly to enjoy it" is fine advice for the freezer cone and ruinous for the July one. **Your alpha signal is the ice cream**, and its decay rate decides whether patience is a virtue or a leak.

**The precise version.** So far we balanced impact against timing *risk*. But if you're trading on a **signal with expected return** — a forecast that the price will move your way — then **waiting forfeits alpha** as the edge decays. Model the signal's value as decaying over time (often roughly exponentially, with half-life $\tau$). The un-captured alpha behaves like an **extra, deterministic timing cost** layered on top of variance:

$$\min_{\text{schedule}}\; \underbrace{E[\text{impact}]}_{\text{trade slower → smaller}} \;+\; \underbrace{\lambda\,\mathrm{Var}[\text{cost}]}_{\text{trade slower → bigger}} \;+\; \underbrace{\text{alpha foregone}}_{\text{trade slower → bigger}}$$

Two of the three terms now *grow* when you slow down, so a decaying signal **pushes the optimum toward FAST execution** — even for a risk-neutral trader who otherwise wouldn't care about speed.

- **Fast-decaying alpha** (stat-arb, microstructure signals, an edge that's gone in minutes/hours): execute aggressively. A clever schedule that saves 3 bps of impact is worthless if it forfeits 15 bps of evaporating edge.
- **Slow-decaying alpha** (a long-horizon value thesis good for months): the signal isn't going anywhere, so you can work the order patiently and let impact savings dominate.

This is exactly why a **stat-arb desk and a long-horizon value fund execute the identical order completely differently.** Same stock, same shares, same frontier — but wildly different $\tau$, so wildly different optimal speed.

<Callout variant="warning" title="The classic blunder: optimizing impact in a vacuum">
Tuning a schedule to minimize market impact while **ignoring alpha decay** systematically makes you **too slow.** You'll proudly report low impact cost in your TCA while quietly bleeding the very edge the trade existed to capture. Always carry the alpha-decay term in the objective.
</Callout>

<MCQ
  client:visible
  question="A stat-arb signal with a 20-minute half-life and a value fund's 6-month thesis both need to liquidate the same 200k-share block. How should each execute?"
  options={[
    { text: "Stat-arb fast (alpha is evaporating); value fund slow (patient, impact-minimizing)", correct: true },
    { text: "Both should use the identical schedule — the order is the same size" },
    { text: "Stat-arb slow to minimize impact; value fund fast to lock in the thesis" },
    { text: "Both should trade instantly to eliminate all timing risk" }
  ]}
  explanation="Optimal speed depends on alpha decay, not just order size. The stat-arb edge half-lives in 20 minutes, so waiting forfeits alpha — trade fast. The value thesis is stable for months, so impact savings dominate — trade patiently."
/>

<FillBlank
  client:visible
  question="Fill in the urgency rule."
  text="A signal that decays QUICKLY makes waiting expensive, so the optimal schedule shifts toward {{faster|slower|unchanged}} execution; a signal with SLOW decay can be {{worked patiently|forced through instantly|ignored}} to capture impact savings."
  explanation="Fast-decaying alpha acts like an extra timing cost that grows with delay, pushing toward speed. Slow-decaying alpha lets impact reduction dominate, so you can be patient."
/>

## Putting it together

Optimal execution is a single decision — *how fast?* — answered by weighing three forces: market impact (cheaper slow), timing risk (cheaper fast), and alpha decay (cheaper fast). Almgren–Chriss draws the frontier of defensible speeds; your risk aversion $\lambda$ and your signal's half-life pick the point.

<MindMap
  client:visible
  title="Optimal execution at a glance"
  caption="Three forces set one number: the speed."
  root={{
    label: 'Optimal execution',
    children: [
      { label: 'Two base costs', children: [
        { label: 'Market impact — falls as you trade slower' },
        { label: 'Timing risk (Var) — rises as you trade slower' }
      ]},
      { label: 'Almgren–Chriss frontier', children: [
        { label: 'Each speed → (E[cost], risk) pair' },
        { label: 'Frontier = non-dominated pairs' },
        { label: 'Optimal trajectory ≈ exponential decay, not linear' }
      ]},
      { label: 'Risk aversion λ', children: [
        { label: 'min E[cost] + λ·Var[cost]' },
        { label: 'λ=0 → slowest; λ→∞ → instant' }
      ]},
      { label: 'Alpha decay', children: [
        { label: 'Fast decay → trade fast (forfeited edge ≈ extra cost)' },
        { label: 'Slow decay → work patiently' }
      ]}
    ]
  }}
/>

<Quiz
  client:visible
  title="Lock in the execution frontier"
  questions={[
    {
      question: "On the Almgren–Chriss frontier, the bottom-right corner (lowest expected cost, highest risk) corresponds to which trader?",
      options: [
        { text: "A risk-neutral trader (λ = 0) with no decaying alpha", correct: true },
        { text: "An infinitely risk-averse trader (λ → ∞)" },
        { text: "A trader on a fast-decaying stat-arb signal" },
        { text: "Any trader — every λ lands there" }
      ],
      explanation: "Low expected cost / high risk = trade as slowly as possible. That's only optimal when variance carries no penalty (λ = 0) and no alpha is leaking away."
    },
    {
      question: "Two execution schedules: A has E = 10 bps, Var = 400; B has E = 8 bps, Var = 900. For λ = 0.01, which has the lower mean–variance score?",
      options: [
        { text: "A: 10 + 0.01·400 = 14, vs B: 8 + 0.01·900 = 17 → A wins", correct: true },
        { text: "B, because its expected cost is lower" },
        { text: "They tie at 14" },
        { text: "Can't tell without σ" }
      ],
      explanation: "Score A = 10 + 4 = 14; Score B = 8 + 9 = 17. A's lower variance more than offsets its 2 bps higher expected cost at this λ. (Raise λ enough and the cheaper-but-riskier B never catches up here, since B is riskier on both counts of the penalty.)"
    },
    {
      question: "Why is the optimal Almgren–Chriss liquidation trajectory typically front-loaded (roughly exponentially decaying) rather than a constant rate?",
      options: [
        { text: "Early on you hold the most inventory, so it's most exposed to timing risk — selling it sooner cuts variance fastest", correct: true },
        { text: "Market impact is always larger at the end of the day" },
        { text: "Exchanges charge lower fees in the morning" },
        { text: "It isn't — the optimum is always a straight line (TWAP)" }
      ],
      explanation: "With positive λ, the large early inventory is the biggest source of timing-risk variance, so it's worth paying a bit more impact to shed it sooner. That yields a decaying schedule; only λ = 0 gives the slow, near-linear extreme."
    },
    {
      question: "A desk reports beautifully low market-impact cost in its TCA but its stat-arb P&L is mysteriously weak. The most likely culprit?",
      options: [
        { text: "It optimized impact in isolation and ignored alpha decay, so it traded too slowly and forfeited the edge", correct: true },
        { text: "It traded too fast and paid excessive impact" },
        { text: "Its risk aversion λ was set too high" },
        { text: "Timing risk variance was negative" }
      ],
      explanation: "Minimizing impact alone makes you slow. For a fast-decaying signal, slowness leaks alpha faster than it saves impact — low impact cost, low P&L. The fix is to carry the alpha-decay term and speed up."
    }
  ]}
/>

You can now reason about *how fast* to trade. The natural next question is *how well did I actually do?* — measuring realized slippage against benchmarks like arrival price, VWAP, and implementation shortfall, and deciding whether your impact and timing-risk estimates were any good. That's the job of the next lesson: **Transaction-Cost Analysis (TCA)**.
