---
title: "What a Stochastic Process Is"
description: "A stochastic process as a whole family of random variables unfolding in time: sample paths versus the cross-sectional distribution, filtrations and the flow of information, and increments, stationarity, and why quants model returns instead of prices."
topic: "en/stochastic-processes"
order: 1
minutes: 9
updated: 2026-06-07
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, Categorize, MindMap, RandomWalkPaths } from '@/components/react';

You already know what a **random variable** is: a single uncertain number — one coin flip, one dice roll, one day's return drawn from some distribution. Useful, but frozen. A coin flip doesn't *evolve*; it just resolves, once. Yet almost everything finance cares about is precisely a thing that evolves: a price ticks all day, a portfolio grows over decades, an interest rate drifts for years. To model *change under uncertainty*, one random variable isn't enough. You need a **whole convoy of them**, one for every moment in time, all tangled together so that today's value constrains tomorrow's. That convoy is a **stochastic process**, and it is the mathematical object underneath every simulated price path, every random walk, every Brownian motion you'll meet on this platform. This lesson is the gentle on-ramp: what the object *is*, and the four ideas you need to think clearly about it.

<MCQ
  client:visible
  pretest
  question="What is a stochastic process, in one sentence?"
  options={[
    { text: 'A whole family of random variables indexed by time — one random variable for each moment, all correlated into an evolving system', correct: true },
    { text: 'A single random number drawn once from a fixed distribution' },
    { text: 'A formula that predicts the exact future value of a price' },
    { text: 'The average of a list of past returns' },
  ]}
  explanation="A stochastic process is not one random variable but a family of them, indexed by time. For every moment t there is a random variable X_t, and the whole collection {X_t} — with all its correlations across time — is the process. It describes how randomness unfolds, not a single frozen draw."
/>

## A process is a whole family of random variables, one per moment

**Analogy.** A random variable is a **photo**: one snapshot, blurry with uncertainty, but a single frame. A stochastic process is the **whole film reel** — a snapshot for every instant, and crucially the frames are *correlated*: frame 100 looks a lot like frame 99 because the story carried over. A process isn't a pile of unrelated photos; it's a continuous, self-referential narrative where each moment inherits the last.

**Definition.** A **stochastic process** is a collection of random variables $\{X_t\}$ indexed by a parameter $t$ drawn from an **index set** (almost always *time*). For every value of $t$, $X_t$ is itself a random variable. Two pieces define the object:

- The **index set** — the set of allowed times $t$. It can be **discrete** (you observe at $t = 0, 1, 2, \dots$, like daily closing prices) or **continuous** (the value is defined at *every* instant $t \ge 0$, like a price ticking in real time).
- The **state space** — the set of values each $X_t$ is allowed to take. It too can be **discrete** (a count, like the number of trades) or **continuous** (any real number, like a price or a rate).

Cross those two choices and you get four flavours, all of which show up in markets:

| | **Discrete time** | **Continuous time** |
|---|---|---|
| **Discrete state** | Coin-flip wealth: +\$1 or &minus;\$1 each round, tracked round by round | Number of trades that have arrived by time $t$ (a counting process) |
| **Continuous state** | Daily closing price: one real number per trading day | Tick-by-tick price or a short interest rate, defined at every instant |

The thing that makes a process more than "a list of random variables" is the **dependence structure**: the correlations linking $X_s$ and $X_t$. Knowing today's price tells you a great deal about tomorrow's (it'll be *near* today's), and almost nothing about a price ten years out. Capturing that web of cross-time dependence is the entire job of the model.

<Callout variant="info" title="Why not just one random variable per question?">
You could model "the price in exactly one year" as a single random variable and stop there. But then you've thrown away the *path* — the route taken, whether it ever crashed through a barrier, what the average was along the way. A staggering amount of finance (path-dependent options, drawdowns, ruin probabilities) depends on the journey, not just the destination. Modelling the whole family $\{X_t\}$ keeps the journey.
</Callout>

<FillBlank
  client:visible
  question="Pin down the definition."
  text="A stochastic process is a family of {{random variables|fixed constants|formulas}} indexed by {{time|price|volume}}. The set of allowed times is the {{index set|state space|sample path}}, and the set of allowed values is the {{state space|index set|filtration}}. A daily closing price is {{discrete in time, continuous in state|continuous in time, discrete in state|discrete in both}}."
  explanation="The process is {X_t}: one random variable per time. The index set holds the times, the state space holds the values. Daily closes are observed at discrete times (one per day) but the price is a continuous real number — so discrete time, continuous state."
/>

## Sample paths vs the cross-section

Here is **the** mental model — get this and the rest of stochastic processes clicks into place. There are two completely different ways to slice a process, and confusing them is the single most common beginner mistake.

**Slice 1 — fix the world, let time run: a sample path.** Run the process *once*. The randomness resolves, and you get **one possible history**: a single value at each time, strung together into a connected line. That line is a **sample path** (or *realisation*, or *trajectory*). It's one frame-by-frame movie of one possible future — the actual squiggle a stock printed last year is exactly one sample path of its price process.

**Slice 2 — fix the time, let the world vary: the cross-section.** Now freeze the clock at one instant $t$ and look across **all the parallel universes** where the process could have run differently. The collection of values $X_t$ takes across all those possible worlds is a **distribution** — the *cross-section* at time $t$. It answers "where could the price be at time $t$?", not "where did this one run go?".

A single sample path is a **horizontal** thread through time. The cross-section is a **vertical** cut through all possible worlds at one instant. The process is the whole cloth woven from both.

The island below makes this concrete. Each thread is one **sample path** of a random walk starting at 0. Watch many of them spread out from the origin — and notice the widening **envelope**: that's the cross-sectional spread, the $\pm\sqrt{t}$ standard deviation of where the walk could be at each time. Drag the drift to tilt the whole cloud.

<RandomWalkPaths
  client:visible
  title="Sample paths spreading into a distribution"
  driftLabel="Drift"
  envelopeLabel="±√t spread (cross-section)"
  pathsLabel="Sample paths"
  resimulateLabel="Resimulate"
  caption="Each thread is one sample path — a single possible history. Freeze the clock at any time t and look across all the threads: that vertical spread is the cross-sectional distribution, growing like √t. One path is a horizontal story through time; the envelope is a vertical cut through all possible worlds. Drag the drift to tilt the cloud; resimulate to draw fresh futures."
/>

**Worked example — the $\pm1$ random walk.** Start at 0. Each step, flip a fair coin: $+1$ on heads, $-1$ on tails. After $t$ steps:

- **One sample path** is a single zig-zag line — say $0, +1, 0, +1, +2, +1, \dots$ — one specific history of coin flips.
- **The cross-section at step $t$** is a *distribution* across all possible flip-sequences. Each step is mean-zero (the average of $+1$ and $-1$ is $0$) with variance $1$. Steps are independent, so variances **add**: after $t$ steps the total variance is $t$, the mean is still $0$, and the standard deviation — the typical distance from the start — is $\sqrt{t}$.

Put numbers on it. After $t = 100$ steps, the spread is $\sqrt{100} = 10$. So the *typical* endpoint sits about $\pm10$ away from the start, even though the *expected* endpoint is exactly $0$. One path might land at $+8$, another at $-14$; average them over millions of runs and you get $0$, with a spread of $10$. That gap — mean $0$ but spread $10$ — is the whole point of slicing two ways: the cross-section's *centre* and its *width* are different facts, and a sample path is just one dot drawn from it.

<MCQ
  client:visible
  question="A symmetric ±1 random walk starts at 0. After 400 steps, what are the mean and standard deviation of where it could be?"
  options={[
    { text: 'Mean 0, standard deviation 20 — the spread grows like the square root of the number of steps', correct: true },
    { text: 'Mean 400, standard deviation 400 — both grow linearly with the steps' },
    { text: 'Mean 20, standard deviation 0 — it always lands at exactly 20' },
    { text: 'Mean 0, standard deviation 400 — the spread equals the number of steps' },
  ]}
  explanation="Each step is mean-zero with variance 1, and independent steps add variances, so after 400 steps the variance is 400 and the standard deviation is √400 = 20. The mean stays 0. The typical endpoint is about ±20 from the start, but the average over all worlds is 0 — centre and width are different facts."
/>

## Information unfolds: the filtration

A process isn't just *what happens* — it's also *what you know, when*. As time passes, the dice resolve one by one, and your information grows. The bookkeeping device for "what's known by time $t$" is the **filtration**.

**Analogy.** Reading a novel. At the end of chapter $t$ you know chapters $1$ through $t$ — every plot point so far is settled, unchangeable history. But the ending is still genuinely uncertain to you; the later chapters haven't been read. With each chapter your known-history set *grows* and never shrinks. You can't un-read chapter 3. That expanding pile of settled pages is exactly a filtration.

**Definition.** A **filtration** $\{\mathcal{F}_t\}$ is a growing family of information sets: $\mathcal{F}_t$ is "everything observable by time $t$" — the entire history of the process up to and including $t$. It's **increasing**: if $s \le t$ then $\mathcal{F}_s \subseteq \mathcal{F}_t$ (later you know everything you knew earlier, plus more). Information accumulates; it never leaks back out.

A process is **adapted** to a filtration when its value at time $t$ is *known* once you've seen the history up to $t$ — that is, $X_t$ is determined by $\mathcal{F}_t$. This is just the sane requirement that the process can't depend on the future: a trading strategy that "knows" tomorrow's price today is not adapted, and it's cheating. Real, implementable processes are adapted by construction.

**Why this matters — conditional expectation.** The filtration is what lets you write the most important expression in all of asset pricing: the **conditional expectation**

$$E[X_{t+1} \mid \mathcal{F}_t],$$

read "the expected value of tomorrow, *given everything I know today*." This is not the unconditional average over all worlds — it's the average over only the worlds *consistent with the history I've already seen*. A **martingale** (the next lesson) is precisely a process whose best forecast of tomorrow, given today's information, is just today's value: $E[X_{t+1}\mid\mathcal{F}_t] = X_t$. You can't even *state* a martingale, or "no free lunch", or a fair game, without a filtration to condition on. It's the grammar of information in time.

<Reveal client:visible>
  <details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
    <summary className="cursor-pointer font-medium">Why does the information set only ever grow — can't you "forget"?</summary>

    A filtration models *recorded, observable* history — the public record of what the process did. Once a price prints, that print is a permanent fact: future-you still knows it happened. So the mathematical object is **monotone** (it only grows) by design, even though a human trader's *attention* is finite and forgetful. The point isn't psychology; it's that any *fair* model of decision-making under uncertainty must forbid using information you couldn't possibly have yet. Growing-only filtrations encode exactly that: at time $t$ you may use the past and present, never the future. A strategy that violates it isn't clever — it's clairvoyant, and clairvoyance is how backtests lie.
  </details>
</Reveal>

## Increments, stationarity, and what stays the same

To say anything quantitative about a process, you need vocabulary for its *changes* and for what stays statistically constant as it evolves.

**Increment.** An **increment** is a change over an interval: $X_{t+s} - X_t$, "how much the process moved between time $t$ and time $t+s$." For a price, an increment is essentially a return-like move; for the $\pm1$ walk, the increment over one step is just that step's $\pm1$.

Two structural properties of increments do enormous work:

- **Independent increments.** Changes over **non-overlapping** time intervals are statistically independent. What the walk did from step 5 to 10 tells you nothing about what it does from step 10 to 15. Each fresh interval is a clean roll of the dice. (This is the defining feature of a random walk and of Brownian motion.)
- **Stationary increments.** The *distribution* of a change depends only on the **gap** $s$, not on **when** the interval starts. A 10-day move has the same distribution whether those 10 days fall in January or July — only the length of 10 days matters, not its location on the calendar.

**(Weak) stationarity, intuitively.** A process is (weakly) **stationary** when its statistical character doesn't drift over time: its mean and its variance are constant, and the correlation between two times depends only on the *gap* between them, never on *where* they sit. Slide your viewing window along the time axis and the statistics look the same — same average, same spread, same rhythm. A stationary process has no preferred era; it looks statistically "the same" forever.

**Worked contrast.** Take the $\pm1$ random walk again. Are its *increments* stationary? **Yes** — every single step is a fresh $\pm1$ with the same distribution, regardless of when you take it. But is the *level* (the position $X_t$ itself) stationary? **No** — its variance is $t$, which *grows without bound* as time passes. The spread at step 100 is $\sqrt{100}=10$; at step 10,000 it's $\sqrt{10{,}000}=100$. A process whose variance balloons over time is the opposite of stationary. So a random walk has stationary *increments* but a wildly non-stationary *level*. Those are different questions about the same process — keep them apart.

**Pitfall — prices aren't stationary, returns nearly are.** Real asset *prices* are emphatically **not** stationary: they trend, they grow, they wander off to new all-time highs, and their variance fans out over time (exactly like the random-walk level above). Fit a "constant mean" to a price series and you're modelling a fiction. But asset **returns** — the percentage changes, the increments — are *far* closer to stationary: a typical daily return looks statistically similar across years (similar average near zero, comparable volatility), even as the price level marches off. **This is why quants model returns, not prices.** Differencing a non-stationary level into its stationary-ish increments is the first move in half of quantitative finance.

<Callout variant="warning" title="Stationary increments ≠ stationary process">
Don't conflate the two. A random walk has perfectly stationary *increments* (every step is the same fresh draw) yet a violently non-stationary *level* (variance grows like t, with no fixed mean or spread). "Is the change always the same kind of change?" and "does the level look the same across eras?" are separate questions — a process can answer yes to the first and no to the second.
</Callout>

## Quick check

Let's sort the ideas and stress-test the boundaries between them.

<Categorize
  client:visible
  question="Sort each example by how it's indexed in time."
  buckets={['Discrete-time', 'Continuous-time']}
  items={[
    { text: 'A list of end-of-day closing prices, one per trading day', bucket: 'Discrete-time' },
    { text: 'Coin-flip wealth updated once per round', bucket: 'Discrete-time' },
    { text: 'Monthly portfolio balances on a statement', bucket: 'Discrete-time' },
    { text: 'A price quote defined at every instant the market is open', bucket: 'Continuous-time' },
    { text: 'A short interest rate modelled as ticking continuously', bucket: 'Continuous-time' },
    { text: 'Brownian motion, defined for every real time t ≥ 0', bucket: 'Continuous-time' },
  ]}
  explanation="Discrete-time processes are observed at separated instants (daily closes, per-round wealth, monthly balances). Continuous-time processes have a value at every instant (live quotes, continuously-modelled rates, Brownian motion). The state space — whether the value is a count or a real number — is a separate axis."
/>

<MCQ
  client:visible
  question="Which statement about a single sample path is true?"
  options={[
    { text: 'It is one possible history — a connected sequence of values, one per time — drawn from the process', correct: true },
    { text: 'It is the distribution of the process across all possible worlds at a fixed time' },
    { text: 'It is the expected value of the process at each time, with no randomness left' },
    { text: 'It is the filtration — the set of everything known by time t' },
  ]}
  explanation="A sample path is one realisation: run the process once, the randomness resolves, and you get a single connected history through time. The distribution across worlds at a fixed time is the cross-section — a different slice. The path is the horizontal thread; the cross-section is the vertical cut."
/>

<FillBlank
  client:visible
  question="Increments and stationarity."
  text="An increment is the change {{X(t+s) − X(t)|X(t) × s|the filtration F_t}} over an interval. Non-overlapping increments being independent means each fresh interval is a {{clean new roll|repeat of the last|deterministic step}}. Real prices are {{not stationary|perfectly stationary|always increasing}}, but their returns are {{closer to stationary|even less stationary|exactly constant}} — which is why quants model {{returns|raw price levels|trading volume}}."
  explanation="An increment is X(t+s) − X(t). Independent increments mean each non-overlapping interval is a clean new roll. Prices trend and their variance grows, so they aren't stationary; returns (the increments) are far closer to stationary, which is exactly why quants difference prices into returns before modelling."
/>

## Putting it together

A **stochastic process** $\{X_t\}$ is not one random variable but a whole family of them, one per time — a film reel of correlated frames, defined by an **index set** (the times) and a **state space** (the values), in discrete or continuous flavours of each. Slice it **horizontally** and you get a **sample path**, one possible history; slice it **vertically** at a fixed time and you get the **cross-sectional distribution** across all possible worlds — for a $\pm1$ walk, mean $0$ but spread $\sqrt{t}$ (about $10$ after $100$ steps). The **filtration** $\{\mathcal{F}_t\}$ tracks the ever-growing pile of what's known by time $t$; a process is **adapted** when it never peeks at the future, and conditioning on $\mathcal{F}_t$ is what makes $E[X_{t+1}\mid\mathcal{F}_t]$ — the heart of martingales and pricing — even expressible. Finally, **increments** ($X_{t+s}-X_t$) can be **independent** (non-overlapping changes don't talk) and **stationary** (a change's distribution depends only on the gap), and a process is **stationary** when its statistics don't drift — which prices flagrantly violate but returns nearly satisfy, the reason quants model returns.

<MindMap
  client:visible
  title="What a stochastic process is — the whole object"
  caption="A family of random variables in time; read it as paths or as cross-sections; track information with a filtration; describe its changes with increments and stationarity."
  root={{
    label: 'Stochastic process',
    children: [
      {
        label: 'A family of random variables',
        children: [
          { label: 'One random variable X_t per time t' },
          { label: 'Index set = the times (discrete or continuous)' },
          { label: 'State space = the values (discrete or continuous)' },
          { label: 'Correlations across time are the whole point' },
        ],
      },
      {
        label: 'Two ways to slice it',
        children: [
          { label: 'Sample path: one possible history (horizontal)' },
          { label: 'Cross-section: distribution at fixed t (vertical)' },
          { label: 'Walk after t steps: mean 0, spread √t' },
        ],
      },
      {
        label: 'Filtration F_t',
        children: [
          { label: 'Everything known by time t, only ever grows' },
          { label: 'Adapted: value at t never peeks at the future' },
          { label: 'Enables E[X next given F_t] — pricing, martingales' },
        ],
      },
      {
        label: 'Increments and stationarity',
        children: [
          { label: 'Increment = X(t+s) − X(t)' },
          { label: 'Independent: non-overlapping changes are independent' },
          { label: 'Stationary: distribution depends on the gap, not when' },
          { label: 'Prices not stationary; returns nearly are → model returns' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: what a stochastic process is"
  questions={[
    {
      question: 'What distinguishes a stochastic process from a single random variable?',
      options: [
        { text: 'A process is an entire family of random variables indexed by time, with correlations linking the moments — not one frozen draw', correct: true },
        { text: 'A process always uses a uniform distribution, while a random variable can use any distribution' },
        { text: 'A process is fully deterministic once started, while a random variable is random' },
        { text: 'They are the same thing; "process" is just a longer name for a random variable' },
      ],
      explanation: 'A random variable is a single uncertain number (a photo). A process is a whole indexed family {X_t} — a random variable for every time, with cross-time correlations (the film reel). That dependence structure is what a process adds.',
    },
    {
      question: 'A ±1 random walk starts at 0. Run it once and it ends at +6 after 36 steps. What does this single run tell you?',
      options: [
        { text: 'It is one sample path — one possible history; the cross-section at step 36 has mean 0 and standard deviation 6, so +6 is just one typical-magnitude draw', correct: true },
        { text: 'The walk has positive drift, since it ended above 0' },
        { text: 'Every run of this walk ends at exactly +6 after 36 steps' },
        { text: 'The expected ending value of the walk is +6' },
      ],
      explanation: 'One run is one sample path, a single draw from the cross-section. After 36 steps that distribution has mean 0 and standard deviation √36 = 6, so landing at +6 is about one standard deviation out — perfectly ordinary, not evidence of drift. The expected endpoint is still 0.',
    },
    {
      question: 'Which is true of a filtration F_t and an adapted process?',
      options: [
        { text: 'F_t holds everything known by time t and only grows; "adapted" means the process value at t is known from that history, never peeking at the future', correct: true },
        { text: 'F_t shrinks over time as old information is forgotten, and adapted processes may use tomorrow’s data today' },
        { text: 'The filtration is the single most likely future value of the process' },
        { text: 'An adapted process must be stationary, with constant mean and variance' },
      ],
      explanation: 'A filtration is the growing record of what is observable by time t (it never shrinks). Adapted means X_t is determined by F_t — the process can use the past and present but not the future. This is exactly the no-clairvoyance rule that conditional expectations like E[X next | F_t] rely on.',
    },
    {
      question: 'Spot the trap: an analyst fits a constant-mean, constant-variance model directly to a stock’s price level and is surprised it forecasts badly. What went wrong?',
      options: [
        { text: 'Price levels are not stationary — they trend and their variance grows over time; returns (the increments) are far closer to stationary, so model those instead', correct: true },
        { text: 'Nothing — prices are stationary, so a constant-mean model is exactly right' },
        { text: 'The analyst should have used the filtration as the dependent variable' },
        { text: 'Stationary increments guarantee a stationary level, so the level must be stationary too' },
      ],
      explanation: 'Prices trend and their variance fans out over time — the hallmark of a non-stationary process — so a constant-mean, constant-variance fit is modelling a fiction. Returns are the increments, and they are far closer to stationary, which is why quants difference prices into returns first. And stationary increments do NOT imply a stationary level: a random walk proves it (stationary steps, variance-t level).',
    },
  ]}
/>

Next up — **Martingales and the fair game** — we put the filtration to work. A martingale is a process whose best forecast of tomorrow, *given everything known today*, is simply today's value: $E[X_{t+1}\mid\mathcal{F}_t] = X_t$. It's the mathematical spine of "no free lunch", efficient markets, and risk-neutral pricing — and you now have every piece of vocabulary you need to state it precisely.
