---
title: "Random Walks & Martingales"
description: "The simple random walk and its √t spread, drift versus no drift, the martingale (fair-game) property via conditional expectation, and why efficient markets behave like martingales."
topic: "en/stochastic-processes"
order: 2
minutes: 9
updated: 2026-06-07
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, MatchConcepts, MindMap, RandomWalkPaths } from '@/components/react';

You've met the idea that randomness can be *modeled*, step by step. Now we meet the two creatures that show up everywhere a price, a bankroll, or a forecast wanders through time: the **random walk** and the **martingale**. The first is the simplest possible model of accumulated chance — a running total of coin flips. The second is the precise mathematical statement of a *fair game*: a process with no predictable lean, where your best guess of tomorrow is exactly today. They are the skeleton under "stock prices look unpredictable," and getting them exactly right — including what they do *not* say — is what separates a real understanding of market efficiency from a folk slogan.

<MCQ
  client:visible
  pretest
  question="A stock's daily moves look completely unpredictable — you can't forecast tomorrow's change from anything you know today. What does that imply about its long-run trend?"
  options={[
    { text: 'Nothing by itself — unpredictable steps can still carry a steady upward drift; "no forecastable change" and "no trend" are different claims', correct: true },
    { text: 'It must drift to zero, since the moves average out' },
    { text: 'It can have no trend at all — unpredictable means flat on average' },
    { text: 'It guarantees the price is safe to hold, with little risk' },
  ]}
  explanation="Unpredictable increments are about the conditional expectation of the NEXT step. They say nothing about a persistent average drift. A walk can be impossible to forecast step-to-step and still climb relentlessly (positive drift), or wander with no lean at all (a martingale). This lesson pins down exactly which is which."
/>

## The simple random walk

**Analogy.** Picture a coin-flipping drunk on a sidewalk. Heads, he lurches one step right; tails, one step left. After many flips, *where* is he? Not nowhere in particular — but not drifting anywhere either. His position is the running sum of independent shoves, and that sum has a beautiful, predictable *shape* even though every step is pure chance.

**Definition.** A **simple random walk** is the running total
$$S_n = X_1 + X_2 + \cdots + X_n,$$
where each **increment** $X_i$ is an independent, identically distributed step — say $+1$ or $-1$ with equal probability (or $\pm c$ for a step of size $c$). $S_0 = 0$; each new position is just the last one plus a fresh independent shock: $S_n = S_{n-1} + X_n$.

Two facts do all the work, and both come straight from arithmetic on the increments.

**Fact 1 — the mean stays put.** Each fair step has $E[X_i] = \tfrac{1}{2}(+1) + \tfrac{1}{2}(-1) = 0$. Expectation adds, so
$$E[S_n] = E[X_1] + \cdots + E[X_n] = 0.$$
On average the walk goes *nowhere*. A symmetric walk has no built-in lean.

**Fact 2 — the spread grows like √n.** Independent variances add. For a $\pm c$ step, $\mathrm{Var}(X_i) = c^2$ (since the mean is 0 and the squared deviation is always $c^2$). So
$$\mathrm{Var}(S_n) = n\,c^2, \qquad \mathrm{SD}(S_n) = c\sqrt{n}.$$
The *variance* grows linearly with the number of steps, but the *standard deviation* — the typical distance from the start — grows only like the **square root** of time. This is the famous **√t spreading law**, and it is why uncertainty piles up slower than you'd guess.

**Worked example.** Take 100 fair steps of \$1 each (win or lose a dollar on each flip), so the step size is $c = 1$.

- Expected final position: $E[S_{100}] = 0$ — you expect to break even.
- Standard deviation: $\mathrm{SD}(S_{100}) = 1 \times \sqrt{100} = 10$, i.e. \$10.
- Because $S_{100}$ is (by the central-limit theorem) roughly normal, about 95% of the time you land within $\pm 2$ standard deviations — that is, between &minus;\$20 and +\$20.

Notice the asymmetry of scale: 100 steps, each worth \$1, could in principle leave you anywhere from &minus;\$100 to +\$100 — but the *typical* outcome is only about \$10 from zero. Cancellation is the rule; extreme runs are rare.

<RandomWalkPaths
  client:visible
  title="A spreading cloud of random-walk paths"
  driftLabel="Drift (μ)"
  envelopeLabel="±√t spread"
  pathsLabel="sample paths"
  resimulateLabel="Resimulate"
  caption="Every path starts at zero and is shoved by tiny independent shocks. Slide the drift to 0 and the cloud is a symmetric martingale — it goes nowhere on average — yet it keeps fanning out: the dashed ±√t funnel is the standard deviation widening like the square root of time, not linearly. Tilt the drift and the whole cloud leans. Resimulate to draw a fresh batch."
/>

<MCQ
  client:visible
  question="You take 400 fair ±1 steps. Roughly how far from the start should you typically expect to end up (one standard deviation)?"
  options={[
    { text: 'About 20 — the SD is √400 = 20, since variance (400) adds linearly but SD grows like √n', correct: true },
    { text: 'About 400 — the steps add up directly' },
    { text: 'About 0 — the mean is zero so you end up at the start' },
    { text: 'About 200 — half the steps, since heads and tails cancel' },
  ]}
  explanation="Variance adds: Var = 400 × 1 = 400, so SD = √400 = 20. The MEAN is 0 (you expect to break even), but the mean is not where you typically land — the typical distance is the standard deviation, which grows like √n. Quadruple the steps (100 → 400) and the spread only doubles (10 → 20)."
/>

## Adding drift

The fair walk is symmetric, but nothing forces a coin to be fair. Tilt it — make each step carry a small *average* push — and you get a **biased random walk** with **drift**.

**Definition.** Let each increment have mean $E[X_i] = \mu \neq 0$ while keeping a finite variance $\sigma^2$. Then
$$E[S_n] = n\mu, \qquad \mathrm{SD}(S_n) = \sigma\sqrt{n}.$$
The expected position now grows **linearly** in $n$ (the drift $n\mu$), while the spread still grows only like $\sqrt{n}$. That mismatch — linear signal versus square-root noise — is the whole story of telling skill from luck.

**Worked example — a tiny edge.** Suppose a trader (or a loaded coin) has a per-step edge of $\mu = +0.02$ with step-scale $\sigma = 1$. Over $n = 100$ steps:

- Drift (signal): $n\mu = 100 \times 0.02 = +2$.
- Noise (spread): $\sigma\sqrt{n} = 1 \times \sqrt{100} = 10$.

After 100 steps the *expected* gain is $+2$, but the noise is $\pm 10$ — **five times larger**. The edge is real, yet it's buried; a single 100-step sample could easily show a *loss* and tell you nothing. The drift is invisible short-term.

**Now stretch the horizon.** The drift $n\mu$ scales with $n$; the noise $\sigma\sqrt{n}$ scales with $\sqrt{n}$. So the *signal-to-noise ratio* is
$$\frac{n\mu}{\sigma\sqrt{n}} = \frac{\mu}{\sigma}\sqrt{n},$$
which **grows without bound** as $n$ increases. At $n = 100$ it's $0.02 \times 10 = 0.2$ (drift is a fifth of the noise). At $n = 10{,}000$ it's $0.02 \times 100 = 2$ (drift is double the noise). Eventually drift always wins — but "eventually" can be a very long time.

<Callout variant="warning" title="Why short track records prove nothing">
A manager with a genuine edge of μ and a manager with none both produce noisy curves that, over a few months, are statistically indistinguishable — the √n noise swamps the nμ signal. You need a sample long enough that (μ/σ)·√n is comfortably bigger than 1 before luck and skill separate. This is also why a *lucky* no-edge manager can look brilliant for years.
</Callout>

<FillBlank
  client:visible
  question="Signal versus noise in a drifting walk."
  text="In a biased random walk the expected position grows {{linearly|like the square root|exponentially}} with the number of steps, while the spread grows only {{like the square root|linearly|not at all}}. So a small edge is {{invisible short-term but dominant long-term|always obvious immediately|never visible at any horizon}}."
  explanation="Drift is nμ (linear in n); noise is σ√n (square-root in n). Short samples are dominated by noise, but because the signal-to-noise ratio (μ/σ)·√n grows with n, drift inevitably dominates over a long enough horizon."
/>

## The martingale: a fair game

Strip the drift back out and you get the single most important object in this lesson: a **martingale**. The word names a precise property — a *fair game* — and the precision matters.

**Definition.** A process $X_0, X_1, X_2, \dots$ is a **martingale** if, given everything known up to today (call that information set $\mathcal{F}_t$), the conditional expectation of tomorrow's value equals today's:
$$E[X_{t+1} \mid \mathcal{F}_t] = X_t.$$
In words: *your best forecast of tomorrow, using all the information you have right now, is exactly today's value.* No predictable push up, no predictable push down. The martingale property is about the **conditional expectation** — the forecast given the present — not about the variance.

**That last point is the whole trap.** A martingale is **not** a constant, and it is **not** low-variance. Tomorrow's value is still uncertain and can swing wildly; the claim is only that the *average* of that swing, conditioned on today, is centered on today. A driftless random walk is the textbook example: $E[S_{n+1} \mid \mathcal{F}_n] = S_n + E[X_{n+1}] = S_n + 0 = S_n$. It's a martingale, yet (we just saw) its spread grows like $\sqrt{n}$ forever.

**Examples.**

- A **driftless random walk** — fair coin flips — is a martingale.
- A **fair casino bankroll**: each bet has expected change zero, so your expected bankroll tomorrow is your bankroll today. A martingale.
- Any process with **no predictable trend**, where knowing the past gives you no edge on the next move's *average*.

**Two cousins** capture biased games:

- A **submartingale** satisfies $E[X_{t+1} \mid \mathcal{F}_t] \ge X_t$ — it tends to **drift up**. A stock earning a risk premium is a submartingale: on average it pays you to hold it.
- A **supermartingale** satisfies $E[X_{t+1} \mid \mathcal{F}_t] \le X_t$ — it tends to **drift down**. A gambler's bankroll at an unfair casino (house edge) is a supermartingale; so is a fee-draining fund.

A handy mnemonic: a *super*martingale is bad luck for the holder (it leaks *down*), a *sub*martingale is good luck (it builds *up*) — the names feel backwards because they're named after the inequality on the expectation, not the direction of the money.

<MatchConcepts
  client:visible
  question="Match each term to its precise definition."
  pairs={[
    { term: 'Martingale', definition: 'Expected next value given today equals today — a fair game, no predictable lean' },
    { term: 'Submartingale', definition: 'Expected next value is at least today (≥) — drifts up, like a stock with a risk premium' },
    { term: 'Supermartingale', definition: 'Expected next value is at most today (≤) — drifts down, like a bankroll facing a house edge' },
    { term: 'Drift', definition: 'A nonzero average push per step, making the expected position move linearly in n' },
    { term: 'Increment', definition: 'A single independent step Xi added to the running total' },
  ]}
  explanation="Martingale: E[next | today] = today (fair). Submartingale ≥ (up). Supermartingale ≤ (down). Drift is the nonzero mean per step; an increment is one step of the walk. All four are statements about the CONDITIONAL expectation, not the variance."
/>

## Why efficient markets look like martingales

Here's the payoff — the reason this abstract machinery is everywhere in finance.

**The argument.** Suppose a price already reflects all available information. Then the *expected* next change, beyond the baseline compensation for time and risk, must be **zero**. Why? Because if everyone could see that the price would predictably rise tomorrow, they'd buy today — pushing the price up *now* until the predictable gain vanished. Any forecastable excess move is free money, and free money gets competed away instantly. What survives is a process whose best forecast of the next (suitably adjusted) move is no move at all: a **martingale**.

More carefully: it's the **discounted, risk-adjusted price** that behaves like a martingale. Strip out the risk-free growth and the risk premium, and what remains has expected change zero. (Pricing theory formalizes this as the existence of a **martingale measure** — a reweighting of probabilities under which discounted prices are exactly martingales. You'll meet it properly later; for now, just hold the picture: efficiency = no predictable *excess* move.)

**Pitfall 1 — "random walk" does not mean "no drift."** Real equity indices are **submartingales**, not martingales: they carry an upward drift, the **equity risk premium**, your reward for bearing risk. Efficiency kills *predictable excess* returns — the part you could arbitrage — **not** the risk premium, which is compensation, not a free lunch. Saying "the market is a random walk so it has no trend" fuses two different claims and gets the second one wrong.

**Pitfall 2 — a martingale is not safe.** "No predictable trend" says nothing about risk. A martingale's variance still grows like $\sqrt{t}$ (or faster), so it can wander enormously far from where it started. Fair does not mean gentle. Confusing "you can't forecast the direction" with "it won't move much" is exactly the error the pretest flagged.

<Reveal client:visible>
  <details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
    <summary className="cursor-pointer font-medium">If markets carry an upward drift, why do quants so often model prices as martingales anyway?</summary>

    Because for *pricing derivatives*, the real-world drift is a red herring. The whole insight of risk-neutral pricing is that you can switch to an artificial probability measure — the **martingale measure** — under which every discounted asset is a true martingale, drifting at exactly the risk-free rate and nothing more. Under that measure expected excess returns are zero by construction, so the price of a payoff is simply its discounted expected value. This isn't a claim that stocks *really* have no risk premium (they do, under the real-world measure); it's a computational trick that works precisely because no-arbitrage forces discounted prices to be martingales under *some* measure. The risk premium hasn't vanished — it's been absorbed into the change of measure. So "prices are martingales" is true under the pricing measure and false under the real-world one, and keeping those two straight is half of what a derivatives quant gets paid for.
  </details>
</Reveal>

<MCQ
  client:visible
  question="A colleague says: 'The S&P 500 is a random walk, so over the long run you shouldn't expect it to go anywhere.' What's wrong?"
  options={[
    { text: 'It conflates unpredictability with no drift — equity indices are submartingales carrying an upward risk premium, so they DO drift up on average', correct: true },
    { text: 'Nothing — a random walk truly has no long-run direction, so the colleague is right' },
    { text: 'Random walks always drift down because of fees and inflation' },
    { text: 'The S&P 500 is perfectly predictable, so it is not a random walk at all' },
  ]}
  explanation="Efficiency removes PREDICTABLE EXCESS returns (the arbitrageable part), not the risk premium. The index is a submartingale: its increments are hard to forecast, yet it carries a positive average drift — your compensation for bearing risk. 'Unpredictable step-to-step' and 'no long-run trend' are different statements."
/>

## Gambler's ruin & optional stopping (intuition)

One last pair of ideas that follow from the martingale property and demolish a lot of wishful thinking.

**Gambler's ruin.** Play a *fair* game (a martingale bankroll) with a **finite** purse against a house with effectively infinite money, and keep playing forever. You go broke with **probability 1**. The game is fair — your expected bankroll never drops — yet ruin is certain, because the $\sqrt{t}$ wandering will *eventually* touch zero, and zero is an absorbing wall (broke is broke; the house, with infinite reserves, has no such wall). You cannot out-wait variance. Fairness protects your *average*, not your *survival*.

**Worked ruin probability.** Suppose you start with stake $a$ and quit either at \$0 (ruin) or at a target $b > a$, playing a fair $\pm1$ walk. The probability of ruin before hitting the target is
$$P(\text{ruin}) = \frac{b - a}{b}.$$
Start with \$20 aiming for \$100 (so $a = 20$, $b = 100$): ruin probability $= (100 - 20)/100 = 0.8$. An 80% chance of going broke before you ever see \$100 — in a perfectly *fair* game. (And against an infinite house, set $b \to \infty$ and the ruin probability $\to 1$.)

**Optional stopping (intuition).** You might hope to *beat* a martingale with a clever exit rule — quit the instant you're ahead, double down when behind, and so on. The **optional stopping theorem** says: under reasonable conditions, no stopping strategy changes your expected payoff. For a fair martingale, your expected value when you stop equals your starting value, whatever rule you use. The "quit while you're ahead" schemes (like the classic Martingale doubling system) don't manufacture an edge — they just reshuffle a near-certain small win against a rare catastrophic loss, leaving the expectation exactly where it started. There is no free lunch hiding in the *timing*.

<MCQ
  client:visible
  question="You start a fair ±1 game with 25 dollars and quit at either 0 or a 100 target. What's the probability of ruin first, and what does it reveal?"
  options={[
    { text: '75 percent — ruin prob is (b−a)/b = (100−25)/100 = 0.75; a fair game still ruins a small bankroll most of the time', correct: true },
    { text: '25 percent — you have a one-in-four chance of going broke since you start with a quarter of the target' },
    { text: '0 percent — the game is fair, so you can never go broke' },
    { text: '50 percent — fair games are always a coin flip between ruin and target' },
  ]}
  explanation="Ruin probability = (b − a)/b = (100 − 25)/100 = 0.75. Fairness keeps your EXPECTED bankroll flat, but with a small purse aiming for a distant target, the √t wandering hits the zero wall first 75% of the time. Against an infinite house (b → ∞) ruin becomes certain. Fair does not mean safe."
/>

## Putting it together

A **simple random walk** is a running sum of independent shocks: its mean stays at the start (for fair steps) while its spread widens like $\sqrt{n}$ — uncertainty piling up slower than the step count. **Drift** adds a per-step lean $\mu$, so the expected position grows *linearly* ($n\mu$) while noise still grows like $\sqrt{n}$ — which is why a small edge is invisible short-term but inevitable long-term. A **martingale** is the precise statement of a fair game: $E[X_{t+1}\mid\mathcal{F}_t] = X_t$, a forecast-equals-today property about the *conditional expectation*, not about variance — so it can still wander enormously. **Submartingales** drift up (a stock's risk premium), **supermartingales** drift down (a house edge). **Efficient markets** look like martingales because any predictable *excess* move would be arbitraged away — but real indices are *submartingales*, carrying a risk premium, and a martingale is never "safe." And gambler's ruin plus optional stopping seal the deal: you can't out-wait variance, and no clever exit rule beats a fair game.

<MindMap
  client:visible
  title="Random walks & martingales — the whole picture"
  caption="A running sum of shocks spreads like √t; drift adds a linear lean; a martingale is a fair game about the conditional expectation; efficient markets are (sub)martingales because predictable excess gets arbitraged away."
  root={{
    label: 'Random walks & martingales',
    children: [
      {
        label: 'Simple random walk',
        children: [
          { label: 'Sn = sum of iid ±step increments' },
          { label: 'Fair steps: mean stays 0' },
          { label: 'Variance = n·step², so SD = step·√n' },
          { label: 'The √t spreading law' },
        ],
      },
      {
        label: 'Adding drift',
        children: [
          { label: 'Each step has mean mu ≠ 0' },
          { label: 'Expected position grows linearly: n·mu' },
          { label: 'Spread still grows like √n' },
          { label: 'Signal-to-noise (mu/sigma)·√n grows with n' },
        ],
      },
      {
        label: 'Martingale: a fair game',
        children: [
          { label: 'E[next | today] = today' },
          { label: 'About the conditional expectation, not variance' },
          { label: 'Driftless walk / fair bankroll' },
          { label: 'Submartingale ≥ up, supermartingale ≤ down' },
        ],
      },
      {
        label: 'Efficient markets',
        children: [
          { label: 'Predictable excess move = free money, arbitraged away' },
          { label: 'Discounted prices ≈ martingales (martingale measure)' },
          { label: 'Real indices are submartingales (risk premium)' },
          { label: 'Pitfall: martingale ≠ safe, variance still grows' },
        ],
      },
      {
        label: 'Ruin & optional stopping',
        children: [
          { label: 'Finite purse vs infinite house → ruin certain' },
          { label: 'Fair-walk ruin prob = (b − a)/b' },
          { label: 'No stopping rule beats a martingale' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: random walks & martingales"
  questions={[
    {
      question: 'A fair ±1 random walk runs for 900 steps. What is the standard deviation of its final position, and what is its expected final position?',
      options: [
        { text: 'SD = 30 (√900) and expected position = 0 — variance adds to 900, but the mean stays at the start', correct: true },
        { text: 'SD = 900 and expected position = 900 — both grow with the number of steps' },
        { text: 'SD = 0 and expected position = 0 — fair walks do not move' },
        { text: 'SD = 450 and expected position = 450 — half the steps land each way' },
      ],
      explanation: 'Variance adds linearly: Var = 900 × 1 = 900, so SD = √900 = 30. The mean is 0 because each fair step has mean 0. The typical distance from the start is the SD (30), not the mean (0) — the √t spreading law.',
    },
    {
      question: 'Which statement about a martingale is TRUE?',
      options: [
        { text: 'Its best forecast of tomorrow, given everything known today, equals today — but it can still have large and growing variance', correct: true },
        { text: 'It has zero variance, so its value barely changes from step to step' },
        { text: 'It must drift steadily upward over time' },
        { text: 'Knowing today lets you predict the exact value tomorrow' },
      ],
      explanation: 'A martingale is defined by E[next | today] = today — a statement about the conditional EXPECTATION. It says nothing about variance, which typically grows like √t. So a martingale is unpredictable in direction yet can wander far. It does not drift (that would be a sub- or supermartingale).',
    },
    {
      question: 'Spot the trap. Someone claims: "Stock prices follow a random walk, which means they have no upward trend." Why is this wrong?',
      options: [
        { text: 'Random/unpredictable step-to-step does not imply zero drift; real equity indices are submartingales with an upward risk-premium drift', correct: true },
        { text: 'It is correct — random walks provably have no drift of any kind' },
        { text: 'Stock prices are fully predictable, so they are not random walks' },
        { text: 'Random walks always trend downward because of trading costs' },
      ],
      explanation: 'Unpredictability concerns the conditional expectation of the next move; it does not forbid a persistent average lean. Efficiency removes predictable EXCESS returns, not the risk premium. Equity indices are submartingales: hard to forecast step-to-step yet drifting up on average.',
    },
    {
      question: 'Scenario: a gambler with 10 dollars plays a perfectly fair ±1 game, quitting at 0 or at a 50 target. What is the chance of ruin, and what general lesson does it teach?',
      options: [
        { text: '80 percent — ruin prob is (50−10)/50 = 0.8; a fair game protects your average bankroll but not your survival with a small purse', correct: true },
        { text: '20 percent — the chance equals the fraction of the target you start with' },
        { text: '0 percent — a fair game can never bankrupt you' },
        { text: '50 percent — every fair game is an even coin flip between ruin and target' },
      ],
      explanation: 'Ruin probability = (b − a)/b = (50 − 10)/50 = 0.8. Fairness keeps the EXPECTED bankroll flat, but the √t wandering hits the zero wall first 80% of the time when the purse is small relative to the target. Against an infinite house, ruin becomes certain — you cannot out-wait variance.',
    },
  ]}
/>

Next up — **Brownian motion** — we let the steps shrink to zero and arrive in a step at a time, taking the random walk's √t spreading to its smooth, continuous limit. That continuous-time martingale is the raw material for every price model you'll build from here: feed it a drift and a volatility, wrap it in an exponential, and you've reinvented the engine behind simulated asset prices.
