---
title: "Expectation, Variance & the Moments"
description: "The four numbers that summarize any return distribution: expectation (the average), variance and volatility (the spread), skewness (the lopsidedness), and kurtosis (the fat tails). Worked from raw return series, with the n−1 correction and the √252 annualizer."
topic: "en/statistics-for-finance"
order: 3
minutes: 11
updated: 2026-06-07
---

import { Callout, MCQ, Quiz, FillBlank, MatchConcepts, MindMap, ReturnDistribution } from '@/components/react';

A probability distribution is a whole shape — an infinite cloud of possible returns, each with its own likelihood. That's far too much to carry around in your head, so finance squeezes the cloud into a handful of summary numbers called **moments**. The first tells you *where* the distribution sits (its average), the second *how wide* it is (its spread), the third *how lopsided* it is, and the fourth *how heavy its tails* are. Master these four and you can describe any return series — a stock, a fund, a strategy — in four numbers that a risk manager will actually understand.

This lesson builds them from the ground up, straight from raw return numbers, and connects every one to the bell curves and fat tails you met last lesson.

<MCQ
  client:visible
  pretest
  question="A distribution's 'moments' (mean, variance, skewness, kurtosis) collectively tell you:"
  options={[
    { text: 'Where it sits, how wide it is, how lopsided it is, and how heavy its tails are', correct: true },
    { text: 'Only the average outcome, nothing about risk' },
    { text: 'The exact future return of the asset' },
    { text: 'Whether the market is open or closed' },
  ]}
  explanation="The four moments summarize a distribution's location (mean), spread (variance/σ), asymmetry (skewness), and tail-heaviness (kurtosis). They describe the shape of randomness — never a guaranteed future outcome."
/>

## Expectation: the probability-weighted average

**Analogy.** Imagine a weighted roulette wheel where each outcome is a possible return and the size of its slot is its probability. Spin it a million times, average the results, and you converge on the **expectation** — the long-run center of gravity of the distribution. It is *not* the most likely single outcome; it's the balance point of the whole wheel.

**Definition.** For a discrete random variable $X$ taking values $x_i$ with probabilities $p_i$, the expectation (or **mean**) is

$$
\mathbb{E}[X] = \sum_i p_i\,x_i.
$$

Each outcome is weighted by how often it happens. In finance $\mathbb{E}[X]$ is the **expected return** — the average return you'd earn if the same odds repeated forever.

**Worked example.** A trade has three possible one-year outcomes:

| Scenario | Return $x_i$ | Probability $p_i$ | $p_i\,x_i$ |
|---|---|---|---|
| Boom | +30% | 0.25 | +7.5% |
| Base | +8% | 0.50 | +4.0% |
| Bust | −20% | 0.25 | −5.0% |

Add the last column: $7.5\% + 4.0\% - 5.0\% = 6.5\%$. So $\mathbb{E}[X] = 6.5\%$. Notice the expected return (6.5%) isn't any of the three outcomes — it's the weighted balance point, pulled down from the 8% base case by the fat 25% chance of a −20% bust.

### Linearity of expectation

The single most useful property of expectation: it passes straight through sums and scalings, *no matter what*. For any constants $a$ and $b$,

$$
\mathbb{E}[aX + b] = a\,\mathbb{E}[X] + b.
$$

**Worked example.** Suppose you lever the trade above $2\times$ and the broker skims a flat 1% fee, so your net return is $Y = 2X - 1\%$. Then

$$
\mathbb{E}[Y] = 2\,\mathbb{E}[X] - 1\% = 2(6.5\%) - 1\% = 13\% - 1\% = 12\%.
$$

You never had to rebuild the scenario table — linearity let you scale the answer directly. This is why expected returns of a portfolio are just the weighted average of the components' expected returns: expectation glides through the sum.

<Callout variant="warning" title="The average is not the outcome">
Expectation is a long-run center of gravity, not a prediction of next year. A trade with a +6.5% expected return can still lose 20% on any single play — and "on average" only shows up after many independent repetitions. Confusing the mean with the next draw is the oldest mistake in the book.
</Callout>

<MCQ
  client:visible
  question="A bet pays +50% with probability 0.2, +5% with probability 0.5, and −30% with probability 0.3. Its expected return is:"
  options={[
    { text: '+3.5%', correct: true },
    { text: '+8.3%' },
    { text: '+5.0%' },
    { text: '−30%' },
  ]}
  explanation="E[X] = 0.2(50%) + 0.5(5%) + 0.3(−30%) = 10% + 2.5% − 9% = +3.5%. It is not the most likely single outcome (+5%, the modal case); it's the probability-weighted balance point."
/>

## Variance and standard deviation: the spread

**Analogy.** Two archers can have the same *average* — both centered on the bullseye — yet one peppers the whole target while the other clusters tight. **Variance** measures that scatter: the average squared distance of each outcome from the mean. Its square root, the **standard deviation**, brings the units back to plain returns and is what finance calls **volatility**.

**Definition.** With mean $\mu = \mathbb{E}[X]$,

$$
\mathrm{Var}(X) = \mathbb{E}\!\left[(X - \mu)^2\right], \qquad \sigma = \sqrt{\mathrm{Var}(X)}.
$$

We square the deviations for two reasons: it stops positive and negative gaps from cancelling, and it punishes big misses far more than small ones. The square root then undoes the squaring so $\sigma$ is back in percent.

**Worked example.** A stock's five monthly returns are:

| Month | Return |
|---|---|
| 1 | +4% |
| 2 | −2% |
| 3 | +6% |
| 4 | −4% |
| 5 | +6% |

First the mean: $(4 - 2 + 6 - 4 + 6)/5 = 10/5 = +2\%$.

Now the squared deviations from that 2% mean:

| Return | Deviation | Squared |
|---|---|---|
| +4% | +2 | 4 |
| −2% | −4 | 16 |
| +6% | +4 | 16 |
| −4% | −6 | 36 |
| +6% | +4 | 16 |

Sum of squares $= 4 + 16 + 16 + 36 + 16 = 88$. Treating these five as the *whole population*, divide by $n = 5$: $\mathrm{Var} = 88/5 = 17.6$ (in %²). The volatility is $\sigma = \sqrt{17.6} \approx 4.2\%$ per month. The bigger the typical swing from 2%, the bigger that number.

The wider bell below has the larger $\sigma$ — same average return, more risk:

<ReturnDistribution
  client:visible
  title="Volatility is the width of the bell, not its center"
  lowVolLabel="Low volatility (small σ)"
  highVolLabel="High volatility (large σ)"
  meanLabel="Same expected return"
  sigmaLabel="Volatility (σ)"
  caption="Both distributions share the same expected return, marked by the dashed line. The wider one simply spreads its outcomes further from the mean — and that spread is exactly the standard deviation we just computed."
/>

<MCQ
  client:visible
  question="Why does the variance formula square each deviation from the mean before averaging?"
  options={[
    { text: 'So positive and negative deviations do not cancel, and large misses are penalized more', correct: true },
    { text: 'Because squaring makes the units easier to read off directly' },
    { text: 'To guarantee the variance is always negative' },
    { text: 'Squaring has no effect; it is purely cosmetic' },
  ]}
  explanation="Without squaring, deviations above and below the mean would cancel to roughly zero. Squaring keeps them positive and weights big deviations heavily. The trade-off is that variance is in squared units — which is exactly why we take the square root to get σ back in plain return units."
/>

## Population vs sample: the n−1 correction

**Analogy.** When you estimate spread from a *sample* rather than the full population, you've already spent some of the data computing the sample mean — and the data always hugs *its own* mean a little too snugly. Dividing by $n$ would therefore underestimate the true spread. The fix is to divide by $n - 1$ instead of $n$: you've "used up" one degree of freedom pinning down the mean, so you have only $n - 1$ independent pieces of spread information left.

**Definition.** The two variance formulas differ only in the denominator:

$$
\sigma^2_{\text{pop}} = \frac{1}{n}\sum_i (x_i - \mu)^2, \qquad s^2_{\text{sample}} = \frac{1}{n-1}\sum_i (x_i - \bar{x})^2.
$$

The $n - 1$ version (called **Bessel's correction**) is the *unbiased* estimator of the true variance — on average across many samples it lands on the right answer rather than systematically low.

**Worked example.** Reuse the five returns above. The sum of squared deviations was 88.

- **As a population** ($\div\,5$): $\sigma^2 = 88/5 = 17.6$, so $\sigma \approx 4.20\%$.
- **As a sample** ($\div\,4$): $s^2 = 88/4 = 22.0$, so $s \approx 4.69\%$.

The sample number is larger — exactly the upward nudge Bessel's correction provides to undo the built-in underestimate. With only five data points the gap is sizable; with hundreds of daily returns, $n$ and $n - 1$ barely differ.

<Callout variant="info" title="Which one does my software use?">
Most finance tools — and the default in spreadsheets' STDEV / numpy's <code>ddof=1</code> conventions — use the *sample* ($n - 1$) version, because you almost always have a sample of returns, not the entire infinite population. Reach for the population ($n$) formula only when your data truly is the whole universe you care about.
</Callout>

<FillBlank
  client:visible
  question="Fill in the logic of the sample-variance correction."
  text="To estimate variance from a sample we divide the sum of squared deviations by {{n − 1|n|n + 1}}, a fix called {{Bessel's|Newton's|Sharpe's}} correction. It exists because the sample data clusters too tightly around its own mean, so dividing by n alone would {{underestimate|overestimate|exactly match}} the true spread."
  explanation="Sample variance uses n − 1 (Bessel's correction). One degree of freedom is consumed estimating the mean, so dividing by n would systematically underestimate the population variance; n − 1 makes the estimator unbiased."
  checkLabel="Check"
  retryLabel="Try again"
/>

## Annualizing volatility: the √252 rule

**Analogy.** Volatility, like risk over time, grows like a random walk, not a straight march. For independent returns, *variance* adds across periods — so over $T$ periods the variance is $T$ times the one-period variance, and the standard deviation is $\sqrt{T}$ times the one-period $\sigma$. To turn a daily volatility into an annual one, you scale by the square root of the number of trading days in a year (about 252).

**The rule.** With roughly 252 trading days per year,

$$
\sigma_{\text{annual}} = \sigma_{\text{daily}}\sqrt{252}.
$$

Note $\sqrt{252} \approx 15.87$, a number worth memorizing: annual vol is daily vol times roughly 16.

**Worked example.** A stock has a daily volatility of $1\%$. Then

$$
\sigma_{\text{annual}} = 1\% \times \sqrt{252} \approx 1\% \times 15.87 = 15.87\%.
$$

Going the other way, an index quoted at $20\%$ annual vol has a daily vol of $20\% / \sqrt{252} \approx 1.26\%$. And to annualize from *monthly* data you'd use $\sqrt{12}$ instead, because there are twelve months in a year — the rule generalizes to $\sqrt{(\text{periods per year})}$.

<Callout variant="warning" title="What the √t rule quietly assumes">
Square-root scaling rides on returns being **independent and identically distributed** with **zero autocorrelation** — today's move tells you nothing about tomorrow's. Real returns bend this: under momentum (trending) moves persist and true risk is *higher* than √252 says; under mean reversion they reverse and it's *lower*. Volatility clustering (calm and stormy stretches) also breaks the i.i.d. assumption. Treat √252 as a clean first approximation, not gospel.
</Callout>

<MCQ
  client:visible
  question="A strategy has a daily return standard deviation of 2%. Its approximate annualized volatility (252 trading days) is:"
  options={[
    { text: 'About 31.7%', correct: true },
    { text: 'About 504%' },
    { text: 'About 2%' },
    { text: 'About 24%' },
  ]}
  explanation="σ_annual = 2% × √252 ≈ 2% × 15.87 = 31.7%. Multiplying by 252 itself (504%) is the classic error — variance scales with time, but σ scales with the square root of time. The √12 (≈24%) would be the monthly-to-annual factor, not the daily one."
/>

## Skewness: the third moment (lopsidedness)

**Analogy.** A symmetric bell is a balanced see-saw. **Skewness** measures which way it tips. A long tail stretching to the *right* (rare big gains) is **positive skew**; a long tail to the *left* (rare big losses) is **negative skew**. The mean gets dragged toward the long tail, away from the bulk of typical outcomes.

**Definition.** Skewness is the standardized third moment:

$$
\mathrm{Skew}(X) = \mathbb{E}\!\left[\left(\frac{X - \mu}{\sigma}\right)^3\right].
$$

Cubing keeps the *sign* of each deviation (unlike squaring), so the formula registers which side carries the heavier, more extreme outcomes. Zero skew means perfectly symmetric, like the normal distribution.

**What the sign means for returns.** This is where skew earns its keep:

- **Negative skew** — frequent small gains, occasional brutal crashes. Most equity indices, credit, and short-volatility strategies live here. Investors *dislike* it: the rare event is a disaster.
- **Positive skew** — frequent small losses, occasional huge wins. Lottery tickets, far out-of-the-money call options, and trend-following live here.

<Callout variant="info" title="Skew you've already met">
A **lognormal** price distribution from last lesson is *positively skewed*: a price can't fall below zero (loss capped at −100%) but can rise without limit, so the right tail runs long. Yet equity *return* distributions are typically *negatively* skewed, because crashes happen faster and sharper than rallies. Same asset, opposite-signed skew depending on whether you look at price levels or returns — a distinction worth keeping straight.
</Callout>

<MCQ
  client:visible
  question="An equity index shows many small positive returns and a few large negative crashes. Its return distribution is best described as:"
  options={[
    { text: 'Negatively skewed — a long left tail of rare large losses', correct: true },
    { text: 'Positively skewed — a long right tail of rare large gains' },
    { text: 'Zero skew — perfectly symmetric like the normal' },
    { text: 'Undefined, because returns cannot be skewed' },
  ]}
  explanation="Frequent small gains plus occasional severe losses create a long left tail — negative skewness. That left tail is precisely why investors fear equity drawdowns: the bad outcomes, though rare, are extreme."
/>

## Kurtosis and excess kurtosis: the fourth moment (fat tails)

**Analogy.** Two distributions can share the same mean, the same variance, *and* the same symmetry, yet one still serves up far more extreme outcomes than the other. **Kurtosis** measures that — how much of the action lives in the tails versus the shoulders. High kurtosis means a sharper peak *and* fatter tails: lots of tiny moves punctuated by rare giant ones.

**Definition.** Kurtosis is the standardized fourth moment:

$$
\mathrm{Kurt}(X) = \mathbb{E}\!\left[\left(\frac{X - \mu}{\sigma}\right)^4\right].
$$

The fourth power weights extreme deviations enormously — a $4\sigma$ event contributes $4^4 = 256$ times as much as a $1\sigma$ one — so kurtosis is dominated by the tails. The normal distribution has a kurtosis of exactly **3**, so it's convenient to subtract that benchmark:

$$
\text{excess kurtosis} = \mathrm{Kurt}(X) - 3.
$$

**Reading the sign.** Excess kurtosis is the fat-tail meter:

- **Excess kurtosis &gt; 0** (*leptokurtic*) — fatter tails than the normal. Extreme moves are more frequent and larger than a bell curve predicts. **Real financial returns live here**, often with excess kurtosis well above zero.
- **Excess kurtosis = 0** — exactly normal-tailed (*mesokurtic*).
- **Excess kurtosis &lt; 0** (*platykurtic*) — thinner tails, more uniform-looking outcomes.

<Callout variant="warning" title="This is the fat-tails problem, quantified">
Last lesson you saw that the normal distribution badly underestimates extreme market moves. Excess kurtosis is the *number* that proves it: daily equity returns routinely show large positive excess kurtosis, meaning $5\sigma$ and $6\sigma$ days that a normal model calls once-in-a-millennium freaks actually arrive every few years. Any risk model built on a kurtosis-3 bell curve — like parametric VaR — will systematically understate how often the worst days happen.
</Callout>

This is the spaced-recall payoff: the **lognormal** prices and **fat tails** from last lesson aren't a separate topic — they're just a distribution's third and fourth moments talking. The chart above lets you widen σ; below, drag the same intuition onto where the *mass* sits.

<MatchConcepts
  client:visible
  question="Match each moment to what it measures about a return distribution."
  pairs={[
    { term: 'Expectation (1st moment)', definition: 'Where the distribution is centered — its average return' },
    { term: 'Variance / σ (2nd moment)', definition: 'How wide the spread of outcomes is — volatility' },
    { term: 'Skewness (3rd moment)', definition: 'Which way the distribution leans — its lopsidedness' },
    { term: 'Kurtosis (4th moment)', definition: 'How heavy the tails are — frequency of extreme moves' },
    { term: 'Excess kurtosis above zero', definition: 'Fatter tails than the normal bell curve' },
  ]}
  explanation="The four moments answer four questions in order: where (mean), how wide (variance), how lopsided (skewness), how tail-heavy (kurtosis). Positive excess kurtosis flags the fat tails that wreck normal-based risk models."
  checkLabel="Check"
  retryLabel="Try again"
  termsLabel="Moments"
  definitionsLabel="What it measures"
/>

<MCQ
  client:visible
  question="A return series has an excess kurtosis of +4. Compared with a normal distribution of the same mean and variance, it has:"
  options={[
    { text: 'Fatter tails — extreme moves are more frequent than the bell curve predicts', correct: true },
    { text: 'Thinner tails — extreme moves are rarer than normal' },
    { text: 'Exactly normal tails, since excess kurtosis only affects the mean' },
    { text: 'A higher average return' },
  ]}
  explanation="Positive excess kurtosis (leptokurtic) means fatter tails than the normal: big surprises arrive more often and larger. It says nothing about the mean — that's the first moment. This is the statistical signature of the fat tails behind real market crashes."
/>

## Putting it together

Any return distribution, however messy, can be summarized by four moments: the **expectation** (where it's centered, and it glides through sums via linearity), the **variance / volatility** (how wide it spreads, scaled across time by √t), the **skewness** (which way it leans), and the **kurtosis** (how fat its tails are). Use the $n - 1$ sample correction when you're estimating from data, and remember that real returns are negatively skewed and fat-tailed — exactly the shape a tidy normal model refuses to admit.

<MindMap
  client:visible
  title="The four moments — a return distribution in four numbers"
  caption="Location, spread, lopsidedness, tail-heaviness: master these and you can describe any return series — and spot exactly where the normal model lies."
  root={{
    label: 'Moments of a distribution',
    children: [
      {
        label: '1st: Expectation (μ)',
        children: [
          { label: 'Probability-weighted average' },
          { label: 'E[X] = Σ pᵢ xᵢ' },
          { label: 'Linear: E[aX+b] = aE[X]+b' },
          { label: 'Not a prediction of one draw' },
        ],
      },
      {
        label: '2nd: Variance & σ',
        children: [
          { label: 'Var = E[(X−μ)²]; σ = √Var' },
          { label: 'σ is volatility — the spread' },
          { label: 'Sample uses n−1 (Bessel)' },
          { label: 'Annualize: σ·√252' },
        ],
      },
      {
        label: '3rd: Skewness',
        children: [
          { label: 'Standardized cube — keeps sign' },
          { label: 'Negative = long left tail (crashes)' },
          { label: 'Positive = long right tail (lottery)' },
          { label: 'Equity returns: negatively skewed' },
        ],
      },
      {
        label: '4th: Kurtosis',
        children: [
          { label: 'Standardized 4th power — tail-heavy' },
          { label: 'Normal kurtosis = 3' },
          { label: 'Excess = Kurt − 3' },
          { label: 'Excess > 0 → fat tails (real returns)' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: expectation, variance & moments"
  scoreLabel="You scored"
  checkLabel="Check"
  questions={[
    {
      question: 'A bet returns +40% with probability 0.3 and −10% with probability 0.7. Its expected return is:',
      options: [
        { text: '+5%', correct: true },
        { text: '+15%' },
        { text: '+40%' },
        { text: '−10%' },
      ],
      explanation: 'E[X] = 0.3(40%) + 0.7(−10%) = 12% − 7% = +5%. The expectation is the probability-weighted balance point, not either single outcome.',
    },
    {
      question: 'By linearity of expectation, if E[X] = 7% then E[3X − 2%] equals:',
      options: [
        { text: '19%', correct: true },
        { text: '21%' },
        { text: '5%' },
        { text: '7%' },
      ],
      explanation: 'E[aX + b] = aE[X] + b = 3(7%) − 2% = 21% − 2% = 19%. Linearity lets you scale and shift the mean without rebuilding the distribution.',
    },
    {
      question: 'Why does sample variance divide by n − 1 rather than n?',
      options: [
        { text: 'One degree of freedom is used estimating the mean, so n would underestimate the true spread', correct: true },
        { text: 'It makes the variance larger purely to be conservative, with no theory behind it' },
        { text: 'Because n − 1 is always a round number' },
        { text: 'To convert variance into standard deviation' },
      ],
      explanation: 'Bessel’s correction: the sample hugs its own mean too tightly, so dividing by n is biased low. Using n − 1 — the remaining degrees of freedom — makes the estimator unbiased.',
    },
    {
      question: 'A stock has a daily volatility of 1.5%. Its approximate annualized volatility is:',
      options: [
        { text: 'About 23.8%', correct: true },
        { text: 'About 378%' },
        { text: 'About 1.5%' },
        { text: 'About 5.2%' },
      ],
      explanation: 'σ_annual = 1.5% × √252 ≈ 1.5% × 15.87 = 23.8%. Multiplying by 252 (378%) wrongly scales variance instead of σ; √252 is the correct time-scaler.',
    },
    {
      question: 'Daily equity returns typically show negative skewness AND positive excess kurtosis. Together this means:',
      options: [
        { text: 'A long left tail of crashes plus tails fatter than the normal — extreme losses are both more frequent and more severe than a bell curve admits', correct: true },
        { text: 'Returns are perfectly symmetric and thin-tailed, exactly normal' },
        { text: 'The average return is guaranteed to be positive' },
        { text: 'Volatility is zero, so there is no risk' },
      ],
      explanation: 'Negative skew = long left (loss) tail; positive excess kurtosis = fatter tails than normal. Combined, they say large losses arrive more often and bigger than a normal model predicts — the core reason parametric, normal-based risk tools understate tail risk.',
    },
  ]}
/>

Next up — **covariance, correlation and regression** — we stop describing one return series in isolation and ask how *two* of them move together: the covariance that drives diversification, the correlation that standardizes it onto a −1-to-+1 scale, and the regression line that turns a cloud of points into an estimate of beta.
