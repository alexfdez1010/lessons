---
title: "Sampling, the CLT & Estimation"
description: "From a sample to the truth: how a statistic estimates a parameter, why the sample mean is itself a random variable, the Central Limit Theorem that makes averages normal, the standard error σ/√n, and confidence intervals — what they do and don't mean."
topic: "en/statistics-for-finance"
order: 5
minutes: 11
updated: 2026-06-07
---

import { Callout, MCQ, Quiz, FillBlank, MindMap, SamplingHistogram } from '@/components/react';

You never get to see the whole truth. You can't observe a strategy's *true* expected return — you only get the handful of months it actually traded. You can't measure a stock's *true* volatility — you get a finite stretch of daily prices and have to guess. All of empirical finance is one long act of squinting at a **sample** and trying to infer the **population** behind it. This lesson is about doing that squinting honestly: how a number you compute from data (a *statistic*) relates to the number you actually care about (a *parameter*), how wrong that number can be, and how to attach an honest margin of error to it.

The punchline is one of the most useful theorems in all of applied math — the Central Limit Theorem — and one of the most *misquoted* objects in finance: the confidence interval. Let's earn both.

<MCQ
  client:visible
  pretest
  question="You compute the average return of a fund from its 60 monthly observations. That 60-month average is best described as:"
  options={[
    { text: 'An estimate of the fund\'s true (unknown) expected return, computed from a sample', correct: true },
    { text: 'The fund\'s true expected return, known exactly' },
    { text: 'A guess with no relationship to the true expected return' },
    { text: 'The population mean, by definition' },
  ]}
  explanation="The 60-month average is a sample statistic — your best estimate of the population parameter (the true expected return), but not the parameter itself. Collect a different 60 months and you'd get a slightly different average. The whole game is quantifying how far that estimate might be from the truth."
/>

## Population vs sample: estimator and estimand

**Analogy.** The **population** is the entire ocean; your **sample** is the bucket you dipped in. You want to know the ocean's average saltiness (a fixed but unknowable number), so you measure your bucket and hope it's representative. The bucket's saltiness is your *estimate*; the ocean's is the *truth*.

**The vocabulary.** A **parameter** is a fixed, usually unknown number describing the whole population — the true mean $\mu$, the true variance $\sigma^2$. The thing you're trying to learn is called the **estimand**. A **statistic** is any number you compute from your sample — the sample mean $\bar{x}$, the sample variance $s^2$. When a statistic is used to estimate a parameter, we call it an **estimator**, and the specific number it spits out is an **estimate**.

The standard notation is a quiet little contract: Greek letters ($\mu$, $\sigma$, $\rho$) are the *true population* quantities; Latin letters or hatted symbols ($\bar{x}$, $s$, $\hat{\mu}$) are the *sample estimates* of them. The sample mean is

$$
\bar{x} = \frac{1}{n}\sum_{i=1}^{n} x_i,
$$

and it's our estimator of the population mean $\mu$.

**Worked example.** A fund posts monthly returns over five months: $2\%, -1\%, 3\%, 0\%, 1\%$. The sample mean is $\bar{x} = (2 - 1 + 3 + 0 + 1)/5 = 5/5 = 1\%$. That $1\%$ is your *estimate* of the fund's true monthly expected return $\mu$. Is $\mu$ actually $1\%$? Almost certainly not exactly — five months is a thimble of data. But $1\%$ is your best single guess given what you have.

<Callout variant="info" title="Greek = truth, Latin = guess">
A clean habit that saves endless confusion: whenever you see a Greek letter ($\mu$, $\sigma$), read it as "the real, fixed, unknowable population value." Whenever you see its Latin or hatted cousin ($\bar{x}$, $s$, $\hat{\mu}$), read it as "my noisy estimate of that, computed from a finite sample." The estimate dances around; the parameter sits still.
</Callout>

<MCQ
  client:visible
  question="Which pairing of estimator → estimand is correct?"
  options={[
    { text: 'The sample mean x̄ estimates the population mean μ', correct: true },
    { text: 'The population mean μ estimates the sample mean x̄' },
    { text: 'The sample size n estimates the population variance σ²' },
    { text: 'The sample mean x̄ is exactly equal to μ for any sample' },
  ]}
  explanation="An estimator is the sample-computed quantity (x̄) that targets a fixed population parameter (μ). Causality runs sample → truth-estimate, never the reverse, and x̄ equals μ only in the impossible limit of observing the entire population."
/>

## The sampling distribution: your estimate is a random variable

**Analogy.** Imagine you and 999 other analysts each independently dip a bucket into the same ocean and report its saltiness. You'd get 1,000 *different* numbers, scattered around the true value. Plot those numbers as a histogram and you get the **sampling distribution** — the distribution of the estimate itself, across all the samples you *could* have drawn.

**The key idea.** Here's the conceptual leap that trips everyone up the first time: the sample mean $\bar{x}$ is **itself a random variable**. Before you collect data, you don't know what value it'll take — it depends on which particular draws land in your sample. So like any random variable (recall lesson 2), it has its own distribution, its own mean, and its own spread. Drawing a *single* sample gives you *one* draw from that sampling distribution.

Two facts pin down the sampling distribution of the mean when you draw $n$ independent observations from a population with mean $\mu$ and variance $\sigma^2$:

- Its **center** is the true mean: $\mathbb{E}[\bar{x}] = \mu$. On average, the sample mean is right — it doesn't systematically lean high or low.
- Its **spread** *shrinks* as you collect more data: $\mathrm{Var}(\bar{x}) = \sigma^2/n$. Averaging cancels noise.

That second fact is the engine of all of statistics: **more data tightens your estimate**. The histogram of sample means gets narrower and narrower as $n$ grows, closing in on the truth.

<SamplingHistogram
  client:visible
  title="Watch a distribution materialize from random draws"
  normalLabel="Normal (thin tails)"
  fatTailLabel="Fat tails"
  drawLabel="Draw samples"
  samplesLabel="Samples drawn"
  densityLabel="Target density"
  caption="Each press flings thousands of random draws into bins, and the bars climb toward the smooth target density — a sampling distribution assembling itself in front of you. The same machinery that builds this histogram is what builds the sampling distribution of a mean: collect many samples, and their averages pile up into a predictable shape. Flip to fat tails to see how stubbornly the extremes keep landing far out."
/>

<MCQ
  client:visible
  question="Why does collecting more observations make the sample mean a better estimate?"
  options={[
    { text: 'The variance of the sample mean is σ²/n, which shrinks as n grows', correct: true },
    { text: 'A larger sample changes the true population mean μ to be closer to x̄' },
    { text: 'Larger samples make the population variance σ² smaller' },
    { text: 'It doesn\'t — sample size has no effect on estimate quality' },
  ]}
  explanation="The sampling distribution of x̄ has variance σ²/n. The population mean μ and variance σ² are fixed — they don't move. What changes is the spread of your estimate around μ: more data, tighter sampling distribution, better estimate."
/>

## The Central Limit Theorem: averages go normal

**Analogy.** Toss one die and the outcomes are flat — $1$ through $6$ are all equally likely, nothing bell-shaped about it. Now average *thirty* dice. Suddenly the totals near the middle ($\sim 3.5$) are overwhelmingly more common than the extremes, and the histogram of those averages is a gorgeous bell — even though a single die is as un-bell-shaped as it gets. The averaging itself manufactures the bell.

**The theorem.** The **Central Limit Theorem (CLT)** says: if you average $n$ independent, identically distributed (i.i.d.) draws from *almost any* population with finite mean $\mu$ and finite variance $\sigma^2$, then as $n$ grows the sampling distribution of the mean approaches a **normal distribution** — regardless of the parent's shape. Formally,

$$
\bar{x} \;\approx\; \mathcal{N}\!\left(\mu,\; \frac{\sigma^2}{n}\right) \quad\text{for large } n.
$$

The parent can be skewed, flat, lumpy, bimodal — doesn't matter. Average enough i.i.d. draws and the average is approximately normal, centered at $\mu$ with variance $\sigma^2/n$. This is why the normal distribution from lesson 2 shows up *everywhere*: most quantities we care about are secretly sums or averages of many small independent effects.

**Why finance leans on it constantly.** A monthly return is roughly the sum of many daily returns; a daily return is the sum of many tick-by-tick moves. Aggregate enough small shocks and the CLT nudges the aggregate toward normal — which is exactly why so many models (parametric VaR, portfolio theory, regression inference) reach for the bell curve. The CLT is also what lets us put normal-based confidence intervals on a sample mean even when individual returns aren't normal.

<Callout variant="warning" title="The CLT has fine print — and finance loves to violate it">
The CLT needs **finite variance** and **independence**. Real markets bend both. Returns have **fat tails** (lesson 2) so heavy that variance is barely finite, which makes convergence to normal painfully slow in the tails — the very region risk managers care about. And returns **cluster** in volatile regimes (today's big move predicts tomorrow's), breaking independence. So "averages are normal" is a fantastic default and a dangerous absolute. The CLT explains why the bell shows up; it does *not* license you to assume normality in the tail of a fat-tailed, autocorrelated series.
</Callout>

<MCQ
  client:visible
  question="The Central Limit Theorem guarantees that, for large n, the sampling distribution of the mean of i.i.d. draws is approximately normal:"
  options={[
    { text: 'Regardless of the shape of the parent distribution, provided it has finite mean and variance', correct: true },
    { text: 'Only if the parent distribution is itself normal' },
    { text: 'Only if the parent distribution is symmetric' },
    { text: 'For any distribution, even those with infinite variance' },
  ]}
  explanation="The CLT's power is that the parent can be skewed, flat, or lumpy — averaging makes the mean normal anyway. The crucial conditions are i.i.d. draws with finite mean and finite variance. Infinite-variance (extremely fat-tailed) parents break the classical CLT."
/>

## Standard error: the spread of your estimate

**Analogy.** The standard deviation $\sigma$ tells you how much a *single* return bounces around. The **standard error** tells you how much your *estimate of the average* bounces around. One is the wobble of a single dart; the other is the wobble of the bullseye you'd compute by averaging many darts. They're different animals, and conflating them is one of the most common errors in applied statistics.

**Definition.** The **standard error of the mean (SE)** is the standard deviation of the sampling distribution of $\bar{x}$. Since $\mathrm{Var}(\bar{x}) = \sigma^2/n$, taking the square root gives

$$
\mathrm{SE} = \frac{\sigma}{\sqrt{n}}.
$$

In practice you don't know the true $\sigma$, so you plug in the sample standard deviation $s$ and use $\mathrm{SE} \approx s/\sqrt{n}$. The defining feature: SE falls with $\sqrt{n}$, not $n$. To **halve** your standard error you need **four times** the data; to cut it tenfold you need a hundredfold more observations. Precision is expensive.

**Worked example.** A fund's monthly returns have a sample standard deviation $s = 4\%$. How precisely do we know its mean return at different sample sizes?

| Sample size $n$ | $\sqrt{n}$ | $\mathrm{SE} = s/\sqrt{n}$ |
|---|---|---|
| 9 months | $3$ | $4\%/3 \approx 1.33\%$ |
| 36 months | $6$ | $4\%/6 \approx 0.67\%$ |
| 144 months | $12$ | $4\%/12 \approx 0.33\%$ |

Quadrupling the data from 9 to 36 months halves the SE ($1.33\% \to 0.67\%$); quadrupling again to 144 months halves it once more. Note the brutal economics: going from 9 to 144 months — *sixteen* times the data — only shrinks the SE by a factor of $4$. That's the $\sqrt{n}$ tax, and it's why estimating a fund's true edge from a few years of data is so maddeningly imprecise.

<Callout variant="warning" title="SE is not the standard deviation — the #1 confusion">
The standard deviation $s$ describes the spread of the **raw data** (how volatile a single return is). The standard error $s/\sqrt{n}$ describes the spread of your **estimate of the mean**. With 144 monthly returns, the data still has $s = 4\%$ volatility — that never shrinks — but your estimate of the *average* return has SE $\approx 0.33\%$. Reporting the standard deviation when you mean the standard error (or vice versa) overstates or understates your certainty by a factor of $\sqrt{n}$. Always ask: "spread of *what* — the data, or my estimate?"
</Callout>

<FillBlank
  client:visible
  question="Fill in the anatomy of the standard error of the mean."
  text="The standard error equals the population standard deviation divided by the square root of the {{sample size|standard deviation|mean}}. So to cut the standard error in half, you must multiply your sample size by {{four|two|ten}}. Crucially, the standard error describes the spread of your estimate of the {{mean|raw data|median}}, whereas the standard deviation describes the spread of the {{raw data|estimate|sampling distribution}}."
  explanation="SE = σ/√n. Halving SE needs 4× the data because of the square root. SE measures the precision of the estimated mean; the standard deviation measures the variability of the underlying observations themselves — two distinct quantities that differ by a factor of √n."
/>

<MCQ
  client:visible
  question="A strategy's daily returns have a standard deviation of 2%. Across 100 trading days, the standard error of the mean daily return is closest to:"
  options={[
    { text: '0.2%', correct: true },
    { text: '2%' },
    { text: '0.02%' },
    { text: '20%' },
  ]}
  explanation="SE = σ/√n = 2% / √100 = 2% / 10 = 0.2%. The 2% trap answer confuses the standard error (precision of the average) with the standard deviation (volatility of a single day's return)."
/>

## Estimation quality: bias, consistency, efficiency, and MLE

Not all estimators are created equal. Three properties tell you whether a recipe for estimating a parameter is any good.

**Bias — does it aim true?** An estimator is **unbiased** if its sampling distribution is centered on the true parameter: $\mathbb{E}[\hat{\theta}] = \theta$. A *biased* estimator systematically misses high or low no matter how much data you feed it. Classic example: the sample variance divides by $n-1$ rather than $n$ precisely to *remove* a downward bias — dividing by $n$ would systematically underestimate $\sigma^2$.

**Consistency — does it home in?** An estimator is **consistent** if it converges to the true parameter as $n \to \infty$. The sample mean is consistent: its SE is $\sigma/\sqrt{n}$, which goes to zero, so with infinite data it nails $\mu$ exactly. Consistency is the bare-minimum sanity check — an estimator that *doesn't* improve with more data is worthless.

**Efficiency — does it waste data?** Among unbiased estimators, the **efficient** one has the smallest variance — it squeezes the most precision out of each observation. Given two unbiased estimators, prefer the tighter one; it gets you to a given confidence with less data.

**Analogy for all three.** Picture a dartboard. **Bias** is whether your shots cluster *around the bullseye* or off to one side. **Efficiency** is how *tightly* they cluster. **Consistency** is whether throwing more and more darts eventually drags the cluster onto the bullseye. The dream estimator is unbiased (centered), efficient (tight), and consistent (converges).

**Maximum Likelihood (MLE) — the universal recipe.** Where do good estimators come from? The workhorse answer is **Maximum Likelihood Estimation**: pick the parameter values that make the data you actually observed *most probable*. You write down the probability of your observed sample as a function of the unknown parameters — the **likelihood** — and then crank the parameters to whatever maximizes it.

**Intuition.** Suppose you saw returns clustered around $1\%$ with moderate scatter. Of all the normal distributions that *could* have generated that data, which one makes it least surprising? The one centered at $\approx 1\%$ with a width matching the observed scatter. MLE formalizes "the parameters that best explain what I saw" — and for normal data it recovers exactly the sample mean and (a version of) the sample variance. It's the engine behind GARCH volatility models, logistic regression, and most of the estimation in quantitative finance.

<MCQ
  client:visible
  question="An estimator whose expected value equals the true parameter for any sample size is called:"
  options={[
    { text: 'Unbiased', correct: true },
    { text: 'Consistent' },
    { text: 'Efficient' },
    { text: 'Maximum-likelihood' },
  ]}
  explanation="Unbiasedness means E[θ̂] = θ — the estimator is centered on the truth. Consistency is about convergence as n → ∞; efficiency is about having the smallest variance among unbiased estimators; MLE is a method for constructing estimators, not a property like these three."
/>

<MCQ
  client:visible
  question="The core idea of Maximum Likelihood Estimation is to:"
  options={[
    { text: 'Choose the parameter values that make the observed data most probable', correct: true },
    { text: 'Choose the parameter values that make the data least probable' },
    { text: 'Average all possible parameter values with equal weight' },
    { text: 'Assume the parameters are zero and test whether the data rejects that' },
  ]}
  explanation="MLE picks the parameters under which the sample you actually observed is most likely — the values that best 'explain' the data. (Assuming a parameter is zero and testing it is hypothesis testing, the next lesson; averaging parameter values with a prior is the Bayesian approach.)"
/>

## Confidence intervals: a range, and what it really means

**Analogy.** A point estimate is a dart throw; a **confidence interval** is admitting the dart has a spread and drawing a circle around where it landed. Instead of claiming "the true mean is exactly $1\%$" — which is almost surely wrong to the decimal — you say "the true mean is plausibly somewhere in *this band*," and you state how confident the band-drawing *procedure* is.

**Definition.** Leaning on the CLT (which makes $\bar{x}$ approximately normal), a **95% confidence interval** for the mean is

$$
\bar{x} \;\pm\; 1.96 \cdot \mathrm{SE} \;=\; \bar{x} \;\pm\; 1.96 \cdot \frac{\sigma}{\sqrt{n}}.
$$

That $1.96$ is the same magic number from the normal distribution in lesson 2: $95\%$ of a normal's mass sits within $1.96$ standard deviations of its center, leaving $2.5\%$ in each tail. Widen to $99\%$ confidence and you stretch the multiplier to $2.576$; the more confident you insist on being, the *wider* the interval you must accept.

**Worked example.** Our fund had $\bar{x} = 1\%$ monthly. Suppose over $n = 36$ months its SE works out to $0.67\%$ (from $s = 4\%$ earlier). The 95% confidence interval is

$$
1\% \;\pm\; 1.96 \times 0.67\% \;=\; 1\% \pm 1.31\%,
$$

i.e. roughly $[-0.31\%,\; 2.31\%]$. Read that carefully: even after three years of data, we can't rule out that the fund's true monthly edge is *negative*. The interval straddles zero — a sobering reminder of how little a few years of returns actually pins down.

<Callout variant="warning" title="What a 95% CI does NOT mean">
The seductive misreading: "there's a 95% probability the true mean lies in *this* interval." **Wrong.** In classical (frequentist) statistics the true mean $\mu$ is a *fixed number*, not random — it's either in your interval or it isn't, with no probability about it. The $95\%$ describes the **procedure, not your particular interval**: if you repeated the whole experiment many times, $95\%$ of the intervals you'd construct would contain the true $\mu$. *Your one interval* either caught it or missed it. (The statement "95% probability μ is in this interval" *is* valid — but only under the Bayesian credible-interval framework, a different machine entirely.) Quote the procedure, not the single shot.
</Callout>

<MCQ
  client:visible
  question="A 95% confidence interval for a fund's mean return is [0.2%, 1.8%]. Which interpretation is correct?"
  options={[
    { text: 'If we repeated the sampling procedure many times, about 95% of the intervals built this way would contain the true mean', correct: true },
    { text: 'There is a 95% probability that the true mean lies between 0.2% and 1.8%' },
    { text: '95% of the fund\'s monthly returns fall between 0.2% and 1.8%' },
    { text: 'The true mean is definitely between 0.2% and 1.8%' },
  ]}
  explanation="The 95% is a property of the procedure across hypothetical repetitions, not of this single interval — in frequentist statistics the true mean is fixed, so it's either in or out. It also describes the estimate of the mean, not where 95% of individual returns fall (that would use the standard deviation, not the standard error)."
/>

## Putting it together

Every empirical number in finance is an estimate squinting at a hidden truth. A **statistic** computed from a **sample** estimates a fixed population **parameter**; because it depends on which data you happened to draw, the statistic is *itself a random variable* with a **sampling distribution**. The **Central Limit Theorem** tells you that distribution is approximately normal for averages of enough i.i.d. data — which is why the bell curve haunts all of quantitative finance. The **standard error** $\sigma/\sqrt{n}$ measures how tight your estimate is (and is emphatically *not* the standard deviation of the data). Good estimators are unbiased, consistent, and efficient, and **MLE** is the universal recipe for building them. Finally, a **confidence interval** wraps your estimate in an honest margin — as long as you quote what it actually means: a property of the *procedure*, not a probability about your single interval.

<MindMap
  client:visible
  title="Sampling, the CLT and estimation — the whole arc"
  caption="A sample yields a statistic that estimates a parameter; the statistic has its own sampling distribution, made normal by the CLT, with spread σ/√n, wrapped in a confidence interval you must quote carefully."
  root={{
    label: 'Sampling & Estimation',
    children: [
      {
        label: 'Population vs sample',
        children: [
          { label: 'Parameter = fixed truth (μ, σ)' },
          { label: 'Statistic = computed from data (x̄, s)' },
          { label: 'Estimator targets the estimand' },
          { label: 'Greek = truth, Latin = guess' },
        ],
      },
      {
        label: 'Sampling distribution',
        children: [
          { label: 'x̄ is itself a random variable' },
          { label: 'Centered at μ (on average right)' },
          { label: 'Variance σ²/n shrinks with data' },
        ],
      },
      {
        label: 'Central Limit Theorem',
        children: [
          { label: 'Averages of i.i.d. draws → normal' },
          { label: 'Any parent shape, finite mean & variance' },
          { label: 'Why the bell curve is everywhere' },
          { label: 'Fails for fat tails & dependence' },
        ],
      },
      {
        label: 'Standard error',
        children: [
          { label: 'SE = σ/√n' },
          { label: 'Half the SE → 4× the data' },
          { label: 'NOT the data\'s standard deviation' },
        ],
      },
      {
        label: 'Estimation quality',
        children: [
          { label: 'Bias: centered on the truth?' },
          { label: 'Consistency: converges as n → ∞?' },
          { label: 'Efficiency: smallest variance?' },
          { label: 'MLE: make the data most probable' },
        ],
      },
      {
        label: 'Confidence interval',
        children: [
          { label: 'x̄ ± 1.96·SE for 95%' },
          { label: 'Wider for higher confidence' },
          { label: 'Property of the procedure, not the interval' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: sampling, the CLT and estimation"
  questions={[
    {
      question: 'A number computed from a sample to estimate a fixed population quantity is called:',
      options: [
        { text: 'An estimator (its value is an estimate of the parameter)', correct: true },
        { text: 'A parameter' },
        { text: 'The estimand' },
        { text: 'The population' },
      ],
      explanation: 'The estimator is the sample-computed recipe (e.g. x̄); the parameter/estimand is the fixed population target (e.g. μ). Greek letters mark the truth, Latin/hatted letters mark the estimate.',
    },
    {
      question: 'The Central Limit Theorem says the sampling distribution of the mean of i.i.d. draws becomes approximately normal:',
      options: [
        { text: 'For large n, whatever the shape of the parent — as long as it has finite mean and variance', correct: true },
        { text: 'Only when the parent distribution is already normal' },
        { text: 'Only for samples drawn from a uniform distribution' },
        { text: 'For any parent, including infinite-variance fat-tailed ones' },
      ],
      explanation: 'The CLT works for almost any parent shape, but it requires i.i.d. draws with finite mean and finite variance. Infinite-variance parents (extreme fat tails) break the classical theorem.',
    },
    {
      question: 'A return series has standard deviation 3%. Over 225 observations, the standard error of the mean is:',
      options: [
        { text: '0.2%', correct: true },
        { text: '3%' },
        { text: '0.013%' },
        { text: '1.5%' },
      ],
      explanation: 'SE = σ/√n = 3% / √225 = 3% / 15 = 0.2%. The 3% answer confuses the standard error (precision of the average) with the standard deviation (volatility of a single observation).',
    },
    {
      question: 'You want to cut your standard error from 0.4% to 0.2%. Holding σ fixed, you must:',
      options: [
        { text: 'Quadruple the sample size', correct: true },
        { text: 'Double the sample size' },
        { text: 'Halve the sample size' },
        { text: 'Increase the sample size by 1.96×' },
      ],
      explanation: 'Because SE = σ/√n, halving SE requires multiplying n by 4 (since √4 = 2). The square-root law makes precision expensive: every extra digit of accuracy costs a hundredfold more data.',
    },
    {
      question: 'A 95% confidence interval for a mean is [0.5%, 2.5%]. The correct reading is:',
      options: [
        { text: '95% of intervals built by this procedure across repeated samples would contain the true mean', correct: true },
        { text: 'There is a 95% probability the true mean is between 0.5% and 2.5%' },
        { text: '95% of individual returns fall between 0.5% and 2.5%' },
        { text: 'The sample mean has a 5% chance of being wrong' },
      ],
      explanation: 'The 95% is a frequentist property of the procedure, not a probability about this one interval (the true mean is fixed — in or out). It concerns the estimated mean, not the spread of individual returns, which would use the standard deviation instead of the standard error.',
    },
    {
      question: 'Maximum Likelihood Estimation chooses parameter values that:',
      options: [
        { text: 'Make the observed data most probable', correct: true },
        { text: 'Minimize the sample size needed' },
        { text: 'Force the estimator to be unbiased by construction' },
        { text: 'Set every parameter to its prior mean' },
      ],
      explanation: 'MLE maximizes the likelihood of the data you actually saw — the parameters that best explain the sample. It does not guarantee unbiasedness (MLEs can be biased), and using a prior is the Bayesian, not the maximum-likelihood, approach.',
    },
  ]}
/>

Next up — **hypothesis testing** — we stop merely *estimating* a number and start *interrogating* it: is this fund's positive average return real edge, or just noise that a confidence interval can't rule out? We'll meet the null and alternative hypotheses, the p-value, the two distinct ways to be wrong, and the finance-specific trap that quietly invalidates most backtests: testing a thousand strategies and crowning the luckiest.
