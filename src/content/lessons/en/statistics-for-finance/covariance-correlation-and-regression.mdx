---
title: "Covariance, Correlation & Regression"
description: "How two assets move together — covariance and its standardized cousin correlation — and how a least-squares line turns that relationship into beta, alpha, and R², with the correlation-is-not-causation traps that ruin careers."
topic: "en/statistics-for-finance"
order: 4
minutes: 12
updated: 2026-06-07
---

import { Callout, MCQ, Quiz, FillBlank, MatchConcepts, Categorize, Reveal, MindMap, CorrelationBlender, BetaScatter } from '@/components/react';

The last lesson handed you the tools to describe *one* random variable: its mean, its variance, its skew and kurtosis. But finance is rarely about one thing in isolation. The whole game — hedging, diversification, factor models, pairs trading — turns on a single deeper question: **when this moves, what does that do?** Do two stocks rise together or trade off? Does your portfolio lurch when the market lurches, and by how much? This lesson is the machinery for answering that. We start with **covariance** (a raw measure of co-movement), standardize it into **correlation** (the readable version), then fit a **regression line** through a cloud of points to extract the two numbers every quant memorizes — **beta** and **alpha** — and finish with the trap that has humbled more analysts than any other: **correlation is not causation**, and it only ever sees *straight lines*.

<MCQ
  client:visible
  pretest
  question="At the highest level, what does covariance measure?"
  options={[
    { text: 'The tendency of two variables to move together — same direction, opposite directions, or unrelated', correct: true },
    { text: 'The average value of a single variable' },
    { text: 'How fat the tails of one distribution are' },
    { text: 'The probability that a portfolio loses money' },
  ]}
  explanation="Covariance is a two-variable statistic: it captures whether X and Y tend to be above (or below) their means at the same time. Positive means they move together, negative means they move oppositely, near zero means no linear relationship. The mean and tail-fatness are single-variable (univariate) ideas from the previous lesson."
/>

## Covariance: do two things move together?

**Analogy.** Picture two friends on a seesaw. If they bob up and down *in sync* — both leaning back at the same moment — they're positively coupled. If one rises exactly as the other drops, they're negatively coupled. If they wobble at random with no relationship, they're uncoupled. Covariance is the number that scores which of these you've got.

**Precise definition.** For two random variables $X$ and $Y$ with means $\mu_X$ and $\mu_Y$, covariance is the *expected product of their deviations from their own means*:

$$
\mathrm{Cov}(X,Y) = E\big[(X-\mu_X)(Y-\mu_Y)\big].
$$

Read the sign off the product inside. When $X$ is above its mean *and* $Y$ is above its mean, both deviations are positive, so their product is positive. When both are below their means, the product of two negatives is again positive. Only when one is above while the other is below do you get a negative product. Average all those products:

- **Positive covariance** — they're usually on the same side of their means together (move together).
- **Negative covariance** — when one is up, the other tends to be down (move oppositely).
- **Near-zero covariance** — no consistent linear pull either way.

Notice that $\mathrm{Cov}(X,X) = E[(X-\mu_X)^2] = \mathrm{Var}(X)$: covariance of a variable with *itself* is just its variance from the previous lesson. Variance is the special case where the "two things" are the same thing.

**Worked example.** Take two assets' returns over four periods (a tiny paired series so the arithmetic is visible):

| Period | Asset X return | Asset Y return |
|---|---|---|
| 1 | $+2\%$ | $+1\%$ |
| 2 | $-1\%$ | $-2\%$ |
| 3 | $+3\%$ | $+4\%$ |
| 4 | $-4\%$ | $-3\%$ |

First the means: $\mu_X = (2 - 1 + 3 - 4)/4 = 0\%$ and $\mu_Y = (1 - 2 + 4 - 3)/4 = 0\%$ — both average to zero, which keeps the deviations equal to the raw numbers. Now multiply the paired deviations period by period and average them (dividing by $n=4$ for a population covariance):

| Period | $X-\mu_X$ | $Y-\mu_Y$ | product |
|---|---|---|---|
| 1 | $+2$ | $+1$ | $+2$ |
| 2 | $-1$ | $-2$ | $+2$ |
| 3 | $+3$ | $+4$ | $+12$ |
| 4 | $-4$ | $-3$ | $+12$ |

Sum of products $= 2 + 2 + 12 + 12 = 28$, so $\mathrm{Cov}(X,Y) = 28/4 = 7$ (in units of percent-squared). Every product came out positive — in each period both assets sat on the *same* side of their means — so the covariance is solidly positive: these two move together.

<Callout variant="info" title="Covariance has ugly units">
That answer, "7 percent-squared," is the dirty secret of covariance: its units are the product of the two variables' units, so they're rarely interpretable. Is $7$ a strong relationship or a weak one? You genuinely cannot tell from the number alone — it depends entirely on how volatile X and Y each are. A covariance of $7$ between two wild assets might be trivial; between two calm ones it might be enormous. That unit problem is exactly what correlation fixes next.
</Callout>

<MCQ
  client:visible
  question="Two assets have a covariance of −0.0008. What does the negative sign tell you?"
  options={[
    { text: 'When one asset is above its average return, the other tends to be below its average — they move in opposite directions', correct: true },
    { text: 'Both assets are losing money on average' },
    { text: 'The two assets are uncorrelated and independent' },
    { text: 'The second asset has negative variance' },
  ]}
  explanation="A negative covariance means the deviation products are negative on average: one asset above its mean tends to coincide with the other below its mean. It says nothing about whether either asset's mean return is positive or negative, and variance can never be negative."
/>

## Correlation: covariance you can actually read

**Analogy.** Covariance tells you the *direction* of the seesaw coupling but mixes in how big each kid is — a tiny twitch from a heavy kid and a huge swing from a light one can produce the same raw number. **Correlation** weighs everyone on the same scale. It strips out the magnitudes and leaves a pure, dimensionless score of *how tightly* the two move, always between $-1$ and $+1$.

**Precise definition.** Correlation is covariance divided by the product of the two standard deviations:

$$
\rho_{XY} = \frac{\mathrm{Cov}(X,Y)}{\sigma_X\,\sigma_Y}.
$$

Dividing by $\sigma_X\sigma_Y$ cancels the messy units (percent-squared on top, percent-squared on the bottom) and rescales everything into the fixed band $-1 \le \rho \le +1$. Now the number means something on its own:

- $\rho = +1$ — perfect positive linear relationship; the points fall exactly on an upward line.
- $\rho = -1$ — perfect negative linear relationship; exactly on a downward line.
- $\rho = 0$ — no *linear* relationship (crucial caveat — hold that thought for the final section).
- In between, $|\rho|$ measures *tightness*: $0.9$ is a tight cloud hugging a line, $0.2$ is a loose, barely-tilted smear.

**Worked example (continuing the four-period series).** We found $\mathrm{Cov}(X,Y) = 7$. Now the standard deviations. For X, the squared deviations are $4, 1, 9, 16$, summing to $30$; the variance is $30/4 = 7.5$, so $\sigma_X = \sqrt{7.5} \approx 2.74\%$. For Y, the squared deviations are $1, 4, 16, 9$, also summing to $30$, giving $\sigma_Y = \sqrt{7.5} \approx 2.74\%$ as well. Then

$$
\rho = \frac{7}{2.74 \times 2.74} = \frac{7}{7.5} \approx 0.933.
$$

A correlation of $0.93$ — very high. The raw covariance of "$7$" was uninterpretable, but once standardized it screams: these two assets are almost lockstep. *That's* why desks quote correlation, not covariance, when they talk about how assets relate.

Drag the controls below to feel how correlation reshapes a portfolio's risk — at $\rho = +1$ the blend's volatility is just the weighted average of its parts, but as $\rho$ falls the real risk drops *below* that line and the diversification benefit opens up:

<CorrelationBlender
  client:visible
  title="Watch correlation blend two volatilities"
  correlationLabel="Correlation (ρ)"
  weightLabel="Weight in Asset A"
  portfolioVolLabel="Portfolio volatility"
  weightedAvgLabel="Naive weighted average (no diversification)"
  benefitLabel="Diversification benefit"
  assetALabel="Asset A"
  assetBLabel="Asset B"
  caption="At ρ = +1 the portfolio's risk is exactly the weighted average of the two volatilities. For every ρ below 1 the real risk sinks below that benchmark — the gap is the diversification benefit, and it widens as ρ heads toward −1."
/>

<FillBlank
  client:visible
  question="Fill in the bounds and meaning of the correlation coefficient."
  text="Correlation is covariance divided by the product of the two {{standard deviations|variances|means}}, which rescales it into the fixed range from {{−1|0|−∞}} to {{+1|+100|+∞}}. A value near {{+1|0|−1}} means the two variables move tightly in the same direction."
  explanation="ρ = Cov(X,Y) / (σ_X·σ_Y). Dividing by the two standard deviations cancels the units and bounds ρ in [−1, +1]. A value near +1 is a tight positive relationship; near −1 a tight negative one; near 0, no linear relationship."
/>

<Categorize
  client:visible
  question="Sort each scenario by the sign of the correlation you'd expect between the two quantities."
  buckets={['Positive correlation', 'Negative correlation', 'Near-zero correlation']}
  items={[
    { text: 'A stock and a call option written on that same stock', bucket: 'Positive correlation' },
    { text: 'Two large-cap stocks in the same industry', bucket: 'Positive correlation' },
    { text: 'A stock portfolio and a put option used to hedge it', bucket: 'Negative correlation' },
    { text: 'Bond prices and the interest rate that just rose', bucket: 'Negative correlation' },
    { text: 'Daily returns of a stock and the daily temperature in a faraway city', bucket: 'Near-zero correlation' },
    { text: "A fair die's roll and the previous independent roll", bucket: 'Near-zero correlation' },
  ]}
  explanation="A call option moves with its underlying (positive), as do same-industry peers. A protective put and rising rates each push opposite to the thing they're paired with (negative). Unrelated quantities — a stock vs. distant weather, or two independent dice — have no consistent linear link (near zero)."
/>

## Simple linear regression: drawing the best line

**Analogy.** You've got a scatterplot — a cloud of dots, one per period, market return on the horizontal axis and your portfolio return on the vertical. Now imagine threading a single straight rod through that cloud. Tilt it and slide it until it sits "most fairly" in the middle of the dots. That rod is the regression line, and the rule for "most fairly" is what makes it precise.

**Precise definition.** Simple linear regression models one variable as a straight-line function of another, plus noise:

$$
y = \alpha + \beta x + \varepsilon.
$$

Here $x$ is the **predictor** (e.g. the market's return), $y$ is the **response** (your asset's return), $\alpha$ (alpha) is the **intercept** — where the line crosses the vertical axis — and $\beta$ (beta) is the **slope** — how many units $y$ rises per unit of $x$. The $\varepsilon$ is the **error term**: the vertical gap between each actual point and the line.

**Least squares — the rule for "best."** There are infinitely many lines you could draw. Ordinary Least Squares (OLS) picks the one and only line that **minimizes the sum of the squared vertical residuals** — the squared gaps between each dot and the line:

$$
\min_{\alpha,\beta}\;\sum_i \big(y_i - \alpha - \beta x_i\big)^2.
$$

Why *squared* gaps rather than the gaps themselves? Squaring makes every miss positive (so overshoots and undershoots can't cancel), and it punishes big misses far more than small ones — a residual of $4$ contributes $16$, while four residuals of $1$ contribute only $4$. The line that wins this contest is unique, and it has a beautifully simple slope, which is the whole reason regression matters in finance.

<Callout variant="tip" title="Why vertical gaps, not perpendicular ones?">
OLS minimizes *vertical* distances, not the shortest perpendicular distance to the line. That's deliberate: we're trying to *predict* $y$ from $x$, so the error that matters is how wrong our prediction of $y$ is — a purely vertical miss. This asymmetry is also why regressing $y$ on $x$ gives a different line than regressing $x$ on $y$; the two are not the same fit run backwards.
</Callout>

<MCQ
  client:visible
  question="Ordinary Least Squares chooses the line that minimizes the sum of the squared residuals. Why square them?"
  options={[
    { text: 'Squaring keeps every residual positive so they cannot cancel, and penalizes large misses disproportionately', correct: true },
    { text: 'Squaring makes the line pass exactly through every data point' },
    { text: 'Because residuals are always negative and squaring fixes the sign only' },
    { text: 'To guarantee the slope equals exactly 1' },
  ]}
  explanation="Squaring serves two purposes: it makes overshoots and undershoots both positive (so they don't cancel out to a misleadingly small total) and it weights big errors much more heavily than small ones. It does not force the line through every point — residuals generally remain — and it certainly doesn't fix the slope at 1."
/>

## Beta and alpha: the slope is sensitivity, the intercept is skill

**Analogy.** Think of beta as a *gearing ratio* between your asset and the market. A beta of $1$ means your asset is locked in the same gear as the market — the market moves $1\%$, you move $1\%$. A beta of $1.5$ is a higher gear: every $1\%$ market wiggle becomes a $1.5\%$ lurch in you. A beta of $0.5$ is a sluggish low gear, half as reactive. Alpha, meanwhile, is the return you earn *even when the market does nothing* — the line's height at $x=0$.

**Beta as the regression slope.** When you regress an asset's returns on the market's returns, the OLS slope has a clean closed form — the covariance of the asset with the market, divided by the market's variance:

$$
\beta = \frac{\mathrm{Cov}(\text{asset},\,\text{market})}{\mathrm{Var}(\text{market})}.
$$

Stare at that and you'll see it's just a *scaled correlation*: it's the co-movement (covariance) renormalized by how volatile the market itself is. This is the exact $\beta$ from the **Capital Asset Pricing Model (CAPM)** you may have met — the measure of how much *systematic* (undiversifiable, market-wide) risk a stock carries. High beta, high market exposure; low beta, defensive.

**Alpha as the intercept.** Once the slope is pinned down, the line is forced to pass through the point of averages $(\bar{x}, \bar{y})$, which fixes the intercept:

$$
\alpha = \bar{y} - \beta\,\bar{x}.
$$

In CAPM language, $\alpha$ is the holy grail: the *excess* return a manager delivers that the market exposure alone can't explain. A persistently positive alpha is evidence of genuine skill (or an unmeasured risk you're being paid for). Most managers, net of fees, have an alpha indistinguishable from zero — which is the entire case for index funds.

**Worked example.** Suppose over a sample the asset's covariance with the market is $\mathrm{Cov} = 0.012$ and the market's variance is $\mathrm{Var} = 0.008$. Then $\beta = 0.012 / 0.008 = 1.5$ — an aggressive, high-gear stock. If the asset averaged $\bar{y} = 1.2\%$ per period while the market averaged $\bar{x} = 0.7\%$, then $\alpha = 1.2\% - 1.5 \times 0.7\% = 1.2\% - 1.05\% = 0.15\%$ per period of return the market exposure doesn't account for.

Toggle the presets below to watch the fitted line rotate as beta changes, and lift it to see alpha in action — the slope is how hard the cloud swings with the market, the intercept is the freebie return at zero market move:

<BetaScatter
  client:visible
  title="Reading alpha and beta off the regression line"
  marketAxisLabel="Market return"
  portfolioAxisLabel="Portfolio return"
  betaLabel="Beta (slope)"
  alphaLabel="Alpha (intercept)"
  defensiveLabel="Defensive (β≈0.5)"
  marketNeutralLabel="Tracks market (β≈1.0)"
  aggressiveLabel="Aggressive (β≈1.5)"
  caption="The slope of the fitted line is beta — the asset's sensitivity to the market. The line's height where the market return is zero is alpha — the return earned independent of the market. Steeper line, harder swings; higher line, more skill."
/>

<MatchConcepts
  client:visible
  question="Match each regression statistic to what it actually means."
  pairs={[
    { term: 'Covariance', definition: 'Raw, unit-dependent measure of whether two variables move together' },
    { term: 'Correlation (ρ)', definition: 'Standardized co-movement bounded between −1 and +1' },
    { term: 'Beta (β)', definition: 'Regression slope — sensitivity of the asset to the market' },
    { term: 'Alpha (α)', definition: 'Regression intercept — return earned independent of the market' },
    { term: 'R²', definition: 'Fraction of the variable’s variance the line explains' },
  ]}
  explanation="Covariance is the raw (ugly-unit) co-movement; correlation standardizes it into [−1, 1]; beta is the slope (market sensitivity); alpha is the intercept (market-independent return); and R² is the share of variance the fitted line accounts for."
/>

## R² and residuals: how good is the line?

**Analogy.** You've drawn the best possible line — but "best available" doesn't mean "good." A perfect line threads every dot exactly; a useless line is a flat smear through a shapeless blob. **R-squared** ($R^2$) scores where on that spectrum your line lands: it's the fraction of the wiggle in $y$ that the line successfully accounts for.

**Residuals.** A **residual** is the leftover for one point: the vertical distance from the actual data point to the line, $e_i = y_i - \hat{y}_i$, where $\hat{y}_i = \alpha + \beta x_i$ is the line's prediction. Residuals are what OLS squares and minimizes; they're the part of $y$ the line *failed* to explain. A point sitting right on the line has a residual of zero; a point far above or below has a large one.

**R² as variance explained.** Total variation in $y$ splits into two buckets — the part the line explains and the part left in the residuals. $R^2$ is the explained share:

$$
R^2 = 1 - \frac{\sum_i (y_i - \hat{y}_i)^2}{\sum_i (y_i - \bar{y})^2} = \frac{\text{variance explained by the line}}{\text{total variance in } y}.
$$

It runs from $0$ to $1$:

- $R^2 = 1$ — the line explains *everything*; every residual is zero, all points dead on the line.
- $R^2 = 0$ — the line explains *nothing*; you'd predict $y$ just as well with its flat average $\bar{y}$.
- $R^2 = 0.7$ — the line accounts for $70\%$ of the variation in $y$; the remaining $30\%$ lives in the residuals (idiosyncratic noise the market factor doesn't capture).

**The beautiful link to correlation.** For a *simple* (one-predictor) regression, $R^2$ is exactly the square of the correlation: $R^2 = \rho^2$. Our four-period assets had $\rho \approx 0.933$, so a regression of one on the other would post $R^2 \approx 0.933^2 \approx 0.87$ — the line would explain about $87\%$ of the variation. That tidy identity is why $\rho$ and $R^2$ are two views of the same relationship: correlation tells you the *direction and tightness*, and squaring it tells you the *share of variance explained*.

<Callout variant="info" title="A low R² isn't always a bad model">
In finance, single-stock regressions on the market routinely post an $R^2$ of $0.2$–$0.4$ — most of a single stock's movement is idiosyncratic, not market-driven, and that's reality, not a modeling failure. A low $R^2$ honestly reports "the market explains only part of this stock." The danger runs the other way: a suspiciously *high* $R^2$ on noisy financial data is often a sign of overfitting or a spurious relationship, which is exactly where the next section bites.
</Callout>

<MCQ
  client:visible
  question="A regression of a stock's returns on the market posts an R² of 0.30. The single best interpretation is:"
  options={[
    { text: 'About 30% of the variation in the stock’s returns is explained by the market; the other 70% is idiosyncratic', correct: true },
    { text: 'The stock returns 30% per year' },
    { text: 'The model is wrong because R² should always exceed 0.9' },
    { text: 'The correlation between the stock and the market is 0.30' },
  ]}
  explanation="R² is the share of the response's variance the line explains — here 30%, leaving 70% as residual (stock-specific) variation. It is not a return figure. And since R² = ρ² in a simple regression, the correlation here is √0.30 ≈ 0.55, not 0.30."
/>

<Reveal client:visible buttonLabel="Spaced recall — link back to lesson 3">
**Question:** In the slope formula $\beta = \mathrm{Cov}(\text{asset}, \text{market}) / \mathrm{Var}(\text{market})$, what is the denominator, and how did the previous lesson define it?

**Answer:** The denominator is the **variance** of the market — the average squared deviation of the market's return from its own mean, $E[(X-\mu)^2]$, introduced as the second central moment in the moments lesson. Its square root is the market's **standard deviation** (volatility). So beta is literally the asset–market covariance *rescaled* by how much the market itself varies: divide co-movement by the market's own spread and you get a clean sensitivity number. Variance from one lesson ago is doing the heavy lifting here.
</Reveal>

## The big trap: correlation is not causation (and only sees lines)

**Analogy.** Ice-cream sales and drowning deaths rise together every summer with a fat positive correlation. Does ice cream drown people? No — a hidden third variable, *hot weather*, drives both. Correlation faithfully reports that two things move together; it is utterly silent on *why*. Reading causation into a correlation is the single most expensive mistake in quantitative finance.

**Spurious correlations.** With enough series to ransack, you can always find two that move together by sheer coincidence — divorce rates and margarine consumption, the S&P 500 and butter production in Bangladesh. These are *spurious*: real, measurable correlations with zero causal link. In markets this is a constant menace because data-mining thousands of candidate signals against a price series will inevitably surface "predictors" that worked beautifully in-sample and evaporate the moment you trade them. A correlation earns the right to be called a relationship only when there's a *mechanism* and it *survives out-of-sample*.

**Correlation only captures *linear* association.** Even setting causation aside, $\rho$ has a second blind spot: it measures *straight-line* relatedness only. Two variables can be tightly, deterministically related and still post a correlation of zero. The classic case: let $Y = X^2$ with $X$ symmetric around zero. As $X$ swings from negative to positive, $Y$ traces a perfect U-shape — a flawless relationship — yet for every positive deviation in $X$ there's an equal-and-opposite negative one producing the *same* $Y$, so the covariance products cancel to zero. Correlation reports "no relationship" about a relationship you could draw with your eyes closed. A near-zero $\rho$ means *no linear* link — it does **not** mean independent.

<Callout variant="warning" title="The two ways correlation lies">
**It lies about cause:** a strong correlation can be pure coincidence (spurious) or driven by a hidden third variable — it never, by itself, proves X moves Y. **And it lies about shape:** ρ = 0 only rules out a *straight-line* relationship, not a curved one (like Y = X²) or a threshold effect. Always plot the scatter before you trust a single correlation number. A coefficient with no picture and no mechanism is a rumor, not a finding.
</Callout>

<MCQ
  client:visible
  question="Which statements about correlation are true? (Select all that apply.)"
  allowMultiple
  options={[
    { text: 'A high correlation can arise from a hidden third variable rather than one causing the other', correct: true },
    { text: 'A correlation of zero rules out a linear relationship but not a curved one', correct: true },
    { text: 'A strong correlation between two series proves one causes the other' },
    { text: 'Two variables with a perfect U-shaped relationship always have correlation near +1' },
  ]}
  explanation="Correlation can be driven by a lurking common cause (the ice-cream/drowning trap), and ρ = 0 only excludes a *linear* link, leaving curved relationships (like Y = X²) invisible. Correlation never proves causation, and a U-shaped relationship produces a correlation near zero — the up and down halves cancel — not near +1."
/>

## Putting it together

Covariance asks the foundational two-variable question — do these move together? — but answers in uninterpretable units, so we standardize it into **correlation**, a clean score in $[-1, +1]$. Push a straight line through the scatter with **least squares** and you extract **beta** (the slope, = $\mathrm{Cov}/\mathrm{Var}_{\text{market}}$, your market sensitivity and CAPM systematic risk) and **alpha** (the intercept, your market-independent return). **R²** ($=\rho^2$ in a simple regression) reports how much of the variation the line actually explains, with the unexplained part living in the **residuals**. And looming over all of it: correlation is *not* causation, and it only ever sees straight lines — so plot the cloud and demand a mechanism before you bet a dollar on a coefficient.

<MindMap
  client:visible
  title="Covariance, correlation & regression — the whole picture"
  caption="From raw co-movement (covariance) to a readable score (correlation) to a fitted line (beta, alpha, R²), with the causation-and-linearity traps that void every coefficient taken on faith."
  root={{
    label: 'Co-movement & regression',
    children: [
      {
        label: 'Covariance',
        children: [
          { label: 'Cov(X,Y) = E[(X−μx)(Y−μy)]' },
          { label: 'Sign: + together, − opposite, ≈0 none' },
          { label: 'Cov(X,X) = Var(X)' },
          { label: 'Ugly units → hard to read' },
        ],
      },
      {
        label: 'Correlation (ρ)',
        children: [
          { label: 'ρ = Cov / (σx·σy)' },
          { label: 'Bounded −1 … +1, dimensionless' },
          { label: '|ρ| = tightness of the line' },
        ],
      },
      {
        label: 'Regression: y = α + βx + ε',
        children: [
          { label: 'OLS minimizes squared residuals' },
          { label: 'Vertical gaps, not perpendicular' },
          { label: 'Unique best-fit line' },
        ],
      },
      {
        label: 'Beta & alpha',
        children: [
          { label: 'β = Cov(asset,mkt)/Var(mkt) = slope' },
          { label: 'β = systematic risk (CAPM)' },
          { label: 'α = intercept = market-independent return' },
        ],
      },
      {
        label: 'R² & residuals',
        children: [
          { label: 'R² = variance explained by line' },
          { label: 'R² = ρ² in simple regression' },
          { label: 'Residual = actual − predicted' },
        ],
      },
      {
        label: 'The traps',
        children: [
          { label: 'Correlation ≠ causation' },
          { label: 'Hidden third variable / spurious' },
          { label: 'ρ sees LINEAR only (Y=X² → ρ≈0)' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: covariance, correlation & regression"
  questions={[
    {
      question: 'Covariance of a variable with itself, Cov(X, X), equals:',
      options: [
        { text: 'The variance of X', correct: true },
        { text: 'The mean of X' },
        { text: 'Always 1' },
        { text: 'The standard deviation of X' },
      ],
      explanation: 'Cov(X, X) = E[(X−μ)(X−μ)] = E[(X−μ)²] = Var(X). Variance is just the self-covariance special case from the previous lesson.',
    },
    {
      question: 'Why do practitioners quote correlation rather than covariance when describing how two assets relate?',
      options: [
        { text: 'Correlation is dimensionless and bounded in [−1, +1], so its size is directly interpretable', correct: true },
        { text: 'Correlation can be negative but covariance cannot' },
        { text: 'Covariance ignores the direction of the relationship' },
        { text: 'Correlation is always larger than covariance' },
      ],
      explanation: 'Covariance carries the product of the two variables units, so its magnitude means nothing on its own. Dividing by σ_X·σ_Y standardizes it into the readable [−1, +1] band. Both can be negative, and both encode direction.',
    },
    {
      question: 'An asset has Cov(asset, market) = 0.018 and the market has Var = 0.009. Its beta is:',
      options: [
        { text: '2.0 — an aggressive, high-sensitivity asset', correct: true },
        { text: '0.5 — a defensive asset' },
        { text: '0.162 — a tiny exposure' },
        { text: '1.0 — it tracks the market exactly' },
      ],
      explanation: 'β = Cov(asset, market) / Var(market) = 0.018 / 0.009 = 2.0. A beta of 2 means the asset tends to swing twice as hard as the market — high systematic risk.',
    },
    {
      question: 'A simple regression posts a correlation ρ = 0.6 between asset and market. Its R² is:',
      options: [
        { text: '0.36 — the line explains about 36% of the variation', correct: true },
        { text: '0.60 — R² always equals ρ' },
        { text: '0.77 — R² is the square root of ρ' },
        { text: '1.20 — R² is twice ρ' },
      ],
      explanation: 'In a simple (one-predictor) regression R² = ρ². Here ρ = 0.6, so R² = 0.36: the fitted line accounts for about 36% of the variance in the asset’s returns, leaving 64% as residual idiosyncratic noise.',
    },
    {
      question: 'Two variables follow Y = X² with X symmetric around zero. Their correlation is closest to:',
      options: [
        { text: '0, because correlation captures only linear association and the U-shape cancels', correct: true },
        { text: '+1, because Y is perfectly determined by X' },
        { text: '−1, because squaring flips the sign' },
        { text: '+0.5, a moderate positive link' },
      ],
      explanation: 'Although Y is perfectly determined by X, the relationship is curved, not linear. For every positive deviation in X there is an equal negative one giving the same Y, so the covariance products cancel and ρ ≈ 0. A zero correlation rules out a *linear* link, never a curved one.',
    },
    {
      question: 'A backtest finds a 0.85 correlation between a fund’s returns and an obscure unrelated index. Before trading on it you should most worry that:',
      options: [
        { text: 'The correlation may be spurious — coincidental or driven by a hidden third variable, with no mechanism behind it', correct: true },
        { text: 'Correlation that high is mathematically impossible' },
        { text: 'A high correlation guarantees the relationship will persist out of sample' },
        { text: 'Correlation proves the index causes the fund’s returns' },
      ],
      explanation: 'Data-mining many candidate series inevitably surfaces high but spurious correlations with no causal mechanism, which vanish out of sample. Correlation never proves causation, and a high in-sample value guarantees nothing about the future — demand a mechanism and out-of-sample evidence.',
    },
  ]}
/>

Next, the **Central Limit Theorem and estimation** — why the average of many messy random variables collapses toward a tidy normal curve, how that single fact justifies the standard-error formulas behind every confidence interval and t-stat, and how we move from describing a sample to *inferring* something trustworthy about the population it came from.
