---
title: "Hypothesis Testing & Significance"
description: "How quants decide whether an edge is real: null vs alternative hypotheses, the t-test and the test statistic, the p-value (and its rampant misreading), Type I/II errors and power, plus finance's deadliest trap — data snooping — and why statistical significance is not economic relevance."
topic: "en/statistics-for-finance"
order: 6
minutes: 12
updated: 2026-06-07
---

import { Callout, MCQ, Quiz, FillBlank, MatchConcepts, Categorize, Reveal, MindMap, SamplingHistogram, TsOverfitCurve } from '@/components/react';

A backtest spits out a number: your strategy made 0.4% a month on average. Glorious — except a coin-flipping monkey would *also* post some average, and sometimes a flattering one, by sheer luck. The whole job of **hypothesis testing** is to stand between you and that monkey: it forces you to answer one ruthless question before you celebrate. *If my edge were actually zero, how often would random noise alone hand me a result this good?* If the answer is "all the time," you've discovered nothing. If it's "almost never," you might — *might* — have something real.

This is the capstone of the statistics course. Everything before it — distributions, the normal, expectation and variance, the standard error and the Central Limit Theorem — was building the machinery you'll now point at a single, career-defining question: **is this edge real, or is it luck?**

<MCQ
  client:visible
  pretest
  question="The core purpose of a hypothesis test is to:"
  options={[
    { text: 'Decide whether the data is surprising enough to reject the assumption that there is no effect', correct: true },
    { text: 'Prove that your hypothesis is definitely true' },
    { text: 'Compute the average return of a strategy' },
    { text: 'Guarantee a strategy will be profitable in the future' },
  ]}
  explanation="A test assumes 'no effect' (the null) and asks how surprising the observed data would be under that assumption. It never proves a hypothesis true and it makes no profit guarantee — it only quantifies how hard the data fights the null."
/>

## The logic of testing: assume nothing, then look for surprise

**Analogy.** A hypothesis test is a courtroom. The defendant — your strategy's edge — is **presumed innocent of having any effect**. "Innocent" here means *the true edge is zero*; that presumption is the **null hypothesis** $H_0$. The prosecution's claim, the **alternative hypothesis** $H_1$, is that there *is* an effect. You don't get to declare guilt because the strategy "looks profitable." You convict only if the evidence is so improbable under innocence that "it was just luck" stops being credible.

**Definitions.**

- **Null hypothesis $H_0$** — the boring, no-effect baseline. For a strategy: *the true mean return is $0$*. For a regression coefficient: *the true slope is $0$*. We assume it's true and try to embarrass it.
- **Alternative hypothesis $H_1$** — the interesting claim that there *is* an effect (mean $\neq 0$, slope $\neq 0$). A **two-sided** $H_1$ says "different from zero in either direction"; a **one-sided** $H_1$ says "specifically greater than zero" (or specifically less).

The asymmetry is the whole point. We never *prove* $H_1$; we only **reject $H_0$** when the data is too surprising to square with it — or **fail to reject** when it isn't. "Fail to reject" is an acquittal, not a declaration of innocence: absence of evidence isn't evidence of absence.

<Callout variant="info" title="Why assume the thing you're hoping is false?">
Assuming the null lets you actually compute something. "There's no edge" is a precise, single-numbered world (mean $= 0$) where you can calculate exactly how the data should scatter. "There's *some* edge" is a vague cloud of infinitely many possible effect sizes — you can't compute a probability against it. So you pin down the null, measure how weird your data looks inside it, and let that weirdness do the talking.
</Callout>

<MCQ
  client:visible
  question="Your strategy posts a positive average return, but the test 'fails to reject the null.' This means:"
  options={[
    { text: 'The data is not surprising enough to rule out luck — you have not shown the edge is real', correct: true },
    { text: 'You have proven the strategy has exactly zero edge' },
    { text: 'The strategy is guaranteed to lose money' },
    { text: 'The null hypothesis has been proven true' },
  ]}
  explanation="Failing to reject is an acquittal, not a proof of innocence. The positive average could easily be luck; you simply lack the evidence to claim a real edge. It does not prove the edge is zero or that you'll lose."
/>

## The test statistic and the t-test: turning an edge into a z-distance

**Analogy.** Raw averages aren't comparable — a 0.4% monthly edge is impressive for a sleepy bond fund and trivial for a wild crypto strategy, because the *noise* differs. A **test statistic** rescales the edge into a universal unit: *how many standard errors away from zero is it?* It's the z-score idea from the normal-distribution lesson, applied to an estimate instead of a single observation.

**Definition.** For a sample of $n$ returns with mean $\bar{x}$ and standard deviation $s$, recall from the sampling lesson that the **standard error** of the mean is $\mathrm{SE} = s / \sqrt{n}$. The **t-statistic** for testing $H_0:\ \mu = 0$ is

$$
t = \frac{\bar{x} - 0}{s / \sqrt{n}} = \frac{\bar{x}}{\mathrm{SE}}.
$$

In words: *the edge, measured in standard errors.* A big $|t|$ means the estimate sits far out in the tail of the "no-effect" distribution — the data is surprising if $H_0$ is true. By the Central Limit Theorem, for a decent sample size $\bar{x}$ is approximately normal, so $t$ behaves like a standard normal (the t-distribution it's named after just adds slightly fatter tails for small $n$). A rough rule of thumb: $|t| > 2$ is the classic "significant at roughly 5%" threshold.

**Worked example.** A strategy has a monthly mean return of $\bar{x} = 0.8\%$ with a monthly standard deviation of $s = 4\%$, measured over $n = 36$ months.

| Step | Computation | Result |
|---|---|---|
| Standard error | $\mathrm{SE} = 4\% / \sqrt{36} = 4\% / 6$ | $0.667\%$ |
| t-statistic | $t = 0.8\% / 0.667\%$ | $1.20$ |

A t-stat of $1.20$ is *below* $2$ — the edge is barely more than one standard error from zero. Verdict: **not significant**. With this much month-to-month noise and only three years of data, an average of 0.8% is the kind of thing luck produces routinely. Now watch what more data does: keep the same $\bar{x}$ and $s$ but stretch to $n = 144$ months (12 years). Then $\mathrm{SE} = 4\%/12 = 0.333\%$ and $t = 0.8\%/0.333\% = 2.40$ — now significant. **The edge didn't change; the standard error shrank because $n$ grew.** That $\sqrt{n}$ in the denominator is doing all the work.

<SamplingHistogram
  client:visible
  title="The null distribution and its tails"
  normalLabel="Thin-tailed null (Normal)"
  fatTailLabel="Fat-tailed reality"
  drawLabel="Draw samples"
  samplesLabel="Samples drawn"
  densityLabel="Sampling density"
  caption="Under the null, your t-statistic is a draw from this bell centered at zero. A result far out in the tail is 'surprising'. But real returns are fat-tailed: extreme t-stats happen far more often than the thin-tailed bell predicts — so a 't above 2' is less rare, and less convincing, than the textbook normal claims. Toggle the fat-tailed view and watch the edges fill in."
/>

<FillBlank
  client:visible
  question="Fill in the mechanics of the t-statistic."
  text="The t-statistic equals the estimate divided by its {{standard error|standard deviation|variance}}. Holding the mean and standard deviation fixed, collecting more data makes the t-statistic {{larger|smaller|unchanged}}, because the standard error shrinks with {{the square root of n|n|n squared}}."
  explanation="t = estimate / SE, and SE = s/√n. More data shrinks SE (by √n), so the same edge yields a larger t — which is exactly why long, clean samples are so prized: they let a small-but-real edge clear the significance bar."
/>

<MCQ
  client:visible
  question="A strategy's mean monthly return is 0.5% with standard deviation 3% over 36 months. Its t-statistic for H₀: μ = 0 is closest to:"
  options={[
    { text: 'About 1.0', correct: true },
    { text: 'About 3.0' },
    { text: 'About 6.0' },
    { text: 'About 0.17' },
  ]}
  explanation="SE = 3% / √36 = 3% / 6 = 0.5%. So t = 0.5% / 0.5% = 1.0 — well below 2, not significant. The trap answers come from forgetting to divide s by √n (using s directly gives the 0.17 mis-step, or skipping the SE entirely)."
/>

## The p-value: the probability of data this extreme — under the null

**Analogy.** The p-value is your **surprise meter**. Crank the machine assuming the null is true, then read off: *how often would pure noise produce a result at least as extreme as the one I actually got?* A tiny p-value means "noise basically never does this" — strong evidence against the null. A big p-value means "noise does this all the time" — shrug.

**Definition.** The **p-value** is the probability, *computed assuming $H_0$ is true*, of observing a test statistic at least as extreme as the one you got. You compare it to a pre-chosen **significance level $\alpha$** (the threshold you'll accept for crying "real," conventionally $0.05$):

- If $p \leq \alpha$: **reject $H_0$** — call the result *statistically significant*.
- If $p > \alpha$: **fail to reject $H_0$**.

A t-stat of $2.0$ corresponds to a two-sided $p \approx 0.046$; a t-stat of $1.20$ (our first worked example) to $p \approx 0.23$ — i.e. noise would reproduce that "edge" about 23% of the time. Of course it wasn't significant.

<Callout variant="warning" title="The single most abused number in statistics">
The p-value is **NOT** the probability that the null is true. Read that again. $p = 0.03$ does **not** mean "there's a 3% chance there's no edge" or "a 97% chance the strategy works." It means: *if the edge were truly zero, data this extreme would show up 3% of the time.* It's $P(\text{data} \mid H_0)$, never $P(H_0 \mid \text{data})$ — the conditional is flipped. Confusing the two is the **prosecutor's fallacy**, and it has talked more traders into doomed strategies than any other single error.
</Callout>

A second, subtler trap: **statistical significance is a yes/no verdict at your chosen $\alpha$, not a measure of effect size.** A microscopic, useless edge can be "highly significant" with enough data, and a huge, valuable edge can be "not significant" with too little. The p-value answers "is it distinguishable from zero?" — never "is it big?"

<MCQ
  client:visible
  question="A study reports p = 0.02 for a trading signal. Which interpretation is correct?"
  options={[
    { text: 'If the signal truly had no edge, data this extreme would occur about 2% of the time', correct: true },
    { text: 'There is a 2% probability that the signal has no edge' },
    { text: 'There is a 98% probability the signal is profitable' },
    { text: 'The signal will be profitable 98% of the time it trades' },
  ]}
  explanation="p = P(data this extreme | null is true) = 2%. It is NOT P(null is true) and says nothing about future profitability or win rate. Flipping the conditional is the prosecutor's fallacy."
/>

<MatchConcepts
  client:visible
  question="Match each piece of testing vocabulary to what it actually means."
  pairs={[
    { term: 'Null hypothesis H₀', definition: 'The no-effect baseline we assume true and try to reject (e.g. mean return = 0)' },
    { term: 'Alternative hypothesis H₁', definition: 'The claim that there is a real effect (mean ≠ 0)' },
    { term: 'p-value', definition: 'P(data at least this extreme | the null is true)' },
    { term: 'Significance level α', definition: 'The pre-chosen threshold a p-value must clear to count as significant' },
    { term: 'Test statistic t', definition: 'The estimate rescaled into standard errors from zero' },
  ]}
  explanation="H₀ is the baseline we attack; H₁ is the effect we hope for; the p-value is the probability of the data under H₀ (not of H₀ given the data); α is the line in the sand; and t is the edge measured in standard errors."
/>

## Type I and Type II errors, and the power to tell them apart

**Analogy.** Back in the courtroom: two ways to blow the verdict. Convict an innocent defendant (declare an edge that isn't there) — a **Type I error**, a *false positive*. Or acquit a guilty one (miss an edge that's real) — a **Type II error**, a *false negative*. You can't drive both to zero at once: a jury that never convicts the innocent also lets more of the guilty walk.

**Definitions.**

- **Type I error (false positive), rate $\alpha$** — rejecting $H_0$ when it's actually true. You "found" an edge that's pure noise. You *set* this rate: choosing $\alpha = 0.05$ means accepting a 5% false-positive rate per test.
- **Type II error (false negative), rate $\beta$** — failing to reject $H_0$ when $H_1$ is actually true. A real edge slipped through undetected.
- **Statistical power $= 1 - \beta$** — the probability of *correctly* detecting a real effect. High power = a sensitive test that rarely misses genuine edges.

**The trade-off.** Tighten $\alpha$ (demand more proof before convicting — say $\alpha = 0.01$) and you cut false positives, but you also make it harder to detect real effects, so $\beta$ rises and power falls. The cure for *both* is the same lever from the last two lessons: **more data**. A larger $n$ shrinks the standard error, which sharpens the whole machine — you can hold $\alpha$ low *and* gain power. There's no free lunch except sample size.

| Reality → / Your verdict ↓ | $H_0$ true (no edge) | $H_1$ true (real edge) |
|---|---|---|
| **Reject $H_0$** ("edge!") | Type I error — false positive (rate $\alpha$) | Correct — power ($1-\beta$) |
| **Fail to reject** ("nothing") | Correct — true negative | Type II error — false negative (rate $\beta$) |

<Categorize
  client:visible
  question="Sort each finance scenario by which error it represents."
  buckets={["Type I (false positive)", "Type II (false negative)"]}
  items={[
    { text: 'You greenlight a strategy whose real edge is zero; it was a lucky backtest', bucket: 'Type I (false positive)' },
    { text: 'You reject a regression signal that is, in truth, a genuine predictor', bucket: 'Type II (false negative)' },
    { text: 'A momentum factor that actually works fails your test on a too-short sample', bucket: 'Type II (false negative)' },
    { text: 'A risk model flags a "regime shift" that was just ordinary noise', bucket: 'Type I (false positive)' },
    { text: 'You discard a real but small alpha because your test lacked the power to see it', bucket: 'Type II (false negative)' },
  ]}
  explanation="Type I = acting on an edge that isn't there (false alarm). Type II = missing an edge that is there (a miss). Underpowered tests on short samples drive Type II errors; loose thresholds and lucky backtests drive Type I."
  instructions="Place each scenario in its error bucket."
  checkLabel="Check"
  retryLabel="Try again"
  explanationLabel="Explanation"
  allCorrectLabel="✓ All sorted"
  partialResultWord="correct"
/>

<MCQ
  client:visible
  question="A risk team tightens its significance threshold from α = 0.05 to α = 0.01 with the sample size unchanged. Holding everything else fixed, this will:"
  options={[
    { text: 'Reduce false positives (Type I) but raise false negatives (Type II), lowering power', correct: true },
    { text: 'Reduce both Type I and Type II errors at once' },
    { text: 'Raise power while also cutting false positives' },
    { text: 'Have no effect on either error rate' },
  ]}
  explanation="With n fixed, α and β trade off: demanding stronger evidence cuts false positives but makes real effects harder to detect, so β rises and power (1−β) falls. The only way to improve both at once is more data."
/>

## The finance trap: data snooping, p-hacking, and backtest overfitting

**Analogy.** Buy one lottery ticket and winning is a miracle. Buy a million tickets and *someone's* ticket wins — but that winner isn't skilled, just selected. Test one strategy at $\alpha = 0.05$ and a false positive is a 1-in-20 fluke. Test **1,000** strategies on the same data and, by pure luck, *about 50 of them* will clear the 5% bar with zero real edge. Cherry-pick the best-looking one, publish its glorious backtest, and you've "discovered" noise. This is **data snooping** — and it is the single most lethal mistake in quantitative finance.

**The mechanism.** Each test has a 5% false-positive rate *in isolation*. But significance was never designed to survive being run a thousand times and reporting only the winner. The probability that *at least one* of $m$ independent true-null tests fires a false positive is $1 - (1 - \alpha)^m$. For $\alpha = 0.05$:

| Strategies tested $m$ | P(at least one false "discovery") |
|---|---|
| 1 | $0.05$ |
| 10 | $1 - 0.95^{10} \approx 0.40$ |
| 50 | $1 - 0.95^{50} \approx 0.92$ |
| 1,000 | $\approx 1.00$ (virtually certain) |

By 50 tries it's a coin flip *against* you; by 1,000 a false discovery is essentially guaranteed. This is why a single dazzling backtest, divorced from the count of how many strategies were tried to find it, is nearly worthless evidence. The same disease wears other names: **p-hacking** (tweaking the rule, the window, the universe until $p$ slips under $0.05$) and **backtest overfitting** (adding parameters until the strategy memorizes the history's noise).

<TsOverfitCurve
  client:visible
  title="Backtest soars, live trading collapses"
  complexityLabel="Strategies tried / parameters added"
  inSampleLabel="In-sample (backtest)"
  outSampleLabel="Out-of-sample (live)"
  performanceLabel="Apparent edge"
  caption="Crank the dial — try more variants, add more parameters — and the in-sample backtest keeps climbing as it fits the noise. But the out-of-sample (live) curve rises to a sweet spot, then falls off a cliff: past it you're mining quirks that won't repeat. The widening gap is overfitting you'll pay for in real money."
/>

**The defenses.** The fix is to make the bar *harder* the more you searched. The crude-but-honest **Bonferroni** correction divides your threshold by the number of tests: testing $m$ strategies, demand $p \leq \alpha/m$ for each (so $1{,}000$ strategies at an overall $5\%$ means each must clear $p \leq 0.00005$ — a t-stat near $4$, not $2$). Practitioners also use the **deflated Sharpe ratio**, which discounts a strategy's Sharpe for the number of trials it took to find and for non-normal returns, and they insist on **out-of-sample** validation: hold back data the strategy never saw, because noise that was memorized in-sample won't reappear out-of-sample.

<Callout variant="tip" title="Always ask: how many did you try?">
A backtest's t-statistic is meaningless without its denominator of attempts. A t-stat of 2 from the *first and only* idea you tested is real evidence. The same t-stat of 2 from the *best of 500* ransacked variants is almost certainly noise — you'd expect dozens of t-stats above 2 from 500 dead strategies by luck alone. The honest question is never "is this significant?" but "is this significant *given how hard I searched*?"
</Callout>

<Reveal client:visible>
**Spaced recall — back to the standard error.** Why does a longer backtest make a real edge easier to prove, and a false one *harder* to fake? Think before you peek.

The t-statistic is $t = \bar{x} / \mathrm{SE}$ with $\mathrm{SE} = s/\sqrt{n}$. As $n$ grows, the standard error shrinks like $1/\sqrt{n}$ (the Central Limit Theorem result from the sampling lesson). For a **real** edge, $\bar{x}$ stays put while SE falls, so $t$ climbs and significance becomes easier. For a **fake** edge, the lucky $\bar{x}$ itself tends to wash toward zero as more data dilutes the fluke — so the false signal *decays* with sample size. More data is the great honest filter: it amplifies truth and erodes luck. That's exactly why out-of-sample testing on fresh data is the data-snooper's nemesis.
</Reveal>

<MCQ
  client:visible
  question="A quant tests 200 strategies at α = 0.05 and reports the single best one, with p = 0.04, as a 'significant discovery.' The biggest problem is:"
  options={[
    { text: 'With 200 tests, roughly 10 false positives are expected by chance, so the cherry-picked winner is likely noise', correct: true },
    { text: 'p = 0.04 is above the 0.05 threshold, so it is not significant' },
    { text: 'The standard error was computed incorrectly' },
    { text: 'Nothing — p < 0.05 means the discovery is solid' },
  ]}
  explanation="At α = 0.05, about 200 × 0.05 = 10 of the 200 tests fire false positives by pure luck. Reporting only the best survivor without correcting for the 200 attempts (e.g. Bonferroni p ≤ 0.05/200 = 0.00025) is textbook data snooping. The p-value itself is fine; the multiplicity is the crime."
/>

## Significance is not economic relevance

**Analogy.** A bathroom scale precise enough to detect a single grain of rice can *prove* you gained weight after a sneeze — a real, statistically significant change that no one would ever act on. In markets, a tiny edge can be unmistakably real and still utterly worthless, because **the market charges admission**: bid-ask spreads, commissions, slippage, and market impact all skim the return before it reaches your pocket.

**The point.** Statistical significance answers *"is this edge distinguishable from zero?"* Economic relevance answers a completely different question: *"is what's left after trading costs worth trading?"* With enough data, a $0.01\%$ monthly edge can post a t-stat of $5$ — bulletproof significance — and yet vanish the instant you subtract a $0.05\%$ round-trip cost. Significant, and net-negative.

**Worked example.** A high-turnover signal earns a gross edge of $0.04\%$ per trade, established over a huge sample with a t-stat of $6$ — about as statistically real as evidence gets.

| Quantity | Value |
|---|---|
| Gross edge per trade | $+0.04\%$ |
| Round-trip transaction cost | $-0.06\%$ |
| **Net edge per trade** | $-0.02\%$ |

The edge is real, significant, and **loses money on every trade** once costs bite. A practitioner who stops at the p-value ships a strategy that's statistically flawless and financially suicidal. Always run the second test: *significant, sure — but does it survive costs, capacity, and turnover?*

<Callout variant="warning" title="Two tests, not one">
Significance and profitability are different hurdles, and a strategy must clear **both**. The p-value tells you the edge is unlikely to be luck; it tells you nothing about whether the edge is *large enough* to beat the spread, the commission, and the slippage. Quant graveyards are full of strategies with gorgeous t-statistics and negative net returns. "Statistically significant" is a starting gun, not a finish line.
</Callout>

<MCQ
  client:visible
  question="A signal's gross edge is highly statistically significant (t = 5) but tiny in size. What's the right conclusion?"
  options={[
    { text: 'It may still be unprofitable after trading costs — significance does not imply economic relevance', correct: true },
    { text: 'A high t-stat guarantees the strategy will be profitable' },
    { text: 'A tiny edge can never be statistically significant' },
    { text: 'Transaction costs are irrelevant once significance is established' },
  ]}
  explanation="Significance only says the edge is distinguishable from zero. A tiny edge can be bulletproof-significant yet net-negative after spreads, commissions and slippage. You must test economic relevance — net of costs — separately."
/>

## Putting it together

Hypothesis testing is the discipline that separates an edge from a fluke. Assume the null (no effect), rescale your estimate into a t-statistic ($t = \bar{x}/\mathrm{SE}$), and read off a p-value: *how often noise alone would fake a result this extreme.* Reject the null only when that p clears your $\alpha$ — never mistaking the p-value for the probability the null is true. Respect the $\alpha/\beta$ trade-off and chase power with more data. Then survive the two finance-specific gauntlets: **data snooping** — correct for every strategy you tried, because 1,000 tests breed ~50 lucky "discoveries" — and **economic relevance** — a significant edge still has to beat the costs. Significance is where the analysis starts, not where it ends.

<MindMap
  client:visible
  title="Hypothesis testing — the whole machine"
  caption="Assume the null, measure surprise with a t-stat and p-value, mind the two error types, then defuse finance's twin traps: data snooping and the gap between significance and profit."
  root={{
    label: 'Hypothesis testing',
    children: [
      {
        label: 'The logic',
        children: [
          { label: 'H₀: no effect (assume true)' },
          { label: 'H₁: a real effect exists' },
          { label: 'Reject H₀, or fail to reject' },
          { label: 'Never prove H₁ — only reject H₀' },
        ],
      },
      {
        label: 'Test statistic',
        children: [
          { label: 't = x̄ / SE, with SE = s/√n' },
          { label: 'Edge measured in standard errors' },
          { label: '|t| > 2 ≈ significant at 5%' },
          { label: 'More data shrinks SE, raises t' },
        ],
      },
      {
        label: 'p-value',
        children: [
          { label: 'P(data this extreme | H₀ true)' },
          { label: 'Compare to α (often 0.05)' },
          { label: 'NOT P(H₀ true) — flipped conditional' },
          { label: 'Significant ≠ large effect' },
        ],
      },
      {
        label: 'Errors & power',
        children: [
          { label: 'Type I: false positive (rate α)' },
          { label: 'Type II: false negative (rate β)' },
          { label: 'Power = 1 − β' },
          { label: 'α/β trade off; more data helps both' },
        ],
      },
      {
        label: 'Finance trap: snooping',
        children: [
          { label: '1,000 tests → ~50 lucky 5% hits' },
          { label: 'p-hacking & backtest overfitting' },
          { label: 'Bonferroni: demand p ≤ α/m' },
          { label: 'Deflated Sharpe; out-of-sample' },
        ],
      },
      {
        label: 'Significance ≠ profit',
        children: [
          { label: 'Tiny real edge can be net-negative' },
          { label: 'Costs: spread, commission, slippage' },
          { label: 'Two tests: significant AND profitable' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: hypothesis testing & significance"
  questions={[
    {
      question: 'In testing whether a strategy has an edge, what does the null hypothesis H₀ assume?',
      options: [
        { text: 'The true edge is zero — there is no effect', correct: true },
        { text: 'The strategy is definitely profitable' },
        { text: 'The returns are not normally distributed' },
        { text: 'The sample size is too small' },
      ],
      explanation: 'H₀ is the no-effect baseline (mean return = 0). We assume it, then check whether the data is too surprising to be compatible with it.',
    },
    {
      question: 'A strategy returns 0.6% monthly with standard deviation 3.6% over 36 months. Its t-statistic for H₀: μ = 0 is closest to:',
      options: [
        { text: '1.0', correct: true },
        { text: '6.0' },
        { text: '0.17' },
        { text: '3.6' },
      ],
      explanation: 'SE = 3.6% / √36 = 3.6% / 6 = 0.6%, so t = 0.6% / 0.6% = 1.0 — not significant. Forgetting to divide by √n (using s = 3.6% directly) gives the 0.17 trap.',
    },
    {
      question: 'A backtest reports p = 0.03. Which statement is correct?',
      options: [
        { text: 'If the true edge were zero, data this extreme would occur 3% of the time', correct: true },
        { text: 'There is a 3% chance the edge is zero' },
        { text: 'There is a 97% chance the strategy is profitable' },
        { text: 'The strategy wins 97% of its trades' },
      ],
      explanation: 'p = P(data this extreme | null true) = 3%. It is not P(null true) and says nothing about profitability or win rate — flipping the conditional is the prosecutor’s fallacy.',
    },
    {
      question: 'Increasing statistical power (1 − β) without raising the false-positive rate is best achieved by:',
      options: [
        { text: 'Collecting more data to shrink the standard error', correct: true },
        { text: 'Loosening α from 0.05 to 0.10' },
        { text: 'Testing more strategies on the same data' },
        { text: 'Reporting only the best backtest' },
      ],
      explanation: 'More data shrinks SE, sharpening the test so you gain power while keeping α fixed. Loosening α buys power only by accepting more false positives; testing more strategies and cherry-picking is data snooping, which manufactures false positives.',
    },
    {
      question: 'A team tests 1,000 strategies at α = 0.05 and trumpets the best one. The core danger is:',
      options: [
        { text: 'Around 50 strategies clear the 5% bar by luck alone, so the winner is probably noise', correct: true },
        { text: 'The p-value formula is wrong for large samples' },
        { text: 'Testing many strategies reduces the false-positive rate' },
        { text: 'A t-stat above 2 can never arise from a true-null strategy' },
      ],
      explanation: 'At α = 0.05, about 1,000 × 0.05 = 50 true-null strategies fire false positives by chance. Cherry-picking the best without a multiple-testing correction (Bonferroni p ≤ α/m, deflated Sharpe, out-of-sample) is data snooping.',
    },
    {
      question: 'A signal has a gross edge of +0.03% per trade with a t-stat of 7, but round-trip costs are 0.05%. The correct read is:',
      options: [
        { text: 'It is statistically significant yet economically negative — significance is not relevance', correct: true },
        { text: 'A t-stat of 7 guarantees profitability regardless of costs' },
        { text: 'The edge is not significant because it is small' },
        { text: 'Transaction costs cannot exceed a significant edge' },
      ],
      explanation: 'Net edge = 0.03% − 0.05% = −0.02% per trade: real and significant, but it loses money after costs. Significance answers "distinguishable from zero?"; profitability is a separate test the strategy must also pass.',
    },
  ]}
/>

That closes the statistics course: you can now take a number off a backtest and interrogate it like a quant — *is this real, or is it luck, and even if it's real, is it worth trading?* Every later course that claims an "edge," a "factor," or a "signal" is implicitly making a hypothesis-testing claim. Now you know how to audit it.
