---
title: "Potential Outcomes & Causal DAGs"
description: "The Rubin potential-outcomes framework — treated and untreated worlds, the fundamental problem of causal inference, ATE vs ATT — and causal directed acyclic graphs as the grammar of assumptions, with the all-important contrast between a confounder you must control and a collider you must not."
topic: "en/causal-inference-for-alpha-and-execution"
order: 2
minutes: 20
updated: 2026-06-23
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, MatchConcepts, Categorize, MindMap, ConfounderDagExplorer } from '@/components/react';

Last lesson left you with a slogan — *a backtest measures association, not effect* — and a promise that there is real machinery behind it. This is the machinery. Two pieces of grammar, both stolen from epidemiology and computer science, that let you say *exactly* what a causal claim is and *exactly* which assumptions would make your data answer it.

The first is the **potential-outcomes** framework (Rubin): a way to define a causal effect for a single trade, a single stock, a single order, without ever mentioning a regression. The second is the **causal DAG** (Pearl): a picture that makes your assumptions about what-causes-what auditable, and that tells you — mechanically — which variables you must control for and which you must leave alone. The punchline of the whole lesson is a single contrast most quants get backwards: a **confounder** you are obligated to control, and a **collider** you are forbidden to. Control the wrong one and you don't reduce bias — you *manufacture* it.

## Potential outcomes: two worlds, one of them always missing

<MCQ
  client:visible
  pretest
  question="For one stock, you define the return it WOULD earn if you trade it, Y(1), and the return it WOULD earn if you do not, Y(0). After the month is over and you traded it, what do you actually get to observe?"
  options={[
    { text: "Only Y(1) — the world you chose; Y(0) for that same stock-month never happened and is forever unobserved", correct: true },
    { text: "Both Y(1) and Y(0), since the month is now in the past" },
    { text: "Neither — potential outcomes are purely theoretical and never observed" },
    { text: "The average of Y(1) and Y(0)" },
  ]}
  explanation="You only ever see the potential outcome corresponding to the treatment you actually applied. The other one — the counterfactual — is missing data, by definition. That is the fundamental problem of causal inference."
/>

**Analogy.** Picture a wardrobe with two doors. Behind the left door is the version of your morning where you took the umbrella; behind the right, the version where you left it. Both versions are perfectly well-defined — there *is* a fact about whether you'd have got soaked — but you only ever walk through *one* door. The other world is real but unwitnessed. Every trade is that wardrobe: there is a genuine return-if-traded and a genuine return-if-not, and the market only ever lets you see through the door you opened.

**Definition.** For each unit $i$ (a stock-month, a trade, an order), the **potential outcomes** are $Y_i(1)$ — the outcome if the unit *is* treated — and $Y_i(0)$ — the outcome if it is *not*. The **individual treatment effect** is

$$\tau_i = Y_i(1) - Y_i(0).$$

"Treatment" is just whatever intervention you care about: holding the high-signal basket, routing the order aggressively, applying the overlay. The effect is the difference *for the same unit, in the same period, all else identical* — which is exactly why it is so hard to get.

**The fundamental problem of causal inference.** For any single unit you observe *at most one* of $Y_i(1)$ and $Y_i(0)$ — whichever corresponds to the treatment that actually happened. The other is a counterfactual: real, well-defined, and permanently unobserved. So $\tau_i$ is, for every individual unit, *uncomputable from data alone*. Causal inference is the science of filling in that missing column with assumptions you can defend.

**Worked example.** Five stock-months. You traded (treated, $T=1$) three of them and skipped (control, $T=0$) two. The column you observe is whichever potential outcome matches the treatment; the other is the unobservable "?".

| Unit | Treated? | Y(1) — if traded | Y(0) — if not | Observed Y | Effect Y(1)−Y(0) |
|---|---|---|---|---|---|
| A | Yes | +2.0% | ? | +2.0% | ? |
| B | Yes | +1.0% | ? | +1.0% | ? |
| C | Yes | +3.0% | ? | +3.0% | ? |
| D | No | ? | +0.5% | +0.5% | ? |
| E | No | ? | −0.5% | −0.5% | ? |

Every "Effect" cell is a "?" because every row is missing one of its two potential outcomes. There is no arithmetic that recovers $\tau_i$ here — the data simply does not contain it. The entire rest of the field exists to estimate *averages* of that missing quantity, which, unlike the individual effect, you *can* get at under the right assumptions.

<Callout variant="warning" title="The counterfactual is missing data, not a modelling choice">
A frequent beginner error is to treat a stock's "return if not traded" as something you can read off the same stock's history, or proxy with a similar stock. You cannot — not for *that* unit in *that* period. The counterfactual is genuinely absent, and pretending otherwise (e.g. "its return last month was the counterfactual") silently smuggles in the assumption that nothing else changed, which is the very thing you have to prove. Treat the missing column as missing, and be explicit about how you intend to fill it.
</Callout>

### When to use it

Reach for potential-outcomes notation the moment a debate gets fuzzy about *what effect we're even estimating*. Forcing yourself to write "the unit is a stock-month, treatment is holding the top decile, $Y(1)$ is its forward return held, $Y(0)$ is its forward return not held" disambiguates more arguments than any regression output. If you cannot name the unit and the two worlds, you are not yet asking a causal question.

<FillBlank
  client:visible
  question="State the fundamental problem precisely."
  text="For any single unit we observe at most {{one|both|neither}} of its two potential outcomes, so the individual treatment effect is never computable from data alone — the other outcome is a missing counterfactual."
  explanation="You see the potential outcome matching the treatment that actually happened; the other is permanently unobserved. Hence the field estimates averages, not individual effects."
/>

## From individual effects to averages: ATE, ATT, and the bias you inherit

<MCQ
  client:visible
  pretest
  question="You cannot get any individual effect. So you compute the naive difference: average observed return of the trades you took minus the average of the ones you skipped. In general, what is this difference equal to?"
  options={[
    { text: "The average effect on the treated PLUS a selection-bias term — because the trades you chose to take were not comparable to the ones you skipped", correct: true },
    { text: "The average treatment effect exactly, with no bias" },
    { text: "Always zero, since the worlds cancel out" },
    { text: "The individual treatment effect of a typical unit" },
  ]}
  explanation="The naive difference-in-means equals the ATT plus a baseline-difference (selection-bias) term. Only when treated and untreated units have the same average untreated outcome does the bias vanish — which random assignment guarantees and self-selection destroys."
/>

**Analogy.** You can't know whether a *specific* patient would have recovered without the drug — but average over thousands and the missing halves start to cancel, *if* the treated and untreated groups were otherwise alike. The danger is when they aren't: if doctors gave the drug only to the healthiest patients, the treated group would look great even if the drug were a sugar pill. The gap between "drug works" and "healthy people got the drug" is selection bias, and it is the exact reason your traded names beating your skipped names proves nothing on its own.

**Definitions.** Two averages do the heavy lifting:

- **ATE — Average Treatment Effect**, over the whole population:
$$\text{ATE} = \mathbb{E}[\,Y(1) - Y(0)\,].$$
- **ATT — Average Treatment effect on the Treated**, over only the units that actually got treated:
$$\text{ATT} = \mathbb{E}[\,Y(1) - Y(0) \mid T = 1\,].$$

ATE answers "what if we treated *everybody*?"; ATT answers "what did treatment do *to the units we actually treated*?" They differ whenever the treated units respond differently from a random unit — which, in a strategy you *chose*, is the normal case.

**Why the naive difference is ATT plus bias.** The thing you can actually compute is the difference in observed means:

$$\mathbb{E}[\,Y \mid T=1\,] - \mathbb{E}[\,Y \mid T=0\,].$$

Decompose it by adding and subtracting the treated group's *untreated* potential outcome:

$$\underbrace{\mathbb{E}[\,Y(1) - Y(0)\mid T=1\,]}_{\text{ATT — what you want}} + \underbrace{\big(\mathbb{E}[\,Y(0)\mid T=1\,] - \mathbb{E}[\,Y(0)\mid T=0\,]\big)}_{\text{selection bias — the baseline gap}}.$$

The second bracket asks: in the *untreated world*, would the units you chose to treat have done differently from the ones you skipped? If yes — if your chosen trades were special even absent the treatment — the naive difference is contaminated.

**Worked example.** Take the five units above. Suppose the truth (which an oracle, not you, can see) is:

| Unit | Treated? | Y(1) | Y(0) | True effect |
|---|---|---|---|---|
| A | Yes | +2.0% | +1.5% | +0.5% |
| B | Yes | +1.0% | +0.6% | +0.4% |
| C | Yes | +3.0% | +2.4% | +0.6% |
| D | No | +0.4% | +0.0% | +0.4% |
| E | No | −0.1% | −0.5% | +0.4% |

The **true ATE** is the average of the effect column: $(0.5+0.4+0.6+0.4+0.4)/5 = 0.46\%$. The **true ATT** (treated units A, B, C) is $(0.5+0.4+0.6)/3 = 0.50\%$.

Now the **naive difference** you'd actually compute from observed returns: treated mean $= (2.0+1.0+3.0)/3 = 2.0\%$; control mean $= (0.0 + (-0.5))/2 = -0.25\%$. Naive difference $= 2.0 - (-0.25) = 2.25\%$.

That $2.25\%$ wildly overstates the true ATT of $0.50\%$. The gap is the **selection bias**: the units you chose to trade (A, B, C) had high *baseline* returns $\mathbb{E}[Y(0)\mid T=1] = (1.5+0.6+2.4)/3 = 1.5\%$, while the skipped units had a baseline of $\mathbb{E}[Y(0)\mid T=0] = (0.0+(-0.5))/2 = -0.25\%$. The baseline gap is $1.5 - (-0.25) = 1.75\%$. And indeed $\text{ATT} + \text{bias} = 0.50\% + 1.75\% = 2.25\%$ — exactly the naive number. You "earned" $2.25\%$; only $0.50\%$ of it was the treatment. The other $1.75\%$ was you cherry-picking names that were going to outperform anyway.

<Callout variant="warning" title="ATT is not ATE, and the naive diff is usually neither">
Two traps in one. First, do not quote a number estimated on your *traded* book as if it applied to the whole universe — that is ATT masquerading as ATE, and it breaks the instant you scale the strategy to names that respond differently. Second, never report the raw traded-minus-skipped spread as "the effect." Under self-selection it is ATT *plus* a selection-bias term that can dwarf the effect itself, as the worked example's $1.75\%$ vs $0.50\%$ shows. The only thing that zeroes the bias term for free is assignment you did not control — randomisation, or a natural experiment.
</Callout>

### When to use it

Decide *up front* whether your decision needs ATE or ATT. Sizing an existing book? You want ATT — the effect on the kind of names you actually hold. Deciding whether to *launch* a strategy across the whole universe, or extrapolate to names you've never traded? You need ATE, and you must worry that your treated sample isn't representative. And always, *always* write the bias term down explicitly before trusting a difference-in-means — if you can't argue the baseline gap is zero, the difference is not an effect.

<Categorize
  client:visible
  question="Sort each quantity by whether it is something you can compute directly from observed data, or something that needs a causal assumption to identify."
  buckets={["Computable from data alone", "Needs a causal assumption"]}
  items={[
    { text: "Average observed return of the trades you took", bucket: "Computable from data alone" },
    { text: "Average observed return of the trades you skipped", bucket: "Computable from data alone" },
    { text: "The individual treatment effect for stock A this month", bucket: "Needs a causal assumption" },
    { text: "The ATT — average effect on the units you actually traded", bucket: "Needs a causal assumption" },
    { text: "The ATE across the whole universe", bucket: "Needs a causal assumption" },
    { text: "The raw difference between the traded and skipped averages", bucket: "Computable from data alone" },
  ]}
  explanation="Observed group means are just arithmetic. But anything involving a missing potential outcome — individual effects, ATT, ATE — requires an assumption (like ignorability or randomisation) to bridge from the observed world to the counterfactual one."
/>

## Ignorability and the do-operator: when conditioning equals intervening

<MCQ
  client:visible
  pretest
  question="What does Pearl's do(X) operator represent, and how does it differ from ordinary conditioning on X?"
  options={[
    { text: "do(X) means forcing X to a value by intervention — erasing the arrows INTO X — whereas conditioning on X just filters to units where X happened to take that value, leaving its causes intact", correct: true },
    { text: "do(X) and conditioning on X are two notations for the same thing" },
    { text: "do(X) means deleting the variable X from the dataset entirely" },
    { text: "do(X) is only defined for continuous treatments, conditioning only for discrete" },
  ]}
  explanation="Intervention surgically sets X and severs whatever normally determines it (the incoming arrows). Conditioning is passive selection on the value X took for its own reasons — which is why E[Y|X] and E[Y|do(X)] differ unless those reasons are causally irrelevant."
/>

**Analogy.** Conditioning on "the patient took the drug" is asking *who, among the people who chose the drug, recovered?* — and the people who chose it might differ in a hundred ways. The `do`-operator is reaching into the world and *making* a randomly chosen patient take it, regardless of who they are. One observes a self-selected slice; the other performs surgery on the causal machine. In markets: conditioning on "the signal was high" pools all the reasons it was high; $do(\text{signal}=\text{high})$ is you forcing it high by trading, snapping the strings that normally raise it.

**Definition.** The interventional quantity you actually want is $\mathbb{E}[\,Y \mid do(X{=}x)\,]$ — the average outcome in a world where $X$ is *set* to $x$ by fiat, deleting the arrows that ordinarily point into $X$. The observational quantity your data hands you is $\mathbb{E}[\,Y \mid X{=}x\,]$. **Ignorability** (a.k.a. unconfoundedness, conditional independence, "no unmeasured confounders") is the assumption that makes them coincide, possibly after controlling for a set of covariates $Z$:

$$\big(\,Y(0),\,Y(1)\,\big) \perp\!\!\!\perp T \;\mid\; Z.$$

Read it as: *once you hold $Z$ fixed, the treatment is as good as randomly assigned — the units' potential outcomes carry no further information about whether they got treated.* Under ignorability, $\mathbb{E}[\,Y \mid do(X{=}x)\,] = \mathbb{E}_Z\big[\,\mathbb{E}[\,Y \mid X{=}x, Z\,]\,\big]$ — the conditional, averaged over the confounders, *is* the causal effect.

**Random assignment is the clean version.** If treatment is assigned by a coin flip, then $T$ is independent of *everything* — including both potential outcomes — with no $Z$ needed: $\big(Y(0), Y(1)\big) \perp\!\!\!\perp T$. The selection-bias term from the previous section, $\mathbb{E}[Y(0)\mid T=1] - \mathbb{E}[Y(0)\mid T=0]$, is then exactly zero, because the groups are interchangeable by construction. That is *why* randomised experiments identify the ATE for free, and why finding a market analogue of a coin flip (a natural experiment) is the holy grail of later lessons.

**Worked example.** Recall the naive difference was $2.25\%$ against a true ATT of $0.50\%$ — a bias of $1.75\%$. Now suppose instead you had assigned treatment by literally flipping a coin per name. Then the treated and skipped groups have, in expectation, the *same* baseline $Y(0)$: the $1.5\%$-vs-$(-0.25\%)$ gap disappears because high-baseline names are no longer over-represented among the treated. The selection-bias bracket collapses to $0$, and the naive difference now estimates the effect itself, $\approx 0.46\%$ (the ATE). Same units, same potential outcomes — only the *assignment mechanism* changed, and that alone is what turned a worthless $2.25\%$ into an honest estimate.

<Callout variant="warning" title="Ignorability is an assumption, not a result — and it is untestable">
You cannot check unconfoundedness from the data, because it is a statement about the *unobserved* counterfactual outcomes. No p-value confirms "no unmeasured confounders." Worse, conditioning on *more* variables does not monotonically help — controlling for the wrong variable (a collider, next section) can *create* the very dependence ignorability requires you not to have. So "I controlled for a lot of stuff" is not evidence for ignorability; it can be evidence against it. You earn ignorability by argument and design, never by throwing covariates at a regression.
</Callout>

### When to use it

Whenever someone hands you an observational "edge" and asks if it's real, the operational question is: *is there a set of observed variables $Z$ such that, holding $Z$ fixed, the signal is as-good-as-randomly assigned?* If yes, you can adjust for $Z$ and read off the effect. If no — if a confounder is unmeasured, or you'd have to condition on something you shouldn't — observational adjustment cannot save you, and you need an actual intervention or a natural experiment. The `do`-operator is the question; ignorability is the licence to answer it with conditioning.

<Reveal client:visible>
<details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
<summary className="cursor-pointer font-medium">If ignorability is untestable, how is causal inference from observational data not just hand-waving?</summary>

**Answer.** The honesty comes from making the assumption *explicit and falsifiable in its consequences*, not from testing it directly. You state the DAG you believe, derive what would have to be true if it held (placebo outcomes that should show no effect, pre-treatment trends that should be parallel, dose-response that should be monotone), and *test those implications*. You also run sensitivity analysis: how strong would an unmeasured confounder have to be to overturn the result? If a tiny hidden confounder flips the sign, the finding is fragile; if it would take an implausibly huge one, it's robust. Causal inference is disciplined argument about an untestable assumption, surrounded by testable consequences — not a magic test that certifies "this is causal."

</details>
</Reveal>

## Causal DAGs: drawing your assumptions so they can be audited

<MCQ
  client:visible
  pretest
  question="In a causal directed acyclic graph (DAG), what do the nodes and arrows mean, and what does 'acyclic' rule out?"
  options={[
    { text: "Nodes are variables, an arrow X to Y asserts X is a direct cause of Y, and acyclic means no variable can (through any path of arrows) cause itself — no feedback loops in the graph", correct: true },
    { text: "Nodes are data points and arrows are correlations of any sign" },
    { text: "Arrows mean statistical association regardless of direction; acyclic just means the graph is a tree" },
    { text: "Nodes are time periods and arrows are the passage of time" },
  ]}
  explanation="A causal DAG encodes a hypothesis: each arrow is a claim of direct causation, and acyclicity forbids a variable from being its own ancestor. The graph is your assumptions made visible and checkable — it does not come from the data, you bring it to the data."
/>

**Analogy.** A DAG is a plumbing diagram for causation. Nodes are junctions (variables); arrows are pipes that carry influence *one way* (direct causes); "acyclic" means no pipe loops back to feed its own source. Crucially, a plumber doesn't *discover* the layout by watching water — they *draw what they believe is connected* and then check whether the observed flows are consistent. Your DAG is the same: it is a declaration of belief about what causes what, drawn *before* you stare at correlations, and its whole value is that it makes those beliefs explicit enough to be wrong.

**Definitions.** A **causal DAG** is a graph of variables (nodes) and directed edges (arrows), where an arrow from $X$ to $Y$ means "$X$ is a direct cause of $Y$," and **acyclic** means you can never follow arrows from a node back to itself (no causal loops). Some vocabulary you will lean on:

- A **path** is any sequence of connected edges between two nodes, *ignoring* arrow direction.
- A **directed (causal) path** follows arrows head-to-tail, e.g. $X \to M \to Y$ — this is genuine causal flow.
- A **back-door path** between treatment $X$ and outcome $Y$ is any path that starts with an arrow *into* $X$ (i.e. $X \leftarrow \dots Y$). Back-door paths carry *non-causal* association — they are how confounding sneaks in.

The central skill is reading a DAG for *which paths transmit association and which are blocked*. A path is blocked if it passes through a controlled-for confounder, or through an *un*controlled collider; it is open otherwise. The effect of $X$ on $Y$ is identified when every back-door path is blocked while every genuine causal path stays open — and the next section shows that "block" and "open" depend entirely on whether the middle node is a confounder or a collider.

**Worked example — encoding an assumption.** Suppose you believe a momentum signal $X$ affects forward return $Y$ both directly and through "attracting follow-on flow" $M$, and that the broad market regime $Z$ drives both the signal and the return. You'd draw:

$$Z \to X, \quad Z \to Y, \quad X \to M, \quad M \to Y, \quad X \to Y.$$

This DAG asserts a *lot*: there are two causal routes from $X$ to $Y$ ($X \to Y$ directly and $X \to M \to Y$), and one back-door path $X \leftarrow Z \to Y$ carrying spurious association. The diagram tells you immediately that to estimate the causal effect of $X$ you must block the back door (control $Z$) but must *not* control $M$ — controlling $M$ would close off part of the very effect you're trying to measure. None of that came from the data; it came from your willingness to commit your assumptions to a picture.

<Callout variant="warning" title="The DAG is your assumption, not a discovery — garbage in, garbage out">
A DAG does not validate itself. If you draw the wrong arrows — omit a confounder, point an edge the wrong way, miss a feedback channel — every identification conclusion you derive from it is confidently wrong. The graph is a *device for being explicit*, and its honesty is entirely on you. Two analysts with different DAGs will reach different "rigorous" conclusions from the *same* data, and the data cannot adjudicate between them. That is not a flaw in DAGs; it is the unavoidable truth that causal claims always rest on assumptions — DAGs just stop you from hiding them.
</Callout>

### When to use it

Draw the DAG *before* you fit anything, at the proposal stage, and circulate it — a DAG is the single best artefact for making a research debate concrete, because disagreements become "you have an arrow I don't" instead of vague unease. Then use it operationally: list every back-door path from treatment to outcome, and for each, identify the minimal set of variables to control to block it (the "back-door criterion"). If no observable set blocks all back doors without opening a collider, the effect is *not* identified by adjustment, and you need a different design.

<FillBlank
  client:visible
  question="Define the dangerous kind of path."
  text="A {{back-door|directed|forward}} path from treatment X to outcome Y is one that begins with an arrow pointing into X; it carries non-causal association and must be blocked to identify the effect."
  explanation="Back-door paths start with an arrow INTO the treatment (X is caused by something on the path). They transmit spurious association — confounding — and the back-door criterion is about blocking all of them."
/>

## Confounder vs collider: the one contrast quants get backwards

<MCQ
  client:visible
  pretest
  question="A confounder sits as X ← Z → Y (common cause); a collider sits as X → Z ← Y (common effect). What is the correct conditioning rule for each?"
  options={[
    { text: "Control the confounder (it opens a back-door path by default, so controlling CLOSES it); do NOT control the collider (it is blocked by default, so controlling OPENS a spurious path)", correct: true },
    { text: "Control both — more covariates always reduce bias" },
    { text: "Control neither — conditioning never changes which paths are open" },
    { text: "Control the collider and leave the confounder alone — colliders are the dangerous ones to ignore", correct: false },
  ]}
  explanation="They are mirror images. A common cause leaks association until you block it by controlling. A common effect blocks association until you control it — at which point you create association out of nothing. Same instinct ('control for stuff'), opposite results."
/>

**Analogy.** A **confounder** is a puppeteer holding strings to *both* the signal and the return — you must cut its strings (control it) or it animates a fake relationship between them. A **collider** is the opposite: it's a *shared victim*, a downstream node that *both* the signal and the return push on. Imagine two unrelated alarms, "low battery" and "intruder," that *both* can trip your security light. The alarms are independent — until you condition on "the light is on." Now, given the light is on, learning the battery was fine makes an intruder *more* likely: you've created a spurious negative dependence by conditioning on their common effect. Controlling a collider is detective-fiction logic — "if it wasn't the butler, it must have been the maid" — and it has no place in your regression unless you *want* phantom correlations.

**Definitions.** With treatment $X$, outcome $Y$, and a third variable $Z$:

- $Z$ is a **confounder** when $X \leftarrow Z \to Y$: a *common cause*. It opens a back-door path $X \leftarrow Z \to Y$ that is **open by default**. Controlling for $Z$ **closes** it, removing the spurious association — *you must control a confounder.*
- $Z$ is a **collider** when $X \to Z \leftarrow Y$: a *common effect*. The path $X \to Z \leftarrow Y$ is **blocked by default** (no association flows through a collider). Controlling for $Z$ (or any descendant of it) **opens** the path, injecting spurious association that was never there — *you must not control a collider.* This is **collider bias** (a.k.a. selection bias, Berkson's paradox).

The symmetry is the whole point: same human instinct ("when in doubt, add the control variable"), diametrically opposite consequences. Confounders are *sins of omission* (forgetting to control), colliders are *sins of commission* (controlling something you shouldn't).

**Worked finance example — index inclusion as a collider.** Suppose a stock's chance of being *added to a major index* is driven by *both* its **size** ($X$) and its **momentum** ($Y$): big stocks get in, and so do strong recent performers. Draw it: $\text{Size} \to \text{Included} \leftarrow \text{Momentum}$. Among *all* stocks, size and momentum may be independent. But run your study *only on index members* — i.e. condition on $\text{Included}=\text{yes}$ — and watch a phantom appear. Concretely: a stock can earn its way into the index by being *huge* even with mediocre momentum, or by having *blazing* momentum despite middling size. So *within the index*, the small-ish members are disproportionately the high-momentum ones (that's how they got in), and the low-momentum members are disproportionately the giants. You've manufactured a *negative* size-momentum correlation among members that does not exist in the wild. Any "edge" you find by combining size and momentum on an index-only sample is partly this artefact — born entirely from conditioning on the collider "Included." The fix is *not* to control for inclusion; it's to study the unconditioned universe (or model the selection explicitly).

<ConfounderDagExplorer client:visible caption="Toggle the third node Z between confounder (X ← Z → Y, a common cause) and collider (X → Z ← Y, a common effect), then tick 'Control for Z'. Watch the rule flip: controlling CLOSES the confounder's back-door path (good — it isolates the real X → Y effect) but OPENS a brand-new spurious path through the collider (bad — it manufactures bias). The genuine X → Y arrow stays put throughout; only the spurious path responds to your conditioning choice. The takeaway the diagram dramatises: 'control for everything' is a trap." />

<Callout variant="warning" title="'Control for everything' is the most expensive mistake in applied causal work">
The kitchen-sink regression — throw in every available covariate and trust the coefficient — feels rigorous and is often catastrophic. Each control is either a confounder (helps), a collider (hurts, creates bias), or a mediator on the causal path (hurts, removes real effect). Adding controls *blindly* is as likely to inject bias as to remove it. The classic finance traps: conditioning on index membership, on *survivors* (a collider on "still trading"), on having *passed a liquidity screen*, or on *post-treatment* outcomes — every one of these is a common effect you opened by selecting on it. Before any variable enters the model, ask of the DAG: is this a common cause, a common effect, or on the path? Only the first earns a place.
</Callout>

### When to use it

Use the confounder/collider test as a gate on *every* candidate control variable and *every* sample filter — because a sample filter is just conditioning by another name. For each one, locate it on your DAG: if it's a common cause of treatment and outcome, control it; if it's a common effect (or a descendant of one, or the basis of your sample selection), leave it out and worry that your *existing* selection already opened it. The most dangerous colliders are the ones you condition on without noticing — survivorship, success filters, "names with enough data" — so audit how your sample was *built*, not just which variables your model *names*.

<MatchConcepts
  client:visible
  pairs={[
    { term: "Potential outcome", definition: "The outcome a single unit would have under a given treatment — only one of the two is ever observed" },
    { term: "Fundamental problem of causal inference", definition: "For any unit you see at most one potential outcome, so the individual effect is never directly computable" },
    { term: "Selection bias", definition: "The baseline-outcome gap between treated and untreated units that contaminates the naive difference-in-means" },
    { term: "do-operator", definition: "Forcing the treatment to a value, severing the arrows into it — the interventional quantity, distinct from passive conditioning" },
    { term: "Confounder", definition: "A common cause of treatment and outcome that opens a back-door path you must control to close" },
    { term: "Collider", definition: "A common effect of treatment and outcome, blocked by default, that controlling for OPENS — manufacturing bias" },
  ]}
/>

## Recap

You now own the two pieces of grammar the rest of the course speaks in. **Potential outcomes** define a causal effect for a single unit as $Y(1) - Y(0)$ — two worlds, one of which is always missing — which is why individual effects are uncomputable and the field chases averages: the **ATE** over everyone, the **ATT** over the treated, and never the raw difference-in-means, which is ATT *plus a selection-bias term* that cherry-picking can blow up (recall $2.25\%$ observed for a true $0.50\%$ effect). The bias vanishes under **ignorability** — treatment as-good-as-random given $Z$ — which is exactly the licence to swap the interventional $\mathbb{E}[Y\mid do(X)]$ for the observational $\mathbb{E}[Y\mid X, Z]$, and which randomisation grants for free. **Causal DAGs** make those assumptions auditable: arrows are claims of direct causation, back-door paths are how confounding leaks in, and identification means blocking every back door while leaving the causal paths open. And the contrast that separates the careful from the burned: control a **confounder** (a common cause, open by default) and *never* a **collider** (a common effect, blocked by default — until you select on it and conjure the bias yourself). "Control for everything" is not caution; it is a coin flip between fixing bias and inventing it.

<MindMap
  client:visible
  title="Potential outcomes & causal DAGs"
  caption="Build the map: two missing worlds, the averages and the bias, the licence to identify, and the confounder-vs-collider rule."
  root={{
    label: 'Potential outcomes & DAGs',
    children: [
      { label: 'Potential outcomes (Rubin)', children: [
        { label: 'Y(1) and Y(0): two worlds' },
        { label: 'Effect = Y(1) − Y(0)' },
        { label: 'Fundamental problem: one is always missing' },
      ]},
      { label: 'Averages, not individuals', children: [
        { label: 'ATE — over everyone' },
        { label: 'ATT — over the treated' },
        { label: 'Naive diff = ATT + selection bias' },
      ]},
      { label: 'Identification', children: [
        { label: 'do(X): intervene, cut arrows into X' },
        { label: 'Ignorability: as-good-as-random given Z' },
        { label: 'Randomisation zeroes the bias term' },
      ]},
      { label: 'Causal DAGs', children: [
        { label: 'Nodes = variables, arrows = direct causes' },
        { label: 'Back-door paths carry spurious association' },
        { label: 'Block back doors, keep causal paths open' },
      ]},
      { label: 'Confounder vs collider', children: [
        { label: 'Confounder X ← Z → Y: control it' },
        { label: 'Collider X → Z ← Y: do NOT control it' },
        { label: 'Index inclusion = collider trap' },
      ]},
    ],
  }}
/>

<Quiz
  client:visible
  title="Mixed check: counterfactuals, bias, and bad controls"
  questions={[
    {
      question: "The true ATT of a strategy is 0.6%, but the units you chose to trade had an average untreated return of 1.2% while the names you skipped averaged 0.1% untreated. What naive traded-minus-skipped difference will you observe?",
      options: [
        { text: "About 1.7% — the ATT of 0.6% plus a selection-bias term of 1.1% from the baseline gap", correct: true },
        { text: "0.6% — the naive difference always equals the ATT" },
        { text: "0.5% — you subtract the bias from the effect" },
        { text: "1.1% — only the selection bias shows up" },
      ],
      explanation: "Naive difference = ATT + (baseline gap of treated minus untreated) = 0.6% + (1.2% − 0.1%) = 0.6% + 1.1% = 1.7%. The bias nearly triples the apparent effect.",
    },
    {
      question: "Your equity universe study runs only on stocks that survived the full sample (still trading at the end). Survivorship filtering is which kind of error, in DAG terms?",
      options: [
        { text: "Conditioning on a collider — 'still trading' is a common effect of many things, so selecting on it opens spurious paths and biases the estimates", correct: true },
        { text: "Failing to control a confounder, which you fix by adding survivorship as a covariate" },
        { text: "Harmless, because survivors are a random subset of the universe" },
        { text: "Conditioning on a mediator that you should always include" },
      ],
      explanation: "Survival is a downstream common effect; restricting to survivors is conditioning on a collider. It manufactures association among the surviving sample. The fix is not to control for it but to study the un-filtered universe (or model the dropout).",
    },
    {
      question: "Which statement about the do-operator is correct?",
      options: [
        { text: "E[Y | do(X=x)] is the average outcome when X is forced to x, deleting the arrows into X; it equals E[Y | X=x] only under causal assumptions like ignorability", correct: true },
        { text: "E[Y | do(X=x)] and E[Y | X=x] are identical by definition" },
        { text: "do(X) is computed by dropping all rows where X is not equal to x" },
        { text: "The do-operator can only be applied after a randomised experiment has been run" },
      ],
      explanation: "do(X=x) is intervention: it severs whatever normally sets X. Plain conditioning is passive selection on the value X took for its own reasons. They coincide only when treatment is as-good-as-random given the controls — that is the whole content of identification.",
    },
    {
      question: "A colleague proposes: 'To be safe, let's just add every variable we have as a control in the regression.' Why is this dangerous?",
      options: [
        { text: "Because some of those variables are colliders or mediators — controlling a collider creates bias and controlling a mediator removes part of the true effect, so blind controls can inject more bias than they remove", correct: true },
        { text: "Because more controls always reduce the R-squared, hurting fit" },
        { text: "Because regressions can only handle a small fixed number of variables" },
        { text: "It is not dangerous at all — controlling for everything is best practice", correct: false },
      ],
      explanation: "Each candidate control is a confounder (control it), a collider (controlling injects bias), or a mediator (controlling removes real effect). Adding them blindly is a coin flip between fixing and creating bias — you must classify each on the DAG first.",
    },
    {
      question: "Index inclusion is caused by both a stock's size and its momentum. If you run a size-momentum study only on index members, what happens?",
      options: [
        { text: "You condition on a collider, so a spurious size-momentum association appears within the members that need not exist in the full universe", correct: true },
        { text: "Nothing — restricting to index members removes confounding and cleans the estimate" },
        { text: "You correctly control for inclusion, which is a confounder of size and momentum" },
        { text: "The estimate becomes the unbiased ATE for the whole market" },
      ],
      explanation: "Inclusion is a common effect of size and momentum — a collider. Selecting on it opens the X → Included ← Y path, fabricating a size-momentum correlation among members. Study the unconditioned universe instead of the index-only sample.",
    },
  ]}
/>
