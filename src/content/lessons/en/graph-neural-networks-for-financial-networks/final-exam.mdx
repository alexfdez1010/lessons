---
title: "Graph Neural Networks for Financial Networks — Final Exam"
description: "The graded final exam for Graph Neural Networks for Financial Networks: markets as graphs (nodes, edges, the four finance graphs), message passing and the node/edge/graph tasks, the GCN/GraphSAGE/GAT/temporal-GNN lineage, systemic risk and the default cascade with DebtRank, fraud-ring and relational-alpha applications, and the honest audit of leakage, over-smoothing, non-stationary topology and the data-plumbing tax."
topic: "en/graph-neural-networks-for-financial-networks"
order: 7
minutes: 25
updated: 2026-06-23
---

import { Callout, FinalExam, MindMap } from '@/components/react';

This is the graded finale for **Graph Neural Networks for Financial Networks**, and it runs across the whole arc the course built. You started by refusing the small act of vandalism that opens most ML pipelines — flattening an interconnected world into a feature table and silently deleting the relationships where the expensive risks live. You learned to see markets as graphs instead: nodes carry the old columns, edges carry who-owes-whom, and the choice of directed-versus-undirected and weighted-versus-binary is never cosmetic because direction is risk direction. From there you built the engine — **message passing**, where each node aggregates from its neighbours with a permutation-invariant pool and then updates itself, growing its receptive field one hop per layer — and pointed it at the three jobs: node-level, edge-level, and graph-level. You walked the architecture lineage from fixed degree-normalised **GCN** through inductive **GraphSAGE** and attention-weighted **GAT** to **temporal GNNs** for a topology that will not hold still. Then the killer application: systemic risk, the default cascade round by round, **DebtRank** as a walk-once measure of impact, and fire-sale feedback that makes the system nonlinear. You aimed the toolbox at money — AML ring detection, relational peer-firm alpha, on-chain wallet analytics — and finally took the cold shower: graphs leak harder than any table, over-smoothing punishes depth, topology is non-stationary, and the data-plumbing tax decides the outcome while everyone argues about the architecture. No hints are shown, each answer **locks the moment you submit**, and your score stays hidden until the very end.

## Course Recap

<MindMap
  client:visible
  title="Graph Neural Networks for Financial Networks — the whole arc"
  caption="Six lessons, one ladder: from markets-as-graphs to the honest audit."
  root={{
    label: "GNNs for Financial Networks",
    children: [
      {
        label: "1 · Markets are graphs",
        children: [
          { label: "Flattening to a table deletes the adjacency" },
          { label: "Graph = nodes plus edges; directed and weighted choices matter" },
          { label: "Four finance graphs: interbank, supply chain, correlation, on-chain" },
        ],
      },
      {
        label: "2 · Message passing and three tasks",
        children: [
          { label: "Aggregate from neighbours, then update self" },
          { label: "Permutation-invariant pool; K layers reach K hops" },
          { label: "Node-level, edge-level, graph-level jobs" },
        ],
      },
      {
        label: "3 · GCN, GraphSAGE, GAT, temporal",
        children: [
          { label: "GCN: fixed degree-normalised average, transductive" },
          { label: "GraphSAGE: sample, learn aggregator, inductive" },
          { label: "GAT: learned attention; temporal GNN for moving topology" },
        ],
      },
      {
        label: "4 · Systemic risk and contagion",
        children: [
          { label: "Cascade: fail when cumulative loss exceeds the buffer" },
          { label: "DebtRank: walk-once impact, not probability" },
          { label: "Fire-sale feedback multiplier one over one minus feedback" },
        ],
      },
      {
        label: "5 · Fraud rings and relational alpha",
        children: [
          { label: "AML: fan-in, chains, cycles, dense clusters" },
          { label: "Relational alpha: propagate a surprise along economic edges" },
          { label: "On-chain: public pseudonymous wallet graph" },
        ],
      },
      {
        label: "6 · The honest audit",
        children: [
          { label: "Leakage radius equals the layer count" },
          { label: "Topology-aware split; over-smoothing punishes depth" },
          { label: "Non-stationary topology; the data-plumbing tax" },
        ],
      },
    ],
  }}
/>

<Callout variant="warning" title="One run, one shot">
This is a **graded, irreversible** exam. There are 24 questions, shown one at a time. The instant you submit a question it **locks** — there is no Back button, no retry, and no Restart. A wrong answer simply fails that question and the exam moves on; you cannot revisit it. Your running score is **hidden** until the final screen. The pass mark is **70%**. Some questions accept more than one correct option — read **every** option before you commit, because once you submit you own the answer.
</Callout>

<FinalExam
  client:visible
  passPercent={70}
  questions={[
    {
      question: "A risk team loads a CSV with one row per bank — assets, leverage, capital ratio — and trains a model. Which fact about a bank's risk is structurally impossible for this representation to hold, no matter how many columns it adds?",
      options: [
        { text: "That this bank is owed a large sum by the riskiest borrower in the set — a relationship between two entities", correct: true },
        { text: "The bank's own leverage ratio" },
        { text: "The bank's reported capital ratio" },
        { text: "The bank's total assets in billions" },
      ],
      explanation: "A feature table stores each entity's own attributes in isolation. A loan from one bank to another is a relationship — the adjacency — and there is no cell in a flat table for who is connected to whom. Adding more per-bank columns never recovers it; the omission is in the shape of the container.",
    },
    {
      question: "Bank A lends 20 billion to bank D, while an analyst tracks the correlation between two stocks. Pick the statements that correctly describe how each relationship should be represented as an edge. (Select all that apply.)",
      options: [
        { text: "The loan edge is directed, because A is exposed to D but D is not thereby exposed to A", correct: true },
        { text: "The correlation edge is undirected, because if one stock correlates with the other it correlates back by definition", correct: true },
        { text: "The loan edge should be undirected and binary so the model treats both banks symmetrically" },
        { text: "The correlation edge must be directed because every financial edge carries a direction" },
      ],
      explanation: "A loan has a giver and a receiver, so its edge is directed and the weight matrix is asymmetric — symmetrizing it would tell the model A's and D's exposures are interchangeable when they are opposite. Correlation is mutual and symmetric, so its edge is undirected. Not every financial edge is directed.",
      allowMultiple: true,
    },
    {
      question: "In an undirected graph with edges A to B, A to C, and C to D, a junior analyst ranks bank A as the most systemically dangerous because it ties the highest degree. Where is the flaw in that reasoning?",
      options: [
        { text: "Degree is a raw count of connections; systemic importance depends on the size of the exposures and the fragility of the counterparties, not the headcount", correct: true },
        { text: "Degree cannot be computed at all for an undirected graph" },
        { text: "Highest degree always means highest systemic risk, so there is no flaw" },
        { text: "A high degree always means a bank is safely diversified" },
      ],
      explanation: "A and C both have degree 2 here, but degree only counts links. A bank with a couple of enormous exposures to shaky counterparties can matter far more than one with many tiny links to healthy ones. Importance is a weighted, recursive property — exactly what DebtRank later formalizes — not a count.",
    },
    {
      question: "An analyst needs to answer: if a key supplier shuts down, which downstream manufacturers also stall? Which representation can answer this, and why can a per-firm feature table not?",
      options: [
        { text: "A directed supply-chain graph, because the answer lives in propagation along supplier-to-customer edges that a table has no place to store", correct: true },
        { text: "A feature table of per-firm revenue and headcount, because those columns fully describe disruption" },
        { text: "A single firm's price time series fed to a sequence model, because time captures the chain" },
        { text: "Any of them; the representation is irrelevant to a propagation question" },
      ],
      explanation: "Propagation questions are graph questions. Ratios, headcounts, and revenues are per-entity attributes a table handles fine, but if one firm stalls then who else stalls is a question about edges. Only the directed supplier-to-customer graph encodes the chain to trace the disruption along.",
    },
    {
      question: "A target node has its own value 0.2 and three neighbours holding 0.6, 0.4, and 0.8. Using a mean aggregate and an update that averages the node's own value with the aggregate, what is its new embedding?",
      options: [
        { text: "0.4 — the aggregate is (0.6 plus 0.4 plus 0.8) divided by 3 equals 0.6, then (0.2 plus 0.6) divided by 2 equals 0.4", correct: true },
        { text: "0.6 — the new value is just the neighbour mean" },
        { text: "0.2 — the node keeps its own value and ignores neighbours" },
        { text: "1.0 — you add the node's value to the aggregate" },
      ],
      explanation: "Aggregate first: the three neighbours average to 1.8 divided by 3 equals 0.6. Then update by averaging the node's own value with that aggregate: (0.2 plus 0.6) divided by 2 equals 0.4. Keeping the self term is exactly what stops the answer collapsing to the neighbour mean of 0.6.",
    },
    {
      question: "Why must the AGGREGATE function in a GNN be permutation-invariant, and why does naive concatenation of the neighbour features fail?",
      options: [
        { text: "Neighbours are an unordered, variable-size set; concatenation gives different-length inputs and would attribute meaning to a listing order the graph never had", correct: true },
        { text: "Concatenation is too slow to compute on modern hardware" },
        { text: "Permutation invariance is optional and only matters for directed graphs" },
        { text: "Concatenation always produces a number larger than one" },
      ],
      explanation: "A node's neighbours have no canonical first or last, and different nodes have different numbers of them. Concatenation breaks twice over: a fixed-size layer cannot accept variable-length inputs, and reordering the same neighbourhood would change the vector. Sum, mean, and max are symmetric pools that sidestep both at once.",
    },
    {
      question: "An interbank network has average degree about 3. Roughly how many institutions can a node feel with a 3-layer GNN, and what is the mechanism?",
      options: [
        { text: "About 39 — each layer adds one more hop, and reach multiplies by the degree, so roughly 3, then 3 plus 9, then plus 27", correct: true },
        { text: "Exactly 3, because the number of layers does not change reach" },
        { text: "The entire network, because any GNN sees everything regardless of depth" },
        { text: "About 6, because 3 layers reaches 2 times the degree" },
      ],
      explanation: "K layers reach K hops because each layer's neighbours have already absorbed their own neighbours the layer before. With degree 3: about 3 at one layer, about 12 at two, about 39 at three. The reach explodes because each hop multiplies by the degree — the power and the danger of stacking layers.",
    },
    {
      question: "You need a single verdict on whether an entire transaction subgraph is a laundering pattern. Which task type is this, and what extra step does it require that node-level scoring does not?",
      options: [
        { text: "Graph-level: a permutation-invariant readout pools all node embeddings into one graph vector, which you then classify", correct: true },
        { text: "Node-level: score the single busiest account and use that as the whole graph's answer" },
        { text: "Edge-level: take the dot product of two representative node embeddings" },
        { text: "No pooling is needed; the verdict can be read off any single node" },
      ],
      explanation: "A whole-graph verdict needs a readout — a permutation-invariant pool (sum, mean, or max) over every node embedding, producing one fixed-size graph vector to classify. Laundering is a pattern across accounts (fan-in, fan-out, round-tripping), so the verdict must live on the graph, not on any one node.",
    },
    {
      question: "A small bank v (degree 3) has neighbours a (degree 3) and b (degree 12), plus a self-loop, in a GCN. Why does the hub neighbour b receive less weight than a?",
      options: [
        { text: "The fixed normalisation one over the square root of deg(v) times deg(u) shrinks edges to high-degree hubs — b's degree 12 makes its weight one over six versus one over three for a", correct: true },
        { text: "The model learned an attention score that down-weighted b" },
        { text: "b was randomly dropped from the sampled neighbour set that step" },
        { text: "b happened to have a smaller feature value" },
      ],
      explanation: "GCN's per-edge weight is structural, not learned: one over the square root of 3 times 3 equals one third for a, but one over the square root of 3 times 12 equals one sixth for the hub b. The degree normalisation automatically turns down the volume of high-degree nodes before any learning happens.",
    },
    {
      question: "Your interbank graph gains about 40 new institutions each quarter that must be scored immediately, without retraining the whole model. Which architecture is the natural default, and why?",
      options: [
        { text: "GraphSAGE — it is inductive, learning a reusable sample-and-aggregate function that embeds brand-new nodes the model never saw in training", correct: true },
        { text: "GCN — its degree normalisation embeds new nodes for free" },
        { text: "A static GAT trained once and never updated again" },
        { text: "Any of them, since inductiveness is irrelevant to scoring new nodes" },
      ],
      explanation: "Constantly arriving new entities is the inductive use case. GCN is transductive — it needs the whole fixed graph at train time and changing the node set changes the degrees and normalisation. GraphSAGE learns a function rather than per-node embeddings, so the same trained weights embed banks that did not exist in training.",
    },
    {
      question: "A GAT node has three neighbours with raw learned scores 2.0, 1.0, and 0.0 and neighbour values 10, 4, and 1. Using exp(2.0) about 7.389, exp(1.0) about 2.718, exp(0.0) equals 1.0, what is the attention-weighted aggregate (with weight matrix 1 and identity activation)?",
      options: [
        { text: "About 7.72 — softmax weights about 0.665, 0.245, 0.090, then 0.665 times 10 plus 0.245 times 4 plus 0.090 times 1", correct: true },
        { text: "Exactly 5.0 — a plain average of 10, 4, and 1" },
        { text: "About 15.0 — the sum of the three neighbour values" },
        { text: "About 0.665 — just the largest attention weight" },
      ],
      explanation: "The exponentials sum to about 11.107, giving softmax weights about 0.665, 0.245, and 0.090 (which sum to one). The weighted aggregate is 6.65 plus 0.98 plus 0.09 equals about 7.72 — pulled sharply above the plain mean of 5.0 toward the neighbour the model scored highest.",
    },
    {
      question: "A risk team trains a single static GCN on a graph built by averaging a full year of daily lending edges. What are the genuine hazards of that time-averaging? (Select all that apply.)",
      options: [
        { text: "It can fabricate edges that never coexisted, joining a counterparty from January with one from June as if simultaneous", correct: true },
        { text: "It smears a trend, hiding a Monday-to-Friday collapse in funding behind a single average number", correct: true },
        { text: "Averaging always improves a graph by reducing noise, so there is no hazard" },
        { text: "A GCN cannot run on weighted graphs at all, so averaging is required" },
      ],
      explanation: "Collapsing a dynamic network into one averaged graph blends incompatible regimes: it can join edges that never existed at the same time and it erases the trajectory a temporal model would catch. It can also leak the future into the past if future snapshots inform a past embedding. GCNs do run on weighted graphs, so that option is false.",
      allowMultiple: true,
    },
    {
      question: "Two banks report identical balance sheets — same capital, leverage, and asset quality — so a per-firm tabular model scores them identically. Why can this be dangerously wrong?",
      options: [
        { text: "Their risk depends on their position in the lending network; one may borrow from a teetering hub and the other from rock-solid lenders, and the identical rows cannot encode that", correct: true },
        { text: "Identical balance sheets guarantee identical risk in every meaningful sense" },
        { text: "The model merely forgot to include the stock-price column" },
        { text: "Tabular models are wrong only because they run more slowly than graph models" },
      ],
      explanation: "A per-firm model scores each bank from its own features, so identical rows earn identical scores. But one bank's 80 million lent to a fragile hub can wipe out a 50 million buffer when that hub defaults, while the other's 80 million is spread across solid counterparties. The risk that differs lives in the edges the table threw away.",
    },
    {
      question: "Trace this cascade. Bank C (the seed) defaults. B had lent 120 to C with LGD 50 percent and a buffer of 50; A had lent 90 to B with LGD 50 percent and a buffer of 40. Who fails?",
      options: [
        { text: "C, then B (loss 60 exceeds 50), then A (loss 45 exceeds 40) — all three fail, with A dying two hops from the seed it never lent to", correct: true },
        { text: "Only C; the cascade stops because A and B lent to different banks" },
        { text: "C and B, but A survives because 45 is below its buffer" },
        { text: "C and A, but B survives because it is closer to the seed" },
      ],
      explanation: "Round 1: B's loss is 0.50 times 120 equals 60, which exceeds its buffer of 50, so B fails. Round 2: now that B has defaulted, A's loss is 0.50 times 90 equals 45, which exceeds its buffer of 40, so A fails too. Contagion reached A even though A never lent a cent to the original defaulter C — damage travels along paths.",
    },
    {
      question: "Distress a node 1 to full distress (level 1) in a DebtRank network where node 1 passes relative-loss weight 0.8 to node 2, and node 2 passes 0.5 to node 3, with equal value weights of one third each. What is the DebtRank (impact beyond the seed)?",
      options: [
        { text: "0.40 — node 2 reaches 0.8 and node 3 reaches 0.5 times 0.8 equals 0.4, so one third times (0.8 plus 0.4) equals 0.40", correct: true },
        { text: "1.00 — the seed's own full distress counts toward the total" },
        { text: "0.60 — you add the two relative-loss weights 0.8 and 0.5 minus 0.7" },
        { text: "2.20 — distress is allowed to loop back and re-inflate the total" },
      ],
      explanation: "Node 1 passes 0.8 times 1 to node 2, then goes inactive; node 2 passes 0.5 times 0.8 equals 0.4 to node 3, then goes inactive. DebtRank is the value-weighted distress of everyone other than the seed: one third times 0.8 plus one third times 0.4 equals 0.40. The walk-once rule (node 1 inactive) is what stops the total looping past any sensible bound.",
    },
    {
      question: "A DebtRank score of 0.40 for a bank is best interpreted as which of the following?",
      options: [
        { text: "If this bank becomes distressed, an additional 40 percent of the system's economic value ends up distressed — a measure of impact, not the bank's probability of failing", correct: true },
        { text: "A 40 percent chance that this bank defaults next year" },
        { text: "The bank holds 40 percent of the system's total assets" },
        { text: "The bank is 40 percent likely to be bailed out by regulators" },
      ],
      explanation: "DebtRank measures systemic impact given distress, not likelihood. A bank can have a tiny default probability and a huge DebtRank (a too-big-to-fail hub) or a large default probability and a tiny DebtRank (a fragile but isolated minnow). Conflating impact with probability is how you mis-rank what to protect.",
    },
    {
      question: "A shock causes 100 of direct losses, and each unit of forced selling creates an extra 0.4 of mark-to-market losses that trigger still more selling. What is the total loss, and what does it expose about a linear model?",
      options: [
        { text: "About 167 — the multiplier one over (one minus 0.4) is about 1.67, so a linear model booking only the first-round 100 under-reserves the tail by about 67", correct: true },
        { text: "Exactly 100, because the feedback cancels out in equilibrium" },
        { text: "About 140, and a linear model captures the full amount perfectly" },
        { text: "About 40, because the feedback reduces total losses" },
      ],
      explanation: "The losses form a geometric series with ratio 0.4: total equals 100 times one over (one minus 0.4) equals 100 times 1.67 equals about 167. A linear per-firm model has no feedback term, so it stops at the first-round 100 and systematically understates crisis-tail losses. The multiplier explodes as feedback approaches one.",
    },
    {
      question: "How does a GNN's message passing relate to the structural systemic-risk measures, and what is the correct way to deploy it?",
      options: [
        { text: "Message passing implements 'your fragility depends on your neighbours' fragility' — the same recursion DebtRank and the cascade hand-code — so the GNN is a fast, differentiable surrogate that must be validated against the simulation", correct: true },
        { text: "It proves the simulation is unnecessary, so the cascade model can be discarded" },
        { text: "It works with no labels and no validation, discovering contagion on its own" },
        { text: "It only averages each bank's own features and never uses the graph at all" },
      ],
      explanation: "A GNN layer updates each node from its neighbours' states, stacked over layers for multi-hop reach — exactly the cascade and DebtRank recursion, but learned and differentiable. It is a surrogate and feature-enricher, fast and topology-aware, that must be checked against the structural simulation (especially in the tail), not a replacement for it.",
    },
    {
      question: "A launderer splits 90,000 into ten transfers of 9,000 — each below a 10,000 reporting threshold — into one collector account. Why does a per-transaction rule miss this while a graph node-classifier catches it?",
      options: [
        { text: "Each transfer is individually below threshold; only aggregating the collector's neighbourhood (in-degree ten, inflow ninety thousand, ten distinct senders) reveals the fan-in pattern", correct: true },
        { text: "Because 9,000 is actually above the threshold and the rule should fire" },
        { text: "Because graph models cannot read transfer amounts" },
        { text: "Because the collector account is legally exempt from monitoring" },
      ],
      explanation: "Structuring (smurfing) is engineered to defeat per-transaction thresholds — no single transfer looks guilty. A node classifier whose message-passing step computes neighbourhood aggregates reads the in-degree, summed inflow, and distinct-sender count and scores the collector as suspicious. Each edge was legal; the node's aggregated structure is not.",
    },
    {
      question: "An AML model ranks a payroll processor and a popular exchange as its top two laundering suspects. What is this, and what is the fix?",
      options: [
        { text: "A false-positive trap — legitimate hubs share the same fan-in and fan-out shape as rings, so a degree-only model over-flags; fix it with finer behavioural, timing, and known-entity features", correct: true },
        { text: "Correct detections, because any high-degree node is laundering by definition" },
        { text: "Proof the model cannot read edge directions and should be discarded" },
        { text: "Evidence that exchanges and payroll firms never appear in transaction graphs" },
      ],
      explanation: "Hub shape is not guilt. A payroll processor fans out to thousands of employees and an exchange fans in from thousands of customers for entirely legitimate reasons. A model keyed on degree or motif counts alone buries investigators in false positives; the fix is to learn the finer distinctions that exonerate legitimate hubs.",
    },
    {
      question: "A semiconductor supplier with revenue-share dependence of 0.5 for customer A, 0.25 for B, and 0.1 for C posts a plus-8-percent earnings surprise, propagated with a decay factor of 0.5. Which customer gets the largest expected nudge, and how big?",
      options: [
        { text: "Customer A, about plus 2.0 percent — decay 0.5 times weight 0.5 times surprise 8 percent", correct: true },
        { text: "Customer C, about plus 4 percent, because the least-exposed firm reacts most" },
        { text: "All three get an identical nudge regardless of dependence" },
        { text: "The supplier itself, because surprises never cross firm boundaries" },
      ],
      explanation: "The propagated one-hop signal is decay times edge weight times surprise. For A that is 0.5 times 0.5 times 8 percent equals 2.0 percent, the largest because A is the most dependent on the supplier. Weighting the message by dependence — how exposed each customer is — is the whole point of relational alpha.",
    },
    {
      question: "Relational alpha that glowed in backtest often fades once deployed. Which of these are real reasons it erodes live? (Select all that apply.)",
      options: [
        { text: "It decays as the lead-lag gets arbitraged once others run the same supplier-customer propagation", correct: true },
        { text: "The supply-chain edges are scraped from lagging filings and vendor data, so you propagate along relationships that may have ended", correct: true },
        { text: "Earnings surprises stop happening entirely once a strategy goes live" },
        { text: "Graph models forget how to perform message passing in production" },
      ],
      explanation: "Crowding arbitrages the edge away, and the supply-chain map itself lags reality by quarters, so the edges are noisy and stale. The lead-lag can also reverse — sometimes the customer's order book leads the supplier. Surprises do not stop occurring, and the message-passing math does not change in production; those options are false.",
      allowMultiple: true,
    },
    {
      question: "You build a fraud GNN, get AUC 0.94 on a random 80/20 node split, and a colleague insists on re-splitting by time (train before March, test after). What happens and why?",
      options: [
        { text: "The AUC drops sharply — the random split let test nodes message-pass with training neighbours and leak the answer; a temporal split removes that crutch and exposes the honest, lower number", correct: true },
        { text: "The AUC rises, because temporal splits give the model more recent data" },
        { text: "The AUC is unchanged, because the split scheme cannot affect a GNN" },
        { text: "The model refuses to train on a temporal split" },
      ],
      explanation: "A node's embedding is a function of its K-hop neighbourhood, so a random split scatters connected nodes across train and test and message passing carries the answer across the boundary. The leakage radius equals the layer count. A temporal split is the finance default, and the AUC almost always falls because the leaky crutch is gone.",
    },
    {
      question: "A team's GNN underperforms a gradient-boosted tree, so they push the depth from 3 layers to 8. What happens to the node embeddings, and why is this backwards?",
      options: [
        { text: "Over-smoothing — each extra layer averages over a larger neighbourhood, pulling embeddings toward a common vector until nodes are indistinguishable and discrimination collapses", correct: true },
        { text: "The embeddings become sharper and more distinct, fixing the underperformance" },
        { text: "Nothing changes; layer count does not affect the embeddings" },
        { text: "The model overfits the labels but the embeddings stay well separated" },
      ],
      explanation: "Stacking message-passing layers repeatedly averages neighbourhoods, so embeddings converge toward the global mean — over-smoothing — and once two nodes share a representation no classifier can tell them apart. Unlike a CNN where depth builds richer features, GNN depth past a few layers destroys information. The cure is usually fewer layers plus skip connections or normalization, not more depth.",
    },
  ]}
/>

<Callout variant="success" title="Where this leaves you on the quant ladder">
Pass or fail, you now carry the habit that separates a graph practitioner from someone running a tutorial on the wrong data: you refuse to flatten an interconnected market into a table, you can name the right finance graph and its edge semantics, you know that message passing is just aggregate-then-update growing one hop per layer, and you can pick the architecture — GCN, GraphSAGE, GAT, or a temporal GNN — whose inductive bias fits a growing, non-stationary network. You can trace a default cascade and a DebtRank walk by hand, size a fire-sale multiplier, and aim the whole toolbox at AML rings and relational alpha without mistaking suspicion for proof or a stale edge for signal. Above all you can *audit* a graph result — demanding a topology-aware temporal split, a shallow network that dodges over-smoothing, a regime-checked topology, and a metric deflated for trials — before you trust a single number it prints. That judgment, more than any architecture, is what lets a financial GNN earn its keep.
</Callout>
