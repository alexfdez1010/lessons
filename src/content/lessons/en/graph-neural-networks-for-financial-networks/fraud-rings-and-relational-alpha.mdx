---
title: "Fraud Rings and Relational Alpha"
description: "Aiming graph learning at money: AML ring detection on transaction graphs where the signal is the structure and no single account looks guilty, relational and peer-firm alpha that propagates a signal across supply-chain and co-movement edges, and on-chain analytics on wallet graphs."
topic: "en/graph-neural-networks-for-financial-networks"
order: 5
minutes: 22
updated: 2026-06-23
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, MatchConcepts, Categorize, MindMap } from '@/components/react';

So far this course has built the machinery: graphs, node and edge and graph-level
tasks, message passing, the contagion dynamics that ripple through a network. Lovely
theory. Now we point all of it at the two things that actually fund a quant desk and a
compliance team: **catching money that is being hidden** and **predicting money that
is about to move**.

Here is the unifying punchline, up front. Fraud detection and relational alpha are not
new problems with new math — they are *the exact node, edge, and graph tasks from the
graph-tasks lesson, aimed at money instead of toy citation networks*. The structure
**is** the signal. A laundering ring is invisible one account at a time and obvious as
a subgraph. A supplier's earnings surprise is silent on the customer's own price chart
and loud along the supply-chain edge that connects them. Graph learning earns its keep
exactly where the value lives between the nodes rather than inside them.

This lesson is the "point it at money" capstone before the honest audit. We will be
witty about it, but the caveats are deadly serious — and the next lesson does nothing
but stress-test them.

## The unifying idea: same graph tasks, aimed at money

<MCQ
  client:visible
  pretest
  question="A team wants to (a) flag laundering accounts, (b) predict a firm's return from its suppliers, and (c) discover hidden links between wallets. In graph-learning terms, what are these three?"
  options={[
    { text: "Node/graph classification, node regression with signal propagation, and link prediction — the same three task types from the graph-tasks lesson, aimed at money", correct: true },
    { text: "All three are link prediction; everything in finance is about edges" },
    { text: "All three require a brand-new task type unique to finance" },
    { text: "They are unrelated problems with no shared structure" },
  ]}
  explanation="Fraud flagging is node classification (illicit account) or subgraph/graph classification (a laundering pattern); relational alpha is node regression with the signal propagated along edges; finding hidden ties is link prediction. Same toolbox, money on the line."
/>

**Analogy.** You already own the toolbox. Earlier lessons sold you a hammer, a saw, and
a drill and made you practice on scrap wood. This lesson hands you the actual house.
Nothing about the tools changed — you are just hitting nails that are worth something.

**Definition.** Every problem in this lesson maps onto one of the canonical graph
machine-learning tasks:

- **Node classification / regression** — label or score each node from its features
  *and* its neighbourhood $\mathcal{N}(v)$. Fraud: is this account illicit (a label)?
  Alpha: what return will this firm post (a number)?
- **Subgraph / graph classification** — label a *whole pattern* rather than a single
  node. Fraud: is this little cluster of accounts a laundering ring?
- **Link prediction** — score node pairs for a *missing or hidden* edge. Fraud: do
  these two pseudonymous wallets secretly share an owner? Alpha: is there an
  undisclosed supply relationship?

**Worked example.** Imagine a single account that sends fifty small payments to fifty
distinct counterparties and receives none. A per-row tabular model sees fifty boring
rows. A node classifier with one round of message passing sees a node whose
out-degree is fifty and in-degree is zero — a fan-out hub — and that *structural*
feature, computed by aggregating over $\mathcal{N}(v)$, is what tips the score. Same
data; the graph framing surfaces a feature the flat table never had a column for.

| Money task | Graph-learning task | Where the signal lives |
|---|---|---|
| Flag a laundering account | Node classification | In the node's neighbourhood pattern |
| Recognise a laundering ring | Subgraph / graph classification | In the whole motif |
| Forecast a firm's return from peers | Node regression + propagation | Along economic edges |
| Find a hidden ownership/supply tie | Link prediction | In the absent-but-likely edge |

**Misconception.** "Graph models replace the features and models I already have." No —
they *augment* them. The node's own features (transaction size, firm fundamentals,
price history) still matter; the graph adds the *relational* channel on top. Throwing
away node features to go "pure graph" usually throws away signal.

### When to use it

Frame a finance problem as graph learning when the value is **relational** — when an
entity is suspicious or predictable *because of who it is connected to*, not just its
own attributes. If a flat per-entity model with no neighbourhood information already
nails it, you do not need a graph; the graph earns its complexity only when structure
carries signal the table cannot.

## AML ring detection on transaction graphs

<MCQ
  client:visible
  pretest
  question="A launderer splits 90,000 units into ten transfers of 9,000 so each stays under a 10,000 reporting threshold, then funnels them into one collector account. Why does a per-account rule miss this?"
  options={[
    { text: "Each transfer is individually innocuous and below threshold; the crime is only visible as the fan-in PATTERN across accounts", correct: true },
    { text: "Because 9,000 is actually above the threshold" },
    { text: "Because per-account rules always catch structured laundering perfectly" },
    { text: "Because the collector account is exempt from monitoring" },
  ]}
  explanation="Structuring (smurfing) is engineered to defeat per-transaction thresholds. No single transfer looks guilty; the aggregate fan-in into one collector is the tell — a structural pattern only a graph view exposes."
/>

**Analogy.** A laundering ring is a heist where every individual on camera is just
buying groceries. One person buys flour, another buys yeast, a third buys a delivery
van — all perfectly legal. Only when you draw the lines between them do you see a
bakery being built to launder bread money. The crime is in the *graph*, not in any
single frame.

**Definition.** Money laundering classically runs in three stages: **placement** (get
dirty cash into the system), **layering** (shuffle it through many transactions to
obscure its origin), and **integration** (bring it back looking clean). Anti-money-
laundering (AML) is the work of detecting this. A **transaction graph** models it
directly: **nodes** are accounts or wallets, **directed weighted edges** are transfers
(direction = who paid whom, weight = amount and/or count, often timestamped). A
graph neural network labels a node by aggregating its neighbourhood, so an account can
"look guilty by association and by pattern" even when its own transactions are dull.

**Why per-account rules miss it.** Threshold rules ("flag any transfer above ten
thousand") are trivially defeated by design. Launderers deliberately *structure*
activity below thresholds and *spread* it across many accounts. The three structural
escapes:

- **Fan-in** (smurfing): many small inbound transfers converge on a collector.
- **Fan-out**: one source sprays funds to many mules.
- **Layering chains / cycles**: long directed paths or loops that recycle funds to
  blur their origin.

**Worked example — the fan-in that beats a threshold.** Ten "smurf" accounts each send
9000 units into one collector account. Each transfer is below the 10000 reporting
threshold, so a per-transaction rule fires zero alerts. But aggregate the collector's
neighbourhood:

$$ \text{inflow}(c) = \sum_{u \in \mathcal{N}^{-}(c)} w_{u \to c} = 10 \times 9000 = 90{,}000, $$

where $\mathcal{N}^{-}(c)$ is the set of accounts sending *into* collector $c$. The
collector's in-degree is ten and its total inflow is ninety thousand units — comfortably
into reporting territory — and it then forwards roughly that whole sum onward in one or
two hops. A node classifier whose message-passing step computes neighbourhood
aggregates (degree, summed inflow, count of distinct senders) reads those features and
scores the collector as suspicious. Each *edge* was legal; the *node's aggregated
structure* is not. That gap is the entire reason graphs beat flat rules here.

| View | What it sees at the collector | Verdict |
|---|---|---|
| Per-transaction threshold rule | Ten transfers, each 9000 (below 10000) | No alert |
| Graph node aggregation | In-degree ten, inflow ninety thousand, ten distinct senders, fast forward-out | Suspicious |

<Callout variant="warning" title="Suspicion is not proof">
A graph fraud model outputs a ranked list of suspicion, never a conviction. Two hard
realities ride along. First, extreme class imbalance: genuine laundering may be well
under one in a thousand accounts, so a model that screams "all clean" is 99.9 percent
accurate and 100 percent useless — you must evaluate with precision and recall on the
rare class, not raw accuracy. Second, the adversary adapts: as soon as fan-in is
flagged, launderers lengthen chains, add legitimate-looking intermediaries, and mimic
normal traffic. A static model decays the moment it ships.
</Callout>

### When to use it

Use graph AML when laundering is *structured* — deliberately fragmented to slip under
per-entity rules — which is most of the serious cases. Treat the model as an alert
prioritiser feeding human investigators, not an autonomous judge: it raises the most
structurally suspicious nodes to the top of a queue, and people (with legal standards
of proof) decide. If your laundering is just one giant obvious transfer, a threshold
rule already catches it and you do not need the graph.

<FillBlank
  client:visible
  question="Fill in why structured laundering defeats simple rules."
  text="In a fan-in (smurfing) ring, each individual transfer is {{below the reporting threshold|above the reporting threshold|completely unrecorded}}, so the crime is visible only as the aggregate pattern across the collector's neighbourhood."
  explanation="Smurfing is engineered to keep every single transfer under the threshold; only the summed inflow over the collector's neighbourhood reveals it."
/>

## Typologies and motifs: matching crimes to shapes

<MCQ
  client:visible
  pretest
  question="An AML team's graph model floods investigators with false positives by flagging every high-degree hub. What is the most likely cause?"
  options={[
    { text: "Legitimate businesses — exchanges, payroll processors, merchants — also have hub-shaped fan-in/fan-out structure, so degree alone over-flags", correct: true },
    { text: "High-degree nodes are always criminal, so the flags are all correct" },
    { text: "The model cannot read edge directions at all" },
    { text: "Hubs never appear in real transaction graphs" },
  ]}
  explanation="Lots of perfectly legal entities are hubs: a payroll processor fans out to thousands of employees; an exchange fans in from thousands of customers. A model keyed on degree alone will bury investigators in false positives unless it learns finer motif and behavioural distinctions."
/>

**Analogy.** Detectives keep a catalogue of *modus operandi* — the safecracker who
always drills the same spot, the forger who reuses one watermark. AML has the same
playbook, except the "MO" is a **shape in the graph**. Learn the shapes and you learn
the crimes.

**Definition.** A **typology** is a recognised laundering pattern; a **motif** is the
small recurring subgraph that encodes it. Detecting them is **subgraph / motif
counting** (how many times does this shape appear around a node?) feeding into a graph-
or node-level classifier. The staple typologies:

| Typology | Graph motif | Why a flat table misses it |
|---|---|---|
| Smurfing | Fan-in: many senders into one collector | Each inbound transfer is small and below threshold; only the convergence is the tell |
| Layering | Long directed chain A to B to C to D | Each hop looks like an ordinary payment; the depth of the chain is invisible per-row |
| Round-tripping | Cycle: funds return to origin via intermediaries | No single row shows the loop closing back on itself |
| Mule network | Dense many-to-many cluster of pass-through accounts | Each account holds funds briefly; the cluster-level "hot potato" behaviour needs the graph |

**Worked example — counting the loop.** Round-tripping hides in a directed cycle. Take
account $A \to B \to C \to A$. A per-account rule sees three accounts that each
sent and received one transfer — utterly normal. A motif counter searches for directed
3-cycles and finds exactly one passing through $A$. If $A$ sits on, say, seven distinct
short directed cycles when a typical account sits on zero, that cycle-count feature
($7$ versus a baseline near $0$) is a strong structural flag. The arithmetic is just
counting closed directed paths — but it is a count the flat table has no column for.

**Misconception / pitfall.** "Hub shape equals guilt." Emphatically no. A payroll
processor *is* a giant fan-out. A popular exchange *is* a giant fan-in. A merchant
acquirer *is* a dense many-to-many cluster. If your model keys on degree or motif
counts alone, it will rank every legitimate hub as a top suspect and drown your
investigators in false positives — the exact failure the pretest described. The fix is
to learn the *finer* distinctions (timing, velocity, counterparty mix, known-entity
labels) that separate a laundering cluster from a Tuesday at a payment processor.

### When to use it

Reach for motif and typology features when you have a known catalogue of laundering
shapes and want interpretable, auditable flags ("this account sits on seven cycles").
They pair well with a GNN: hand-crafted motif counts give a regulator-friendly
explanation, while the learned message passing catches variants the catalogue missed.
Do not deploy motif counts naked against the false-positive problem — always combine
with features that exonerate legitimate hubs.

<Categorize
  client:visible
  question="Match each laundering typology to the graph structure that exposes it."
  buckets={["Fan-in (convergence)", "Directed chain or cycle", "Dense cluster"]}
  items={[
    { text: "Smurfing: many small deposits into one collector", bucket: "Fan-in (convergence)" },
    { text: "Layering: A to B to C to D pass-through", bucket: "Directed chain or cycle" },
    { text: "Round-tripping: funds loop back to the origin", bucket: "Directed chain or cycle" },
    { text: "Mule network: many accounts shuffling funds among themselves", bucket: "Dense cluster" },
    { text: "Ten accounts each wiring a sub-threshold amount to one node", bucket: "Fan-in (convergence)" },
  ]}
  explanation="Smurfing and structured deposits are convergence/fan-in motifs; layering and round-tripping are chains and cycles; mule networks are dense many-to-many clusters. Each typology has a signature shape."
/>

## Relational / peer-firm alpha

<MCQ
  client:visible
  pretest
  question="A key automotive-chip supplier posts a strong positive earnings surprise before its carmaker customers report. How can a graph model turn this into alpha on the customers?"
  options={[
    { text: "Propagate a fraction of the supplier's surprise along supply-chain edges to its customers, weighted by how much each customer depends on that supplier", correct: true },
    { text: "Buy the supplier again, since the surprise only ever affects the supplier itself" },
    { text: "Ignore it entirely because earnings surprises never spill across firms" },
    { text: "Short every firm in the market equally, regardless of any relationship" },
  ]}
  explanation="A supplier's results lead its customers' results when the customers depend on that input. A GNN sends a weighted share of the supplier's surprise across the supply-chain edge — message passing used as alpha, not just classification."
/>

**Analogy.** A supplier's bad quarter is the cough before the customer's flu. If the
factory that makes your only engine part just warned on demand, you do not need to wait
for the carmaker's own report to update your view — the symptom already travelled down
the supply chain. Relational alpha is listening to the cough.

**Definition.** **Relational (peer-firm) alpha** propagates a *signal* — an earnings
surprise, a news-sentiment shock, an analyst revision — from one firm along **economic
edges** (supplier-customer links, same-sector co-movement, shared ownership) to predict
a *connected* firm's return. In graph terms it is **node regression** where each node's
target (forward return) is informed by messages aggregated from $\mathcal{N}(v)$. This
is **message passing used as alpha**: the same aggregate-from-neighbours operation that
flags a laundering hub, repurposed to forecast a price.

**Worked example — weighted propagation of a surprise.** A supplier $S$ posts an
earnings surprise of $+8\%$. It has three customers, and we weight the propagated signal
by each customer's revenue share sourced from $S$ (how exposed each is):

| Customer | Revenue share from supplier $S$ ($w$) | Decay factor $\alpha$ | Propagated signal $\alpha \cdot w \cdot 8\%$ |
|---|---|---|---|
| Customer $A$ | 0.50 | 0.5 | $0.5 \times 0.50 \times 8\% = 2.0\%$ |
| Customer $B$ | 0.25 | 0.5 | $0.5 \times 0.25 \times 8\% = 1.0\%$ |
| Customer $C$ | 0.10 | 0.5 | $0.5 \times 0.10 \times 8\% = 0.4\%$ |

The one-hop message into customer $v$ is a weighted sum over its supplier neighbourhood,

$$ m_v = \alpha \sum_{u \in \mathcal{N}(v)} w_{u \to v} \, s_u, $$

where $s_u$ is the source firm's surprise, $w_{u \to v}$ the edge weight (dependence),
and $\alpha$ a global decay capturing how much of a supplier's news actually transmits.
Customer $A$, half-dependent on $S$, inherits a $2\%$ expected nudge; the barely-exposed
$C$ inherits $0.4\%$. The same surprise, fanned out and *scaled by the edge*, becomes a
cross-sectional return forecast.

**Tie to the deep-learning course.** A sequence model (RNN, TCN, attention) captures one
firm's *own* history — its price path, its seasonality, its momentum. The GNN adds the
**cross-firm channel** the sequence model is blind to: information that lives in *other*
firms and reaches this one along an edge. The two compose naturally — sequence model per
node for the temporal signal, graph layer across nodes for the relational signal.

<Callout variant="warning" title="Relational alpha decays, crowds, and reverses">
Three ways this bites. It **decays**: once everyone runs the same supplier-customer
propagation, the lead-lag gets arbitraged into the price and the edge stops paying.
It **reverses**: lead-lag is not a law — sometimes the customer's order book leads the
supplier, and a model that hard-codes one direction trades the wrong way. And the
**edges are noisy and stale**: supply-chain maps are scraped from filings, news, and
vendor datasets that lag reality by quarters, so you are often propagating along a
relationship that ended last year. Garbage edges, garbage alpha.
</Callout>

### When to use it

Use relational alpha when (1) you have a defensible economic edge — a real, reasonably
current supply or ownership link, not a guessed correlation — and (2) the signal plausibly
leads, i.e. the source firm's news arrives *before* the target's price has absorbed it.
It is strongest right after a discrete event (an earnings surprise, a guidance cut, an
M&A headline) on a well-mapped network. It is weakest, and most dangerous, when the edge
data is stale or the lead-lag is actually the other way round.

<MatchConcepts
  client:visible
  pairs={[
    { term: "Economic edge", definition: "A supplier-customer, co-movement, or ownership link along which a firm signal can travel" },
    { term: "Signal propagation", definition: "Sending a weighted fraction of one firm's surprise to its neighbours as a return forecast" },
    { term: "Edge weight (dependence)", definition: "How exposed the target firm is to the source — e.g. revenue share sourced from that supplier" },
    { term: "Cross-firm channel", definition: "The relational information a single-firm sequence model is blind to and a GNN adds" },
    { term: "Lead-lag reversal", definition: "The risk that the assumed direction of influence is backwards, so the trade is too" },
  ]}
/>

## On-chain analytics on wallet graphs

<MCQ
  client:visible
  pretest
  question="A blockchain records every transfer publicly and pseudonymously. What does that make it, for a graph analyst?"
  options={[
    { text: "A giant, fully public transaction graph anyone can crawl — pseudonymous, not anonymous", correct: true },
    { text: "A private ledger that only the exchange can read" },
    { text: "A perfectly anonymous system where no analysis is possible" },
    { text: "A flat list of balances with no relationships between addresses" },
  ]}
  explanation="Public blockchains expose every transfer between addresses, so the whole thing is one enormous transaction graph open to anyone. Addresses are pseudonyms (not names), but pseudonymity is not anonymity — the graph structure leaks ownership and behaviour."
/>

**Analogy.** Imagine if every bank transaction on Earth were posted to a public bulletin
board, forever, under nicknames instead of names. You could not immediately read "Alice
paid Bob," but you could watch the nicknames, see which ones always move together, and
slowly deduce who is who. That bulletin board is a public blockchain, and the deduction
is on-chain analytics.

**Definition.** **On-chain analytics** builds a **wallet / address graph** from raw
blockchain data — nodes are addresses, edges are transfers — and runs graph tasks on it:

- **Entity clustering** — which addresses share one owner? (Node clustering / link
  prediction.)
- **Illicit-flow tracing** — connect an address to a known hack, scam, sanctioned
  entity, or mixer. (Labelled-source propagation / path tracing.)
- **Exchange / mixer / service classification** — label an address by behaviour
  (node classification).

**Worked / illustrative example — clustering by common input.** In a UTXO-model chain
(think of coins as discrete bills, where a transaction *spends* specific bills), a single
transaction often spends inputs from several addresses at once. The **common-input
heuristic** says: if addresses $X$, $Y$, and $Z$ all appear as inputs to the *same*
transaction, they are almost certainly controlled by one wallet — you generally need all
the private keys to sign such a spend. So a transaction with inputs $\{X, Y, Z\}$
collapses those three nodes into one entity. A GNN then *refines* the heuristic: it
learns from address behaviour (timing, amounts, counterparties) to merge clusters the
naive rule split apart, and to *avoid* merging when the heuristic over-reaches.
Account-model chains (one persistent balance per address, no discrete coins) break the
common-input heuristic entirely, so the clustering tactics differ by chain model — a
loose tie to the UTXO-versus-account distinction from earlier.

<Callout variant="warning" title="Pseudonymity is not anonymity, but tracing breaks">
The chain is transparent, so naive tracing feels easy — follow the edges. Then the
adversary fights back. Mixers and privacy tools (CoinJoin, tumblers, shielded pools)
deliberately scramble the input-output mapping so the common-input heuristic and edge
tracing collapse. Chain-hopping — bridging funds across blockchains — snaps the trail at
the bridge. And ground-truth labels (this address is a known exchange, that one a
sanctioned wallet) are scarce, noisy, and often crowdsourced, so a confident illicit
label can be flat wrong. Transparent does not mean traceable.
</Callout>

### When to use it

Use on-chain graph analytics for compliance screening (is this counterparty wallet tied
to a sanctioned entity or a hack?), forensic tracing of stolen funds, and labelling
exchange or service addresses. It is at its best where the heuristics hold — common-input
clustering on UTXO chains, propagation from a few high-confidence labels. It is at its
worst, and you should be loudest about uncertainty, when funds have passed through a
mixer or hopped chains, or when your only labels are a noisy public blocklist.

<Reveal client:visible>
<details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
<summary className="cursor-pointer font-medium">If the blockchain is fully public, why is tracing illicit funds still hard?</summary>

**Answer.** Public means *visible*, not *attributable*. You can see every edge, but you
cannot natively see *who* owns each address, and adversaries actively destroy the link
between flows. Mixers pool many users' coins and re-issue them so the input-to-output
mapping is statistically scrambled; bridges move value to another chain where the trail
restarts; and the labels that tie pseudonyms to real entities are scarce and noisy.
Graph models help — clustering and propagation recover a lot of structure — but they
output *probabilistic attribution*, never certainty, and a determined launderer can push
that probability low enough to be useless as evidence.

</details>
</Reveal>

## The common honest caveat (bridge to next lesson)

<MCQ
  client:visible
  pretest
  question="Across BOTH fraud detection and relational alpha, which problem is the single biggest threat to a graph model's reported performance?"
  options={[
    { text: "Leakage from careless splits — if related nodes/edges straddle the train/test boundary, the model peeks at the answer and the score is a mirage", correct: true },
    { text: "Graphs are simply too small to ever train on" },
    { text: "There is no shared problem; fraud and alpha have nothing in common" },
    { text: "Message passing is mathematically impossible on financial graphs" },
  ]}
  explanation="Beyond scarce/noisy labels, imbalance, and an adapting adversary or decaying edge, the killer is leakage: graph splits are subtle, and if connected nodes or future edges bleed across the train/test line the reported score is fiction. The next lesson audits exactly this."
/>

Step back and the two halves of this lesson rhyme. Whether you are hunting laundering
rings or harvesting peer-firm alpha, the same four gremlins are waiting. **Labels are
scarce and noisy** — confirmed laundering cases and clean supply-chain maps are both
rare and error-prone. **Classes are imbalanced** — fraud is a needle in a haystack, and
naive accuracy lies about it. **The world fights back** — the launderer adapts to your
last model and the alpha edge decays as it crowds. And the big one, the gremlin that
quietly invalidates more graph papers than any other: **these models leak badly if you
split carelessly.** Connected nodes that should be on the same side of the train/test
line end up on both; future edges sneak into past training; a "test" node has already
whispered its label to a training neighbour through one hop of message passing. Your
backtest glows, and live performance faceplants.

<Callout variant="info" title="Next: the honest audit">
This was the optimistic capstone — here is where graphs put money on the table. The
next lesson is the cold shower: how graph splits leak, why standard cross-validation is
not enough on a network, and how to audit a graph model honestly before you trust a
single number it prints. Bring everything you just learned; we are about to stress-test
all of it.
</Callout>

## Recap

You took the whole graph-learning toolbox and aimed it at money. Fraud detection turned
out to be node and subgraph classification on a transaction graph, where structured
laundering hides under per-account thresholds and only the fan-in, chain, cycle, and
cluster *motifs* give it away — while legitimate hubs threaten to bury you in false
positives. Relational alpha turned out to be node regression with message passing as the
engine, propagating a supplier's surprise along weighted economic edges to forecast a
customer's return, adding the cross-firm channel a single-firm sequence model never sees.
On-chain analytics turned out to be the same transaction-graph game on a public,
pseudonymous ledger, where clustering and tracing recover structure until a mixer or a
chain hop fights back. And all of it shares one set of honest caveats — scarce labels,
imbalance, an adapting adversary or decaying edge, and the leakage trap that the next
lesson exists to expose.

<MindMap
  client:visible
  title="Fraud rings and relational alpha"
  caption="Build the map: the unifying idea, the three application areas, and the shared caveats."
  root={{
    label: 'Graph learning aimed at money',
    children: [
      { label: 'Unifying idea', children: [
        { label: 'Fraud = node / subgraph classification' },
        { label: 'Relational alpha = node regression + propagation' },
        { label: 'Hidden ties = link prediction' },
      ]},
      { label: 'AML ring detection', children: [
        { label: 'Transaction graph: accounts, directed weighted transfers' },
        { label: 'Per-account rules miss structured laundering' },
        { label: 'Fan-in beats the threshold (ten times nine thousand)' },
        { label: 'Suspicion, not proof; imbalance; adversary adapts' },
      ]},
      { label: 'Typologies and motifs', children: [
        { label: 'Smurfing = fan-in' },
        { label: 'Layering = chain' },
        { label: 'Round-tripping = cycle' },
        { label: 'Mule network = dense cluster' },
        { label: 'Pitfall: legit hubs (exchanges, payroll)' },
      ]},
      { label: 'Relational / peer-firm alpha', children: [
        { label: 'Propagate surprise along economic edges' },
        { label: 'Weighted by dependence (revenue share)' },
        { label: 'Adds cross-firm channel to sequence models' },
        { label: 'Decays, crowds, reverses; stale edges' },
      ]},
      { label: 'On-chain analytics', children: [
        { label: 'Public pseudonymous wallet graph' },
        { label: 'Entity clustering (common-input heuristic)' },
        { label: 'Illicit-flow tracing and classification' },
        { label: 'Mixers and chain-hopping break tracing' },
      ]},
      { label: 'Shared honest caveats', children: [
        { label: 'Scarce, noisy labels' },
        { label: 'Class imbalance' },
        { label: 'Adversary adapts / edge decays' },
        { label: 'Leakage from careless splits (next lesson)' },
      ]},
    ],
  }}
/>

<Quiz
  client:visible
  title="Mixed check: did the structure click?"
  questions={[
    {
      question: "Ten accounts each transfer nine thousand units into one collector under a ten thousand reporting threshold. Why does a per-transaction rule fail while a graph model succeeds?",
      options: [
        { text: "Each transfer is below threshold and innocuous; only the collector's aggregated inflow and in-degree across its neighbourhood reveal the ninety-thousand fan-in", correct: true },
        { text: "Each transfer is above threshold, so the rule should have fired" },
        { text: "Graph models cannot read transfer amounts at all" },
        { text: "The collector account is legally exempt from all monitoring" },
      ],
      explanation: "Smurfing structures every transfer below threshold by design. The crime appears only when you aggregate the collector's neighbourhood: in-degree ten, inflow ninety thousand, ten distinct senders.",
    },
    {
      question: "An AML model ranks a payroll processor and a popular exchange as its top two laundering suspects. What is this, and what is the fix?",
      options: [
        { text: "A false-positive trap: legitimate hubs have the same fan-in/fan-out shape as rings, so a degree-only model over-flags; fix it by learning finer behavioural and known-entity features", correct: true },
        { text: "Correct detections: any high-degree node is laundering by definition", correct: false },
        { text: "Proof the model has no edge directions and should be discarded", correct: false },
        { text: "Evidence that exchanges and payroll firms never appear in transaction graphs", correct: false },
      ],
      explanation: "This is the classic spot-the-trap: hub shape is not guilt. Exchanges fan in and payroll fans out for entirely legitimate reasons. A degree-only model buries investigators in false positives; you must add features that exonerate legitimate hubs.",
    },
    {
      question: "A semiconductor supplier with revenue-share dependence of 0.5 for customer A, 0.25 for B, and 0.1 for C posts a plus eight percent earnings surprise, propagated with decay factor 0.5. Which customer gets the largest expected nudge, and how big?",
      options: [
        { text: "Customer A, about plus two percent (0.5 times 0.5 times 8 percent)", correct: true },
        { text: "Customer C, about plus four percent, because least exposed firms react most", correct: false },
        { text: "All three get an identical nudge regardless of dependence", correct: false },
        { text: "The supplier itself, since surprises never cross firm boundaries", correct: false },
      ],
      explanation: "The propagated signal is decay times edge weight times surprise. For A: 0.5 times 0.5 times 8 percent equals 2.0 percent, the largest because A is the most dependent. The weighting by dependence is the whole point.",
    },
    {
      question: "Why does relational alpha that worked beautifully in backtest often fade once deployed?",
      options: [
        { text: "It decays as the lead-lag gets arbitraged once others run it, can reverse direction, and is propagated along supply-chain edges that are noisy and stale", correct: true },
        { text: "Earnings surprises stop happening after a strategy goes live", correct: false },
        { text: "Graph models forget how to do message passing in production", correct: false },
        { text: "Customers and suppliers merge into one firm, deleting the edge", correct: false },
      ],
      explanation: "Crowding arbitrages the edge away, the assumed lead-lag direction can be backwards, and the supply-chain map itself lags reality by quarters. All three erode live performance even when the backtest glowed.",
    },
    {
      question: "On a public blockchain you trace stolen funds along the transaction graph until they vanish at a mixer. What does this illustrate about on-chain analytics?",
      options: [
        { text: "Transparent does not mean traceable: pseudonymity is breakable by clustering, but mixers and chain-hopping deliberately scramble the input-output mapping and snap the trail", correct: true },
        { text: "The blockchain stopped recording transactions at the mixer", correct: false },
        { text: "Tracing is always trivial because every edge is public", correct: false },
        { text: "The funds were never on the chain in the first place", correct: false },
      ],
      explanation: "Public ledgers expose every edge, and clustering recovers a lot of ownership structure, but mixers pool and re-issue funds to scramble the mapping and bridges restart the trail on another chain. Visibility is not attribution.",
    },
  ]}
/>
