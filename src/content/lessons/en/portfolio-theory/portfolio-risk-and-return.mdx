---
title: "Portfolio Risk & Return: Combining Two Assets"
description: "Mix two assets and the expected return is a clean weighted average — but the risk isn't. See why volatility bows below the naive blend and find the min-variance mix."
topic: "en/portfolio-theory"
order: 3
minutes: 9
updated: 2026-06-04
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, Categorize, MatchConcepts, MindMap, TwoAssetFrontier } from '@/components/react';

You've met correlation and the portfolio-variance formula in the abstract. Now we put two real assets in a blender and watch what comes out. Pick a spicy growth stock and a calm bond fund, hold some of each, and two very different things happen to the two numbers you care about. The **return** of the mix behaves exactly as your gut expects — it lands neatly between the two. The **risk** of the mix does something almost magical: it can drop *below* either a simple average would predict, and sometimes below the calmer asset on its own. That gap between "what a naive average says" and "what actually happens" is the entire free lunch of diversification — and it has an exact equation. This lesson is where return-math and risk-math part ways.

<MCQ
  client:visible
  pretest
  question="You split your money between two assets. A portfolio's expected return is the weighted average of the two assets' returns; its volatility is also just the weighted average of the two volatilities. Which part is right?"
  options={[
    { text: "The return part is right; the volatility part is usually wrong — the mix is normally less volatile than the average", correct: true },
    { text: "Both are right — return and risk are both simple weighted averages" },
    { text: "Both are wrong — neither is a weighted average" },
    { text: "The volatility part is right; the return part is the wrong one" },
  ]}
  explanation="Expected return really is a clean weighted average — it's linear in the weights. Volatility is not: unless the two assets are perfectly correlated, the mix's volatility comes in below the weighted average of the two vols. That gap is diversification, and it's the whole point of this lesson."
/>

## Expected return is a weighted average

Start with the easy half. A **portfolio** is just a set of **weights** — what fraction of your money sits in each asset — and those weights must sum to 1 (you can't invest 130% of your cash unless you borrow, which we'll save for later). Call the weight in asset $A$ simply $w_A$, with $w_B = 1 - w_A$.

The portfolio's **expected return** is the weighted average of the pieces:

$$\mathbb{E}[R_p] = \sum_i w_i\,\mathbb{E}[R_i] = w_A\,\mathbb{E}[R_A] + w_B\,\mathbb{E}[R_B].$$

This is **linear** in the weights — double a weight's effect by doubling the weight, no surprises. Think of it like mixing paint *by the bucket*: 60% of a 12%-return bucket and 40% of a 6%-return bucket gives you a return that sits proportionally between them. No cancellation, no bonus, no penalty.

**Worked example.** Asset A (a stock) expects 12%; Asset B (a bond) expects 6%. Hold 60% A, 40% B:

$$\mathbb{E}[R_p] = 0.60 \times 12\% + 0.40 \times 6\% = 7.2\% + 2.4\% = 9.6\%.$$

Sweep the weight and the return marches in a straight line between 6% and 12%:

| Weight in A ($w_A$) | Weight in B | Expected return |
|---|---|---|
| 0% | 100% | 6.0% |
| 25% | 75% | 7.5% |
| 50% | 50% | 9.0% |
| 60% | 40% | 9.6% |
| 75% | 25% | 10.5% |
| 100% | 0% | 12.0% |

Each 1% you shift from bond to stock adds exactly the same $0.06$ percentage points of return. Perfectly linear.

<Callout variant="info" title="Why return is the easy one">
Expectation is a **linear operator**: $\mathbb{E}[aX + bY] = a\,\mathbb{E}[X] + b\,\mathbb{E}[Y]$, no matter how $X$ and $Y$ relate to each other. Correlation, covariance, all of it — irrelevant to the *average*. That's why return is a plain weighted average. Risk leans on variance, which is *not* linear, and that's where the whole story lives.
</Callout>

## Risk is NOT a weighted average

Here's the trap that sinks beginners. If 60/40 of two returns is a weighted average, surely 60/40 of two volatilities is too? It is not. Variance has a cross term that the naive average completely ignores. For two assets:

$$\sigma_p^2 = w_A^2\sigma_A^2 + w_B^2\sigma_B^2 + 2\,w_A w_B\,\rho\,\sigma_A\sigma_B,$$

where $\rho$ is the **correlation** between A and B. The first two terms are each asset's own variance, scaled by the *square* of its weight. The last term — the **cross term** — is where correlation enters, and it's the hero of the story. When $\rho < 1$, that cross term is smaller than it would be if the assets moved in lockstep, and the whole sum lands below the naive blend.

The **wrong** thing to compute is the weighted average of the volatilities:

$$\sigma_{\text{naive}} = w_A\sigma_A + w_B\sigma_B \quad (\textbf{wrong unless } \rho = 1).$$

That formula is only correct in the one case where diversification gives you nothing — perfect positive correlation. For anything less, the real number is smaller.

**Worked example.** Same assets, now with risk: $\sigma_A = 28\%$, $\sigma_B = 14\%$, correlation $\rho = 0.2$. Weights 60% A, 40% B. First the cross-term ingredient:

$$\text{covariance term inputs: } \rho\,\sigma_A\sigma_B = 0.2 \times 28 \times 14 = 78.4.$$

Now plug into the variance formula (working in percent-squared):

$$
\begin{aligned}
\sigma_p^2 &= (0.6)^2(28)^2 + (0.4)^2(14)^2 + 2(0.6)(0.4)(78.4)\\
&= 0.36 \times 784 + 0.16 \times 196 + 0.48 \times 78.4\\
&= 282.24 + 31.36 + 37.632 = 351.23.
\end{aligned}
$$

Take the square root: $\sigma_p = \sqrt{351.23} \approx 18.74\%$.

Now compare to the naive blend: $\sigma_{\text{naive}} = 0.6 \times 28 + 0.4 \times 14 = 16.8 + 5.6 = 22.4\%$.

| Quantity | Value |
|---|---|
| Naive weighted-average vol (wrong) | 22.4% |
| Actual portfolio vol $\sigma_p$ | 18.74% |
| **Risk shaved off by diversification** | **3.66 pts** |

You held exactly the same 60/40 mix and got **3.66 percentage points** less risk than the naive average claimed — for free, just because the two assets don't move in perfect step. The return was still a clean 9.6%. That's the free lunch in one calculation.

<FillBlank
  client:visible
  question="Plug the numbers in. Same two assets, but now a 50/50 split (use σ_A=28, σ_B=14, ρ=0.2)."
  text="The expected return is 0.5×12% + 0.5×6% = {{9.0%|7.2%|18.0%}}. The naive (wrong) volatility blend would be 0.5×28 + 0.5×14 = {{21%|18%|42%}}. But the cross term keeps the real variance lower, so the true portfolio volatility comes in {{below|exactly at|above}} that naive number. The single ingredient that makes the real risk smaller than the naive blend is the {{correlation being less than 1|expected return|weights summing to 1}}."
  explanation="Return: 0.5×12 + 0.5×6 = 9.0%. Naive vol blend: 0.5×28 + 0.5×14 = 21%. Because ρ = 0.2 < 1, the cross term shrinks, so real volatility lands below 21%. Sub-1 correlation is the whole mechanism — it's why combining assets reduces risk without touching the average return."
/>

## The bullet curve

So return moves in a straight line as you shift weight, but risk bows below the naive average. Put those two facts on the same chart — risk on the x-axis, return on the y-axis — and you don't get a line connecting the two assets. You get a **curve that bulges to the left**, called the **bullet** (or the **portfolio frontier** for two assets). Drag the weight below and watch the marker trace it.

<TwoAssetFrontier
  client:visible
  title="Mixing two assets traces a curved bullet, not a line"
  weightLabel="Weight in the stock (Asset A)"
  riskLabel="Risk (volatility, %)"
  returnLabel="Expected return (%)"
  assetALabel="Stock (A)"
  assetBLabel="Bond (B)"
  minVarLabel="Minimum-variance portfolio"
  retA={12}
  retB={6}
  volA={28}
  volB={14}
  correlation={0.2}
  caption="Return rises in a straight line as you load up on the stock, but volatility bows leftward because the two assets aren't perfectly correlated. The leftmost tip is the minimum-variance portfolio; only the bright upper arc is worth holding."
/>

Why a curve and not a line? Trace the two coordinates separately as $w_A$ sweeps $0 \to 1$:

- **The y-coordinate (return)** moves linearly — equal steps for equal weight changes, exactly the straight ladder from the first table.
- **The x-coordinate (risk)** does *not* — it dips below the straight line because of that $\rho < 1$ cross term, deepest somewhere in the middle where both weights are sizeable.

A point whose height climbs steadily but whose horizontal position pulls inward sweeps out a curve bowing toward the **low-risk (left) side**. The more sub-1 the correlation, the deeper the bow. If $\rho$ were exactly 1, the cross term wouldn't shrink, the risk would equal the naive blend at every weight, and the bullet would collapse into a **straight line** between the two assets — no diversification, no curve.

<Reveal client:visible buttonText="Why exactly does the curve bend left instead of just sloping straight?">
Because risk and return scale differently with the weight. Return is **linear** in $w_A$ — a clean weighted average. Volatility comes from a **square root of a quadratic** in $w_A$ (the variance formula), and that quadratic has a cross term that's discounted whenever $\rho < 1$. So as you move along the weights, you keep climbing in return at a constant rate while your risk grows *slower than linearly* through the middle — you're getting return without paying the full "average" price in risk. Plot that and the locus has to bow toward the left. The bend **is** diversification made visible: the horizontal gap between the curve and the straight line connecting the two assets is the risk you saved for free.
</Reveal>

## The minimum-variance portfolio

That leftmost tip of the bullet has a name: the **minimum-variance portfolio (MVP)** — the single mix with the lowest possible volatility you can build from this pair. It's the "calmest" portfolio available, and it often sits at a *more extreme* weight than you'd guess. There's a closed-form weight for it:

$$w_A^{\min} = \frac{\sigma_B^2 - \mathrm{Cov}_{AB}}{\sigma_A^2 + \sigma_B^2 - 2\,\mathrm{Cov}_{AB}}, \qquad \mathrm{Cov}_{AB} = \rho\,\sigma_A\sigma_B.$$

**Worked example.** Our numbers: $\sigma_A = 28$, $\sigma_B = 14$, $\rho = 0.2$. First the covariance:

$$\mathrm{Cov}_{AB} = 0.2 \times 28 \times 14 = 78.4.$$

Now the weight (variances are $28^2 = 784$ and $14^2 = 196$):

$$w_A^{\min} = \frac{196 - 78.4}{784 + 196 - 2(78.4)} = \frac{117.6}{823.2} \approx 0.143.$$

So the minimum-variance portfolio holds about **14.3% in the stock and 85.7% in the bond**. Notice it's *not* 100% bond — even though the bond is the calmer asset, sprinkling in a little stock actually *lowers* total risk, because the stock's wobble partly cancels the bond's. That's the counterintuitive punchline: the safest mix usually contains a dash of the risky asset.

Let's check its return and risk. Return: $0.143 \times 12\% + 0.857 \times 6\% \approx 6.86\%$. Its volatility (plug $w_A = 0.143$ into the variance formula) works out to about **13.6%** — below the bond's own 14%. The MVP is *less risky than either asset alone*.

| Portfolio | Weight in A | Expected return | Volatility |
|---|---|---|---|
| All bond (B) | 0% | 6.0% | 14.0% |
| **Minimum-variance** | **14.3%** | **6.86%** | **13.6%** |
| 60/40 | 60% | 9.6% | 18.74% |
| All stock (A) | 100% | 12.0% | 28.0% |

The MVP splits the bullet into two arcs. Everything **above** the MVP is the **efficient** part of the frontier: for each of those portfolios, no other mix gives more return at the same risk. Everything **below** it is **dominated** — for any lower-arc portfolio there's an upper-arc one with the *same risk but higher return*, so no rational investor would ever pick it. When people say "the efficient frontier," they mean that bright upper arc, starting at the MVP and running up to the high-return asset.

<Categorize
  client:visible
  question="Sort each statement by whether it describes how RETURN behaves or how RISK behaves when you combine two assets."
  buckets={["Expected return behaves like this", "Risk behaves like this"]}
  items={[
    { text: "A clean weighted average of the two assets' values", bucket: "Expected return behaves like this" },
    { text: "Perfectly linear — equal weight steps give equal steps", bucket: "Expected return behaves like this" },
    { text: "Completely unaffected by correlation", bucket: "Expected return behaves like this" },
    { text: "Has a cross term that depends on correlation", bucket: "Risk behaves like this" },
    { text: "Can fall below the weighted average of the two pieces", bucket: "Risk behaves like this" },
    { text: "Can be lower than either asset on its own", bucket: "Risk behaves like this" },
  ]}
  explanation="Return is the linear, well-behaved one — a weighted average that ignores correlation entirely. Risk is the interesting one — its cross term means the mix can be calmer than the naive average, and even calmer than the safer asset alone."
/>

## Scaling to many assets

Two assets gave us one cross term. The pattern generalizes, but the bookkeeping explodes. With $N$ assets the variance is a **double sum** over every pair:

$$\sigma_p^2 = \sum_i \sum_j w_i w_j\,\mathrm{Cov}(R_i, R_j).$$

When $i = j$ that's an asset's own variance; when $i \neq j$ it's a pairwise covariance, counted twice. This grid of all the covariances is the **covariance matrix** — the complete risk DNA of the portfolio.

Here's the part worth internalizing: the number of covariance terms grows *much* faster than the number of assets. With $N$ assets you have $N$ variances but $\tfrac{N(N-1)}{2}$ distinct pairwise covariances. For 10 assets that's 10 variances vs. 45 covariances; for 100 assets it's 100 vs. **4,950**. The covariances swamp the variances. So in a large portfolio, your total risk is governed far more by **how your holdings move together** than by how jumpy any single one is. Diversification doesn't kill an asset's own variance so much as it averages away the parts that *aren't* shared — which is exactly why a basket of correlated-but-not-identical assets can be so much calmer than its parts. We'll trace the full many-asset frontier in the next lesson; for now, just hold onto the idea that **covariances, not variances, run a big portfolio's risk**.

## Pitfalls

A few ways this goes wrong in practice:

- **Averaging the volatilities.** The single most common error: treating $\sigma_p$ as $w_A\sigma_A + w_B\sigma_B$. That overstates your risk (it's the $\rho = 1$ case) and makes diversification invisible. Always go through variance and the cross term.
- **Forgetting the cross term's sign.** The cross term carries the sign of $\rho$. Positive correlation *adds* risk; negative correlation *subtracts* it — a negatively correlated pair can drive portfolio risk dramatically low, even to zero in the idealized $\rho = -1$ case. Drop or mis-sign that term and your whole risk estimate is off.
- **Trusting your correlation estimate too much.** $\rho$ is *estimated* from noisy past data, and it drifts — correlations notoriously spike toward 1 in a crash, exactly when you wanted diversification most. The math is exact; the inputs are not.
- **Letting weights drift.** Your 60/40 doesn't stay 60/40. When the stock rallies it becomes, say, 70/30 on its own, quietly raising your risk above what you signed up for. Holding a target mix requires periodic **rebalancing** — selling the winner back down to weight.

<MatchConcepts
  client:visible
  question="Match each term to what it means."
  pairs={[
    { term: "Expected return of a portfolio", definition: "The weighted average of the assets' returns — linear, ignores correlation" },
    { term: "Cross term", definition: "The 2·w_A·w_B·ρ·σ_A·σ_B piece that injects correlation into portfolio variance" },
    { term: "The bullet", definition: "The curved risk–return locus of all mixes of two assets, bowing left when ρ < 1" },
    { term: "Minimum-variance portfolio", definition: "The single lowest-risk mix — the leftmost tip of the bullet" },
    { term: "Efficient frontier", definition: "The upper arc above the MVP, where no mix offers more return at the same risk" },
    { term: "Covariance matrix", definition: "The full grid of pairwise covariances that governs a many-asset portfolio's risk" },
  ]}
  explanation="Return is the linear weighted average; the cross term is where correlation enters risk; the bullet is the curve those two produce; the MVP is its tip; the efficient frontier is the worth-holding upper arc; and the covariance matrix is the same idea scaled to N assets."
/>

## Key Takeaways

<Callout variant="success" title="What to remember">
- **Expected return is a clean weighted average:** $\mathbb{E}[R_p] = w_A\mathbb{E}[R_A] + w_B\mathbb{E}[R_B]$ — linear, and correlation doesn't touch it.
- **Risk is not.** Portfolio variance is $\sigma_p^2 = w_A^2\sigma_A^2 + w_B^2\sigma_B^2 + 2w_Aw_B\rho\sigma_A\sigma_B$. The naive vol average $w_A\sigma_A + w_B\sigma_B$ is only right when $\rho = 1$ — otherwise it overstates the risk.
- **Worked:** 60/40 of (12%, 6%) returns 9.6%; with $\sigma=$ (28%, 14%) and $\rho=0.2$ the real vol is 18.74%, vs. a naive 22.4% — 3.66 points saved for free.
- **The bullet bows left** because return moves linearly but risk grows sub-linearly through the middle. The lower the correlation, the deeper the bow.
- **The minimum-variance portfolio** is the bullet's leftmost tip ($w_A^{\min} \approx 14.3\%$ here, vol ≈ 13.6% — calmer than either asset). The arc above it is the **efficient frontier**; the arc below is dominated.
- **At scale, covariances dominate** — $\tfrac{N(N-1)}{2}$ of them — so a big portfolio's risk is about how holdings move together, not their individual jumpiness.
</Callout>

<MindMap
  client:visible
  title="Combining two assets at a glance"
  caption="The one-screen map: return is the easy half, risk is where the magic lives."
  root={{
    label: "Two-asset portfolio",
    children: [
      {
        label: "Return (easy)",
        children: [
          { label: "Weighted average" },
          { label: "Linear in weights" },
          { label: "Ignores correlation" },
        ],
      },
      {
        label: "Risk (the magic)",
        children: [
          { label: "Variance formula + cross term" },
          { label: "Below naive vol average when ρ < 1" },
          { label: "Naive average wrong unless ρ = 1" },
        ],
      },
      {
        label: "The bullet",
        children: [
          { label: "Curve, not a line" },
          { label: "Bows left as ρ drops" },
          { label: "Min-variance portfolio at the tip" },
          { label: "Efficient upper arc vs dominated lower arc" },
        ],
      },
      {
        label: "Pitfalls",
        children: [
          { label: "Averaging the vols" },
          { label: "Mis-signing the cross term" },
          { label: "Noisy ρ estimates" },
          { label: "Weight drift → rebalance" },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Lesson 3 check"
  questions={[
    {
      question: "A 70/30 portfolio of assets returning 10% and 5%. What's its expected return?",
      options: [
        { text: "8.5%", correct: true },
        { text: "7.5%" },
        { text: "15.0%" },
        { text: "Can't tell without the correlation" },
      ],
      explanation: "Return is a plain weighted average and correlation is irrelevant to it: 0.7×10% + 0.3×5% = 7% + 1.5% = 8.5%.",
    },
    {
      question: "Which scenario makes the naive weighted-average-of-volatilities formula actually correct?",
      options: [
        { text: "When the two assets are perfectly positively correlated (ρ = 1)", correct: true },
        { text: "When the two assets are uncorrelated (ρ = 0)" },
        { text: "When the two assets are perfectly negatively correlated (ρ = −1)" },
        { text: "It's always correct for any correlation" },
      ],
      explanation: "Only at ρ = 1 does the cross term reach its maximum and the variance reduce to a perfect square, making σ_p equal the weighted average of the vols. For any ρ < 1 the real volatility is lower, so the naive blend overstates risk.",
    },
    {
      question: "Cause → effect: you lower the correlation between two assets while keeping everything else fixed. What happens to the bullet curve?",
      options: [
        { text: "It bows farther to the left — more risk reduction at the same returns", correct: true },
        { text: "It straightens into a line connecting the two assets" },
        { text: "It shifts straight up — every portfolio earns more return" },
        { text: "Nothing — correlation only affects expected return" },
      ],
      explanation: "Lower correlation shrinks the cross term, so portfolio volatility drops further below the naive blend at every interior weight. The curve bows deeper left. Correlation never touches return, so it can't shift the curve vertically.",
    },
    {
      question: "Spot the trap: which statement about the minimum-variance portfolio is FALSE?",
      options: [
        { text: "It always sits 100% in whichever single asset has the lower volatility", correct: true },
        { text: "It's the leftmost tip of the bullet curve" },
        { text: "It can be less volatile than either asset held alone" },
        { text: "Portfolios on the arc below it are dominated and not worth holding" },
      ],
      explanation: "False: the MVP usually holds a slice of BOTH assets, not 100% of the calmer one — adding a little of the risky asset can cut total risk because its moves partly offset. The other three are all true: it's the bullet's tip, it can beat both assets on risk, and the lower arc is dominated.",
    },
    {
      question: "In a 100-asset portfolio, why do covariances matter more than individual variances for total risk?",
      options: [
        { text: "There are far more covariance terms (4,950) than variance terms (100), so how assets move together dominates the sum", correct: true },
        { text: "Variances are always zero in large portfolios" },
        { text: "Covariances are easier to estimate than variances" },
        { text: "Expected return depends on covariances, so risk must too" },
      ],
      explanation: "Variance is a double sum: N own-variances but N(N−1)/2 pairwise covariances — 100 vs. 4,950 here. The covariance terms vastly outnumber the variances, so a big portfolio's risk is driven by co-movement, which is exactly what diversification can wash away.",
    },
  ]}
/>

Next up: **the efficient frontier** — the same logic with many assets at once, where the bullet becomes a whole region and only its upper edge is worth holding.
