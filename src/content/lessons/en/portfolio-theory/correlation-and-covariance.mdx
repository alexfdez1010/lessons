---
title: "Correlation & Covariance: The Math of Moving Together"
description: "How covariance and correlation measure whether two assets move together — and the two-asset variance formula that turns low correlation into real portfolio risk reduction."
topic: "en/portfolio-theory"
order: 2
minutes: 9
updated: 2026-06-04
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, Categorize, MatchConcepts, MindMap, CorrelationBlender } from '@/components/react';

You already know how to measure a single asset's risk: take the standard deviation of its returns and call it volatility. One number, one asset, done. But a portfolio isn't one asset — it's several, jostling against each other, and the surprise of Modern Portfolio Theory is that **how they move together** matters more than how wild any one of them is on its own.

Two assets can each be terrifyingly volatile and yet, glued into a portfolio, partly cancel each other out — like two people on a seesaw who, by leaning opposite ways, keep the plank steadier than either could alone. The tool that measures this "moving together" is **covariance**, and its tidy, unitless cousin is **correlation**. This lesson defines both from scratch, then shows the exact formula where low correlation pays off as lower risk.

<MCQ
  client:visible
  pretest
  question="Two stocks each have 20% annual volatility, and their returns are completely unrelated (correlation 0). You put half your money in each. What is the volatility of the 50/50 portfolio?"
  options={[
    { text: "About 14% — meaningfully less than 20%", correct: true },
    { text: "Exactly 20% — the volatilities just average out" },
    { text: "Exactly 40% — the two risks add together" },
    { text: "0% — unrelated risks always cancel completely" },
  ]}
  explanation="Volatilities do NOT simply average. With zero correlation the cross term vanishes, so σ_p² = 0.5²·20² + 0.5²·20² = 100 + 100 = 200, and √200 ≈ 14.1%. Combining two unrelated risks of 20% gives 14%, not 20% — that gap is diversification, and it's exactly what this lesson explains."
/>

## Covariance — how two assets move together

Volatility tells you how far an asset's returns swing around their own average. **Covariance** asks a different question about *two* assets at once: when asset A is above its average, is asset B usually above its average too — or below it?

Think of two dancers. If they're in sync — both stepping forward together, both back together — they have **positive covariance**. If they mirror each other — one forward exactly when the other steps back — that's **negative covariance**. If they're oblivious to each other, wandering the floor independently, their covariance is roughly **zero**.

The precise definition is the average product of each asset's deviation from its own mean:

$$\mathrm{Cov}(R_A, R_B) = \mathbb{E}\big[(R_A - \mu_A)(R_B - \mu_B)\big]$$

Read the sign off the product inside. When both assets are above their means in the same period, both deviations are positive, so the product is positive. When both are below, both deviations are negative — and a negative times a negative is *still positive*. So moving the same direction (up together **or** down together) pushes covariance positive. Moving opposite directions makes one deviation positive and the other negative, so the product is negative.

- **Positive covariance** → the assets tend to move the **same** direction.
- **Negative covariance** → they tend to move **opposite** directions.
- **Near-zero covariance** → no reliable relationship.

### A worked example

Here are three years of returns for two assets, A and B:

| Year | $R_A$ | $R_B$ |
|---|---|---|
| 1 | +10% | +4% |
| 2 | −2% | 0% |
| 3 | +4% | +2% |

First the means: $\mu_A = (10 - 2 + 4)/3 = 4\%$ and $\mu_B = (4 + 0 + 2)/3 = 2\%$.

Now the deviations and their products:

| Year | $R_A - \mu_A$ | $R_B - \mu_B$ | product |
|---|---|---|---|
| 1 | +6 | +2 | +12 |
| 2 | −6 | −2 | +12 |
| 3 | 0 | 0 | 0 |

Average the products: $\mathrm{Cov}(R_A, R_B) = (12 + 12 + 0)/3 = 8$. The covariance is **+8 (percent-squared)** — positive, so A and B tend to move together, which matches the table: when A jumped, so did B; when A fell, B fell too.

<Callout variant="warning" title="The units problem">
What are the units of that 8? **Percent times percent — percent-squared.** That's meaningless to a human: nobody intuits "8 squared-percent of co-movement." Worse, covariance scales with the assets' volatilities, so a big number might just mean both assets are wild, not that they're tightly linked. You can't compare covariances across pairs. That's exactly why we normalize it into correlation next.
</Callout>

### When it matters

Covariance is the raw ingredient — it's what actually appears inside the portfolio-variance formula later. But on its own it's a poor *summary*, because its size is contaminated by the assets' volatilities. Use covariance when you're plugging into a formula; reach for correlation when you want to *judge* how related two things are.

## Correlation — the normalized, unitless version

To fix covariance's units problem, divide it by the product of the two volatilities. That strips out the scale and leaves a pure, dimensionless number called the **correlation coefficient**, written $\rho$ (Greek "rho"):

$$\rho_{AB} = \frac{\mathrm{Cov}(R_A, R_B)}{\sigma_A\,\sigma_B}$$

The magic of that division is that $\rho$ is always trapped in a fixed range: $-1 \le \rho \le +1$. Now the number means the same thing for any pair of assets, so you can finally compare. Read it like this:

- $\rho = +1$ — **perfect lockstep.** The two move in exact proportion, every time. No diversification possible.
- $\rho = 0$ — **unrelated.** Knowing one tells you nothing about the other.
- $\rho = -1$ — **perfect mirror.** When one zigs, the other zags in exact proportion — the ideal hedge.
- Anything in between is a partial relationship, e.g. $\rho = 0.6$ is "fairly correlated but not glued."

### Turning the covariance into a correlation

Take the example above. We found $\mathrm{Cov} = 8$. Suppose the volatilities are $\sigma_A = 6\%$ and $\sigma_B = 2\%$ (computed from the same deviations). Then:

$$\rho_{AB} = \frac{8}{6 \times 2} = \frac{8}{12} \approx 0.67$$

A correlation of about **+0.67** — strongly positive, but not perfect. Notice how much friendlier 0.67 is than "8 percent-squared": it instantly says "these two move together most of the time, but not in perfect lockstep," with no units to wrestle and nothing to compare against.

<FillBlank
  client:visible
  question="Fill in the relationship between covariance and correlation."
  text="Covariance measures whether two assets move together, but its units are {{percent-squared|percent|dollars}}, which makes it {{hard to compare across pairs|easy to interpret|always negative}}. Dividing covariance by the {{product of the two volatilities|sum of the returns|number of years}} gives the correlation ρ, which is always between {{−1 and +1|0 and 100|0 and 1}}. A ρ of {{+1|0|−1}} means perfect lockstep, while a ρ of {{−1|+1|0.5}} means a perfect mirror."
  explanation="Covariance is in percent-squared and scales with volatility, so it can't be compared across pairs. Normalizing by σ_A·σ_B gives ρ ∈ [−1, +1], where +1 is perfect lockstep and −1 is a perfect mirror."
/>

## Why it matters for a portfolio

Here's where it all pays off. Mix weight $w_A$ of asset A and weight $w_B = 1 - w_A$ of asset B. The portfolio's variance is **not** the average of the two variances. It's:

$$\sigma_p^2 = w_A^2\sigma_A^2 + w_B^2\sigma_B^2 + 2\,w_A w_B\,\rho\,\sigma_A\sigma_B$$

Three terms, and each earns its place:

- $w_A^2\sigma_A^2$ — asset A's own risk contribution, scaled by how much of it you hold (squared).
- $w_B^2\sigma_B^2$ — the same for asset B.
- $2\,w_A w_B\,\rho\,\sigma_A\sigma_B$ — the **cross term**, and the star of the show. It's the only place $\rho$ appears. When $\rho$ is high, this term is big and positive, inflating portfolio variance. As $\rho$ falls, the cross term shrinks; at $\rho = 0$ it disappears entirely; and when $\rho$ is negative it goes negative and *subtracts* from total variance.

That cross term is where diversification lives. The lower the correlation, the smaller (or more negative) it is, and the more your portfolio's risk drops below the naive weighted average of the two volatilities. Drag the sliders below and watch the gap open up.

<CorrelationBlender
  client:visible
  title="How correlation blends two assets' risk"
  volA={30}
  volB={20}
  caption="Drag ρ from +1 down toward −1: the solid bar (true portfolio volatility) falls away from the faint tick (the naive weighted average you'd get at ρ=+1). The green gap is the diversification benefit — pure risk reduction, no return given up."
/>

## The three cases

To make the cross term concrete, hold a **50/50 mix** of asset A ($\sigma_A = 30\%$) and asset B ($\sigma_B = 20\%$), and walk $\rho$ through its three signature values. With $w_A = w_B = 0.5$, the formula becomes $\sigma_p^2 = 0.25(900) + 0.25(400) + 2(0.5)(0.5)\,\rho\,(30)(20) = 225 + 100 + 300\rho = 325 + 300\rho$.

| ρ | σ_p² = 325 + 300ρ | σ_p = √(σ_p²) | vs. weighted avg (25%) |
|---|---|---|---|
| **+1** | 325 + 300 = 625 | $\sqrt{625} = 25\%$ | no benefit |
| **0** | 325 + 0 = 325 | $\sqrt{325} \approx 18.0\%$ | 7.0% cut |
| **−1** | 325 − 300 = 25 | $\sqrt{25} = 5\%$ | 20.0% cut |

The arithmetic tells the whole story:

- At $\rho = +1$, $\sigma_p$ equals the **weighted average**, $0.5(30) + 0.5(20) = 25\%$. The assets move in perfect lockstep, so blending them buys you nothing — you just average the risk.
- At $\rho = 0$, the cross term vanishes and risk drops to **≈18.0%**, well below the 25% average. Two unrelated assets partly cancel.
- At $\rho = -1$, the cross term goes fully negative and risk collapses to **5%** — far below *either* asset's own volatility. A perfect mirror lets the swings hedge each other out. (With the right weights you could even drive it to zero.)

<Callout variant="info" title="The punchline of all of portfolio theory">
$\rho = +1$ gives **zero** diversification benefit. Every value below it cuts your risk, and the lower $\rho$ goes, the bigger the cut — all the way to a near-perfect hedge at $\rho = -1$. You reduce risk **without** reducing expected return. That free lunch *is* diversification, and it's entirely powered by the cross term in the variance formula.
</Callout>

## Pitfalls

Correlation is powerful and routinely abused. Four traps to keep in mind:

- **Correlation is not causation.** Two assets can move together because of a shared driver (interest rates, oil prices) or pure coincidence. A high $\rho$ tells you they *did* move together; it never tells you *why*, and certainly not that one causes the other.
- **ρ is unstable over time — and spikes in crises.** A pair that looked uncorrelated for years can snap toward $\rho = +1$ in a crash, when everyone sells everything at once. This is **tail dependence**: correlations rise exactly when you were counting on them to be low, so diversification evaporates at the worst moment.
- **ρ only captures *linear* co-movement.** Correlation measures straight-line relationships. Two assets can be tightly linked in a curved or threshold way and still show $\rho \approx 0$. A low correlation does not prove independence.
- **A single ρ hides regime changes.** One number averaged over a long window can mask the fact that the relationship was strongly positive in calm markets and strongly negative in stressed ones. The average flatters; the regimes bite.

<Reveal client:visible buttonText="If correlations jump toward +1 in a crash, is diversification useless?">
No — but it's weaker than the long-run number suggests, and it fails right when you most want it. The fix isn't to abandon diversification; it's to (1) stress-test portfolios using *crisis-period* correlations rather than calm averages, (2) hold genuinely different risk drivers (e.g. assets whose value rises in a panic, like certain government bonds), and (3) treat any historical $\rho$ as an estimate with a wide error bar, not a constant of nature. Diversification still works; just don't assume a backtested $\rho$ of 0.1 will hold when the market is on fire.
</Reveal>

<Categorize
  client:visible
  question="A new asset is added to a portfolio. Based on its correlation with the existing holdings, sort each ρ into how much diversification benefit it brings."
  buckets={["Big benefit", "Some benefit", "No benefit"]}
  items={[
    { text: "ρ = −0.9", bucket: "Big benefit" },
    { text: "ρ = −0.4", bucket: "Big benefit" },
    { text: "ρ = +0.2", bucket: "Some benefit" },
    { text: "ρ = +0.6", bucket: "Some benefit" },
    { text: "ρ = +1.0", bucket: "No benefit" },
  ]}
  explanation="Benefit grows as ρ falls. Negative correlations give the biggest cut (the cross term subtracts), positive-but-below-1 values still help (the cross term is smaller than the lockstep case), and ρ = +1 gives nothing — σ_p is just the weighted average."
/>

<MatchConcepts
  client:visible
  question="Match each term to its precise meaning."
  pairs={[
    { term: "Covariance", definition: "The average product of two assets' deviations from their means — in percent-squared" },
    { term: "Correlation (ρ)", definition: "Covariance divided by the two volatilities, always between −1 and +1" },
    { term: "Variance", definition: "An asset's risk measured as the square of its volatility" },
    { term: "The cross term", definition: "2·w_A·w_B·ρ·σ_A·σ_B — the only part of portfolio variance that depends on ρ" },
  ]}
  explanation="Covariance is the raw co-movement (messy units); correlation normalizes it into [−1, +1]; variance is σ²; and the cross term is where ρ enters portfolio risk and where diversification happens."
/>

## Key Takeaways

<Callout variant="success" title="What to remember">
- **Covariance** measures whether two assets move together: positive = same direction, negative = opposite, zero = unrelated. Its units (percent-squared) make it impossible to interpret or compare directly.
- **Correlation** $\rho = \mathrm{Cov}/(\sigma_A\sigma_B)$ fixes that — a unitless number always between **−1 and +1**. $+1$ is perfect lockstep, $0$ unrelated, $-1$ a perfect mirror.
- Portfolio variance is $\sigma_p^2 = w_A^2\sigma_A^2 + w_B^2\sigma_B^2 + 2w_Aw_B\,\rho\,\sigma_A\sigma_B$. The **cross term** is the only place $\rho$ lives, and it's where diversification happens.
- The lower the correlation, the bigger the risk cut. A 50/50 mix of 30% and 20% assets gives 25% at $\rho=+1$, ≈18% at $\rho=0$, and just 5% at $\rho=-1$.
- Watch the traps: correlation ≠ causation, $\rho$ is unstable and **rises toward 1 in crises**, it only captures linear co-movement, and a single number hides regime changes.
</Callout>

<MindMap
  client:visible
  title="Correlation & covariance at a glance"
  caption="The one-screen map of this lesson — the cross term is the bridge to everything that follows."
  root={{
    label: "Moving together",
    children: [
      {
        label: "Covariance",
        children: [
          { label: "Avg product of deviations" },
          { label: "Sign = direction" },
          { label: "Units: percent-squared (messy)" },
        ],
      },
      {
        label: "Correlation ρ",
        children: [
          { label: "Cov ÷ (σ_A·σ_B)" },
          { label: "Always −1 to +1" },
          { label: "+1 lockstep, 0 unrelated, −1 mirror" },
        ],
      },
      {
        label: "Portfolio variance",
        children: [
          { label: "Own risks: w²σ² each" },
          { label: "Cross term: 2·w_A·w_B·ρ·σ_A·σ_B" },
          { label: "Lower ρ → lower σ_p" },
        ],
      },
      {
        label: "Pitfalls",
        children: [
          { label: "ρ ≠ causation" },
          { label: "ρ spikes in crises" },
          { label: "Only linear; hides regimes" },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Lesson 2 check"
  questions={[
    {
      question: "Two assets each have 10% volatility and a correlation of 0. For a 50/50 portfolio, what is σ_p?",
      options: [
        { text: "About 7.1% — √(0.25·100 + 0.25·100) = √50", correct: true },
        { text: "Exactly 10% — the volatilities average" },
        { text: "Exactly 20% — the risks add" },
        { text: "0% — zero correlation cancels all risk" },
      ],
      explanation: "With ρ = 0 the cross term is zero, so σ_p² = 0.5²·10² + 0.5²·10² = 25 + 25 = 50, and √50 ≈ 7.1%. Unrelated risks partly cancel, dropping 10% to about 7%.",
    },
    {
      question: "Why do we normalize covariance into correlation?",
      options: [
        { text: "Covariance is in percent-squared and scales with volatility, so it can't be interpreted or compared across pairs; ρ is unitless and bounded in [−1, +1]", correct: true },
        { text: "Correlation can be negative but covariance can never be" },
        { text: "Covariance can't be used inside the portfolio-variance formula" },
        { text: "Correlation removes the need to know the assets' volatilities entirely" },
      ],
      explanation: "Covariance's units are meaningless and its size is contaminated by the assets' volatilities. Dividing by σ_A·σ_B strips the scale, giving a comparable number locked in [−1, +1]. (Covariance is exactly what goes into the variance formula — it's still essential.)",
    },
    {
      question: "Spot the trap: which statement is FALSE?",
      options: [
        { text: "Because two assets had a correlation near 0 for the last decade, you can rely on them staying uncorrelated in the next crash", correct: true },
        { text: "A correlation of −1 means the two assets move in exact opposite proportion" },
        { text: "At ρ = +1, a portfolio's volatility equals the weighted average of the two volatilities" },
        { text: "The cross term is the only part of portfolio variance that depends on ρ" },
      ],
      explanation: "False: correlations are unstable and tend to spike toward +1 in crises (tail dependence), so a calm-period ρ near 0 is exactly what fails when you need it. The other three are all true.",
    },
    {
      question: "A 50/50 mix of a 30%-vol asset and a 20%-vol asset has σ_p = 5%. What must the correlation be?",
      options: [
        { text: "ρ = −1 — only a perfect mirror collapses risk that far below either asset", correct: true },
        { text: "ρ = 0 — that gives about 18%, not 5%" },
        { text: "ρ = +1 — that gives 25%, the weighted average" },
        { text: "ρ = +0.5 — somewhere in the middle" },
      ],
      explanation: "σ_p² = 325 + 300ρ. For σ_p = 5%, σ_p² = 25, so 325 + 300ρ = 25 → ρ = −1. A perfect mirror lets the swings hedge each other, dropping risk below even the calmer asset.",
    },
    {
      question: "Cause → effect: as the correlation between two held assets falls (with weights and volatilities fixed), what happens to the portfolio's risk?",
      options: [
        { text: "It falls — the cross term shrinks (then goes negative), so σ_p drops below the weighted average", correct: true },
        { text: "It rises — lower correlation means more uncertainty" },
        { text: "It stays the same — only the individual volatilities matter" },
        { text: "It rises until ρ = 0, then falls" },
      ],
      explanation: "ρ enters portfolio variance only through the cross term, 2·w_A·w_B·ρ·σ_A·σ_B. Lower ρ makes that term smaller (and eventually negative), so σ_p falls — the whole mechanism of diversification.",
    },
  ]}
/>

Next up: **combining assets into a portfolio and watching the risk–return curve bend** — how mixing two assets traces a curve in risk–return space that bows inward exactly because of the correlation math you just learned.
