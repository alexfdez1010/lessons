---
title: "Quantum Computing for Finance — Final Exam"
description: "The graded final exam for Quantum Computing for Finance: qubits, superposition and the gate-model vs quantum-annealing split; amplitude estimation's quadratic Monte-Carlo speedup for pricing and VaR/CVaR; QUBO, QAOA and quantum annealing for cardinality-constrained portfolios; quantum kernels and variational classifiers with a skeptic's eye; the NISQ noise, error-correction and data-loading walls; and the quantum-inspired classical methods that pay rent today."
topic: "en/quantum-computing-for-finance"
order: 7
minutes: 22
updated: 2026-06-23
---

import { Callout, FinalExam, MindMap } from '@/components/react';

This is the graded finale for **Quantum Computing for Finance**, and it runs across the whole sober map. You started with the **foundations**: a qubit holds a superposition $a\lvert 0\rangle + b\lvert 1\rangle$ with $\lvert a\rvert^2 + \lvert b\rvert^2 = 1$, measurement collapses it, and the "$2^n$ states at once" headline buys you nothing unless interference concentrates amplitude on the answer you want — and you learned the two hardware worlds, the universal **gate model** (QAE, QAOA) versus special-purpose **quantum annealing** (Ising/QUBO). Then the crown jewel: **amplitude estimation** turns classical Monte Carlo's $1/\sqrt{N}$ error into $1/N$, a genuine *quadratic* speedup for option pricing and VaR/CVaR — paid for by an oracle you must assume is cheap. You recast **portfolio optimization as QUBO** ($x^\top Q x$ over binary $x$), enforced cardinality with a penalty $\lambda(\sum_i x_i - k)^2$, and solved it with QAOA or annealing — strong mainly where discrete constraints make classical convex solvers choke. You audited **quantum machine learning** — quantum kernels and variational classifiers — with the deepest skepticism, since expressiveness was never finance's binding constraint. And you walked the **walls**: NISQ noise that kills circuit depth, error-correction overhead of ~1000+ physical qubits per logical one, and above all the **data-loading** problem that can erase the very speedup it serves. The payoff: **quantum-inspired classical** methods like tensor networks that run today, judged — like everything here — **end-to-end** against the best classical baseline. No hints are shown, each answer **locks the instant you submit**, and your score stays hidden until the very end.

## Course Recap

<MindMap
  client:visible
  title="Quantum Computing for Finance — the whole arc"
  caption="Six lessons, one sober map: from qubits to the quantum-inspired methods that pay rent today."
  root={{
    label: "Quantum for Finance",
    children: [
      {
        label: "1 · Foundations",
        children: [
          { label: "Qubit superposition; measurement collapses" },
          { label: "2^n states ≠ free parallelism; interference is the art" },
          { label: "Gate model (universal) vs annealing (QUBO-only)" },
        ],
      },
      {
        label: "2 · Amplitude estimation",
        children: [
          { label: "Classical MC error ~ 1/sqrt(N)" },
          { label: "QAE error ~ 1/N: quadratic speedup" },
          { label: "Option pricing + VaR/CVaR as expectations" },
        ],
      },
      {
        label: "3 · QUBO & portfolios",
        children: [
          { label: "Minimize x^T Q x over binary x" },
          { label: "Cardinality penalty lambda*(sum x - k)^2" },
          { label: "QAOA (gate) vs annealing (D-Wave); discrete constraints" },
        ],
      },
      {
        label: "4 · Quantum ML",
        children: [
          { label: "Quantum kernels; variational classifiers" },
          { label: "Barren plateaus; overfitting tiny samples" },
          { label: "Thin finance evidence; loading wall" },
        ],
      },
      {
        label: "5 · The reality check",
        children: [
          { label: "NISQ noise caps circuit depth" },
          { label: "Error correction: ~1000+ physical per logical" },
          { label: "Data loading O(N) can cancel the speedup" },
        ],
      },
      {
        label: "6 · Quantum-inspired",
        children: [
          { label: "Tensor networks / MPS run classically today" },
          { label: "Digital annealers rival quantum annealers" },
          { label: "Judge end-to-end vs best classical baseline" },
        ],
      },
    ],
  }}
/>

<Callout variant="warning" title="One run, one shot">
This is a **graded, irreversible** exam. There are 25 questions, shown one at a time. The instant you submit a question it **locks** — there is no Back button, no retry, and no Restart. A wrong answer simply fails that question and the exam moves on; you cannot revisit it. Your running score is **hidden** until the final screen. The pass mark is **70%**. Some questions accept more than one correct option — read **every** option before you commit, because once you submit you own the answer.
</Callout>

<FinalExam
  client:visible
  passPercent={70}
  questions={[
    {
      question: "A qubit is in the state with amplitudes a = b = 1/sqrt(2) on the basis states 0 and 1. When you measure it, what happens?",
      options: [
        { text: "It collapses to 0 or 1, each with probability 1/2 (the squared magnitude of the amplitude).", correct: true },
        { text: "It returns the value 1/sqrt(2) directly as a continuous reading." },
        { text: "It stays in superposition and reports both 0 and 1 at once." },
        { text: "It always returns 0 because that amplitude is listed first." },
      ],
      explanation: "Measurement yields a basis outcome with probability equal to the squared amplitude. With a = b = 1/sqrt(2), each squared magnitude is 1/2, so it is a fair coin — and the superposition is destroyed in the act of reading.",
    },
    {
      question: "An enthusiast argues that because n qubits encode 2^n amplitudes, a quantum computer simply evaluates 2^n possibilities 'in parallel' and reads off the best one for free. Where does this break down?",
      options: [
        { text: "A single measurement returns just one basis state; the algorithm's job is to arrange interference so the wanted answer has high amplitude before you measure.", correct: true },
        { text: "It is correct — quantum computers do return all 2^n answers at once." },
        { text: "It breaks down only because today's qubit counts are small; the parallelism is otherwise free." },
        { text: "The 2^n amplitudes can each be read out individually with one run." },
      ],
      explanation: "Superposition holds 2^n amplitudes, but measurement collapses to one outcome. Useful quantum algorithms engineer constructive/destructive interference so the answer you want dominates the probability — there is no free lunch of reading all branches.",
    },
    {
      question: "Match the hardware paradigm to its nature: which statement about the gate model versus quantum annealing is correct?",
      options: [
        { text: "The gate model is universal and runs general algorithms like QAE and QAOA; quantum annealing is special-purpose and finds low-energy states of an Ising/QUBO problem.", correct: true },
        { text: "Quantum annealing is the universal paradigm; the gate model only solves optimization." },
        { text: "Both are universal and fully interchangeable for any quantum algorithm." },
        { text: "The gate model cannot run optimization algorithms at all." },
      ],
      explanation: "Gate-model machines (superconducting, trapped ions) apply arbitrary circuits and can in principle run any quantum algorithm. Annealers (D-Wave) are special-purpose: they relax toward the ground state of an Ising/QUBO Hamiltonian, which is one specific kind of optimization.",
    },
    {
      question: "Classical Monte Carlo estimates an expectation with standard error proportional to 1/sqrt(N). To cut the error in half, what must you do to the sample count?",
      options: [
        { text: "Roughly quadruple it — halving 1/sqrt(N) requires multiplying N by about 4.", correct: true },
        { text: "Roughly double it." },
        { text: "Roughly halve it." },
        { text: "Leave it unchanged; error does not depend on N." },
      ],
      explanation: "Since error scales as 1/sqrt(N), to halve it you need sqrt(N) to double, i.e. N to quadruple. That quadratic appetite for samples is precisely the wall amplitude estimation attacks.",
    },
    {
      question: "Quantum Amplitude Estimation reaches a target error epsilon using a number of oracle queries that scales how, compared with classical Monte Carlo?",
      options: [
        { text: "About 1/epsilon queries for QAE versus about 1/epsilon^2 samples classically — a quadratic speedup.", correct: true },
        { text: "About 1/epsilon^2 for QAE versus 1/epsilon classically — quantum is slower." },
        { text: "An exponential speedup: about log(1/epsilon) for QAE." },
        { text: "Both scale identically as 1/epsilon, so there is no advantage." },
      ],
      explanation: "QAE estimates the amplitude to precision epsilon with O(1/epsilon) queries, against O(1/epsilon^2) for classical sampling. That is a quadratic (square-root) reduction in the number of queries — genuine, but not exponential.",
    },
    {
      question: "You want an estimation error of 1e-3. Roughly how many classical samples versus quantum queries does the scaling predict, and what is the speedup factor?",
      options: [
        { text: "About 1e6 classical samples versus about 1e3 quantum queries — a speedup of about 1000.", correct: true },
        { text: "About 1e3 classical versus 1e6 quantum — quantum needs more." },
        { text: "About 1e6 each — no difference." },
        { text: "About 1e3 each — both already at the target." },
      ],
      explanation: "Classical ~ 1/epsilon^2 = 1/(1e-3)^2 = 1e6 samples; quantum ~ 1/epsilon = 1/1e-3 = 1e3 queries. The ratio 1e6/1e3 = 1000 ≈ 1/epsilon is the speedup — the quantum query count is the square root of the classical sample count.",
    },
    {
      question: "How does pricing a European option map onto amplitude estimation?",
      options: [
        { text: "The price is an expected discounted payoff — an expectation — which QAE can target by encoding it into the amplitude of an ancilla qubit.", correct: true },
        { text: "QAE prices the option by directly inverting the Black-Scholes formula on hardware." },
        { text: "It does not map; QAE only estimates probabilities, never expectations." },
        { text: "QAE prices it by sorting all possible payoffs and picking the median." },
      ],
      explanation: "A European option's value is the discounted expectation of its payoff under the risk-neutral measure. You build an operator that loads the distribution and writes E[f(X)] into an ancilla amplitude; QAE reads that amplitude with the quadratic query advantage.",
    },
    {
      question: "Which risk measures are tail quantities that QAE can estimate with the same quadratic query advantage? (Select all that apply.)",
      options: [
        { text: "Value at Risk (VaR), a quantile of the loss distribution.", correct: true },
        { text: "Conditional VaR / Expected Shortfall (CVaR), the average loss beyond the VaR.", correct: true },
        { text: "The number of qubits in the machine." },
        { text: "The per-gate error rate of the hardware." },
      ],
      explanation: "VaR (a tail quantile) and CVaR/Expected Shortfall (the mean loss in the tail) are both expectations/quantiles of the loss distribution, exactly the kind of quantity QAE estimates with O(1/epsilon) queries. Qubit count and gate error are hardware properties, not quantities QAE computes.",
      allowMultiple: true,
    },
    {
      question: "The QAE speedup carries a 'giant asterisk.' What is the most important caveat to state alongside it?",
      options: [
        { text: "It is quadratic, not exponential, and it assumes the distribution-loading oracle is cheap; if loading costs O(N), the speedup can evaporate.", correct: true },
        { text: "It only works for options, never for risk measures." },
        { text: "It is actually exponential, making classical Monte Carlo obsolete overnight." },
        { text: "It requires no oracle at all, so loading is never a concern." },
      ],
      explanation: "The advantage is a square-root reduction in queries to an oracle that is assumed efficient. If preparing the state that loads your distribution costs O(N) per run, that cost can cancel the query savings — the wall later lessons hammer.",
    },
    {
      question: "Continuous mean-variance portfolio optimization is a polynomial-time quadratic program. What turns it into an NP-hard combinatorial problem that motivates QUBO/quantum approaches?",
      options: [
        { text: "Discrete constraints like cardinality (hold exactly k names), minimum lot sizes, or 'zero-or-at-least-x%' rules.", correct: true },
        { text: "Adding a risk-aversion parameter to the objective." },
        { text: "Using daily instead of monthly returns." },
        { text: "Allowing short positions in the continuous problem." },
      ],
      explanation: "The convex continuous problem is easy. Integrality — choose exactly k assets, respect lot sizes, semi-continuous holdings — makes the feasible set combinatorial and the problem NP-hard, which is the regime QUBO formulations and quantum heuristics target.",
    },
    {
      question: "A QUBO is the problem of minimizing x^T Q x over binary x. How is a cardinality constraint 'choose exactly k assets' encoded into it?",
      options: [
        { text: "Add a penalty term lambda*(sum of x_i minus k)^2, which is zero when exactly k are chosen and positive otherwise.", correct: true },
        { text: "Hard-code k of the variables to 1 before solving." },
        { text: "Remove the quadratic term so only k assets remain." },
        { text: "Constraints cannot be expressed in a QUBO at all." },
      ],
      explanation: "QUBO is unconstrained, so constraints become penalties. The squared term lambda*(sum x - k)^2 is minimized (zero) exactly when the selection size equals k; lambda must be large enough that violating it costs more than any objective gain.",
    },
    {
      question: "A 3-asset QUBO penalizes risk and rewards return. Selection A has energy -2.0 and selection B has energy -3.5; both satisfy the cardinality constraint. Which is the better portfolio and why?",
      options: [
        { text: "Selection B, because lower energy means a better objective in a QUBO minimization.", correct: true },
        { text: "Selection A, because higher energy means more return." },
        { text: "They are equally good since both meet the constraint." },
        { text: "Neither; only positive energies are valid solutions." },
      ],
      explanation: "QUBO is a minimization, so the lower-energy configuration is preferred. -3.5 is lower than -2.0, so selection B is the better portfolio. The sign of the energy is irrelevant; only its relative magnitude matters.",
    },
    {
      question: "Quantum annealing solves a QUBO by an adiabatic-style process. What is the core idea?",
      options: [
        { text: "Start in the easy ground state of a simple Hamiltonian and slowly morph toward the problem Hamiltonian, ideally ending in its ground state — the optimum.", correct: true },
        { text: "Apply a fixed sequence of universal gates exactly like the gate model." },
        { text: "Measure all 2^n configurations and return the lowest by brute force." },
        { text: "Train a neural network to approximate the energy landscape." },
      ],
      explanation: "Annealing begins in the known ground state of a trivial Hamiltonian and adiabatically interpolates to the problem Hamiltonian; if done slowly enough the system stays in the ground state, landing on the optimum. In practice it is a heuristic, not a guarantee.",
    },
    {
      question: "A portfolio QUBO over 200 assets is fully dense. Your annealer has 5,000 qubits but each connects to only about 15 neighbors. Why might the problem still not fit?",
      options: [
        { text: "Limited connectivity forces minor-embedding with long qubit chains, so a dense 200-variable problem can need far more than 5,000 physical qubits — and chains can break during the anneal.", correct: true },
        { text: "5,000 qubits is always more than enough for any 200-variable problem." },
        { text: "Dense problems are trivial for annealers; only sparse ones are hard." },
        { text: "The annealer can only handle problems with fewer than 15 variables total." },
      ],
      explanation: "Sparse hardware graphs require embedding each logical variable as a chain of physical qubits to achieve the needed connectivity. A fully dense problem blows up the physical-qubit count well beyond the headline number, and long chains are fragile — a real practical wall.",
    },
    {
      question: "QAOA is a gate-model approach to QUBO. Which description is accurate, including its honest status?",
      options: [
        { text: "It alternates problem and mixer layers (depth p) with classically optimized angles; at shallow p it has not shown clear advantage over good classical heuristics, and suffers barren plateaus.", correct: true },
        { text: "It is a proven exponential-speedup algorithm for all QUBOs." },
        { text: "It is identical to quantum annealing and runs on D-Wave hardware." },
        { text: "It requires no classical optimization; the angles are fixed analytically." },
      ],
      explanation: "QAOA is a variational, hybrid quantum-classical method: a parameterized circuit whose angles a classical optimizer tunes. As p grows it can approach the optimum, but shallow-p runs have not beaten strong classical heuristics, and training faces barren plateaus.",
    },
    {
      question: "A quantum kernel method encodes each data point as a quantum state and uses the squared overlap of two encoded states as a similarity. What are genuine concerns about this for trading signals? (Select all that apply.)",
      options: [
        { text: "For many feature maps the kernel is classically simulable, so there is no quantum advantage.", correct: true },
        { text: "An over-expressive kernel can concentrate, making all similarities nearly equal and the kernel useless.", correct: true },
        { text: "Quantum kernels remove the need for any out-of-sample testing." },
        { text: "Quantum kernels guarantee higher Sharpe ratios on real data." },
      ],
      explanation: "Two real concerns: many quantum feature maps yield classically computable kernels (no advantage), and highly expressive maps suffer kernel concentration where similarities collapse toward a constant. They do not remove evaluation discipline nor guarantee any PnL — the opposite of the hype.",
      allowMultiple: true,
    },
    {
      question: "Variational quantum classifiers face 'barren plateaus.' What is the problem this names?",
      options: [
        { text: "Gradients vanish exponentially as the qubit count grows, making the circuit's parameters effectively untrainable at scale.", correct: true },
        { text: "The classifier runs out of memory on classical hardware." },
        { text: "The plateaus are flat regions of high accuracy where training can safely stop." },
        { text: "The model overfits because gradients are too large." },
      ],
      explanation: "In many parameterized circuits the gradient magnitude shrinks exponentially with qubit count, so the loss landscape is almost flat almost everywhere. The optimizer gets no signal, and training becomes intractable as you scale — a fundamental obstacle for VQCs.",
    },
    {
      question: "Why do quantum machine-learning demonstrations almost always use tiny, low-dimensional datasets?",
      options: [
        { text: "Because loading each data point into a quantum state costs circuit depth per sample, so large or high-dimensional data makes the data-loading bill dominate and erase any advantage.", correct: true },
        { text: "Because larger datasets always overfit quantum models specifically." },
        { text: "Because regulators forbid large datasets on quantum hardware." },
        { text: "Because quantum kernels only accept exactly eight features by design." },
      ],
      explanation: "State preparation (loading) is expensive and must be done for every train and test point. Small, low-dimensional demos keep that cost hidden; scaling up resurrects the loading wall, which is why few QML results survive realistic data sizes.",
    },
    {
      question: "NISQ hardware has per-gate error around 1e-3 and no error correction. For a circuit of about 5,000 gates, what is the rough probability the whole circuit runs correctly, and what does it imply?",
      options: [
        { text: "About 0.999^5000 ≈ 0.007 — essentially noise, so deep circuits are unusable without error correction.", correct: true },
        { text: "About 0.999 — depth has almost no effect." },
        { text: "About 0.5 — a coin flip that is still tradeable." },
        { text: "About 1.0 — gate errors cancel out over many gates." },
      ],
      explanation: "Success probability compounds as roughly (1 - p)^(gates) = 0.999^5000 ≈ 0.007. The deeper the circuit, the lower the chance every gate behaves, so NISQ machines are limited to shallow circuits until error correction arrives.",
    },
    {
      question: "Coherence (the time a qubit holds its state) is about 100 microseconds and a gate takes about 100 nanoseconds. Roughly how many gates can you run before decoherence, and why does it matter?",
      options: [
        { text: "About 1,000 gates (100 us / 100 ns); the circuit must finish before the state decays, capping algorithm depth.", correct: true },
        { text: "About 1,000,000 gates; decoherence is rarely a constraint." },
        { text: "About 10 gates; coherence is far too short for anything." },
        { text: "Unlimited gates, because gate time is irrelevant to coherence." },
      ],
      explanation: "Usable gate count is roughly coherence time divided by gate time: 100 us / 100 ns = 1,000. Beyond that the qubit has lost its quantum information to the environment, so coherence directly bounds how deep a circuit can be.",
    },
    {
      question: "Fault-tolerant quantum computing needs error correction. A surface code may require around 1,000 physical qubits per logical qubit. For an algorithm needing 100 logical qubits, roughly how many physical qubits are required, and what does that say about timelines?",
      options: [
        { text: "Around 100,000 physical qubits — far beyond today's hundreds, so the fault-tolerant era is years away.", correct: true },
        { text: "Around 100 physical qubits — we already have enough." },
        { text: "Around 1,000 physical qubits — a modest overhead." },
        { text: "Error correction reduces the requirement to fewer than 100 physical qubits." },
      ],
      explanation: "100 logical x ~1,000 physical = ~100,000 physical qubits, against current machines with hundreds. The error-correction overhead is the reason most useful quantum-advantage claims sit in the fault-tolerant future, not the NISQ present.",
    },
    {
      question: "Amplitude encoding can pack N numbers into log2(N) qubits — apparently exponential compression. Why does this not hand you a free win for QAE on market data?",
      options: [
        { text: "Preparing a general such state costs about O(N) gates, so if you must reload data each run, the loading cost can cancel QAE's quadratic query savings.", correct: true },
        { text: "Because log2(N) qubits cannot store real numbers at all." },
        { text: "Because amplitude encoding violates the laws of physics." },
        { text: "Because QAE never reads amplitudes, only counts qubits." },
      ],
      explanation: "The qubit count shrinks, but preparing an arbitrary amplitude-encoded state generally takes O(N) gates. QAE's advantage is in queries to an oracle assumed cheap; if building that oracle (loading the distribution) is O(N), the end-to-end speedup shrinks or vanishes — the data-loading wall.",
    },
    {
      question: "A vendor advertises a 'quadratic quantum speedup' for portfolio-risk Monte Carlo on real market data, benchmarking only oracle queries. What is the single most important number they likely omitted, and why is it decisive here?",
      options: [
        { text: "The state-preparation (data-loading) cost — roughly O(N) per run — which, against only a quadratic query saving, can cancel the advantage end to end.", correct: true },
        { text: "The cost of the classical random-number generator, which dominates everything." },
        { text: "The electricity bill of the cooling system, which is the real bottleneck." },
        { text: "Nothing important is missing; query count fully captures runtime." },
      ],
      explanation: "Because the advertised advantage is only quadratic, an O(N) loading bill of comparable order can erase it. Counting only queries assumes free loading; the honest, end-to-end accounting includes state preparation and often flips the verdict.",
    },
    {
      question: "Tensor networks (e.g. Matrix Product States) are quantum-INSPIRED but run classically. A state over n=40 assets naively needs 2^40 ≈ 1e12 numbers; an MPS with bond dimension D=50 stores about n*D^2 = 40*2500 = 100,000. When does this compression actually help?",
      options: [
        { text: "When the underlying correlation structure is low-entanglement / low bond dimension; rich, high-rank correlations require a huge D and kill the advantage.", correct: true },
        { text: "Always — tensor networks compress any object regardless of its structure." },
        { text: "Only when you have a quantum computer to run them on." },
        { text: "Never — the compression is purely theoretical and never realized classically." },
      ],
      explanation: "MPS cost grows with the square of the bond dimension D. When correlations are genuinely low-rank, a small D captures them and the compression (1e12 to 1e5 here) is enormous; when they are high-rank, the required D explodes and you are back to the dense cost — so tensor networks are a real but conditional classical tool.",
    },
    {
      question: "After the whole course, what is the honest bottom line for a practitioner deciding whether to bet on a 'quantum advantage in finance' claim today?",
      options: [
        { text: "Judge it end-to-end — loading, computation, readout, repetitions — against the best classical baseline; today the reliable wins are quantum-inspired classical methods, with fault-tolerant QAE a plausible someday.", correct: true },
        { text: "Adopt any quantum method immediately, since exponential speedups are already routine in trading." },
        { text: "Dismiss everything quantum outright; none of the ideas will ever matter." },
        { text: "Trust query-count benchmarks alone, since they fully predict real performance." },
      ],
      explanation: "The course's creed: a theoretical speedup is not a PnL edge until the full pipeline beats the best classical baseline at the same precision. Quantum-inspired classical methods (tensor networks, digital annealers) pay rent now; gate-model QAE for pricing and risk is the most credible long-run bet, pending fault tolerance.",
    },
  ]}
/>

<Callout variant="success" title="Where this leaves you on the quant ladder">
Pass or fail, you now own the rarest skill in this corner of finance: the ability to read a "quantum advantage" claim and price it honestly. You can place any algorithm on the map — gate model or annealer — name the speedup it invokes (and whether it is merely quadratic), check whether data-loading and readout were counted, and demand an end-to-end comparison against the best classical baseline before believing a word of it. You know where the real near-term value lives (quantum-inspired classical methods you can run today) and where the credible long-run bet sits (fault-tolerant amplitude estimation for pricing and risk). That is exactly the clear-eyed judgment — neither hype nor reflexive dismissal — that separates a frontier-aware quant from a press release. You finish the zero-to-expert ladder where every good quant should: trusting end-to-end evidence over enthusiasm.
</Callout>
