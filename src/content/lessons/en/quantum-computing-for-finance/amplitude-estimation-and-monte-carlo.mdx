---
title: "Amplitude Estimation and the Monte-Carlo Speedup"
description: "Quantum Amplitude Estimation turns Monte Carlo's 1/sqrt(N) error into 1/N — a quadratic speedup for option pricing and VaR/CVaR risk, the cleanest, most provable quantum win in finance."
topic: "en/quantum-computing-for-finance"
order: 2
minutes: 18
updated: 2026-06-23
---

import { Callout, MCQ, Quiz, Reveal, ThinkReveal, FillBlank, MatchConcepts, Categorize, MindMap, AmplitudeEstimationConvergence, MonteCarloConverge } from '@/components/react';

Every other quantum-finance claim you will hear is either a maybe, a someday, or a polite lie. This one is neither. **Quantum Amplitude Estimation (QAE) takes the single most stubborn law in all of computational finance — that Monte Carlo error shrinks like one over the square root of the sample count — and replaces it with one over the sample count.** That is a *quadratic* speedup, and it is provable, not hand-wavy. It is the crown jewel of this whole course: the one place where the math unambiguously says the quantum machine asks fewer questions to get the same answer.

So we will spend this lesson earning the right to be excited — and then, at the very end, earning the right to be skeptical. Because "fewer queries in theory" and "fewer seconds on a real chip with real noise and a real data-loading bill" are very different claims, and the gap between them is the rest of this course.

If your memory of *Monte Carlo for Finance* is hazy, dust it off now. The entire payoff of QAE is felt only against the backdrop of why classical Monte Carlo is so frustratingly slow to converge.

## The classical Monte-Carlo wall

<MCQ
  client:visible
  pretest
  question="Classical Monte Carlo estimates an expectation by averaging random samples. If you want to cut your estimation error in half, roughly how many more samples do you need?"
  options={[
    { text: "About 4 times as many — error shrinks like 1/sqrt(N), so halving error needs 4x the samples", correct: true },
    { text: "About 2 times as many — error is proportional to 1/N" },
    { text: "About 10 times as many, regardless of the error law" },
    { text: "No more samples; error does not depend on sample count" },
  ]}
  explanation="Standard error scales like sigma/sqrt(N). Halving it means sqrt(N) must double, so N must quadruple. That punishing square-root law is exactly what QAE attacks."
/>

**Analogy.** Classical Monte Carlo is a pollster. To estimate what fraction of voters favor a candidate, you call random people and average. Poll 100 people and your margin of error is some width; to *halve* that margin you do not call 200 people — you call 400. Each extra digit of precision costs you a hundredfold more phone calls. The pollster's pain is the quant's pain.

**Definition.** To estimate an expectation $\mu = \mathbb{E}[f(X)]$, classical Monte Carlo draws $N$ independent samples and averages them. By the Central Limit Theorem the **standard error** of that average is

$$ \text{SE} = \frac{\sigma}{\sqrt{N}}, $$

where $\sigma$ is the standard deviation of $f(X)$. The estimation error therefore shrinks like $1/\sqrt{N}$ — slope $-1/2$ on a log-log plot. Inverting it: to reach a target error $\varepsilon$ you need

$$ N \approx \frac{\sigma^2}{\varepsilon^2} \;\propto\; \frac{1}{\varepsilon^2}. $$

The cost grows with the *square* of the precision you demand. This is the wall.

**Worked example.** Set $\sigma = 1$ for cleanliness. To reach an error of $\varepsilon = 10^{-2}$ (one percent) you need about $N = 1/(10^{-2})^2 = 1/10^{-4} = 10^{4}$ samples — ten thousand. Now you want one more digit, $\varepsilon = 10^{-3}$:

$$ N \approx \frac{1}{(10^{-3})^2} = \frac{1}{10^{-6}} = 10^{6}. $$

A million. **One extra digit of accuracy cost you a 100-fold increase in samples** — from ten thousand to one million. That factor of 100 is not a coincidence; it is $(10^{-2}/10^{-3})^2 = 10^2$. Squaring the precision ratio is the whole story.

<MonteCarloConverge client:visible caption="Classical Monte Carlo: the running estimate wanders toward the truth, but the error band only narrows like 1/sqrt(N). The last digits of precision are agonizingly expensive — that slow square-root crawl is the wall QAE breaks." />

<Callout variant="warning" title="The square-root tax">
The 1/sqrt(N) law is not a quirk of a bad implementation — it is the Central Limit Theorem, and no amount of classical cleverness changes the *exponent*. Variance-reduction tricks (antithetic variates, control variates, importance sampling) shrink the constant sigma, which helps, but the error still falls like 1/sqrt(N). You can make the wall thinner; you cannot move it. Only changing how the samples are *generated and combined* — which is what a quantum computer does — changes the exponent itself.
</Callout>

### When to use it

Reach for classical Monte Carlo whenever an expectation has no closed form and the dimension is too high for a grid — pricing path-dependent options, aggregating portfolio risk, propagating uncertainty. It is the default workhorse precisely because it is general and embarrassingly parallel. Just budget honestly: every extra digit of precision is a four-fold (per half-digit) or hundred-fold (per full digit) sample bill, and that bill is the thing QAE promises to slash.

<FillBlank
  client:visible
  question="State the classical convergence law in your own words."
  text="Classical Monte Carlo error shrinks like 1 over the {{square root of N|square of N|logarithm of N}}, so the number of samples needed for a target error grows like 1 over {{epsilon squared|epsilon|the square root of epsilon}}."
  explanation="Standard error is sigma/sqrt(N), so error falls like 1/sqrt(N) and the sample budget grows like 1/epsilon^2. Quadratic in the precision you demand."
/>

## The core QAE result: 1/sqrt(N) becomes 1/N

<MCQ
  client:visible
  pretest
  question="Quantum Amplitude Estimation estimates an unknown amplitude (probability) 'a' to error epsilon. How does its query cost scale with epsilon, compared to classical sampling?"
  options={[
    { text: "QAE needs O(1/epsilon) oracle queries vs classical O(1/epsilon^2) samples — a quadratic speedup", correct: true },
    { text: "QAE needs O(1/epsilon^2), the same as classical — no asymptotic gain" },
    { text: "QAE needs O(log(1/epsilon)) — an exponential speedup over classical" },
    { text: "QAE needs O(1/epsilon^3), strictly worse than classical" },
  ]}
  explanation="QAE buys a quadratic speedup: O(1/epsilon) queries instead of O(1/epsilon^2) samples. Quadratic, not exponential — that distinction is the whole ballgame, and we hammer it at the end."
/>

**Analogy.** Imagine two referees timing how often a biased coin lands heads. The classical referee flips the coin many times and counts — and is stuck with the pollster's square-root tax. The quantum referee does not flip and count; she puts the coin into a kind of *resonant rotation* whose angle encodes the bias directly, then reads the angle. Reading an angle precisely costs effort proportional to the precision you want, not its square. Same question, fundamentally cheaper measurement.

**Definition.** Suppose an operator $\mathcal{A}$ prepares a state in which the "good" outcome you care about has amplitude $\sqrt{a}$, so the probability of measuring it is exactly $a$. Classically you would estimate $a$ by sampling: prepare, measure, repeat $N$ times, count — error $\propto 1/\sqrt{N}$. **Amplitude estimation** instead uses amplitude amplification (the engine behind Grover's algorithm) to estimate $a$ to additive error $\varepsilon$ using a number of applications of $\mathcal{A}$ (oracle "queries") that scales as

$$ N_{\text{quantum}} = O\!\left(\frac{1}{\varepsilon}\right), $$

against the classical

$$ N_{\text{classical}} = O\!\left(\frac{1}{\varepsilon^2}\right). $$

The ratio is $N_{\text{classical}} / N_{\text{quantum}} \approx 1/\varepsilon$ — the quantum query count is the *square root* of the classical sample count. On a log-log error-vs-cost plot, classical has slope $-1/2$ and quantum has slope $-1$. The widening vertical gap between those two lines **is** the speedup.

**Worked example.** Demand a target error $\varepsilon = 10^{-3}$. Classical sampling needs

$$ N_{\text{classical}} \approx \frac{1}{(10^{-3})^2} = 10^{6} \;\text{samples}, $$

while QAE needs only

$$ N_{\text{quantum}} \approx \frac{1}{10^{-3}} = 10^{3} \;\text{queries}. $$

A million versus a thousand. The speedup ratio is $10^{6}/10^{3} = 10^{3} = 1/\varepsilon$. **Tighten the target to $\varepsilon = 10^{-4}$ and the gap explodes to $10^{8}$ vs $10^{4}$ — a ten-thousand-fold edge.** The more precision you demand, the more lopsided the contest becomes, because you are racing slope $-1$ against slope $-1/2$.

<AmplitudeEstimationConvergence
  client:visible
  caption="Drag the target-error slider: the classical sample count (slope -1/2 line) grows like 1/epsilon^2 while the quantum query count (slope -1 line) grows like only 1/epsilon. The speedup chip reads about 1/epsilon, so at epsilon = 1e-3 quantum needs ~1,000 queries where classical needs ~1,000,000 samples. Hit Sweep N to watch the gap between the two laws widen as precision tightens."
/>

<Callout variant="success" title="Why this one is real">
Most quantum-advantage claims rest on a problem structure nobody can guarantee exists in your data. QAE's quadratic speedup is a *theorem about the cost of estimating an amplitude* — it holds whenever you can build the state-preparation operator. That is why it is the cleanest win in finance: it is not "maybe the data has hidden structure," it is "this is provably how amplitude estimation scales." The asterisks are all about cost-per-query and loading, not about whether the speedup is real.
</Callout>

### When to use it

QAE is the tool whenever your quantity of interest is an **expectation or a probability** you would otherwise Monte-Carlo, and you need *many digits* of precision. The speedup ratio is $1/\varepsilon$, so the win is negligible at coarse precision ($\varepsilon = 0.1$ buys you only about 10x) and enormous at fine precision ($\varepsilon = 10^{-4}$ buys 10,000x). If you only ever need two-digit answers, the quadratic speedup barely matters; if you are chasing tight risk numbers or tail probabilities, it is exactly where the lever is longest.

<MatchConcepts
  client:visible
  pairs={[
    { term: "Standard error", definition: "Spread of the Monte Carlo average; equals sigma/sqrt(N), giving the 1/sqrt(N) error law" },
    { term: "Quadratic speedup", definition: "Quantum query cost grows like 1/epsilon instead of classical 1/epsilon^2" },
    { term: "Oracle query", definition: "One application of the state-preparation operator A, the unit QAE counts its cost in" },
    { term: "Amplitude a", definition: "The probability of the marked good outcome, equal to the squared amplitude QAE reads out" },
    { term: "Speedup ratio", definition: "Classical samples divided by quantum queries, approximately 1/epsilon at a target error epsilon" },
  ]}
/>

## How a payoff becomes an amplitude

<MCQ
  client:visible
  pretest
  question="For QAE to price an option, the expected payoff E[f(X)] must first be encoded as something the quantum computer can read out. As what?"
  options={[
    { text: "The probability (squared amplitude) of measuring a marked ancilla qubit in state 1", correct: true },
    { text: "The number of qubits in the circuit" },
    { text: "The total runtime of the algorithm in nanoseconds" },
    { text: "The classical random seed used to generate samples" },
  ]}
  explanation="The expectation is loaded into the amplitude of a marked ancilla qubit, so that the probability of measuring that qubit as 1 equals E[f(X)] (suitably normalized). QAE then estimates that probability."
/>

**Analogy.** Think of building a custom dartboard whose geometry is rigged so that the *probability a random dart lands in the bullseye* equals exactly the number you want — the option's expected payoff. Once the board is built, you do not throw thousands of darts and count (that is classical sampling). You hand the board to QAE, which estimates the bullseye probability directly and far more cheaply. The cleverness lives in building the board; the speedup lives in how you read it.

**Definition.** You construct, in two conceptual stages, an operator $\mathcal{A}$ acting on a register of qubits plus one ancilla:

1. **Load the distribution.** A sub-operator $\mathcal{P}$ prepares a superposition over the possible outcomes $x$ with amplitudes set so that measuring the register gives outcome $x$ with probability $p(x)$ — the distribution of your risk factor $X$. Formally it builds $\sum_x \sqrt{p(x)}\,\lvert x \rangle$.
2. **Encode the payoff.** A second sub-operator rotates the ancilla qubit by an angle that depends on the payoff $f(x)$, so that conditioned on outcome $x$ the ancilla is $\lvert 1 \rangle$ with probability proportional to $f(x)$.

Put together, the probability of measuring the ancilla in $\lvert 1 \rangle$ is

$$ a = \sum_x p(x)\, \tilde f(x) = \mathbb{E}\big[\tilde f(X)\big], $$

where $\tilde f$ is $f$ rescaled into $[0,1]$. So $a$ — the amplitude QAE estimates — *is* the (normalized) expectation. Estimate $a$ to error $\varepsilon$ and you have estimated the expectation to error $\varepsilon$ (times the rescaling factor), in $O(1/\varepsilon)$ queries.

**Worked example.** Suppose a payoff takes just two values on a toy two-outcome model: $f = 0$ with probability $0.7$ and $f = 1$ with probability $0.3$. After loading, the ancilla is $\lvert 1 \rangle$ with probability $a = 0.7 \times 0 + 0.3 \times 1 = 0.3$. The true expected payoff is $0.3$. Classically you would prepare-and-measure $N$ times and your estimate of $0.3$ would carry error $\sqrt{0.3 \times 0.7}/\sqrt{N} \approx 0.458/\sqrt{N}$ — the familiar $1/\sqrt{N}$. QAE instead reads $a = 0.3$ to error $\varepsilon$ in $O(1/\varepsilon)$ queries. Same $0.3$; quadratically cheaper precision.

**The canonical machinery, honestly.** The original QAE wraps this in **Quantum Phase Estimation (QPE)**: it treats the Grover-style amplitude-amplification operator as a rotation, and QPE reads its rotation angle — from which $a$ falls out. QPE is powerful but expensive: it needs many extra ancilla qubits and long, coherent circuits, which real noisy hardware hates. So a family of **QPE-free / iterative amplitude estimation** variants now exists (iterative QAE, maximum-likelihood QAE, and relatives) that recover the same $O(1/\varepsilon)$ scaling with far shallower circuits — at the cost of more, simpler measurement rounds. The headline speedup is the same; these variants are what make it remotely runnable on near-term (NISQ) machines.

<Callout variant="info" title="The board is the bottleneck">
Notice where the work moved. Classically, generating each sample is cheap and you just need a lot of them. In QAE, the per-query state-preparation operator A — especially loading the distribution P — can be *expensive*, and you build it into every single query. Keep one eye on that cost: it is the hinge the entire real-world speedup swings on, and we return to it in the asterisk section (and devote a later lesson to it entirely).
</Callout>

### When to use it

This encoding step is exactly where QAE meets your specific problem: any quantity expressible as $\mathbb{E}[f(X)]$ for a distribution you can load and a payoff you can compute in-circuit is a candidate. Choose QAE when (a) you can write your target as such an expectation, (b) you can build a reasonably cheap loader $\mathcal{P}$, and (c) you need enough precision that the $1/\varepsilon$ vs $1/\varepsilon^2$ gap pays for the quantum overhead. If you cannot load the distribution cheaply, stop — the speedup may not survive, as the next-to-last section warns.

<ThinkReveal
  client:visible
  prompt="Before reading on: if the expected payoff you want is a dollar figure like 4.20, but the ancilla probability 'a' must live between 0 and 1, what must you do to the payoff before encoding it — and how do you recover the dollar answer at the end?"
  text="Rescale (normalize) the payoff into [0,1] before loading it — e.g. divide by an upper bound on the payoff so f-tilde lies in [0,1]. QAE then estimates a = E[f-tilde(X)] in that scaled world. You recover the real expectation by multiplying the estimated a back by the same scaling factor (and any discount factor). The 1/epsilon query scaling is on the scaled amplitude; the rescaling just converts units at the end."
/>

## Pricing a European option with QAE

<MCQ
  client:visible
  pretest
  question="Pricing a European call option boils down to which mathematical object?"
  options={[
    { text: "An expected discounted payoff under a risk-neutral distribution — exactly an expectation QAE can target", correct: true },
    { text: "A sorting problem over historical prices" },
    { text: "A matrix inversion with no expectation involved" },
    { text: "A classification task best handled by a neural net" },
  ]}
  explanation="A European option price is the discounted expectation of its payoff under the risk-neutral measure. Any expectation you would Monte-Carlo is a QAE target."
/>

**Analogy.** A European option price is a weighted average of "what you get paid at expiry," weighted by how likely each future price is (in the risk-neutral world) and shrunk by a discount factor. That is the pollster's average again — and the pollster's square-root tax again — which means it is precisely the kind of average QAE was built to estimate more cheaply.

**Definition.** A European call with strike $K$ on an underlying with terminal price $S_T$ has price

$$ C = e^{-rT}\, \mathbb{E}^{\mathbb{Q}}\!\big[\max(S_T - K,\, 0)\big], $$

the discounted risk-neutral expectation of the payoff $\max(S_T - K, 0)$. To QAE this, you (1) load the risk-neutral distribution of $S_T$ into the register via $\mathcal{P}$, (2) encode the rescaled payoff $\max(S_T - K, 0)$ into the ancilla amplitude, and (3) run amplitude estimation to read $a = \mathbb{E}^{\mathbb{Q}}[\widetilde{\max(S_T - K,0)}]$ to error $\varepsilon$ in $O(1/\varepsilon)$ queries, then undo the rescaling and apply $e^{-rT}$.

**Worked example.** Say you want the option price to a relative precision of $\varepsilon = 10^{-3}$ (three digits, typical for a desk that cares about basis points on a book). Side by side:

| Target error $\varepsilon$ | Classical samples $\approx 1/\varepsilon^2$ | QAE queries $\approx 1/\varepsilon$ | Speedup ratio $\approx 1/\varepsilon$ |
|---|---|---|---|
| $10^{-1}$ | $10^{2}$ (100) | $10^{1}$ (10) | 10x |
| $10^{-2}$ | $10^{4}$ (10,000) | $10^{2}$ (100) | 100x |
| $10^{-3}$ | $10^{6}$ (1,000,000) | $10^{3}$ (1,000) | 1,000x |
| $10^{-4}$ | $10^{8}$ (100,000,000) | $10^{4}$ (10,000) | 10,000x |

At the desk's $10^{-3}$ target, classical pricing burns a million payoff evaluations while QAE asks a thousand queries — a thousand-fold reduction in the *number of times you must evaluate the payoff scenario*. Push to four digits and the ratio is ten-thousand-fold. The table is just the two scaling laws made concrete; notice the speedup column is literally the inverse of the target error.

<Callout variant="info" title="The win compounds across a book">
A single option is rarely the point — desks price and re-price thousands of instruments and run them through scenario grids. Because the per-instrument speedup is multiplicative across the whole book, a quadratic improvement on each pricing is, in principle, a quadratic improvement on the entire nightly valuation run. That is why option pricing is the textbook QAE application: it is an expectation, it needs precision, and it is run enormously often. (In principle. The asterisk section is coming.)
</Callout>

### When to use it

QAE pricing is most attractive for instruments where classical Monte Carlo is *already* the chosen method and precision is expensive: path-dependent and exotic payoffs (Asian, barrier, basket) where no closed form exists, and where you need many digits. For a vanilla European with a Black–Scholes closed form you would not Monte-Carlo at all, let alone reach for a quantum computer — there is nothing to speed up. The rule of thumb: QAE helps exactly where classical MC hurts most.

<Categorize
  client:visible
  question="Sort each pricing situation by whether QAE's quadratic speedup is a natural fit."
  buckets={["Good QAE target", "Poor QAE target"]}
  items={[
    { text: "Path-dependent Asian option with no closed form, needing 4-digit precision", bucket: "Good QAE target" },
    { text: "Large basket option priced by Monte Carlo to tight tolerance", bucket: "Good QAE target" },
    { text: "Tail risk number where extra digits genuinely matter", bucket: "Good QAE target" },
    { text: "Vanilla European with an exact Black-Scholes formula", bucket: "Poor QAE target" },
    { text: "A quantity you only ever need to one or two digits", bucket: "Poor QAE target" },
    { text: "A problem where loading the distribution costs more than the sampling it replaces", bucket: "Poor QAE target" },
  ]}
  explanation="QAE shines on expectations that classical MC must compute to high precision and that have no closed form. It is wasted on closed-form instruments, coarse-precision needs, or cases where loading the distribution eats the speedup."
/>

## Risk: VaR and CVaR as QAE targets

<MCQ
  client:visible
  pretest
  question="Value at Risk (VaR) and Conditional VaR / Expected Shortfall (CVaR) are tail quantities. Why are they natural QAE targets?"
  options={[
    { text: "They are tail probabilities and conditional expectations — exactly the kind of expectation QAE estimates with a quadratic speedup", correct: true },
    { text: "They require sorting and quantum computers sort exponentially faster" },
    { text: "They are deterministic, so no estimation is needed at all" },
    { text: "They are unrelated to expectations and cannot be QAE targets" },
  ]}
  explanation="VaR is defined through a tail probability; CVaR is the expected loss beyond VaR — a conditional expectation. Both are expectations/probabilities, the bread and butter of amplitude estimation."
/>

**Analogy.** Measuring tail risk is like estimating how often a river overtops a flood wall, and how bad it gets when it does. "How often" is a tail *probability* (VaR's quantile); "how bad when it does" is a conditional *expectation* (CVaR). Both are averages over rare events — the worst kind for the pollster, because rare events demand huge samples to pin down. QAE's quadratic edge is therefore widest exactly here, where classical sampling is most starved.

**Definition.** For a loss $L$ at confidence level $\alpha$ (say $99\%$):

$$ \text{VaR}_\alpha = \inf\{\,\ell : \mathbb{P}(L \le \ell) \ge \alpha\,\}, \qquad \text{CVaR}_\alpha = \mathbb{E}\big[\,L \mid L \ge \text{VaR}_\alpha\,\big]. $$

VaR is the loss threshold the probability mass $\alpha$ sits below; CVaR (Expected Shortfall) is the average loss *given* you are past that threshold. To find VaR with QAE you estimate the tail probability $\mathbb{P}(L \le \ell)$ — an amplitude — for candidate thresholds $\ell$ (a bisection search over thresholds, each evaluated by QAE). To get CVaR you estimate the conditional expectation of the loss in the tail — again an amplitude. Both inherit the $O(1/\varepsilon)$ query scaling against classical $O(1/\varepsilon^2)$.

**Worked example.** You want the $99\%$ tail probability of a portfolio loss to error $\varepsilon = 10^{-3}$. Classical Monte Carlo needs about $N_{\text{classical}} \approx 1/(10^{-3})^2 = 10^{6}$ scenario simulations to nail that probability — and rare-tail estimation is even hungrier in practice because few of those million scenarios land in the $1\%$ tail. QAE estimates the same tail amplitude in $N_{\text{quantum}} \approx 1/10^{-3} = 10^{3}$ queries — a thousand versus a million, the same $1/\varepsilon = 1000\times$ ratio. For CVaR, you run the same machinery on the conditional expectation; the quadratic speedup carries over unchanged because it, too, is just an amplitude.

| Risk quantity | What you estimate | Classical cost at $\varepsilon=10^{-3}$ | QAE cost at $\varepsilon=10^{-3}$ |
|---|---|---|---|
| VaR$_{99\%}$ | Tail probability $\mathbb{P}(L \le \ell)$ | $\approx 10^{6}$ sims | $\approx 10^{3}$ queries |
| CVaR$_{99\%}$ | Conditional expectation $\mathbb{E}[L \mid L \ge \text{VaR}]$ | $\approx 10^{6}$ sims | $\approx 10^{3}$ queries |

<Callout variant="tip" title="Tails are where classical MC bleeds">
The deeper into the tail you go (99.9%, 99.99%), the more brutal the classical sample bill — you are trying to estimate a probability that is itself tiny, so most simulations contribute nothing. This is exactly the regime where you crave more precision and where the quadratic speedup is most valuable. If QAE ever earns its keep in production finance, regulatory tail-risk computation (with its appetite for many digits on rare events) is a leading candidate.
</Callout>

### When to use it

Target QAE at risk numbers when you need *tight* tail estimates and classical MC is straining — deep-tail VaR/CVaR, large netting sets, regulatory capital calculations rerun constantly. As with pricing, the lever is precision: a back-of-envelope $95\%$ VaR to two digits will not justify a quantum machine, but a $99.9\%$ Expected Shortfall to four digits, computed nightly across a huge book, is precisely the shape of problem where slope $-1$ crushes slope $-1/2$. Just remember the loader still has to exist and be cheap.

<FillBlank
  client:visible
  question="Connect the risk measures to the QAE machinery."
  text="VaR is found by estimating a tail {{probability|variance|duration}}, while CVaR is a conditional {{expectation|quantile|histogram}} of the loss beyond VaR — and because both are amplitudes, QAE estimates them in about 1 over {{epsilon|epsilon squared|N}} queries."
  explanation="VaR works through a tail probability; CVaR is the conditional expectation past the VaR threshold. Both are amplitudes, so QAE inherits the O(1/epsilon) query scaling."
/>

## The asterisk: quadratic, not magic

<MCQ
  client:visible
  pretest
  question="The QAE speedup over classical Monte Carlo is best described as:"
  options={[
    { text: "Quadratic — it turns 1/epsilon^2 into 1/epsilon, valuable but bounded, and it does not make intractable problems trivial", correct: true },
    { text: "Exponential — it turns intractable Monte Carlo problems into instant ones" },
    { text: "Unconditional — it always wins on wall-clock time regardless of hardware" },
    { text: "Irrelevant — there is no real asymptotic difference from classical sampling" },
  ]}
  explanation="QAE is a QUADRATIC speedup: 1/epsilon^2 to 1/epsilon. That is meaningful but not transformative the way an exponential speedup would be — and it is a query-count win, not yet a wall-clock or PnL win."
/>

**Analogy.** A quadratic speedup is like upgrading from walking to cycling — genuinely faster, you will arrive much sooner, and over long distances the gap is large. But it is not teleportation. An *exponential* speedup (the thing quantum hype constantly implies) would be teleportation: it would turn a problem that takes the age of the universe into one that takes a second. QAE does not do that. It cannot turn a classically intractable expectation into a trivial one; it can make a classically *expensive-but-doable* one cheaper — *if* every other cost cooperates.

**Definition and the catch.** The clean statement is about **query count**: QAE uses $O(1/\varepsilon)$ applications of $\mathcal{A}$ versus classical $O(1/\varepsilon^2)$ samples. But the *wall-clock* (or dollar) cost is

$$ \text{total cost} \approx (\text{number of queries}) \times (\text{cost per query}) \;+\; (\text{loading} + \text{readout} + \text{error-handling overhead}). $$

The quadratic win lives entirely in the first factor. It can be devoured by the rest:

- **Loading is the silent killer.** Building $\mathcal{A}$ requires loading the distribution $\mathcal{P}$ into the quantum state. If that loading costs $O(2^n)$ or $O(N)$ gates — as a naive general loader can — then *every query* pays a cost comparable to the classical sampling you were trying to avoid, and the $1/\varepsilon$ vs $1/\varepsilon^2$ advantage evaporates. The speedup assumes a *cheap* loader, which is not free for arbitrary distributions.
- **Noise.** On real NISQ hardware, the deep, coherent circuits canonical QAE wants accumulate errors fast; you may need so many repetitions or so much error mitigation that the practical scaling looks nothing like the textbook $O(1/\varepsilon)$.
- **Readout and constants.** The big-O hides constant factors and the comparatively glacial clock speed of today's quantum gates versus a classical FLOP.

**Worked example.** Recall at $\varepsilon = 10^{-3}$ the query-count win is $10^{6}$ vs $10^{3}$ — a $1000\times$ edge in *queries*. Now suppose each quantum query, including loading, is $10^{4}\times$ slower (in wall-clock) than drawing one classical sample — entirely plausible on near-term hardware. Then wall-clock cost is roughly $10^{3} \times 10^{4} = 10^{7}$ "time units" for QAE against $10^{6} \times 1 = 10^{6}$ for classical. **The quantum machine is now 10x *slower* in wall-clock despite asking 1000x fewer questions.** The query speedup was real; it just did not survive contact with the cost-per-query. That arithmetic — query advantage versus per-query and loading penalty — is the entire fight, and it is what the rest of this course adjudicates.

<Callout variant="warning" title="A theorem in query count is not a PnL line item">
Internalize the distinction or you will overclaim like every press release: QAE's quadratic speedup is a *proven* statement about how many oracle queries you need. It is *not*, by itself, a statement that a quantum computer prices your book faster, cheaper, or at all today. Loading + readout + noise sit between the theorem and the trading desk. Lessons 5 and 6 of this course are devoted to those walls — the data-loading / state-preparation problem and the noise / error-correction reality. Hold the excitement of this lesson in one hand and that skepticism in the other; both are correct.
</Callout>

### When to use it

Invoke the quadratic speedup as a *real but conditional* argument: it is the strongest quantum-finance case precisely because the algorithmic win is proven, but you must always pair it with the loading-and-noise caveat. Use it to justify research interest and long-horizon bets; do *not* use it to claim a present-day production advantage. The honest one-liner: "QAE provably needs quadratically fewer queries — and whether that becomes fewer seconds depends on the loader, the readout, and the hardware noise, which today usually say no."

<Reveal client:visible>
<details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
<summary className="cursor-pointer font-medium">Why doesn't a quadratic speedup turn an intractable Monte Carlo problem into a trivial one?</summary>

**Answer.** Because squaring the number of digits is not the same as collapsing an exponential. If a classical problem needs $10^{12}$ samples to reach the precision you want, QAE needs about $\sqrt{10^{12}} = 10^{6}$ queries — a million, still a lot, and possibly still infeasible if each query is slow or the loader is expensive. The speedup *reduces the exponent of the precision cost from 2 to 1*; it does not change the fact that very tight precision is intrinsically demanding. An exponential speedup (like Shor's for factoring) would turn $10^{12}$ into roughly $\log_2(10^{12}) \approx 40$ — a different universe. QAE lives in the milder, bounded world of quadratic gains, which is exactly why it is the *credible* quantum-finance story rather than the fantastical one.

</details>
</Reveal>

## Recap

You came in wanting one honest quantum win, and here it is: amplitude estimation reads an unknown probability $a$ to error $\varepsilon$ in $O(1/\varepsilon)$ queries, demolishing classical Monte Carlo's $O(1/\varepsilon^2)$ — a *quadratic* speedup, slope $-1$ against slope $-1/2$. Map any expectation onto an ancilla amplitude and QAE prices options (discounted expected payoffs) and measures risk (VaR tail probabilities, CVaR conditional expectations) with that same square-root reduction in queries. Then the asterisk: it is quadratic, not exponential; it presumes a cheap distribution loader; and a query-count theorem only becomes a wall-clock or PnL win once loading, readout, and noise are paid for — the subject of the lessons ahead.

<MindMap
  client:visible
  title="Amplitude estimation and the Monte-Carlo speedup"
  caption="Build the map: the classical wall, the quantum result, the two applications, and the all-important asterisk."
  root={{
    label: 'QAE Monte-Carlo speedup',
    children: [
      { label: 'Classical MC wall', children: [
        { label: 'Standard error = sigma / sqrt(N)' },
        { label: 'Error shrinks like 1/sqrt(N)' },
        { label: 'Samples needed ~ 1/epsilon^2' },
        { label: 'One more digit = 100x samples' },
      ]},
      { label: 'Core QAE result', children: [
        { label: 'Estimate amplitude a to error epsilon' },
        { label: 'Queries ~ 1/epsilon (slope -1)' },
        { label: 'Quadratic speedup, ratio ~ 1/epsilon' },
      ]},
      { label: 'Payoff to amplitude', children: [
        { label: 'Load distribution with operator P' },
        { label: 'Encode payoff into ancilla amplitude' },
        { label: 'QPE-based vs iterative / QPE-free QAE' },
      ]},
      { label: 'Applications', children: [
        { label: 'Option price = discounted expected payoff' },
        { label: 'VaR = tail probability' },
        { label: 'CVaR = conditional tail expectation' },
      ]},
      { label: 'The asterisk', children: [
        { label: 'Quadratic, NOT exponential' },
        { label: 'Assumes cheap loader P' },
        { label: 'Loading + readout + noise can eat it' },
        { label: 'Query win is not yet a PnL win' },
      ]},
    ],
  }}
/>

<Quiz
  client:visible
  title="Mixed check: is the crown jewel really a jewel?"
  questions={[
    {
      question: "A desk needs an option price to error 1e-4 instead of 1e-2. By what factor does the CLASSICAL Monte Carlo sample count grow?",
      options: [
        { text: "About 10,000x — samples scale like 1/epsilon^2, and (1e-2 / 1e-4)^2 = (100)^2 = 10,000", correct: true },
        { text: "About 100x — samples scale like 1/epsilon" },
        { text: "About 2x — error halves each time you double samples" },
        { text: "No change — sample count does not depend on target error" },
      ],
      explanation: "Classical cost is 1/epsilon^2. Going from 1e-2 to 1e-4 is a 100x tighter error, squared gives 10,000x more samples. That punishing growth is what QAE attacks.",
    },
    {
      question: "At a target error of 1e-3, roughly how many QAE oracle queries are needed, and what is the speedup ratio over classical sampling?",
      options: [
        { text: "About 1,000 queries (1/epsilon), versus ~1,000,000 classical samples — a ~1,000x speedup", correct: true },
        { text: "About 1,000,000 queries, the same as classical — no speedup" },
        { text: "About 30 queries (log of 1/epsilon) — an exponential speedup" },
        { text: "About 1,000,000,000 queries — slower than classical" },
      ],
      explanation: "QAE queries scale like 1/epsilon = 1,000; classical samples like 1/epsilon^2 = 1,000,000. The ratio is 1/epsilon = 1,000x.",
    },
    {
      question: "How is the expected discounted payoff of a European option made readable by QAE?",
      options: [
        { text: "It is encoded as the probability (squared amplitude) of measuring a marked ancilla qubit, after loading the risk-neutral distribution and the rescaled payoff", correct: true },
        { text: "It is stored as the number of qubits used in the circuit" },
        { text: "It is computed classically and only verified by the quantum machine" },
        { text: "It is read off the runtime of the quantum gates" },
      ],
      explanation: "Load the distribution with P, rotate the ancilla by the rescaled payoff, and the ancilla-equals-1 probability becomes E[f-tilde(X)]. QAE estimates that amplitude.",
    },
    {
      question: "Which statement about the QAE speedup is the honest one?",
      options: [
        { text: "It is a proven QUADRATIC reduction in query count, but loading, readout, and noise can erase any wall-clock win", correct: true },
        { text: "It is an exponential speedup that makes intractable pricing instant" },
        { text: "It always beats classical wall-clock time on today's hardware" },
        { text: "It only applies to option pricing, never to risk measures" },
      ],
      explanation: "Quadratic, not exponential; a query-count theorem, not a guaranteed wall-clock or PnL win. The loader-and-noise caveat is essential and is the focus of later lessons.",
    },
    {
      question: "Why are deep-tail VaR and CVaR especially attractive QAE targets compared to a coarse 95% VaR?",
      options: [
        { text: "Deep tails demand high precision on rare events, where classical MC bleeds most and the 1/epsilon vs 1/epsilon^2 gap is widest", correct: true },
        { text: "Deep tails have closed-form solutions, so quantum is unnecessary" },
        { text: "Coarse VaR cannot be expressed as an expectation at all" },
        { text: "Quantum computers can only estimate probabilities below 1%" },
      ],
      explanation: "The speedup ratio is 1/epsilon, so it grows with the precision you demand. Deep-tail risk needs many digits on rare events, exactly where classical sampling is most starved and QAE's edge is largest.",
    },
  ]}
/>
