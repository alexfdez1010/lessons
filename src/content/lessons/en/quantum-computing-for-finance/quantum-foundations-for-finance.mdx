---
title: "Quantum Foundations for Finance"
description: "Qubits, superposition, entanglement and quantum gates explained for the quant — plus the gate-model vs quantum-annealing split that decides which algorithms you can even run."
topic: "en/quantum-computing-for-finance"
order: 1
minutes: 16
updated: 2026-06-23
---

import { Callout, MCQ, Quiz, Reveal, ThinkReveal, FillBlank, MatchConcepts, Categorize, MindMap } from '@/components/react';

Every quantum-finance pitch leans on four words — *qubit*, *superposition*, *entanglement*,
*gate* — and a promise that they add up to exponential free lunch. They do not. But you
cannot audit a "quantum advantage in option pricing" claim if those four words are fog,
so this lesson turns them into working intuition. Not the physicist's version with bra-ket
gymnastics and Hilbert-space measure theory — the *quant's* version: just enough machinery
to know what a quantum computer can and cannot do, and which of the two very different
machines you are even talking about.

Here's the thesis up front: **a quantum computer is not a magic parallel processor that
tries all answers at once and hands you the best one.** It is a device that prepares a
delicate cloud of possibilities, then — through carefully arranged *interference* — makes
the answer you want loud and everything else quiet, so that a single measurement is likely
to return it. The whole game is amplitude bookkeeping. Get that one idea and the rest of
the course (the quadratic Monte-Carlo speedup, QAOA, the data-loading wall) snaps into place.

## The qubit vs the bit

<MCQ
  client:visible
  pretest
  question="A classical bit is 0 or 1. A qubit before measurement is best described as:"
  options={[
    { text: "A weighted blend of 0 and 1 at once, with two numbers (amplitudes) that set how likely each outcome is when you measure", correct: true },
    { text: "Secretly already a 0 or a 1; we just don't know which yet" },
    { text: "A number between 0 and 1, like 0.37, that we read off directly" },
    { text: "Both 0 and 1 simultaneously, which we can both observe at the same time" },
  ]}
  explanation="A qubit holds two complex amplitudes that define a superposition of 0 and 1. It is not a hidden classical value (that's the 'hidden variable' misread), nor an analog dial we read directly — measurement forces a single 0 or 1 outcome with probabilities set by the amplitudes."
/>

**Analogy.** A bit is a light switch: up or down, on or off, end of story. A qubit is more
like a spinning coin before it lands. While it spins it is genuinely neither heads nor
tails — it carries a *propensity* toward each — and the instant it hits the table it
commits to exactly one face. The spin is the superposition; the landing is the measurement.

**Definition.** A qubit's state is a **superposition** of the two basis states |0⟩ and |1⟩:

$$ |\psi\rangle = a\,|0\rangle + b\,|1\rangle, \qquad |a|^2 + |b|^2 = 1. $$

The numbers $a$ and $b$ are **amplitudes** (complex numbers, in general). They are *not*
probabilities — you square their magnitudes to get probabilities. When you **measure** the
qubit in the |0⟩/|1⟩ basis, you get outcome 0 with probability $|a|^2$ and outcome 1 with
probability $|b|^2$, and the state instantly **collapses** to whichever you saw. The
normalization $|a|^2 + |b|^2 = 1$ just says "the probabilities of the two outcomes sum to 1."

**Worked example.** Take the most famous single-qubit state, the equal superposition:

$$ |\psi\rangle = \tfrac{1}{\sqrt{2}}\,|0\rangle + \tfrac{1}{\sqrt{2}}\,|1\rangle, \qquad a = b = \tfrac{1}{\sqrt{2}}. $$

Check the normalization first: $|a|^2 + |b|^2 = \left(\tfrac{1}{\sqrt 2}\right)^2 + \left(\tfrac{1}{\sqrt 2}\right)^2 = \tfrac{1}{2} + \tfrac{1}{2} = 1.$ Good. Now the measurement
probabilities: $P(0) = |a|^2 = \tfrac{1}{2}$ and $P(1) = |b|^2 = \tfrac{1}{2}$ — a perfect
50/50 coin. Notice that the amplitude was $\tfrac{1}{\sqrt 2} \approx 0.707$, *not* $0.5$;
you square it to land at the 50% you expected. Get an unequal state, say $a = \tfrac{\sqrt 3}{2}$
and $b = \tfrac{1}{2}$, and you get $P(0) = \tfrac{3}{4} = 75\%$ and $P(1) = \tfrac{1}{4} = 25\%$ —
again the squares, and again they sum to 1.

| Property | Classical bit | Qubit |
|---|---|---|
| State before reading | Already 0 or 1 | Superposition $a$|0⟩ + $b$|1⟩ |
| Numbers describing it | One value in {0, 1} | Two amplitudes ($a$, $b$), constrained by $|a|^2+|b|^2=1$ |
| What reading it does | Reports the stored value, no change | Collapses to 0 or 1 with prob $|a|^2$, $|b|^2$ |
| Can you copy it freely? | Yes | No (the no-cloning theorem) |
| Information you extract per read | The full bit | A single 0/1 sample |

<Callout variant="warning" title="Amplitudes are not probabilities">
The single most common slip is reading $a$ as "the chance of 0." It is not. You must square
the magnitude: probability of 0 is $|a|^2$. That is why an equal superposition uses 1 over
root 2 (about 0.707) and not 0.5 — squaring 0.707 gives the 0.5 you actually wanted. Treating
amplitudes as probabilities will make every later calculation, including the amplitude-estimation
speedup, come out wrong.
</Callout>

### Why it matters for finance

Amplitudes can be *negative* (or complex), and probabilities cannot — that sign is the entire
source of quantum advantage. Two paths to the same outcome with amplitudes $+0.5$ and $-0.5$
**cancel** (destructive interference) to probability zero; classical probabilities, always
positive, can only pile up. Every speedup later in this course — the $1/N$ Monte-Carlo error
decay of amplitude estimation especially — is squeezed out of arranging amplitudes so the
wrong answers cancel and the right one survives. No negative amplitudes, no advantage.

<FillBlank
  client:visible
  question="Pin down the amplitude-to-probability rule."
  text="To get the probability of measuring outcome 0, you take the {{squared magnitude|raw value|reciprocal}} of the amplitude a, and the two outcome probabilities must sum to one."
  explanation="Probability is the squared magnitude of the amplitude. The raw value can be negative or complex (that's what enables interference), and reciprocals have nothing to do with it."
/>

## Superposition and the "2ⁿ states at once" claim

<MCQ
  client:visible
  pretest
  question="A 50-qubit register can be in a superposition of 2 to the 50th (about a quadrillion) basis states. The honest implication is:"
  options={[
    { text: "You hold all those amplitudes during the computation, but one measurement yields ONE 50-bit string — so the trick is making the wanted answer's amplitude large", correct: true },
    { text: "You get to read out all quadrillion results at once for free" },
    { text: "It runs a quadrillion classical computers in parallel and returns the best" },
    { text: "It is exponentially faster on every problem with no caveats" },
  ]}
  explanation="A register can hold an exponential number of amplitudes, but measurement returns a single basis state sampled by those amplitudes. There is no free read-out of all branches. Useful algorithms must concentrate amplitude on the answer via interference before you measure."
/>

**Analogy.** Picture a giant orchestra where every possible answer to your problem is a
musician playing a note. In superposition, all of them play at once — a deafening chord
containing every candidate. But you are allowed exactly *one* recording, and a microphone
captures whichever note is loudest. Quantum computing is not "hear every musician
separately." It is *conducting*: arranging the music so that destructive interference
silences the wrong notes and constructive interference swells the right one, so the single
note your microphone grabs is overwhelmingly likely to be the answer.

**Definition.** With $n$ qubits, the state lives in a space of $2^n$ basis states, and the
general superposition is

$$ |\psi\rangle = \sum_{x=0}^{2^n - 1} c_x\, |x\rangle, \qquad \sum_{x} |c_x|^2 = 1, $$

where each $|x\rangle$ is one of the $2^n$ possible $n$-bit strings and $c_x$ is its amplitude.
Yes, that is exponentially many amplitudes evolving together — the source of the "exponential
power" headline. But the **measurement rule is unchanged**: one measurement returns a single
string $x$ with probability $|c_x|^2$, and the rest of the state is gone. You never get to
read the whole vector of amplitudes. The "art" of a quantum algorithm is choreographing the
amplitudes so that, *before* you measure, $|c_x|^2$ is near 1 for the $x$ you want and near 0
for the $2^n - 1$ you don't.

**Worked example.** Take $n = 3$ qubits, all placed in equal superposition. There are
$2^3 = 8$ basis states (000 through 111), each with amplitude $c_x = \tfrac{1}{\sqrt{8}}$.
Each one's measurement probability is $|c_x|^2 = \tfrac{1}{8} = 12.5\%$. So a naive measurement
is just a uniform random 3-bit string — *worse* than useless, you could have flipped three
coins. Now suppose a clever circuit (Grover-style) bumps the amplitude of the target string
101 up to, say, $0.97$ while shrinking the other seven. Then $P(101) = 0.97^2 \approx 0.94$:
a single measurement returns your answer 94% of the time. Same $2^n$ states held throughout —
the *only* thing that changed is who got the amplitude. That redistribution is the whole job.

<Callout variant="warning" title="The 'free parallelism' fallacy">
"It tries 2 to the n possibilities at once, so it's exponentially fast" is the marketing
line that has oversold quantum computing for twenty years. The register does hold an
exponential number of amplitudes — but you extract a single sample per run, and only for a
narrow class of problems can interference be arranged to make that sample useful. For most
problems no such choreography exists, and the quantum computer is no faster than your laptop.
When a vendor says "explores all portfolios simultaneously," ask the only question that
matters: how does the wanted answer's amplitude get large?
</Callout>

### Why it matters for finance

This is the single most important corrective for a finance audience, because almost every
hype cycle rests on the free-parallelism fallacy. When you reach **amplitude estimation** in
lesson 2, you will see the speedup is a *quadratic* one — turning Monte Carlo's $1/\sqrt{N}$
error decay into $1/N$ — precisely *because* it is interference-limited, not magic. And when
optimization vendors claim a quantum box "evaluates every portfolio at once," you will know
to ask: where does the amplitude on the optimal portfolio actually come from? If they can't
answer, the speedup is imaginary.

<Reveal client:visible>
<details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
<summary className="cursor-pointer font-medium">If superposition isn't free parallelism, where does any speedup come from at all?</summary>

**Answer.** From **interference plus a clever problem structure**. Amplitudes can be negative
or complex, so contributions from many computational paths can cancel. A good quantum algorithm
(Grover's search, Shor's factoring, amplitude estimation) is engineered so that the paths
leading to wrong answers destructively interfere toward zero amplitude while paths to the right
answer constructively interfere toward large amplitude. You still measure once and get one
string — but now that string is *probably the answer*. No interference structure (which is most
problems), no speedup. That is why quantum advantage is rare and narrow, not universal.

</details>
</Reveal>

## Entanglement

<MCQ
  client:visible
  pretest
  question="Two qubits are entangled when:"
  options={[
    { text: "Their joint state cannot be written as one qubit's state times the other's — measuring one instantly constrains the other's outcome", correct: true },
    { text: "They are physically touching inside the chip" },
    { text: "They always hold the same value, like two copies of one bit" },
    { text: "One qubit sends a faster-than-light signal to control the other" },
  ]}
  explanation="Entanglement means the joint state does not factor into independent single-qubit states; outcomes are correlated beyond anything classical. It needs no physical contact, the qubits need not be equal, and no signal travels — the correlation just is."
/>

**Analogy.** Imagine two coins minted together with a strange rule: each spins independently
and lands heads or tails 50/50, yet whenever you check them they *always* land on opposite
faces — one heads, one tails — no matter how far apart you've carried them. Neither coin has
a predetermined face (each is genuinely 50/50 on its own), yet the pair shares a destiny that
no description of either coin alone can capture. That shared, non-factorable destiny is
entanglement. There is no hidden note slipped between them and no signal flying across the
room — the correlation is baked into the joint state.

**Definition.** A two-qubit state is **entangled** when it *cannot* be factored into a product
of two single-qubit states. A factorable (separable) state looks like
$|\psi\rangle = |\phi_A\rangle \otimes |\phi_B\rangle$. The canonical entangled state, the
**Bell state**, does not:

$$ |\Phi^+\rangle = \tfrac{1}{\sqrt{2}}\big(|00\rangle + |11\rangle\big). $$

Try to write that as (something for qubit A) times (something for qubit B) and you fail —
there is no such pair. The correlation lives in the *joint* description, not in either qubit.

**Worked example.** Measure the first qubit of $|\Phi^+\rangle$. The amplitudes on |00⟩ and
|11⟩ are each $\tfrac{1}{\sqrt 2}$, so you get outcome 0 with probability $|\tfrac{1}{\sqrt 2}|^2 = \tfrac{1}{2}$
and outcome 1 with probability $\tfrac{1}{2}$ — a fair coin, individually. But the instant you
see 0 on the first qubit, the |11⟩ branch is annihilated and the state collapses to |00⟩, so
the second qubit is now *certainly* 0 ($P = 1$). See 1 instead, and the second is certainly 1.
Individually random, perfectly correlated jointly: that is the signature you cannot reproduce
by handing each qubit its own private probability table.

<Callout variant="info" title="No mysticism, no signaling">
Entanglement is often dressed up as spooky telepathy. It isn't, and the honest framing matters
for finance modeling. The measured outcomes are correlated, but you cannot use them to send a
message: each side alone just sees random bits, and the correlation only appears when the two
result lists are later compared. Think of it as the strongest possible *statistical* coupling —
a joint distribution that no product of independent marginals can match — not a hotline.
</Callout>

### Why it matters for finance

Entanglement is the resource that makes the state space genuinely $2^n$-dimensional rather than
$n$ independent coins. Without it, $n$ qubits would carry no more correlated structure than $n$
separate biased coins, and there would be nothing to compute with. The interference tricks that
power amplitude estimation and QAOA all rely on building up entanglement across the register.
For the quant, the useful mental hook is *correlation with no classical factorization* — the
same instinct you use when a covariance matrix refuses to diagonalize into independent factors,
pushed to a regime classical probability literally cannot represent.

<ThinkReveal
  client:visible
  prompt="You measure the first qubit of the Bell state (|00⟩ + |11⟩)/√2 and read a 1. Before reading on, predict: what does a measurement of the second qubit now give, and with what probability?"
  text="Certainly 1, with probability 1. Reading 1 on the first qubit collapses the joint state to |11⟩, deleting the |00⟩ branch. The second qubit, which was individually 50/50 before, is now fully determined. That perfect post-measurement correlation — despite each qubit being a fair coin on its own — is exactly what 'cannot be factored into independent states' means."
/>

## Quantum gates and circuits

<MCQ
  client:visible
  pretest
  question="Quantum gates differ from classical logic gates (AND, OR) most fundamentally in that they are:"
  options={[
    { text: "Reversible — every gate has an inverse, so you can always run the computation backward; an AND gate throws information away and cannot", correct: true },
    { text: "Slower but otherwise identical in behavior" },
    { text: "Able to read the qubit's amplitudes directly without measuring" },
    { text: "Random number generators that scramble the state unpredictably" },
  ]}
  explanation="Quantum gates are unitary, hence reversible: each has an inverse and no information is destroyed mid-circuit. Classical gates like AND are irreversible (two inputs collapse to one output). Gates don't read amplitudes (only measurement extracts anything), and they are deterministic transformations, not random."
/>

**Analogy.** A classical AND gate is a paper shredder: feed it two bits, get one out, and you
can never reconstruct the inputs — information was destroyed. A quantum gate is more like a
Rubik's-cube twist: it rearranges the state in a fully reversible way, and the exact opposite
twist always undoes it. A quantum *circuit* is a choreographed sequence of those twists applied
to your qubits, and only at the very end do you "open your eyes" — measure — and read out bits.

**Definition.** A **quantum gate** is a **unitary** operation $U$ on the qubits: unitary means
$U^\dagger U = I$, which guarantees it is reversible (its inverse is $U^\dagger$) and preserves
the total probability ($\sum |c_x|^2 = 1$ stays true). Familiar gates:

- The **Hadamard** ($H$) turns |0⟩ into the equal superposition $\tfrac{1}{\sqrt 2}(|0\rangle + |1\rangle)$ — it *creates* superposition from a definite state.
- A **rotation** gate nudges the amplitudes by a chosen angle, used to load numbers and angles into the state.
- The **CNOT** (controlled-NOT) flips a target qubit only when the control qubit is 1 — a two-qubit gate that *creates entanglement*.

A **circuit** is a sequence of gates applied to an initialized register, ending in one
measurement. Two numbers describe a circuit's size: its **width** (how many qubits) and its
**depth** — the number of *sequential* gate layers from input to measurement.

**Worked example — building a Bell state in two gates.** Start with both qubits in |00⟩.
Apply $H$ to the first qubit: the state becomes $\tfrac{1}{\sqrt 2}(|00\rangle + |10\rangle)$ —
the first qubit is now a fair superposition, the second still 0. Now apply a CNOT with the
first qubit as control and the second as target: the |00⟩ term is left alone (control is 0),
while the |10⟩ term flips its second qubit to give |11⟩. The result is
$\tfrac{1}{\sqrt 2}(|00\rangle + |11\rangle) = |\Phi^+\rangle$ — the entangled Bell state from
the last section, built with **depth 2** (Hadamard, then CNOT). Two gates; an entangled pair.

<Callout variant="warning" title="Depth is the clock you're racing">
Every gate takes time, and real qubits quietly decay (decohere) the whole while. The deeper the
circuit — the more sequential gate layers — the more chances for noise to corrupt the fragile
amplitudes before you measure. On today's hardware a circuit that is too deep returns garbage.
Hold this thought: when we reach the NISQ reality check in lesson 5, "circuit depth versus
coherence time" is the wall that quietly kills most ambitious quantum-finance circuits. A clean
algorithm on paper that needs depth in the millions is, for now, science fiction.
</Callout>

### When to use it

Think in gates and depth whenever you evaluate a quantum algorithm's *feasibility*, not just
its theoretical speedup. Two algorithms can promise the same answer; the one with shallower
depth and fewer two-qubit gates (the noisy, expensive ones) is the one that might actually run
on near-term hardware. When lesson 2's amplitude estimation and lesson 3's QAOA show up, you'll
weigh them partly by the circuit depth they demand — because depth, not cleverness, is usually
what stands between a quantum result and a press release.

<Categorize
  client:visible
  question="Sort each statement under whether it describes a quantum GATE or a MEASUREMENT."
  buckets={["Quantum gate", "Measurement"]}
  items={[
    { text: "Reversible: has an inverse that undoes it", bucket: "Quantum gate" },
    { text: "Transforms amplitudes without revealing them", bucket: "Quantum gate" },
    { text: "Hadamard creating an equal superposition", bucket: "Quantum gate" },
    { text: "Collapses the state to a single basis string", bucket: "Measurement" },
    { text: "Irreversible: information is lost", bucket: "Measurement" },
    { text: "Returns classical bits you can read", bucket: "Measurement" },
  ]}
  explanation="Gates are unitary, reversible, and never reveal amplitudes — they just transform the state. Measurement is the one irreversible step: it collapses the superposition and hands back classical bits, destroying the amplitude structure in the process."
/>

## Gate model vs quantum annealing

<MCQ
  client:visible
  pretest
  question="A vendor says their D-Wave machine has '5,000 qubits' while IBM's gate-model chip has '127 qubits.' Why is comparing those numbers directly misleading?"
  options={[
    { text: "They are different machines: D-Wave is a special-purpose annealer that only finds low-energy states of one problem type; IBM's is a universal gate-model computer that runs arbitrary algorithms. Qubit counts are not comparable across paradigms", correct: true },
    { text: "D-Wave is simply 40 times more powerful at everything" },
    { text: "IBM miscounted; both should have the same number" },
    { text: "Annealers and gate machines run identical algorithms, so only the count differs" },
  ]}
  explanation="Annealing qubits and gate-model qubits do fundamentally different things. A 5,000-qubit annealer cannot run amplitude estimation; a 127-qubit gate machine is universal but small. Headcounts across the two paradigms measure different things and must never be compared directly."
/>

**Analogy.** The gate model is a **general-purpose computer** — like your laptop's CPU, it runs
any algorithm you can express, from option pricing to chess. Quantum annealing is a
**single-purpose appliance** — like a marble rolling down a bumpy landscape until it settles in
the lowest valley. The marble is fantastic at *one* job (finding the lowest point) and useless
at everything else. Asking an annealer to run amplitude estimation is like asking a ball bearing
to play chess.

**Definition.** The two hardware paradigms:

- The **gate model** builds computations from sequences of unitary gates (the circuits above).
  It is **universal**: any quantum algorithm — Grover's search, Shor's factoring, Quantum
  Amplitude Estimation (QAE), QAOA — can be expressed as a gate circuit. Hardware: superconducting
  qubits (IBM, Google) and trapped ions (IonQ, Quantinuum).
- **Quantum annealing** is **special-purpose**. It physically prepares a system whose energy
  encodes your problem, then slowly evolves it so it relaxes into a **low-energy state**. It
  solves exactly one shape of problem: minimizing an **Ising** energy or its equivalent **QUBO**
  (Quadratic Unconstrained Binary Optimization), of the form

$$ \min_{x \in \{0,1\}^n} \; \sum_i h_i\, x_i + \sum_{i<j} J_{ij}\, x_i x_j. $$

  It cannot run a general circuit. Hardware: D-Wave, with thousands of qubits — but qubits that
  only anneal, not gate.

**Worked example — reading the two spec sheets.** Suppose you want option pricing via amplitude
estimation. That is a *gate* algorithm, so a 5,000-qubit annealer scores **zero** on it — wrong
machine entirely; you need the (much smaller) gate-model device. Now suppose you want to pick the
best 10-asset subset out of 50 under a cardinality constraint — a discrete QUBO. The annealer's
thousands of qubits become relevant, while the 127-qubit gate machine must emulate the same QUBO
through QAOA and may run out of room. The lesson: the right qubit count depends entirely on which
*paradigm* your problem needs, which is why a single "how many qubits?" number tells you almost
nothing on its own.

| Dimension | Gate model | Quantum annealing |
|---|---|---|
| Generality | Universal — runs any quantum algorithm | Special-purpose — minimizes one energy/QUBO form |
| Core abstraction | Circuits of unitary gates, then measure | Slow relaxation to a low-energy state |
| Algorithms it runs | QAE, QAOA, Grover, Shor, quantum ML | QUBO / Ising minimization only |
| Hardware (examples) | Superconducting (IBM, Google), trapped ions (IonQ, Quantinuum) | D-Wave |
| Qubit counts today | Tens to a few hundred | Thousands (but annealing-only) |
| Finance relevance | Option pricing & risk (QAE, lesson 2); portfolio QAOA (lesson 3) | Discrete portfolio selection / QUBO (lesson 3) |
| Main near-term wall | Circuit depth vs noise (lesson 5) | Limited connectivity, no proven general speedup |

<Callout variant="tip" title="The map you'll keep reusing">
Before you evaluate any quantum-finance claim, locate it on this split. Is the proposed win a
*gate* algorithm (amplitude estimation, QAOA) or an *annealing* one (a QUBO fed to D-Wave)? They
have different speedup arguments, different hardware, and different failure modes. Lesson 2 lives
entirely in the gate model; lesson 3 visits *both*, because portfolio optimization can be cast as
a QUBO that runs on either. Mixing up the paradigms is the fastest way to be fooled by a spec sheet.
</Callout>

### When to use it

Match the machine to the problem shape. If your task is "estimate an expectation or price a
derivative," you want the gate model and amplitude estimation — annealing has nothing to offer.
If your task is "choose a discrete subset to minimize a quadratic objective" (asset selection,
lot-size constraints), a QUBO formulation opens the door to *both* annealing and gate-model QAOA,
and you compare them — and a classical solver — end to end. The instinct to cultivate: never let
a raw qubit count substitute for the question "is this even the right kind of machine?"

<MatchConcepts
  client:visible
  pairs={[
    { term: "Superposition", definition: "A weighted blend a|0⟩ + b|1⟩; measurement collapses it to one outcome with probability |a|² or |b|²" },
    { term: "Amplitude", definition: "A (possibly negative or complex) number whose squared magnitude gives a measurement probability; its sign enables interference" },
    { term: "Entanglement", definition: "A joint state that cannot be factored into independent single-qubit states; correlated outcomes with no classical analog" },
    { term: "Unitary gate", definition: "A reversible transformation of the state with an inverse; preserves total probability and never reveals amplitudes" },
    { term: "Circuit depth", definition: "The number of sequential gate layers; the quantity noise and decoherence will eventually destroy" },
    { term: "Quantum annealing", definition: "A special-purpose process that relaxes to the low-energy state of an Ising/QUBO problem; D-Wave hardware" },
  ]}
/>

## Recap

You came in with four fog-words and a promise of free lunch. You leave with working intuition:
a **qubit** is a superposition $a|0\rangle + b|1\rangle$ whose amplitudes square to measurement
probabilities; **superposition** holds $2^n$ amplitudes but yields one sample, so the real game
is **interference** that makes the wanted answer loud; **entanglement** is correlation no product
of independent qubits can fake; **gates** are reversible unitary twists assembled into circuits
whose **depth** is the clock noise is racing; and the field splits into the universal **gate
model** (QAE, QAOA) versus special-purpose **quantum annealing** (QUBO minimization on D-Wave).
That last split is your compass for the rest of the course.

<MindMap
  client:visible
  title="Quantum foundations for finance"
  caption="Build the map: four concepts, then the two machines that run them."
  root={{
    label: 'Quantum foundations',
    children: [
      { label: 'Qubit vs bit', children: [
        { label: 'State a|0⟩ + b|1⟩' },
        { label: 'Probabilities are |a|^2, |b|^2' },
        { label: 'Measurement collapses to one outcome' },
        { label: 'No-cloning: cannot copy freely' },
      ]},
      { label: 'Superposition', children: [
        { label: 'Holds 2^n amplitudes at once' },
        { label: 'But one measurement = one sample' },
        { label: 'Interference makes the answer loud' },
        { label: 'No free parallelism' },
      ]},
      { label: 'Entanglement', children: [
        { label: 'Joint state cannot be factored' },
        { label: 'Correlated outcomes, no classical analog' },
        { label: 'No signaling, no mysticism' },
      ]},
      { label: 'Gates and circuits', children: [
        { label: 'Unitary, reversible' },
        { label: 'Hadamard makes superposition' },
        { label: 'CNOT makes entanglement' },
        { label: 'Depth = noise will kill it (lesson 5)' },
      ]},
      { label: 'Two paradigms', children: [
        { label: 'Gate model: universal (QAE, QAOA)' },
        { label: 'Annealing: QUBO only (D-Wave)' },
        { label: 'Qubit counts not comparable' },
      ]},
    ],
  }}
/>

<Quiz
  client:visible
  title="Mixed check: did the four fog-words clear up?"
  questions={[
    {
      question: "A qubit is in state with amplitudes a = √3/2 and b = 1/2. What is the probability of measuring 1?",
      options: [
        { text: "1/4, because probability is |b|² = (1/2)² = 1/4", correct: true },
        { text: "1/2, because b is one of two outcomes" },
        { text: "1/2 exactly, the raw value of b" },
        { text: "3/4, because that's the larger amplitude squared" },
      ],
      explanation: "Probability of outcome 1 is the squared magnitude of b: (1/2)² = 1/4 = 25%. (And P(0) = (√3/2)² = 3/4, which correctly sums to 1.) The amplitude itself is never the probability.",
    },
    {
      question: "A startup pitches that its 60-qubit gate computer 'evaluates a quintillion portfolios simultaneously and returns the best.' What is the precise flaw?",
      options: [
        { text: "It can hold 2^60 amplitudes, but one measurement returns one string; without interference concentrating amplitude on the optimum, you just get a random portfolio", correct: true },
        { text: "60 qubits is too few to hold a quintillion states" },
        { text: "Gate computers cannot represent superposition at all" },
        { text: "The flaw is only that 60 qubits decohere too fast, nothing about the parallelism claim itself" },
      ],
      explanation: "The free-parallelism fallacy. The register does hold 2^60 amplitudes, but you extract a single sample per run. Without an interference structure that makes the optimal portfolio's amplitude large, the measurement is just a random draw — no advantage.",
    },
    {
      question: "Why can't the Bell state (|00⟩ + |11⟩)/√2 be described by giving each qubit its own independent probability table?",
      options: [
        { text: "Because the outcomes are perfectly correlated jointly while each qubit alone is 50/50 — a joint distribution no product of independent marginals can reproduce", correct: true },
        { text: "Because one qubit is always 0 and the other always 1" },
        { text: "Because the qubits secretly share a hidden value decided in advance" },
        { text: "Because a signal passes between them at the moment of measurement" },
      ],
      explanation: "That is the definition of entanglement: the joint state does not factor. Each qubit measured alone is a fair coin, yet the two are perfectly correlated — something no pair of independent single-qubit tables can capture.",
    },
    {
      question: "You apply a Hadamard to qubit 1 of |00⟩, then a CNOT (control qubit 1, target qubit 2). What did you build, and at what depth?",
      options: [
        { text: "The Bell state (|00⟩ + |11⟩)/√2, at depth 2 — H creates the superposition, CNOT entangles", correct: true },
        { text: "Two independent superpositions, at depth 1" },
        { text: "A collapsed measurement result, at depth 0" },
        { text: "The state |11⟩ with certainty, at depth 2" },
      ],
      explanation: "H turns |00⟩ into (|00⟩ + |10⟩)/√2; the CNOT flips qubit 2 only in the |10⟩ branch, giving (|00⟩ + |11⟩)/√2 — the Bell state. Two sequential gates means depth 2.",
    },
    {
      question: "You need to price a basket option via amplitude estimation. Which machine fits, and why is a 5,000-qubit D-Wave the wrong tool?",
      options: [
        { text: "The gate model, because QAE is a gate algorithm; D-Wave only minimizes QUBO/Ising energies and cannot run arbitrary circuits regardless of its qubit count", correct: true },
        { text: "D-Wave, because more qubits always means more pricing accuracy" },
        { text: "Either one; annealers and gate machines run the same algorithms" },
        { text: "Neither; amplitude estimation is a purely classical method" },
      ],
      explanation: "Amplitude estimation is a gate-model algorithm (lesson 2). A quantum annealer is special-purpose — it only relaxes to low-energy QUBO/Ising states — so its 5,000 qubits are irrelevant to QAE. You need the universal gate model, even if it has far fewer qubits.",
    },
  ]}
/>
