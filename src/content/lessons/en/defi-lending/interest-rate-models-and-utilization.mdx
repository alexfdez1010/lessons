---
title: "Interest-Rate Models: Where the Yield Comes From"
description: "DeFi interest rates are set by code, not a committee: the utilization rate, the kinked two-slope borrow curve, the reserve factor, and how the supply APR falls out of all three — with worked numbers and a live curve."
topic: "en/defi-lending"
order: 3
minutes: 9
updated: 2026-06-03
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, Categorize, MatchConcepts, MindMap, UtilizationRateCurve } from '@/components/react';

In a bank, your interest rate is the end of a long, opaque chain: a central-bank
target, a risk committee, a pricing desk, and a fee schedule nobody reads.
In a DeFi pool there is no committee, no desk, and no human at all. The rate is a
**public formula** anyone can read on-chain, and it stares at exactly one number:
how busy the pool is right now. That's it. Deposits flow in, loans flow out, and
a curve recomputes the rate every block. This lesson is about that curve — where
the yield you earn (and the cost you pay) actually comes from.

<MCQ
  client:visible
  pretest
  question="Guess before reading: what does a DeFi pool's interest-rate formula watch to set the rate?"
  options={[
    { text: "How much of the supplied money is currently borrowed out", correct: true },
    { text: "A rate voted on weekly by token holders", correct: false },
    { text: "The price of the collateral token", correct: false },
    { text: "Whatever the central bank's target rate is that day", correct: false },
  ]}
  explanation="The rate is a pure function of utilization — the fraction of supplied funds that's lent out. No vote, no price feed, no central bank. Busier pool, higher rate."
/>

## Utilization: how busy the pool is

Picture the pool as a reservoir. Suppliers pour water in; borrowers pump water
out. **Utilization** ($U$) is simply how much of the reservoir is currently
pumped out:

$$U = \frac{\text{total borrowed}}{\text{total supplied}}$$

Worked example: suppliers have deposited \$10,000,000 and borrowers have taken
\$8,000,000 of it. Then

$$U = \frac{8{,}000{,}000}{10{,}000{,}000} = 0.8 = 80\%.$$

That leftover \$2,000,000 is the **free liquidity** — the part suppliers could
still withdraw on demand. As $U$ climbs toward 100%, that free slice shrinks
toward zero: the pool is fully lent out, and a supplier who wants their money
back may find the cupboard bare. Utilization is the single dial every other
number in this lesson turns on.

<FillBlank
  client:visible
  question="Fill in the definition of utilization."
  text="Utilization equals total {{borrowed|supplied|reserved}} divided by total {{supplied|borrowed|withdrawn}}, so a pool that is fully lent out has U near {{100%|0%|50%}}."
  explanation="U = borrowed / supplied. When almost everything supplied has been borrowed, U approaches 100% and there's barely any free liquidity left to withdraw."
/>

## The kinked (two-slope) model

Here's the clever part. A naive pool would just let the rate rise in a straight
line as $U$ goes up. DeFi pools instead use a **kinked**, two-slope curve, and
the kink is the whole point.

Below an **optimal utilization** $U^{*}$ (the kink — say 80%), the borrow rate
rises on a **gentle** slope: borrowing is cheap and the protocol is happy to lend
out more. Cross $U^{*}$, though, and the rate jumps onto a **much steeper** slope.
The message to borrowers is blunt: *the pool is running low, so each extra dollar
you borrow now costs a lot more.* That steep section is a deliberate brake to
keep some liquidity free so suppliers can still withdraw.

The model, in two pieces. For $U \le U^{*}$:

$$r_{borrow} = r_0 + \frac{U}{U^{*}}\,s_1$$

and for $U > U^{*}$:

$$r_{borrow} = r_0 + s_1 + \frac{U - U^{*}}{1 - U^{*}}\,s_2$$

Here $r_0$ is the base rate at zero utilization, $s_1$ is the gentle first slope,
and $s_2$ is the steep second slope. Take the common example parameters
$r_0 = 0$, $s_1 = 4\%$, $s_2 = 75\%$, $U^{*} = 80\%$.

**Borrow APR at $U = 50\%$** (below the kink, so the first formula):

$$r_{borrow} = 0 + \frac{0.50}{0.80}\times 4\% = 0.625 \times 4\% = 2.5\%.$$

**Borrow APR at $U = 90\%$** (above the kink, so the second formula):

$$r_{borrow} = 0 + 4\% + \frac{0.90 - 0.80}{1 - 0.80}\times 75\% = 4\% + \frac{0.10}{0.20}\times 75\% = 4\% + 0.5 \times 75\% = 4\% + 37.5\% = 41.5\%.$$

Notice the violence of that jump: going from 50% to 80% busy only takes the rate
from 2.5% to 4%, but pushing past the kink to 90% rockets it to **41.5%**. That
asymmetry is the kink doing its job.

<Callout variant="info" title="Why two slopes, not one?">
A single gentle slope would let utilization drift to 100% with barely a rate
change — and then nobody could withdraw. A single steep slope would make normal
borrowing absurdly expensive. The kink gives you both: cheap borrowing in the
normal zone, and a panic-button slope that defends the last slice of liquidity.
</Callout>

## See the curve move

Drag the slider below. Watch the borrow curve bend sharply upward at the kink,
and watch the supply curve (the next section) track underneath it.

<UtilizationRateCurve client:visible caption="Drag utilization. Below the kink the borrow rate rises gently; above it the second, steep slope kicks in to defend the pool's last liquidity. The supply rate is the borrow rate scaled by utilization and the reserve cut." />

## The supply rate falls out of it

Suppliers don't get their own formula — their rate is **derived** from the borrow
rate:

$$r_{supply} = r_{borrow}\times U \times (1 - \text{reserveFactor}).$$

The intuition is fair, once you see it. Suppliers only earn interest on the
**fraction that's actually lent out** — that's the $\times\,U$. Money sitting idle
in the reservoir earns nothing. Then the protocol skims a cut for itself before
paying the rest to suppliers — that's the $\times\,(1 - \text{reserveFactor})$.

Worked example at $U = 80\%$ (right at the kink, so $r_{borrow} = 4\%$) with a
reserve factor of 10%:

$$r_{supply} = 4\% \times 0.80 \times (1 - 0.10) = 4\% \times 0.80 \times 0.90 = 2.88\%.$$

A side-by-side at our example parameters:

| Utilization $U$ | Borrow APR | Supply APR (reserve 10%) |
|---|---|---|
| 50% | 2.5% | $2.5\% \times 0.50 \times 0.90 = 1.125\%$ |
| 80% | 4.0% | $4.0\% \times 0.80 \times 0.90 = 2.88\%$ |
| 90% | 41.5% | $41.5\% \times 0.90 \times 0.90 = 33.62\%$ |

<Reveal client:visible summary="Why is the supply rate ALWAYS below the borrow rate?">
Because the supply rate multiplies the borrow rate by two factors that are each
**less than 1**: utilization $U$ (you can't lend out more than 100% of the pool,
so $U \le 1$) and $(1 - \text{reserveFactor})$ (the protocol always keeps a
non-negative cut, so this is $\le 1$ too). Multiplying any positive number by two
sub-1 factors can only shrink it. So $r_{supply} < r_{borrow}$ always — the gap
is where idle liquidity and the protocol's reserve quietly eat your yield.
</Reveal>

<MCQ
  client:visible
  question="Compute it: a pool has borrow APR = 10%, U = 60%, reserve factor = 20%. What's the supply APR?"
  options={[
    { text: "4.8%", correct: true },
    { text: "6.0%", correct: false },
    { text: "8.0%", correct: false },
    { text: "10.0%", correct: false },
  ]}
  explanation="r_supply = 10% × 0.60 × (1 − 0.20) = 10% × 0.60 × 0.80 = 4.8%. The 0.60 strips out idle liquidity; the 0.80 is the supplier's share after the 20% reserve cut."
/>

## The reserve factor

The **reserve factor** is the slice of borrower interest the protocol keeps for
itself instead of forwarding to suppliers. It funds a **safety buffer** — a
treasury that can absorb bad debt if a liquidation goes wrong, or backstop the
pool in a shortfall. A reserve factor of 10% means suppliers receive 90% of the
interest borrowers pay (after the utilization adjustment); 10% accrues to the
protocol. Riskier assets often carry higher reserve factors, because they're more
likely to need that buffer.

<MatchConcepts
  client:visible
  question="Match each rate-model term to what it means."
  pairs={[
    { term: "Utilization (U)", definition: "Fraction of supplied funds currently borrowed out" },
    { term: "Optimal utilization (the kink)", definition: "The U where the borrow rate switches from gentle to steep slope" },
    { term: "Reserve factor", definition: "The cut of borrower interest the protocol keeps as a buffer" },
    { term: "Borrow rate", definition: "What borrowers pay, set directly by the kinked curve" },
    { term: "Supply rate", definition: "Borrow rate scaled by U and the supplier's after-reserve share" },
  ]}
  instructions="Pick a term, then its definition."
/>

## Why the kink exists

The deepest reason for the kink is a bank-run problem. A bank promises you instant
withdrawals but lends most of your deposit out; if everyone shows up at once,
there isn't enough cash, and you get a run. A DeFi pool faces the same trap: if
$U$ hits 100%, every dollar is lent out and **no supplier can withdraw** until a
borrower repays.

The steep second slope is the pool's automatic defense — a pure **price signal**.
As utilization creeps past the kink, borrowing gets punishingly expensive, which
**prices borrowers out** (some repay to escape the high rate) and the soaring
supply rate **lures fresh suppliers in**. Both forces push $U$ back down toward
$U^{*}$, restoring free liquidity — no committee required, just incentives doing
the steering. (The flip side, what happens to *your* loan when liquidity or
prices move against you, is the liquidation story we tackle in lesson 5.)

<Categorize
  client:visible
  question="Sort each force by which way it pushes utilization."
  buckets={["Pushes utilization down", "Pushes utilization up"]}
  items={[
    { text: "Borrow rate spikes past the kink", bucket: "Pushes utilization down" },
    { text: "Borrowers repay to escape a high rate", bucket: "Pushes utilization down" },
    { text: "A juicy supply APR attracts new deposits", bucket: "Pushes utilization down" },
    { text: "Borrowers take out more loans", bucket: "Pushes utilization up" },
    { text: "Suppliers withdraw their deposits", bucket: "Pushes utilization up" },
  ]}
  explanation="Anything that adds free liquidity (new deposits) or removes loans (repayments) lowers U. Anything that drains the reservoir — new borrowing or withdrawals — raises it. The steep slope exists to trigger the first column when U gets dangerous."
/>

## Variable vs stable rates

Most DeFi borrow rates are **variable**: they float every single block as $U$
moves, so the cost of your loan can change minute to minute. Some protocols also
offered a **stable** rate — typically set *higher* than the variable rate at
borrow time, but steadier, so you trade a premium for predictability (in practice
these can still be re-balanced under extreme conditions, so "stable" means
"smoother," not "fixed forever").

## Key Takeaways

<Callout variant="success" title="What to remember">
- **Utilization runs everything**: $U = \text{borrowed} / \text{supplied}$, and the entire rate is a public function of it.
- **The borrow rate is a kinked two-slope curve**: gentle below the optimal utilization $U^{*}$, brutally steep above it — that steepness defends the pool's last liquidity.
- **The supply rate is derived, not set**: $r_{supply} = r_{borrow}\times U \times (1 - \text{reserveFactor})$, so it's always below the borrow rate.
- **The reserve factor** is the protocol's cut, building a safety buffer out of borrower interest.
- **No human sets these rates** — incentives push utilization back toward $U^{*}$ on their own.
</Callout>

<MindMap
  client:visible
  title="The rate model at a glance"
  caption="Utilization is the dial; everything else turns on it."
  root={{
    label: "Interest-rate model",
    children: [
      {
        label: "Utilization U = borrowed / supplied",
        children: [
          { label: "High U → little free liquidity" },
          { label: "Drives every rate" },
        ],
      },
      {
        label: "Borrow rate (kinked)",
        children: [
          { label: "Below kink: gentle slope s1" },
          { label: "Above kink: steep slope s2" },
        ],
      },
      {
        label: "Supply rate (derived)",
        children: [
          { label: "× U: earn only on lent funds" },
          { label: "× (1 − reserveFactor): after the cut" },
        ],
      },
      {
        label: "Reserve factor",
        children: [
          { label: "Protocol's safety buffer" },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  questions={[
    {
      question: "A pool has supplied $20,000,000 and borrowed $15,000,000. What is utilization?",
      options: [
        { text: "75%", correct: true },
        { text: "133%", correct: false },
        { text: "5%", correct: false },
        { text: "25%", correct: false },
      ],
      explanation: "U = 15,000,000 / 20,000,000 = 0.75 = 75%. Borrowed over supplied, always.",
    },
    {
      question: "Using r0=0, s1=4%, s2=75%, U*=80%, what's the borrow APR at U = 40%?",
      options: [
        { text: "2.0%", correct: true },
        { text: "4.0%", correct: false },
        { text: "1.6%", correct: false },
        { text: "20.0%", correct: false },
      ],
      explanation: "Below the kink: r = 0 + (0.40/0.80) × 4% = 0.5 × 4% = 2.0%.",
    },
    {
      question: "Why is the second slope so much steeper than the first?",
      options: [
        { text: "To price borrowers out and lure suppliers in when liquidity runs low, pushing U back down", correct: true },
        { text: "To maximize protocol revenue at all times", correct: false },
        { text: "Because gas fees rise with utilization", correct: false },
        { text: "To punish suppliers for withdrawing", correct: false },
      ],
      explanation: "The steep slope is a price signal that defends the pool's withdrawability: expensive borrowing slows new loans and high supply APR attracts deposits, both lowering U.",
    },
    {
      question: "Which statement about the supply rate is TRUE?",
      options: [
        { text: "It can never exceed the borrow rate, because it's the borrow rate times two factors each ≤ 1", correct: true },
        { text: "It equals the borrow rate when the pool is empty", correct: false },
        { text: "It's set independently by governance each week", correct: false },
        { text: "It ignores utilization entirely", correct: false },
      ],
      explanation: "r_supply = r_borrow × U × (1 − reserveFactor). Both U and (1 − reserveFactor) are at most 1, so the supply rate is always ≤ the borrow rate.",
    },
    {
      question: "Borrow APR is 30%, U = 90%, reserve factor = 10%. What's the supply APR?",
      options: [
        { text: "24.3%", correct: true },
        { text: "27.0%", correct: false },
        { text: "30.0%", correct: false },
        { text: "21.0%", correct: false },
      ],
      explanation: "r_supply = 30% × 0.90 × (1 − 0.10) = 30% × 0.90 × 0.90 = 24.3%.",
    },
    {
      question: "Spot the myth about the kinked model.",
      options: [
        { text: "Myth: a human committee adjusts the rate as utilization changes", correct: true },
        { text: "Fact: the rate is a public on-chain function of utilization", correct: false },
        { text: "Fact: above U*, each extra dollar borrowed costs sharply more", correct: false },
        { text: "Fact: suppliers earn only on the borrowed fraction of the pool", correct: false },
      ],
      explanation: "No human sets it block to block — the formula does. The other three statements are accurate descriptions of the model.",
    },
  ]}
/>

Next up in **lesson 4**: when prices move against a borrower, the **health
factor** decides whether their position survives or gets **liquidated** — and how
that keeps the whole pool solvent.
