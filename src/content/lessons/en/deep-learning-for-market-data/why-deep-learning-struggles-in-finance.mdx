---
title: "Why Deep Learning Struggles in Finance"
description: "Deep learning conquered images and language, then markets humbled it — the effective-sample-size catastrophe, why a plain MLP both over- and under-fits low-signal returns, and why 'bigger model' is the wrong reflex in a low-SNR, non-stationary world."
topic: "en/deep-learning-for-market-data"
order: 1
minutes: 15
updated: 2026-06-19
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, MatchConcepts, Categorize, MindMap, TsOverfitCurve, ReturnDistribution, TsStationarityChart } from '@/components/react';

Deep learning has a highlight reel that would make any technology blush. It crushed
ImageNet, learned to transcribe speech better than tired humans, and now writes
poetry, code, and passable cover letters. So a reasonable person thinks: point this
firehose of expressive power at the stock market and retire by Thursday.

Then the firehose meets the market, and the market — politely, expensively — hands
it back its lunch money.

Here's the punchline up front: **deep learning's troubles in finance are not new
problems. They are the exact lessons from *Machine Learning for Alpha* turned up to
eleven, handed to a model class with millions of parameters and a world-class talent
for memorizing noise.** Everything you learned — overfitting is the enemy, an honest
information coefficient (IC) lives around 0.02–0.05, out-of-sample R² of 0.01–0.05 is
a *good day*, you must purge and embargo your cross-validation, you must deflate your
Sharpe for the number of trials — all of it still holds. It just bites harder, because
a neural net is a far more enthusiastic noise-memorizer than a ridge regression.

This lesson is the "why it's hard" foundation for the whole course. Once you respect
the difficulty, the later tools (RNNs, TCNs, attention, embeddings) become *scalpels*
instead of *footguns*.

## The effective-sample-size catastrophe

<MCQ
  client:visible
  pretest
  question="ImageNet has ~14 million labeled images. Twenty years of daily equity returns is ~5,000 rows. Which statement best captures the real gap for training a model?"
  options={[
    { text: "The gap is far worse than 14M vs 5,000, because financial rows are autocorrelated, overlapping, and non-stationary — the effectively independent count is far smaller", correct: true },
    { text: "The gap is exactly 14M vs 5,000 — rows are rows" },
    { text: "There is no real gap; 5,000 rows is plenty for any model" },
    { text: "Finance actually has more usable data because markets trade every second" },
  ]}
  explanation="Raw row count flatters financial data. Autocorrelation, overlapping labels, and regime shifts mean the effectively independent sample size is dramatically smaller than the raw N."
/>

**Analogy.** Counting raw data rows in finance is like counting frames in a slow-motion
video and calling each one an independent photo. Sure, there are 5,000 frames — but
consecutive frames are nearly identical. The *information* is in the changes, and there
are far fewer real changes than frames.

**Definition.** The **effective sample size** is the number of *effectively independent*
observations your data contains, after accounting for correlation between rows. For a
series with lag-1 autocorrelation $\rho$, a common rough adjustment is

$$ N_{\text{eff}} \approx N \cdot \frac{1 - \rho}{1 + \rho}. $$

That formula assumes a simple AR(1)-style decay; the deeper point is universal:
**correlated rows each carry less than one row's worth of new information.**

Three multipliers shrink financial $N_{\text{eff}}$ at once:

1. **Autocorrelation.** Returns have structure (recall volatility clustering and
   autocorrelation from *Time-Series Finance*). Today's regime looks a lot like
   yesterday's.
2. **Overlapping labels.** Predict the *next 20-day* return and consecutive samples
   share 19 of 20 days. Lopez de Prado's whole purged/embargoed CV machinery exists
   precisely because these labels overlap — they are not independent draws.
3. **Non-stationarity.** The 2010 market and the 2022 market are not samples from the
   same distribution. Old rows describe a *different* game (this is the
   stationarity problem from *Time-Series Finance*, now a sample-size problem).

**Worked example.** Take 20 years ≈ 5,000 daily rows. Suppose the relevant signal has
lag-1 autocorrelation $\rho = 0.97$ (high, but realistic for slow regime variables).
Then

$$ N_{\text{eff}} \approx 5000 \cdot \frac{1 - 0.97}{1 + 0.97} = 5000 \cdot \frac{0.03}{1.97} \approx 5000 \cdot 0.0152 \approx 76. $$

Seventy-six. Now layer overlapping 20-day labels on top — divide again by roughly the
overlap factor — and you are flirting with *tens* of independent observations. Compare
that to even a *small* MLP: one hidden layer of 64 units on 30 inputs already has
$30 \times 64 + 64 = 1984$ weights, plus $64 \times 1 + 1 = 65$ in the output layer —
about 2,049 parameters.

| Quantity | Value |
|---|---|
| Raw rows (20y daily) | ~5,000 |
| $N_{\text{eff}}$ after $\rho = 0.97$ | ~76 |
| $N_{\text{eff}}$ after 20-day label overlap | tens |
| Params in a *tiny* 64-unit MLP | ~2,049 |
| Params : effective observations | ~30 : 1 (or worse) |

ImageNet runs the ratio the *other* way: ~14M near-i.i.d. examples against model
parameters, so each weight is disciplined by many independent examples. In finance you
hand a 2,000-parameter model a few dozen real observations and ask it to be humble. It
will not be humble.

<TsStationarityChart client:visible caption="Price level (drifting, non-stationary) vs returns (noisy, near-stationary). The thing you can model has far less stable structure than the row count suggests — and old rows describe a different regime." />

<Callout variant="warning" title="The headline-N trap">
The single most expensive mistake here is quoting the raw row count to justify a
big model: "I have 30 years of minute bars — that's millions of rows!" Minute bars are
*ferociously* autocorrelated and overlapping. Your effective sample size for a daily-or-slower
signal might be in the dozens. Sizing a network to your raw N instead of your effective
N is how you build a confident, well-trained, completely useless model.
</Callout>

### When to use it

Compute (or at least estimate) effective sample size *before* choosing model capacity —
it is the budget your parameter count must respect. If $N_{\text{eff}}$ is in the tens,
your "model" should probably be a linear rule with a handful of coefficients, not a net.
Deep learning earns its keep when you can *raise* $N_{\text{eff}}$ — pooling thousands
of names, or working on a high-frequency subproblem with genuinely many independent
events (more on both later).

<FillBlank
  client:visible
  question="Fill in the core idea of effective sample size."
  text="Because financial rows are autocorrelated and overlapping, the number of {{effectively independent|raw labeled|total available}} observations is far smaller than the raw row count."
  explanation="Effective sample size discounts correlated rows. Raw and total counts ignore the correlation entirely, which is the whole trap."
/>

## Why a plain MLP both over- AND under-fits

<MCQ
  client:visible
  pretest
  question="In a very low signal-to-noise setting, what is the trap a plain MLP falls into?"
  options={[
    { text: "It overfits when flexible, but regularizing hard enough to stop that also crushes the tiny real signal — there's no comfortable operating point", correct: true },
    { text: "It always underfits because returns are simply unpredictable" },
    { text: "It always overfits and more data fixes it cleanly" },
    { text: "It hits a perfect bias–variance sweet spot like in textbook examples" },
  ]}
  explanation="Low SNR squeezes the bias–variance tradeoff from both sides: flexibility buys variance (fits noise), regularization buys bias (kills the faint signal)."
/>

**Analogy.** Tuning an MLP on returns is like adjusting a radio whose station is
broadcasting at a whisper inside a thunderstorm. Turn up the gain and you amplify
static (overfit). Turn on aggressive noise-cancelling and you also cancel the whisper
(underfit). There is no knob setting where you hear the song clearly.

**Definition.** Expected prediction error decomposes as

$$ \mathbb{E}[\text{error}] = \underbrace{\text{Bias}^2}_{\text{too rigid}} + \underbrace{\text{Variance}}_{\text{too flexible}} + \underbrace{\sigma^2}_{\text{irreducible noise}}. $$

In images $\sigma^2$ is tiny relative to signal: there *is* a cat, and the pixels
scream it. In returns, $\sigma^2$ dominates by orders of magnitude — the signal might
explain 1–5% of variance (the OOS R² range you already know), and noise the other
95–99%. When $\sigma^2$ swamps everything, the bias and variance terms you *can*
control are both fighting over scraps, and reducing one inflates the other with almost
no net win.

**Worked example.** Suppose the *true* edge produces an in-sample monthly IC of 0.04.
Watch what three capacity settings do to a held-out (out-of-sample) Sharpe:

| MLP capacity | In-sample Sharpe | Out-of-sample Sharpe | What happened |
|---|---|---|---|
| Tiny + heavy L2 | 0.35 | 0.30 | Underfit — bias crushed the faint signal too |
| "Just right" (in theory) | 1.40 | 0.45 | Best OOS, still mediocre, knife-edge to find |
| Wide + lightly regularized | 3.10 | -0.20 | Overfit — memorized noise, negative live |

Notice the cruelty: the in-sample Sharpe rises smoothly and seductively from 0.35 to
3.10, while the *real* (out-of-sample) number peaks weakly and then goes **negative**.
The "just right" column technically exists, but in a regime that's drifting under your
feet, last quarter's sweet spot is this quarter's overfit. You are aiming at a target
that moves while you pull the trigger.

<TsOverfitCurve client:visible caption="In-sample error keeps falling with complexity; out-of-sample error bottoms out early and then climbs. In low-SNR finance that out-of-sample valley is shallow and easy to overshoot." />

<Callout variant="warning" title="More capacity is not more edge">
The reflex "my Sharpe is low, let me add layers/width" is exactly backwards. In a
low-SNR world, added capacity almost always moves you *right* on the overfit curve —
better in-sample, worse out-of-sample. The disciplined move is usually *less* model:
fewer features, stronger regularization, simpler functional form. Bigger is the wrong
default reflex.
</Callout>

### When to use it

A plain MLP on raw return prediction is rarely the right first tool — start linear,
prove an edge exists, and only add nonlinearity if a *held-out, purged-CV* test says
the linear model leaves real money on the table. If your in-sample Sharpe rockets while
OOS sags, you've found the overfit cliff, not alpha.

<Categorize
  client:visible
  question="Sort each symptom under the failure mode it signals."
  buckets={["Overfit", "Underfit"]}
  items={[
    { text: "In-sample Sharpe 3+, out-of-sample negative", bucket: "Overfit" },
    { text: "Wide network, almost no regularization", bucket: "Overfit" },
    { text: "Model memorizes idiosyncratic noise events", bucket: "Overfit" },
    { text: "Heavy L2 plus a tiny network", bucket: "Underfit" },
    { text: "Both in- and out-of-sample Sharpe near zero", bucket: "Underfit" },
    { text: "Regularization so strong the faint real signal is erased", bucket: "Underfit" },
  ]}
  explanation="Overfit = high in-sample, collapsing out-of-sample (variance dominates). Underfit = both flat near zero because bias crushed even the real signal. Low SNR makes both easy to hit."
/>

## Universal approximation is a curse here

<MCQ
  client:visible
  pretest
  question="The universal approximation theorem says a big-enough MLP can approximate any continuous function. In low-SNR return data, why is that a liability?"
  options={[
    { text: "Because most of the 'function' it can fit is noise, so unlimited expressiveness mostly buys an exquisite fit to randomness", correct: true },
    { text: "Because the theorem is false for financial data" },
    { text: "Because it makes the model train too slowly to be useful" },
    { text: "Because approximating any function guarantees a profitable strategy" },
  ]}
  explanation="Universal approximation is power to fit whatever is in front of you. When 95–99% of what's in front of you is noise, that power is mostly aimed at noise."
/>

**Analogy.** Universal approximation is a sculptor who can carve *anything* from a block
of marble. Wonderful — if the block contains a statue. In finance, you hand the sculptor
a block that's 97% sand and 3% marble dust, and say "carve the hidden figure." A
less-talented sculptor would shrug and give you a rough lump (and accidentally be right
about how little is there). The genius confidently carves an intricate, beautiful figure
out of the *sand*. That figure is your overfit.

**Definition.** The **universal approximation theorem** states that a feedforward
network with a single hidden layer of sufficient width can approximate any continuous
function on a compact set to arbitrary accuracy. The catch the theorem never mentions:
it makes *no promise about generalization*. It guarantees the model *can* match your
training data — including every noisy wiggle — not that the matched function is the
*true* one.

**Worked example.** Imagine 60 training points where the true relationship is
$y = 0.05\,x + \varepsilon$ with noise $\varepsilon$ ten times larger than the
$0.05\,x$ term. A linear fit recovers a slope near 0.05 — modest, honest, roughly right.
A wide MLP can drive training error to nearly zero by threading a wiggly curve through
all 60 points. Decompose what each "learned":

| Model | Training R² | Fraction of fit that is real signal | Out-of-sample behavior |
|---|---|---|---|
| Linear (1 slope) | ~0.03 | almost all of its (tiny) fit | reproduces the modest edge |
| Wide MLP | ~0.98 | ~3% (the rest is fitted noise) | falls apart; the wiggles don't recur |

The MLP's 0.98 training R² is *mostly a portrait of this particular noise sample*.
Next month's noise is a different sample, the wiggles don't line up, and the gorgeous
fit evaporates. Expressive power converted noise into false confidence.

<ReturnDistribution client:visible caption="Returns are fat-tailed and noise-dominated. A universal approximator will happily fit the one-off tail events as if they were rules — that's memorized noise, not signal." />

<Callout variant="warning" title="Expressiveness ≠ usefulness">
In high-SNR domains (vision, speech) more expressive power usually helps because there's
a rich true function to capture. In low-SNR finance, expressiveness mostly increases the
amount of noise you can fit. Capacity is a liability you must *defend against* with
regularization, simplicity, and brutal out-of-sample testing — not an asset to maximize.
</Callout>

### When to use it

Treat expressive power as something to *spend deliberately*, only where there's
demonstrable structure to capture (nonlinear interactions a linear model provably
misses, on a held-out purged test). Default to the least expressive model that clears
the bar. "Could a simpler model do this?" should precede every architecture decision.

<Reveal client:visible>
<details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
<summary className="cursor-pointer font-medium">If an MLP can approximate any function, why doesn't it find the true return function?</summary>

**Answer.** Because the theorem promises *representational capacity*, not *recoverability
from a finite, noisy sample*. With effective sample size in the tens and noise drowning
the signal 20-to-1, there are countless wiggly functions that fit the training data
better than the true (nearly flat) one — and the optimizer happily picks one of those.
The true function is *representable*; it is just not *identifiable* from this data. More
capacity widens the set of noise-fitting impostors it can choose from.

</details>
</Reveal>

## The parameter–trial tax

<MCQ
  client:visible
  pretest
  question="You try 200 architectures (varying width, depth, learning rate) and report the best backtest Sharpe. From a multiple-testing view, what have you done?"
  options={[
    { text: "Run 200 trials — so the best Sharpe is inflated by selection and must be deflated for the number tried", correct: true },
    { text: "Found the one true architecture, no adjustment needed" },
    { text: "Reduced overfitting by exploring more options" },
    { text: "Nothing relevant to multiple testing; that only applies to feature selection" },
  ]}
  explanation="Every architecture you evaluate and select among is a trial. The maximum of many noisy Sharpes is biased upward — exactly the deflated-Sharpe problem from ML for Alpha."
/>

**Analogy.** Buying 200 lottery tickets and then bragging about the winning one as if
it proved your *system*. The win was selection from many draws, not skill. Architecture
search is buying lottery tickets with your backtest as the draw.

**Definition.** Recall **deflated Sharpe** from *Machine Learning for Alpha*: when you
select the best of $N$ independent trials, the expected maximum Sharpe under the null
(no real skill) grows with

$$ \mathbb{E}[\max \text{SR}] \approx \sigma_{\text{SR}} \cdot \sqrt{2 \ln N}, $$

where $\sigma_{\text{SR}}$ is the standard deviation of a single trial's Sharpe estimate.
**Every architecture, width, depth, learning rate, and seed you evaluate is another $N$.**
Deep learning's enormous configuration space makes $N$ explode — and you must subtract
this selection bias before believing any number.

**Worked example.** Suppose a single backtest's Sharpe has estimation noise
$\sigma_{\text{SR}} = 0.5$. Watch the null-expected "best" Sharpe climb purely from
trying more nets:

| Architectures tried, $N$ | $\sqrt{2 \ln N}$ | Expected best Sharpe under the null |
|---|---|---|
| 1 | 0.00 | 0.00 |
| 10 | 2.15 | $0.5 \times 2.15 \approx 1.07$ |
| 100 | 3.03 | $0.5 \times 3.03 \approx 1.52$ |
| 1,000 | 3.72 | $0.5 \times 3.72 \approx 1.86$ |

Do the arithmetic for $N = 100$: $2 \ln 100 = 2 \times 4.605 = 9.21$, and
$\sqrt{9.21} \approx 3.03$, so the expected best Sharpe from *pure luck* is about
$0.5 \times 3.03 \approx 1.52$. If your grid search over 100 nets returns a Sharpe of
1.5, you have found exactly what randomness predicts — *zero evidence of skill.* Your
deflated Sharpe is essentially nil.

<Callout variant="warning" title="Architecture search launders overfitting">
Each net you discard is invisible in the final write-up but very real in the statistics.
A pristine purged/embargoed CV on the *winning* architecture does nothing to undo the
selection bias from the 199 you threw away. Log every configuration you try, count them
as trials, and deflate. The honest Sharpe is almost always far below the one in the
victory-lap slide.
</Callout>

### When to use it

Budget your trials *before* you start, log every configuration, and report a Sharpe
deflated for the full count — not the per-fold count of the survivor. Prefer a small,
pre-registered set of architectures over a sprawling search. If you must search, hold
out a final, never-touched test period that no configuration ever saw, and judge there.

<FillBlank
  client:visible
  question="Connect architecture search to multiple testing."
  text="The expected best Sharpe under the null grows with {{the square root of 2 ln N|the log of the data size|the number of features only}}, where N counts every architecture tried."
  explanation="Deflated Sharpe scales the per-trial Sharpe noise by √(2 ln N). N counts every configuration evaluated, not just features."
/>

## When deep learning CAN help

<MCQ
  client:visible
  pretest
  allowMultiple
  question="Where does deep learning have a genuine fighting chance in markets? (Select all that apply.)"
  options={[
    { text: "High-SNR microstructure tasks like limit-order-book dynamics", correct: true },
    { text: "A huge pooled cross-section: thousands of names sharing one model for more breadth", correct: true },
    { text: "Representation learning on alt-data: text, images, audio", correct: true },
    { text: "Predicting tomorrow's single-stock return from 50 daily price rows with a giant net" },
  ]}
  explanation="DL shines where you can raise effective sample size or SNR: fast microstructure (many independent events), pooled cross-sections (breadth), and rich unstructured alt-data. A giant net on a few daily rows of one name is the classic trap."
/>

After four sections of bad news, here's the turn: deep learning is not useless in
finance — it's *misapplied* when pointed at low-SNR, low-$N_{\text{eff}}$ single-series
return prediction. Aim it where the math is on your side and it earns its keep.

**Analogy.** A Formula 1 car is a disaster in city traffic and unbeatable on a track.
The problem was never the car; it was driving it through stop-and-go. Find the track.

**The three legitimate tracks:**

| Where DL helps | Why the math flips in your favor | Course tools |
|---|---|---|
| **High-SNR microstructure** (limit-order-book dynamics, short-horizon volatility) | Many genuinely independent events per day → large $N_{\text{eff}}$; signal is stronger at short horizons | TCNs, attention over order-book states |
| **Pooled cross-section** (one model across thousands of names) | Breadth: thousands of names sharing parameters multiply *effective* observations and stabilize estimates | Shared embeddings, panel models |
| **Representation learning on alt-data** (news text, satellite images, earnings-call audio) | The hard part is *encoding* unstructured data — exactly what DL is best at; SNR can be higher than price-only | Embeddings, transformers, CNNs |

**Worked example — breadth via pooling.** The fundamental law of active management says
information ratio scales as $\text{IR} \approx \text{IC} \cdot \sqrt{\text{breadth}}$.
One model on one name with daily IC 0.03 and ~50 effectively independent bets:
$0.03 \times \sqrt{50} \approx 0.21$ — flimsy. Pool the *same* model across 3,000 names,
each contributing roughly independent bets, and breadth balloons. Even holding IC at a
modest 0.03, $0.03 \times \sqrt{3000} \approx 1.64$. Same per-bet skill, vastly more
breadth — *that's* the lever deep learning can actually pull, because one shared model
now learns from 3,000 cross-sectional examples per day instead of one time step.

<Callout variant="tip" title="The reframing">
The fixes for deep learning in finance are almost all about *raising effective sample
size or SNR* — pool across names for breadth, drop to microstructure horizons for
independent events, or move to alt-data where encoding is the bottleneck. Architecture
cleverness matters far less than putting the model somewhere the statistics aren't
hopeless. The rest of this course is a tour of those somewheres.
</Callout>

### When to use it

Reach for deep learning when at least one of three boxes is checked: (1) the subproblem
is high-SNR with many independent events, (2) you can pool a wide cross-section to buy
breadth, or (3) the real challenge is *representing* unstructured alt-data. If none are
checked — you're predicting one slow series from a few hundred autocorrelated rows —
close the laptop and write down a linear rule.

<MatchConcepts
  client:visible
  pairs={[
    { term: "Effective sample size", definition: "Number of effectively independent observations after discounting correlated, overlapping rows" },
    { term: "Universal approximation", definition: "A net can fit any function — including all your noise, with no generalization promise" },
    { term: "Parameter–trial tax", definition: "Each architecture tried is a trial; the best Sharpe inflates by √(2 ln N) under the null" },
    { term: "Pooled cross-section", definition: "Sharing one model across thousands of names to multiply breadth and effective observations" },
    { term: "Low SNR squeeze", definition: "Flexibility overfits noise; heavy regularization underfits the faint signal — no comfortable operating point" },
  ]}
/>

## Recap

You arrived expecting markets to fall to the same firehose that flattened ImageNet.
Instead you learned *why* the firehose mostly sprays the floor: tiny effective sample
sizes, a signal-to-noise ratio that punishes both flexibility and rigidity, an
approximation theorem that mostly empowers noise-fitting, and a configuration space so
large that architecture search quietly launders overfitting into a victory-lap Sharpe.
And you learned where the F1 car finds its track — high-SNR microstructure, pooled
cross-sections, and alt-data representation.

<MindMap
  client:visible
  title="Why deep learning struggles in finance"
  caption="Build the map: the four headwinds, then the three places DL still wins."
  root={{
    label: 'DL in finance',
    children: [
      { label: 'Effective-sample-size catastrophe', children: [
        { label: 'Autocorrelation' },
        { label: 'Overlapping labels' },
        { label: 'Non-stationarity (regimes)' },
        { label: 'N_eff in tens, not thousands' },
      ]},
      { label: 'Over- AND under-fit squeeze', children: [
        { label: 'High capacity → fits noise' },
        { label: 'Heavy regularization → kills signal' },
        { label: 'No comfortable operating point' },
      ]},
      { label: 'Universal approximation curse', children: [
        { label: 'Can fit any function' },
        { label: 'Mostly fits the 95–99% noise' },
        { label: 'Representable ≠ identifiable' },
      ]},
      { label: 'Parameter–trial tax', children: [
        { label: 'Each net = one trial' },
        { label: 'Deflate by √(2 ln N)' },
      ]},
      { label: 'Where DL wins', children: [
        { label: 'High-SNR microstructure' },
        { label: 'Pooled cross-section (breadth)' },
        { label: 'Alt-data representation' },
      ]},
    ],
  }}
/>

<Quiz
  client:visible
  title="Mixed check: did the market humble you back?"
  questions={[
    {
      question: "A quant brags about 30 years of minute bars as justification for a large network. What is the core flaw?",
      options: [
        { text: "Raw row count ignores autocorrelation and overlap; effective sample size may be in the dozens", correct: true },
        { text: "Minute bars are too recent to be useful" },
        { text: "Thirty years is simply not enough calendar time" },
        { text: "Large networks always train faster, so it's fine" },
      ],
      explanation: "Minute bars are heavily autocorrelated and overlapping. Sizing capacity to raw N instead of N_eff builds a confident, useless model.",
    },
    {
      question: "Across capacity settings, in-sample Sharpe climbs from 0.35 to 3.10 while out-of-sample peaks at 0.45 then goes negative. What does this show?",
      options: [
        { text: "The low-SNR over/under-fit squeeze — added capacity buys in-sample fit and out-of-sample collapse", correct: true },
        { text: "A clean bias–variance sweet spot that's easy to hold" },
        { text: "Proof the strategy has a strong, stable edge" },
        { text: "The model is underfitting at every setting" },
      ],
      explanation: "The seductive in-sample rise alongside an OOS peak-then-crash is the signature of overfitting noise in a low-SNR regime that also drifts under you.",
    },
    {
      question: "You searched 100 architectures (single-trial Sharpe noise ≈ 0.5) and the best scored 1.5. What's the honest read?",
      options: [
        { text: "About what pure luck predicts: √(2 ln 100) ≈ 3.03, so expected best ≈ 1.52 under the null — essentially no evidence of skill", correct: true },
        { text: "A robust 1.5 Sharpe strategy ready to trade" },
        { text: "Selection bias only matters for feature search, not architectures" },
        { text: "The purged CV on the winner already removed all selection bias" },
      ],
      explanation: "Deflated Sharpe scales by √(2 ln N) over all trials. 0.5 × 3.03 ≈ 1.52, so 1.5 is the null expectation — deflated Sharpe ≈ 0.",
    },
    {
      question: "Which deployment best raises effective observations for a deep model?",
      options: [
        { text: "Pooling one model across thousands of names to multiply cross-sectional breadth", correct: true },
        { text: "Adding more hidden layers to a single-name daily predictor" },
        { text: "Extending one stock's history from 20 to 40 years" },
        { text: "Lowering the learning rate on a single-series model" },
      ],
      explanation: "IR ≈ IC·√breadth. Pooling across 3,000 names buys breadth and effective observations; deeper nets on one slow series just buy overfitting.",
    },
    {
      question: "Why is universal approximation a liability rather than an asset in low-SNR return data?",
      options: [
        { text: "Unlimited expressiveness mostly fits noise; the true function is representable but not identifiable from a tiny, noisy sample", correct: true },
        { text: "The theorem is mathematically false for time series" },
        { text: "It guarantees a profitable fit if the network is wide enough" },
        { text: "It only applies to images, never to tabular finance data" },
      ],
      explanation: "Capacity to fit any function, aimed at 95–99% noise, mostly produces an exquisite fit to randomness. Representable ≠ recoverable from finite noisy data.",
    },
  ]}
/>
