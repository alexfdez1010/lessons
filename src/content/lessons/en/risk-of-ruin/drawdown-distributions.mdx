---
title: "Drawdown Distributions"
description: "Drawdown depth and duration as random variables, the distribution of maximum drawdown, time under water, and why the same system run twice gives two very different worst-cases."
topic: "en/risk-of-ruin"
order: 4
minutes: 11
updated: 2026-06-07
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, MatchConcepts, MindMap, DrawdownChart, RorDrawdownDist } from '@/components/react';

A drawdown is the gap between where your account *was* at its best and where it is *now* — the pain of having been richer. Most people quote one number, "max drawdown 30%," as if it were a fixed property of a strategy. It isn't. Run the same system again and the worst drawdown comes out different, because **drawdown is a random variable** — it has a depth distribution, a duration distribution, and a brutal cousin called *time under water*. This lesson teaches you to think in those distributions, because the single backtest number you trust is just *one draw* from them — and rarely the worst one the future holds.

<MCQ
  client:visible
  pretest
  question="Your backtest reports a maximum drawdown of 25%. What does that number actually tell you about the drawdown you'll experience trading it live?"
  options={[
    { text: 'Relatively little on its own — it is one sample from a distribution; live trading can easily produce a deeper drawdown the backtest never happened to hit', correct: true },
    { text: 'It is a hard ceiling — live drawdown can never exceed the backtested 25%' },
    { text: 'It is the exact drawdown you will experience every year' },
    { text: 'It guarantees the average drawdown will be 25%' },
  ]}
  explanation="A backtest's max drawdown is a single realization of a random variable. The same system, run over a different (but statistically identical) sequence of trades, would produce a different worst drawdown — sometimes shallower, often deeper. Treating one historical number as a guaranteed ceiling is how traders get blindsided by a drawdown 'that shouldn't have been possible.'"
/>

## Drawdown depth: how far down from the peak

**Analogy.** Picture hiking and tracking your highest altitude so far. A drawdown is how far *below* that high-water mark you currently are. You can be at a perfectly fine altitude and still be in a deep drawdown if you'd previously summited higher. The pain is relative to your best, not your start.

**Definition.** At any time $t$, the **drawdown** is the percentage fall from the running peak:
$$\text{DD}_t = \frac{\text{Peak}_t - \text{Equity}_t}{\text{Peak}_t},$$
where $\text{Peak}_t$ is the highest equity reached up to time $t$. It's 0 when you're at a new high and grows as you fall below it. The **maximum drawdown** (MDD) is the worst value $\text{DD}_t$ takes over the whole path — the deepest peak-to-trough plunge.

**Worked example.** An equity curve runs 100 → 134 (new peak) → 76 (trough) → 101. The deepest fall is from the 134 peak to the 76 trough:
$$\text{MDD} = \frac{134 - 76}{134} = \frac{58}{134} \approx 0.433 = 43.3\%.$$
Note the recovery to 101 doesn't change the max drawdown — MDD records the worst pain *along the way*, regardless of where you end up. Note also the asymmetry: recovering from that 43% drawdown back to the 134 peak requires a gain of $58/76 \approx 76\%$, far more than the 43% you lost. Deep drawdowns are arithmetically expensive to climb out of.

<DrawdownChart
  client:visible
  title="Maximum drawdown: the worst peak-to-trough fall"
  playLabel="Draw the curve"
  replayLabel="Replay"
  peakLabel="Peak"
  troughLabel="Trough"
  drawdownLabel="Max drawdown"
  caption="Maximum drawdown is the deepest peak-to-trough drop along the path — here about 43% — no matter how the curve recovers afterward. The recovery to a new level doesn't shrink the number; MDD records the worst pain you'd have felt holding through."
/>

<Callout variant="warning" title="The recovery asymmetry">
Losses and the gains needed to undo them are not symmetric. A 10% loss needs an 11% gain; a 25% loss needs 33%; a 50% loss needs 100%; a 75% loss needs 300%. This convex 'recovery wall' is why deep drawdowns are so dangerous — and why they shade into ruin. Below some depth, the gain required to recover is so large it's effectively unreachable.
</Callout>

<MCQ
  client:visible
  question="An equity curve goes 200 (peak) → 120 (trough) → 180. What is the maximum drawdown, and what gain from the trough was needed to fully recover the peak?"
  options={[
    { text: '40% drawdown; a 66.7% gain was needed to recover (120 → 200 is +80/120)', correct: true },
    { text: '40% drawdown; a 40% gain recovers it, since losses and gains are symmetric' },
    { text: '80% drawdown; the account fell by 80 points' },
    { text: '33% drawdown; measured from the trough up to the peak' },
  ]}
  explanation="MDD = (200 − 120)/200 = 80/200 = 40%. But recovering from 120 back to 200 requires +80 on a base of 120 = 66.7%. The gain to recover always exceeds the loss percentage — the recovery asymmetry — and the gap widens fast as drawdowns deepen."
/>

## Maximum drawdown is a distribution, not a number

Here's the conceptual leap. The maximum drawdown of a strategy isn't a fixed attribute — it's the outcome of a *particular sequence* of wins and losses. Shuffle the same trades into a different order, or generate a fresh statistically-identical run, and you get a different MDD. So MDD has a **distribution**.

**The shape.** The distribution of maximum drawdown is **right-skewed**: most runs cluster around a "typical" worst drawdown (the median), but there's a long tail of unlucky runs with much deeper drawdowns. The single backtest you have is one random draw — and there's no reason it landed on the bad tail. The *expected* max drawdown over many runs is usually *deeper* than the median, dragged up by that tail.

**Why it matters.** If you size your strategy so that your *backtested* 25% drawdown is just barely survivable, you're betting that the future won't draw from the tail — and over a long enough life, it will. Robust sizing plans for a drawdown well beyond the historical worst: a common rule of thumb is to assume the real max drawdown could be 1.5–2× the backtested one.

<RorDrawdownDist
  client:visible
  title="The same system, many runs — max drawdown is a distribution"
  resimulateLabel="Resimulate"
  riskLabel="Risk per trade"
  xAxisLabel="Maximum drawdown reached"
  yAxisLabel="Share of runs"
  medianLabel="Median max drawdown"
  caption="Each run is the same statistics in a different random order, so each has a different worst drawdown. The result is a right-skewed distribution: most runs cluster near the median, but a long tail of unlucky orderings reaches far deeper. Your one backtest is a single sample from this — and rarely the tail. Raise risk-per-trade and the whole distribution slides deeper."
/>

<FillBlank
  client:visible
  question="Maximum drawdown as a random variable."
  text="Maximum drawdown is {{a random variable with a distribution|a fixed property of the strategy|always equal to the average loss}}. Its distribution is {{right-skewed, with a long tail of deeper drawdowns|perfectly symmetric|always bell-shaped and narrow}}, so the drawdown you actually experience could be {{much deeper than the backtested worst|exactly the backtested worst|impossible to exceed the backtest}}. Sizing should therefore assume a drawdown {{beyond the historical worst|equal to zero|equal to the average win}}."
  explanation="MDD is a random variable; its distribution is right-skewed with a long deep tail. Your backtest is one draw, so live drawdown can exceed it — plan for worse than the historical worst (commonly 1.5–2×)."
/>

## Duration and time under water

Depth is only half the suffering. The *other* half is how *long* you're stuck below your high-water mark.

**Definitions.**
- **Drawdown duration** is the time from a peak until equity *recovers* back to that peak (the full underwater episode, start to finish).
- **Time under water (TUW)** is the total or longest stretch spent below the previous high-water mark — how long you go without setting a new equity high.

These are *also* random variables with their own distributions, and they're often more psychologically and commercially damaging than depth. A 20% drawdown that recovers in a month is forgettable; a 20% drawdown that grinds on for *three years* destroys investor patience, triggers redemptions, and breaks the trader's nerve — even though the *depth* is identical.

**Worked example.** A fund peaks in January 2021, drops 18% by October 2021, then claws back to its old high only in March 2024. The drawdown *depth* is 18%, but the *duration* is about 38 months — over three years under water. For an investor who entered at the peak, that's three years of no new highs, watching every quarterly statement show red against their entry. Many redeem long before the recovery, converting a survivable depth into a realized loss and a closed fund — a drawdown that *became* ruin through duration, not depth.

<Callout variant="tip" title="Depth gets the headlines; duration ends careers">
A deep, fast drawdown is terrifying but brief. A shallow, endless one is what actually breaks people — investors redeem, allocators cut you, and you abandon the system right before it recovers. When you evaluate a strategy, look at the time-under-water distribution as hard as the depth distribution. The longest underwater stretch is often the real survival test.
</Callout>

<MatchConcepts
  client:visible
  question="Match each drawdown concept to its precise meaning."
  pairs={[
    { term: 'Drawdown', definition: 'The percentage fall from the running peak equity' },
    { term: 'Maximum drawdown', definition: 'The deepest peak-to-trough fall over the whole path' },
    { term: 'Drawdown duration', definition: 'Time from a peak until equity recovers back to it' },
    { term: 'Time under water', definition: 'The longest stretch spent below the previous high-water mark' },
    { term: 'Recovery asymmetry', definition: 'The gain needed to undo a loss exceeds the loss itself' },
  ]}
  explanation="Drawdown = fall from running peak; max drawdown = the deepest such fall; duration = peak-to-recovery time; time under water = longest below-the-high stretch; recovery asymmetry = the convex gain-to-recover wall."
/>

<MCQ
  client:visible
  question="Two strategies both have an 18% maximum drawdown. Strategy A recovers in 2 months; Strategy B stays underwater for 3 years. Why might B be far more dangerous despite equal depth?"
  options={[
    { text: 'Long time under water drives investor redemptions and trader abandonment, so the survivable depth can turn into realized ruin before recovery', correct: true },
    { text: 'B is not more dangerous — only depth matters for risk' },
    { text: 'B must have a higher max drawdown that the backtest hid' },
    { text: 'A is more dangerous because fast drawdowns are unpredictable' },
  ]}
  explanation="Depth and duration are separate dimensions of drawdown risk. A long underwater period — even at modest depth — exhausts investor patience and the trader's conviction. Redemptions or abandonment at the bottom convert a recoverable drawdown into a realized loss and a dead strategy: ruin via duration, not depth."
/>

## Using drawdown distributions

### What to do with them
- **Size against the tail, not the median.** Assume a realized max drawdown meaningfully deeper than your backtest's — a 1.5–2× cushion is a sane default — so a tail draw is a bad month, not the end.
- **Set a drawdown budget that is also a survival limit.** Decide the depth at which you'd stop (your ruin barrier from lesson 1), and size so that hitting it is genuinely unlikely under the *distribution*, not just absent from one backtest.
- **Watch time under water for capital-stability risk.** If your money can leave (investors, your own nerve), the duration distribution may bind harder than the depth distribution.

### Pitfalls
- **Over-trusting a single backtest's MDD** — it's one sample, and survivorship bias means the strategies you're even looking at are the ones that *got lucky* on drawdown.
- **Ignoring the order dependence** — the same trades reshuffled give a different MDD, which is the whole reason Monte Carlo (next lesson but one) resamples them to map the distribution.
- **Confusing depth and duration** — a strategy can be excellent on one and lethal on the other.

<Reveal client:visible>
  <details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
    <summary className="cursor-pointer font-medium">Is there a rough formula linking drawdown to a strategy's return and volatility?</summary>

    Yes, in approximation. For a strategy modeled as a random walk with drift (per-period mean return $\mu$ and volatility $\sigma$), the *expected maximum drawdown* over a horizon grows roughly with $\sigma^2/\mu$ — meaning drawdown is dominated by the ratio of variance to drift. Double the volatility and expected max drawdown roughly *quadruples* (it scales with $\sigma^2$); double the drift and it roughly halves. This is why low-Sharpe strategies (small $\mu$ relative to $\sigma$) suffer punishing drawdowns even when they're profitable on average: the drift that pulls you back to new highs is weak relative to the noise that drags you down. It also explains the time dimension — expected *time under water* scales with $\sigma^2/\mu^2$, so a strategy with half the Sharpe spends roughly *four times* as long underwater. The practical upshot: a strategy's drawdown profile is largely a function of its Sharpe ratio, and chasing return without controlling volatility buys you deep, long drawdowns. These are approximations (real returns have fat tails and autocorrelation that worsen the tails), but the scaling intuition — drawdown grows with variance and shrinks with drift — is robust and worth carrying around.
  </details>
</Reveal>

<MCQ
  client:visible
  question="Why is sizing a strategy so that its BACKTESTED maximum drawdown is 'just survivable' a dangerous practice?"
  options={[
    { text: 'The backtested MDD is one draw from a right-skewed distribution; the live drawdown can easily land deeper in the tail and breach your limit', correct: true },
    { text: 'Backtested drawdowns are always larger than live ones, so it is overly cautious' },
    { text: 'It is fine — the backtest is a guaranteed worst case' },
    { text: 'Drawdowns never recur once they have appeared in a backtest' },
  ]}
  explanation="Maximum drawdown is right-skewed: the historical worst is just one sample, and the distribution has a long tail of deeper outcomes you simply haven't seen yet. Sizing to the backtest leaves no room for a tail draw, so a perfectly ordinary unlucky run breaches your survival limit. Size to the distribution's tail, with a cushion above the historical worst."
/>

## Putting it together

A **drawdown** is the fall from your running peak, and **maximum drawdown** is the deepest such fall over a path — recorded regardless of recovery, and arithmetically expensive to climb out of (the **recovery asymmetry**: a 50% loss needs a 100% gain). The key mental shift is that max drawdown is a **random variable**, not a fixed number: its distribution is **right-skewed**, your one backtest is a single draw rarely on the bad tail, and live drawdowns can run well past the historical worst. **Duration** and **time under water** are separate distributions that often matter more than depth — a shallow, endless drawdown ends careers and triggers redemptions, turning survivable pain into realized ruin. Size against the *tail* of these distributions (a 1.5–2× cushion over backtest), budget your drawdown as a survival limit, and remember that a strategy's whole drawdown profile is largely set by its Sharpe ratio — variance deepens and lengthens the pain, drift shortens it.

<MindMap
  client:visible
  title="Drawdown distributions — the whole picture"
  caption="Drawdown is a random variable: depth, duration, and time under water all have skewed distributions, so the backtest number is one draw — size against the tail."
  root={{
    label: 'Drawdown distributions',
    children: [
      {
        label: 'Depth',
        children: [
          { label: 'DD = (peak − equity) / peak' },
          { label: 'Max drawdown = deepest peak-to-trough' },
          { label: 'Recovery asymmetry: 50% loss needs 100% gain' },
        ],
      },
      {
        label: 'A distribution, not a number',
        children: [
          { label: 'Right-skewed with a long deep tail' },
          { label: 'Backtest MDD is one random draw' },
          { label: 'Live can exceed historical worst' },
        ],
      },
      {
        label: 'Duration & time under water',
        children: [
          { label: 'Duration = peak to recovery' },
          { label: 'TUW = longest below the high' },
          { label: 'Duration ends careers, triggers redemptions' },
        ],
      },
      {
        label: 'Using them',
        children: [
          { label: 'Size against the tail (1.5–2× cushion)' },
          { label: 'Budget drawdown as a survival limit' },
          { label: 'Profile is set by the Sharpe ratio' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: drawdown distributions"
  questions={[
    {
      question: 'An account peaks at 500, falls to 350, then recovers to 480. What is the maximum drawdown?',
      options: [
        { text: '30% — (500 − 350)/500 = 150/500, measured from the peak to the trough regardless of recovery', correct: true },
        { text: '4% — (500 − 480)/500, measured to the final value' },
        { text: '43% — (500 − 350)/350, measured from the trough' },
        { text: '70% — the account fell 150 points' },
      ],
      explanation: 'MDD = (peak − trough)/peak = (500 − 350)/500 = 150/500 = 30%. The recovery to 480 is irrelevant — max drawdown records the deepest fall along the path. Measuring from the trough (350) or to the final value (480) are both common mistakes.',
    },
    {
      question: 'Which statement about the distribution of maximum drawdown is TRUE?',
      options: [
        { text: 'It is right-skewed, so the expected (and possible) drawdown is often deeper than the median or a single backtest', correct: true },
        { text: 'It is a single fixed number determined by the strategy' },
        { text: 'It is left-skewed, so deep drawdowns are nearly impossible' },
        { text: 'It is uniform, so every drawdown depth is equally likely' },
      ],
      explanation: 'Max drawdown is a random variable with a right-skewed distribution: a cluster of typical worst-cases plus a long tail of deeper, unluckier runs. Your backtest is one draw, usually not the tail, so the realistic worst is deeper than what you have seen. Plan accordingly.',
    },
    {
      question: 'Spot the trap. "Strategy X has only a 12% max drawdown, so it is very safe." What might this miss?',
      options: [
        { text: 'It ignores duration — a shallow 12% drawdown that stays underwater for years can still trigger redemptions and abandonment, and the 12% is only one sample anyway', correct: true },
        { text: 'Nothing — a low max drawdown fully captures the risk' },
        { text: 'A 12% drawdown is mathematically impossible to recover from' },
        { text: 'Max drawdown below 15% means the strategy has no risk of ruin' },
      ],
      explanation: 'Depth is one dimension; duration is another, and the 12% figure is a single draw from a distribution. A shallow but long-lasting drawdown exhausts investor and trader patience, converting survivable pain into realized loss. And the true depth distribution has a tail beyond 12% that one backtest simply did not sample.',
    },
    {
      question: 'Why does recovering from a drawdown require a larger percentage gain than the percentage lost?',
      options: [
        { text: 'Because the gain is computed on the smaller post-loss base — e.g. a 50% loss leaves you needing +100% on the reduced capital to get back to even', correct: true },
        { text: 'Because of trading fees on the recovery trades' },
        { text: 'It does not — losses and gains are always symmetric' },
        { text: 'Because volatility increases during recoveries' },
      ],
      explanation: 'A loss shrinks the base on which the recovery gain is computed. Lose 50% and you have half left; doubling (a +100% gain) is required to return to the start. This convexity — the recovery wall — is why deep drawdowns are so dangerous and shade into ruin: below some depth the required gain is effectively unreachable.',
    },
  ]}
/>

Next up — **sequencing risk** — the most counterintuitive result in the topic: with no cashflows the *order* of your returns is irrelevant, but add withdrawals or contributions and order becomes destiny. The retiree who meets a crash in year one can run dry while an identical retiree who meets it in year fifteen sails through.
