---
title: "Monte Carlo VaR"
description: "Estimate Value at Risk by simulating thousands of synthetic tomorrows from a chosen model, fully revaluing the portfolio in each, and reading the percentile of the simulated losses."
topic: "en/value-at-risk"
order: 4
minutes: 9
updated: 2026-06-05
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, Categorize, MatchConcepts, MindMap, VarDistributionChart } from '@/components/react';

Historical VaR has a fatal flaw: it can only ever show you tomorrows that already happened. Your one year of data is your one year of data — 252 days, take it or leave it, and the crash you're really afraid of probably isn't in there. Parametric VaR fixes the data shortage by *assuming* a tidy normal bell curve, but then it lies about anything non-linear (hello, options). Monte Carlo VaR is the method that says: fine — if reality won't give me enough tomorrows, I'll **manufacture** them.

The trick is gloriously simple in spirit. Pick a statistical model for how your risk factors move. Use a random-number generator to roll *thousands* of synthetic tomorrows out of that model. Reprice your entire portfolio in each one. Now you have ten thousand fake P&Ls — sort them, walk into the loss tail, and read off the percentile. It's historical VaR, except the history is one **you** invented, and you can invent as much of it as your laptop can stand.

<MCQ
  client:visible
  pretest
  question="What is the core idea of Monte Carlo VaR?"
  options={[
    { text: 'Simulate many synthetic scenarios from a chosen model, revalue the portfolio in each, and read the loss percentile', correct: true },
    { text: 'Use only the worst day actually observed in the past year as the VaR' },
    { text: 'Assume returns are perfectly normal and read VaR off a z-table — no simulation needed' },
    { text: 'Average every analyst’s VaR guess and call the mean your number' },
  ]}
  explanation="Monte Carlo VaR generates a large set of random scenarios from a statistical model, fully reprices the portfolio under each, collects the resulting P&Ls, and takes the percentile of the simulated losses. It is 'historical VaR on synthetic data you generate yourself.'"
/>

## Simulate a thousand tomorrows

**Analogy.** A pilot doesn't wait for ten real engine fires to learn how the plane handles one — she runs the flight simulator a thousand times, each with slightly different gremlins. Monte Carlo VaR is the flight simulator for your portfolio: you can't *afford* to live through ten thousand market days, so you generate them in software and watch how your book reacts.

**Definition.** **Monte Carlo VaR** estimates Value at Risk by (1) choosing a statistical model for the risk factors that drive your portfolio, (2) drawing many random scenarios from that model, (3) revaluing the full portfolio in each scenario to get a profit-or-loss, and (4) taking the appropriate percentile of that simulated loss distribution. The name comes from the casino — it's the dice-rolling, random-sampling family of methods, applied to risk.

A **risk factor** is any market variable your portfolio's value depends on: a stock price, an FX rate, an interest rate, an implied volatility. The **model** is your assumption about how those factors move — most commonly that one-day returns are **normal** (or that prices are **lognormal**, i.e. their logs are normal, which keeps prices positive) with a chosen mean $\mu$, volatility $\sigma$, and — when you have more than one factor — a **correlation matrix** tying them together so they move realistically in lockstep rather than independently.

That correlation piece is the soul of the method for a real book. If your portfolio holds tech and oil, and your simulator lets them wander independently, you'll badly misjudge the days they crater *together*. The correlation matrix is what makes your synthetic tomorrows hang together like real market days.

<MCQ
  client:visible
  question="Why does a multi-asset Monte Carlo VaR need a correlation matrix and not just each asset’s own σ?"
  options={[
    { text: 'Because diversification and joint crashes depend on how assets move together, not just how much each moves alone', correct: true },
    { text: 'Because the random-number generator refuses to run without one' },
    { text: 'Because correlation replaces the need to know each asset’s volatility' },
    { text: 'Because it converts lognormal returns into normal ones' },
  ]}
  explanation="Portfolio risk depends on co-movement. The correlation matrix lets the simulator draw scenarios where assets fall together (or offset), capturing both diversification benefits and the joint tail losses that single-asset σ alone would miss."
/>

## The recipe, step by step

Here is the whole method as a five-step cookbook. Memorise the shape — every Monte Carlo VaR engine on earth is some elaboration of these five lines.

1. **Specify the model and its parameters.** Decide the distribution (e.g. lognormal prices), and pin down each factor's $\mu$ and $\sigma$ plus the **covariance/correlation matrix** linking them. This is the one creative, dangerous step — everything downstream inherits whatever you assume here.
2. **Generate $M$ scenarios.** Draw $M$ random scenarios — say $M = 10{,}000$ — from that model. Each scenario is one synthetic "tomorrow": a complete set of new values for every risk factor, drawn jointly so the correlations hold.
3. **Fully reprice the portfolio under each scenario.** For every one of the $M$ scenarios, recompute the portfolio's value with the new factor values, and subtract today's value to get that scenario's P&L. "Fully" is the operative word — you re-run each instrument's actual pricing formula, not a linear approximation.
4. **Sort the $M$ P&Ls** from worst to best. You now hold an empirical distribution of simulated outcomes — a histogram of ten thousand fake tomorrows.
5. **Read the percentile.** The VaR at confidence $c$ is the loss at the $(1-c)$ percentile of that sorted list. For $c = 99\%$ and $M = 10{,}000$, that's the **100th-worst** simulated loss (1% of 10,000) — every scenario below it is in the tail.

The chart below is exactly the object you build in steps 4–5: the histogram of simulated outcomes, with the VaR line sitting at the chosen percentile and the loss tail beyond it shaded. Drag the confidence slider and watch the line march deeper into the manufactured tail.

<VarDistributionChart
  client:visible
  title="The histogram of simulated outcomes"
  confidenceLabel="Confidence level"
  varLabel="VaR"
  esLabel="Tail average"
  meanLabel="Average outcome"
  lossLabel="Loss"
  caption="Ten thousand synthetic tomorrows pile up into this distribution. VaR is the percentile line; everything to its left is the simulated loss tail. Crank the confidence and the line slides further into the tail you manufactured."
/>

<FillBlank
  client:visible
  question="Fill in the Monte Carlo VaR recipe."
  text="First specify the {{model and its parameters|exact future price|day’s closing level}}, then generate {{M random scenarios|one worst case|a single average path}}, then {{fully reprice|delta-approximate|ignore}} the portfolio under each, sort the P&Ls, and read VaR as the {{(1−c) percentile|mean|maximum gain}} of the simulated losses."
  explanation="The pipeline is: model + parameters → M scenarios → full repricing in each → sort the P&Ls → take the (1−c) percentile of the losses. Full repricing (not a linear shortcut) is what lets Monte Carlo handle non-linear books correctly."
/>

## The killer feature: nonlinear portfolios

This is the reason Monte Carlo VaR exists, and the one argument that wins the meeting. **Parametric (delta-normal) VaR** approximates every position as a straight line: it takes each instrument's **delta** (its first-order sensitivity to the underlying) and assumes the P&L is just `delta × move`, with moves drawn from a normal bell. That's fine for a portfolio of plain stocks, whose payoff really *is* a straight line. It is dangerously wrong for options.

**Analogy.** Parametric VaR draws the tangent line to a curve and pretends the curve *is* the line. Near the touch point, the lie is harmless. Out in the tail — exactly where VaR lives — the curve has peeled far away from its tangent, and the line is telling you a fairy tale.

An option's payoff curves: it has **gamma**, the curvature of its value as the underlying moves (delta's own rate of change). Monte Carlo doesn't approximate — in step 3 it **fully reprices** every option at each simulated price, running the actual pricing model. So when a scenario throws a violent move, the simulated P&L reflects the option's true curved payoff, gamma and all. The tail of an optioned book — short straddles, structured notes, anything convex — comes out *right* instead of linearised into nonsense.

**Worked intuition.** You're short a put, delta $-0.5$ today, on a \$100 stock. Parametric VaR sees a 10% down-move and predicts a loss of about `0.5 × $10 = $5` per share — straight-line. But as the stock falls, the put's delta steepens toward $-1$ (that's gamma at work), so the real loss accelerates past \$5, maybe to \$8 or \$9 per share. Monte Carlo, repricing the put at the simulated \$90, *sees* that accelerating loss. Delta-normal never does — it's still riding its flat tangent line off the cliff, reporting a comfortable number while the real book bleeds.

<MCQ
  client:visible
  question="Spot the trap. A risk team runs delta-normal (parametric) VaR on a book stuffed with short options and reports a reassuringly small number. What’s the danger?"
  options={[
    { text: 'Linearising convex payoffs hides gamma, so the true tail loss on a big move is far worse than the straight-line estimate', correct: true },
    { text: 'Delta-normal always overstates risk, so the number is needlessly scary' },
    { text: 'Monte Carlo would give the identical number, so the choice never matters' },
    { text: 'Short options have no delta, so VaR is undefined and the report is blank' },
  ]}
  explanation="Delta-normal treats each option as its tangent line and assumes normal moves. Convex (gamma) payoffs peel away from that line in the tail, so the real loss on a large move dwarfs the linear estimate. Monte Carlo fully reprices each option and captures that curvature; parametric VaR confidently understates it."
/>

## Convergence and standard error

Monte Carlo gives a different answer every time you run it, because it's built on random draws. Run 10,000 paths today and 10,000 tomorrow and your VaR estimate wobbles a little between them. That wobble is **sampling error**, and the central fact about it is how it shrinks.

**Definition.** The standard error of a Monte Carlo estimate falls like $1/\sqrt{M}$, where $M$ is the number of paths. In plain terms: **to halve the error, you must quadruple the paths.** Accuracy is expensive — it improves only with the *square root* of effort, so the last digit of precision costs brutally more compute than the first.

**Analogy.** It's like polling. A poll of 10,000 people isn't twice as precise as one of 5,000 — it's only about $\sqrt 2 \approx 1.4$ times as precise. Doubling the sample barely moves the margin of error; you have to *quadruple* it to cut that margin in half. Monte Carlo paths are voters, and the VaR is the election.

**Worked example.** Suppose at $M = 10{,}000$ paths your VaR estimate has a sampling standard error of \$1.0 million. Push to $M = 40{,}000$ — a 4× increase. The error scales by $1/\sqrt{4} = 1/2$, so it drops to about \$0.5 million. Quadruple the work, halve the noise. Want to halve it *again*, down to \$0.25 million? That's another 4×, to 160,000 paths. The compute bill compounds fast.

Because brute force is so costly, quants reach for **variance-reduction** tricks — ways to get the same accuracy from fewer paths:

- **Antithetic variates.** For every random scenario you draw, also use its mirror image (flip the sign of the random shocks). The pair partly cancels each other's noise, so the average converges faster.
- **Control variates.** Lean on a related quantity whose answer you know *exactly* (say, the parametric VaR of a simplified version), and use the known error in that to correct your simulated estimate.

You don't need to implement these today — just know they exist, and that a good engine reaches a target precision with far fewer paths than the naive $1/\sqrt M$ count would demand.

<FillBlank
  client:visible
  question="Fill in the convergence rule."
  text="Monte Carlo sampling error shrinks like {{1/√M|1/M|1/M²}}, so to {{halve|quarter|double}} the error you must {{quadruple|double|halve}} the number of paths. {{Antithetic and control variates|Bigger time-steps|Lower confidence}} reach the same accuracy with fewer paths."
  explanation="Error ∝ 1/√M means a 4× path increase halves the error. Variance-reduction techniques (antithetic variates, control variates) cut the noise for a given path count, so you hit a target precision more cheaply."
/>

<MCQ
  client:visible
  question="Your Monte Carlo VaR has a sampling error of $800k at 25,000 paths. Roughly how many paths do you need to cut that error to about $400k?"
  options={[
    { text: '100,000 paths — a 4× increase halves the error', correct: true },
    { text: '50,000 paths — doubling the paths halves the error' },
    { text: '12,500 paths — fewer paths, less error' },
    { text: '26,000 paths — a tiny bump is enough' },
  ]}
  explanation="Error ∝ 1/√M, so halving it requires 4× the paths: 25,000 × 4 = 100,000. Doubling to 50,000 only cuts the error by 1/√2 ≈ 29%, not in half."
/>

## Strengths and weaknesses

Monte Carlo is the **most flexible** VaR method, full stop. You can feed it *any* distribution — fat-tailed Student-t instead of the thin-tailed normal, skewed, regime-switching, whatever fits the data. It handles **non-linearity** (options, convexity) by full repricing. It handles **path-dependence** (barrier options, anything whose payoff depends on the route, not just the endpoint) by simulating whole paths. Multiple correlated factors? Just enlarge the covariance matrix. Nothing else in the VaR toolkit is this general.

But that power comes with two heavy tolls.

The first is the one that bites in practice: **it is only as good as the model you feed it.** A normal model with too-low a $\sigma$, a stale correlation matrix, or a thin-tailed distribution doesn't produce a vague or uncertain VaR — it produces a *confidently precise wrong number*. Ten thousand beautifully-sorted scenarios drawn from a bad model give you ten thousand beautifully-sorted lies. The pretty histogram launders the bad assumption into something that *looks* rigorous.

The second is mundane but real: **it's compute-heavy.** Full repricing of a large book across tens of thousands of paths — sometimes nested, if instruments themselves need simulation to price — can mean millions of valuations per VaR run. Historical and parametric VaR are nearly free by comparison.

<Callout variant="warning" title="Garbage in, garbage out — at high resolution">
Monte Carlo's realism is a trap as much as a feature. The polished, ten-thousand-path histogram **looks** authoritative no matter what went in — but if you fed it the wrong σ, a stale correlation matrix, or a thin-tailed normal where the real returns have fat tails, every digit of that VaR is wrong with total confidence. The method never warns you; it just renders your assumption in high resolution. Stress-test the *inputs*, not only the output, and never mistake a precise number for an accurate one.
</Callout>

<MatchConcepts
  client:visible
  question="Match each Monte Carlo property to what it buys you (or costs you)."
  pairs={[
    { term: 'Any distribution (e.g. Student-t)', definition: 'Capture fat tails the normal model misses' },
    { term: 'Full repricing each scenario', definition: 'Get non-linear (option/gamma) payoffs right' },
    { term: 'Simulate whole paths', definition: 'Handle path-dependent instruments like barriers' },
    { term: 'Garbage-in, garbage-out', definition: 'A wrong model yields a confidently wrong VaR' },
    { term: 'Millions of valuations per run', definition: 'Heavy compute cost versus the other methods' },
  ]}
  explanation="Monte Carlo’s flexibility — arbitrary distributions, full repricing, full paths — is its strength. Its weaknesses are total dependence on the input model and a steep compute bill."
/>

## The three methods, side by side

You now hold all three VaR engines. Here's the cheat-sheet that decides which to reach for.

| | **Historical** | **Parametric (delta-normal)** | **Monte Carlo** |
|---|---|---|---|
| **Distribution assumption** | None — uses the actual past returns | Assumes normal returns | Any chosen model (normal, t, custom) |
| **Handles non-linearity?** | Yes (reprices on real past moves) | No — linearises via delta | Yes — full repricing each scenario |
| **Data / compute cost** | Cheap; limited by data length | Cheapest — a formula and a z-score | Most expensive — many full revaluations |
| **Fat-tail capture** | Only tails that actually occurred | Poor — thin normal tails | As good as the distribution you pick |

The quick read: **parametric** is the fast, cheap default for linear books; **historical** is the assumption-light choice when you trust your data window and your book isn't wildly optioned; **Monte Carlo** is the heavyweight you bring out for non-linear, multi-factor, fat-tailed books where getting the tail right is worth the compute.

<Categorize
  client:visible
  question="Which VaR method best fits each job?"
  buckets={['Parametric (delta-normal)', 'Historical simulation', 'Monte Carlo']}
  items={[
    { text: 'A simple long-only equity book, need a number in seconds', bucket: 'Parametric (delta-normal)' },
    { text: 'A linear FX book where speed beats tail precision', bucket: 'Parametric (delta-normal)' },
    { text: 'Trust the last year’s returns, want no distribution assumption', bucket: 'Historical simulation' },
    { text: 'Reuse real past moves, modestly non-linear book', bucket: 'Historical simulation' },
    { text: 'A book full of exotic options needing full repricing', bucket: 'Monte Carlo' },
    { text: 'Many correlated factors with fat-tailed returns to model', bucket: 'Monte Carlo' },
  ]}
  explanation="Parametric wins on speed for linear books; historical wins when you trust your data and want no distribution assumption; Monte Carlo wins for non-linear, multi-factor, fat-tailed books where full repricing matters."
/>

<Reveal client:visible>
  <details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
    <summary className="cursor-pointer font-medium">Why not always just use Monte Carlo, since it’s the most general?</summary>

    Because generality isn't free. Monte Carlo costs the most compute, demands the most modelling judgement (you must *choose* the distribution and estimate the covariance), and rewards a bad choice with a confidently wrong answer. For a plain linear book, parametric VaR gives essentially the same number in a millisecond with one formula — running 50,000 full revaluations to confirm what a z-score already told you is wasted effort. Use the heaviest tool only when the book's non-linearity, fat tails, or path-dependence actually *demand* it. The right method is the lightest one that doesn't lie about your particular portfolio.
  </details>
</Reveal>

## Putting it together

Monte Carlo VaR manufactures the tomorrows reality won't give you: pick a model, roll $M$ random scenarios, **fully reprice** the book in each, sort the P&Ls, and read the $(1-c)$ percentile of the losses. Its superpower is non-linearity — full repricing captures the gamma curvature that delta-normal linearises away — and its flexibility extends to any distribution, correlation structure, or path-dependence you can model. Its price is compute and, more insidiously, total dependence on the input model: feed it garbage and it returns a beautifully-rendered, high-confidence lie.

<MindMap
  client:visible
  title="Monte Carlo VaR — the whole engine"
  caption="Manufacture scenarios from a model, fully reprice, read the percentile — flexible and powerful, but only as honest as its inputs."
  root={{
    label: 'Monte Carlo VaR',
    children: [
      {
        label: 'The recipe',
        children: [
          { label: 'Specify model + parameters + covariance' },
          { label: 'Generate M scenarios (e.g. 10,000)' },
          { label: 'Fully reprice the portfolio in each' },
          { label: 'Sort the P&Ls' },
          { label: 'VaR = the (1−c) percentile loss' },
        ],
      },
      {
        label: 'Killer feature',
        children: [
          { label: 'Full repricing captures non-linearity' },
          { label: 'Gamma curvature gets the option tail right' },
          { label: 'Parametric linearises and understates it' },
        ],
      },
      {
        label: 'Convergence',
        children: [
          { label: 'Error shrinks like 1/√M' },
          { label: 'Quadruple paths to halve the error' },
          { label: 'Variance reduction: antithetic, control variates' },
        ],
      },
      {
        label: 'Strengths and weaknesses',
        children: [
          { label: 'Most flexible: any distribution, fat tails, paths' },
          { label: 'Garbage in, garbage out — model-dependent' },
          { label: 'Compute-heavy: many full revaluations' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: Monte Carlo VaR"
  questions={[
    {
      question: 'In the Monte Carlo recipe, what does "fully reprice the portfolio under each scenario" mean?',
      options: [
        { text: 'Re-run each instrument’s actual pricing model at the simulated factor values, not a linear approximation', correct: true },
        { text: 'Multiply today’s value by the average simulated return' },
        { text: 'Use each position’s delta and assume the P&L is linear' },
        { text: 'Take the single worst scenario and ignore the rest' },
      ],
      explanation: 'Full repricing recomputes the true value of every instrument at each scenario’s factor values, capturing non-linear payoffs. Using delta and a linear P&L is the parametric shortcut, which Monte Carlo deliberately avoids.',
    },
    {
      question: 'Why does Monte Carlo beat delta-normal VaR for a book of options?',
      options: [
        { text: 'It captures gamma (payoff curvature) by repricing each option at the simulated price, instead of linearising', correct: true },
        { text: 'It always uses more confidence than parametric VaR' },
        { text: 'Options have no delta, so parametric VaR cannot be computed' },
        { text: 'It uses fewer scenarios, so there is less noise' },
      ],
      explanation: 'Option payoffs curve (gamma). Delta-normal rides the tangent line and understates the tail; Monte Carlo reprices each option at every simulated price, so the convex loss on a big move shows up correctly.',
    },
    {
      question: 'You currently run 16,000 paths with a sampling error of $600k. Roughly how many paths halve the error to $300k?',
      options: [
        { text: '64,000 paths', correct: true },
        { text: '32,000 paths' },
        { text: '8,000 paths' },
        { text: '20,000 paths' },
      ],
      explanation: 'Error ∝ 1/√M, so halving it needs 4× the paths: 16,000 × 4 = 64,000. Doubling to 32,000 only cuts the error by about 29%.',
    },
    {
      question: 'Which statement best captures Monte Carlo VaR’s biggest weakness?',
      options: [
        { text: 'It is only as good as the model you feed it — a wrong σ or distribution yields a confidently precise but wrong VaR', correct: true },
        { text: 'It cannot handle correlated risk factors' },
        { text: 'It is forced to assume returns are normal, like parametric VaR' },
        { text: 'It can only reproduce losses that actually occurred in the past' },
      ],
      explanation: 'Garbage in, garbage out: the polished histogram looks authoritative regardless of the inputs, so a bad model produces a precise-looking but wrong number. (Handling correlations and arbitrary distributions are strengths; reusing only past losses describes historical VaR.)',
    },
    {
      question: 'A risk manager needs VaR in seconds for a plain long-only equity book and tail precision barely matters. Best method?',
      options: [
        { text: 'Parametric (delta-normal) — fast, cheap, and accurate enough for a linear book', correct: true },
        { text: 'Monte Carlo with 100,000 paths and full repricing' },
        { text: 'Monte Carlo with a fat-tailed Student-t and nested simulation' },
        { text: 'Whichever method gives the smallest number' },
      ],
      explanation: 'A linear book’s payoff really is a straight line, so delta-normal is accurate and effectively instant. Reserving Monte Carlo’s heavy compute for that job wastes effort; pick the lightest method that doesn’t lie about your portfolio.',
    },
  ]}
/>

Next up — **Expected Shortfall (CVaR)** — we confront VaR's deepest flaw: it tells you the line you rarely cross, but says *nothing* about how bad it gets once you do. A \$10M VaR is identical whether the worst 1% averages an \$11M loss or a \$100M wipeout. Expected Shortfall answers the question VaR ducks — *given* we've blown past the threshold, what's the **average** loss in that tail? — and it's exactly the shaded region you've been watching to the left of every VaR line.
