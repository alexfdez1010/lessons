---
title: "Conjugate Priors & Sequential Updating"
description: "The Beta–Binomial conjugate prior made concrete: how a belief about a strategy's win rate updates trade-by-trade in closed form, why conjugacy is so convenient, and how a prior acts like pseudo-data."
topic: "en/bayesian-finance"
order: 3
minutes: 10
updated: 2026-06-06
---

import { Callout, MCQ, Quiz, FillBlank, MatchConcepts, MindMap, BetaUpdateChart } from '@/components/react';

In the last two lessons you learned the machinery of Bayes' rule: a prior belief, a likelihood for the data, and a posterior that blends them. You also met the dirty secret of that machinery — the **denominator**. To normalise the posterior you must compute the marginal probability of the data, $P(D) = \int P(D \mid \theta)\,P(\theta)\,d\theta$, and that integral is, in general, a nightmare. For most prior-likelihood pairs you reach for numerical integration or MCMC and call it a day.

But there's a small, beautiful corner of the Bayesian world where the integral *evaporates* — where updating is so cheap you can do it in your head, trade by trade, as the data streams in. That corner is **conjugacy**, and its poster child is the question every quant secretly cares about: *what is my strategy's true win rate, and how should each new trade nudge my belief?* This lesson is about answering that in closed form, with nothing harder than addition.

<MCQ
  client:visible
  pretest
  question="You believe your strategy wins about 60% of trades. You then watch it win 7 of its next 10. In a Bayesian update, what happens to your belief about the win rate?"
  options={[
    { text: 'It shifts toward what the data suggests but stays anchored partly to your prior, and it gets more concentrated as you gather more evidence', correct: true },
    { text: 'It jumps to exactly 70% — the data overrides the prior completely' },
    { text: 'It stays at 60% — a single batch of 10 trades is too small to matter at all' },
    { text: 'It becomes perfectly certain at some value, with no remaining uncertainty' },
  ]}
  explanation="A Bayesian update produces a compromise between the prior and the data, weighted by how much evidence each carries, and the spread of your belief shrinks as data accumulates. It does not snap to the raw frequency (the prior still pulls), nor does it ignore the data, nor does it ever become perfectly certain from finite samples."
/>

## The problem: estimating a probability from a stream of wins and losses

**The setup.** You have a strategy. Each trade is, to a first approximation, a coin flip with some unknown true win probability $p$ — win with probability $p$, lose with probability $1 - p$. You want to estimate $p$, and you want to do it *as the trades happen*, updating your belief after every single one rather than waiting for a giant batch.

The naive answer is the **sample frequency**: 7 wins out of 10 means $\hat{p} = 0.70$. But after only 10 trades that estimate is wildly unstable — flip two losses to wins and it jumps to 0.90. A frequency throws away two things you actually have: any prior knowledge ("most edges are small, near 50%"), and any honest measure of *how uncertain* you still are. The Bayesian approach keeps both.

**Why the integral is the obstacle.** Each trade is a **Bernoulli** draw, and a batch of $n$ trades with $s$ wins is **Binomial**. The likelihood of seeing $s$ wins in $n$ trades is $p^{s}(1-p)^{n-s}$ (times a counting constant). To turn a prior on $p$ into a posterior, Bayes demands you divide by $\int_0^1 p^{s}(1-p)^{n-s}\,P(p)\,dp$. For an arbitrary prior that's an integral you'd rather not do by hand after every trade. Conjugacy is the trick that makes it free.

## Conjugate priors: when the posterior stays in the family

**Analogy.** Think of a prior as a *shape of clay* and the data as *hands* that press on it. For most clays, pressing produces an unpredictable new blob you have to re-measure from scratch. A **conjugate** clay is special: after you press it, it's still recognisably the *same kind of shape* — same family, just new dimensions. You don't re-derive anything; you read off the new parameters.

**Definition.** A prior is **conjugate** to a likelihood if the resulting posterior belongs to the *same distributional family* as the prior. The family is closed under Bayesian updating. When that holds, the scary normalising integral is guaranteed to work out to whatever constant keeps the known family a valid distribution — so you never compute it explicitly. You just update the parameters.

For a Bernoulli/Binomial likelihood, the conjugate prior is the **Beta distribution**. Beta priors go in, Beta posteriors come out, forever. That's the whole reason the Beta–Binomial pair is the first conjugate model everyone learns.

### The Beta distribution, defined

The **Beta distribution**, written $\text{Beta}(\alpha, \beta)$, is a probability distribution *over probabilities*: its support is the interval $[0, 1]$, exactly the range a win rate $p$ can live in. It has two positive shape parameters, $\alpha$ and $\beta$, and its mean is

$$\mathbb{E}[p] = \frac{\alpha}{\alpha + \beta}.$$

The intuition that makes everything click: **$\alpha$ behaves like a count of prior successes and $\beta$ like a count of prior failures.** More precisely $\alpha \approx (\text{prior wins}) + 1$ and $\beta \approx (\text{prior losses}) + 1$, but the "plus one" is a technicality — the mood is "alpha is wins, beta is losses." A Beta whose $\alpha$ and $\beta$ are both large is a *confident* belief (a tall, narrow peak); one where both are small is *vague* (a wide, flat smear).

The flat special case is worth memorising: **$\text{Beta}(1, 1)$ is the uniform distribution on $[0, 1]$** — every win rate from 0 to 1 equally plausible, a perfect "I know nothing" prior. Its mean is $1/(1+1) = 0.5$, dead centre.

<Callout variant="info" title="A distribution over a probability">
Beta is meta: it doesn't model a return or a price, it models your *uncertainty about a probability itself*. The x-axis is "possible values of the win rate p," and the curve's height says how plausible each one is. As data arrives, the curve marches toward the truth and sharpens — that motion *is* learning.
</Callout>

## The update rule: just add the counts

Here is the payoff, and it is almost suspiciously simple. Start with a prior $\text{Beta}(\alpha, \beta)$. Observe $s$ successes (wins) and $f$ failures (losses). The posterior is:

$$\text{Beta}(\alpha,\beta)\xrightarrow{\;s\text{ wins},\,f\text{ losses}\;}\text{Beta}(\alpha+s,\beta+f).$$

That's it. **Add the wins to $\alpha$, add the losses to $\beta$.** No integral, no normalising constant, no MCMC. The conjugacy guarantee did the calculus for you in advance, once, forever. This is why people who fit Bayesian win rates do it with a pencil while everyone else spins up a sampler.

**Sequential updating — the property that makes this magic.** Because the posterior is itself a Beta, you can feed it the *next* observation as if it were a fresh prior. Today's posterior is tomorrow's prior. Three consequences fall out for free:

- **One-at-a-time equals all-at-once.** Updating after each of 10 trades, or once with the batch of 10, lands on the *identical* posterior. Adding 1 to $\alpha$ ten times equals adding 10 once.
- **Order doesn't matter.** WLWWL or LWWLW — same final $\text{Beta}$, because addition is commutative. The data is just two running tallies: total wins, total losses.
- **It's an online algorithm.** You hold two numbers in memory and increment them as trades print. No re-reading history, no growing dataset. Perfect for a live strategy.

<Callout variant="info" title="Today's posterior is tomorrow's prior">
This is the slogan of sequential Bayesian updating. Each new trade transforms your belief, and that transformed belief is the starting point for the next trade. There is no "final" posterior — only the current one, which is always exactly the prior plus every win and loss you've seen, summed.
</Callout>

The chart below is the lesson in motion. It plots your current $\text{Beta}$ belief about the win rate, with a posterior-mean line and a 90% credible band. Click **+Win** and **+Loss** to feed it single trades and watch the curve sharpen and slide toward the data; hit **+10** to dump a batch and feel how fast a stack of evidence narrows the peak. Then play with the prior: set a flat $\text{Beta}(1,1)$ with the sliders and notice how quickly even a few trades take over, versus a strong informative prior that stubbornly resists. The whole intuition of "data eventually dominates the prior" is one slider-drag away.

<BetaUpdateChart
  client:visible
  title="Updating a win-rate belief, trade by trade"
  priorAlpha={1}
  priorBeta={1}
  winLabel="+ Win"
  lossLabel="+ Loss"
  win10Label="+10 wins"
  loss10Label="+10 losses"
  resetLabel="Reset to prior"
  priorAlphaLabel="Prior alpha"
  priorBetaLabel="Prior beta"
  meanReadoutLabel="Posterior mean"
  intervalReadoutLabel="90% credible interval"
  winsReadoutLabel="Wins observed"
  lossesReadoutLabel="Losses observed"
  posteriorLabel="Posterior belief"
  bandLabel="90% credible band"
  meanLineLabel="Posterior mean"
  densityAxisLabel="Plausibility density"
  caption="The curve is your belief about the strategy's true win rate. Each Win adds 1 to alpha; each Loss adds 1 to beta. Watch the peak sharpen as observations pile up, and the 90% credible band narrow. Start from a flat Beta(1,1) prior and a few trades shift it dramatically; start from a strong prior and the same trades barely move it — that contrast is the heart of conjugate updating."
/>

## A prior is pseudo-data

Once you internalise "$\alpha$ is wins, $\beta$ is losses," a lovely interpretation appears: **a prior is just data you pretend you already saw.**

A $\text{Beta}(8, 2)$ prior is *as if* you'd already watched 8 wins and 2 losses before the real trades began — an informative prior "worth" about 10 fake trades, centred on a mean of $8/10 = 0.8$. When real data arrives, it competes with those 10 pseudo-observations on equal footing. Ten real trades will roughly *match* the prior's weight; a hundred real trades will *swamp* it.

Contrast the flat $\text{Beta}(1, 1)$: it's worth almost nothing (a single pseudo-win and a single pseudo-loss). Real data overwhelms it almost immediately, which is exactly why a flat prior lets the sample frequency shine through fast. The strength of a Beta prior is literally measured by $\alpha + \beta$ — its **pseudo-count** or "concentration." Big sum, stubborn prior; small sum, pushover prior.

<FillBlank
  client:visible
  question="Fill in the vocabulary of conjugate Beta–Binomial updating."
  text="A prior is {{conjugate|adjacent|inverse}} to a likelihood when the {{posterior|prior|likelihood}} stays in the same distributional family. For a Bernoulli or Binomial likelihood, that family is the {{Beta|Normal|Poisson}} distribution. Starting from Beta(alpha, beta) and observing s wins and f losses, the posterior is Beta({{alpha + s, beta + f|alpha + f, beta + s|alpha times s, beta times f}}). The strength of the prior — how much real data it takes to overrule it — is measured by {{alpha + beta|alpha − beta|alpha / beta}}, because the prior acts like that many pieces of {{pseudo-data|raw price data|missing data}}."
  explanation="Conjugacy means the posterior shares the prior's family; Beta is conjugate to Bernoulli/Binomial. The update just adds counts: Beta(alpha + s, beta + f). The prior's weight is its pseudo-count alpha + beta, since a Beta(alpha, beta) behaves like alpha pseudo-wins and beta pseudo-losses already observed."
/>

## Worked example: a flat prior meets the data

Let's grind the arithmetic. You start agnostic — flat prior $\text{Beta}(1, 1)$ — and observe **7 wins and 3 losses** over 10 trades.

**The update.** Add the counts: $\alpha = 1 + 7 = 8$ and $\beta = 1 + 3 = 4$. Posterior is $\text{Beta}(8, 4)$.

**The posterior mean.**

$$\mathbb{E}[p] = \frac{\alpha}{\alpha + \beta} = \frac{8}{8 + 4} = \frac{8}{12} \approx 0.667.$$

So your best point estimate of the win rate is about **66.7%**. Compare that to the **naive frequency**, $7/10 = 0.70$. The prior pulled the estimate *down*, toward its own mean of 0.5 — by a little, because a flat prior is weak (pseudo-count just 2 against 10 real trades). The compromise sits much closer to the data than to the prior, which is exactly what a weak prior should do.

## Worked example: a skeptical prior updates less

Now suppose you're a hardened skeptic who *doubts edges exist* — you believe win rates cluster around 50%, and you're moderately confident about it. Encode that as $\text{Beta}(5, 5)$: mean $5/10 = 0.5$, with a pseudo-count of 10 (worth ten fake trades evenly split).

Feed it the **same 7 wins, 3 losses**:

$$\text{Beta}(5, 5) \xrightarrow{\;7\text{ wins},\,3\text{ losses}\;} \text{Beta}(5 + 7,\; 5 + 3) = \text{Beta}(12, 8).$$

**Posterior mean:** $12/(12+8) = 12/20 = 0.60$, i.e. **60%**.

The same data moved the skeptic only from 0.50 to 0.60 — a 10-point shift — whereas it moved the agnostic from 0.50 to 0.667, a 16.7-point shift. The skeptic's prior carried 10 pseudo-trades, so the 10 real trades had to share influence fifty-fifty; the agnostic's prior carried almost nothing, so the data ran the show. **Stronger prior, smaller update.** Here it is side by side:

| Estimator | Prior | Prior mean | After 7W / 3L | Posterior mean |
|---|---|---|---|---|
| Naive frequency | none | — | — | 7/10 = **0.700** |
| Flat (agnostic) | Beta(1, 1) | 0.500 | Beta(8, 4) | 8/12 ≈ **0.667** |
| Skeptical | Beta(5, 5) | 0.500 | Beta(12, 8) | 12/20 = **0.600** |

Same ten trades, three different conclusions — and the difference is *entirely* the prior's pseudo-weight. The naive frequency is just the limiting case of a prior with zero weight.

<MCQ
  client:visible
  question="You hold a prior of Beta(3, 7) on a coin's probability of heads, then flip it and see 12 heads and 8 tails. What is the posterior, and its mean?"
  options={[
    { text: 'Beta(15, 15), with a posterior mean of one-half (0.50)', correct: true },
    { text: 'Beta(12, 8), with a posterior mean of 0.60 — the prior is discarded' },
    { text: 'Beta(36, 56), because you multiply the counts together' },
    { text: 'Beta(15, 15), but the mean is 0.60 to match the data' },
  ]}
  explanation="Add the counts: alpha = 3 + 12 = 15, beta = 7 + 8 = 15, giving Beta(15, 15). The mean is 15/(15 + 15) = 0.50. The prior leaned toward tails (mean 0.30) and the data leaned heads (0.60); they meet exactly in the middle here because their weights happen to balance."
/>

<MCQ
  client:visible
  question="Two analysts study the same strategy with the same data. Aïsha uses a flat Beta(1, 1) prior; Ben uses a strong Beta(40, 40) prior centred on no-edge. After 12 wins and 8 losses, whose posterior mean ends up closer to the raw frequency of 0.60, and why?"
  options={[
    { text: 'Aïsha — her flat prior carries almost no pseudo-weight, so the 20 real trades dominate and pull her estimate near 0.60', correct: true },
    { text: 'Ben — a stronger prior always tracks the data more tightly' },
    { text: 'They tie exactly, because the data is identical' },
    { text: 'Neither moves at all, because 20 trades is too few to update a Beta' },
  ]}
  explanation="An informative prior resists the data in proportion to its pseudo-count (alpha + beta). Aïsha's Beta(1, 1) has pseudo-count 2, so 20 real trades swamp it and her posterior mean sits near the frequency 0.60. Ben's Beta(40, 40) has pseudo-count 80; against only 20 trades it barely budges, staying close to 0.50. Strong prior = small update."
/>

## The posterior mean is a shrinkage estimator

Step back and notice what the posterior mean *is*: a **weighted average** of the prior mean and the sample frequency. Write the prior pseudo-count as $\alpha + \beta$ and the data as $s$ wins in $n = s + f$ trades. Then

$$\underbrace{\frac{\alpha + s}{\alpha + \beta + n}}_{\text{posterior mean}} = \underbrace{\frac{\alpha + \beta}{\alpha + \beta + n}}_{\text{weight on prior}}\cdot\underbrace{\frac{\alpha}{\alpha + \beta}}_{\text{prior mean}} \;+\; \underbrace{\frac{n}{\alpha + \beta + n}}_{\text{weight on data}}\cdot\underbrace{\frac{s}{n}}_{\text{sample frequency}}.$$

The posterior mean **shrinks** the noisy sample frequency toward the prior mean, and the weights are exactly the two pseudo-counts. When $n$ is tiny the prior dominates; as $n \to \infty$ the data wins and the posterior mean converges to the truth. This is your first taste of **shrinkage** — pulling a wild estimate toward a sensible anchor to reduce its variance — which lesson 5 develops in full. The prior is, quite literally, the anchor.

And the **uncertainty** shrinks too. The posterior's 90% **credible interval** — the range that holds 90% of the belief's probability — is wide when $\alpha + \beta$ is small and narrows as trades accumulate. After 10 trades it's embarrassingly wide; after 1,000 it's a sliver. (Lesson 6 makes credible intervals precise; for now, just watch the band in the chart contract as you feed it data.)

<Callout variant="warning" title="Pitfall: a point estimate hides the uncertainty">
A posterior mean of 0.667 after 10 trades is *not* "the win rate is 66.7%." The posterior is still a fat curve — its 90% credible interval might run from roughly 0.4 to 0.85. Reporting only the mean throws away the very thing Bayes gave you: an honest measure of how little you actually know. Always carry the spread, especially with small samples.
</Callout>

## Misconceptions to avoid

- **"The prior never washes out."** False. With enough data the prior's fixed pseudo-count is overwhelmed by the growing $n$, and the posterior converges to the truth regardless of where you started (as long as the prior didn't assign zero probability to the true value). A reasonable prior is training wheels, not a permanent bias.
- **"Use the strongest prior you can to be confident."** Dangerous. A $\text{Beta}(500, 500)$ "no-edge" prior will ignore hundreds of genuine winning trades and keep insisting the strategy is a coin flip. An over-strong prior isn't rigour — it's stubbornness that refuses to learn. Match the prior's strength to how much you *actually* know.
- **"Ten trades pinned the win rate."** No. Small samples give wide posteriors. Treating a 10-trade mean as a settled fact is how people deploy capital on noise. The whole point of the Beta is that it *shows you* the remaining uncertainty — don't discard it.
- **"Order or batching changes the answer."** It doesn't. Sequential and batch updates, in any order, reach the identical posterior. If your two computations disagree, you made an arithmetic error.

<MatchConcepts
  client:visible
  question="Match each conjugate-updating term to its precise description."
  pairs={[
    { term: 'Beta(1, 1)', definition: 'The flat, uniform prior — every win rate equally plausible, mean one-half, almost no pseudo-weight' },
    { term: 'Conjugate prior', definition: 'A prior whose posterior stays in the same distributional family, so the normalising integral never has to be computed' },
    { term: 'Posterior = Beta(alpha + s, beta + f)', definition: 'The closed-form update rule: add wins to alpha and losses to beta' },
    { term: 'Pseudo-data', definition: 'The interpretation of a prior as fake wins and losses already observed, with weight alpha + beta' },
  ]}
  explanation="Beta(1, 1) is the uniform prior with negligible weight. Conjugacy keeps the posterior in the prior's family, dodging the marginal integral. The Beta–Binomial update is pure addition of counts. And a Beta prior is exactly pseudo-data: alpha pseudo-wins plus beta pseudo-losses, total weight alpha + beta."
/>

### When conjugacy is the right tool

Reach for the Beta–Binomial conjugate model when the situation fits its shape:

- **Fast online updating.** A live strategy printing trades, where you want to revise your win-rate belief instantly after each result without re-running anything heavy. Two counters, incremented — that's the entire implementation.
- **Limited data.** When you have few observations, the prior's regularising pull genuinely helps: it stabilises an estimate that the raw frequency would let swing wildly. Conjugacy makes that principled and cheap.
- **A/B-style yes/no questions.** Win-or-lose, click-or-no-click, convert-or-bounce, up-tick-or-down-tick — any binary outcome with an unknown rate is a textbook Beta–Binomial problem.

And know its limits. Conjugacy only buys you the closed form for *matching* prior-likelihood pairs, and most real questions don't reduce to Bernoulli trials. Trade **returns** are continuous and roughly bell-shaped, not wins and losses — modelling their mean calls for a different conjugate pair, the **Normal–Normal**, which is exactly where lesson 4 goes next. When no conjugate prior fits your likelihood at all, you're back to numerical methods (MCMC), the topic of the lessons after that. Conjugacy is a gift; it's just not a universal one.

## Putting it together

The Beta–Binomial conjugate model turns Bayesian win-rate estimation into arithmetic. A $\text{Beta}(\alpha, \beta)$ prior — a distribution over the probability $p$, with mean $\alpha/(\alpha+\beta)$ and $\alpha$ acting like prior wins, $\beta$ like prior losses — meets Bernoulli/Binomial data and produces a posterior in the *same* family, so the dreaded marginal integral never appears. The update is pure addition: $\text{Beta}(\alpha + s, \beta + f)$. Updates compose — today's posterior is tomorrow's prior, order-free, batch-or-stream identical — which makes it a perfect online learner. The prior is pseudo-data with weight $\alpha + \beta$: weak priors (like the flat $\text{Beta}(1,1)$) let data take over fast, strong priors resist. The posterior mean is a shrinkage estimator, a weighted average of prior mean and sample frequency, and its credible interval narrows as evidence piles up. Use it for fast, small-data, binary-outcome questions — and switch to Normal–Normal when the data is continuous returns rather than wins and losses.

<MindMap
  client:visible
  title="Conjugate priors & sequential updating — the whole picture"
  caption="Beta is conjugate to Bernoulli/Binomial, so updating a win-rate belief is just adding wins to alpha and losses to beta — composable, order-free, and interpretable as a prior made of pseudo-data."
  root={{
    label: 'Beta–Binomial conjugacy',
    children: [
      {
        label: 'The problem',
        children: [
          { label: 'Estimate a win rate p from a stream of wins/losses' },
          { label: 'Bayes needs the marginal P(D) — an integral' },
          { label: 'Naive frequency throws away prior + uncertainty' },
        ],
      },
      {
        label: 'Conjugate prior',
        children: [
          { label: 'Posterior stays in the prior\'s family' },
          { label: 'Beta is conjugate to Bernoulli/Binomial' },
          { label: 'The scary integral evaporates' },
        ],
      },
      {
        label: 'Beta(alpha, beta)',
        children: [
          { label: 'A distribution over a probability, support 0 to 1' },
          { label: 'Mean = alpha / (alpha + beta)' },
          { label: 'alpha ≈ prior wins, beta ≈ prior losses' },
          { label: 'Beta(1, 1) = flat / uniform prior' },
        ],
      },
      {
        label: 'The update rule',
        children: [
          { label: 'Beta(alpha + s, beta + f): add the counts' },
          { label: 'Today\'s posterior = tomorrow\'s prior' },
          { label: 'Order-free; batch = one-at-a-time' },
        ],
      },
      {
        label: 'Prior as pseudo-data',
        children: [
          { label: 'Weight = alpha + beta (pseudo-count)' },
          { label: 'Beta(1, 1) weak → data dominates fast' },
          { label: 'Beta(5, 5) skeptic → smaller update' },
        ],
      },
      {
        label: 'Worked numbers',
        children: [
          { label: 'Flat + 7W/3L → Beta(8, 4), mean 0.667' },
          { label: 'Skeptic + 7W/3L → Beta(12, 8), mean 0.60' },
          { label: 'Naive frequency = 0.70' },
        ],
      },
      {
        label: 'Cautions & next',
        children: [
          { label: 'Prior washes out with enough data' },
          { label: 'Posterior mean = shrinkage (lesson 5)' },
          { label: 'Returns aren\'t Bernoulli → Normal–Normal (lesson 4)' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: conjugate priors & sequential updating"
  questions={[
    {
      question: 'What does it mean for the Beta distribution to be the conjugate prior for a Binomial likelihood?',
      options: [
        { text: 'A Beta prior combined with Binomial data yields a posterior that is also a Beta — so the update never requires computing the marginal integral', correct: true },
        { text: 'The Beta and Binomial distributions are numerically identical' },
        { text: 'A Beta prior forces the posterior to equal the sample frequency exactly' },
        { text: 'It means the prior can never be updated by data' },
      ],
      explanation: 'Conjugacy means the posterior stays in the prior\'s family. Beta in, Beta out, so the normalising integral resolves to a known constant and the update reduces to adding counts — no integration needed.',
    },
    {
      question: 'You start with a Beta(2, 6) prior on a strategy\'s win rate and then observe 10 wins and 2 losses. Compute the posterior and its mean.',
      options: [
        { text: 'Beta(12, 8), mean 0.60 — add 10 to alpha and 2 to beta, then 12 / 20', correct: true },
        { text: 'Beta(10, 2), mean 0.83 — the prior is replaced by the data' },
        { text: 'Beta(20, 12), mean 0.625 — multiply the counts' },
        { text: 'Beta(12, 8), mean 0.40 — beta over the total' },
      ],
      explanation: 'Add wins to alpha and losses to beta: alpha = 2 + 10 = 12, beta = 6 + 2 = 8, giving Beta(12, 8). The mean is alpha / (alpha + beta) = 12 / 20 = 0.60. The prior leaned low (mean 0.25) and the data leaned high; the posterior compromises.',
    },
    {
      question: 'Which statement about an informative (strong) prior is correct?',
      options: [
        { text: 'It carries a large pseudo-count (alpha + beta), so a given batch of real data shifts the posterior less than a flat prior would', correct: true },
        { text: 'It makes the posterior converge to the data faster than a flat prior' },
        { text: 'It guarantees the posterior mean equals the prior mean forever, regardless of data' },
        { text: 'It has no effect unless the data set is larger than a thousand points' },
      ],
      explanation: 'A strong prior behaves like many pseudo-observations (large alpha + beta), so real data must outweigh that pseudo-count to move the estimate. The same data therefore produces a smaller update than under a weak prior — though with enough data even a strong prior eventually washes out.',
    },
    {
      question: 'Why does updating a Beta after each trade give the same result as one batch update at the end?',
      options: [
        { text: 'Because the update only adds counts, and addition is associative and commutative — order and batching never change the running totals of wins and losses', correct: true },
        { text: 'Because the Beta distribution forgets all but the most recent observation' },
        { text: 'Because each trade resets the prior back to Beta(1, 1)' },
        { text: 'It actually does not — sequential and batch updates give different posteriors' },
      ],
      explanation: 'The posterior is Beta(alpha + total wins, beta + total losses). Whether you add the wins one at a time or all at once, in any order, you reach the same two totals — so the final Beta is identical. That is what makes sequential Bayesian updating an exact online algorithm.',
    },
  ]}
/>

Next up — **The Normal–Normal conjugate model** — we leave the world of wins and losses and tackle the quantity you actually trade on: a *continuous return*. When you put a Normal prior on an unknown mean return and observe Normally-distributed data, the posterior is again Normal in closed form, and its mean turns out to be a precision-weighted blend of prior and data — the very same shrinkage idea you just met, now for the bell curve.
