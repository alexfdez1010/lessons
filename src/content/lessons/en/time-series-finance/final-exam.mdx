---
title: "Time-Series Finance — Final Exam"
description: "The graded final exam for Time-Series Finance: stationarity and unit roots, autocorrelation and the ACF/PACF, AR/MA/ARIMA models, volatility clustering and GARCH, EWMA and RiskMetrics, and backtesting pitfalls."
topic: "en/time-series-finance"
order: 7
minutes: 15
updated: 2026-06-07
---

import { Callout, FinalExam, MindMap } from '@/components/react';

This is the capstone. Six lessons built the machinery for modeling returns through time — from the gate every technique must pass to the discipline that keeps you from fooling yourself. You learned why prices are non-stationary (a unit root, shocks that never decay) while returns are roughly stationary; how the ACF and PACF read a series' memory against the white-noise band and identify a model; how AR, MA, and ARIMA turn those patterns into fitted equations for the mean; how volatility clustering breaks the constant-variance assumption and GARCH(1,1) forecasts the variance with three parameters; how EWMA is the IGARCH special case that powers RiskMetrics VaR; and how look-ahead bias, overfitting, and data-snooping make a backtest lie. No formula sheet, no hints, no take-backs: every answer locks the instant you submit, the wrong options are the exact traps that fool real desks, and your score stays hidden until the end.

<MindMap
  client:visible
  title="Time-Series Finance — the whole ladder"
  caption="From the stationarity gate to honest backtesting: six lessons, one continuous thread of modeling returns through time."
  root={{
    label: 'Time-Series Finance',
    children: [
      {
        label: 'Stationarity & returns',
        children: [
          { label: 'Constant mean, variance, lag-only autocovariance' },
          { label: 'Prices: unit root (φ=1), non-stationary' },
          { label: 'Returns ≈ I(0); ADF tests, logs & differencing fix it' },
        ],
      },
      {
        label: 'Autocorrelation, ACF & PACF',
        children: [
          { label: 'White-noise band ±2/√n' },
          { label: 'AR cuts off in PACF, MA in ACF' },
          { label: 'Ljung–Box: joint test, clean on residuals' },
        ],
      },
      {
        label: 'AR, MA & ARIMA',
        children: [
          { label: 'AR: past values; MA: past shocks' },
          { label: 'ARIMA(p,d,q): difference d times then ARMA' },
          { label: 'Choose by AIC/BIC; models the mean, not variance' },
        ],
      },
      {
        label: 'Volatility clustering & GARCH',
        children: [
          { label: 'Flat return ACF, persistent squared-return ACF' },
          { label: 'GARCH(1,1): ω + α·ε² + β·σ²' },
          { label: 'Persistence α+β; long-run ω/(1−α−β)' },
        ],
      },
      {
        label: 'EWMA & RiskMetrics',
        children: [
          { label: 'σ²=λσ²+(1−λ)r²; half-life ln0.5/lnλ' },
          { label: 'IGARCH: ω=0, α+β=1, no mean reversion' },
          { label: 'λ=0.94 daily; VaR ≈ 2.33·σ·position' },
        ],
      },
      {
        label: 'Backtesting pitfalls',
        children: [
          { label: 'Look-ahead bias: one-bar-lag rule' },
          { label: 'Overfitting: in-sample up, out-of-sample falls' },
          { label: 'Data-snooping: 1 − 0.95^N; walk-forward' },
        ],
      },
    ],
  }}
/>

<Callout variant="warning" title="How this exam works">
This is a graded exam. Questions arrive **one at a time**. **Once you submit an answer it is final** — there is no going back, no second try, and a wrong answer simply fails that question. Your score stays hidden until the very end, where you need **70%** to pass. Read every option before you commit.
</Callout>

<FinalExam
  client:visible
  passPercent={70}
  questions={[
    {
      question: "What does it mean for a time series to be (weakly) stationary?",
      options: [
        { text: "Its mean and variance are constant over time and its autocovariance depends only on the lag, not on when you look", correct: true },
        { text: "Its values never change from one period to the next" },
        { text: "It always trends steadily upward with a fixed slope" },
        { text: "It has exactly zero autocorrelation at every lag" },
      ],
      explanation: "Weak (covariance) stationarity requires a constant mean, a constant variance, and an autocovariance that depends only on the lag k. A stationary series can have plenty of autocorrelation; what it cannot have is a drifting mean or a time-growing variance.",
    },
    {
      question: "Why are raw stock PRICES typically non-stationary while RETURNS are approximately stationary?",
      options: [
        { text: "Prices carry a unit root — no fixed mean and a variance that grows without bound — whereas differencing into returns removes the trend, leaving a roughly stable mean and spread", correct: true },
        { text: "Returns are always larger numbers, which makes them stationary" },
        { text: "Prices are perfectly stationary and returns are the non-stationary ones" },
        { text: "Stationarity has nothing to do with prices or returns" },
      ],
      explanation: "A price behaves like a unit-root (random-walk) process: its level drifts and its variance grows like t. Taking the (log) return is differencing the series once, which strips the trend and yields an approximately stationary I(0) series — exactly what models require.",
    },
    {
      question: "In an AR(1) process X_t = φ·X_{t−1} + ε_t, which value of φ corresponds to a unit root (a non-stationary random walk)?",
      options: [
        { text: "φ = 1 — shocks never decay; they accumulate permanently and the variance grows without bound", correct: true },
        { text: "φ = 0 — the series is pure white noise" },
        { text: "φ = 0.5 — the standard stationary case" },
        { text: "φ = −1 — the series is perfectly stable" },
      ],
      explanation: "φ = 1 makes X_t = X_{t−1} + ε_t, a random walk in which each shock is permanent and the variance grows like t. |φ| < 1 is stationary and mean-reverting; |φ| > 1 is explosive. The unit root is precisely φ = 1.",
    },
    {
      question: "The augmented Dickey–Fuller (ADF) test has which null hypothesis, and how do you reject it?",
      options: [
        { text: "Null: the series has a unit root (non-stationary); reject it when the test statistic is sufficiently NEGATIVE, concluding stationarity", correct: true },
        { text: "Null: the series is stationary; reject it when the statistic is positive" },
        { text: "Null: the series has zero mean; reject it when the mean is large" },
        { text: "Null: the series is normally distributed; reject on heavy tails" },
      ],
      explanation: "ADF regresses ΔX_t on the lagged level; its null is a unit root (γ = φ − 1 = 0). A strongly negative statistic rejects the null in favor of stationarity. Note its low power — failing to reject is not proof of a unit root, so corroborate with KPSS and a plot.",
    },
    {
      question: "A log price is non-stationary but becomes stationary after one difference. How is it classified, and what is that difference called?",
      options: [
        { text: "Integrated of order one, I(1); its first difference is the log return", correct: true },
        { text: "I(0); its difference is the price level" },
        { text: "I(2); its difference is the variance" },
        { text: "It cannot be made stationary by differencing" },
      ],
      explanation: "The order of integration is the number of differences needed for stationarity. A log price needs one (I(1)); differencing it once gives the log return, which is the approximately stationary I(0) series models are built on.",
    },
    {
      question: "For a sample of n return observations, the approximate 95% white-noise confidence band for a sample autocorrelation is...",
      options: [
        { text: "±2/√n — bars inside it are indistinguishable from zero; bars outside are statistically significant", correct: true },
        { text: "±0.05 regardless of sample size" },
        { text: "±2n, growing with the sample" },
        { text: "±1, since correlations range from −1 to 1" },
      ],
      explanation: "Under the white-noise null each sample autocorrelation is roughly Normal with standard error 1/√n, so the 95% band is about ±2/√n. More data tightens the band, making smaller true effects detectable; a value inside the band is consistent with white noise.",
    },
    {
      question: "A return series has n = 400 and a lag-1 sample autocorrelation of 0.07. Is it statistically significant at 95%?",
      options: [
        { text: "No — the band is ±2/√400 = ±0.10, and 0.07 lies inside it, so it is indistinguishable from white-noise sampling error", correct: true },
        { text: "Yes — any nonzero autocorrelation is significant" },
        { text: "Yes — 0.07 exceeds the 0.05 universal cutoff" },
        { text: "Cannot be determined without the mean" },
      ],
      explanation: "The 95% band for n = 400 is ±2/√400 = ±0.10. A sample autocorrelation of 0.07 sits inside the band, so it is not distinguishable from zero. Significance depends on sample size, not a fixed 0.05 threshold on the correlation itself.",
    },
    {
      question: "What does the partial autocorrelation function (PACF) measure that the ordinary ACF does not?",
      options: [
        { text: "The DIRECT correlation at lag k after removing the influence of all intermediate lags, isolating genuine dependence from effects that leak through the chain", correct: true },
        { text: "The correlation with future values rather than past ones" },
        { text: "The variance of the series at each lag" },
        { text: "Nothing different — the ACF and PACF are identical" },
      ],
      explanation: "The ACF reports total correlation at each lag, including indirect effects transmitted through intermediate lags. The PACF strips those out to show the direct relationship at lag k — which is why an AR(p) cuts off in the PACF at lag p but only decays in the ACF.",
    },
    {
      question: "A stationary series has an ACF that decays gradually and a PACF with two significant spikes (lags 1 and 2) then nothing. Which model does this point to?",
      options: [
        { text: "An AR(2) — the PACF cutoff at lag 2 gives the AR order; the ACF of an AR process only decays", correct: true },
        { text: "An MA(2) — because the PACF cuts off" },
        { text: "White noise — because both plots show some structure" },
        { text: "An MA(1) — because the ACF decays" },
      ],
      explanation: "AR order is read from the PACF cutoff: two significant PACF spikes then silence → AR(2), with an ACF that decays gradually. An MA(q) is the mirror image — ACF cuts off at q, PACF decays.",
    },
    {
      question: "You fit a model and run the Ljung–Box test on its RESIDUALS, getting a p-value of 0.45. Is this good, and why?",
      options: [
        { text: "Yes — you fail to reject white noise, so no significant autocorrelation remains in the residuals, which is exactly what a well-fitted model should produce", correct: true },
        { text: "No — a high p-value means the model is badly mis-specified" },
        { text: "No — it proves the model has overfit" },
        { text: "Ljung–Box cannot be applied to residuals" },
      ],
      explanation: "On residuals, a LARGE p-value (0.45 > 0.05) is the success condition: you fail to reject white noise, so the model extracted the autocorrelation. (On the raw series the interpretation flips — a small p-value flags structure worth modeling.)",
    },
    {
      question: "What is the essential difference between an AR(p) and an MA(q) model?",
      options: [
        { text: "AR expresses today via past VALUES (shock memory decays geometrically, never fully gone); MA expresses it via past SHOCKS (memory is finite, exactly q steps)", correct: true },
        { text: "AR is only for non-stationary data and MA only for stationary data" },
        { text: "AR has finite memory and MA has infinite memory" },
        { text: "They are identical models with different names" },
      ],
      explanation: "AR depends on lagged values, so a shock propagates through the recursion and decays geometrically forever. MA depends on lagged shocks directly, so a shock affects exactly the next q observations and then vanishes. This is why AR cuts off in the PACF and MA in the ACF.",
    },
    {
      question: "An AR(1) model is X_t = 6 + 0.4·X_{t−1} + ε_t. What is its long-run (unconditional) mean?",
      options: [
        { text: "10 — solve μ = 6 + 0.4μ, giving μ = 6/(1 − 0.4) = 10", correct: true },
        { text: "6 — the constant term is the mean" },
        { text: "2.4 — the constant times the coefficient" },
        { text: "0 — AR processes always revert to zero" },
      ],
      explanation: "The unconditional mean of a stationary AR(1) solves μ = c + φμ, so μ = c/(1 − φ) = 6/0.6 = 10. Forecasts decay geometrically toward this value from any starting point.",
    },
    {
      question: "What does the ARIMA specification (0,1,0) describe?",
      options: [
        { text: "A random walk — difference once and the change is pure white noise; the canonical model for a price level", correct: true },
        { text: "An AR(1) with one moving-average term" },
        { text: "White noise with no differencing" },
        { text: "A deterministic linear trend with no randomness" },
      ],
      explanation: "(0,1,0) means p = 0, d = 1, q = 0: difference once, and the differenced series is pure noise — that is a random walk, X_t = X_{t−1} + ε_t, the standard non-stationary price model.",
    },
    {
      question: "You compare three ARIMA models by BIC: A = 980, B = 962, C = 991. Which do you pick and why?",
      options: [
        { text: "B — BIC rewards fit while penalizing parameter count, and LOWER is better, so 962 is the best trade-off; then confirm its residuals pass Ljung–Box", correct: true },
        { text: "C — the highest BIC indicates the best fit" },
        { text: "A — always pick the most parameters" },
        { text: "It does not matter; BIC is not comparable across models" },
      ],
      explanation: "BIC = −2ln(likelihood) + ln(n)·k, and lower is better — it balances goodness-of-fit against a complexity penalty. B's 962 is lowest, so it's preferred. Final step: validate that the chosen model's residuals show no leftover autocorrelation (Ljung–Box).",
    },
    {
      question: "What is volatility clustering, and how does it appear in the autocorrelation plots?",
      options: [
        { text: "Large moves follow large moves and calm follows calm; the level-return ACF is roughly flat but the SQUARED-return ACF is strongly positive and slow to decay", correct: true },
        { text: "Returns trend steadily upward, shown by positive ACF at every lag" },
        { text: "Volatility is constant, shown by a flat squared-return ACF" },
        { text: "Direction is perfectly forecastable, shown by a large lag-1 return autocorrelation" },
      ],
      explanation: "Volatility clustering = turbulent and calm periods bunching together. Direction stays hard to forecast (flat return ACF) while magnitude is persistent (large, slow-decaying squared-return ACF). That persistent squared-return structure is exactly what GARCH models.",
    },
    {
      question: "In GARCH(1,1), σ²_t = ω + α·ε²_{t−1} + β·σ²_{t−1}, what does the β term capture?",
      options: [
        { text: "The persistence of yesterday's variance — how much elevated volatility carries over to today (the memory term)", correct: true },
        { text: "The reaction to the latest squared shock (the news term)" },
        { text: "The constant long-run baseline variance" },
        { text: "The mean return of the series" },
      ],
      explanation: "β multiplies the previous conditional variance σ²_{t−1}, capturing how persistently elevated volatility carries forward — the memory term. α multiplies the last squared shock (the news/reaction term); ω is the constant baseline.",
    },
    {
      question: "A fitted GARCH(1,1) has ω = 0.00002, α = 0.06, β = 0.90. What are its persistence and long-run variance?",
      options: [
        { text: "Persistence = 0.96; long-run variance = 0.00002/(1 − 0.96) = 0.0005", correct: true },
        { text: "Persistence = 0.96; long-run variance = 0.00002 × 0.96" },
        { text: "Persistence = 0.84; long-run variance = 0.00002/0.84" },
        { text: "Persistence = 1.06; long-run variance = 0.00002" },
      ],
      explanation: "Persistence = α + β = 0.06 + 0.90 = 0.96. The unconditional variance = ω/(1 − α − β) = 0.00002/0.04 = 0.0005. Because 0.96 < 1, the variance is mean-reverting and this long-run level is well defined.",
    },
    {
      question: "A GARCH(1,1) comes back from estimation with α + β = 1.03. What should you conclude?",
      options: [
        { text: "The model is non-stationary/explosive and its long-run variance is undefined — likely a mis-estimation or structural break; do not trust its long-run forecasts", correct: true },
        { text: "It is the ideal model, since high persistence means accuracy" },
        { text: "It is exactly EWMA, which requires α + β = 1.03" },
        { text: "Nothing — persistence above 1 is normal and stable" },
      ],
      explanation: "Stationary GARCH needs α + β < 1. At exactly 1 you have IGARCH (EWMA); above 1 the variance is explosive and ω/(1 − α − β) divides by a negative number — meaningless. A value of 1.03 signals mis-estimation or a regime break.",
    },
    {
      question: "EWMA variance is σ²_t = λ·σ²_{t−1} + (1 − λ)·r²_{t−1}. To which GARCH(1,1) is it equivalent, and what follows?",
      options: [
        { text: "GARCH with ω = 0 and α + β = 1 (IGARCH) — so EWMA has no long-run variance and no mean reversion; its multi-step forecast is flat", correct: true },
        { text: "GARCH with ω = 1 and α + β = 0 — a constant forecast" },
        { text: "GARCH with α = β = 0 — it ignores all data" },
        { text: "It has no relationship to GARCH" },
      ],
      explanation: "Matching the equations gives ω = 0, α = 1 − λ, β = λ, so α + β = 1 — the IGARCH case. With persistence exactly 1 there is no mean reversion and the long-run variance is undefined, so EWMA forecasts a flat line at the current estimate.",
    },
    {
      question: "Why does an equal-weighted rolling-window volatility show a 'ghosting' jump that EWMA avoids?",
      options: [
        { text: "When a large past return drops out of the fixed window, the equal-weighted estimate lurches with no new information; EWMA's geometrically decaying weights have no hard cutoff, so old shocks fade smoothly", correct: true },
        { text: "EWMA uses no past data, so it cannot ghost" },
        { text: "Rolling windows are always more accurate, so there is no ghosting" },
        { text: "Ghosting happens only in GARCH, never in rolling windows" },
      ],
      explanation: "An equal-weighted window gives every in-window day the same weight and zero outside; the day a big old return exits, the estimate jumps even though nothing happened — the ghosting artifact. EWMA decays weights smoothly with no cutoff, so influence fades gradually.",
    },
    {
      question: "RiskMetrics popularized EWMA for Value-at-Risk with which fixed default decay factor for DAILY data?",
      options: [
        { text: "λ = 0.94 (with 0.97 for monthly data) — a single fixed constant requiring no per-asset estimation", correct: true },
        { text: "λ = 0.50 for daily data" },
        { text: "λ = 0.99 for daily data" },
        { text: "λ = 0.06 for daily data" },
      ],
      explanation: "RiskMetrics fixed λ = 0.94 for daily and 0.97 for monthly data — not fitted per asset but chosen as universal constants, so a single, reproducible, no-estimation recipe could compute VaR consistently across thousands of instruments.",
    },
    {
      question: "A backtest computes a buy signal from today's closing price but records the trade at today's open. What error is this?",
      options: [
        { text: "Look-ahead bias — it uses the close (known only at end of day) to trade at the earlier open; fix it by trading at the next price realized after the signal is fully known", correct: true },
        { text: "Overfitting — fix it by adding parameters" },
        { text: "Data-snooping — fix it by testing more strategies" },
        { text: "No error — this is standard practice" },
      ],
      explanation: "The signal depends on the close, unavailable until end of day, yet the trade is booked at the earlier open — future information leaking in. That is look-ahead bias; the one-bar-lag rule (transact only at prices realized after the signal is observable) fixes it.",
    },
    {
      question: "Across model complexity, in-sample backtest performance keeps rising while out-of-sample performance peaks then falls. What is this the hallmark of?",
      options: [
        { text: "Overfitting — the extra complexity fits non-repeating noise, boosting the backtest but hurting live performance; the widening in-sample/out-of-sample gap is the warning", correct: true },
        { text: "Genuine improvement — always add maximum complexity" },
        { text: "A data error, since the two can never differ" },
        { text: "Look-ahead bias, which only affects out-of-sample results" },
      ],
      explanation: "Rising in-sample with peaking-then-falling out-of-sample is the textbook overfitting signature: past the sweet spot, added complexity captures noise that won't recur. The growing gap between the curves is the diagnostic, and parsimony is the cure.",
    },
    {
      question: "A researcher tests 100 strategy variants on the same data and presents only the best. Roughly what is the chance that at least one worthless variant looked 'significant' at the 5% level by luck?",
      options: [
        { text: "About 99% — 1 − 0.95^100 ≈ 0.994, so a spurious winner is nearly guaranteed; the number of trials must be disclosed and the Sharpe deflated", correct: true },
        { text: "Exactly 5% — the per-test rate, regardless of how many were tried" },
        { text: "0% — testing more strategies removes false positives" },
        { text: "50% — a coin flip independent of the number of trials" },
      ],
      explanation: "This is data-snooping. With 100 independent useless strategies the probability of at least one false positive is 1 − 0.95^100 ≈ 0.994. Reporting only the winner without the trial count manufactures a fake discovery; defenses are deflated Sharpe, Bonferroni, and pre-registration.",
    },
    {
      question: "Why is walk-forward analysis generally preferred over a single fixed in-sample/out-of-sample split?",
      options: [
        { text: "It rolls train/test windows through time, mimicking live trading and testing the strategy across many regimes, stitching multiple out-of-sample periods into a more honest record", correct: true },
        { text: "It uses the whole dataset for training to maximize fit" },
        { text: "It removes the need for any out-of-sample data" },
        { text: "It guarantees future profitability" },
      ],
      explanation: "Walk-forward repeatedly trains on a past window and tests on the following unseen period, sliding forward. This always predicts the future from the past (no look-ahead), spans multiple regimes (not one lucky era), and produces a far more honest stitched out-of-sample estimate.",
    },
    {
      question: "A team gets a weak result on their out-of-sample holdout, tweaks the strategy, re-tests on the same holdout, and repeats until it looks good. What have they done?",
      options: [
        { text: "Contaminated the holdout — repeatedly testing and tweaking against it turns it into in-sample data and re-introduces data-snooping; the result is no longer honest", correct: true },
        { text: "Performed correct walk-forward analysis" },
        { text: "Eliminated look-ahead bias by reusing the holdout" },
        { text: "Nothing wrong — reuse makes the estimate more robust" },
      ],
      explanation: "A holdout is valid only if judged ONCE. Peeking, tweaking, and re-testing leaks holdout information back into the design, effectively making it training data — data-snooping again. An honest holdout is touched exactly once, after the strategy is frozen.",
    },
    {
      question: "A colleague claims that because a return series has a nearly flat ACF, 'there is no time-series structure left to model.' What is the oversight?",
      options: [
        { text: "A flat return ACF only rules out forecastable direction; the squared returns can still be strongly autocorrelated (volatility clustering), which is highly forecastable and drives risk and option pricing", correct: true },
        { text: "Nothing — a flat return ACF means nothing is left to model" },
        { text: "They should use the price ACF, which is always flat" },
        { text: "Flat ACFs are impossible for real financial data" },
      ],
      explanation: "Returns often have a near-white-noise level ACF (hard to forecast direction) while their squared/absolute returns are strongly autocorrelated — volatility clustering. That structure powers GARCH, VaR, and options pricing. A flat return ACF redirects you from the mean to the variance, not to giving up.",
    },
  ]}
/>

<Callout variant="tip" title="Passed? Here's what you now own">
You can take a raw price series and do the whole pipeline a quant does: test it for a unit root, difference it into stationary returns, read the ACF and PACF to identify a model, fit and validate an ARIMA for the mean, layer a GARCH or EWMA on top for the time-varying variance and VaR, and — most importantly — evaluate the result with enough suspicion that look-ahead bias, overfitting, and data-snooping don't fool you. You know which tool fits which job, and why a beautiful backtest is guilty until proven innocent.
</Callout>

That's the time-series toolkit, end to end — the statistical language of returns over time, and the discipline to use it without deceiving yourself. You now own both the models and the judgment to know when to trust them.
