---
title: "Volatility Clustering & GARCH"
description: "Volatility clustering in returns, the ARCH insight, the GARCH(1,1) model and its three parameters, persistence (α + β), the unconditional variance, and forecasting future variance."
topic: "en/time-series-finance"
order: 4
minutes: 12
updated: 2026-06-07
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, MatchConcepts, MindMap, TsGarchVolPath } from '@/components/react';

ARIMA modeled the *mean* of returns and assumed the variance was constant. That assumption is spectacularly false. Stare at any return chart and you'll see it: stretches of placid little wiggles, then bursts of violent swings, then calm again. Big moves cluster with big moves; quiet clusters with quiet. This is **volatility clustering**, and it's one of the most robust empirical facts in all of finance — and one the ARCH/GARCH family was built to capture. Modeling it is how you forecast tomorrow's *risk*, price options, and size positions. If ARIMA is the grammar of the mean, GARCH is the grammar of the variance.

<MCQ
  client:visible
  pretest
  question="A return series has a nearly flat ACF (you can't forecast direction), yet its risk is clearly NOT constant — turbulent weeks bunch together. Which statement best captures this?"
  options={[
    { text: 'Returns can be unforecastable in DIRECTION while their VARIANCE is highly forecastable — volatility clustering means a big move today raises the odds of a big move tomorrow', correct: true },
    { text: 'A flat ACF proves the variance must also be constant' },
    { text: 'It is impossible for variance to change if direction is unpredictable' },
    { text: 'The clustering is an illusion; returns always have constant variance' },
  ]}
  explanation="Direction (the mean) and magnitude (the variance) are separate. Returns can have near-zero autocorrelation in levels yet strong autocorrelation in SQUARED returns — that's volatility clustering. The size of moves is forecastable even when their sign isn't, which is exactly what GARCH exploits."
/>

## Volatility clustering: the stylized fact

**Analogy.** Volatility behaves like weather, not climate. You can't predict whether tomorrow brings rain or sun (the *direction*), but you absolutely can predict that a storm season is upon you — turbulent days come in runs, and calm days come in runs. Markets are the same: a panicky day is usually followed by more panicky days, and a sleepy day by more sleepy days.

**The evidence.** Recall from the autocorrelation lesson the tell-tale signature: the ACF of **returns** is nearly flat (direction unforecastable), but the ACF of **squared returns** (or absolute returns) is strongly positive and decays *slowly* over many lags. That slow decay in squared returns is the fingerprint of volatility clustering — today's squared return helps predict tomorrow's.

**Why it matters.** A risk model that assumes constant variance will badly *understate* risk in turbulent regimes (and overstate it in calm ones). During a crisis, "average" volatility is useless; you need the *current, elevated* volatility. Volatility clustering means recent volatility is your best guide to near-term volatility — and that's forecastable structure worth real money in risk management and options pricing.

<TsGarchVolPath
  client:visible
  title="Volatility clustering in action"
  returnsLabel="Returns"
  volLabel="Conditional volatility (σₜ)"
  persistenceLabel="Persistence (α + β)"
  resimulateLabel="Resimulate"
  caption="Each return is a fresh standardized shock scaled by the current conditional volatility. Push persistence (α + β) toward 1 and a big move inflates tomorrow's volatility, which inflates the next — calm and storm visibly bunch into runs. Drop persistence and the clustering dissolves into uniform noise."
/>

## ARCH: variance that depends on recent shocks

The breakthrough (Robert Engle, 1982, eventually a Nobel) was simple and radical: let the variance *itself* be a function of recent data. The **ARCH** model — AutoRegressive Conditional Heteroskedasticity — says today's variance depends on the size of recent shocks.

**Unpacking the name:** *Heteroskedasticity* = non-constant variance. *Conditional* = given the recent past. *Autoregressive* = the variance feeds on its own recent history. Put together: the variance, conditional on recent returns, varies over time and depends on its own recent inputs.

**Definition.** An **ARCH(1)** model sets the conditional variance to a baseline plus a piece of the last squared shock:
$$\sigma_t^2 = \omega + \alpha\,\varepsilon_{t-1}^2,$$
where $\varepsilon_{t-1}$ is the previous return shock, $\omega > 0$ is a baseline, and $\alpha \geq 0$. The mechanism: a big move last period ($\varepsilon_{t-1}^2$ large) pushes up this period's variance — clustering, built right in.

**The limitation.** Pure ARCH needs *many* lagged squared terms (ARCH(q) with large q) to capture the slow, persistent decay real volatility shows. That's clunky and over-parameterized — which is exactly the problem GARCH solves.

<FillBlank
  client:visible
  question="The ARCH idea."
  text="ARCH stands for AutoRegressive Conditional {{Heteroskedasticity|Homoskedasticity|Heteroscope}}, meaning the variance is {{non-constant and depends on the recent past|fixed forever|always zero}}. In ARCH(1), today's variance rises whenever the {{previous squared shock|previous mean|next forecast}} is large — which builds volatility clustering directly into the variance equation."
  explanation="Heteroskedasticity = non-constant variance; 'conditional' = given the recent past. ARCH(1) makes σ²_t = ω + α·ε²_{t−1}, so a large previous shock inflates today's variance. The weakness is that pure ARCH needs many lags to capture slow-decaying real volatility — motivating GARCH."
/>

## GARCH(1,1): the workhorse

The fix — Tim Bollerslev, 1986 — was to add a term that lets variance feed on its *own previous value*, not just past shocks. That's **GARCH** (Generalized ARCH), and the version that dominates practice is **GARCH(1,1)**.

**Definition.**
$$\sigma_t^2 = \omega + \alpha\,\varepsilon_{t-1}^2 + \beta\,\sigma_{t-1}^2.$$
Three forces set tomorrow's variance:

- **$\omega$** — a constant baseline (the long-run anchor).
- **$\alpha\,\varepsilon_{t-1}^2$** — the **reaction** to the *last shock*: how much a fresh surprise jolts volatility (the "news" term).
- **$\beta\,\sigma_{t-1}^2$** — the **persistence** of *yesterday's variance*: how much elevated volatility carries over (the "memory" term).

This recursion is enormously efficient. Because $\sigma_{t-1}^2$ itself depends on $\sigma_{t-2}^2$, and so on, GARCH(1,1) implicitly contains an *infinite* weighted sum of all past squared shocks — capturing the slow decay that pure ARCH needed dozens of terms for, with just three parameters. That parsimony is why GARCH(1,1) became the default.

**Worked example — one update.** Suppose $\omega = 0.00001$, $\alpha = 0.08$, $\beta = 0.90$. Yesterday's variance was $\sigma_{t-1}^2 = 0.0004$ (a daily vol of $\sqrt{0.0004} = 0.02 = 2\%$), and yesterday's return shock was a large $\varepsilon_{t-1} = -0.03$ (a $-3\%$ day, so $\varepsilon_{t-1}^2 = 0.0009$). Then:
$$\sigma_t^2 = 0.00001 + 0.08(0.0009) + 0.90(0.0004) = 0.00001 + 0.000072 + 0.00036 = 0.000442.$$
Today's vol forecast is $\sqrt{0.000442} \approx 0.021 = 2.1\%$ — the big move bumped expected volatility up. A *calm* day ($\varepsilon_{t-1} = 0$) would instead give $\sigma_t^2 = 0.00001 + 0 + 0.00036 = 0.00037$, vol $\approx 1.92\%$ — volatility drifting back down. That up-on-shock, decay-on-calm behavior *is* clustering.

<MatchConcepts
  client:visible
  question="Match each GARCH(1,1) term to its role."
  pairs={[
    { term: 'ω (omega)', definition: 'The constant baseline that anchors the long-run variance level' },
    { term: 'α (alpha)', definition: 'The reaction to the last squared shock — how hard fresh news jolts volatility' },
    { term: 'β (beta)', definition: 'The persistence of yesterday\'s variance — how much elevated volatility carries over' },
    { term: 'α + β', definition: 'Total persistence — how slowly a volatility shock decays; near 1 means very sticky' },
    { term: 'Conditional variance', definition: 'The variance given the recent past, σ²_t, which GARCH lets vary over time' },
  ]}
  explanation="ω anchors the long-run level; α is the news/reaction term on the last squared shock; β is the memory/persistence term on the last variance. Their sum α+β is total persistence (closer to 1 = slower decay). The conditional variance is the time-varying σ²_t GARCH forecasts."
/>

## Persistence and the long-run variance

The single most important quantity in a fitted GARCH is the sum $\alpha + \beta$ — the **persistence**.

**What it controls.** $\alpha + \beta$ measures how slowly a volatility shock dies out. For typical daily equity data it's often around $0.95$–$0.99$ — *very* persistent: a volatility spike takes weeks to fade. The closer to 1, the longer turbulence lingers.

**The unconditional (long-run) variance.** Provided $\alpha + \beta < 1$, the variance is *mean-reverting* and settles toward a long-run level:
$$\bar{\sigma}^2 = \frac{\omega}{1 - \alpha - \beta}.$$
This is the "climate" volatility the process reverts to between storms. Notice the parallel with the AR(1) long-run mean $c/(1-\phi)$ — same structure, applied to variance.

**Worked example — the long-run anchor.** With $\omega = 0.00001$, $\alpha = 0.08$, $\beta = 0.90$: persistence $= 0.98$, and
$$\bar{\sigma}^2 = \frac{0.00001}{1 - 0.98} = \frac{0.00001}{0.02} = 0.0005,$$
so long-run daily vol $= \sqrt{0.0005} \approx 0.0224 = 2.24\%$ (annualized, $\times\sqrt{252} \approx 35.5\%$). Today's $2.1\%$ forecast sits just below this anchor and will drift back toward it as calm returns.

<Callout variant="warning" title="When α + β ≥ 1: the variance stops reverting">
If the estimated $\alpha + \beta = 1$ exactly, you have **IGARCH** (integrated GARCH): shocks to variance are *permanent* — the unconditional variance is undefined (the formula divides by zero) and forecasts don't revert to any level. EWMA, the next lesson's topic, is exactly this $\alpha + \beta = 1$ special case with $\omega = 0$. And if $\alpha + \beta > 1$ the model is non-stationary and explosive — usually a sign of mis-estimation or a structural break in your data. Always check that the persistence is below 1 before trusting the long-run variance.
</Callout>

<MCQ
  client:visible
  question="A fitted GARCH(1,1) has ω = 0.000004, α = 0.10, β = 0.88. What is its persistence and long-run (unconditional) variance?"
  options={[
    { text: 'Persistence = 0.98; long-run variance = 0.000004 / (1 − 0.98) = 0.0002', correct: true },
    { text: 'Persistence = 0.22; long-run variance = 0.000004 / 0.22 ≈ 0.000018' },
    { text: 'Persistence = 0.98; long-run variance = 0.000004 × 0.98 = 0.0000039' },
    { text: 'Persistence = 1.10; the long-run variance is 0.000004' },
  ]}
  explanation="Persistence = α + β = 0.10 + 0.88 = 0.98. The unconditional variance = ω/(1 − α − β) = 0.000004/(1 − 0.98) = 0.000004/0.02 = 0.0002. Since 0.98 < 1, the variance is mean-reverting and this long-run level is well defined."
/>

## Forecasting future variance

GARCH's payoff is a *term structure* of volatility — a forecast not just for tomorrow but for any horizon.

**One step ahead** is direct from the equation: $\sigma_{t+1}^2 = \omega + \alpha\varepsilon_t^2 + \beta\sigma_t^2$, using today's known shock and variance.

**Multi-step ahead** is where mean reversion shows up. The $h$-step forecast pulls geometrically from today's variance toward the long-run anchor:
$$E[\sigma_{t+h}^2] = \bar{\sigma}^2 + (\alpha + \beta)^{h-1}\big(\sigma_{t+1}^2 - \bar{\sigma}^2\big).$$
The gap between current and long-run variance shrinks by a factor of $(\alpha + \beta)$ each step. High persistence → slow convergence → today's elevated vol matters far into the future. Low persistence → fast convergence → the forecast snaps back to the anchor quickly.

**Worked example — the volatility term structure.** Using $\bar{\sigma}^2 = 0.0005$, $\alpha + \beta = 0.98$, and a current elevated $\sigma_{t+1}^2 = 0.0009$ (vol $\approx 3\%$, mid-crisis):

- 1 day: $0.0009$ → vol $3.00\%$.
- 10 days: $0.0005 + 0.98^{9}(0.0009 - 0.0005) = 0.0005 + 0.834 \times 0.0004 = 0.000834$ → vol $\approx 2.89\%$.
- 50 days: $0.0005 + 0.98^{49}(0.0004) = 0.0005 + 0.372 \times 0.0004 = 0.000649$ → vol $\approx 2.55\%$.
- 250 days: $0.0005 + 0.98^{249}(0.0004) \approx 0.0005 + 0.0066\times 0.0004 \approx 0.000503$ → vol $\approx 2.24\%$ (back at the anchor).

The forecast slides smoothly from today's crisis level back toward the long-run climate — exactly the shape that lets a risk desk say "elevated now, normalizing over the quarter."

<Reveal client:visible>
  <details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
    <summary className="cursor-pointer font-medium">GARCH gives time-varying variance, but does it fix the fat tails of returns too?</summary>

    Partly — and this is a subtle, important point. GARCH *generates* fat-tailed *unconditional* returns even when the *conditional* shocks are normal: mixing normals with different variances (calm days and storm days) produces an aggregate distribution with fatter tails than any single normal. So plain GARCH-with-normal-errors already explains a chunk of the observed kurtosis purely through volatility clustering. But empirically it's usually *not enough* — real returns have tails fatter than even normal-GARCH predicts. So practitioners fit GARCH with **fat-tailed conditional distributions** (Student-t errors are standard) or use asymmetric variants like **GJR-GARCH / EGARCH** that let bad news raise volatility more than good news (the *leverage effect* — crashes spike vol more than rallies of the same size). The progression is: constant variance → GARCH-normal (clustering + some fat tails) → GARCH-t (fatter conditional tails) → asymmetric GARCH (leverage). Each step closes a remaining gap between the model and the stubborn reality of market returns.
  </details>
</Reveal>

<MCQ
  client:visible
  question="A GARCH(1,1) with persistence α + β = 0.95 is currently forecasting volatility well above its long-run level after a crash. What does its multi-step forecast do as the horizon grows?"
  options={[
    { text: 'It decays geometrically by a factor of 0.95 each step, sliding from the elevated current level back toward the long-run variance anchor', correct: true },
    { text: 'It stays permanently at the elevated crash level forever' },
    { text: 'It jumps instantly to the long-run level at the very next step' },
    { text: 'It rises without bound because volatility is persistent' },
  ]}
  explanation="When α + β < 1, GARCH is mean-reverting. The h-step forecast pulls toward the long-run variance, with the gap shrinking by (α+β) = 0.95 each step. So an elevated post-crash level decays geometrically back toward the anchor — fast if persistence is low, slowly if it's near 1."
/>

## Putting it together

**Volatility clustering** — turbulent and calm stretches bunching into runs — is the robust stylized fact that returns have a flat level ACF but a strongly persistent *squared-return* ACF: magnitude is forecastable even when direction isn't. **ARCH** captured this by letting today's variance react to the last squared shock ($\sigma_t^2 = \omega + \alpha\varepsilon_{t-1}^2$), but needed too many lags. **GARCH(1,1)** fixed it by adding a persistence term, $\sigma_t^2 = \omega + \alpha\varepsilon_{t-1}^2 + \beta\sigma_{t-1}^2$: $\omega$ anchors the baseline, $\alpha$ is the reaction to fresh news, $\beta$ is the carryover of yesterday's variance, and three parameters implicitly weight all past shocks. The key diagnostic is **persistence $\alpha + \beta$** — near 1 for daily equities, so shocks linger — and, provided it's below 1, the variance mean-reverts to the **long-run level $\omega/(1-\alpha-\beta)$**. Forecasts form a **volatility term structure** that decays geometrically by $(\alpha + \beta)$ per step from today's level back toward that anchor. GARCH-normal already manufactures some fat tails through clustering; Student-t errors and asymmetric variants (GJR/EGARCH, the leverage effect) close the rest of the gap to real returns.

<MindMap
  client:visible
  title="Volatility clustering & GARCH — the whole picture"
  caption="Clustering is the fact; ARCH was the first fix; GARCH(1,1) is the workhorse; persistence sets the decay; forecasts revert to the long-run variance."
  root={{
    label: 'Volatility clustering & GARCH',
    children: [
      {
        label: 'Volatility clustering',
        children: [
          { label: 'Big moves follow big moves' },
          { label: 'Flat return ACF, persistent squared-return ACF' },
          { label: 'Magnitude forecastable even if direction is not' },
        ],
      },
      {
        label: 'ARCH',
        children: [
          { label: 'Conditional heteroskedasticity (varying variance)' },
          { label: 'σ²_t = ω + α·ε²_{t−1}' },
          { label: 'Needs many lags for slow decay' },
        ],
      },
      {
        label: 'GARCH(1,1)',
        children: [
          { label: 'σ²_t = ω + α·ε²_{t−1} + β·σ²_{t−1}' },
          { label: 'ω baseline, α news, β memory' },
          { label: 'Three params imply infinite weighted past' },
        ],
      },
      {
        label: 'Persistence & long-run',
        children: [
          { label: 'Persistence = α + β (often ~0.98 daily)' },
          { label: 'Long-run variance = ω/(1−α−β)' },
          { label: 'α+β = 1 is IGARCH (EWMA); > 1 explosive' },
        ],
      },
      {
        label: 'Forecasting & extensions',
        children: [
          { label: 'Multi-step decays by (α+β) per step' },
          { label: 'Volatility term structure → long-run anchor' },
          { label: 'GARCH-t for fatter tails' },
          { label: 'GJR/EGARCH for the leverage effect' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: volatility clustering & GARCH"
  questions={[
    {
      question: 'What empirical fact about returns is "volatility clustering," and how does it show up in the autocorrelation plots?',
      options: [
        { text: 'Large moves tend to follow large moves and calm follows calm; the level-return ACF is roughly flat but the SQUARED-return ACF is strongly positive and slow to decay', correct: true },
        { text: 'Returns trend steadily upward, shown by a positive ACF at every lag' },
        { text: 'Volatility is constant, shown by a flat squared-return ACF' },
        { text: 'Direction is perfectly forecastable, shown by a large lag-1 return autocorrelation' },
      ],
      explanation: 'Volatility clustering = turbulent and calm periods bunching together. Direction stays hard to forecast (flat return ACF), but magnitude is persistent — the squared-return ACF is large and decays slowly. That persistent squared-return structure is precisely what GARCH models.',
    },
    {
      question: 'In GARCH(1,1), σ²_t = ω + α·ε²_{t−1} + β·σ²_{t−1}, what does the β term represent?',
      options: [
        { text: 'The persistence of yesterday\'s variance — how much elevated volatility carries over to today (the memory term)', correct: true },
        { text: 'The reaction to the most recent squared shock (the news term)' },
        { text: 'The constant long-run baseline variance' },
        { text: 'The mean return of the series' },
      ],
      explanation: 'β multiplies the PREVIOUS conditional variance σ²_{t−1}, so it captures how persistently elevated volatility carries forward — the memory term. α multiplies the last squared shock (the news/reaction term), and ω is the constant baseline.',
    },
    {
      question: 'A GARCH(1,1) has ω = 0.00002, α = 0.06, β = 0.90. What is the long-run (unconditional) variance?',
      options: [
        { text: '0.0005 — since ω/(1 − α − β) = 0.00002/(1 − 0.96) = 0.00002/0.04', correct: true },
        { text: '0.00002 — the long-run variance always equals ω' },
        { text: '0.96 — the long-run variance equals the persistence' },
        { text: 'Undefined — the variance never reverts' },
      ],
      explanation: 'Persistence = α + β = 0.96 < 1, so the variance is mean-reverting. Unconditional variance = ω/(1 − α − β) = 0.00002/0.04 = 0.0005. (It would be undefined only if α + β = 1, the IGARCH case.)',
    },
    {
      question: 'Spot the trap. A fitted GARCH(1,1) comes back with α + β = 1.03. What should you conclude?',
      options: [
        { text: 'The model is non-stationary/explosive and its long-run variance is undefined — likely a mis-estimation or a structural break; do not trust its long-run forecasts', correct: true },
        { text: 'It is the ideal model, since high persistence means accurate forecasts' },
        { text: 'It is exactly EWMA, which requires α + β = 1.03' },
        { text: 'Nothing — persistence above 1 is perfectly normal and stable' },
      ],
      explanation: 'α + β must be below 1 for a stationary, mean-reverting GARCH. At exactly 1 you have IGARCH (EWMA); above 1 the variance is explosive and the unconditional variance formula divides by a negative number — meaningless. A value of 1.03 signals mis-estimation or a regime break in the data.',
    },
  ]}
/>

Next up — **EWMA & RiskMetrics** — we meet GARCH's famous simplified cousin: the exponentially weighted moving average. It's the $\alpha + \beta = 1$ special case, it powers the classic RiskMetrics risk system, and its single decay parameter $\lambda$ makes it the pragmatic volatility estimator that desks reach for first.
