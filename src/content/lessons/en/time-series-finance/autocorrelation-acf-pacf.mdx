---
title: "Autocorrelation, ACF & PACF"
description: "Autocorrelation and the autocorrelation function (ACF), the partial autocorrelation function (PACF), the white-noise null and its significance band, and the Ljung–Box test for whether any structure remains."
topic: "en/time-series-finance"
order: 2
minutes: 11
updated: 2026-06-07
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, MatchConcepts, MindMap, TsAcfPlot } from '@/components/react';

You now have a stationary series — returns — and the obvious question is whether it carries any *memory*. Does a big up-day make tomorrow more (or less) likely to be up? Is there a weekly rhythm, a momentum effect, a reversal? **Autocorrelation** is the measurement that answers this, and its two diagnostic plots — the **ACF** and the **PACF** — are the fingerprint readers of time-series analysis. They tell you whether there's any forecastable structure at all (the **white-noise** question) and, if there is, which kind of model will capture it. Learn to read these two plots and half of model selection becomes pattern recognition.

<MCQ
  client:visible
  pretest
  question="You compute the correlation between a return series and the same series shifted by one day. It comes out at 0.02. Before celebrating a forecasting edge, what should you ask?"
  options={[
    { text: 'Whether 0.02 is statistically distinguishable from zero — for a long sample the white-noise band is roughly ±2/√n, so a tiny value may just be sampling noise around a true zero', correct: true },
    { text: 'Nothing — any nonzero autocorrelation is automatically tradable signal' },
    { text: 'Whether to immediately lever up, since 0.02 guarantees a profit' },
    { text: 'Whether the market is open on weekends' },
  ]}
  explanation="Even genuinely random (white-noise) data shows small nonzero sample autocorrelations by chance. The question is whether the value pokes outside the ±2/√n significance band. A 0.02 in a large sample is usually inside the band — indistinguishable from zero, not a signal."
/>

## Autocorrelation: a series correlated with its own past

**Analogy.** Autocorrelation is asking, "How much does today rhyme with yesterday?" If hot days tend to follow hot days, temperature has positive autocorrelation at lag 1. If a bounce tends to follow a drop, the series has *negative* autocorrelation. It's just the ordinary correlation coefficient, but between a series and a time-shifted copy of itself.

**Definition.** The **autocorrelation at lag $k$** is the correlation between $X_t$ and $X_{t-k}$:
$$\rho_k = \frac{\mathrm{Cov}(X_t, X_{t-k})}{\mathrm{Var}(X_t)} = \frac{\sum_{t=k+1}^{n}(X_t - \bar{X})(X_{t-k} - \bar{X})}{\sum_{t=1}^{n}(X_t - \bar{X})^2}.$$
Like any correlation, $\rho_k$ lives in $[-1, 1]$. By construction $\rho_0 = 1$ (a series is perfectly correlated with itself at lag 0). The whole sequence $\rho_1, \rho_2, \rho_3, \dots$ is the **autocorrelation function (ACF)**.

**Worked example.** Take a tiny series $X = (2, 4, 6, 4, 2)$ with mean $\bar{X} = 3.6$. Deviations: $(-1.6, 0.4, 2.4, 0.4, -1.6)$.

- Denominator (variance sum): $(-1.6)^2 + 0.4^2 + 2.4^2 + 0.4^2 + (-1.6)^2 = 2.56 + 0.16 + 5.76 + 0.16 + 2.56 = 11.2$.
- Lag-1 numerator: $(0.4)(-1.6) + (2.4)(0.4) + (0.4)(2.4) + (-1.6)(0.4) = -0.64 + 0.96 + 0.96 - 0.64 = 0.64$.
- So $\rho_1 = 0.64 / 11.2 \approx 0.057$ — weak positive lag-1 autocorrelation in this toy series.

The arithmetic is just correlation; the *interpretation* is "memory across one step in time."

## White noise: the null hypothesis of "no structure"

Before you can call any autocorrelation meaningful, you need a baseline for "no memory at all." That baseline is **white noise**.

**Definition.** A **white-noise** process has zero mean, constant variance, and **zero autocorrelation at every nonzero lag** ($\rho_k = 0$ for all $k \neq 0$). Each value is uncorrelated with every other — pure unforecastable noise. It is the time-series equivalent of "nothing to see here."

**The significance band.** Here's the subtlety the pretest flagged: even *true* white noise produces small *nonzero* sample autocorrelations, because you're estimating from a finite sample. Under the white-noise null, each sample $\hat\rho_k$ is approximately normal with standard error $1/\sqrt{n}$. So the **95% confidence band** is roughly
$$\pm \frac{2}{\sqrt{n}}.$$
A bar inside the band is **indistinguishable from zero** — sampling noise. A bar poking *outside* it is a **statistically significant** autocorrelation: real structure.

**Worked example — the band shrinks with data.** For $n = 100$ observations the band is $\pm 2/\sqrt{100} = \pm 0.20$ — a sample autocorrelation needs to exceed $0.20$ in magnitude to count. For $n = 2500$ (about ten years of daily data) the band is $\pm 2/\sqrt{2500} = \pm 0.04$. More data means a tighter band, so smaller real effects become detectable — but it also means you must *not* be impressed by a $0.10$ when $n = 100$ (inside the band) even though the same $0.10$ would be significant at $n = 2500$.

<TsAcfPlot
  client:visible
  title="Reading the ACF: white noise vs AR vs MA"
  whiteNoiseLabel="White noise"
  arLabel="AR(1) decay"
  maLabel="MA(1) spike"
  lagLabel="Lag k"
  acfLabel="Autocorrelation"
  bandLabel="White-noise band (±2/√n)"
  caption="Toggle the three patterns. White noise keeps every bar inside the grey band — no forecastable structure. The AR(1) decays geometrically across many lags. The MA(1) fires one significant spike at lag 1 and then collapses. The shape is the model's signature."
/>

<MCQ
  client:visible
  question="A return series has n = 400 observations. Its lag-1 sample autocorrelation is 0.07. Is it statistically significant at the usual 95% level?"
  options={[
    { text: 'No — the band is ±2/√400 = ±0.10, and 0.07 sits inside it, so it is indistinguishable from white-noise sampling error', correct: true },
    { text: 'Yes — any positive autocorrelation is significant regardless of sample size' },
    { text: 'No — but only because 0.07 is too small to ever matter at any sample size' },
    { text: 'Yes — 0.07 is above 0.05, which is the universal significance threshold' },
  ]}
  explanation="The white-noise 95% band for n = 400 is ±2/√400 = ±0.10. A sample autocorrelation of 0.07 lies inside the band, so you cannot distinguish it from zero — it is consistent with white noise. Significance depends on the sample size, not on a fixed 0.05 cutoff for the correlation itself."
/>

## The PACF: autocorrelation with the middle removed

The ACF has a blind spot. If today is correlated with yesterday, and yesterday with the day before, then today will show correlation with *two* days ago **even if there's no direct link** — the effect just leaks through the chain. The ACF can't tell a direct relationship from an indirect, transmitted one. The **partial autocorrelation function (PACF)** fixes exactly this.

**Definition.** The **partial autocorrelation at lag $k$** is the correlation between $X_t$ and $X_{t-k}$ **after removing the influence of all the intermediate lags** $X_{t-1}, \dots, X_{t-k+1}$. It's the *direct* effect of $k$ periods ago, with the indirect transmission stripped out.

**Analogy.** The ACF asks, "Are grandparents correlated with grandchildren?" — and the answer is yes, but partly *because* parents sit in between. The PACF asks, "Is there a *direct* grandparent–grandchild link once we account for the parents?" Usually much smaller. The PACF isolates the genuinely direct dependence.

This is why the two plots together identify the model — they answer different questions, and each model type leaves a distinctive joint signature.

<FillBlank
  client:visible
  question="ACF versus PACF."
  text="The ACF measures the {{total|direct only|future}} correlation between a value and its lag-k past, including effects that leak through intermediate lags. The PACF measures the {{direct|total|random}} correlation at lag k after {{removing the intermediate lags|adding more lags|ignoring the mean}}. Together they form the fingerprint used to choose a model."
  explanation="The ACF captures the total (direct + indirect) correlation at each lag; the PACF strips out the intermediate lags to isolate the direct effect. Comparing their shapes — which one decays and which one cuts off — is how you read off the model order."
/>

## The model-identification cheat sheet

Here's the payoff that makes these two plots indispensable. Each of the basic models (you'll meet them in detail next lesson) leaves a *characteristic* pattern across the ACF and PACF:

| Model | ACF | PACF |
|---|---|---|
| **White noise** | all bars inside the band | all bars inside the band |
| **AR(p)** | decays gradually (geometric / damped) | **cuts off after lag $p$** |
| **MA(q)** | **cuts off after lag $q$** | decays gradually |
| **ARMA(p,q)** | decays after lag $q$ | decays after lag $p$ |

The mnemonic: **AR shows its order in the PACF cutoff; MA shows its order in the ACF cutoff.** They're mirror images. An AR(2), for instance, has a PACF with two significant spikes and then silence, while its ACF tapers off smoothly. An MA(1) does the reverse: one significant ACF spike, then silence, with a tapering PACF.

**Worked reading.** Suppose you plot a return series and see: the ACF has a single big spike at lag 1 (outside the band), everything after inside; the PACF tapers off gradually over several lags. Reading the table: ACF cuts off after lag 1 → that's the $q$ of an **MA(1)**. You'd fit an MA(1) and check the residuals.

<MatchConcepts
  client:visible
  question="Match each pattern to the model it points to."
  pairs={[
    { term: 'ACF and PACF both inside the band', definition: 'White noise — no forecastable structure to model' },
    { term: 'PACF cuts off after lag p, ACF decays', definition: 'An AR(p) process — order read from the PACF cutoff' },
    { term: 'ACF cuts off after lag q, PACF decays', definition: 'An MA(q) process — order read from the ACF cutoff' },
    { term: 'Both ACF and PACF decay gradually', definition: 'An ARMA(p,q) process — a blend of both components' },
    { term: '±2/√n band', definition: 'The 95% white-noise confidence interval; bars outside it are significant' },
  ]}
  explanation="AR order shows as a PACF cutoff; MA order shows as an ACF cutoff; ARMA decays in both; white noise stays inside the band everywhere. The ±2/√n band is the threshold separating real structure from sampling noise."
/>

## Ljung–Box: is there ANY structure left?

Eyeballing 20 individual bars invites trouble — with a 95% band, you *expect* about 1 in 20 to poke outside by pure chance. You want a single test of the joint question: "taken together, are the first $m$ autocorrelations collectively different from zero?" That's the **Ljung–Box test**.

**The idea.** It bundles the first $m$ sample autocorrelations into one statistic:
$$Q = n(n+2)\sum_{k=1}^{m}\frac{\hat\rho_k^2}{n-k},$$
which, under the white-noise null, follows a chi-squared distribution. A **large $Q$** (small p-value) means the autocorrelations are *jointly* too big to be noise — there's leftover structure. A **small $Q$** (large p-value) means the series is statistically indistinguishable from white noise across those lags.

**The two ways quants use it:**

1. **On the raw series:** is there any forecastable autocorrelation worth modeling at all? A small p-value says yes.
2. **On model residuals:** the crucial diagnostic. After fitting a model, the residuals should be white noise — you've extracted all the structure. Run Ljung–Box on them: a **large p-value (fail to reject)** is what you *want* — it means no autocorrelation remains, the model has done its job. A small p-value means structure is still leaking through and the model is mis-specified.

**Worked intuition.** You fit an AR(1), then run Ljung–Box on the residuals and get p = 0.62. Since $0.62 > 0.05$, you fail to reject white noise — the residuals look clean, so the AR(1) captured the autocorrelation. If instead p = 0.001, you'd reject: there's still structure (maybe you need an AR(2) or an MA term), and your model is incomplete.

<Callout variant="warning" title="A clean ACF on returns does NOT mean clean on squared returns">
Financial returns are notorious: their *level* ACF is often nearly white noise (you can't forecast direction), yet the ACF of their **squared** (or absolute) returns shows strong, slowly-decaying autocorrelation. That's volatility clustering — big moves follow big moves — and it's invisible to a Ljung–Box test run on the raw returns. Always check squared returns too; it's the gateway to the GARCH lesson.
</Callout>

<Reveal client:visible>
  <details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
    <summary className="cursor-pointer font-medium">If the return ACF is basically flat, why does anyone bother with time-series models at all?</summary>

    Because "can't forecast the direction" and "no structure to exploit" are different claims. Three things hide behind a flat return ACF. First, **volatility is forecastable** even when returns aren't: the squared-return ACF is far from flat, which is the entire ARCH/GARCH industry and the basis of risk forecasting and options pricing. Second, the autocorrelation that *does* survive in returns is often small but real and persistent (short-horizon reversal, longer-horizon momentum), and at scale a tiny edge times enormous turnover is a business. Third, even a perfectly white-noise return still needs its *unconditional* distribution modeled — fat tails, skew — for risk and capital. So a flat ACF kills naive direction-betting, not time-series modeling. It just redirects you from the mean to the variance and the tails.
  </details>
</Reveal>

<MCQ
  client:visible
  question="You fit a model and run the Ljung–Box test on its residuals, getting a p-value of 0.45. What does this tell you, and is it good news?"
  options={[
    { text: 'You fail to reject white noise — no significant autocorrelation remains in the residuals — which is exactly what you want from a well-fitted model', correct: true },
    { text: 'You reject white noise; the model is badly mis-specified and must be discarded' },
    { text: 'The model has overfit, since a high p-value always signals overfitting' },
    { text: 'Nothing — Ljung–Box cannot be applied to residuals' },
  ]}
  explanation="A high p-value (0.45 > 0.05) means you fail to reject the white-noise null: the residuals show no significant leftover autocorrelation, so the model captured the structure. On residuals, a LARGE p-value is the success condition — the opposite of testing the raw series, where a small p-value flags structure worth modeling."
/>

## Putting it together

**Autocorrelation** measures how much a series rhymes with its own past: $\rho_k$ is the correlation between $X_t$ and $X_{t-k}$, and the sequence is the **ACF**. The baseline is **white noise** — zero autocorrelation at every nonzero lag — and because finite samples wiggle, you judge significance against the **$\pm 2/\sqrt{n}$ band**: bars inside it are indistinguishable from zero, bars outside are real structure. The **PACF** removes the indirect, transmitted-through-intermediate-lags correlation to isolate the *direct* effect at each lag. Read together, the two plots are a model-identification cheat sheet: **AR(p)** cuts off in the PACF, **MA(q)** cuts off in the ACF, **ARMA** decays in both, white noise stays inside the band everywhere. The **Ljung–Box test** bundles the first $m$ autocorrelations into one chi-squared statistic — use it on the raw series to ask "is there anything to model?" (small p-value = yes) and on residuals to confirm "did the model extract everything?" (large p-value = success). And beware: a clean return ACF can still hide ferocious autocorrelation in the *squared* returns — volatility clustering, the doorway to GARCH.

<MindMap
  client:visible
  title="Autocorrelation, ACF & PACF — the whole picture"
  caption="Autocorrelation measures memory; white noise is the null; the ±2/√n band judges significance; ACF+PACF identify the model; Ljung–Box tests jointly."
  root={{
    label: 'Autocorrelation, ACF & PACF',
    children: [
      {
        label: 'Autocorrelation',
        children: [
          { label: 'Correlation of X_t with X_{t−k}' },
          { label: 'Lives in [−1, 1]; ρ0 = 1' },
          { label: 'The full sequence is the ACF' },
        ],
      },
      {
        label: 'White noise & the band',
        children: [
          { label: 'Zero autocorrelation at all nonzero lags' },
          { label: '95% band ≈ ±2/√n' },
          { label: 'Inside = noise; outside = significant' },
          { label: 'More data → tighter band' },
        ],
      },
      {
        label: 'PACF',
        children: [
          { label: 'Direct correlation at lag k' },
          { label: 'Removes intermediate-lag transmission' },
          { label: 'Grandparent link with parents controlled' },
        ],
      },
      {
        label: 'Model identification',
        children: [
          { label: 'AR(p): PACF cuts off after p' },
          { label: 'MA(q): ACF cuts off after q' },
          { label: 'ARMA: both decay gradually' },
          { label: 'White noise: both inside the band' },
        ],
      },
      {
        label: 'Ljung–Box',
        children: [
          { label: 'Joint test of first m autocorrelations' },
          { label: 'Raw series: small p = structure to model' },
          { label: 'Residuals: large p = model is clean' },
          { label: 'Check squared returns for clustering' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: autocorrelation, ACF & PACF"
  questions={[
    {
      question: 'A series of n = 625 daily returns shows a lag-1 sample autocorrelation of 0.11. Is it significant at 95%?',
      options: [
        { text: 'Yes — the band is ±2/√625 = ±0.08, and 0.11 lies outside it, so it is a statistically significant autocorrelation', correct: true },
        { text: 'No — 0.11 is far too small to ever be significant' },
        { text: 'No — the band is ±0.20, so 0.11 is inside it' },
        { text: 'Cannot tell without knowing the mean of the series' },
      ],
      explanation: 'The 95% white-noise band is ±2/√n = ±2/25 = ±0.08. A sample autocorrelation of 0.11 exceeds 0.08, so it pokes outside the band and is statistically significant. The same 0.11 would NOT be significant at, say, n = 100 (band ±0.20).',
    },
    {
      question: 'What does the PACF measure that the ACF does not?',
      options: [
        { text: 'The DIRECT correlation at lag k after removing the influence of all intermediate lags, isolating genuine dependence from effects that just leak through the chain', correct: true },
        { text: 'The correlation of the series with future values instead of past ones' },
        { text: 'The variance of the series at each lag' },
        { text: 'Nothing different — the PACF and ACF are identical' },
      ],
      explanation: 'The ACF reports total correlation at each lag, including indirect effects transmitted through intermediate lags. The PACF strips those out to show the direct relationship at lag k. This is exactly why an AR(p) cuts off in the PACF at lag p but only decays in the ACF.',
    },
    {
      question: 'A stationary series has an ACF that decays gradually over many lags and a PACF with two significant spikes (lags 1 and 2) then nothing. Which model does this point to?',
      options: [
        { text: 'An AR(2) — the PACF cutoff at lag 2 gives the AR order, while the ACF only decays', correct: true },
        { text: 'An MA(2) — because the PACF cuts off at lag 2' },
        { text: 'White noise — because both plots have some structure' },
        { text: 'An MA(1) — because the ACF decays' },
      ],
      explanation: 'AR order is read from the PACF cutoff: two significant PACF spikes then silence → AR(2). The ACF of an AR process decays gradually rather than cutting off. (For an MA(q) it would be the reverse: ACF cuts off, PACF decays.)',
    },
    {
      question: 'Spot the trap. An analyst checks the ACF of raw returns, sees it is flat, and concludes "there is no time-series structure here to exploit." What is the oversight?',
      options: [
        { text: 'A flat return ACF only rules out forecastable direction; the SQUARED returns can still show strong autocorrelation — volatility clustering — which is highly forecastable and economically important', correct: true },
        { text: 'Nothing — a flat return ACF truly means there is nothing left to model' },
        { text: 'The analyst should have used the price ACF, which is always flat' },
        { text: 'Flat ACFs are impossible for real financial data' },
      ],
      explanation: 'Returns often have a near-white-noise ACF (direction is hard to forecast) while their squared/absolute returns are strongly autocorrelated — volatility clustering. That structure drives risk forecasting, VaR, and options pricing. A flat return ACF redirects you from the mean to the variance, not to giving up.',
    },
  ]}
/>

Next up — **AR, MA & ARIMA models** — we turn the patterns these plots reveal into actual fitted equations: the autoregressive, moving-average, and integrated models that turn "the PACF cuts off at lag 2" into a forecasting machine you can estimate and interpret.
