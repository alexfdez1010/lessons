---
title: Three Roads to Privacy — Mixers, Monero, Zcash
description: How CoinJoin mixers, Monero ring signatures, and Zcash zk-SNARKs hide payments — what each conceals, and exactly where each one leaks.
topic: "en/zcash"
order: 3
minutes: 9
updated: 2026-05-30
seo:
  keywords:
    - coinjoin
    - monero ring signatures
    - zcash zk-snarks
    - anonymity set
---

import { Callout, MCQ, Quiz, StepThrough, Reveal, CopyButton } from '@/components/react';

Everyone wants payment privacy. Almost nobody agrees on how to get it. Three big
approaches dominate, and they're not just different brands of the same thing —
they hide *different* parts of a transaction, lean on *different* math, and fail
in *different* ways.

Let's walk all three roads, then draw the map.

## Road 1 — CoinJoin: hiding in a crowd (on a glass chain)

Bitcoin was never built for privacy, so privacy got bolted on. The headline
bolt-on is **CoinJoin**: a bunch of users team up to build *one* collaborative
transaction. They throw their coins in together and the transaction pays out a
pile of **equal-denomination outputs** — say 50 outputs of exactly 0.1 BTC.

Because every output looks identical, an outside observer can't say which output
belongs to which input. Your **anonymity set** is the number of indistinguishable
peers you're mixed with (Wasabi targets roughly 50–100).

<Callout type="analogy" title="The identical-envelopes room">
CoinJoin is like 50 people walking into a room and swapping identical sealed
envelopes. From the outside it's chaos. But someone watching the *doors* can
still count who came in and who left — and if your envelope was a weird size,
they can follow it right out.
</Callout>

Here's the catch, and it's a big one:

- **Fixed denominations create change.** Spend 0.13 BTC into a 0.1 mix and you've
  got 0.03 BTC left over. That leftover — *toxic change* — is **deterministically
  linkable** straight back to your input. No plausible deniability.
- **Address reuse shrinks the set.** Sloppy pre-mix or post-mix address handling
  re-links your "anonymous" coins to your identity.
- **It's obfuscation, not encryption.** Every byte still sits on a **transparent
  chain**. You're hiding in a crowd, not behind a wall.

<Callout type="warning" title="Mixing ≠ Monero">
A common myth: "CoinJoin gives me Monero-level privacy." It doesn't. CoinJoin is
crowd-based *obfuscation* on a fully public ledger with linkable change. Useful,
clever, but a different league.
</Callout>

## Road 2 — Monero: privacy by default, three pillars

Monero flips the model: privacy isn't an add-on you opt into, it's **always on,
for every transaction**. It stands on three pillars.

<StepThrough client:visible>
  <div>
    **Pillar 1 — Ring signatures.** Your real input is hidden among a set of
    **decoys** pulled from the chain. That set is the *ring*. The current **ring
    size is 16** (raised from 11). So per input, your anonymity set is about 16:
    small, but **mandatory on every single transaction**.
  </div>
  <div>
    **Pillar 2 — Stealth addresses.** Each payment goes to a fresh, **one-time
    destination address** derived on the fly. The receiver stays anonymous and
    payments to the same person can't be linked together.
  </div>
  <div>
    **Pillar 3 — RingCT (Ring Confidential Transactions).** This **hides the
    amount** using Bulletproofs / Bulletproofs+ range proofs — cryptographic
    promises that the numbers add up without revealing what they are.
  </div>
</StepThrough>

<Callout type="analogy" title="The lineup of look-alikes">
Monero is hiding your signature in a lineup of 16 near-identical look-alikes —
*every single time you sign*. No one round is bulletproof, but you never, ever
step out of the lineup.
</Callout>

The trade-off: the per-transaction set is small (16), and clever
**decoy-selection statistical attacks** are an active research worry — if decoys
are picked in a guessable way, the real input can sometimes be inferred. But the
floor is high: privacy is **never off**.

## Road 3 — Zcash: prove it without showing it

Zcash takes the boldest swing. Using **zk-SNARKs** (today via the **Halo 2**
proving system), a transaction comes with a **zero-knowledge proof**: math that
proves the transaction is valid — inputs exist, nothing's double-spent, amounts
balance — **without revealing the sender, receiver, or amount**.

The mind-bending part is the anonymity set. A fully **shielded** Zcash transaction
doesn't hide among 16 decoys or 50 peers — it draws on the **entire shielded
pool**: every note ever created in that pool. In principle, far larger than a
Monero ring.

<Callout type="analogy" title="No lineup at all">
Where Monero hides you in a lineup, Zcash skips the lineup entirely. It proves
*"a valid member of the whole club authorized this"* — without ever naming, or
even pointing at, which member.
</Callout>

But there's a sharp asterisk: privacy in Zcash is **opt-in**, not the default.

<Callout type="warning" title="Zcash isn't automatically private">
Send a **transparent** Zcash transaction and it's basically Bitcoin — fully
public. Only **shielded** transactions get the zk magic. And because shielding is
optional, your *practical* anonymity set is only as big as the pool of people
actually shielding (recall the usage stats from the last lesson — theory and
practice can diverge hard here).
</Callout>

## The cheat-sheet

One table to rule them all. Note where the "No"s live.

| | CoinJoin | Monero | Zcash (shielded) |
|---|---|---|---|
| Hides amount | No (Wasabi) | Yes (RingCT) | Yes |
| Hides sender | Partially (mix set) | Yes (ring) | Yes |
| Hides receiver | No (transparent) | Yes (stealth) | Yes |
| Anonymity set | ~50–100 | ring = 16 | whole shielded pool |
| Default-on | No | Yes | No (opt-in) |
| Mechanism | obfuscation | decoys / one-time addr | cryptographic proof |

<Callout type="tip" title="The one-line summary">
CoinJoin *obfuscates* on a glass ledger. Monero *hides every tx* among decoys, by
default. Zcash *proves validity* without revealing anything — biggest set in
principle, smallest set in practice if nobody shields.
</Callout>

## Bust the myths

<Reveal summary="“Monero has a bigger anonymity set than Zcash.”" client:visible>
Per transaction, Monero hides among **ring size 16**. A fully shielded Zcash
transaction draws on the **entire shielded pool** — potentially far larger. The
twist is *practical* usage: Zcash's real set is only as big as actual shielded
activity. So "bigger" depends on whether you mean the math or the crowd.
</Reveal>

<Reveal summary="“CoinJoin is just as private as Monero.”" client:visible>
No. CoinJoin is **obfuscation on a transparent chain**, complete with
deterministically linkable toxic change. Monero encrypts amounts, hides receivers
with stealth addresses, and rings every input — mandatorily.
</Reveal>

<Reveal summary="“Zcash transactions are always private.”" client:visible>
Only **shielded** ones. **Transparent** Zcash transactions are Bitcoin-like and
fully public. Privacy is opt-in.
</Reveal>

## Check yourself

<MCQ
  client:visible
  question="Why is CoinJoin called 'obfuscation, not encryption'?"
  options={[
    'It uses weaker cryptography than AES',
    'Everything still sits on a transparent chain — you hide in a crowd rather than behind a wall',
    'It only works for amounts under 0.1 BTC',
    'The outputs are encrypted but the inputs are not',
  ]}
  correctIndex={1}
  explanation="CoinJoin makes equal-denomination outputs indistinguishable, but the whole transaction is public. Add linkable toxic change and address reuse, and it's crowd-based obfuscation, not amount/sender encryption."
/>

<MCQ
  client:visible
  question="What makes a fully shielded Zcash transaction's anonymity set potentially larger than a Monero transaction's?"
  options={[
    'Zcash uses a ring size of 100 instead of 16',
    'A zk-SNARK proof lets it draw on the entire shielded pool, not a fixed set of decoys',
    'Zcash mixes with Bitcoin transactions too',
    'Stealth addresses generate infinite decoys',
  ]}
  correctIndex={1}
  explanation="Monero hides among ~16 decoys per input. A shielded Zcash tx proves validity against the whole pool of shielded notes — far larger in principle, though only as large as real shielded usage in practice."
/>

<Quiz
  client:visible
  title="Three roads, one quiz"
  questions={[
    {
      question: "What is 'toxic change' in a CoinJoin?",
      options: [
        'A fee paid to the mixing coordinator',
        'Leftover from fixed-denomination outputs that is deterministically linkable back to your input',
        'A decoy output added to confuse observers',
        'The portion of the mix held in escrow',
      ],
      correctIndex: 1,
      explanation: 'Fixed denominations leave change that ties straight back to your original input — no plausible deniability.',
    },
    {
      question: 'Which Monero pillar hides the AMOUNT of a transaction?',
      options: ['Ring signatures', 'Stealth addresses', 'RingCT (with Bulletproofs)', 'Halo 2'],
      correctIndex: 2,
      explanation: 'Ring Confidential Transactions hide amounts using Bulletproofs/Bulletproofs+ range proofs. Rings hide the sender; stealth addresses hide the receiver.',
    },
    {
      question: 'Why is Zcash privacy described as opt-in?',
      options: [
        'You must pay extra to enable it',
        'Only shielded transactions use zk-SNARKs; transparent ones are Bitcoin-like and public',
        'It is only available to verified users',
        'It turns off automatically after each block',
      ],
      correctIndex: 1,
      explanation: 'Shielded transactions get the zero-knowledge protection; transparent ones reveal everything. So the practical anonymity set depends on how many people actually shield.',
    },
  ]}
/>

## Where this is heading

Notice that two of our three roads lean on the same magic word: **zero-knowledge
proofs**. Monero uses them to hide amounts; Zcash uses them to hide *everything*.

How can you possibly prove a transaction is valid — that the coins exist, that
nothing's double-spent, that the math balances — **without revealing a single
number**? That's not a metaphor. It's real cryptography, and it's coming up next.
