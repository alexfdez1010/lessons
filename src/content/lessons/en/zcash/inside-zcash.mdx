---
title: Inside Zcash — Addresses, Notes, and Nullifiers
description: How Zcash works under the hood — transparent vs shielded addresses, notes, commitments, nullifiers, and the Sapling/Orchard shielded pools.
topic: "en/zcash"
order: 5
minutes: 13
updated: 2026-05-30
seo:
  keywords:
    - zcash shielded pool
    - nullifier
    - note commitment
    - sapling orchard
    - halo 2 trusted setup
---

import { Callout, MCQ, Quiz, StepThrough, Categorize, FillBlank, MatchConcepts, MindMap } from '@/components/react';

So far you've heard that Zcash can hide who paid whom, how much, and why. Lovely
marketing. But "magic privacy crypto" is not an explanation — it's a shrug with
extra steps. This lesson opens the hood and shows you the *actual machinery*: the
addresses, the cryptographic objects, and the clever trick that stops you from
spending the same coin twice **without anyone learning which coin you spent**.

It's the dense one. Grab coffee. We'll use a lot of banknote analogies, because
they happen to be eerily accurate.

## Addresses: who can see what

<MCQ
  client:visible
  pretest
  question="Guess: in a fully shielded (z → z) transfer, what does the network learn?"
  options={[
    { text: 'Sender, receiver, and amount' },
    { text: 'Only the amount' },
    { text: 'Almost nothing — just that a valid transaction happened', correct: true },
    { text: 'The memo, but nothing else' },
  ]}
  explanation="A z → z transfer hides sender, receiver, amount, and even the memo. Observers only learn a valid transaction occurred. Hold that thought."
/>

Zcash has two worlds living in the same chain.

**Transparent addresses** (t-addrs) are Bitcoin cosplaying as Zcash. Everything
is public — sender, receiver, amount, all on display.

- `t1…` — a P2PKH address (pay-to-public-key-hash, single-signature).
- `t3…` — a P2SH address (pay-to-script-hash, e.g. multisig).

**Shielded addresses** (z-addrs) are where the privacy lives:

- `zc…` — the legacy **Sprout** shielded address (the original 2016 design).
- `zs…` — the modern **Sapling** shielded address.

And then there's the grown-up version everyone should use today:

- `u1…` — a **Unified Address** (UA), introduced in ZIP 316. One string that
  *bundles multiple receivers* (transparent + Sapling + Orchard) so the sender's
  wallet automatically picks the best one both wallets support. Think of it as a
  single phone number that quietly knows whether to ring your landline, mobile,
  or encrypted line.

<Callout type="info" title="Prefix cheat sheet">
`t1` = transparent single-sig · `t3` = transparent multisig · `zc` = Sprout ·
`zs` = Sapling · `u1` = Unified Address. If you only memorize one: `u1` is the
modern default.
</Callout>

<Categorize
  client:visible
  question="Sort each address prefix by which world it belongs to."
  buckets={['Transparent', 'Shielded', 'Bundles both']}
  items={[
    { text: 't1… (P2PKH)', bucket: 'Transparent' },
    { text: 't3… (P2SH)', bucket: 'Transparent' },
    { text: 'zc… (Sprout)', bucket: 'Shielded' },
    { text: 'zs… (Sapling)', bucket: 'Shielded' },
    { text: 'u1… (Unified Address)', bucket: 'Bundles both' },
  ]}
  explanation="t-prefixes are transparent (Bitcoin-like). zc/zs are shielded. A u1 Unified Address bundles transparent + Sapling + Orchard receivers in one string."
/>

### The four transfer types

Because money can hop between the transparent and shielded worlds, every Zcash
transaction is one of **four flavors**, depending on which side the coins start
and end on:

| Transfer | Name | What's public | What's hidden |
|---|---|---|---|
| **t → t** | Transparent | Everything (Bitcoin-like) | Nothing |
| **t → z** | **Shielding** | Source address + amount | Destination |
| **z → t** | **Deshielding** | Destination + amount (revealed on exit) | Source |
| **z → z** | **Fully shielded** | Nothing | Sender, receiver, amount, **and** memo |

The dream scenario is `z → z`: sender, receiver, amount, and even the encrypted
memo are all hidden. The dangerous ones are the boundary crossings — shielding
and deshielding leak an amount, and amounts are surprisingly easy to fingerprint.
(More on how people accidentally doxx themselves in the next lesson.)

<FillBlank
  client:visible
  question="Name the two boundary-crossing transfer types from memory."
  text="Moving funds t → z is called {{shielding}}, and moving them z → t is called {{deshielding}}. Both leak an {{amount}}."
  explanation="t → z = shielding (hides the destination), z → t = deshielding (reveals the destination + amount on exit). The leaked amount is the dangerous part."
/>

## The cryptographic objects

<MCQ
  client:visible
  pretest
  question="Guess: how does Zcash's shielded side track coins if it can't use public UTXOs like Bitcoin?"
  options={[
    { text: 'It encrypts each UTXO with the owner’s password' },
    { text: 'It replaces every public concept with a private cryptographic object', correct: true },
    { text: 'It stores balances in a trusted central server' },
    { text: 'It simply makes UTXOs invisible but keeps the amounts public' },
  ]}
  explanation="Right idea to check: Zcash swaps each public concept (the coin, its identity, its spend) for a private one — notes, commitments, nullifiers."
/>

Bitcoin tracks money as public **UTXOs** — unspent coins anyone can see. Zcash's
shielded side can't do that; the whole point is that the coins are invisible. So
it replaces every public concept with a private one.

### Notes — the shielded coin

A **note** is the shielded analog of a UTXO: an (encrypted) record saying *"this
much value belongs to this shielded address."* It's your coin, sealed in an
envelope only you can open.

> **Analogy:** a note is a **sealed banknote** whose denomination only the owner
> can read.

### Note commitment — the public fingerprint

You can't publish the note (that would reveal the value), but the chain needs
*something* to point at. So Zcash publishes a **note commitment**: a
cryptographic fingerprint of the note. It has two magic properties:

- **Hiding** — you cannot learn the note's value from the commitment.
- **Binding** — the sender can't later change the value and claim the same
  commitment. It's locked in.

> **Analogy:** the commitment is a **tamper-evident serial sticker** the whole
> world can see but can't read through. Everyone knows a banknote exists; nobody
> can read its face value.

<FillBlank
  client:visible
  question="Lock in the two properties of a note commitment."
  text="A note commitment is {{hiding}} (you can't learn the value from it) and {{binding}} (the sender can't swap the value later)."
  explanation="Hiding = the value stays secret. Binding = the committed value is locked and can't be changed afterward."
/>

### The note-commitment Merkle tree

Every commitment ever created is appended as a leaf to one giant **append-only
Merkle tree**. When you spend, your zero-knowledge proof demonstrates
**Merkle membership** — *"a note matching this commitment exists in the tree"* —
**without revealing which leaf it is**. You prove you're holding a real ticket
without showing the ticket's seat number.

### Nullifier — the "spent" stub

Here's the elegant bit, and the heart of the whole system. When you spend a note,
you reveal its **nullifier**: a unique serial number derived from the note.

- There is **exactly one** valid nullifier per note.
- Full nodes keep a **nullifier set**. Spend a note, its nullifier joins the set.
- If a nullifier shows up **twice**, that's a double-spend → the transaction is
  **rejected**.
- **Crucially:** the nullifier does **not** reveal *which note* it came from. So
  the spend can't be linked back to the commitment that created the note.

That's the trick: double-spend prevention **without revealing the note**.

> **Analogy:** the nullifier is a unique **"spent" stub you tear off** when you
> cash a banknote, so the same note can never be cashed twice — yet the stub
> doesn't say which banknote it came from. The cashier knows *a* note was spent;
> not *yours*.

<Callout type="tip" title="Why this is the whole ballgame">
Public-ledger coins (Bitcoin) prevent double-spends by making every coin's
history visible. Zcash prevents them with nullifiers — a one-way "used" marker —
so it gets the same safety with **none** of the visibility. Privacy and integrity
at the same time.
</Callout>

<MatchConcepts
  client:visible
  pairs={[
    { term: 'Note', definition: 'The shielded analog of a UTXO — sealed value belonging to a z-address.' },
    { term: 'Note commitment', definition: 'A public, hiding-and-binding fingerprint of a note.' },
    { term: 'Merkle tree', definition: 'The append-only structure that proves a note exists without revealing which leaf.' },
    { term: 'Nullifier', definition: 'A unique per-note "spent" marker that stops double-spends without revealing the note.' },
  ]}
/>

### Spend authority

Inside the zero-knowledge proof, the prover privately supplies the note values,
the **commitment openings** (the secret data that produced each commitment), and
the **secret keys** authorizing the spend. The proof says "I know all of this and
it's consistent" — and reveals none of it.

### Value commitments and homomorphic balance

How does the network confirm that inputs equal outputs (no coins conjured from
thin air) when all the amounts are hidden? With **Pedersen value commitments**,
which are **homomorphic** — you can do math on the sealed values without opening
them. The input and output value commitments are required to **sum to zero**.

If they balance, inputs = outputs. Full stop.

<Callout type="warning" title="Precision check (important)">
The proof does **not** "reveal that the amounts are equal." It reveals nothing
about the amounts at all. The homomorphic commitments **summing to zero** is what
proves balance — the numbers themselves stay sealed. Say it the wrong way and a
cryptographer will sigh audibly.
</Callout>

<MCQ
  client:visible
  question="How do homomorphic value commitments prove a shielded transaction balances?"
  options={[
    { text: 'The proof reveals that the input and output amounts are equal' },
    { text: 'Input and output value commitments are required to sum to zero, proving inputs = outputs without revealing amounts', correct: true },
    { text: 'A trusted third party compares the amounts off-chain' },
    { text: 'Each amount is stored as a Merkle leaf for auditing' },
  ]}
  explanation="Pedersen value commitments are homomorphic, so the input and output commitments must sum to zero. That proves inputs equal outputs while the amounts themselves stay sealed — the proof never reveals the numbers."
/>

### Encrypted memo field

Every shielded output carries an **encrypted memo** (512 bytes), readable only by
the recipient — or by anyone the recipient hands a **viewing key**. It's a sealed
note tucked inside the sealed banknote: payment references, invoice IDs, "thanks
for lunch," whatever. Encrypted to everyone else.

<FillBlank
  client:visible
  question="Active recall: the memo's escape hatch."
  text="A shielded output's encrypted memo is readable by the recipient — or by anyone the recipient hands a {{viewing key}}."
  explanation="A viewing key lets a third party read incoming shielded data (like the memo) without the ability to spend."
/>

## A z → z spend, step by step

<MCQ
  client:visible
  pretest
  question="Guess: when a z → z spend lands on-chain, what two opaque things appear?"
  options={[
    { text: 'The sender’s address and the amount' },
    { text: 'A nullifier (for the spent note) and a fresh note commitment (for the recipient)', correct: true },
    { text: 'A viewing key and the memo' },
    { text: 'The Merkle root and the spending key' },
  ]}
  explanation="On-chain you only ever see the spent note's nullifier and a new note commitment for the recipient. Everything else stays sealed."
/>

Let's watch a fully shielded spend happen. Notice how much gets *proved* versus
how little gets *published*. This one's genuinely sequential, so step through it.

<StepThrough client:visible>
  <div data-step data-step-title="1 · A note already lives in the tree">
    At some earlier point, someone paid you. That created a **note** for you and
    appended its **commitment** as a leaf in the global note-commitment Merkle
    tree. Public observers see one more anonymous leaf appear — nothing more.
  </div>
  <div data-step data-step-title="2 · You build the proof's private inputs">
    To spend, your wallet privately gathers the secret stuff: the note's value,
    its commitment opening, and the **spending key**. None of this leaves your
    machine in the clear.
  </div>
  <div data-step data-step-title="3 · Prove membership in zero-knowledge">
    The proof shows a note matching a real commitment **exists in the tree** —
    without revealing **which** leaf. Membership: proven. Identity of the leaf:
    hidden.
  </div>
  <div data-step data-step-title="4 · Prove spend authority + derive the nullifier">
    The proof also shows you hold the key that authorizes spending this note, and
    it derives the note's one-and-only **nullifier**.
  </div>
  <div data-step data-step-title="5 · Prove balance with value commitments">
    Using homomorphic **value commitments** that **sum to zero**, the proof shows
    inputs equal outputs — no inflation — while keeping every amount sealed.
  </div>
  <div data-step data-step-title="6 · Publish the nullifier + the new commitment">
    On-chain, the world sees only two new opaque things: the **nullifier** (so
    this note can never be spent again) and a fresh **note commitment** for the
    recipient. Sender, receiver, amount, memo — invisible.
  </div>
</StepThrough>

That's it. A coin moved. The chain stayed integrity-preserving and double-spend-
proof. And an observer learned approximately nothing.

<Categorize
  client:visible
  question="During that z → z spend, which facts stayed sealed and which became public?"
  buckets={['Stays sealed', 'Becomes public']}
  items={[
    { text: 'The note’s value / amount', bucket: 'Stays sealed' },
    { text: 'Which leaf in the tree was spent', bucket: 'Stays sealed' },
    { text: 'The encrypted memo', bucket: 'Stays sealed' },
    { text: 'The nullifier of the spent note', bucket: 'Becomes public' },
    { text: 'A fresh note commitment for the recipient', bucket: 'Becomes public' },
  ]}
  explanation="Only the nullifier and the new commitment go on-chain. The amount, the spent leaf's identity, and the memo all stay sealed."
/>

## The shielded pools, and how the crypto evolved

<MCQ
  client:visible
  pretest
  question="Guess: which Zcash shielded pool needs NO trusted setup?"
  options={[
    { text: 'Sprout' },
    { text: 'Sapling' },
    { text: 'Orchard', correct: true },
    { text: 'All of them need one' },
  ]}
  explanation="Orchard (built on Halo 2) is the one with no trusted setup. We'll see exactly why below."
/>

Zcash didn't get here in one shot. The shielded machinery has had three
generations, each with a different proving system. (A "proving system" is the
zero-knowledge engine that produces and checks those proofs.)

| Pool | Activated | Proving system | Curve(s) | Trusted setup? |
|---|---|---|---|---|
| **Sprout** | 2016 launch | BCTV14, later Groth16 | BN-254 family | **Yes** — 6-participant ceremony |
| **Sapling** | Oct 2018 | Groth16 | BLS12-381 (+ Jubjub in-circuit) | **Yes** — Powers of Tau + Sapling MPC, ~90 participants |
| **Orchard** | NU5, May 2022 | Halo 2 | Pallas + Vesta ("Pasta") | **No** trusted setup |

A few accuracy notes worth internalizing:

- **Sprout launched on BCTV14** and was *later* upgraded to **Groth16**. It was
  not Groth16 from day one.
- **Sapling** uses **Groth16 over BLS12-381**. **Jubjub** is the *in-circuit*
  curve (defined over BLS12-381's scalar field) used for in-circuit operations —
  it is **not** the proof curve. Keep those two straight; they're a classic mixup.
- Sapling was a monster efficiency leap: roughly **97% less memory** (down to
  ~40 MB) and about **81% faster proving**. That's what made **shielded mobile
  wallets** actually feasible instead of a laptop-melting fantasy.
- **NU5 (May 2022)** shipped *three* things at once: **Halo 2**, the **Orchard**
  pool, and **Unified Addresses**. Three birthday presents, one upgrade.

<Categorize
  client:visible
  question="Match each proving system to the pool that uses it."
  buckets={['Sprout', 'Sapling', 'Orchard']}
  items={[
    { text: 'BCTV14, later Groth16', bucket: 'Sprout' },
    { text: 'Groth16 over BLS12-381', bucket: 'Sapling' },
    { text: 'Halo 2', bucket: 'Orchard' },
  ]}
  explanation="Sprout launched on BCTV14 (later Groth16). Sapling uses Groth16 over BLS12-381 (Jubjub is the in-circuit curve, not the proof curve). Orchard uses Halo 2."
/>

<MCQ
  client:visible
  allowMultiple
  question="Which THREE things shipped together in NU5 (May 2022)? (Select all.)"
  options={[
    { text: 'Halo 2', correct: true },
    { text: 'The Orchard pool', correct: true },
    { text: 'Unified Addresses', correct: true },
    { text: 'The Sprout pool' },
    { text: 'Jubjub' },
  ]}
  explanation="NU5 shipped Halo 2, the Orchard pool, and Unified Addresses at once. Sprout launched back in 2016; Jubjub is Sapling's in-circuit curve."
/>

## Trusted setup, toxic waste, and how Halo 2 nuked it

<MCQ
  client:visible
  pretest
  question="Guess: what's the danger if someone keeps a copy of a trusted setup's secret randomness?"
  options={[
    { text: 'They can read everyone’s memos' },
    { text: 'They can forge proofs and mint coins undetectably', correct: true },
    { text: 'They can deanonymize past transactions' },
    { text: 'Nothing — the secret is useless after the ceremony' },
  ]}
  explanation="The leftover secret is 'toxic waste.' A kept copy lets its holder forge proofs and counterfeit coins with no alarm bells — which is why the ceremony must destroy it."
/>

Here's the uncomfortable part of older SNARKs like Groth16: they need a **Common
Reference String** (CRS) generated from secret randomness. That secret is called
**toxic waste**, and it earns the name — if *anyone* keeps a copy, they can
**forge proofs and mint coins undetectably**. Counterfeit money, no alarm bells.

So how do you generate a secret that *nobody* keeps? You hold a ceremony.

- **Powers of Tau** is the first, **circuit-independent** phase of the
  multi-party computation (MPC) ceremony. It produces reusable parameters that
  later circuits build on.
- The **security model is "1-of-N honest":** many participants each inject their
  own randomness, and the setup is safe **as long as at least one** participant
  genuinely **destroyed** their secret. Everyone else can be compromised.
- **Sprout** used **6 participants**, who physically destroyed the hardware
  afterward. **Sapling's** ceremony had **~90 participants** — more people, more
  margin, since you only need one honest shredder.

> **Analogy:** trusted setup is a **group ritual where everyone shreds a piece of
> a master key**. As long as *one* person truly shredded theirs, no one can
> reassemble it to counterfeit. The nightmare is everyone secretly keeping a copy.

<FillBlank
  client:visible
  question="Recall the security model in your own words."
  text="A trusted setup is safe under the {{1-of-N|one-of-n}} honest model: it holds as long as at least {{one}} participant truly destroyed their share of the secret."
  explanation="1-of-N honest: a single honest participant who destroyed their randomness is enough to keep the whole setup secure, even if everyone else cheated."
/>

Then **Halo 2** showed up and asked: what if we just... didn't do the ritual?

**Halo 2 removes trusted setup entirely.** It uses an **inner-product-argument**
polynomial commitment (in the spirit of Bulletproofs), so there is **no toxic
waste to begin with**, plus **recursive proof composition**. **Orchard** is built
on Halo 2 → **no ceremony, no toxic-waste risk, nothing to trust**.

> **Analogy:** Halo 2 is **no ritual at all**. There's no master key to shred,
> because one was never made.

<Callout type="success" title="The one-line summary">
Notes replace UTXOs · commitments are public fingerprints · the Merkle tree
proves a note exists without saying which · nullifiers prevent double-spends
without revealing the note · homomorphic value commitments prove balance without
revealing amounts · and Orchard (Halo 2) needs no trusted setup.
</Callout>

<FillBlank
  client:visible
  question="Spaced recall — tie it back to the very first idea you met."
  text="A {{nullifier}} stops a note from being spent twice without revealing which note it was, and Orchard's proving system, {{Halo 2}}, removes the trusted setup entirely."
  explanation="The nullifier (from the cryptographic-objects section) gives integrity without visibility; Halo 2 (the newest proving system) drops the trusted-setup ceremony altogether."
/>

## The big picture

<MindMap
  client:visible
  title="Inside Zcash in one picture"
  caption="Each branch is one section of this lesson — chunk them together."
  root={{
    label: 'Inside Zcash',
    children: [
      {
        label: 'Addresses',
        children: [
          { label: 't-addr public' },
          { label: 'z-addr shielded' },
          { label: 'u1 Unified bundles both' },
        ],
      },
      {
        label: 'Crypto objects',
        children: [
          { label: 'Note replaces UTXO' },
          { label: 'Commitment hides and binds' },
          { label: 'Merkle tree proves membership' },
          { label: 'Nullifier stops double-spend' },
          { label: 'Value commitments sum to zero' },
        ],
      },
      {
        label: 'z to z spend',
        children: [
          { label: 'Prove privately' },
          { label: 'Publish nullifier and commitment' },
        ],
      },
      {
        label: 'Pools',
        children: [
          { label: 'Sprout 2016' },
          { label: 'Sapling 2018' },
          { label: 'Orchard NU5 2022' },
        ],
      },
      {
        label: 'Trusted setup',
        children: [
          { label: 'Toxic waste risk' },
          { label: '1-of-N honest' },
          { label: 'Halo 2 removes it' },
        ],
      },
    ],
  }}
/>

## Check yourself

<Quiz
  client:visible
  questions={[
    {
      question: 'What does a nullifier accomplish when a shielded note is spent?',
      options: [
        { text: "It publicly reveals the note's value so nodes can verify the amount" },
        { text: 'It uniquely marks the note as spent (preventing double-spends) without revealing which note it was', correct: true },
        { text: 'It encrypts the memo field so only the recipient can read it' },
        { text: 'It generates the trusted-setup parameters for the transaction' },
      ],
      explanation:
        'A nullifier is a unique per-note serial revealed at spend time. Nodes keep a nullifier set; a repeat means a double-spend and is rejected. Critically, it does NOT reveal which note it corresponds to.',
    },
    {
      question: 'Which Zcash shielded pool requires NO trusted setup?',
      options: [
        { text: 'Sprout' },
        { text: 'Sapling' },
        { text: 'Orchard', correct: true },
        { text: 'All of them need one' },
      ],
      explanation:
        'Orchard is built on Halo 2, which uses an inner-product-argument commitment and recursive composition — no toxic waste, no ceremony. Sprout and Sapling both relied on trusted-setup MPC ceremonies.',
    },
    {
      question: "Which curve does Sapling's proof system (Groth16) operate over?",
      options: [
        { text: 'Jubjub' },
        { text: 'BLS12-381', correct: true },
        { text: 'Pallas + Vesta' },
        { text: 'BN-254' },
      ],
      explanation:
        "Sapling uses Groth16 over BLS12-381. Jubjub is the in-circuit curve (over BLS12-381's scalar field), NOT the proof curve — a classic mixup.",
    },
    {
      question: 'How do homomorphic value commitments prove a shielded transaction balances?',
      options: [
        { text: 'The proof reveals that the input and output amounts are equal' },
        { text: 'Input and output value commitments must sum to zero, proving inputs = outputs without revealing amounts', correct: true },
        { text: 'A trusted third party compares the amounts off-chain' },
        { text: 'The Merkle tree stores each amount as a leaf for auditing' },
      ],
      explanation:
        'Pedersen value commitments are homomorphic, so input and output commitments must sum to zero. That proves inputs equal outputs while the amounts stay sealed — the proof never reveals the numbers.',
    },
  ]}
/>

## Next: actually using it (and the mistakes that betray people)

You now know the parts: notes, commitments, the Merkle tree, nullifiers, value
commitments, the pools, and why Halo 2 made trusted setup obsolete. In the
**final lesson** we put it to work — **making a genuinely anonymous transaction**
end to end — and, more importantly, cataloguing the **rookie mistakes that quietly
unmask people** (amount fingerprinting, careless deshielding, address reuse, and
timing tells). The cryptography is bulletproof; humans, less so.
