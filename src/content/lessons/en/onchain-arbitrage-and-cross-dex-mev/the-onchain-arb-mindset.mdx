---
title: "The On-chain Arb Mindset"
description: "Relative value ported to a venue with no shorting and atomic settlement: why on-chain arbitrage is both safer than TradFi stat-arb (no inventory risk) and harsher (you compete in an auction you usually lose)."
topic: "en/onchain-arbitrage-and-cross-dex-mev"
order: 1
minutes: 9
updated: 2026-06-18
---

import { Callout, MCQ, FillBlank, Categorize, MatchConcepts, MindMap, Quiz, Reveal, ConstantProductCurve } from '@/components/react';

You already know how prices *should* line up: the same asset shouldn't trade at two different prices at the same instant. On-chain, that old TradFi instinct survives — but the venue it lands in is so different that the trade changes shape, sheds some risks, and grows a brand-new one. This lesson rewires the pairs-trading instinct for a world of automated market makers, all-or-nothing transactions, and an auction you mostly lose.

## Relative value, ported to a hostile venue

<MCQ
  client:visible
  pretest
  question="An arbitrageur sees ETH at 2000 USDC on DEX A and 2010 USDC on DEX B. In classic pairs trading, you'd profit when the two prices reconcile. On-chain, what actually causes them to reconcile?"
  options={[
    { text: "Your own trade moves the pool prices toward each other", correct: true, explanation: "Right. You buy on the cheap pool (pushing its price up) and sell on the dear pool (pushing its price down). Convergence is something you cause, not something you wait for." },
    { text: "A central exchange operator manually re-pegs both prices", explanation: "There is no operator. DEX prices are set by reserves and the people trading against them." },
    { text: "Arbitrage is illegal on-chain, so the gap just sits there", explanation: "It's not illegal — and a sitting gap is exactly the free money searchers race to grab." },
    { text: "Block validators are required to equalize prices each block", explanation: "Validators order transactions; they don't equalize prices as a duty." },
  ]}
/>

In a classic **pairs trade**, you spot two prices that *must* belong together — two share classes of the same company, gold in London versus gold in New York — and you bet they'll **reconcile**. In TradFi, you then wait, because convergence is a *market force*: other people's flow eventually drags the prices back together.

On-chain, the twist is brutal and beautiful: **convergence isn't a force you wait for — it's a thing your own trade does.** A decentralized exchange (DEX) using an **automated market maker (AMM)** prices an asset purely from the ratio of tokens in its pool, following the constant-product rule $x \cdot y = k$. When you buy ETH from the cheap pool, you remove ETH and add USDC, *walking the price up the curve*. When you sell into the dear pool, you push its price down. The gap closes **because you traded**, not because the market felt like it.

<ConstantProductCurve
  client:visible
  title="Your trade walks the price along the curve"
  caption="An AMM prices from reserves alone. Sell more ETH into the pool and you slide down the curve — each unit you sell gets a worse price. That self-impact is exactly what closes an arbitrage gap: the act of arbitraging moves the price toward fair value."
/>

So define it precisely. **On-chain arbitrage** is a **relative-value** trade (you bet two prices that should match will match), it is **market-neutral** (you don't care if ETH goes up or down, only that the two venues disagree), and it is **self-closing** (the very trade that captures the gap also erases it).

<MatchConcepts
  client:visible
  question="Match each property of on-chain arbitrage to what it means."
  pairs={[
    { term: "Relative value", definition: "You bet on the gap between two prices, not on either price's direction" },
    { term: "Market-neutral", definition: "Profit doesn't depend on ETH rising or falling, only on the venues disagreeing" },
    { term: "Self-closing", definition: "The trade that captures the gap is also what makes the gap disappear" },
  ]}
/>

<Callout variant="warning" title="Convergence isn't free — you paid for it">
The romantic image is "prices snap back to fair value." The unromantic reality: *you* moved them, and each unit you traded got a slightly worse price than the last (that's the curve bending away from you). The gap you actually capture is smaller than the gap you saw on screen. More on that "slippage" tax later.
</Callout>

### When it matters

Every other lesson in this topic is a special case of this one idea. Cross-DEX, triangular, and sandwich plays are all just *which* prices you bet must reconcile and *how* you route the trade to force it. Get the mindset — relative value, market-neutral, self-closing — and the rest is plumbing.

## No shorting, so the trade changes shape

<MCQ
  client:visible
  pretest
  question="A TradFi stat-arb desk goes long the cheap leg and short the dear leg. On most DEX venues you cannot borrow-and-sell. How does on-chain arbitrage capture a price gap instead?"
  options={[
    { text: "Buy where the asset is cheap and sell where it is dear, in the same breath", correct: true, explanation: "Exactly. With no native shorting, arbitrage becomes long-only round-tripping: acquire cheap, dispose dear, simultaneously." },
    { text: "Short the dear venue and hold the position for weeks", explanation: "Most DEX venues offer no native shorting, and holding for weeks reintroduces the very risk on-chain arb avoids." },
    { text: "Wait for the cheap venue to issue more tokens", explanation: "Venues don't issue tokens to close arbitrage gaps." },
    { text: "Email the protocol team to fix the price", explanation: "There is no one to email; prices come from pool reserves." },
  ]}
/>

In TradFi statistical arbitrage, the two-legged shape is the whole point: **long** the underpriced leg, **short** the overpriced leg, and you're hedged against the broad market. To short, you *borrow* the asset and sell it, promising to return it later.

On most DEX venues there's no native borrow-and-sell mechanism. So arbitrage goes **long-only** and changes shape: instead of holding a long leg and a short leg, you **buy where it's cheap and sell where it's dear in the same breath**, routing the same units straight through. You never hold a naked short; you round-trip.

| | TradFi stat-arb | On-chain arbitrage |
|---|---|---|
| Legs | Long cheap **and** short dear | Long-only round-trip |
| Holding period | Hours to weeks | One transaction |
| Capital | Margin / borrowed shares | Often a **flash loan** |
| Market exposure | Hedged via the short leg | Neutral by closing instantly |

Where does the capital come from if you can't short and don't want to tie up your own millions? **Flash loans** (previewed here, taught in depth later): you borrow a large amount with *no collateral*, on the condition that you repay it **within the same transaction**. It substitutes for leverage — you wield big size for a few milliseconds, then give it all back, keeping only the spread.

<FillBlank
  client:visible
  question="Fill in how the trade shape differs."
  text="Because most DEX venues offer no native shorting, on-chain arbitrage is {{long-only|two-legged|short-biased}}, capturing the gap by buying cheap and selling dear in {{one transaction|several weeks|two accounts}}, often funded by a {{flash loan|margin call|dividend}}."
  explanation="No shorting forces a long-only round-trip, settled in a single transaction, frequently bankrolled by an uncollateralized flash loan repaid in that same transaction."
/>

<Callout variant="info" title="Long-only isn't the same as bullish">
You're long the asset for an instant, but you immediately sell it on the other venue — your *net* exposure rounds to zero. Long-only describes the mechanics of capture, not a bet on the asset going up.
</Callout>

### When it matters

The no-shorting constraint is why on-chain arb is so often paired with flash loans, and why "do you have the inventory?" stops being the binding question. The binding question becomes "can you route the buy and the sell so the whole thing nets a profit?" — which is the next section.

## Atomic settlement: the gift and the catch

<MCQ
  client:visible
  pretest
  question="An on-chain arbitrage bundles a buy and a sell into one atomic transaction. The buy fills, but by the time the sell would execute, the gap is gone and the trade would lose money. What happens?"
  options={[
    { text: "The entire transaction reverts — as if it never happened (minus gas)", correct: true, explanation: "That's atomicity: all-or-nothing. An unprofitable leg makes the whole transaction revert, so you're never left holding a losing position." },
    { text: "You're stuck holding the ETH you bought, hoping it recovers", explanation: "That would be inventory risk — exactly what atomicity removes. The buy doesn't stick if the sell fails." },
    { text: "The protocol auto-completes the sell at a loss to be fair", explanation: "Nothing auto-completes a losing leg; the transaction simply reverts." },
    { text: "Your funds are locked until the next block", explanation: "Reverts release immediately within the same block; nothing is locked overnight." },
  ]}
/>

Here's the part that should make a TradFi trader jealous. On-chain, you can bundle every leg into **one atomic transaction**. **Atomic** means **all-or-nothing**: either *every* step succeeds and settles together, or the whole thing **reverts** and it's as if nothing happened (you forfeit only the gas — the small fee to attempt it).

That's **the gift**. If any leg would be unprofitable — the gap closed, someone front-ran you, the route turned sour — the transaction reverts and you walk away clean. There is **no inventory risk** (you never get stuck holding the asset), **no leg risk** (you can't get one leg filled and the other rejected), and **no overnight risk** (nothing carries to the next block). You either grab the spread or you grab nothing.

Contrast TradFi execution. To arbitrage gold between London and New York, you *sprint*: buy in one venue, then race to sell in the other while **holding inventory** in between. Prices move during the sprint. The other leg might slip, or fail, or fill at a worse price. You wear that risk on your own balance sheet.

<Categorize
  client:visible
  question="Sort each risk by whether atomic on-chain settlement removes it or a TradFi sprint still carries it."
  buckets={['Removed by atomicity', 'Still present in TradFi sprint']}
  items={[
    { text: "Inventory risk while holding the asset between legs", bucket: 'Still present in TradFi sprint' },
    { text: "One leg fills, the other is rejected", bucket: 'Removed by atomicity' },
    { text: "Overnight exposure carried to the next session", bucket: 'Still present in TradFi sprint' },
    { text: "The price moves against you during the round-trip", bucket: 'Removed by atomicity' },
  ]}
  explanation="Atomicity makes the round-trip a single all-or-nothing event: leg risk, mid-trade price drift, and inventory all vanish because an unprofitable bundle simply reverts. The TradFi sprint, holding real inventory between venues, carries every one of those."
/>

<Callout variant="warning" title="The catch hides in plain sight">
Atomicity removes execution risk, but it adds a constraint: the entire trade must clear **inside one transaction that you don't get to order**. You can compose the perfect bundle and still never land it — because *when* and *whether* your transaction enters the block isn't yours to decide. That's the whole next section.
</Callout>

### When it matters

Atomicity is why beginners over-estimate on-chain arb ("free money, no risk!") and pros don't. The risk didn't disappear — it *moved*, from "will my legs fill?" to "will I win the right to be included, and at what cost?"

## You don't set the order — someone auctions it

<MCQ
  client:visible
  pretest
  question="You've built a flawless atomic arbitrage. Who decides whether your transaction actually lands in the block, and in what position relative to competitors?"
  options={[
    { text: "Builders and validators who order transactions — not you, the searcher", correct: true, explanation: "You compose the opportunity (a searcher), but the right to order transactions belongs to builders/validators. They auction that ordering, and you bid for it." },
    { text: "You do — the first to hit 'send' always lands first", explanation: "'First to send' means nothing on its own; ordering is decided by whoever builds the block, usually via a bid auction." },
    { text: "The DEX smart contract picks the most profitable trader", explanation: "The contract just executes whatever order it's handed; it doesn't choose who goes first." },
    { text: "It's random and uniform across all participants", explanation: "It's an economic auction, not a fair lottery — higher bids win ordering." },
  ]}
/>

You spotted the gap. You built the perfect atomic bundle. And now the cruelest fact in the business: **you don't set the order**. In this game you're a **searcher** — you find and assemble the opportunity — but the **right to order** transactions inside a block belongs to **builders and validators** (exactly the MEV-and-ordering machinery you met earlier). They run an auction for that ordering, and you have to *bid* to win your spot.

This is why so many beautiful arbitrages revert into thin air: someone bid more to be placed ahead of you, took the gap first, and your transaction — arriving a position too late — finds nothing left and reverts. The opportunity was real; the *right to act on it first* was the scarce thing, and it was auctioned away.

Worse, the auction is competitive enough that it **bids most of your profit away**. If a gap is worth 100 USDC, rival searchers will keep raising their bids to the ordering layer until almost all of that 100 is paid out as priority fees / bribes — whoever is willing to keep the *least* sliver wins. That whole **cost stack** (gas, priority fees, builder payments, and the slippage from earlier) is a later lesson; for now, just internalize the shape: **gross edge minus the auction equals your take, and the auction is hungry.**

<FillBlank
  client:visible
  question="Fill in who controls ordering and what it costs you."
  text="In on-chain arbitrage you're a {{searcher|validator|market maker}} who assembles the trade, but the right to order it belongs to {{builders and validators|the DEX contract|other searchers}}. Because that ordering is {{auctioned|free|randomized}}, competition bids away {{most of|none of|exactly half}} your gross profit."
  explanation="Searchers find opportunities; builders/validators own ordering and auction it; competitive bidding for that ordering eats most of the gross edge."
/>

<Callout variant="tip" title="The mindset, in one line">
On-chain arb is *safer* than TradFi stat-arb on the dimension everyone fears (no inventory, leg, or overnight risk — thanks, atomicity) and *harsher* on a dimension TradFi barely has: you compete in an auction for ordering that you usually lose. Master both halves and you understand why this is a margin business, not a magic one.
</Callout>

### When it matters

This reframes the entire topic. "Find the gap" is the easy 10%. "Win the right to act on it before everyone else, and still keep a profit after the auction" is the hard 90% — and it's what the cost-stack and bidding lessons drill into next.

## Recap

<MindMap
  client:visible
  title="The on-chain arb mindset"
  caption="Relative value ported to a venue with no shorting and atomic settlement, where ordering is auctioned away."
  outlineLabel="Text outline"
  root={{
    label: 'On-chain arb mindset',
    children: [
      {
        label: 'Relative value',
        children: [
          { label: 'Bet two prices reconcile' },
          { label: 'Market-neutral' },
          { label: 'Self-closing trade' },
        ],
      },
      {
        label: 'No shorting',
        children: [
          { label: 'Long-only round-trip' },
          { label: 'Buy cheap sell dear' },
          { label: 'Flash loans for size' },
        ],
      },
      {
        label: 'Atomic settlement',
        children: [
          { label: 'All-or-nothing' },
          { label: 'Reverts if unprofitable' },
          { label: 'No inventory risk' },
        ],
      },
      {
        label: 'You dont order it',
        children: [
          { label: 'You are a searcher' },
          { label: 'Builders order blocks' },
          { label: 'Auction eats the edge' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Check the mindset"
  questions={[
    {
      question: "On-chain, what forces two disagreeing DEX prices to converge?",
      options: [
        { text: "The arbitrageur's own trade walking each pool's price along its curve", correct: true },
        { text: "A central operator manually re-pegging both venues" },
        { text: "Validators are legally required to equalize prices" },
        { text: "The prices simply drift back over several weeks" },
      ],
      explanation: "Convergence isn't a force you wait for — buying the cheap pool and selling the dear one moves both prices together. The trade closes the gap.",
    },
    {
      question: "Why is on-chain arbitrage long-only instead of long/short like TradFi stat-arb?",
      options: [
        { text: "Most DEX venues offer no native borrow-and-sell, so you round-trip: buy cheap, sell dear", correct: true },
        { text: "Shorting is banned by every blockchain protocol" },
        { text: "Long-only trades are tax-free on-chain" },
        { text: "You're betting the asset will rise" },
      ],
      explanation: "No native shorting reshapes the trade into a long-only round-trip; flash loans supply the size.",
    },
    {
      question: "An atomic arbitrage transaction's sell leg would lose money. What happens?",
      options: [
        { text: "The whole transaction reverts — all-or-nothing — so you're never stuck with the asset", correct: true },
        { text: "You hold the asset and wait for it to recover" },
        { text: "The protocol completes the trade at a loss for fairness" },
        { text: "Half the trade settles and half is refunded" },
      ],
      explanation: "Atomicity means all legs settle together or none do. An unprofitable bundle reverts, killing inventory, leg, and overnight risk.",
    },
    {
      question: "Which risk does atomic on-chain settlement remove that a TradFi cross-venue sprint still carries?",
      options: [
        { text: "Holding inventory while one leg is filled and the other isn't yet", correct: true },
        { text: "The risk of paying gas to attempt the trade" },
        { text: "The risk that competitors also see the gap" },
        { text: "The risk that ordering is auctioned" },
      ],
      explanation: "Atomicity erases inventory and leg risk; the sprint between London and New York wears exactly that risk on the balance sheet. Gas and auction competition are separate, on-chain-specific costs.",
    },
    {
      question: "You assembled a flawless arbitrage but it reverted with no profit. The most on-chain-specific reason is:",
      options: [
        { text: "A rival searcher bid more for ordering, took the gap first, and your transaction landed too late", correct: true },
        { text: "The blockchain randomly rejected your transaction" },
        { text: "You forgot to short the dear leg" },
        { text: "Atomic transactions always revert the first time" },
      ],
      explanation: "Searchers don't set ordering — builders/validators auction it, and competition bids most of the edge away. Losing that auction is why a real opportunity yields nothing.",
    },
  ]}
/>
