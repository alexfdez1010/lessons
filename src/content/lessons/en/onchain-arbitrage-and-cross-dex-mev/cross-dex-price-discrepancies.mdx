---
title: "Cross-DEX Price Discrepancies"
description: "How independent AMM curves drift apart, the exact constant-product math of buying cheap and selling dear, and why the optimal arbitrage size is finite — not as big as possible."
topic: "en/onchain-arbitrage-and-cross-dex-mev"
order: 2
minutes: 11
updated: 2026-06-18
---

import { Callout, MCQ, FillBlank, Categorize, MatchConcepts, MindMap, Quiz, Reveal, ConstantProductCurve, CrossDexArb } from '@/components/react';

The same ETH can cost 1900 USDC on one exchange and 2100 USDC on another *at the very same instant*. That isn't a glitch — it's the default state of a world built from independent automated market makers. This lesson is about why that gap opens, exactly how much money sits inside it, and the deeply unintuitive reason you can't just scoop up the whole thing.

## Why two pools drift apart

<MCQ
  client:visible
  pretest
  question="Two different DEXes both let you swap ETH↔USDC. A big trader buys a pile of ETH on DEX A only. What happens to the ETH price on DEX B?"
  options={[
    { text: "Nothing directly — B has its own reserves and its own curve, so a trade on A doesn't touch it.", correct: true, explanation: "Exactly. There's no shared order book. Each pool prices off its own reserves, so a swap on A moves A and leaves B untouched — which is precisely how the two prices drift apart." },
    { text: "B's price drops to match, because arbitrage is instant.", explanation: "Arbitrage isn't a law of physics that fires automatically — it's a trade somebody has to send. Until they do, B sits at its old price." },
    { text: "B's price rises immediately, because all DEXes share a global price feed.", explanation: "AMMs don't share a price feed. That's the whole reason discrepancies exist." },
  ]}
/>

Picture two lemonade stands on opposite ends of a beach. They don't share a till, a menu, or a manager. If a thirsty crowd swarms the north stand, *its* prices creep up; the south stand, oblivious, keeps selling cheap. Nothing connects them except your willingness to walk between them.

An **automated market maker (AMM)** is exactly that lemonade stand. Each pool holds two reserves — say ETH and USDC — and prices every trade off the **constant-product invariant**:

$$x \cdot y = k$$

Here $x$ is the ETH reserve, $y$ is the USDC reserve, and $k$ is a constant the pool defends on every swap. The **spot price** of ETH (in USDC) is just the ratio of reserves:

$$P = \frac{y}{x}$$

Crucially, there is **no shared order book** across DEXes. Uniswap's ETH/USDC pool and SushiSwap's ETH/USDC pool are two separate piles of coins with two separate $k$ values. A swap on one pool changes only *that* pool's reserves, and therefore only *that* pool's price.

So when traders on different venues do their own thing — some buying on A, some selling on B — the two prices wander off in different directions. They diverge and *stay* diverged until someone deliberately trades the gap shut. That someone is an arbitrageur, and the gap is their paycheck.

<Callout variant="info" title="Same asset, two prices, zero contradiction">
There's no arbitrage-free law being broken here. A price is just "what this specific pool charges given its current reserves." Two pools with different reserves quote different prices for the same token — that's arithmetic, not a market failure.
</Callout>

<MatchConcepts
  client:visible
  question="Match each piece of AMM plumbing to what it actually is."
  pairs={[
    { term: "Constant-product invariant", definition: "The rule x · y = k that the pool defends on every swap." },
    { term: "Spot price", definition: "The reserve ratio y / x at this instant." },
    { term: "Why prices diverge", definition: "Each pool has its own reserves; a trade moves only that pool." },
    { term: "What closes the gap", definition: "An arbitrageur deliberately trading across both pools." },
  ]}
  checkLabel="Check"
  retryLabel="Try again"
  allCorrectLabel="✓ All correct"
/>

### When it matters

Every time you see "the price of ETH," ask *whose* price. On-chain there is no single number — there's a price per pool, and the spread between them is the raw material of cross-DEX MEV. The bigger and more fragmented the liquidity, the more often these gaps appear.

## Buy cheap, sell dear — the constant-product math

<MCQ
  client:visible
  pretest
  question="Pool A quotes ETH at 1900 USDC; Pool B quotes 2100 USDC. Ignoring price impact, what's the gross spread on flipping 1 ETH (buy on A, sell on B)?"
  options={[
    { text: "200 USDC", correct: true, explanation: "Buy 1 ETH for 1900 on the cheap pool, sell it for 2100 on the dear pool. 2100 − 1900 = 200 USDC gross, before any price impact eats into it." },
    { text: "2000 USDC", explanation: "That's roughly the average price, not the spread. The spread is the difference between the two quotes." },
    { text: "0 USDC — arbitrage always nets zero.", explanation: "Arbitrage nets toward zero as the gap closes, but a 200 USDC gap is very much open money at the first marginal unit." },
  ]}
/>

Our two canonical pools:

| Pool | ETH reserve ($x$) | USDC reserve ($y$) | Spot price $P = y/x$ |
|---|---|---|---|
| **A (cheap — buy here)** | 100 | 190,000 | \$1900 |
| **B (dear — sell here)** | 100 | 210,000 | \$2100 |

The strategy writes itself: **buy ETH where it's cheap (A), sell it where it's dear (B), pocket the difference.** At the very first marginal unit, the math is gloriously simple — buy 1 ETH for \$1900 on A, sell it for \$2100 on B, and the gross spread is:

$$2100 - 1900 = 200 \text{ (USDC per ETH)}$$

That "200 USDC for moving one token between two pools" is the headline number that makes arbitrageurs salivate. It's also a *lie of omission*, because it assumes the prices don't budge while you trade. They do. The island below lets you slide the arbitrage size and watch the naive spread get devoured by reality.

<CrossDexArb client:visible />

Notice the left edge of the slider: at a tiny size, you really do capture roughly 200 USDC per ETH. The trouble starts the moment you get greedy.

<FillBlank
  client:visible
  question="Fill in the first-unit arbitrage."
  text="Buy ETH on the {{cheap|dear|frozen}} pool at 1900 USDC, sell on the dear pool at 2100 USDC, for a gross spread of {{200 USDC|2000 USDC|20 USDC}} on the first ETH — before {{price impact|gas fees|slippage tolerance}} bites."
  explanation="Buy low (the cheap pool, A), sell high (the dear pool, B). The first ETH nets the full 200 USDC gap; price impact then erodes every unit after it."
  instructions="Choose the correct option for each blank and check."
  checkLabel="Check"
  retryLabel="Try again"
/>

### When it matters

The naive spread tells you whether an opportunity *exists*; it never tells you how much you can *extract*. Confusing the two is how a bot "sees" a 200-USDC-per-ETH gap, fires a huge trade, and walks away with far less — or a loss. The real number always comes from the curve, never from the quote.

## The trade moves the prices together

<MCQ
  client:visible
  pretest
  question="As you buy more and more ETH on the cheap pool A, what happens to A's quoted ETH price?"
  options={[
    { text: "It rises — each ETH you remove leaves fewer in the pool, so the next one costs more.", correct: true, explanation: "Buying ETH means taking ETH out of A and putting USDC in. The reserve ratio y/x climbs, so A's price walks up the curve toward B's." },
    { text: "It stays flat until the pool is empty.", explanation: "AMM prices move continuously with every unit traded — there's no flat zone. That's the whole point of the curve." },
    { text: "It falls, because you're adding USDC liquidity.", explanation: "Adding USDC while removing ETH raises y/x, which raises the price. Liquidity provision is a different action entirely." },
  ]}
/>

Here's the twist that makes arbitrage self-extinguishing. When you **buy ETH on A**, you hand the pool USDC and take ETH out. A's reserves shift: $x$ falls, $y$ rises, so its price $y/x$ **climbs**. When you **sell that ETH on B**, you do the reverse there: B's $x$ rises, $y$ falls, so B's price **drops**.

So the two prices march toward each other from both ends:

- **Pool A** (started at 1900): walks *up* as you buy.
- **Pool B** (started at 2100): walks *down* as you sell.

Keep going and they meet in the middle — at roughly **1999 USDC** for the trade sizes around our optimum. The visual below shows a single pool climbing its own curve as you sell tokens into it; the spot price (the slope of the line from the origin) steepens with every unit.

<ConstantProductCurve
  client:visible
  title="One pool walking up its curve"
  caption="Sell ETH into a pool and you slide along the x·y = k hyperbola: the ETH reserve drops, the USDC reserve rises, and the spot price (the reserve ratio) shifts with every unit. In arbitrage, pool A climbs this way while pool B slides the other — until they meet."
/>

The profound bit: **the act that captures the spread is the act that closes it.** You don't arbitrage a gap *and then* watch it vanish — your own trade *is* the vanishing. Push enough volume through and the two pools quote the same price, the gap is gone, and there's nothing left to capture. Arbitrage is a snake eating its own tail.

<Callout variant="warning" title="The gap is a melting ice cube">
Every unit you trade shrinks the remaining spread. So the marginal profit per ETH is highest on your *first* unit and falls toward zero as you continue. If you size your trade as if all units earned the full 200-USDC gap, you've badly overcounted — the back of your trade earns almost nothing.
</Callout>

<Categorize
  client:visible
  question="As you run the arbitrage (buy on A, sell on B), sort what happens to each pool's price."
  buckets={['Price walks up', 'Price walks down']}
  items={[
    { text: "Pool A (the cheap pool you buy from)", bucket: 'Price walks up' },
    { text: "Pool B (the dear pool you sell into)", bucket: 'Price walks down' },
    { text: "The pool whose ETH reserve is shrinking", bucket: 'Price walks up' },
    { text: "The pool whose ETH reserve is growing", bucket: 'Price walks down' },
  ]}
  explanation="Buying ETH out of A shrinks its ETH reserve and raises y/x — price up. Selling ETH into B grows its ETH reserve and lowers y/x — price down. They converge toward ~1999 USDC."
  checkLabel="Check"
  retryLabel="Try again"
  allCorrectLabel="✓ All correct"
/>

### When it matters

This convergence is why no one stays rich off a single gap forever, and why the *first* bot to a discrepancy gets the cream. By the time the second arbitrageur arrives, the first one's trade has already flattened most of the spread. Speed and ordering — the heart of MEV — exist precisely because the opportunity self-destructs on contact.

## Why the optimal size is finite

<MCQ
  client:visible
  pretest
  question="If each extra ETH closes the gap a little, what does total arbitrage profit look like as a function of trade size?"
  options={[
    { text: "Hump-shaped — it rises, peaks at an optimal size, then falls.", correct: true, explanation: "Early units earn a fat (shrinking) margin, so profit rises. Past the optimum the prices have crossed and extra units lose money, dragging total profit back down. A clean hump." },
    { text: "Strictly increasing — bigger trade, bigger profit, always.", explanation: "That would be true only if the gap never closed. It does, with every unit, so profit eventually turns down." },
    { text: "Flat — profit is the same at every size.", explanation: "Price impact guarantees it isn't flat: marginal profit per unit falls as you trade, so the total can't be linear." },
  ]}
/>

Now the punchline. Naively you'd think: 200 USDC of gross edge per ETH, and the pools hold 100 ETH each — so just trade a *huge* amount and get rich. Wrong. Because every unit you trade closes the gap, your **marginal profit per ETH falls** as you go. Eventually it hits zero (the prices have met). Push *past* that and the prices **cross** — you're now buying on the pool that's become *expensive* and selling into the one that's become *cheap*. You start handing the spread *back*.

That makes total profit **hump-shaped** in trade size: rising, peaking, then falling. For our canonical pools the peak sits at about **2.5 ETH**, netting roughly **250 USDC**:

| Arbitrage size | A's price after | B's price after | Net profit |
|---|---|---|---|
| 0.5 ETH | ~\$1929 | ~\$2069 | ~\$70 |
| 1.5 ETH | ~\$1959 | ~\$2040 | ~\$190 |
| **2.5 ETH (optimal)** | **~\$1999** | **~\$1999** | **~\$250** |
| 4.0 ETH | ~\$2061 | ~\$1938 | ~\$210 |
| 6.0 ETH | ~\$2133 | ~\$1872 | ~\$60 |

Read the table top to bottom: profit climbs to its \$250 peak at 2.5 ETH — exactly where the two prices kiss at ~1999 USDC — and then *declines* even though you're trading more. By 6 ETH you've shoved A's price clean above B's and given most of the edge back.

What sets the cap is **depth** — how much liquidity each pool holds. Deeper pools (bigger reserves) move less per unit traded, so the gap closes slowly and the optimal trade is larger. Shallow pools impact-out almost immediately, so even a tiny arbitrage exhausts the edge. The optimum is wherever the *marginal* profit of one more ETH hits zero — not "as big as the reserves allow."

<Callout variant="warning" title="'As big as possible' is the rookie's ruin">
Maximizing trade size does not maximize profit — it maximizes price impact. Past the optimum you're paying to undo your own edge. The right size is the one where the next ETH would earn zero, and that's usually a small fraction of the pool.
</Callout>

<MCQ
  client:visible
  question="An arbitrageur could trade 2.5 ETH (net ~250 USDC) or 6 ETH (net ~60 USDC) across our two pools. Why is the smaller trade more profitable?"
  options={[
    { text: "At 6 ETH the prices have already converged and started to cross, so the extra units earn negative margin and drag total profit down.", correct: true, explanation: "Past ~2.5 ETH the gap is closed; each further unit buys high and sells low relative to the converged price, clawing back profit already earned." },
    { text: "Larger trades always pay more gas, and gas alone eats the 190 USDC difference.", explanation: "Gas matters in practice, but the dominant effect here is price impact crossing the pools, not fees." },
    { text: "The 6 ETH trade is rejected by the pool for exceeding reserves.", explanation: "6 ETH is well within 100-ETH reserves; the pool happily fills it — it's just a bad idea because the prices have crossed." },
  ]}
/>

### When it matters

Sizing *is* the strategy. Two bots see the identical 200-USDC gap; the one that solves for the hump's peak — and stops there — out-earns the one that fires the biggest possible trade. In the next lessons this exact "find the optimum, then race to land it first" problem is what the whole MEV supply chain is built around.

## Recap

<MindMap
  client:visible
  title="Cross-DEX price discrepancies, in one map"
  caption="From why gaps open to why you can't take all of it."
  root={{
    label: 'Cross-DEX gaps',
    children: [
      {
        label: 'Why they open',
        children: [
          { label: 'No shared order book' },
          { label: 'Each pool prices off own reserves' },
          { label: 'A trade moves only that pool' },
        ],
      },
      {
        label: 'The naive edge',
        children: [
          { label: 'Buy cheap on A 1900' },
          { label: 'Sell dear on B 2100' },
          { label: 'Gross spread 200 per ETH' },
        ],
      },
      {
        label: 'Self-extinguishing',
        children: [
          { label: 'Buying walks A up' },
          { label: 'Selling walks B down' },
          { label: 'Converge near 1999' },
        ],
      },
      {
        label: 'Optimal size finite',
        children: [
          { label: 'Profit is hump-shaped' },
          { label: 'Optimum near 2.5 ETH' },
          { label: 'Net about 250 USDC' },
          { label: 'Depth sets the cap' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Cross-DEX discrepancies — final check"
  questions={[
    {
      question: "Why can the same ETH quote 1900 USDC on one DEX and 2100 on another at the same moment?",
      options: [
        { text: "Each AMM prices off its own separate reserves; there's no shared order book linking them.", correct: true },
        { text: "One of the two prices is simply a stale, cached number.", },
        { text: "Exchanges deliberately collude to set different prices.", },
      ],
      explanation: "Independent pools, independent reserves, independent prices. A trade on one doesn't touch the other until an arbitrageur connects them.",
    },
    {
      question: "Ignoring price impact, the gross spread on flipping the first 1 ETH between a 1900 pool and a 2100 pool is:",
      options: [
        { text: "200 USDC", correct: true },
        { text: "2000 USDC", },
        { text: "100 USDC", },
      ],
      explanation: "2100 − 1900 = 200 USDC on the first marginal unit, before the curve fights back.",
    },
    {
      question: "What happens to the cheap pool A's price as the arbitrageur buys ETH from it?",
      options: [
        { text: "It walks up — fewer ETH in reserve raises the y/x ratio.", correct: true },
        { text: "It walks down toward zero.", },
        { text: "It is locked until the trade settles.", },
      ],
      explanation: "Buying removes ETH and adds USDC, raising y/x. A climbs toward B; meanwhile B falls toward A.",
    },
    {
      question: "The deepest idea of the lesson is best stated as:",
      options: [
        { text: "The act that captures the spread is the same act that closes it.", correct: true },
        { text: "Arbitrage profit grows without limit if pools are deep enough.", },
        { text: "Prices on two DEXes can never actually converge.", },
      ],
      explanation: "Your trade is the convergence. There's no 'capture, then later it closes' — they're one motion.",
    },
    {
      question: "Total arbitrage profit as a function of trade size is best described as:",
      options: [
        { text: "Hump-shaped: it rises to a peak at the optimal size, then falls.", correct: true },
        { text: "Strictly increasing with size.", },
        { text: "Constant regardless of size.", },
      ],
      explanation: "Marginal profit shrinks as the gap closes; past the optimum the prices cross and you give the edge back.",
    },
    {
      question: "For our canonical pools, the optimal arbitrage is about 2.5 ETH for ~250 USDC net. Trading 6 ETH instead earns far less mainly because:",
      options: [
        { text: "The prices have already converged and crossed, so the extra units lose money.", correct: true },
        { text: "The pool refuses any trade above 5 ETH.", },
        { text: "Depth increases as you trade, making each ETH cheaper.", },
      ],
      explanation: "Depth is fixed; once the prices meet at ~1999 and cross, every further ETH buys high and sells low, clawing back profit.",
    },
  ]}
  questionLabel="Question"
  ofLabel="of"
  scoreLabel="Your score"
  restartLabel="Restart"
  nextLabel="Next"
  backLabel="Back"
/>
