---
title: "Reinforcement Learning for Trading — Final Exam"
description: "The graded final exam for Reinforcement Learning for Trading: markets as a Markov decision process, reward design and reward hacking, value-based versus policy-gradient methods, optimal execution and Almgren–Chriss, market-making inventory and adverse selection, and the sim-to-real gap that wrecks naive RL backtests."
topic: "en/reinforcement-learning-for-trading"
order: 7
minutes: 18
updated: 2026-06-19
---

import { Callout, FinalExam, MindMap } from '@/components/react';

This is the capstone. Six lessons reframed trading as something supervised learning can never be: a **sequential decision problem**, played against a market that reacts to every move you make. You learned the formal language of a **Markov decision process** — state, action, reward, transition, policy, return, and the discount factor that sets your horizon — and the one feature that separates a market from a video game: **your action moves the environment**. You learned that **reward design** is the hardest, most dangerous part of applied RL, because the agent optimizes the literal scalar you write, not the goal you meant — so a reward without risk and cost terms breeds a reckless overtrader, and any loophole becomes a **reward hack**. You learned the two great algorithm families — **value-based** (Q-learning, DQN, with experience replay and target networks fighting the deadly triad) and **policy-gradient** (REINFORCE, actor–critic, PPO) — and why continuous actions and exploration that *costs real spread* push trading toward policy methods and offline RL. You saw the canonical win, **optimal execution**, where RL re-derives Almgren–Chriss under its assumptions and earns its keep beyond them, and the perilous craft of **market making**, where inventory skew, the Avellaneda–Stoikov reservation price, and adverse selection collide with a reward function full of perverse incentives. And you ended on the lesson that humbles all the rest: the **sim-to-real gap**, where an agent that prints money in a flawed simulator — overfitting its own chosen data — loses it live. No formula sheet, no hints, no take-backs: every answer locks the instant you submit, the wrong options are the exact traps that fool real desks, and your score stays hidden until the end.

<MindMap
  client:visible
  title="Reinforcement Learning for Trading — the whole ladder"
  caption="From a sequential-decision framing to a monitored live agent: six lessons, one thread — extract a fragile edge by acting, not predicting, without letting the agent fool you in simulation."
  root={{
    label: 'Reinforcement Learning for Trading',
    children: [
      {
        label: 'Markets as an MDP',
        children: [
          { label: 'State, action, reward, transition, policy, return' },
          { label: 'Discount factor γ sets the horizon' },
          { label: 'Control not prediction: the action moves the environment' },
        ],
      },
      {
        label: 'Reward design & pitfalls',
        children: [
          { label: 'One scalar per step: risk-adjusted, cost-aware' },
          { label: 'γ too low = myopic overtrading' },
          { label: 'Potential-based shaping; beware reward hacking' },
        ],
      },
      {
        label: 'Value vs policy methods',
        children: [
          { label: 'Q-learning, Bellman, TD; DQN replay + target nets' },
          { label: 'Policy gradient / REINFORCE: continuous, high variance' },
          { label: 'Actor–critic & PPO; explore costs spread → offline RL' },
        ],
      },
      {
        label: 'Optimal execution',
        children: [
          { label: 'Impact vs timing risk; Almgren–Chriss front-loads' },
          { label: 'State = inventory + time + signal; reward = −cost − λ·risk' },
          { label: 'RL re-derives AC, adds signal/nonlinear impact/constraints' },
        ],
      },
      {
        label: 'Market making',
        children: [
          { label: 'Capture the spread; skew quotes against inventory' },
          { label: 'Avellaneda–Stoikov reservation price; adverse selection' },
          { label: 'Reward ignoring inventory = blow-up; perverse incentives' },
        ],
      },
      {
        label: 'The sim-to-real gap',
        children: [
          { label: 'Agent learns the simulator, exploits its flaws' },
          { label: 'RL overfits worse: chooses its own data + huge search' },
          { label: 'Realistic fills, multi-regime, walk-forward, kill-switch' },
        ],
      },
    ],
  }}
/>

<Callout variant="warning" title="One run, one shot">
This exam is **graded and irreversible**. Each question locks the moment you submit it — there is no Back button, no retry, and no Restart. A wrong answer simply fails that question and the exam moves on. Your pass/fail score appears only at the very end. Read every option before you commit.
</Callout>

<FinalExam
  client:visible
  passPercent={70}
  questions={[
    {
      question: "Why is trading a fundamentally poor fit for plain supervised learning, even with a great return forecast?",
      options: [
        { text: "Supervised learning predicts a fixed label from i.i.d. data and never decides HOW MUCH to trade, WHEN to stop, or what to do after your fill moved the price — trading is a sequential decision (control) problem, not a prediction one", correct: true },
        { text: "Supervised learning is too slow to train on market data" },
        { text: "Return forecasts are always wrong, so prediction is pointless" },
        { text: "Supervised learning cannot handle numeric inputs like prices" },
      ],
      explanation: "A forecast is necessary but not sufficient: it tells you what might happen, not the policy (size, timing, exit, response to your own impact). Those are sequential, self-affecting decisions with no ground-truth label to copy — exactly what reinforcement learning, not supervised learning, is built for.",
    },
    {
      question: "In the MDP framing of a market, what specifically does the TRANSITION (dynamics) capture?",
      options: [
        { text: "How the environment moves to the next state given the current state and the agent's action — including the market's own evolution AND the effect of the agent's trade", correct: true },
        { text: "The scalar feedback the agent receives after each action" },
        { text: "The mapping from states to the actions the agent will take" },
        { text: "The cumulative discounted sum of future rewards" },
      ],
      explanation: "The transition P(s′ | s, a) is the world's response: it produces the next state from the current state and action. In markets it bundles exogenous price moves with the endogenous effect of the agent's own order — which is why the action moves the environment.",
    },
    {
      question: "The single biggest structural difference between a market MDP and a typical video-game MDP is that...",
      options: [
        { text: "In a market the agent's ACTION MOVES THE ENVIRONMENT — your trade changes the price/liquidity you face next — whereas a game's world is largely indifferent to your existence", correct: true },
        { text: "Markets have no reward signal, so RL cannot be applied at all" },
        { text: "Markets are fully deterministic while games are stochastic" },
        { text: "Markets have a finite state space and games do not" },
      ],
      explanation: "Market impact means the environment is not a passive backdrop: your order consumes liquidity and shifts the price, so the very data distribution you will see next depends on what you do now. This feedback loop is the heart of why execution and sizing are control problems.",
    },
    {
      question: "An agent receives rewards of +2, then +4, then +8 over three steps. With a discount factor γ = 0.5, the discounted return from the first step (G = r₀ + γ·r₁ + γ²·r₂) is...",
      options: [
        { text: "6 — that is 2 + 0.5·4 + 0.25·8 = 2 + 2 + 2", correct: true },
        { text: "14 — you simply add 2 + 4 + 8" },
        { text: "3.5 — the average of the three rewards" },
        { text: "2 — only the immediate reward counts when γ = 0.5" },
      ],
      explanation: "Return is the discounted sum: 2 + 0.5·4 + 0.5²·8 = 2 + 2 + 2 = 6. A lower γ shrinks distant rewards faster; γ = 0 would be purely myopic (just the 2), γ → 1 would approach the undiscounted 14.",
    },
    {
      question: "Why must an RL trading reward typically be RISK-ADJUSTED rather than just per-step PnL?",
      options: [
        { text: "Maximizing raw PnL rewards taking ever more leverage and variance; a risk term (mean–variance penalty, differential Sharpe, drawdown penalty) makes the agent prefer steadier returns instead of reckless bets", correct: true },
        { text: "Raw PnL cannot be computed in a simulator" },
        { text: "Risk adjustment makes the reward sparse, which always speeds learning" },
        { text: "Regulators forbid using PnL as a reward signal" },
      ],
      explanation: "A pure-PnL objective is indifferent between a smooth 8% and a coin-flip that averages 8% with huge swings — and with leverage it will chase the latter. Folding risk into the reward (e.g. r − λ·variance, or an online Sharpe increment) aligns the agent with risk-adjusted profit, the thing you actually want.",
    },
    {
      question: "An agent's gross signal earns +$4 per trade, but spread, commission, and impact cost $5 per trade. What happens with a FRICTIONLESS reward versus a COST-AWARE one?",
      options: [
        { text: "The frictionless reward sees +$4 and trades constantly; the cost-aware reward sees −$1 and learns to skip the trade (or trade only when edge beats the cost hurdle) — same signal, opposite policy", correct: true },
        { text: "Both rewards lead to identical policies because costs cancel out over time" },
        { text: "The cost-aware reward trades MORE because it wants to recover the costs" },
        { text: "Costs are irrelevant to RL because the agent only cares about direction" },
      ],
      explanation: "Net edge is −$1: gross 4 minus cost 5. A reward that ignores costs optimizes a fantasy and overtrades; a reward that charges realistic spread/commission/impact turns the agent into its own transaction-cost analyst, trading only when expected edge clears the hurdle.",
    },
    {
      question: "What is the danger of a discount factor γ set TOO LOW for a trading agent?",
      options: [
        { text: "The agent becomes myopic — it grabs immediate reward and ignores delayed consequences like the cost of unwinding a position or a slow mean-reversion payoff", correct: true },
        { text: "The agent becomes too far-sighted and refuses to ever trade" },
        { text: "A low γ guarantees the optimal policy regardless of the task" },
        { text: "γ has no effect on which action looks best" },
      ],
      explanation: "γ sets the horizon. Too low and future rewards are crushed, so the agent over-weights the next tick and under-weights the eventual cost of exiting or a payoff that arrives several steps later. Too high and credit assignment becomes noisy and slow. It is a genuine trade-off to tune.",
    },
    {
      question: "POTENTIAL-BASED reward shaping is preferred over ad-hoc shaping because...",
      options: [
        { text: "It adds guidance to speed learning while provably PRESERVING the optimal policy, whereas naive shaping can silently change the optimum so the agent chases the proxy instead of the goal", correct: true },
        { text: "It removes the need for any real reward signal" },
        { text: "It always makes the reward sparser and therefore harder to hack" },
        { text: "It guarantees the agent converges in a single episode" },
      ],
      explanation: "Shaping injects intermediate rewards to help a sparse/delayed signal. Potential-based shaping (the difference of a potential function between states) leaves the set of optimal policies unchanged. Arbitrary shaping bonuses do not — the agent can maximize the shaped proxy while abandoning the true objective.",
    },
    {
      question: "Which of the following are genuine examples of REWARD HACKING by a trading agent?",
      allowMultiple: true,
      options: [
        { text: "Churning trades to farm a volume-based rebate term while losing money on the underlying", correct: true },
        { text: "Marking illiquid positions at favorable prices to inflate a mark-to-market reward", correct: true },
        { text: "Exploiting a simulator that always fills at the mid with no spread or impact", correct: true },
        { text: "Trading only when the expected edge exceeds realistic transaction costs" },
      ],
      explanation: "Reward hacking is maximizing the literal metric against your intent: rebate-farming churn, mark-to-market inflation, and milking an unrealistic fill model all do that. Trading only when edge beats costs is exactly the ALIGNED behavior a well-designed reward produces — not a hack.",
    },
    {
      question: "For an action that is a CONTINUOUS position size (any fraction in [−1, 1]), why are policy-gradient methods a more natural fit than vanilla Q-learning?",
      options: [
        { text: "Value methods must take argmax over actions, which is awkward over a continuum, while policy methods parameterize and output a continuous (possibly stochastic) action directly", correct: true },
        { text: "Q-learning cannot represent rewards, only policy methods can" },
        { text: "Policy gradients never have high variance, unlike value methods" },
        { text: "Continuous actions make the problem non-Markov, which only policy methods handle" },
      ],
      explanation: "Acting greedily in a value method means maximizing Q over the action set — easy when discrete, painful over a continuum. Policy-gradient (and actor–critic) methods output the action directly from a parameterized policy, so continuous control (sizing, quoting) is their home turf.",
    },
    {
      question: "A tabular Q-learning update is Q(s,a) ← Q(s,a) + α[r + γ·maxₐ′Q(s′,a′) − Q(s,a)]. With Q(s,a)=4.0, α=0.1, r=2, γ=0.9, and maxₐ′Q(s′,a′)=6.0, the new Q(s,a) is...",
      options: [
        { text: "4.34 — the TD target is 2 + 0.9·6 = 7.4, the error is 7.4 − 4.0 = 3.4, and 4.0 + 0.1·3.4 = 4.34", correct: true },
        { text: "7.40 — you just take the TD target r + γ·maxQ" },
        { text: "4.00 — the update leaves Q unchanged" },
        { text: "3.40 — the new value equals the TD error" },
      ],
      explanation: "TD target = r + γ·maxₐ′Q = 2 + 0.9·6 = 7.4. TD error = 7.4 − 4.0 = 3.4. Q moves a fraction α toward the target: 4.0 + 0.1·3.4 = 4.34. The learning rate α controls how far each step nudges the estimate.",
    },
    {
      question: "In a Deep Q-Network (DQN), what problem do EXPERIENCE REPLAY and a separate TARGET NETWORK address?",
      options: [
        { text: "Instability of bootstrapped Q-learning with a neural net — replay de-correlates consecutive samples and the target network gives a slowly-moving target so the bootstrap doesn't chase itself", correct: true },
        { text: "They eliminate the need for a reward function" },
        { text: "They make the action space continuous" },
        { text: "They guarantee the agent never explores suboptimal actions" },
      ],
      explanation: "Function approximation + bootstrapping + off-policy data is the 'deadly triad' that can diverge. Replay breaks the temporal correlation of online samples, and a periodically-updated target network stops the network from bootstrapping off its own rapidly-shifting estimates — together they stabilize training.",
    },
    {
      question: "A known weakness of vanilla policy-gradient (REINFORCE) is HIGH VARIANCE. How does an ACTOR–CRITIC method reduce it?",
      options: [
        { text: "A critic estimates a value/advantage baseline (A = Q − V), so the actor's gradient is scaled by how much better than expected an action was rather than by the raw, noisy return", correct: true },
        { text: "It discards the policy entirely and acts greedily on Q" },
        { text: "It removes exploration so returns stop varying" },
        { text: "It trains on a single trajectory and never updates again" },
      ],
      explanation: "REINFORCE weights each action by the full Monte-Carlo return, which is noisy. Actor–critic introduces a critic to estimate value, and uses the advantage (return relative to the baseline) to update the actor — cutting variance while keeping the gradient unbiased-ish, which is why it underlies modern methods like A2C and PPO.",
    },
    {
      question: "What does PPO's 'clipping' actually do?",
      options: [
        { text: "It limits how far each update can move the policy from the previous one, preventing a single large, destructive step and keeping training stable", correct: true },
        { text: "It clips rewards to the range [−1, 1] to normalize them" },
        { text: "It deletes the lowest-value actions from the action set" },
        { text: "It caps the discount factor at 0.99" },
      ],
      explanation: "Proximal Policy Optimization clips the probability-ratio objective so the new policy can't stray too far from the old one in a single batch. That trust-region-like constraint avoids catastrophic policy collapse and is a big reason PPO is the workhorse default for continuous control.",
    },
    {
      question: "Why is the explore-vs-exploit trade-off ESPECIALLY painful in live trading compared to a video game?",
      options: [
        { text: "Every exploratory trade pays real spread, commission, and impact — exploration literally costs money — which pushes practitioners toward offline/batch RL that learns from historical data without live random actions", correct: true },
        { text: "Exploration is free in trading, so it is never a concern" },
        { text: "Markets forbid taking any random actions by regulation" },
        { text: "Video games have no exploration problem at all" },
      ],
      explanation: "In a game a wasted exploratory move costs nothing but time. In markets each exploratory action crosses the spread and may move the price, so naive ε-greedy bleeds cash. That cost motivates offline/batch RL, conservative policies, and careful, small-size exploration when live exploration is unavoidable.",
    },
    {
      question: "The core trade-off in optimal execution of a large order is between...",
      options: [
        { text: "Market IMPACT (trade fast → consume liquidity → move the price against yourself) and TIMING/volatility RISK (trade slow → longer exposure to adverse price moves)", correct: true },
        { text: "Commission and exchange fees only" },
        { text: "Long versus short direction of the trade" },
        { text: "Whether to use a neural net or a decision tree" },
      ],
      explanation: "Liquidate too aggressively and you pay heavy impact; stretch it out and you sit exposed to volatility while the price can drift away. Execution algorithms — and an RL executor — exist to balance these two costs over the trading horizon.",
    },
    {
      question: "In the Almgren–Chriss framework, increasing the RISK-AVERSION parameter λ changes the optimal schedule how?",
      options: [
        { text: "It FRONT-LOADS the schedule — trade more early to cut exposure fast — giving a faster, more aggressive trajectory; λ = 0 (risk-neutral) trades uniformly like TWAP", correct: true },
        { text: "It back-loads the schedule, saving most of the order for the deadline" },
        { text: "It makes the schedule random to confuse other traders" },
        { text: "λ has no effect on the optimal trajectory" },
      ],
      explanation: "Almgren–Chriss minimizes E[cost] + λ·Var[cost]. A risk-neutral trader (λ = 0) spreads trading evenly to minimize expected impact. As λ rises, reducing variance matters more, so the trader front-loads execution to shed inventory (and its risk) sooner — an exponentially-decaying schedule.",
    },
    {
      question: "When you frame execution as an MDP, the typical (state, action, reward) is...",
      options: [
        { text: "State = inventory remaining, time remaining, and market features; action = how much to trade this step; reward = −(impact cost) − λ·(risk) each step", correct: true },
        { text: "State = the final P&L; action = buy-and-hold; reward = the Sharpe ratio at the end only" },
        { text: "State = the model's weights; action = the learning rate; reward = training accuracy" },
        { text: "State = the calendar date; action = the ticker symbol; reward = trading volume" },
      ],
      explanation: "Execution's state must capture what's left to do (inventory, time) plus conditions (spread, volatility, signal); the action is the trade size/participation now; and the per-step reward charges impact and risk — exactly the cost-aware, risk-adjusted reward design from earlier lessons.",
    },
    {
      question: "Under Almgren–Chriss's own assumptions (linear impact, no signal, single asset), a well-trained RL executor will tend to...",
      options: [
        { text: "RE-DERIVE the Almgren–Chriss solution — which is a useful sanity check; RL earns its keep only when reality breaks those assumptions (nonlinear/uncertain impact, a short-term signal, regime-dependent liquidity, constraints, multi-asset)", correct: true },
        { text: "Beat Almgren–Chriss by a huge margin because RL is always superior" },
        { text: "Fail completely, because RL cannot handle execution" },
        { text: "Ignore inventory and trade randomly" },
      ],
      explanation: "If the world really is Almgren–Chriss, the closed form is optimal and RL should reproduce it — passing that test is reassuring. The value of RL appears when the assumptions fail: nonlinear impact, exploitable alpha, changing liquidity, hard constraints, or coupled multi-asset execution where no clean closed form exists.",
    },
    {
      question: "A market maker keeps getting filled on its BID as the price falls, accumulating a growing long inventory. The standard quoting response is to...",
      options: [
        { text: "SKEW quotes against the inventory — shift both quotes down so the ask is more attractive — to encourage offsetting sells and bring inventory back toward zero", correct: true },
        { text: "Quote a wider spread symmetrically and hope the trend reverses" },
        { text: "Keep quotes unchanged because inventory does not affect risk" },
        { text: "Buy even more aggressively to lower the average cost" },
      ],
      explanation: "One-sided fills in a trend build dangerous inventory. Skewing the quotes (here, lowering them so your ask is keener) leans against the position, nudging the flow to sell to you less and buy from you more, which mean-reverts inventory toward a neutral target — the core of inventory management.",
    },
    {
      question: "In the Avellaneda–Stoikov model, the RESERVATION (indifference) price differs from the mid by a term that grows with...",
      options: [
        { text: "Inventory q, risk aversion γ, volatility σ², and time remaining — so a larger position, more risk aversion, or higher vol pushes the maker's effective price further from the mid", correct: true },
        { text: "Only the bid–ask spread, nothing about inventory or risk" },
        { text: "The number of exchanges the asset trades on" },
        { text: "The maker's account balance in dollars" },
      ],
      explanation: "The reservation price r ≈ mid − q·γ·σ²·(T−t) is the price at which the maker is indifferent to holding its current inventory. It leans away from the mid in proportion to inventory, risk aversion, and variance — encoding 'I'll lean my quotes to offload risk' directly into where the maker centers its prices.",
    },
    {
      question: "ADVERSE SELECTION for a market maker means...",
      options: [
        { text: "Informed traders trade against stale quotes and pick the maker off, so realized spread is worse than the quoted spread — the spread must be wide enough to cover this cost", correct: true },
        { text: "The exchange selects which orders to fill at random" },
        { text: "The maker can always choose which counterparties to trade with" },
        { text: "Adverse selection only affects buyers, never sellers" },
      ],
      explanation: "When someone with better information trades with you, they tend to be right and you tend to be wrong — you buy just before prices fall and sell just before they rise. This erodes the spread you thought you were capturing, so quotes must price in an adverse-selection component to stay profitable.",
    },
    {
      question: "A market-making reward that IGNORES inventory entirely is dangerous because the agent will...",
      options: [
        { text: "Quote insanely tight to capture spread, accumulate a huge one-sided position in a trend, and blow up on the mark-to-market loss — inventory penalties exist to prevent exactly this", correct: true },
        { text: "Refuse to quote at all, since there is no incentive" },
        { text: "Always converge to the Almgren–Chriss execution schedule" },
        { text: "Perfectly hedge its inventory automatically" },
      ],
      explanation: "Without an inventory term the agent sees only spread capture and quotes as tight as possible to maximize fills — right up until a trend buries it in a one-sided position whose mark-to-market loss dwarfs the spread it earned. Over-penalizing inventory has the opposite failure (it never trades), so the reward must balance the two.",
    },
    {
      question: "Why does reinforcement learning tend to OVERFIT EVEN WORSE than supervised learning in markets?",
      options: [
        { text: "The agent chooses its own data distribution (it visits flattering states), long-horizon credit assignment multiplies noise, and you tune the environment + reward + algorithm + hyperparameters — a massive multiple-testing search", correct: true },
        { text: "RL uses smaller datasets that are always clean" },
        { text: "RL has no objective function, so it cannot overfit" },
        { text: "Supervised learning never overfits, so any gap is RL-specific" },
      ],
      explanation: "Supervised models at least train on a fixed dataset. An RL agent steers its own experience toward states where it looks good, propagates noisy credit across long horizons, and is selected from an enormous search over env/reward/algo/hyperparameters — every degree of freedom another chance to fit noise. The deflated-Sharpe skepticism from the ML course applies double.",
    },
    {
      question: "Which simulator assumptions make an RL backtest look BETTER than live reality would?",
      allowMultiple: true,
      options: [
        { text: "Assuming every order fills at the mid with no spread paid", correct: true },
        { text: "Assuming a static order book that does not react to your trades (no market impact)", correct: true },
        { text: "Assuming zero latency and guaranteed queue priority you won't actually get", correct: true },
        { text: "Charging realistic spread, impact, and conservative (pessimistic) fills", correct: false },
      ],
      explanation: "Mid-price fills, a non-reacting book, and free latency/priority all hand the agent profits it could never capture live — and it will optimize straight into those fantasies. Charging realistic costs and assuming conservative fills does the opposite: it makes the sim honest, so an edge that survives is more likely to be real.",
    },
    {
      question: "Which practices best CLOSE the sim-to-real gap before risking real capital?",
      options: [
        { text: "Realistic fills/impact in the simulator, training and stress-testing across multiple regimes, walk-forward / purged-and-embargoed evaluation, paper trading, monitoring realized-vs-expected reward, a pre-set kill-switch, and starting at tiny size", correct: true },
        { text: "Re-using the test set to tune the reward until the equity curve looks perfect, then deploying at full size" },
        { text: "Training on one calm regime and assuming it generalizes forever" },
        { text: "Skipping paper trading to capture the edge before it decays" },
      ],
      explanation: "Treat the agent as guilty until proven innocent: an honest simulator, cross-regime training and stress tests, leak-free walk-forward evaluation, a paper-trading shakeout, live reward monitoring against expectations, a kill-switch, and small initial size. The wrong answers — reusing the test set, single-regime training, skipping paper trading — are precisely how a backtest mirage becomes a live loss.",
    },
  ]}
/>

<Callout variant="info" title="Where this sits on the ladder">
Passing this exam closes out the reinforcement-learning rung of the quant ladder — the point where control theory, deep learning, and a hard-won skepticism fuse into an agent that can *act* in a market rather than merely forecast it. You now own the discipline that separates a working RL trader from the graveyard of beautifully-converged backtests: frame the problem as an MDP, design a reward that can't be hacked, pick the method family that fits your action space, respect the impact-versus-risk trade-off in execution, balance inventory against adverse selection when making markets, and — above all — assume your simulator is lying until reality says otherwise. The deepest lesson echoes every course before it: the algorithm is never your biggest risk; the human who trusted a flawless simulation always is.
</Callout>
