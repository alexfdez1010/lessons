---
title: "FRAs: The Building Blocks of a Swap"
description: "Lock a future interest rate today: forward rates by no-arbitrage, the FRA contract, its discounted cash settlement, and why a swap is just a strip of FRAs."
topic: "en/swaps-and-rate-derivatives"
order: 2
minutes: 13
updated: 2026-06-12
---

import { Callout, MCQ, Quiz, FillBlank, MatchConcepts, MindMap, ThinkReveal, Reveal, ForwardRateBridge } from '@/components/react';

You already know what a swap *is* — two parties trading a fixed cash flow for a floating one. But before we can price one, we need to zoom all the way in to a single screw in the machine. That screw is the **FRA**: a contract that lets you lock *one* future interest rate today, on a single notional, for a single period. Master the FRA and a swap stops being a mysterious blob — it becomes a tidy row of FRAs bolted together. And the rate each FRA fairly locks? It's the **forward rate** you already met in the yield-curve lessons. So we start there, then build up.

<MCQ
  client:visible
  pretest
  question="You want to borrow money one year from now, for one year, but you're terrified rates will jump before then. What kind of instrument would let you nail down that future borrowing rate today?"
  options={[
    { text: 'A contract that locks a single future period’s interest rate on a notional — a forward rate agreement', correct: true },
    { text: 'A longer-maturity bond bought today' },
    { text: 'A higher coupon on your existing debt' },
    { text: 'Nothing exists for future rates; you must just wait and pray' },
  ]}
  explanation="An FRA (forward rate agreement) is exactly this: an over-the-counter contract that fixes the interest rate for a single future period on an agreed notional, so a future rate move can't hurt (or help) you. The fair rate it locks is the forward rate implied by today's curve."
/>

## Forward rates: the rate you can lock today for a future period

**Pretest first:**

<MCQ
  client:visible
  pretest
  question="Today's 1-year spot rate is 4% and the 2-year spot rate is 5%. Roughly what rate can you lock TODAY for borrowing during year 2 (one year, starting one year from now)?"
  options={[
    { text: 'About 6% — the second-year rate that, compounded after the first year, reproduces the 2-year return', correct: true },
    { text: 'Exactly 5%, the 2-year spot rate' },
    { text: 'Exactly 4%, the 1-year spot rate' },
    { text: '4.5%, the simple average of the two spots' },
  ]}
  explanation="The forward rate is pinned by no-arbitrage: growing at 4% for a year then at the forward for the next year must equal growing at 5% for two years. That forces the forward above both spots when the curve slopes up — here about 6%, not the average."
/>

**Analogy.** A spot rate is the price of a trip that starts *now*. A **forward rate** is the price, quoted *now*, of a trip that starts *later* — like booking next summer's flight at today's locked-in fare. The airline can't make money offering you a future fare that's inconsistent with today's prices, and neither can a bank: the forward rate is forced by **no-arbitrage** from the rates already on the curve.

**The definition.** A **forward rate** $f_{1,2}$ is the interest rate, agreed *today*, for borrowing or lending over a *future* window — here from year 1 to year 2. It isn't a forecast and it isn't a free parameter. It's locked by the rule that two ways of investing for two years must pay the same:

$$
(1 + z_2)^2 = (1 + z_1)\,(1 + f_{1,2}),
$$

where $z_1$ and $z_2$ are today's 1-year and 2-year **spot (zero) rates**. The left side grows your money at the 2-year rate for two years. The right side grows it at the 1-year rate, then rolls into the forward for the second year. If they differed, you could borrow cheap on one path and lend dear on the other for free money — so the market won't let them differ.

<ForwardRateBridge
  client:visible
  title="The forward rate is the bridge between two spot rates"
  nearLabel="1-year spot rate"
  farLabel="2-year spot rate"
  forwardLabel="Implied 1y forward, 1y out"
  spotAnchorLabel="Spot rates"
  bridgeLabel="Forward rate (the locked future rate)"
  nearTick="Year 1"
  farTick="Year 2"
  todayTick="Today"
  caption="Drag the two spot rates. The forward is the segment spanning the future window — and no-arbitrage forces it above both spots when the curve rises, below both when it inverts. It's the rate you can lock today for that future period."
/>

**Worked example — extract the 1-year forward, 1 year out.** Take $z_1 = 4\%$ and $z_2 = 5\%$. Solve for $f_{1,2}$:

$$
1 + f_{1,2} = \frac{(1 + z_2)^2}{1 + z_1} = \frac{1.05^2}{1.04} = \frac{1.1025}{1.04}.
$$

Working the arithmetic: $1.1025 / 1.04 = 1.06010$, so $f_{1,2} = 0.06010 = 6.01\%$. Notice it lands *above* both spots — that's the upward-sloping-curve signature. If the curve had inverted ($z_2 \lt z_1$), the forward would have dropped below both. The forward is a leveraged readout of the curve's slope, not its level.

<FillBlank
  client:visible
  question="Fill in the no-arbitrage logic."
  text="A forward rate is the rate agreed {{today|at maturity|next year}} for a {{future|past|current}} borrowing period. It's pinned by no-arbitrage: compounding the near spot then the forward must equal the {{far spot|coupon|recovery rate}} compounded over the full horizon. When the curve slopes upward, the forward sits {{above|below|exactly between}} both spot rates."
  explanation="The forward is quoted today for a future window; rolling the near spot into the forward must reproduce the far spot's compounded return; an upward curve forces the forward above both spots."
/>

## The FRA: a forward contract whose underlying is an interest rate

**Pretest:**

<MCQ
  client:visible
  pretest
  question="In a '3x9 FRA', what does the notation tell you?"
  options={[
    { text: 'It starts in 3 months and runs to month 9 — covering a 6-month interest period', correct: true },
    { text: 'It pays 3% and costs 9% to enter' },
    { text: 'It is a 3-year contract renewed 9 times' },
    { text: 'It references a 3-month rate multiplied by 9' },
  ]}
  explanation="The two numbers are months from today: the period STARTS at month 3 and ENDS at month 9, so it locks a rate over the 6-month window between them. A 3x9 FRA therefore fixes a 6-month rate, three months forward."
/>

**The definition.** A **forward rate agreement (FRA)** is an **over-the-counter** (privately negotiated, not exchange-traded) contract that locks a single future period's interest rate, called the **FRA rate** or strike $K$, on an agreed **notional** $N$. No principal changes hands — the notional is just the size on which interest is calculated. It's a forward contract like any other, except the underlying isn't a barrel of oil or a bushel of wheat; it's an **interest rate**.

**Who wins which way.** There are two sides:

- The **buyer / long** is, economically, a future *borrower*. They've locked their cost at $K$, so they **gain if the reference rate rises above $K$** — the market got expensive, but they pay the cheaper locked rate.
- The **seller / short** is a future *lender*. They've locked their return at $K$, so they **gain if the reference rate falls below $K$** — the market got stingy, but they still earn the higher locked rate.

It's a zero-sum bet on where one future rate prints relative to $K$. The **reference rate** $r_{\text{ref}}$ is some agreed benchmark (historically LIBOR, now rates like SOFR or EURIBOR), observed at the *start* of the contract period — the **fixing**.

**The notation.** FRAs are quoted as "**$a$ x $b$**", both numbers being months from today: the rate period **starts at month $a$** and **ends at month $b$**. So a **3x9 FRA** starts in 3 months, ends in 9, and locks the **6-month rate** that fixes 3 months from now. A **1x4** locks a 3-month rate, one month out. The gap $b - a$ is the length of the rate you're fixing.

<Callout variant="info" title="The fair K is just the forward rate">
What strike $K$ should an FRA be struck at so neither side has to pay the other to enter? Exactly the **forward rate** for that window, read off today's curve. That's the whole link: a fairly-priced FRA simply *crystallises the forward rate into a binding contract*. The forward rate is the prediction the curve makes; the FRA is the bet you place on it.
</Callout>

<MatchConcepts
  client:visible
  question="Match each FRA term to its meaning."
  pairs={[
    { term: 'Notional N', definition: 'The size on which interest is computed; never exchanged' },
    { term: 'FRA rate / strike K', definition: 'The fixed rate the contract locks for the period' },
    { term: 'Reference rate r_ref', definition: 'The benchmark observed at the fixing, e.g. SOFR' },
    { term: 'Buyer / long', definition: 'Future borrower; gains when the rate rises above K' },
    { term: '3x9 FRA', definition: 'Starts in 3 months, ends in 9 — a 6-month rate' },
  ]}
  explanation="Notional sizes the interest but isn't paid; K is the locked rate; r_ref is the benchmark fixed at the period start; the long is the future borrower who profits from rising rates; and 3x9 names the start and end months."
/>

## FRA settlement: cash today, discounted because the interest is owed later

**Pretest:**

<MCQ
  client:visible
  pretest
  question="An FRA references a 6-month interest period, but it cash-settles a single payment at the START of that period. Why is that payment discounted rather than paid in full?"
  options={[
    { text: 'Because the interest it represents would naturally be paid at the END of the period, so paying early requires present-valuing it', correct: true },
    { text: 'Because the buyer always owes a fee to the seller' },
    { text: 'Because reference rates are never trusted at full value' },
    { text: 'Because the notional shrinks over the period' },
  ]}
  explanation="Interest on a loan is paid at the period's end. The FRA instead settles up front, at the start, so the payment is discounted back over the period at the reference rate — otherwise the early-paying side would be over- or under-compensated by the time value of money."
/>

**The mechanics.** Here's the wrinkle that trips everyone up. The interest an FRA references would, on a real loan, be paid at the **end** of the period. But an FRA **cash-settles a single net payment at the *start*** of the period, right after the rate fixes. Paying early means you must **discount** the interest difference back to the settlement date. The settlement to the long (buyer) is:

$$
\text{Settlement} = \frac{N\,(r_{\text{ref}} - K)\,\tau}{1 + r_{\text{ref}}\,\tau},
$$

where $N$ is the notional, $K$ the strike, $r_{\text{ref}}$ the realized reference rate, and $\tau$ the **day-count fraction** — the period length as a fraction of a year (e.g. a 6-month period is roughly $\tau = 0.5$). The numerator is the plain interest difference; the denominator $1 + r_{\text{ref}}\,\tau$ is the discount factor that drags that end-of-period amount back to the start. If $r_{\text{ref}} \gt K$, the number is positive and flows to the long; if $r_{\text{ref}} \lt K$, it's negative and the long pays.

**Worked example.** A treasurer buys a 6x12 FRA on a **\$10 million** notional at a strike of $K = 5\%$, fixing a 6-month rate. Six months later the rate fixes at $r_{\text{ref}} = 6\%$. Day-count $\tau = 0.5$.

- **Raw interest difference** (what an actual loan would settle at period-end):
  $$
  N\,(r_{\text{ref}} - K)\,\tau = 10{,}000{,}000 \times (0.06 - 0.05) \times 0.5 = 10{,}000{,}000 \times 0.005 = 50{,}000,
  $$
  i.e. **\$50,000**.
- **Discount factor** (settle now, not in 6 months):
  $$
  1 + r_{\text{ref}}\,\tau = 1 + 0.06 \times 0.5 = 1.03.
  $$
- **Settlement paid to the long at the start of the period:**
  $$
  \frac{50{,}000}{1.03} = 48{,}543.69,
  $$
  i.e. **\$48,543.69**.

So the long pockets **\$48,543.69** up front. Why not the full \$50,000? Because that \$50,000 was an *end-of-period* figure; receiving it six months early is worth a touch less, so we present-value it. The treasurer is exactly compensated: the cheaper-than-market locked rate saved \$50,000 of future interest, and \$48,543.69 today *is* \$50,000 in six months at 6%.

<Callout variant="warning" title="The #1 FRA misconception">
A wildly common error is to think the FRA pays the **undiscounted** interest (the \$50,000). It does not. Because settlement happens at the *start* of the period while the referenced interest belongs to the *end*, the payment is always discounted at the realized reference rate. Forget the denominator and you'll overstate every FRA cash flow — and mis-hedge a swap built from them.
</Callout>

<ThinkReveal
  client:visible
  prompt="You SELL a 3x9 FRA (so you're the short, a future lender) on a $20 million notional at K = 4%. The 6-month rate fixes at 3% (τ = 0.5). Do you receive or pay, and roughly how much?"
  hint="Short gains when the rate FALLS below K. Settlement to the long = N(r_ref − K)τ / (1 + r_ref·τ); the short gets the opposite sign."
  text="You RECEIVE about $98,522. The long's settlement is N(r_ref − K)τ / (1 + r_ref·τ) = 20,000,000 × (0.03 − 0.04) × 0.5 / (1 + 0.03 × 0.5) = (−$100,000) / 1.015 = −$98,522.17. The long pays $98,522.17, so you (the short) receive it. That fits the rule: as the future lender you locked 4% while the market only offers 3%, so the rate FALLING in your favour pays you. Note the discounting: the raw figure is $100,000, but it settles up front at 3%, so it's $98,522, not the full $100,000."
  buttonLabel="Reveal"
/>

## A swap is a strip of FRAs

**Pretest:**

<MCQ
  client:visible
  pretest
  question="An interest-rate swap exchanges a stream of floating payments for fixed ones over several periods. How is each individual floating period best understood?"
  options={[
    { text: 'As a single FRA whose fair locked rate is that period’s forward rate', correct: true },
    { text: 'As a separate zero-coupon bond' },
    { text: 'As an option on the reference rate' },
    { text: 'As an unrelated cash flow with no link to forwards' },
  ]}
  explanation="Each floating period of a swap is economically one FRA — a single future rate exchanged for a fixed rate — and the fair fixed rate for that lone period is the forward rate the curve implies for it. A swap is those FRAs bolted together."
/>

**The central insight.** Here's the payoff for all that FRA grinding. Take a multi-period swap and slice it period by period. **Each floating period is, economically, a single FRA** — one future reference rate exchanged against a fixed rate, on the notional, for that window. The fair fixed rate for *that period alone* is simply its **forward rate**, read off today's curve. So a swap is a **strip of FRAs**: a row of single-period rate locks, one per payment date, stapled together.

Now the trick that turns a strip of FRAs into a *swap*. In a swap, both sides want **one** constant fixed rate across all periods, not a different forward rate every period. So we replace the jagged sequence of forward rates with a **single fixed rate — the swap rate** — chosen so the deal is still fair overall. That swap rate is a **discount-factor-weighted average of the forward rates**: the periods don't count equally, because later cash flows are discounted more, so nearer forwards carry more weight.

| Period | Forward rate (fair single-period lock) | Read as | What the swap does |
|---|---|---|---|
| 0–1y | 4.00% | This period = one FRA at 4.00% | Replace each period's forward... |
| 1–2y | 5.00% | This period = one FRA at 5.00% | ...with ONE constant fixed rate... |
| 2–3y | 5.80% | This period = one FRA at 5.80% | ...the swap rate, ~5.2%... |
| 3–4y | 6.20% | This period = one FRA at 6.20% | ...a discount-weighted average... |
| Swap rate | ≈ 5.2% (one number for all) | The single fixed leg | ...of all the period forwards. |

The swap rate (~5.2% here) isn't any single forward — it's the level that, applied flat to every period, has the same present value as paying each period's own forward. Lock each period at its forward (a strip of FRAs) or lock all periods at the blended swap rate (a swap): same total value today, by no-arbitrage.

<Callout variant="info" title="Why this reframing is the whole game">
Once you see a swap as a strip of FRAs, pricing it stops being scary. You already know how to value one FRA (discount its rate difference). A swap is just the sum of those values — and setting that sum to zero at inception is what *defines* the swap rate. Every swap-pricing formula you'll meet next is this idea wearing a suit.
</Callout>

<FillBlank
  client:visible
  question="Fill in the strip-of-FRAs picture."
  text="Each floating period of a swap is economically a single {{FRA|bond|option}}, whose fair locked rate is that period's {{forward rate|spot rate|coupon}}. A swap replaces those varying rates with one constant {{swap rate|reference rate|notional}}, which is a discount-factor-weighted {{average|sum|maximum}} of the period forwards."
  explanation="A swap is a strip of single-period FRAs; each FRA's fair rate is the period's forward; the swap rate is the one fixed rate that discount-weighted-averages those forwards into a single number."
/>

<Reveal client:visible buttonLabel="Why a weighted average, not a plain one?">
Later swap payments are discounted more heavily — a dollar in year 4 is worth less today than a dollar in year 1. So when blending the forwards into one fixed rate that keeps the deal fair *in present-value terms*, nearer periods (lighter discounting) pull harder on the average. A plain arithmetic mean would over-weight distant forwards and misprice the swap. The correct weights are the period discount factors, which is exactly what falls out of setting the swap's net present value to zero.
</Reveal>

## When to use it

Use an **FRA** when you have exactly *one* future interest-rate exposure to neutralise — a single loan rolling in three months, a deposit maturing in six. Use the **forward rate** as the fair strike to set it at, and remember the settlement is **discounted to the period start**, not paid in full at period end. Reach for the **strip-of-FRAs view** the moment you face a *multi-period* exposure: that's a swap, and decomposing it into its component FRAs is how you'll price it, risk it, and hedge it. The FRA is the atom; the swap is the molecule.

## Putting it together

A **forward rate** is the interest rate you can lock *today* for a *future* period, pinned by no-arbitrage so that rolling the near spot into the forward reproduces the far spot's compounded return — pushing the forward above both spots on an upward curve. An **FRA** crystallises one such rate into a binding OTC contract: it locks a strike $K$ on a notional for a single window (quoted "$a$x$b$" in months), the long gains when the reference rate rises above $K$, and it **cash-settles up front at the discounted value** $N(r_{\text{ref}} - K)\tau / (1 + r_{\text{ref}}\tau)$ — discounted precisely because the referenced interest belongs to the period's end. And the grand reframing: a **swap is a strip of FRAs**, each period's fair rate being its forward, with the single **swap rate** a discount-factor-weighted average of those forwards. Hold that picture and a swap is no longer a black box — it's Lego.

<MindMap
  client:visible
  title="FRAs & forward rates"
  caption="Forward rates pin the fair future rate; an FRA locks one such rate and cash-settles it discounted; a swap is a strip of FRAs blended into one swap rate."
  root={{
    label: 'FRAs & Forwards',
    children: [
      {
        label: 'Forward rate',
        children: [
          { label: 'Future-period rate locked today' },
          { label: '(1+z₂)² = (1+z₁)(1+f₁,₂)' },
          { label: 'No-arbitrage, not a forecast' },
          { label: 'Above both spots if curve rises' },
        ],
      },
      {
        label: 'The FRA',
        children: [
          { label: 'OTC contract locking one rate K' },
          { label: 'On a notional; principal never exchanged' },
          { label: 'Long = future borrower, wins if rate rises' },
          { label: '“3x9” = month 3 to month 9' },
          { label: 'Fair K = the forward rate' },
        ],
      },
      {
        label: 'Settlement',
        children: [
          { label: 'Cash-settles at period START' },
          { label: 'N(r_ref − K)τ / (1 + r_ref·τ)' },
          { label: 'Discounted: interest belongs to END' },
          { label: 'Trap: it is NOT the undiscounted amount' },
        ],
      },
      {
        label: 'Swap = strip of FRAs',
        children: [
          { label: 'Each floating period = one FRA' },
          { label: 'Its fair rate = that period’s forward' },
          { label: 'Swap rate = one constant fixed rate' },
          { label: 'Discount-factor-weighted avg of forwards' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: FRAs & forward rates"
  questions={[
    {
      question: 'Today the 1-year spot is 3% and the 2-year spot is 4%. The 1-year forward, one year out, is approximately:',
      options: [
        { text: '5.01%', correct: true },
        { text: '4.00%' },
        { text: '3.50%' },
        { text: '3.00%' },
      ],
      explanation: '(1+f) = 1.04² / 1.03 = 1.0816 / 1.03 = 1.05010, so f ≈ 5.01%. The upward curve forces the forward above both spots — it is not the 2-year spot (4%) nor the simple average (3.5%).',
    },
    {
      question: 'In a 2x8 FRA, what rate length and start does the contract lock?',
      options: [
        { text: 'A 6-month rate, starting 2 months from today', correct: true },
        { text: 'A 2-month rate, starting 8 months out' },
        { text: 'An 8-month rate, starting today' },
        { text: 'A 10-month rate (2 + 8) starting today' },
      ],
      explanation: 'Both numbers are months from today: starts at month 2, ends at month 8, so it locks the 8 − 2 = 6-month rate that fixes 2 months from now.',
    },
    {
      question: 'You buy a 6x12 FRA on $5 million at K = 4% (τ = 0.5). The rate fixes at 5%. Your settlement is closest to:',
      options: [
        { text: '+$24,390 received', correct: true },
        { text: '+$25,000 received' },
        { text: '−$24,390 paid' },
        { text: '+$50,000 received' },
      ],
      explanation: 'Raw difference = 5,000,000 × (0.05 − 0.04) × 0.5 = $25,000; discount by 1 + 0.05 × 0.5 = 1.025, giving 25,000 / 1.025 = $24,390. Positive (rate rose above K) so the long receives it. The full $25,000 ignores the start-of-period discounting.',
    },
    {
      question: 'The seller (short) of an FRA — economically a future lender — profits when:',
      options: [
        { text: 'The reference rate fixes BELOW the strike K', correct: true },
        { text: 'The reference rate fixes above K' },
        { text: 'The notional increases' },
        { text: 'The day-count fraction τ rises' },
      ],
      explanation: 'The short locked a lending rate at K; if the market rate falls below K, they still earn the higher locked rate and receive the settlement. The long (future borrower) is the one who profits when the rate rises above K.',
    },
    {
      question: 'Why is a swap rate a DISCOUNT-FACTOR-WEIGHTED average of the period forward rates rather than a simple average?',
      options: [
        { text: 'Later cash flows are discounted more, so nearer periods carry more weight when blending forwards into one fair fixed rate', correct: true },
        { text: 'Because forward rates are always equal' },
        { text: 'Because only the first period matters' },
        { text: 'Because the notional grows each period' },
      ],
      explanation: 'Setting the swap’s present value to zero blends the forwards using the period discount factors as weights. Distant payments are discounted harder, so nearer forwards pull more on the average — a plain mean would over-weight far forwards and misprice the swap.',
    },
  ]}
/>

Next — **pricing a swap as the difference of two bonds** — we cash in the strip-of-FRAs insight and value the whole thing as a fixed-rate bond minus a floating-rate bond, solving for the swap rate that makes them balance at inception.
