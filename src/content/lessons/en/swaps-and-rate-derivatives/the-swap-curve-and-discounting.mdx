---
title: "The Swap Curve and OIS Discounting"
description: "Read the par swap curve, bootstrap it into discount factors and forwards, and meet the post-2008 dual-curve world where OIS discounting splits projection from discounting."
topic: "en/swaps-and-rate-derivatives"
order: 4
minutes: 14
updated: 2026-06-12
---

import { Callout, MCQ, Quiz, FillBlank, MatchConcepts, MindMap, ThinkReveal, Reveal, SwapCurve } from '@/components/react';

You already know how to price a single swap and read off its par rate (lesson 3). Now zoom out. Plot the par swap rate at *every* tenor — 1y, 2y, 5y, 10y, 30y — and you get the **swap curve**, the single most-watched interest-rate object on the planet. Dealers quote it, trading desks price off it, and central-bank watchers read the market's whole rate forecast in its slope. But a curve of par rates is only the *surface*. Underneath sit the **discount factors**, the **zero rates**, and the **implied forwards** — and, since 2008, a quiet revolution split that one curve into *two*. This lesson takes you from the quoted surface all the way down to the dual-curve machinery that values every collateralised swap in the world.

<MCQ
  client:visible
  pretest
  question="The 'swap curve' that desks quote is best described as:"
  options={[
    { text: 'The term structure of par swap rates across maturities (1y, 2y, … 30y)', correct: true },
    { text: 'The fixed coupon of a single 10-year swap' },
    { text: 'The list of every floating payment a swap will make' },
    { text: 'The credit spread of the swap counterparty' },
  ]}
  explanation="The swap curve is the set of par (fair fixed) swap rates plotted against tenor — a term structure, not one number. Each point is the fixed rate that makes a new swap of that maturity worth zero at inception."
/>

## The swap curve: the term structure of par swap rates

**Analogy.** Think of the swap curve as the *menu board* of a money diner: for each holding period — 1 year, 2 years, all the way to 30 — it posts the fixed rate you can lock in today by trading fixed-for-floating. One curve, every maturity, refreshed by the second.

**The definition.** The swap curve is the par swap rate $s_n$ plotted against tenor $n$. Recall from lesson 3 that the par rate is the fixed coupon making a brand-new swap worth exactly zero — neither side pays the other to enter. Collect $s_1, s_2, \dots, s_{30}$ and you have the curve. Because swaps are deep, liquid, and standardised, this curve is the **benchmark** off which corporate loans, mortgages, structured notes, and other derivatives are priced.

**Reading the slope.** The shape is a message from the market:

- **Upward-sloping (normal):** longer swaps lock in *higher* rates. A long rate is roughly the average of the short rates expected over its life *plus* a **term premium** (extra yield for tying money up longer). An upward slope says the market expects short rates to rise and/or demands that premium.
- **Inverted:** longer swaps lock in *lower* rates — only possible if the market expects the short rate to *fall* (a classic recession/rate-cut signal), enough to overpower any term premium.

<SwapCurve
  client:visible
  title="The par swap curve and its implied forwards"
  parRateLabel="Par swap rate"
  forwardLabel="Implied 1y forward"
  normalLabel="Normal"
  invertedLabel="Inverted"
  showForwardLabel="Show implied forwards"
  tenorAxisLabel="Swap tenor (years)"
  rateAxisLabel="Rate"
/>

Toggle the island between **Normal** and **Inverted**, then flip on the forward overlay — we'll explain exactly why the forwards sit where they do in the next section.

<Callout variant="info" title="Why everyone watches this one curve">
The swap curve packs the market's collective rate forecast into a single picture. Its slope feeds growth and recession calls; its level sets borrowing costs across the economy; and its daily moves are a real-time vote on what central banks will do next. When commentators say "the curve inverted," they usually mean a rates curve like this one — and they mean the market is bracing for cuts.
</Callout>

## Bootstrapping the curve: from par rates to discount factors to forwards

<MCQ
  client:visible
  pretest
  question="To turn observed par swap rates into discount factors, you mainly rely on:"
  options={[
    { text: 'No-arbitrage and recursion — each new discount factor is solved using the ones already locked in', correct: true },
    { text: 'A single average of all the par rates' },
    { text: 'The credit rating of the swap dealer' },
    { text: 'Guessing, since par rates contain no discounting information' },
  ]}
  explanation="Bootstrapping is the same recursive, no-arbitrage trick as bond bootstrapping: the shortest instrument pins the first discount factor, then each longer par rate plus the earlier discount factors leaves exactly one unknown — the next discount factor."
/>

**The goal.** Par rates are what the market *quotes*; discount factors are what you actually *need* to value any cash flow. **Bootstrapping** converts one into the other, recursively, by no-arbitrage — the identical trick you used to strip a bond curve into spot rates.

**The engine.** From lesson 3, a par swap of tenor $n$ satisfies

$$
s_n = \frac{1 - DF_n}{\sum_{i=1}^{n} \tau_i\, DF_i},
$$

where $DF_i$ is the discount factor to payment date $i$ and $\tau_i$ is its accrual fraction (the year-fraction of period $i$). The denominator $\sum_i \tau_i DF_i$ is the **annuity** — the PV of receiving \$1 of fixed rate each period. Rearrange to isolate the *new* unknown $DF_n$, assuming every earlier $DF_i$ is already known:

$$
DF_n = \frac{1 - s_n \sum_{i=1}^{n-1} \tau_i\, DF_i}{1 + s_n\, \tau_n}.
$$

That's the whole recursion. Solve $DF_1$ from the 1y rate, plug it in to solve $DF_2$ from the 2y rate, and climb the ladder.

**Worked example — solve the 2y discount factor.** Suppose annual payments ($\tau_i = 1$), the 1y discount factor is $DF_1 = 0.9615$ (a 1y rate near 4%), and the 2y par swap rate is $s_2 = 4.50\% = 0.045$. Then

$$
DF_2 = \frac{1 - s_2\, DF_1}{1 + s_2} = \frac{1 - 0.045 \times 0.9615}{1 + 0.045} = \frac{1 - 0.04327}{1.045} = \frac{0.95673}{1.045} = 0.9156.
$$

So $DF_2 = 0.9156$. From there the **2y zero (spot) rate** $z_2$ follows from $DF_2 = (1 + z_2)^{-2}$:

$$
z_2 = DF_2^{-1/2} - 1 = 0.9156^{-1/2} - 1 \approx 0.0451 = 4.51\%.
$$

And the **implied 1y forward** one year out, $f_{1,2}$, is the rate that links the two discount factors:

$$
1 + f_{1,2} = \frac{DF_1}{DF_2} = \frac{0.9615}{0.9156} = 1.0501 \;\Rightarrow\; f_{1,2} \approx 5.01\%.
$$

Notice the punchline: the 1y rate was ~4.00%, the 2y *par* rate is 4.50%, but the implied forward for the second year is **~5.01% — above the par curve.** That's the overlay in the island made concrete: when the par curve slopes up, forwards must sit *above* it, because each longer par rate is a running average of short rates, and to keep dragging that average upward the marginal (forward) rate has to lead. Invert the curve and the logic flips — forwards drop *below* par to pull the average down.

| Quantity | Symbol | Value |
|---|---|---|
| 1y discount factor (given) | $DF_1$ | 0.9615 |
| 2y par swap rate (given) | $s_2$ | 4.50% |
| 2y discount factor (solved) | $DF_2$ | 0.9156 |
| 2y zero rate | $z_2$ | 4.51% |
| Implied 1y forward, yr 1→2 | $f_{1,2}$ | 5.01% |

<ThinkReveal
  client:visible
  prompt="With annual payments, DF₁ = 0.9709 and the 2y par swap rate is 3.00%. What is DF₂?"
  hint="DF₂ = (1 − s₂·DF₁) / (1 + s₂). Plug s₂ = 0.03 and DF₁ = 0.9709."
  text="DF₂ ≈ 0.9420. Compute the numerator: 1 − 0.03 × 0.9709 = 1 − 0.029127 = 0.970873. Divide by 1 + 0.03 = 1.03: 0.970873 / 1.03 ≈ 0.9426. (Rounding the intermediate steps lands you around 0.942–0.943.) The recursion needs only the earlier discount factor and the current par rate — exactly the bond-bootstrapping move applied to swaps."
  buttonLabel="Reveal"
/>

<FillBlank
  client:visible
  question="Fill in the bootstrapping logic."
  text="The denominator of the par-rate formula, the sum of τ·DF, is called the {{annuity|coupon|notional}}. Solving each new discount factor from the par rate and the earlier discount factors is justified by {{no-arbitrage|the credit rating|inflation}}. When the par curve slopes upward, the implied forwards lie {{above|below|exactly on}} the par curve."
  explanation="The PV of a unit of fixed rate per period is the annuity; bootstrapping is a no-arbitrage recursion; and an upward-sloping par curve forces forwards to sit above par, since par is a running average of the short (forward) rates."
/>

## OIS discounting and the dual-curve world

<MCQ
  client:visible
  pretest
  question="After 2008, a collateralised swap (under a CSA) is discounted using:"
  options={[
    { text: 'The OIS curve, because posted cash collateral earns the overnight rate', correct: true },
    { text: 'The LIBOR curve, exactly as before the crisis' },
    { text: 'The counterparty’s corporate bond yield' },
    { text: 'A flat 0% rate, since collateral removes all risk' },
  ]}
  explanation="Cash collateral posted under a Credit Support Annex earns interest at the overnight rate, so the funding/discount rate of a collateralised swap IS the OIS rate. LIBOR-discounting everything was the pre-2008 convention that the crisis killed."
/>

**The old world (pre-2008).** One curve did *both* jobs. The LIBOR curve was used to **project** the floating cash flows (LIBOR was the floating index) *and* to **discount** them back to PV. Tidy, single-curve, and quietly wrong — it assumed LIBOR was a near-risk-free funding rate. Then 2008 revealed that LIBOR carried real bank-credit and liquidity risk, and the basis between LIBOR and truly overnight rates blew out to hundreds of basis points.

**The collateral insight.** Most dealer swaps trade under a **CSA** (Credit Support Annex): each day the out-of-the-money party posts **cash collateral** equal to the swap's mark-to-market. Here's the key fact — *posted cash collateral earns interest at the overnight rate.* So if you're owed money on a swap, you hold collateral that grows at the overnight rate; that overnight rate is your true funding/reinvestment rate on the position. By no-arbitrage, **the discount rate must equal the rate your collateral earns** — the **OIS rate** (Overnight Indexed Swap rate; today SOFR-based in USD), not LIBOR.

<Callout variant="info" title="The collateral analogy">
Imagine you lend a friend \$100 but insist they leave their \$100 watch with you as collateral, and you agree the watch's "rent" accrues at the overnight rate. The cost of that arrangement is set entirely by the overnight rate on the collateral — not by some risky bank-lending rate. A collateralised swap is the same: the cash you hold earns OIS, so OIS is the rate you discount at. Change what the collateral earns and you change the discount curve.
</Callout>

**The result: dual curves.** Projection and discounting split into two separate curves:

- **Projection (forward) curve** — built from the floating *index* (e.g., 3-month LIBOR/term rate). It answers: *what floating cash flows will this swap throw off?*
- **Discount (OIS) curve** — built from OIS swap rates. It answers: *what are those cash flows worth today, given collateral earns the overnight rate?*

You **project on one curve and discount on another.** That is the dual-curve framework, and it is now the standard way every collateralised linear rate product is valued.

| Era | Project floating cash flows with | Discount with |
|---|---|---|
| Pre-2008 (single-curve) | LIBOR curve | LIBOR curve |
| Post-2008 (dual-curve, CSA) | Index/forward curve (LIBOR → term SOFR) | OIS curve (SOFR-based) |

<MatchConcepts
  client:visible
  question="Match each curve or term to its role."
  pairs={[
    { term: 'Projection curve', definition: 'Builds the forward floating cash flows from the index' },
    { term: 'Discount (OIS) curve', definition: 'PVs cash flows at the rate collateral earns' },
    { term: 'CSA', definition: 'Agreement requiring daily collateral posting' },
    { term: 'OIS rate', definition: 'Overnight indexed swap rate; the collateral funding rate' },
    { term: 'Pre-2008 single curve', definition: 'One LIBOR curve did both projecting and discounting' },
  ]}
  explanation="Post-crisis, the projection curve generates the floating flows from the index while the OIS curve discounts them, because a CSA forces collateral posting and that collateral earns the OIS (overnight) rate. Before 2008, a single LIBOR curve did both jobs."
/>

## Why it matters: valuations, hedges, and the basis

<MCQ
  client:visible
  pretest
  question="Which statement reflects the modern (post-2008) reality?"
  options={[
    { text: 'Projection and discounting use two different curves for a collateralised swap', correct: true },
    { text: 'Projection and discounting always use the same single curve' },
    { text: 'Discounting is irrelevant once a swap is collateralised' },
    { text: 'The OIS curve is used only to project floating cash flows' },
  ]}
  explanation="The defining misconception is assuming one curve does both jobs — true pre-2008, false now. Collateralised swaps project on the index/forward curve and discount on the OIS curve."
/>

**Valuation moves.** Switching the discount curve from LIBOR to OIS re-prices every swap, and the effect grows with maturity and with how off-market the fixed rate is. For long-dated or deeply in/out-of-the-money trades, the PV difference between LIBOR- and OIS-discounting can run into real money — the kind of revaluation that resized entire derivative books in 2009–2010.

**Hedge ratios move.** Your risk now lives on *two* curves, so you carry sensitivities to both. A swap's DV01 splits into exposure to the **discount (OIS) curve** and exposure to the **projection (forward) curve**; hedging only one leaves the other naked. Dual-curve risk reporting is now standard precisely because a single-curve hedge under-hedges.

**The basis becomes a traded thing.** The gap between the two curves — **LIBOR-OIS** (and today the **SOFR basis**) — is itself a market with its own quotes and its own swaps (basis swaps). It widens in funding stress (a classic crisis stress gauge) and tightens in calm. What used to be assumed away is now a P&L line.

<Callout variant="warning" title="Uncollateralised? Now you need a funding adjustment">
The clean "discount at OIS" logic assumes a CSA with cash collateral. **Uncollateralised** trades (with some corporates, say) have no collateral earning the overnight rate, so the dealer funds the position at its own (higher) funding rate. Capturing that gap requires a **funding/valuation adjustment** — part of the **XVA** family (FVA, CVA, DVA, …). We only name it here; the full XVA stack is its own beast for a later lesson.
</Callout>

<Reveal client:visible buttonLabel="Why does ignoring the dual curve under-hedge a book?">
Because a swap's value depends on two curves, its true risk is two-dimensional: a piece sensitive to the OIS (discount) curve and a piece sensitive to the projection (forward) curve. If you build one blended curve and hedge against shifts in it alone, you neutralise a *combination* of the two risks but leave the *difference* — the basis risk between them — unhedged. When LIBOR-OIS / SOFR basis then moves (as it does sharply in stress), the unhedged basis exposure shows up as surprise P&L. Splitting the curves makes both risks explicit so each can be hedged with the right instrument.
</Reveal>

## Putting it together

The **swap curve** is the term structure of par swap rates — the world's benchmark rates curve — and its slope encodes expected short rates plus a term premium. **Bootstrapping** strips that surface into **discount factors** by the no-arbitrage recursion $DF_n = (1 - s_n \sum_{i=1}^{n-1} \tau_i DF_i)/(1 + s_n \tau_n)$, and from the discount factors you read off **zero rates** and **implied forwards** — which sit *above* the par curve when it slopes up and *below* when it inverts. Since 2008, the single LIBOR curve split in two: a **projection curve** generates the floating cash flows while the **OIS curve** discounts them, because cash collateral under a **CSA** earns the overnight rate, making OIS the true funding/discount rate. That dual-curve world changes valuations, doubles your hedge dimensions, and turns the **LIBOR-OIS / SOFR basis** into a traded risk — while uncollateralised trades need a separate **XVA** funding adjustment.

<MindMap
  client:visible
  title="The swap curve & OIS discounting"
  caption="From quoted par rates down to discount factors and forwards, and out into the post-2008 dual-curve world where projection and discounting live on separate curves."
  root={{
    label: 'Swap Curve & Discounting',
    children: [
      {
        label: 'The swap curve',
        children: [
          { label: 'Term structure of par swap rates' },
          { label: "World's benchmark rates curve" },
          { label: 'Slope = expected rates + term premium' },
          { label: 'Inverted = market expects cuts' },
        ],
      },
      {
        label: 'Bootstrapping',
        children: [
          { label: 'Par rates → discount factors' },
          { label: 'No-arbitrage recursion' },
          { label: 'DFn from sn and earlier DFs' },
          { label: '→ zero rates and forwards' },
        ],
      },
      {
        label: 'Forwards vs par',
        children: [
          { label: 'Upward par curve → forwards above' },
          { label: 'Inverted par curve → forwards below' },
          { label: 'Par = running average of forwards' },
        ],
      },
      {
        label: 'OIS discounting',
        children: [
          { label: 'Pre-2008: one LIBOR curve did both' },
          { label: 'CSA: cash collateral earns overnight rate' },
          { label: 'Discount at OIS (SOFR-based)' },
          { label: 'Dual curves: project vs discount' },
        ],
      },
      {
        label: 'Why it matters',
        children: [
          { label: 'Re-prices long-dated swaps' },
          { label: 'Risk on two curves → hedge both' },
          { label: 'LIBOR-OIS / SOFR basis is traded' },
          { label: 'Uncollateralised → XVA funding adj.' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: the swap curve & OIS discounting"
  questions={[
    {
      question: 'The swap curve is:',
      options: [
        { text: 'The set of par swap rates plotted against tenor', correct: true },
        { text: 'The fixed coupon of a single swap' },
        { text: 'The schedule of a swap’s floating payments' },
        { text: 'The OIS rate at a single maturity' },
      ],
      explanation: 'It is a term structure — par (fair fixed) swap rates across maturities — not one rate or one swap’s cash flows.',
    },
    {
      question: 'With annual payments, DF₁ = 0.9615 and the 2y par swap rate is 4.50%. DF₂ equals approximately:',
      options: [
        { text: '0.9156', correct: true },
        { text: '0.9615' },
        { text: '0.8830' },
        { text: '1.0450' },
      ],
      explanation: 'DF₂ = (1 − s₂·DF₁)/(1 + s₂) = (1 − 0.045 × 0.9615)/1.045 = 0.95673/1.045 ≈ 0.9156. That is the no-arbitrage bootstrap step using the earlier discount factor.',
    },
    {
      question: 'When the par swap curve slopes upward, the implied 1-year forwards:',
      options: [
        { text: 'Lie above the par curve', correct: true },
        { text: 'Lie below the par curve' },
        { text: 'Sit exactly on the par curve' },
        { text: 'Are always negative' },
      ],
      explanation: 'Each longer par rate is a running average of short (forward) rates; to keep dragging that average up, the marginal forward rate must lead above par. Inversion flips this — forwards drop below par.',
    },
    {
      question: 'A collateralised swap under a CSA is discounted on the OIS curve because:',
      options: [
        { text: 'Posted cash collateral earns the overnight rate, so OIS is the true funding/discount rate', correct: true },
        { text: 'OIS rates are always higher than LIBOR' },
        { text: 'Collateral removes the need to discount at all' },
        { text: 'Regulators ban the use of any other curve' },
      ],
      explanation: 'Cash collateral accrues at the overnight rate; by no-arbitrage the discount rate must match what the collateral earns — the OIS rate. That is the whole basis of OIS discounting.',
    },
    {
      question: 'In the post-2008 dual-curve framework, the projection curve and the discount curve are:',
      options: [
        { text: 'Two separate curves — the index/forward curve projects, the OIS curve discounts', correct: true },
        { text: 'Always the same single curve' },
        { text: 'Both equal to the counterparty’s bond yield' },
        { text: 'Irrelevant once a CSA is in place' },
      ],
      explanation: 'The defining shift is splitting the jobs: project floating cash flows on the index/forward curve, discount them on the OIS curve. Assuming one curve does both is the pre-2008 mistake.',
    },
  ]}
/>

Next — **SOFR and the death of LIBOR** — we follow the overnight rate that took over: how a scandal-ridden survey rate was retired and a transaction-based benchmark rebuilt the curves from the ground up.
