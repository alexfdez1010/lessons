---
title: "Hedging a Rate Book with Swaps"
description: "Neutralise a rate book with swaps: compute a swap's DV01, size the hedge ratio, pick payer vs receiver, and face the residual curve, basis and convexity risks."
topic: "en/swaps-and-rate-derivatives"
order: 8
minutes: 14
updated: 2026-06-12
---

import { Callout, MCQ, Quiz, FillBlank, MatchConcepts, MindMap, ThinkReveal, Reveal, ImmunizationBalance } from '@/components/react';

This is the lesson the whole topic has been building toward. You learned what a swap *is* (two legs trading fixed for floating), how to *price* and *value* one off the swap curve, and how a bond's **DV01** measures its dollar sensitivity to rates. Now we fuse those: a swap is just another instrument with a DV01, so you can bolt it onto a portfolio to **cancel** the portfolio's rate risk. A trading desk's bond inventory, a bank's loan book, a pension fund's liabilities — all of them are giant bets on rates that nobody *meant* to make. The swap is the surgical tool that turns that accidental bet to zero. By the end you'll size a hedge, pick the right side, and — crucially — know exactly which risks the hedge *doesn't* kill.

<MCQ
  client:visible
  pretest
  question="A bond desk holds a portfolio that loses $50,000 for every 1 bp rise in rates. To neutralise that rate risk with an interest-rate swap, the desk should add a swap position that:"
  options={[
    { text: 'Gains roughly \$50,000 per 1 bp rise — an offsetting, opposite-signed DV01', correct: true },
    { text: 'Also loses \$50,000 per 1 bp rise, doubling the exposure' },
    { text: 'Has zero DV01, so it cannot affect the portfolio either way' },
    { text: 'Matches the bonds’ coupons rather than their DV01' },
  ]}
  explanation="Hedging means adding an offsetting DV01 of the opposite sign. If the book loses $50k per bp up, you need a swap that gains $50k per bp up so the total DV01 nets to zero. Same-signed DV01 doubles the bet; a zero-DV01 swap does nothing; coupons are irrelevant to first-order rate risk."
/>

## A swap has a DV01

<MCQ
  client:visible
  pretest
  question="Recall: DV01 is the dollar change in an instrument's value per 1 bp move in rates. A swap's DV01 is dominated by which leg?"
  options={[
    { text: 'The fixed leg — a stream of fixed payments behaves like a bond and carries the rate sensitivity', correct: true },
    { text: 'The floating leg — it resets every period and carries all the duration' },
    { text: 'Neither leg; a swap’s value never changes with rates' },
    { text: 'Both legs equally, since the notionals match' },
  ]}
  explanation="The floating leg resets to market each period, so it sits near par and has almost no duration. The fixed leg is a locked stream of payments — essentially a bond — and that's where the swap's rate sensitivity lives. So a swap's DV01 ≈ the DV01 of its fixed leg."
/>

**Analogy.** A swap is a tug-of-war between a *fixed* rope and a *floating* rope. The floating rope is elastic — it stretches to match the market every reset, so it barely pulls when rates move. The fixed rope is rigid steel: locked payments that gain or lose value exactly like a bond as rates shift. So when you ask "how much does this swap move per basis point?", you're really asking about the **fixed leg**.

**The definition.** DV01 (dollar value of a basis point) is the change in an instrument's value per 1 bp rate move:

$$
\text{DV01} = -\frac{\partial V}{\partial y}\times 0.0001.
$$

The floating leg of a vanilla swap resets to the prevailing rate each period, so right after a reset it's worth roughly par and has near-zero duration. All the sensitivity sits in the **fixed leg**, which is an annuity (the coupons) plus a notional repayment at maturity — i.e. it looks just like a **par bond** of the same tenor. Hence:

$$
\text{DV01}_{\text{swap}} \approx \text{DV01}_{\text{fixed leg}} \approx \text{DV01}_{\text{par bond, same tenor}}.
$$

**Which way does it lean?** A swap has *two* sides, and the sign of its DV01 flips depending on which you take:

- **Receiver swap** (receive fixed, pay floating): you own the fixed-rate stream, so you behave like you're **long a bond** — positive duration, you **gain when rates fall**.
- **Payer swap** (pay fixed, receive floating): you're short the fixed stream, so you behave like you're **short a bond** — negative duration, you **gain when rates rise**.

<Callout variant="info" title="Receiver = long a bond, Payer = short a bond">
Burn this into memory: a <strong>receiver</strong> swap is the duration-equivalent of <em>owning</em> a bond (rates down → you win), and a <strong>payer</strong> swap is the duration-equivalent of <em>shorting</em> a bond (rates up → you win). Picking payer vs receiver is just picking the <em>sign</em> of the DV01 you bolt onto your book.
</Callout>

**Approximate DV01 by tenor.** DV01 grows with tenor, because a longer fixed leg means more discounted cash flows reaching further out. Here are rough magnitudes per **\$1 million notional** (around a 3–4% rate environment — exact figures depend on the curve):

| Swap tenor | Approx. DV01 per \$1 mm notional |
|---|---|
| 2-year  | ~\$190 per bp |
| 5-year  | ~\$460 per bp |
| 10-year | ~\$850 per bp |
| 30-year | ~\$1,950 per bp |

Notice the 10-year carries roughly **4.5×** the DV01 of the 2-year: doubling-and-then-some per decade of tenor, because duration compounds. This is why desks quote hedges in terms of *DV01 per million* — it lets you convert any target risk into a notional.

<FillBlank
  client:visible
  question="Fill in the swap-DV01 fundamentals."
  text="A swap's DV01 lives almost entirely in its {{fixed|floating|notional}} leg, which behaves like a par {{bond|deposit|option}} of the same tenor. A {{receiver|payer|basis}} swap acts like being long a bond and gains when rates {{fall|rise|stay flat}}, while a payer swap acts like being short a bond."
  explanation="The floating leg resets to par and has near-zero duration, so the fixed leg carries the DV01 and looks like a par bond. A receiver swap (receive fixed) is long-duration — it gains when rates fall — and a payer swap is the mirror image."
/>

## The hedge ratio

<MCQ
  client:visible
  pretest
  question="Your book has a DV01 of $50,000 per bp. A 10-year swap has a DV01 of $850 per $1 mm notional. Roughly how much swap notional do you need to neutralise the book?"
  options={[
    { text: 'About \$59 million', correct: true },
    { text: 'About \$5.9 million' },
    { text: 'About \$425 million' },
    { text: 'About \$850,000' },
  ]}
  explanation="Notional = book DV01 ÷ DV01-per-unit = $50,000 ÷ $850 ≈ 58.8, i.e. ~58.8 units of $1 mm ≈ $59 million. The other answers misplace the decimal or invert the ratio."
/>

**The recipe.** To neutralise a book to first order, you want the **total** DV01 — book plus swap overlay — to land at zero. Solve for the swap notional:

$$
N_{\text{swap}} = \frac{\text{DV01}_{\text{book}}}{\text{DV01 per unit notional}},
$$

then choose **payer or receiver** so the swap's DV01 carries the **opposite sign** to the book's. Size first, sign second.

**Worked example.** A bond desk holds long-duration assets with a **book DV01 of \$50,000 per bp** — meaning the book *loses* \$50,000 for every 1 bp rise in rates (long bonds → hurt by rising rates). A **10-year swap** has a **DV01 ≈ \$850 per \$1 million notional**. Step through it:

1. **Size the hedge.**
   $$
   N_{\text{swap}} = \frac{\text{DV01}_{\text{book}}}{\text{DV01 per million}} = \frac{50{,}000}{850} \approx 58.8 \text{ million in notional}.
   $$

   (The `\$` sign sits in the prose around the formula, never inside the math.)
2. **Pick the side.** The book is *long* bonds (it loses when rates rise), so we need an overlay that *gains* when rates rise — that's a **payer** swap (pay fixed, receive floating). A receiver swap would add *more* long-duration risk, doubling down instead of hedging.
3. **Check the net.** Book: **−\$50,000** per bp. Payer swap of \$58.8 mm: $+58.8 \times 850 \approx +49{,}980$, i.e. **+\$49,980** per bp. **Net DV01 ≈ \$0** — a 1 bp move now barely scratches the combined position.

So the desk enters a **\$58.8 million 10-year payer swap**, and its parallel-rate risk is gone. The visual below shows the same idea as a balance scale: the book's DV01 on one pan, the swap overlay's on the other — level when they match.

<ImmunizationBalance
  client:visible
  title="DV01 hedge: book risk vs swap overlay"
  liabilityLabel="Book DV01 (to offset)"
  assetLabel="Swap-overlay DV01 (built)"
  longLabel="10-year payer-swap weight"
  shortLabel="2-year payer-swap weight"
  matchedStatus="Hedged — DV01s matched"
  tooShortStatus="Under-hedged — residual risk left"
  tooLongStatus="Over-hedged — risk flipped sign"
  matchedCaption="The swap overlay's DV01 now equals the book's: a small parallel rate move changes both by the same amount with opposite sign, so the combined book is neutral. That is a first-order DV01 hedge."
  mismatchCaption="The beam is tilted: the overlay's DV01 doesn't yet match the book's, so a rate move still moves the net position. Adjust the swap notional/tenor mix until the beam is level."
  shortDuration={2}
  longDuration={12}
  liabilityDuration={7}
  longWeight={40}
/>

<Callout variant="info" title="Read the balance as DV01, not duration">
The scale above is the same balance-point widget you met in immunization, but read it as a <strong>DV01 match</strong>: the left pan is the risk you must offset (the book), the right pan is the DV01 you build with the swap overlay (here a blend of tenors). Level beam = matched DV01 = hedged. Tilt = residual risk. The mechanics — weight a long-tenor and short-tenor swap to hit a target sensitivity — are exactly how desks size real overlays.
</Callout>

<ThinkReveal
  client:visible
  prompt="A book has DV01 = $30,000 per bp and GAINS when rates rise (it's net short bonds). A 5-year swap has DV01 ≈ $460 per $1 mm. What notional and which side hedges it?"
  hint="Notional = book DV01 ÷ DV01-per-million. The book already gains when rates rise, so you need an overlay that loses when rates rise (long-duration)."
  text="Notional = 30,000 ÷ 460 ≈ 65.2 units ≈ $65 mm. Because the book gains when rates rise (short-bond-like), the hedge must lose when rates rise — a long-duration overlay — so you enter a $65 mm 5-year RECEIVER swap (receive fixed). Receiver = long a bond, which offsets the book's short-bond tilt. Net DV01 ≈ 0."
  buttonLabel="Reveal"
/>

## Who hedges which way

<MCQ
  client:visible
  pretest
  question="A bank makes long-term fixed-rate mortgages (long-duration assets) funded by short-term deposits (short-duration liabilities). If rates rise, the bank:"
  options={[
    { text: 'Loses — its assets fall in value while its funding cost reprices up; it should pay fixed in swaps', correct: true },
    { text: 'Gains — rising rates always help banks' },
    { text: 'Is unaffected — assets and liabilities offset automatically' },
    { text: 'Should receive fixed in swaps to add more duration' },
  ]}
  explanation="Long fixed-rate assets lose value when rates rise, and short deposits reprice to the new higher rate, squeezing the bank. It's effectively long duration, so it hedges by entering PAYER swaps (pay fixed, receive floating) to add offsetting short-duration exposure."
/>

The hedge direction isn't arbitrary — it falls straight out of the **shape of your balance sheet**. Two classic institutions, two opposite trades:

**The bank (asset-liability management, ALM).** A bank holds **long-duration assets** — fixed-rate loans and mortgages — funded by **short-duration liabilities** — deposits that reprice quickly. When rates rise, the loan portfolio loses value *and* the deposit funding gets more expensive: a double squeeze. The bank is effectively **long duration**, so it enters **payer swaps** — *receive floating* (which moves with its deposit cost) and *pay fixed* (locking the spread). The payer swap's "gain when rates rise" exactly counters the bank's "lose when rates rise."

**The pension / insurer (liability-driven investing, LDI).** A pension promises payouts decades out, so its **liabilities are extremely long-duration** — often far longer than the bonds it can buy. When rates fall, the present value of those promises balloons, opening a funding hole. To plug it, the fund **receives fixed in long-dated swaps**, which behaves like owning a very long bond — gaining when rates fall, right alongside the liability. This **extends asset duration** to match the liability without having to find scarce 30-year+ physical bonds.

| Institution | Natural exposure | Pain scenario | Hedge | Why |
|---|---|---|---|---|
| Bank (ALM) | Long-duration assets, short-duration funding | Rates **rise** | **Payer** swap | Gains when rates rise; pays fixed, receives floating ≈ deposit cost |
| Pension/insurer (LDI) | Very long-duration liabilities | Rates **fall** | **Receiver** swap | Gains when rates fall; extends asset duration to match liabilities |

<MatchConcepts
  client:visible
  question="Match each player to the swap side that hedges its rate risk."
  pairs={[
    { term: 'Bank with fixed-rate loans, deposit funding', definition: 'Payer swap — gains when rates rise, offsets long-asset risk' },
    { term: 'Pension fund with 30-year liabilities', definition: 'Receiver swap — gains when rates fall, extends asset duration' },
    { term: 'Bond desk long a Treasury inventory', definition: 'Payer swap — adds short-duration to offset the long bonds' },
    { term: 'Investor who shorted bonds', definition: 'Receiver swap — adds long-duration to offset the short' },
  ]}
  explanation="Long-duration exposure (banks, long bond inventory) is hedged by paying fixed (payer swap = short a bond). Long-duration liabilities or short-bond positions need added long-duration, hedged by receiving fixed (receiver swap = long a bond)."
/>

## The residual risks

<MCQ
  client:visible
  pretest
  question="You've DV01-matched your book with a single 10-year swap. Which statement is true?"
  options={[
    { text: 'You’ve neutralised parallel shifts only; curve twists, basis and convexity risks remain', correct: true },
    { text: 'Your book is now completely risk-free' },
    { text: 'You’ve hedged every possible rate move, including steepening' },
    { text: 'You’ve eliminated credit risk as well as rate risk' },
  ]}
  explanation="A single-tenor DV01 hedge cancels first-order PARALLEL shifts only. Curve reshaping (twists/steepening), basis between indices, and convexity (DV01 changing as rates move) all survive. The hedge is powerful but not a force field."
/>

A DV01-matched hedge is excellent — and **not** a force field. It zeroes out *one specific* risk and leaves several others standing. Know them or get blindsided:

**1. Curve risk (twists and steepening).** A single-tenor swap assumes rates move in a **parallel** shift — every point on the curve up or down by the same amount. But curves *twist*: the 2-year can rise while the 30-year falls (a flattener), or vice versa. If your book's risk is concentrated at the 5-year but you hedged with a 10-year swap, a steepening leaves you exposed even though your *total* DV01 reads zero. **Fix:** hedge with swaps at **multiple tenors**, matching **key-rate DV01s** (the DV01 attributable to each maturity bucket) rather than one lumped number.

**2. Basis risk.** Your book might earn an index the swap doesn't reference. A loan tied to **Prime** hedged with a **SOFR** swap leaves you exposed to the *spread* between Prime and SOFR drifting. The two move together — mostly — but "mostly" is where basis risk lives. **Fix:** hedge with the matching index where possible, or accept and size the residual basis.

**3. Convexity.** DV01 itself isn't constant — it **changes as rates move** (that's convexity, the second-order term). A hedge that's perfectly DV01-matched today drifts out of balance after a large rate move, because the book's DV01 and the swap's DV01 change by *different* amounts. **Fix:** **re-hedge dynamically** — rebalance the swap notional as rates move and as time passes (the book and swap both season).

<Callout variant="warning" title="A DV01-matched hedge is NOT risk-free">
The single most expensive misconception in rate hedging: "my DV01 nets to zero, so I'm flat." You are flat to a <em>small parallel shift</em> — and nothing else. Curve twists, index basis, convexity, and the simple passage of time all keep grinding on the position. A DV01 hedge is the <em>first</em> line of defence, not the last. Professionals layer key-rate hedges on top and re-balance constantly.
</Callout>

<Reveal client:visible buttonLabel="Show the three residuals">
After a single-tenor DV01 hedge, three risks remain: <strong>curve risk</strong> (the curve twists/steepens, not just shifts parallel — fix with multi-tenor / key-rate DV01 hedges), <strong>basis risk</strong> (your index ≠ the swap's index, e.g. Prime vs SOFR — fix by matching the index), and <strong>convexity</strong> (DV01 changes as rates move, so the match drifts — fix by re-hedging dynamically). DV01-matching kills first-order parallel risk only.
</Reveal>

## Putting it together

A swap is just another instrument with a **DV01**, concentrated in its **fixed leg**, which behaves like a par bond of the same tenor — so a **receiver** swap is long-duration (gains when rates fall, like owning a bond) and a **payer** swap is short-duration (gains when rates rise, like shorting one). To hedge a book you compute $N_{\text{swap}} = \text{DV01}_{\text{book}} / \text{DV01 per unit notional}$, then pick the side whose DV01 carries the **opposite sign** to the book's. A **bank** with long fixed-rate assets pays fixed (payer); a **pension** with long-dated liabilities receives fixed (receiver, LDI). And the catch that separates pros from amateurs: a DV01 match neutralises **parallel shifts only** — **curve, basis and convexity** risk all survive, so real desks hedge multiple key-rate buckets and re-balance dynamically. That's the full arc of this topic: from a single swap's two legs, to pricing and valuing it off the curve, to wielding it as the surgical instrument that flattens an entire rate book's risk.

<MindMap
  client:visible
  title="Hedging a rate book with swaps"
  caption="A swap's DV01 (fixed-leg-driven) lets you size a hedge ratio and pick payer vs receiver to zero a book's DV01 — neutralising parallel shifts, while curve, basis and convexity risk remain."
  root={{
    label: 'Hedging with swaps',
    children: [
      {
        label: 'A swap has a DV01',
        children: [
          { label: 'Lives in the fixed leg (≈ par bond)' },
          { label: 'Floating leg ≈ par, ~zero duration' },
          { label: 'Receiver = long a bond (up when rates fall)' },
          { label: 'Payer = short a bond (up when rates rise)' },
          { label: 'DV01 grows with tenor' },
        ],
      },
      {
        label: 'Hedge ratio',
        children: [
          { label: 'N = book DV01 ÷ DV01 per unit' },
          { label: 'Size first, then pick the sign' },
          { label: 'Opposite-signed DV01 to offset' },
          { label: 'Ex: \$50k ÷ \$850 ≈ \$59 mm payer' },
        ],
      },
      {
        label: 'Who hedges which way',
        children: [
          { label: 'Bank (ALM): long assets → payer' },
          { label: 'Pension (LDI): long liabilities → receiver' },
          { label: 'Match exposure sign, then offset' },
        ],
      },
      {
        label: 'Residual risks',
        children: [
          { label: 'Curve risk → key-rate / multi-tenor' },
          { label: 'Basis risk → match the index' },
          { label: 'Convexity → re-hedge dynamically' },
          { label: 'DV01 match ≠ risk-free' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: hedging a rate book"
  questions={[
    {
      question: 'A swap’s DV01 is dominated by which leg, and why?',
      options: [
        { text: 'The fixed leg — it’s a locked stream like a bond, while the floating leg resets to par with ~zero duration', correct: true },
        { text: 'The floating leg — it carries all the duration because it changes every period' },
        { text: 'Neither — a swap’s value is rate-insensitive' },
        { text: 'Both equally, since notionals match' },
      ],
      explanation: 'The floating leg resets to market each period and sits near par, so it has almost no duration. The fixed leg is an annuity-plus-notional that behaves like a par bond — that is where the swap’s DV01 lives.',
    },
    {
      question: 'A book loses \$40,000 per bp when rates rise. A 10-year swap has DV01 ≈ \$850 per \$1 mm. The correct hedge is approximately:',
      options: [
        { text: 'A \$47 mm 10-year PAYER swap', correct: true },
        { text: 'A \$47 mm 10-year RECEIVER swap' },
        { text: 'A \$4.7 mm 10-year payer swap' },
        { text: 'A \$340 mm 10-year receiver swap' },
      ],
      explanation: 'Notional = 40,000 ÷ 850 ≈ 47 units ≈ \$47 mm. The book loses when rates rise (long-duration), so the hedge must gain when rates rise — a payer swap. A receiver would double the long-duration risk.',
    },
    {
      question: 'A pension fund with very long-dated liabilities wants to protect against falling rates. It should:',
      options: [
        { text: 'Receive fixed in long-dated swaps to extend asset duration toward the liability', correct: true },
        { text: 'Pay fixed in long-dated swaps to shorten its duration' },
        { text: 'Avoid swaps entirely and hold cash' },
        { text: 'Enter short-tenor payer swaps' },
      ],
      explanation: 'Falling rates inflate the present value of long liabilities. A receiver swap behaves like a long bond — it gains when rates fall — so receiving fixed extends asset duration to match the liability. That is the core LDI trade.',
    },
    {
      question: 'After DV01-matching a book with a single 10-year swap, which risk is NOT addressed?',
      options: [
        { text: 'Curve risk — a twist/steepening can still move the net position even with zero total DV01', correct: true },
        { text: 'A small parallel shift in the entire curve' },
        { text: 'First-order sensitivity to a uniform rate move' },
        { text: 'The book’s overall direction to a flat rate change' },
      ],
      explanation: 'A single-tenor DV01 hedge zeroes first-order PARALLEL shifts. Curve reshaping (twists, steepeners/flatteners) is left exposed when book and hedge sit at different maturities — that’s why desks match key-rate DV01s.',
    },
    {
      question: 'A loan book earning Prime is hedged with a SOFR swap of matched DV01. The leftover exposure is best described as:',
      options: [
        { text: 'Basis risk — the Prime/SOFR spread can drift even when both rates move together', correct: true },
        { text: 'Pure parallel-shift risk, fully removed by the DV01 match' },
        { text: 'Credit risk on the swap counterparty only' },
        { text: 'No risk at all, since both are interest rates' },
      ],
      explanation: 'When the book’s index (Prime) differs from the hedge’s index (SOFR), the spread between them can move, leaving basis risk even after a perfect DV01 match. The fix is to hedge with the matching index or to size and accept the residual basis.',
    },
  ]}
/>

That's every teaching lesson in the topic. Next up is the **final exam** — one shot, one question at a time, no going back. Bring everything: swap mechanics, pricing off the curve, valuation, and the DV01 hedging you just mastered.
