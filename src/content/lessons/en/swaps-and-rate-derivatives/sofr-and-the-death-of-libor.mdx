---
title: "SOFR and the Death of LIBOR"
description: "How LIBOR — a survey-based unsecured term rate — was rigged, then replaced by SOFR: secured, overnight, transaction-based, and compounded in arrears."
topic: "en/swaps-and-rate-derivatives"
order: 5
minutes: 13
updated: 2026-06-12
---

import { Callout, MCQ, Quiz, FillBlank, MatchConcepts, MindMap, ThinkReveal, Reveal, RatePathChart } from '@/components/react';

For four decades, the floating leg of nearly every swap, the reset on your mortgage, and the coupon on hundreds of trillions of dollars of loans all pointed at a single number: **LIBOR**. It was the heartbeat of global finance. It was also, it turned out, a number that a handful of banks essentially *made up over the phone* — and that, after the financial crisis, described a market that had largely stopped existing. This lesson is the autopsy and the rebirth: why LIBOR died, what **SOFR** is, and the one genuinely awkward consequence of the switch — that you now have to **compound an overnight rate in arrears** to get the period rate you used to be quoted up front.

<MCQ
  client:visible
  pretest
  question="LIBOR was famously described as 'a rate in search of a market.' What did that jab mean?"
  options={[
    { text: 'After the crisis, banks barely borrowed unsecured for a term, so the rate rested on banks’ judgement rather than real transactions', correct: true },
    { text: 'No bank ever actually used LIBOR in a contract' },
    { text: 'LIBOR was only quoted in currencies nobody traded' },
    { text: 'The market for LIBOR moved to a different time zone' },
  ]}
  explanation="LIBOR was supposed to reflect what banks pay to borrow unsecured for a term. But post-2008 that interbank borrowing dried up, so panel banks were 'estimating' a price for trades that scarcely happened — a benchmark floating on judgement, not transactions."
/>

## What LIBOR was: a benchmark built on banks' word

**Analogy.** Imagine setting the national price of milk by phoning eighteen dairy farmers each morning and asking, *"What do you reckon you'd pay for milk today?"* — then averaging their guesses. Nobody has to actually buy any milk. That guess-based average was LIBOR.

**The definition.** LIBOR — the **London Interbank Offered Rate** — answered one question: *at what rate could a leading bank borrow* **unsecured** *funds from another bank, for a given* **term**? Every business day, a panel of major banks each **submitted** an estimate for several tenors (overnight, 1-month, 3-month, 6-month, 12-month) in several currencies. The top and bottom submissions were trimmed and the rest averaged. That average, published, became the reference rate that floating legs, loans, and adjustable mortgages reset against.

Three features matter, because SOFR will overturn every one of them:

- **Survey-based.** It came from banks' *submissions* — what they said — not from a ledger of actual trades.
- **Unsecured.** It priced lending with *no collateral*, so it embedded the **credit risk** of banks: the chance the borrowing bank goes bust before repaying.
- **Term.** It was quoted *for a period in advance* — a 3-month LIBOR fixed today told you the rate for the next three months, known up front.

<Callout variant="info" title="Why the credit component was a feature, then a bug">
Because LIBOR was unsecured bank-to-bank lending, it rose whenever banks feared *each other*. In 2008, 3-month LIBOR spiked far above central-bank rates — a real-time fever chart of banking stress. Lenders liked that: a bank's funding cost and its loan benchmark moved together. The problem is that the same credit sensitivity made LIBOR jumpy and, ultimately, manipulable.
</Callout>

<FillBlank
  client:visible
  question="Pin down LIBOR's three defining traits."
  text="LIBOR was {{survey|transaction|auction}}-based — built from banks' submissions, not real trades. It was {{unsecured|secured|collateralised}}, so it carried banks' credit risk. And it was a {{term|overnight|spot}} rate, quoted for a period in advance."
  explanation="Survey-based (estimates, not trades), unsecured (no collateral, so credit-sensitive), and term (known up front for the whole period). SOFR will invert all three."
/>

## Why LIBOR died: a scandal on top of structural rot

**Analogy.** LIBOR didn't die of one disease. It had a *heart attack* — a public scandal — sitting on top of a chronic, terminal *organ failure* nobody had treated.

**Failure one: the rigging scandal (2012).** Because submissions were *opinions*, they could be *nudged*. Investigators found traders at multiple banks colluding to shade their firm's LIBOR submissions up or down — sometimes to flatter the bank's apparent health, more cynically to profit on their own swap and futures positions that paid off if LIBOR fixed a hair higher or lower. A few basis points, multiplied across trillions in notional, is real money. Billions in fines followed; criminal cases were brought. A global benchmark had been treated as a tradeable knob.

**Failure two: the rate had no market underneath it.** This was the deeper, quieter killer. After 2008, banks stopped lending to each other *unsecured for a term* — it was too risky, and regulation made it expensive. So the very transactions LIBOR was meant to summarise had largely **vanished**. Panel banks were left submitting expert *judgement* about a price that almost no real trade was setting. Regulators called it **"a rate in search of a market."** A benchmark resting on opinion isn't just manipulable — it's *unanchored*.

So the UK's **Financial Conduct Authority (FCA)**, LIBOR's regulator, announced it would stop compelling banks to submit, setting LIBOR's end date in motion. You can't fine your way out of a rate that no longer measures anything.

<MCQ
  client:visible
  question="Select EVERY reason LIBOR was retired."
  multiple
  options={[
    { text: 'Traders colluded to manipulate submissions for profit', correct: true },
    { text: 'Submissions were estimates, not actual transactions', correct: true },
    { text: 'Post-crisis, the unsecured term interbank lending it measured had dried up', correct: true },
    { text: 'It was published in too few currencies to be useful' },
    { text: 'It rose during the 2008 crisis, which regulators considered illegal' },
  ]}
  explanation="Two intertwined failures: the manipulation scandal (submissions were a riggable opinion) AND the structural rot (it was survey-based, and the unsecured term market beneath it had evaporated). Currency coverage wasn't the issue, and LIBOR rising in a crisis was a true signal, not a crime."
/>

## SOFR, the replacement: secured, overnight, real

**Analogy.** If LIBOR was *"what do you reckon milk costs?"*, SOFR is a till-roll: it tots up **every actual milk sale that happened yesterday** and reports the real average price. No opinions, no phone calls — just the tape.

**The definition.** **SOFR** — the **Secured Overnight Financing Rate** — is the rate at which firms borrow cash **overnight**, **secured** by **US Treasury** collateral, in the **repo** (repurchase agreement) market. The New York Fed publishes it daily as a volume-weighted median of those *actual trades* — roughly **a trillion dollars-plus of real transactions every day**. Its four properties are the exact mirror image of LIBOR's:

- **Secured.** Loans are collateralised by Treasuries. If the borrower fails, you keep the collateral — so there's essentially **no bank credit risk** baked in.
- **Overnight.** It's a one-day rate, not a term rate. (We'll deal with that wrinkle next.)
- **Transaction-based.** Computed from a deep tape of real trades, not a survey — nothing to "submit," and nothing to nudge.
- **Nearly risk-free.** With collateral and an overnight horizon, SOFR is about as close to a true risk-free rate as a real market produces.

| | **LIBOR** | **SOFR** |
|---|---|---|
| Collateral | **Unsecured** | **Secured** (US Treasuries) |
| Horizon | **Term** (1m / 3m / 6m, set in advance) | **Overnight** (one day) |
| Source | **Survey** of bank submissions | **Transactions** (~\$1T+/day of repo) |
| Credit content | Bank credit risk embedded | **Nearly risk-free**, no bank credit |
| Manipulability | High — it's an opinion | Low — it's the tape |

<Callout variant="warning" title="SOFR has no built-in credit spread — and that bites in a crisis">
This is the most important practical difference. LIBOR *rose* when banks feared each other; SOFR, being secured and risk-free, **does not**. In a banking panic, a lender funded with deposits whose loans reset on SOFR earns the same low rate even as its own funding costs climb — its margin gets squeezed exactly when stress hits. LIBOR's credit sensitivity was a natural hedge for bank lenders; SOFR removes it. That gap is why "credit-sensitive" alternatives and explicit spread add-ons exist — and why the conversion of old contracts needs a spread adjustment (coming up).
</Callout>

The chart below shows the same idea swaps people care about: lock a fixed rate, or float on an overnight benchmark that can drift up, down, or sideways. The flat line is the certainty LIBOR-style term setting *felt* like; the moving line is the overnight reality you now have to compound.

<RatePathChart
  client:visible
  title="Lock it in, or float on the overnight rate?"
  fixedLabel="Fixed rate"
  variableLabel="Overnight-linked rate"
  scenarioLabel="What does the overnight rate do?"
  risingLabel="Rates rise"
  fallingLabel="Rates fall"
  flatLabel="Rates flat"
  fixedPaymentReadoutLabel="Fixed payment"
  variablePaymentReadoutLabel="Overnight-linked payment (end of term)"
  caption="A fixed coupon is a flat line — known up front, the way LIBOR was quoted. An overnight-linked coupon re-prices with the market: it falls when rates fall and bites when they rise. SOFR lives on that moving line, which is why you have to compound it to know what a period actually cost."
/>

<MatchConcepts
  client:visible
  question="Match each property to the benchmark it belongs to."
  pairs={[
    { term: 'Secured by US Treasuries', definition: 'SOFR' },
    { term: 'Unsecured interbank lending', definition: 'LIBOR' },
    { term: 'Built from a daily survey of submissions', definition: 'LIBOR' },
    { term: 'Built from ~$1T/day of real repo trades', definition: 'SOFR' },
    { term: 'Embeds bank credit risk; rises in panics', definition: 'LIBOR' },
    { term: 'Nearly risk-free; no built-in credit spread', definition: 'SOFR' },
  ]}
  explanation="LIBOR = unsecured, survey-based, credit-sensitive term rate. SOFR = secured, transaction-based, nearly risk-free overnight rate. Every property flips."
/>

## The compounding-in-arrears challenge

**The catch.** SOFR is *overnight*. But your swap's floating leg pays over a **period** — say three months. So how do you turn 90-odd one-day rates into one period rate? You **compound** them: roll the money forward day by day, each day earning that day's SOFR. For a period made of days $t$, the compounded period rate is

$$
\text{Period rate} = \prod_{t} \left(1 + \text{SOFR}_t \cdot \frac{d_t}{360}\right) - 1,
$$

where $d_t$ is the number of calendar days that day's rate applies (a weekend uses Friday's rate for all three days) and we divide by 360 for the money-market day count. It's the same "interest on interest" you've seen since the very first lesson — just applied one day at a time.

**The genuinely new problem.** LIBOR was set **in advance**: on day one you *knew* the rate for the whole next period. Compounded SOFR is only known **in arrears** — you can't finish the product until the *last* day of the period has printed. You learn the period's rate roughly when the payment is due, not at the start. That's not a bug; it's the unavoidable cost of using a real overnight rate instead of a forecast.

**Worked example.** Take a tiny five-business-day stub, Monday through Friday, on a notional of \$10,000,000. Friday's rate covers the weekend (Saturday and Sunday too), so it applies for 3 days. Each daily factor takes the form $\left(1 + \text{SOFR}_t \cdot \frac{d_t}{360}\right)$:

| Day | SOFR | Days applied | Daily factor | Cumulative factor |
|---|---|---|---|---|
| Mon | 5.30% | 1 | 1.00014722 | 1.00014722 |
| Tue | 5.31% | 1 | 1.00014750 | 1.00029474 |
| Wed | 5.32% | 1 | 1.00014778 | 1.00044257 |
| Thu | 5.31% | 1 | 1.00014750 | 1.00059013 |
| Fri | 5.33% | 3 | 1.00044417 | 1.00103456 |

Multiply the daily factors together and you get $F = 1.00103456$. Subtract 1 and the **compounded period rate is 0.10346%** over those 7 calendar days. On \$10,000,000 that's a floating-leg accrual of $10{,}000{,}000 \times 0.0010346 \approx 10{,}346$ — i.e. **\$10,346**.

Annualise it ($0.0010346 \times \frac{360}{7}$) and you get about **5.32%** — sensibly between the daily rates, exactly as it should be. Notice you couldn't have written that \$10,346 cheque on Monday: Friday's print is part of the product, so the number only finalises at the end.

<ThinkReveal
  client:visible
  prompt="Suppose every day in a 90-day period printed exactly 5.00% SOFR. Roughly what is the compounded period rate, and why is it a hair ABOVE 5% × 90/360 = 1.25%?"
  hint="Simple interest sums the daily rates; compounding multiplies (1 + daily) factors, so you earn a tiny bit of interest-on-interest."
  text="About 1.2535%. Simple interest would give 5% × 90/360 = 1.2500%. Compounding multiplies 90 daily factors of (1 + 0.05/360), giving (1 + 0.05/360)^90 − 1 ≈ 0.012535 = 1.2535%. The small excess over 1.25% is the interest-on-interest — each day you also earn a sliver on the prior days' accrued interest. Over short stubs the gap is tiny; over longer, higher-rate periods it grows."
  buttonLabel="Reveal"
/>

<Callout variant="info" title="Conventions that buy back some certainty">
Pure in-arrears compounding leaves zero time between knowing the rate and paying it. Markets patch this with conventions: a **lookback** (use SOFR from a few days earlier, e.g. a 5-day lookback, so the final inputs are known before payment day), an **observation shift**, or a **payment delay** (pay a couple of days after the period ends). These give operations teams room to compute and settle, without abandoning the real overnight rate.
</Callout>

**For products that truly need the rate up front.** Some cash products — a small-business loan, a consumer mortgage — *can't* tell the borrower their rate only at the end. For those, the CME publishes **Term SOFR**: a *forward-looking* SOFR term rate (1m / 3m / 6m) derived from SOFR futures, so it's known **in advance** like LIBOR was, while still being rooted in the SOFR market. Most derivatives use compounded SOFR; many loans use Term SOFR.

**Converting legacy LIBOR contracts.** A swap written against 3-month LIBOR can't just be repointed at SOFR — SOFR is *lower*, because it lacks LIBOR's credit component. Repapering it cleanly would hand value from one counterparty to the other. The fix is the **Credit Spread Adjustment (CSA)**: a fixed add-on, defined in the **ISDA fallback** as the **median LIBOR-minus-SOFR difference over the five years before LIBOR's cessation**. Bolt that historical spread onto compounded SOFR and the converted contract lands economically close to the old LIBOR one — no windfall, no robbery.

<MCQ
  client:visible
  question="A SOFR floating leg pays every three months. When do you definitively know that period's floating rate?"
  options={[
    { text: 'Only near the END of the period — you must compound the daily rates, including the last day, before the number finalises', correct: true },
    { text: 'At the START of the period, exactly as LIBOR was set' },
    { text: 'Never — SOFR period rates are inherently unknowable' },
    { text: 'Exactly halfway through the period' },
  ]}
  explanation="Compounded SOFR is known in arrears: the product of daily factors isn't complete until the last day prints. Conventions like a lookback or payment delay give a small head start, but the rate is fundamentally an end-of-period number — the opposite of LIBOR's set-in-advance term rate. Term SOFR is the carve-out for products that need it up front."
/>

## The transition: how the world actually switched

**The timeline.** This wasn't a flip of a switch; it was a multi-year migration with hard deadlines:

- **End of 2021:** banks stopped writing *new* contracts referencing USD LIBOR.
- **~June 2023:** the remaining USD LIBOR tenors (including the heavily used 1m, 3m, 6m) **ceased** publication.
- **Legacy trades** still tied to LIBOR fell back automatically via the **ISDA fallback protocol**: counterparties who adhered agreed in advance that, on cessation, their LIBOR references would switch to **compounded SOFR plus the fixed credit spread adjustment**. "Tough legacy" contracts that couldn't be repapered got legislative backstops naming a SOFR-based replacement.

The result: trillions of dollars of swaps, loans, and securities were re-pointed from a phoned-in survey rate to a transaction-based overnight rate, mostly without litigation, because the fallback math was agreed *before* the lights went out on LIBOR.

<Callout variant="warning" title="Bust the myth: 'SOFR is just LIBOR with a new name'">
No. It is a different *kind* of rate in four ways: **secured vs unsecured**, **overnight vs term**, **transaction-based vs survey-based**, and **risk-free vs credit-sensitive** (no built-in credit spread). That last point is why converting LIBOR contracts needs an explicit spread adjustment, and why some lenders miss LIBOR's crisis-hedging behaviour. Same job, completely different machine.
</Callout>

<Reveal client:visible>

**The one-line summary to carry forward:** LIBOR was an *opinion about an unsecured term loan*; SOFR is a *measurement of a secured overnight one*. Everything awkward about the switch — compounding in arrears, Term SOFR, the credit spread adjustment — flows directly from that single change in what the number actually *is*.

</Reveal>

<MCQ
  client:visible
  question="Why did legacy LIBOR swaps need a credit spread adjustment when falling back to SOFR, rather than a straight swap of one rate for the other?"
  options={[
    { text: 'SOFR is risk-free and structurally lower than credit-sensitive LIBOR, so a fixed historical spread is added to keep the converted contract economically fair', correct: true },
    { text: 'SOFR is always higher than LIBOR, so the spread is subtracted' },
    { text: 'Regulators required a random adjustment to discourage LIBOR use' },
    { text: 'The spread compensates for SOFR being a survey rate' },
  ]}
  explanation="SOFR carries no bank-credit component, so it sits below where LIBOR fixed. The ISDA fallback adds a fixed Credit Spread Adjustment — the median LIBOR−SOFR gap over the five years pre-cessation — so neither side gains or loses value at the switch. SOFR is lower, not higher, and it's transaction-based, not a survey."
/>

## Putting it together

LIBOR was the **London Interbank Offered Rate**: a **survey-based**, **unsecured**, **term** benchmark — banks' guesses about what they'd pay to borrow for a month or three, set **in advance**. It died of two diseases at once: a **rigging scandal** (an opinion you can nudge for profit) and a **structural rot** (the unsecured term market beneath it evaporated, leaving "a rate in search of a market"). Its replacement, **SOFR**, flips every property: **secured** by Treasuries, **overnight**, **transaction-based** (\$1T+ a day), and **nearly risk-free** — with **no built-in credit spread**, which is why it stays calm in crises that once sent LIBOR soaring. The price of using a real overnight rate is **compounding in arrears**: you multiply daily $(1 + \text{SOFR}_t \cdot d_t/360)$ factors and only know the period rate at the **end** — softened by lookbacks and payment delays, sidestepped by **Term SOFR** for products needing the rate up front, and reconciled with old contracts via the **credit spread adjustment** in the ISDA fallback. New USD LIBOR stopped end-2021, the main tenors ceased mid-2023, and legacy trades fell back to compounded SOFR plus that spread. SOFR is **not** LIBOR renamed — it's a different machine that happens to do the same job.

<MindMap
  client:visible
  title="SOFR and the death of LIBOR"
  caption="LIBOR (survey / unsecured / term / credit-sensitive) was rigged and unanchored, so it gave way to SOFR (transaction / secured / overnight / risk-free), compounded in arrears."
  root={{
    label: 'LIBOR → SOFR',
    children: [
      {
        label: 'What LIBOR was',
        children: [
          { label: 'Survey of bank submissions' },
          { label: 'Unsecured (carries bank credit risk)' },
          { label: 'Term rate, set in advance' },
          { label: 'Set hundreds of trillions in contracts' },
        ],
      },
      {
        label: 'Why it died',
        children: [
          { label: '2012 rigging scandal — submissions nudged for profit' },
          { label: 'Unsecured term market dried up post-crisis' },
          { label: '“A rate in search of a market”' },
          { label: 'FCA ended mandatory submissions' },
        ],
      },
      {
        label: 'SOFR',
        children: [
          { label: 'Secured by US Treasuries (repo)' },
          { label: 'Overnight, not term' },
          { label: 'Transaction-based (~$1T/day)' },
          { label: 'Nearly risk-free — no credit spread' },
        ],
      },
      {
        label: 'Compounding in arrears',
        children: [
          { label: 'Product of (1 + SOFR×d/360) daily factors' },
          { label: 'Period rate known only at the END' },
          { label: 'Lookback / payment delay conventions' },
          { label: 'Term SOFR = forward-looking, set up front' },
          { label: 'Credit spread adjustment for legacy LIBOR' },
        ],
      },
      {
        label: 'The transition',
        children: [
          { label: 'No new USD LIBOR after end-2021' },
          { label: 'Main USD tenors ceased ~June 2023' },
          { label: 'ISDA fallback repapered legacy trades' },
          { label: 'Myth: “SOFR is LIBOR renamed” — false' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: SOFR and the death of LIBOR"
  questions={[
    {
      question: 'Which trio of properties defined LIBOR?',
      options: [
        { text: 'Survey-based, unsecured, term', correct: true },
        { text: 'Transaction-based, secured, overnight' },
        { text: 'Survey-based, secured, overnight' },
        { text: 'Transaction-based, unsecured, term' },
      ],
      explanation: 'LIBOR was a survey of submissions (not trades), unsecured (so credit-sensitive), and a term rate set in advance. SOFR is the exact opposite on all three.',
    },
    {
      question: 'The deeper, non-scandal reason LIBOR was untenable was that:',
      options: [
        { text: 'The unsecured term interbank market it measured had largely dried up after the crisis, so submissions were judgement, not trades', correct: true },
        { text: 'Banks refused to publish it' },
        { text: 'It was denominated only in pounds sterling' },
        { text: 'It became identical to the central-bank policy rate' },
      ],
      explanation: '“A rate in search of a market”: the transactions LIBOR was meant to summarise had evaporated, leaving an unanchored estimate. The rigging scandal was the headline; this structural rot was the terminal disease.',
    },
    {
      question: 'A 3-month SOFR floating leg compounds the daily rate. When is the period rate definitively known?',
      options: [
        { text: 'Only at (or just before) the end of the period, once the last daily SOFR has printed', correct: true },
        { text: 'At the start of the period, like LIBOR' },
        { text: 'It is never knowable' },
        { text: 'Exactly when the swap is first traded' },
      ],
      explanation: 'Compounded SOFR is an in-arrears number: the product of daily factors isn’t complete until the final day. Conventions (lookback, payment delay) give a small lead time; Term SOFR is the up-front alternative for products that need it.',
    },
    {
      question: 'Five business days print SOFR of 5.30%, 5.31%, 5.32%, 5.31%, and 5.33% (Friday covering the weekend, 3 days). The compounded factor is 1.00103456. On $10,000,000 notional, the floating accrual is closest to:',
      options: [
        { text: '$10,346', correct: true },
        { text: '$1,034,560' },
        { text: '$103' },
        { text: '$53,000' },
      ],
      explanation: 'Period rate = factor − 1 = 0.00103456 = 0.10346%. Accrual = $10,000,000 × 0.0010346 ≈ $10,346. The other answers misplace the decimal or annualise incorrectly.',
    },
    {
      question: 'Why does converting a legacy LIBOR swap to SOFR require adding a credit spread adjustment?',
      options: [
        { text: 'SOFR is secured and nearly risk-free, so it sits below credit-sensitive LIBOR; a fixed historical spread keeps the converted contract fair', correct: true },
        { text: 'SOFR is higher than LIBOR, so value must be removed' },
        { text: 'Because SOFR is also a survey rate and needs smoothing' },
        { text: 'To penalise counterparties who used LIBOR' },
      ],
      explanation: 'SOFR lacks LIBOR’s bank-credit component, so it’s structurally lower. The ISDA fallback adds the median LIBOR−SOFR gap over the five years before cessation, so neither side gains or loses at the switch.',
    },
  ]}
/>

Next — **swap spreads and asset swaps** — we measure the gap between the swap rate and the government yield, and use an asset swap to strip a bond down to a clean spread over SOFR.
