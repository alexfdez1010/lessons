---
title: "Variance & Vol Swaps"
description: "Trade volatility directly with variance and vol swaps: variance vs vega notional, why long variance is long convexity, and the option-replication link to the VIX."
topic: "en/volatility-trading"
order: 2
minutes: 15
updated: 2026-06-12
---

import { Callout, MCQ, Quiz, FillBlank, MatchConcepts, Categorize, MindMap, ThinkReveal, Reveal, VarianceSwapConvexity } from '@/components/react';

You already know the difference between realized and implied volatility, and you know your way around the Greeks. So here's the natural next question: if you have a *view* on volatility — you think the market will be wilder, or calmer, than the options market is pricing — how do you actually bet on it? The obvious answer ("buy a straddle") turns out to be a surprisingly clumsy way to trade vol, for reasons we'll dismantle in a second. The clean answer is a contract that pays you the difference between the volatility that *actually happens* and a number you agree today — no delta, no gamma, no babysitting. Meet the **variance swap** and its cousin the **vol swap**.

<MCQ
  client:visible
  pretest
  question="You're convinced the next month will be far more turbulent than the options market expects. You buy a straddle to profit from the chaos. What annoying chore have you just signed up for?"
  options={[
    { text: 'Constantly delta-hedging, because the straddle picks up directional exposure as the underlying moves, and your P&L depends on the path, not just how wild it gets', correct: true },
    { text: 'Nothing — a straddle is a pure, hands-off bet on volatility' },
    { text: 'Paying a daily fee to the exchange for holding the position' },
    { text: 'Posting the full notional of the underlying upfront' },
  ]}
  explanation="A straddle is long vol, but it's a leaky bet. As the underlying drifts, the straddle accumulates delta, so unless you keep re-hedging you're really betting on direction too. And even if you hedge perfectly, the final P&L depends on the path the stock took — not just on how volatile it was. That path-dependence and constant hedging are exactly what a variance swap throws in the bin."
/>

## Why a swap to trade volatility?

**The problem with options.** Say you want to bet purely on *how much the market moves*, regardless of direction. Your first instinct is options — buy a straddle (a call plus a put), which gains from big moves either way. Reasonable, but messy. The moment the underlying ticks away from the strike, your straddle sprouts a **delta**: it starts caring which way the market goes, not just how far. To keep it a clean volatility bet you must **delta-hedge continuously** — trade the underlying over and over to neutralize that drift. That's expensive, operationally annoying, and your final profit ends up **path-dependent**: it depends on *when* the moves happened and at *what spot level*, not simply on how volatile the period was. Two months with identical realized volatility can hand a delta-hedged straddle wildly different P&L. As a vol instrument, it leaks.

**The fix: trade vol with a forward contract.** A **variance swap** (and its simpler relative the **vol swap**) is an over-the-counter (OTC) forward contract whose payoff is tied *directly* to realized volatility, with **no delta to manage**. You don't hedge anything. At expiry you compare the volatility that actually occurred against a strike fixed at the start, and cash settles the difference. It's the purest vol bet on the menu — exactly the "clean shot" the straddle can't give you.

A **vol swap** is the intuitive one. Its payoff is **linear** in realized volatility:

$$\text{payoff}_{\text{vol}} = N_{\text{vega}}\left(\sigma_{\text{realized}} - K_{\text{vol}}\right)$$

Here $\sigma_{\text{realized}}$ is the volatility the underlying actually printed over the life of the swap (in vol points, e.g. 18 means 18%), $K_{\text{vol}}$ is the **strike volatility** agreed at inception, and $N_{\text{vega}}$ is the **vega notional** — the dollars you make per **one vol point** that realized beats the strike. Long the swap, realized comes in 3 points above strike, you pocket $3 \times N_{\text{vega}}$.

A **variance swap** does the same thing but on **variance** — volatility *squared*:

$$\text{payoff}_{\text{var}} = N_{\text{var}}\left(\sigma_{\text{realized}}^2 - K^2\right)$$

where $K$ is the strike vol, so the **strike variance** is $K^2$, and $N_{\text{var}}$ is the **variance notional** — the dollars per **one variance point** ($\text{vol}^2$ unit) of difference. That little square is the entire personality of the product, as we'll see.

Because traders think in vol points but the contract pays in variance points, dealers quote a variance swap's size in **vega notional** anyway and convert. The standard bridge between the two notionals is:

$$N_{\text{vega}} = N_{\text{var}} \times 2 K_{\text{vol}}$$

The factor $2K_{\text{vol}}$ is just the slope of $\sigma^2$ at the strike (the derivative of $\sigma^2$ is $2\sigma$). It means: near the strike, a variance swap behaves like a vol swap with that vega notional. Far from the strike, the square takes over — and that's where the magic is.

<Callout variant="info" title="Two notionals, one product — don't mix them up">
**Vega notional** ($N_{\text{vega}}$) is dollars per *vol point*; **variance notional** ($N_{\text{var}}$) is dollars per *variance point* ($\text{vol}^2$). They are linked by $N_{\text{vega}} = N_{\text{var}} \times 2 K_{\text{vol}}$. When someone says "a \$100,000-vega variance swap struck at 20," they mean a variance swap whose variance notional is \$100,000 / (2 × 20) = \$2,500 per variance point, sized so it *feels* like \$100,000 per vol point right at the strike. Vega notional is the human-friendly label; variance notional is what the payoff formula actually multiplies.
</Callout>

<MatchConcepts
  client:visible
  question="Match each piece of swap vocabulary to what it actually means."
  pairs={[
    { term: 'Vol swap payoff', definition: 'N_vega × (realized vol − strike vol) — linear in vol' },
    { term: 'Variance swap payoff', definition: 'N_var × (realized variance − strike variance) — uses vol squared' },
    { term: 'Variance notional', definition: 'Dollars earned per one variance (vol²) point of difference' },
    { term: 'Vega notional', definition: 'Dollars earned per one vol point of difference' },
    { term: 'Strike variance', definition: 'The square of the strike vol, K²' },
    { term: 'N_vega = N_var × 2K', definition: 'The relation converting between the two notionals' },
  ]}
  explanation="The vol swap is linear in vol; the variance swap is linear in variance (vol squared). Variance notional pays per variance point, vega notional per vol point, and 2K (the slope of σ² at the strike) is the conversion factor. Strike variance is simply K²."
/>

## Worked settlement: run the numbers

<MCQ
  client:visible
  pretest
  question="A variance swap is struck at 20 (strike variance = 400) with variance notional $2,500 per variance point. Realized volatility comes in at 25. Before doing the arithmetic, what's the rough size of the long's payoff?"
  options={[
    { text: 'A bit over half a million dollars — because the payoff uses 25² − 20² = 625 − 400 = 225, not just 25 − 20 = 5', correct: true },
    { text: 'Exactly $12,500, from 2,500 × (25 − 20)' },
    { text: 'Zero — realized only beat the strike by 5 points' },
    { text: 'Negative, because a higher realized vol hurts the long' },
  ]}
  explanation="The variance swap pays on the squared difference: 25² − 20² = 225 variance points, times $2,500 = $562,500. The trap is treating it like a linear vol bet (5 points × something). The square is the whole point, and it's about to make the long very happy."
/>

Let's settle a real ticket. We strike a variance swap at **20** (so strike vol $K_{\text{vol}} = 20$ and strike variance $K^2 = 400$), and we size it so the vega notional is roughly **\$100,000 per vol point**. Using the conversion:

$$N_{\text{var}} = \frac{N_{\text{vega}}}{2 K_{\text{vol}}} = \frac{100{,}000}{2 \times 20} = 2{,}500 \text{ per variance point}$$

So the variance notional is \$2,500 per variance point. Now two scenarios. The payoff to the **long** (the side that *wants* volatility) is $N_{\text{var}}\left(\sigma_{\text{realized}}^2 - K^2\right)$. We'll also compute what a **vol swap** struck at the same level with \$100,000 vega notional would have paid, $N_{\text{vega}}(\sigma_{\text{realized}} - K_{\text{vol}})$, to see the difference.

| Scenario | $\sigma_{\text{realized}}$ | $\sigma_{\text{realized}}^2$ | Variance diff vs 400 | **Variance swap payoff** | Vol swap payoff |
|---|---|---|---|---|---|
| Vol spikes | 25 | 625 | +225 | 2,500 × 225 = **+\$562,500** | 100,000 × 5 = +\$500,000 |
| Vol collapses | 15 | 225 | −175 | 2,500 × (−175) = **−\$437,500** | 100,000 × (−5) = −\$500,000 |

Stare at that table, because it contains the entire lesson. In **both** scenarios realized moved **5 vol points** away from the strike. The vol swap, being linear, pays a symmetric \$500,000 either way. The variance swap does not: it pays **more on the up-move** (\$562,500 vs \$500,000) and **loses less on the down-move** (−\$437,500 vs −\$500,000).

That asymmetry — bigger gain when vol rises, smaller loss when vol falls, for the *same* move size — is not a quirk of these numbers. It is **convexity**, and it is the reason variance swaps exist as their own product instead of everyone just trading vol swaps. Hold that thought; the next section is entirely about it.

<FillBlank
  client:visible
  question="Fill in the settlement arithmetic for the variance swap struck at 20, with variance notional $2,500 per variance point."
  text="When realized vol is 25, the squared difference is 625 − 400 = {{225|205|125}} variance points, so the long receives 2,500 × that = {{$562,500|$500,000|$450,000}}. When realized vol is 15, the squared difference is 225 − 400 = −175, so the long {{loses $437,500|loses $500,000|gains $437,500}}. For the same 5-point move, the up-payoff is {{larger|equal|smaller}} than the down-loss."
  explanation="625 − 400 = 225 → 2,500 × 225 = $562,500. 225 − 400 = −175 → 2,500 × (−175) = −$437,500. So the +5-point move pays more than the −5-point move costs. That gap between the gain and the loss is convexity in action."
/>

## Long variance is long convexity

<MCQ
  client:visible
  pretest
  question="Why would a long-variance position be worth MORE than an otherwise-identical long-vol-swap position struck at the same level?"
  options={[
    { text: 'Because variance is vol squared, the payoff curves upward in vol — gaining more on spikes and losing less on drops, which is a valuable asymmetry the long pays up for', correct: true },
    { text: 'Because variance swaps settle daily while vol swaps settle at expiry' },
    { text: 'Because variance swaps carry no counterparty risk' },
    { text: "They're worth exactly the same — variance and vol are interchangeable" },
  ]}
  explanation="Squaring makes the payoff convex in vol: the curve bends upward, so symmetric moves in vol produce asymmetric (favorable) payoffs for the long. Convexity is valuable, so you pay for it — which is why variance strikes sit a touch above the vol-swap level."
/>

Here's the core idea, stated plainly. The variance swap's payoff is built on $\sigma_{\text{realized}}^2$, and a square is a **convex** function: its graph bends upward. So if you plot the swap's payoff against *realized vol* (not variance), the variance swap traces a **curve**, while the vol swap traces a **straight line**. They're tangent at the strike — near 20 they behave almost identically — but as you move away in either direction, the curve pulls *above* the line.

Above the strike, "above the line" means **bigger gains**. Below the strike, "above the line" means **smaller losses**. Either way the long-variance holder is better off than the long-vol-swap holder for the same realized vol. That free upgrade is convexity, and in trader slang being long it is being long **"vol of vol"** — you benefit from volatility being *uncertain and jumpy*, because the square rewards the extremes.

The interactive below is the whole concept in one picture. Toggle between the linear vol-swap payoff, the convex variance-swap payoff, and both overlaid, and watch the curve sit above the line everywhere except the single tangent point at the strike.

<VarianceSwapConvexity
  client:visible
  title="Variance swap vs vol swap: the convexity of being long variance"
/>

Now, nothing in finance is free. Because that convexity is *valuable* — you'd always rather hold the curve than the line — the long has to **pay** for it. The way the market charges you is in the **strike**: a variance swap's strike (the "variance swap rate," often quoted as its square root, the **fair vol**) trades **slightly above** at-the-money implied volatility. That gap is the **convexity premium** (you'll also hear it tied to the "variance risk premium"). So if 30-day ATM implied is 18, the fair strike on a 30-day variance swap might be 19 or 20 — the extra point or two is what you hand over for the upward-bending payoff. A vol swap, lacking that convexity, strikes closer to the plain ATM implied.

<Callout variant="tip" title="Convexity is the polite word for 'heads I win more, tails I lose less'">
Every convex payoff is, at heart, the same good deal: symmetric moves in the underlying variable produce *asymmetric*, favorable payoffs. It's the same reason a long option (convex in spot, via gamma) beats a linear forward, and the same reason you'd rather own optionality than be short it. Long variance is just that idea applied to volatility itself. And just like with options, the market makes you pay an upfront premium for the privilege — here, baked into a richer strike.
</Callout>

<ThinkReveal
  client:visible
  prompt="Our variance swap struck at 20, N_var = $2,500. Realized vol comes in at 35 — a genuine spike. What does the long collect, and how does it compare to the $500k-vega vol swap?"
  hint="Variance payoff = 2,500 × (35² − 20²). Vol-swap payoff = 100,000 × (35 − 20). Square first."
  text="Variance swap: 35² − 20² = 1,225 − 400 = 825 variance points, times $2,500 = $2,062,500. The vol swap pays only 100,000 × (35 − 20) = $1,500,000. The variance swap pays over half a million dollars MORE on the same 15-point spike — and the gap widens the further vol runs. That's convexity rewarding the extremes: exactly why you want to be long variance into a crisis, and exactly why dealers who are short it lose terrifying amounts when vol explodes."
  buttonLabel="Reveal the spike payoff"
/>

## Replication: a variance swap is a strip of options

<MCQ
  client:visible
  pretest
  question="A variance swap's strike can be computed with no volatility model at all — it's 'model-free.' How is that possible?"
  options={[
    { text: 'Its payoff can be replicated by a static portfolio of out-of-the-money options across all strikes, each weighted by 1/K², so its fair value is read straight off option prices', correct: true },
    { text: "By assuming volatility is constant and plugging it into Black-Scholes" },
    { text: 'By forecasting realized volatility with a statistical model' },
    { text: 'It cannot — every variance swap needs a volatility input to price' },
  ]}
  explanation="The deep result: a continuum of OTM options weighted by 1/K² (plus a dynamic futures position) has a payoff that exactly tracks realized variance. So the swap's fair strike falls out of observed option prices with no vol model — it's 'model-free.' This is the engine behind the VIX."
/>

Now for the result that makes variance swaps genuinely beautiful, and ties this whole topic together. A variance swap can be **replicated** — its payoff manufactured — by a **static portfolio of options** plus a little dynamic trading in the underlying. "Static" means you buy the options once at the start and *don't* rebalance them; the only ongoing trade is a delta-hedge in futures that follows a simple rule. Crucially, the replication is **model-free**: you don't need to assume any volatility, any Black-Scholes, anything. The fair variance strike falls straight out of the prices of options you can see in the market.

What's in the portfolio? You hold **out-of-the-money options across the entire range of strikes** — OTM puts below the current price, OTM calls above it — and you weight each one by $1/K^2$, inversely proportional to the *square* of its strike. So low-strike puts get heavy weights, high-strike calls get light ones, in a smooth $1/K^2$ taper.

Why $1/K^2$? Keep it intuitive: a single option's sensitivity to the underlying isn't constant across price levels, and the $1/K^2$ weighting is precisely the recipe that makes the *whole portfolio's* aggregate payoff come out proportional to **realized variance** — the sum of squared returns — rather than to the price level or to plain volatility. Stack options with exactly that taper and their combined gamma exposure is flat in log-price, which is the mathematical fingerprint of a variance payoff. The dynamic futures position just sweeps up the leftover directional drift so what remains is pure variance. You don't need to memorize the derivation; the takeaway is that **a basket of vanilla options, correctly weighted, *is* a variance bet**.

<Callout variant="info" title="This is exactly how the VIX is built">
The VIX — the market's famous "fear gauge" — is not a magic number conjured from sentiment. It is the **fair strike of a 30-day variance swap on the S&P 500**, computed model-free from a **strip of OTM SPX options** weighted by $1/K^2$, and then expressed as an annualized vol. Everything you just learned about variance-swap replication *is* the VIX methodology. We'll pull that calculation apart strike by strike in the next lesson — but now you know the VIX is really a variance swap wearing a ticker.
</Callout>

<Categorize
  client:visible
  question="Sort each statement by whether it's true of the variance-swap replication."
  buckets={['True of the replication', 'False / not part of it']}
  items={[
    { text: 'Uses OTM options across many strikes', bucket: 'True of the replication' },
    { text: 'Each option weighted by 1/K²', bucket: 'True of the replication' },
    { text: 'Is model-free — needs no volatility input', bucket: 'True of the replication' },
    { text: 'Includes a dynamic futures/underlying position', bucket: 'True of the replication' },
    { text: 'Is the basis of how the VIX is computed', bucket: 'True of the replication' },
    { text: 'Requires constantly rebalancing the option holdings', bucket: 'False / not part of it' },
    { text: 'Needs a Black-Scholes volatility assumption to price', bucket: 'False / not part of it' },
    { text: 'Uses only at-the-money options', bucket: 'False / not part of it' },
  ]}
  explanation="The option leg is STATIC (bought once, not rebalanced) — only the futures hedge is dynamic. It spans OTM strikes weighted by 1/K², is model-free (no vol input), and is precisely the VIX recipe. It does NOT need Black-Scholes, is not ATM-only, and the options are not continuously rebalanced."
/>

## Risks & the cap

<MCQ
  client:visible
  pretest
  question="Why do dealers often sell CAPPED variance swaps — capping the payoff at, say, 2.5× the strike variance?"
  options={[
    { text: "Because a single enormous daily return is squared into realized variance, so an uncapped short variance position has an unbounded, potentially ruinous left tail", correct: true },
    { text: 'Because regulators forbid uncapped variance swaps entirely' },
    { text: 'Because capping makes the swap cheaper for the long to buy' },
    { text: 'Because variance swaps otherwise never settle' },
  ]}
  explanation="Realized variance is a sum of SQUARED daily returns, so one monster day (think a −20% crash) contributes a hugely outsized amount. The short's losses are essentially unbounded. A cap (commonly 2.5× the strike, i.e. ~1.58× in vol terms) puts a ceiling on the carnage so dealers can actually warehouse the risk."
/>

The same square that makes long variance so attractive makes **short** variance genuinely dangerous. Realized variance is the **sum of squared daily returns**, so a single catastrophic day doesn't just add to the total — it gets *squared* into it. One −20% session contributes $20^2 = 400$ to the sum where a −2% day contributes only $4$: a hundred times the impact for a tenfold-bigger move. The distribution of realized variance therefore has a **fat right tail** — most of the time it's quiet, and occasionally it explodes. For the long, that tail is a jackpot. For the short (often a dealer who sold the swap to harvest the variance risk premium), it's a potential wipeout.

To stay in business, dealers usually sell **capped variance swaps**, where realized variance is clipped at a ceiling — a common one is **2.5× the strike variance** (which, since you take the square root to get vol, corresponds to roughly $\sqrt{2.5} \approx 1.58\times$ the strike *vol*). The cap bounds the short's worst-case loss, turning an unbounded tail into a merely-very-bad one.

History is blunt about why this matters. In **2008**, short-variance and short-vol positions were savaged as realized vol on the S&P 500 ran far above anything implied beforehand. In **February 2018** — the episode nicknamed **"Volmageddon"** — a spike in volatility detonated short-vol products (most infamously the inverse-VIX ETN, XIV, which lost almost its entire value in a day) and torched anyone short variance without a cap. The lesson practitioners keep relearning: being short variance is collecting nickels in front of a steamroller, and the steamroller's size is squared.

<Callout variant="warning" title="The short-variance graveyard">
Selling variance feels like easy money for years: the variance risk premium means realized usually undershoots the strike, so the short quietly pockets the difference month after month. Then one crash arrives, the squared returns blow the realized number sky-high, and a single settlement can erase years of those tidy premiums — or the whole fund. 2008 and Volmageddon 2018 are the canonical tombstones. If you're going to be short variance, the cap isn't optional decoration; it's the seatbelt.
</Callout>

<MCQ
  client:visible
  question="Select every statement that is TRUE about variance-swap risk and the cap."
  allowMultiple
  options={[
    { text: 'Realized variance sums squared daily returns, so one huge day has outsized impact', correct: true },
    { text: 'The short side faces an unbounded left tail without a cap', correct: true },
    { text: 'A common cap is 2.5× the strike variance, ~1.58× in vol terms', correct: true },
    { text: '2008 and Feb-2018 ("Volmageddon") hammered short-variance/short-vol positions', correct: true },
    { text: 'Capping a variance swap removes all risk for the short' },
    { text: 'The long side is the one exposed to the fat right tail' },
  ]}
  explanation="Squaring gives realized variance a fat right tail that is a jackpot for the LONG and a disaster for the short; the cap (often 2.5× strike variance ≈ 1.58× in vol) bounds but does not eliminate the short's loss. 2008 and Volmageddon 2018 are the textbook blow-ups. The long is not the one in danger from the tail."
/>

## Putting it together

A **variance swap** and a **vol swap** let you bet on realized volatility *directly* — an OTC forward that pays the gap between what actually happened and a strike fixed today, with **no delta to hedge** and none of the path-dependence that makes a straddle a leaky vol bet. The vol swap is **linear** in vol, $N_{\text{vega}}(\sigma_{\text{realized}} - K_{\text{vol}})$; the variance swap is linear in **variance**, $N_{\text{var}}(\sigma_{\text{realized}}^2 - K^2)$, with the two notionals bridged by $N_{\text{vega}} = N_{\text{var}} \times 2K_{\text{vol}}$. That square makes the variance payoff **convex** in vol: for the same-size move it pays more on the upside and loses less on the downside, so long variance is long convexity (long "vol of vol") — and you pay for it via a strike that sits **above** ATM implied. The deepest result is that a variance swap is a **static, $1/K^2$-weighted strip of OTM options** plus a futures hedge — model-free, and *exactly* how the **VIX** is computed. Finally, because realized variance squares the daily returns, it carries a fat right tail that has bankrupted short-variance traders (2008, Volmageddon 2018), so dealers sell **capped** swaps. Get all that, and you can trade volatility as cleanly as you trade anything else — and you're ready to see the VIX for what it really is.

<MindMap
  client:visible
  title="Variance & vol swaps"
  caption="A clean, delta-free way to trade realized volatility: vol vs variance notional, convexity from the square, a model-free option-strip replication that IS the VIX, and a fat-tailed risk that demands a cap."
  root={{
    label: 'Variance & Vol Swaps',
    children: [
      {
        label: 'A clean vol bet',
        children: [
          { label: 'OTC forward on realized vol/variance' },
          { label: 'No delta to hedge, no path-dependence' },
          { label: 'Beats a leaky delta-hedged straddle' },
        ],
      },
      {
        label: 'Variance vs vol notional',
        children: [
          { label: 'Vol swap: linear, N_vega × (σ − K)' },
          { label: 'Variance swap: N_var × (σ² − K²)' },
          { label: 'N_vega = N_var × 2K' },
          { label: 'Strike variance = K²' },
        ],
      },
      {
        label: 'Convexity',
        children: [
          { label: 'σ² is convex → curve above the line' },
          { label: 'More on up-moves, less lost on down-moves' },
          { label: 'Long "vol of vol"' },
          { label: 'Strike sits above ATM implied (premium)' },
        ],
      },
      {
        label: 'Option replication & VIX',
        children: [
          { label: 'Static strip of OTM options' },
          { label: 'Weighted by 1/K², plus a futures hedge' },
          { label: 'Model-free — no vol input needed' },
          { label: 'Exactly how the VIX is computed' },
        ],
      },
      {
        label: 'Tail risk & the cap',
        children: [
          { label: 'Squared returns → fat right tail' },
          { label: 'Short variance can blow up' },
          { label: '2008 & Volmageddon 2018' },
          { label: 'Dealers sell capped swaps (~2.5× strike)' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: variance & vol swaps"
  questions={[
    {
      question: 'Why is a delta-hedged straddle a clumsier way to bet on volatility than a variance swap?',
      options: [
        { text: 'Its P&L is path-dependent and it needs constant re-hedging, whereas the swap pays directly on realized vol with no delta', correct: true },
        { text: 'A straddle can never profit from a volatility increase' },
        { text: 'A variance swap requires you to forecast the direction of the market' },
        { text: 'A straddle settles the full notional of the underlying at expiry' },
      ],
      explanation: 'A straddle picks up delta as spot moves, so without continuous hedging it becomes a directional bet, and even hedged its payoff depends on the path. A variance swap is a forward on realized vol — no delta, no path-dependence.',
    },
    {
      question: 'A variance swap is struck at 20 (strike variance 400) with variance notional $2,500 per variance point. Realized vol prints 28. What does the long collect?',
      options: [
        { text: '$960,000', correct: true },
        { text: '$20,000' },
        { text: '$562,500' },
        { text: '$1,225,000' },
      ],
      explanation: '28² − 20² = 784 − 400 = 384 variance points. Times $2,500 = $960,000. (A linear vol swap at $100k vega would have paid only 100,000 × 8 = $800,000 — the convexity adds the extra $160,000.)',
    },
    {
      question: 'Compared with a vol swap struck at the same level, a long variance swap pays:',
      options: [
        { text: 'More on an up-move in vol and less on an equal down-move — a convex, favorable asymmetry', correct: true },
        { text: 'Exactly the same in every scenario' },
        { text: 'Less on up-moves and more on down-moves' },
        { text: 'A fixed coupon regardless of realized vol' },
      ],
      explanation: 'Because variance = vol², the payoff curves upward in vol. For equal-size moves the curve sits above the vol-swap line: bigger gains above the strike, smaller losses below it. That convexity is why variance strikes trade above ATM implied.',
    },
    {
      question: 'Which best describes how a variance swap is replicated and priced model-free?',
      options: [
        { text: 'A static strip of OTM options across strikes weighted by 1/K², plus a dynamic futures hedge', correct: true },
        { text: 'A single at-the-money straddle, rolled daily' },
        { text: 'A Black-Scholes valuation using forecast realized volatility' },
        { text: 'A portfolio of bonds matching the swap’s maturity' },
      ],
      explanation: 'The portfolio holds OTM puts and calls across all strikes, each weighted by 1/K², so the aggregate payoff tracks realized variance with no vol model. A futures hedge sweeps up the drift. This exact recipe is how the VIX is computed.',
    },
    {
      question: 'Why do dealers cap the variance swaps they sell, and what do 2008 and Feb-2018 illustrate?',
      options: [
        { text: 'Squared daily returns give realized variance a fat right tail, so an uncapped short can be wiped out — as short-vol blew up in both episodes', correct: true },
        { text: 'Caps make the long pay a smaller premium, attracting more buyers' },
        { text: 'Without a cap, variance swaps would never reach settlement' },
        { text: 'The cap eliminates all risk for the short side' },
      ],
      explanation: 'Realized variance sums squared returns, so one crash day contributes enormously — the short’s tail is essentially unbounded. 2008 and Volmageddon 2018 (e.g. the XIV collapse) showed how fast short variance can implode. The cap (often 2.5× the strike variance) limits, but does not erase, that loss.',
    },
  ]}
/>

Next — **the VIX itself**: we'll take the option-strip replication you just met and walk the actual CBOE calculation strike by strike, turning a wall of SPX option quotes into the single number everyone calls the fear gauge.
