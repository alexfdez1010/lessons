---
title: "Layer-2 Rollups at a Glance"
description: "How Ethereum scales without giving up security: rollups execute transactions off-chain and post compressed data back to L1, amortizing one L1 cost across many users. Optimistic vs ZK rollups, fraud vs validity proofs, and the trade-offs."
topic: "en/ethereum"
order: 7
minutes: 9
updated: 2026-06-03
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, Categorize, MatchConcepts, MindMap, RollupBatch } from '@/components/react';

You now know how Ethereum works under the hood: one shared EVM, every node re-executing every transaction, all of it secured by proof-of-stake. That redundancy is the source of Ethereum's security — and also its bottleneck. Because *everyone* redoes *everything*, Ethereum Layer 1 can only process roughly **15 transactions per second**. When the network gets busy, block space runs short, a fee auction breaks out, and a simple token swap can cost more than the swap is worth.

The mainstream fix is not "make Ethereum bigger." It's **Layer-2 rollups**: separate, faster chains that do the heavy lifting elsewhere and then check their homework back on Ethereum. This lesson is how that trick works — and why it's cheaper without being less safe.

## The Scaling Problem

<MCQ
  client:visible
  question="From the EVM lesson: why can't Ethereum L1 just process thousands of transactions per second the way a normal web server can?"
  options={[
    { text: "Because every node independently re-executes every transaction, so the slowest-affordable node sets the ceiling for everyone.", correct: true, explanation: "Right. Throughput is capped by the requirement that the whole network re-runs all the work — that redundancy is the security, and also the bottleneck." },
    { text: "Because Ethereum runs on a single underpowered server that needs an upgrade.", explanation: "There is no central server. The EVM is replicated across thousands of nodes; the limit is the redundant re-execution, not one machine." },
    { text: "Because proof-of-stake validators are forbidden from processing more than 15 transactions.", explanation: "There's no hard-coded count like that. The limit comes from how much work every node can re-do per block while staying decentralized." },
    { text: "Because ETH transactions are encrypted and decryption is slow.", explanation: "Ethereum transactions aren't encrypted, and that isn't the bottleneck. Universal re-execution is." },
  ]}
/>

Let's name the thing. **Throughput** is how many transactions a chain can settle per unit of time, usually measured in **transactions per second (TPS)**. Ethereum L1 sits around 15 TPS. Visa, for comparison, handles thousands. The gap isn't because Ethereum's engineers forgot how to type fast — it's a *deliberate* consequence of the design you already learned: thousands of nodes each re-execute every transaction so that nobody has to be trusted. Block space is therefore scarce, and scarce things get auctioned, which is why gas fees spike exactly when you most want to transact.

This tension has a name: the **scalability trilemma**. A blockchain wants three things at once — **security** (hard to attack), **decentralization** (anyone can run a node on modest hardware), and **scalability** (high throughput) — but it's brutally hard to maximize all three together. Crank scalability by demanding beefy, expensive nodes, and you quietly sacrifice decentralization. Ethereum L1 deliberately picks security and decentralization, and accepts low base throughput. Rollups are the clever way to buy back scalability *without* paying with the other two.

<FillBlank
  client:visible
  question="Fill in the blanks about the scaling problem."
  text="Ethereum L1's throughput, measured in {{transactions per second|blocks per node|hashes per watt}}, is low because every node must {{re-execute every transaction|store one block|sign one message}}. The {{scalability trilemma|halting problem|gas auction}} says it's hard to get security, decentralization, and scalability all at once."
  explanation="Throughput = TPS. The cap exists because every node re-executes everything. The scalability trilemma describes the three-way tension a chain can't fully resolve at L1."
/>

## What a Rollup Does

Time for the two layers. **Layer 1 (L1)** is the base chain — Ethereum itself, with its full security and decentralization. A **Layer 2 (L2)** is a *separate* system built **on top of** L1 that handles transactions for it, then leans on L1 for the guarantees it can't provide on its own.

A **rollup** is the dominant kind of L2. The recipe has two halves:

1. **Off-chain execution.** The rollup runs transactions on its own faster chain — a chain that does *not* need thousands of independent nodes re-executing everything, so it can be cheap and quick.
2. **On-chain data + settlement.** It periodically **posts the compressed transaction data (and/or a proof) back to Ethereum L1**. A **batch** is that bundle: many L2 transactions rolled into a single L1 submission (hence the name "rollup"). Because the data lands on L1, anyone can reconstruct the L2's state from Ethereum alone — that property is called **data availability** — and L1 remains the final court of **settlement** and security.

The analogy: picture a packed restaurant. If every diner walked into the kitchen and read out their order individually, the kitchen would melt down — that's L1 trying to do everything. Instead, each table's server takes the orders out front (fast, off to the side), then carries **one consolidated ticket** back to the kitchen. The kitchen (L1) still cooks the canonical meal and is the single source of truth about what was ordered — but it only had to read *one ticket* instead of a hundred shouted orders. The serving happens off-chain; the authoritative record lands on-chain.

The genius is that you keep Ethereum's trust model. You don't have to trust the rollup operator's word for your balance, because the underlying data is published to L1, where it can be checked.

<MCQ
  client:visible
  question="On a rollup, where does transaction execution happen, and where does final settlement / data availability live?"
  options={[
    { text: "Execution runs off-chain on the L2; the compressed data and settlement land on L1.", correct: true, explanation: "Exactly the rollup split: do the heavy execution on the fast L2, then post data back to Ethereum so L1 guarantees data availability and settlement." },
    { text: "Both execution and settlement happen entirely on L1; the L2 is just a cache.", explanation: "If everything stayed on L1 you'd gain nothing. The whole point is to move execution off-chain while keeping data/settlement on L1." },
    { text: "Both happen entirely on the L2; Ethereum is never involved.", explanation: "Then it wouldn't inherit Ethereum's security — that's a sidechain, not a rollup. Rollups post data back to L1." },
    { text: "Execution is on L1; the L2 only stores backups.", explanation: "Backwards. Execution moves to the L2; L1 holds the canonical data and settles." },
  ]}
/>

## Why It's Cheaper: Amortizing the L1 Cost

Here's the money question, literally. Posting a batch to Ethereum costs gas — a **fixed cost** that exists no matter how many transactions are inside the batch. The trick is that this one cost is **shared across everyone in the batch**. Stuff more transactions into a single batch and the **cost-per-user** falls, because the same L1 bill is split more ways. This sharing-out of a fixed cost is called **amortizing** it.

Drag the batch size up below and watch the cost-per-user collapse while the total L1 cost barely moves. Flip the optimistic/ZK toggle to see the finality readout change — we'll explain that difference in the next section.

<RollupBatch
  client:visible
  caption="One L1 batch carries many L2 transactions. The fixed cost of posting it is split across everyone inside, so cost-per-user drops sharply as the batch grows — that's amortization. Toggle optimistic vs ZK to see how each settles."
/>

A worked example in plain numbers. Suppose posting a batch to L1 costs about `0.02 ETH` in gas, and **200** transactions ride in that batch. Each user's share of the L1 cost is `0.02 / 200 = 0.0001 ETH` — a hundredth of a percent of an ETH. Compare that to sending a full standalone L1 transaction, which pays the whole base fee by itself. The more riders share the batch's fixed bill, the cheaper each seat. Double the batch to 400 transactions and the per-user share halves again to `0.00005 ETH`. (These are illustrative round numbers, not a live quote.)

One more lever made this dramatically cheaper: **EIP-4844**, the "blobs" upgrade. A **blob** is a chunk of cheap, *temporary* data space on L1, purpose-built for rollups — it's much cheaper than regular permanent transaction storage (**calldata**) and is automatically deleted after a few weeks, which is fine because rollups only need the data available long enough for anyone to verify the batch. Blobs slashed the data-posting bill, which is the dominant cost for a rollup, so per-user fees dropped by an order of magnitude.

<MatchConcepts
  client:visible
  question="Match each rollup building block to what it actually is."
  pairs={[
    { term: "Rollup", definition: "Off-chain execution plus on-chain data and settlement" },
    { term: "Batch", definition: "Many L2 transactions posted to L1 together as one submission" },
    { term: "Sequencer", definition: "The component that orders and packages L2 transactions" },
    { term: "Blob", definition: "Cheap, temporary L1 data space built for rollups" },
    { term: "L1", definition: "The base chain that provides settlement and security" },
  ]}
  explanation="A rollup executes off-chain and posts data on-chain. A batch bundles many txs together; the sequencer orders them; blobs hold the data cheaply; and L1 is the settlement/security layer underneath it all."
/>

A quick definition you just met: the **sequencer** is the component that receives incoming L2 transactions, decides their order, and packages them into batches. Today it's usually a single operator — convenient and fast, but a centralization point we'll revisit in the risks section.

## Two Flavors: Optimistic vs ZK

All rollups post data to L1. They differ in **how L1 becomes convinced the batch is actually correct** — and that single choice drives everything else.

### Optimistic rollups

An **optimistic rollup** does what the name says: it **assumes every batch is valid** and posts it without proof. Verification is "innocent until proven guilty." After a batch is posted, there's a **challenge window** — typically about **7 days** — during which anyone watching can submit a **fraud proof**: a compact on-chain demonstration that a specific batch contained an invalid state transition. If a valid fraud proof lands, the bad batch is reverted and the cheater loses their bond. If nobody successfully challenges within the window, the batch is final.

- **Upside:** dirt cheap to operate — no expensive cryptography per batch, just publish and wait.
- **Downside:** **slow withdrawal finality.** Moving funds back to L1 means waiting out the full challenge window (the ~7 days), because until then a fraud proof could still overturn things.

### ZK rollups

A **ZK rollup** (zero-knowledge rollup) flips the assumption to "guilty until proven innocent." With every batch it attaches a **validity proof** (a ZK proof): a succinct piece of cryptography that mathematically proves the batch's state transition was computed correctly, *without* re-running all the transactions. L1 verifies that little proof on the spot. If it checks out, the batch is correct — there's nothing to dispute and no waiting window.

- **Upside:** **fast finality.** Once the validity proof is verified on L1, the batch is settled immediately, so withdrawals don't need a multi-day wait.
- **Downside:** **heavier to produce.** Generating those cryptographic proofs is computationally expensive and, historically, harder to engineer for a fully general EVM.

Flip the toggle in the island above to watch the finality readout switch between the optimistic challenge-window wait and ZK's near-instant settlement.

| Trait | Optimistic rollup | ZK rollup |
|---|---|---|
| How correctness is ensured | Assumed valid; anyone can submit a **fraud proof** to dispute | A **validity proof** proves correctness up front |
| Withdrawal finality | Slow — wait out the ~7-day challenge window | Fast — settles as soon as the proof verifies on L1 |
| Computation cost | Cheap to operate (no proof per batch) | Heavy — generating validity proofs is expensive |
| Example networks | Optimism, Arbitrum | zkSync, Starknet |

<Categorize
  client:visible
  question="Sort each trait into the rollup family it describes."
  buckets={["Optimistic rollup", "ZK rollup"]}
  items={[
    { text: "Assumes batches are valid unless challenged", bucket: "Optimistic rollup" },
    { text: "Attaches a validity proof verified on L1 right away", bucket: "ZK rollup" },
    { text: "Has a multi-day challenge window before withdrawals finalize", bucket: "Optimistic rollup" },
    { text: "Relies on fraud proofs to catch bad batches", bucket: "Optimistic rollup" },
    { text: "Offers fast finality at the cost of heavier computation", bucket: "ZK rollup" },
    { text: "Cheap to operate because it skips per-batch cryptography", bucket: "Optimistic rollup" },
  ]}
  explanation="Optimistic = assume-valid + fraud proofs + slow challenge-window withdrawals + cheap operation. ZK = validity proof verified immediately + fast finality + heavier proof generation."
/>

<Reveal client:visible summary="If a ZK proof proves the batch is valid, why does anyone still use optimistic rollups?">
Because "prove it cryptographically" used to be the hard part. Generating validity proofs for a fully general EVM is computationally heavy and was, for years, difficult to engineer well — so optimistic rollups shipped first, matured first, and built the deepest ecosystems and tooling. They're also simpler and cheap to operate, since they skip per-batch proving entirely. The trade you accept is the slow withdrawal window. ZK technology is catching up fast and may eventually win on the merits, but ecosystem maturity, simplicity, and inertia keep optimistic rollups very much in the game today.
</Reveal>

## Trade-offs and Risks

Rollups are a genuine win, but "inherits L1 security" deserves an asterisk. A rollup inherits Ethereum's guarantees for **data availability and settlement** — the data is on L1, and L1 is the final court. But rollups add their *own* surface area of risk on top:

- **Centralized sequencers.** Most rollups today run a single sequencer. It can't steal your funds (the data and proofs keep it honest), but it *can* censor or delay your transactions, or go offline. Decentralizing sequencers is active, unfinished work.
- **Upgrade keys.** Many rollups are governed by admin/multisig keys that can upgrade the contracts. A malicious or compromised key is a real risk, mitigated by timelocks and multisigs but not zero.
- **Withdrawal delay (optimistic).** That ~7-day challenge window means your funds aren't instantly portable back to L1 — annoying, and a reason "fast bridges" (which front you the money for a fee) exist.
- **Bridge risk.** Moving assets between L1 and L2 goes through bridge contracts, which have historically been juicy hacking targets. The bridge is only as safe as its code.

And one important cousin to distinguish: a **sidechain** is *not* a rollup. A sidechain runs its own independent consensus and provides its **own** security — it does **not** post its data to Ethereum, so it does **not** inherit L1's guarantees. If a sidechain's validators collude, Ethereum can't save you. A rollup's safety is anchored to L1; a sidechain's is anchored only to itself.

<MCQ
  client:visible
  question="Which statement most accurately captures what a rollup does — and doesn't — inherit from Ethereum L1?"
  options={[
    { text: "It inherits L1's data availability and settlement, but adds its own risks like a centralized sequencer and upgrade keys.", correct: true, explanation: "Correct. The L1 anchoring secures the data and settlement, but the rollup layers on operational risks that aren't covered by Ethereum's consensus." },
    { text: "It inherits absolutely everything from L1, so a rollup carries zero additional risk.", explanation: "Too rosy. Sequencer centralization, upgrade keys, withdrawal delays, and bridges are real rollup-specific risks." },
    { text: "It inherits nothing from L1 and is secured entirely by its own validators — like a sidechain.", explanation: "That describes a sidechain. A rollup posts data to L1 and inherits its data-availability and settlement guarantees." },
    { text: "It inherits L1's speed, which is why rollups are slow.", explanation: "Rollups are faster than L1, not slower, and 'speed' isn't what's inherited — data availability and settlement are." },
  ]}
/>

## The Big Picture

<MindMap
  client:visible
  title="Layer-2 rollups at a glance"
  root={{
    label: "Layer-2 rollups",
    children: [
      { label: "Scaling problem", children: [
        { label: "Every node re-executes everything" },
        { label: "Low TPS, scarce block space" },
        { label: "Scalability trilemma" },
      ]},
      { label: "What a rollup does", children: [
        { label: "Off-chain execution on L2" },
        { label: "Posts data + proofs to L1" },
        { label: "L1 keeps data availability & settlement" },
      ]},
      { label: "Cheaper via amortizing", children: [
        { label: "Fixed L1 batch cost shared by all" },
        { label: "Cost-per-user falls as batch grows" },
        { label: "Blobs make data cheap" },
      ]},
      { label: "Optimistic vs ZK", children: [
        { label: "Optimistic: assume valid, fraud proofs" },
        { label: "ZK: validity proof, fast finality" },
      ]},
      { label: "Trade-offs", children: [
        { label: "Centralized sequencer" },
        { label: "Withdrawal delay (optimistic)" },
        { label: "Bridge & upgrade-key risk" },
      ]},
    ],
  }}
  caption="Five ideas: why L1 is slow, what a rollup does, why it's cheap, the two flavors, and what to watch out for."
/>

<Quiz
  client:visible
  questions={[
    {
      question: "A batch costs 0.05 ETH to post to L1 and carries 500 transactions. What is each user's amortized share of the L1 cost?",
      options: [
        { text: "0.0001 ETH each", correct: true },
        { text: "0.05 ETH each" },
        { text: "0.5 ETH each" },
        { text: "25 ETH each" },
      ],
      explanation: "0.05 / 500 = 0.0001 ETH. The fixed batch cost is split across everyone riding in it — 0.05 ETH is the whole batch paid once, and per-user cost falls (not rises) as the batch grows, so you divide rather than multiply.",
    },
    {
      question: "What is the core difference between how optimistic and ZK rollups convince L1 a batch is valid?",
      options: [
        { text: "Optimistic rollups assume validity and rely on fraud proofs during a challenge window; ZK rollups attach a validity proof verified immediately.", correct: true },
        { text: "Optimistic rollups use cryptographic proofs while ZK rollups use a challenge window." },
        { text: "Optimistic rollups run on L1 and ZK rollups run on L2." },
        { text: "There is no real difference; the names are marketing." },
      ],
      explanation: "The defining split is assume-and-challenge (optimistic, fraud proofs) versus prove-up-front (ZK, validity proof), which is why ZK has fast finality and optimistic has a withdrawal delay. Both are L2s that post to L1 — the difference is the validity mechanism, not the layer.",
    },
    {
      question: "Spot the trap. Someone tells you: 'A Layer-2 rollup is just a separate trusted chain — you have to trust its operator the same way you'd trust a bank.' What's wrong with that?",
      options: [
        { text: "A rollup posts its transaction data and/or proofs to L1, so its correctness can be verified on Ethereum — you don't have to trust the operator's word.", correct: true },
        { text: "Nothing is wrong; rollups really are fully trusted chains like banks." },
        { text: "It's wrong because rollups have no operator at all." },
        { text: "It's wrong because rollups are actually slower and more expensive than L1." },
      ],
      explanation: "Data availability on L1 plus fraud or validity proofs means the operator can't fake your balance — that trust-minimization separates a rollup from a plain trusted chain or a sidechain. Rollups do have operators (sequencers) today; posting to L1 is what keeps them honest. The flaw in the claim is about trust, not speed.",
    },
    {
      question: "Why does posting many transactions in one batch lower the fee each user pays?",
      options: [
        { text: "The fixed cost of posting the batch to L1 is amortized — divided across every transaction in it.", correct: true },
        { text: "Ethereum gives bulk discounts to large transactions." },
        { text: "Larger batches are exempt from gas fees entirely." },
        { text: "Each extra transaction reduces the L1 base fee for everyone." },
      ],
      explanation: "One L1 bill, many riders: the more transactions share a batch, the smaller each one's slice of the fixed cost. There's no bulk-discount tier and no exemption — batches still pay L1 gas, and adding transactions doesn't lower L1's base fee; it just spreads the batch's own cost.",
    },
    {
      question: "How does a rollup differ from a sidechain?",
      options: [
        { text: "A rollup posts data to L1 and inherits Ethereum's security; a sidechain runs its own consensus and provides only its own security.", correct: true },
        { text: "A rollup is slower than a sidechain because it uses Ethereum." },
        { text: "A sidechain posts proofs to L1 while a rollup never touches L1." },
        { text: "There's no difference; the terms are interchangeable." },
      ],
      explanation: "The rollup is anchored to L1, so its security comes from Ethereum; the sidechain stands alone, so trusting it means trusting its own validators. Both aim for high throughput — the real distinction is where their security comes from, and it's the rollup (not the sidechain) that posts data/proofs to L1.",
    },
    {
      question: "A user wants to withdraw funds from an optimistic rollup back to L1 and is surprised it takes about a week. Why?",
      options: [
        { text: "The challenge window must elapse so anyone could still submit a fraud proof against the batch before it's final.", correct: true },
        { text: "Ethereum L1 always takes a week to settle any transaction." },
        { text: "The validity proof takes a week to generate." },
        { text: "Sidechain bridges impose a mandatory weekly batch." },
      ],
      explanation: "Optimistic rollups assume validity, so they leave a ~7-day window for fraud proofs, and withdrawals wait it out to be safe. L1 itself settles in minutes; validity proofs belong to ZK rollups (and don't take a week); and this is a rollup's challenge window, not a sidechain schedule.",
    },
  ]}
/>

## Key Takeaways

<Callout variant="success" title="What to remember">
- **The problem:** Ethereum L1 re-executes everything on every node, capping throughput at roughly 15 TPS and making block space scarce and pricey — the **scalability trilemma** in action.
- **The fix:** a **Layer-2 rollup** executes transactions **off-chain** on a fast L2, then posts the **compressed batch** of data (and/or proofs) back to **L1**, which keeps **data availability** and **settlement**. You inherit Ethereum's security without trusting the operator.
- **Why it's cheap:** the fixed cost of posting a batch is **amortized** across everyone in it, so **cost-per-user** falls as the batch grows — and **blobs** (EIP-4844) made that data dramatically cheaper.
- **Two flavors:** **optimistic** rollups assume validity and rely on **fraud proofs** within a ~7-day challenge window (cheap, slow withdrawals); **ZK** rollups attach a **validity proof** verified on L1 immediately (fast finality, heavier to produce).
- **Watch out for:** centralized **sequencers**, **upgrade keys**, the optimistic **withdrawal delay**, and **bridge** risk. And a **sidechain** is not a rollup — it secures itself, not via L1.
</Callout>

That's the whole arc. You've gone from "what is Ethereum?" through the EVM, accounts, gas, contracts, and proof-of-stake, and now up the scaling ladder to Layer-2 rollups — the design the ecosystem is actually building on. You understand Ethereum end to end. There's one thing left: prove it. Head to the **final exam** for this topic and put it all together.
