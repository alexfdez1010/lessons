---
title: "The EVM: Ethereum's World Computer"
description: "How Ethereum's shared computer works: the EVM as a deterministic state machine, world state, transactions as state transitions, why every node re-executes everything, and what bytecode and 'Turing-complete' really mean."
topic: "en/ethereum"
order: 2
minutes: 9
updated: 2026-06-03
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, Categorize, MatchConcepts, MindMap, EvmStateMachine, BlockchainChain } from '@/components/react';

Last lesson we said Ethereum doesn't just move money around — it *runs programs*. Smart contracts, tokens, whole applications, all living on the chain. Which raises an annoying follow-up question that most explainers conveniently skip:

**Where do those programs actually run?**

Your laptop runs programs on its CPU. A website runs on a server somewhere. But a smart contract isn't on anyone's particular machine — it belongs to the whole network. So Ethereum needs a computer that *everyone* runs, that *nobody* owns, and that *always* agrees with itself. That computer is the **EVM**, and this lesson is the tour.

## One Computer, a Thousand Copies

Before we explain it, a gut check.

<MCQ
  client:visible
  question="There are thousands of Ethereum nodes around the world. How many 'computers' does a smart contract effectively run on?"
  options={[
    { text: "One — a single shared virtual computer that every node runs an identical copy of.", correct: true, explanation: "Exactly. The EVM is one logical machine; each node maintains its own copy and they all agree." },
    { text: "Thousands — each node runs its own separate, independent program with its own results.", explanation: "If results could differ per node there'd be no single truth. They run the same machine and reach the same answer." },
    { text: "Zero — contracts are just data, nothing executes them.", explanation: "Contracts are executable code; the EVM runs it. They're not inert data." },
    { text: "One physical server that Ethereum rents in the cloud.", explanation: "There's no central server. The 'one computer' is virtual and replicated across all nodes." },
  ]}
/>

The **EVM (Ethereum Virtual Machine)** is a **virtual machine**: a computer defined by *rules*, not by hardware. A virtual machine is a piece of software that pretends to be a CPU — it has its own instruction set, its own memory model, its own notion of what a "step" is — and any real computer can run it by following those rules. Java works this way; so do game-console emulators. The point is portability: write the rules once, and every machine that follows them behaves *identically*.

Ethereum takes that idea and makes it social. There is conceptually **one EVM**, but its state is **replicated across every node** on the network. Think of it as a giant shared spreadsheet — a Google Doc the size of the planet — that thousands of people have open at once. Nobody edits a cell by hand. The document only changes when a rule-following edit is submitted and *everyone applies that same edit*. Because they all start from the same document and apply the same edits by the same rules, they all end up looking at the same document. One computer, a thousand identical copies.

<Callout variant="info" title="Virtual ≠ fake">
"Virtual" doesn't mean pretend. It means *defined by a specification instead of by silicon*. The EVM is as real as any computer — it just lives in an agreed-upon rulebook that thousands of machines obey simultaneously.
</Callout>

## World State: What the Computer Remembers

A computer without memory is a very expensive paperweight. The EVM's memory is called the **world state**.

The **world state** is the complete set of everything the EVM remembers at a given moment: every **account** and all of its data. For each account that means:

- its **balance** (how much ETH it holds),
- for contract accounts, the **code** that runs when you call it,
- and that contract's **storage** (its private, persistent variables — a token contract's "who owns how much" table lives here).

If the EVM is a game console, the world state is the **save file**. Press the button (send a transaction) and the save file updates. Reboot a node from scratch, replay every save up to now, and you land on the exact same save file as everyone else.

<FillBlank
  client:visible
  question="Fill in the blanks about what the world state holds."
  text="The {{world state|block reward|gas limit}} is the set of all accounts and their data — balances, contract code, and each contract's persistent {{storage|gossip|signature}} — at one moment in time. A handy analogy is the game console's {{save file|power cable|controller}}."
  explanation="World state = all accounts + their data. Contract storage is the persistent, on-chain memory of a contract. The save-file analogy captures 'the whole remembered state right now'."
/>

## A Transaction Is a State Transition

Here's the single most important sentence in this entire lesson, so we'll put it in code:

`newState = EVM(oldState, transaction)`

That's the whole model. A **transaction** is an instruction submitted to the network ("send 3 ETH from A to B", or "call this contract's `mint` function"). The EVM takes the **old world state** plus that transaction and computes a **new world state**. Nothing more mystical than that: a transaction is a **state transition** — a recipe for turning one save file into the next.

And the EVM is a **state machine**: a system that sits in some state and moves to a new, fully-determined state when an input arrives. (Light switch: states "on" and "off", input "flip". Same idea, slightly more accounts.)

The magic word is **deterministic**. Deterministic means *no surprises, ever*: the same starting state plus the same transaction produces the **exact same** new state — on every node, every time, forever. No randomness, no "depends on the weather," no "works on my machine." That property is the bedrock that lets thousands of strangers agree without trusting each other.

Play with one below. Step a few preset transactions through the machine and watch the world state go from *before* to *after*. Try a plain ETH transfer first (watch two balances change), then a contract call (watch the contract's stored number change), then hit reset and run the *same* sequence again — note that you get the identical result. That repeatability is determinism, live.

<EvmStateMachine
  client:visible
  caption="Same start + same transaction ⇒ same result, every time. That is determinism — and it's why every node on Earth agrees on the world state."
/>

<MCQ
  client:visible
  question="A node in Tokyo and a node in São Paulo both apply transaction T to the same starting world state S. What does determinism guarantee about their results?"
  options={[
    { text: "They compute the exact same new world state — byte for byte.", correct: true, explanation: "Same input + same start state ⇒ same output, always. That's what makes 'one shared computer' possible." },
    { text: "They probably get similar states, but small differences are expected.", explanation: "No — 'probably similar' would break consensus. Determinism means identical, not approximate." },
    { text: "Tokyo's result wins because it likely executed first.", explanation: "Order of execution between nodes doesn't change the math. Both reach the same state regardless of who ran it first." },
    { text: "The result depends on each node's CPU and operating system.", explanation: "The EVM is specified precisely so hardware can't change the answer. That's the point of a virtual machine." },
  ]}
/>

### A transfer, worked step by step

Say the world state has account **A** holding 10 ETH and account **B** holding 2 ETH. A signs and broadcasts the transaction "send 3 ETH to B." The EVM does the obvious arithmetic against the *old* state:

| Account | Before | Change | After |
|---|---:|---:|---:|
| A | 10 ETH | −3 | 7 ETH |
| B | 2 ETH | +3 | 5 ETH |

The new world state is "A: 7, B: 5." Two cells in the giant spreadsheet changed; nothing else moved. (We're ignoring the small fee for now — that's the next lesson.) Every node that applies this transaction to that same old state lands on A: 7, B: 5. No exceptions, because there's no room for any.

## Why Every Node Re-Runs Everything (Gas Preview)

Here's the part that sounds absurd until it clicks: **every node independently re-executes every transaction.** Your node doesn't take some other node's word for the new balances. It downloads the transaction, runs it through *its own* EVM, computes the result itself, and checks that everyone else got the same thing.

Picture thousands of meticulous accountants, each handed the same stack of receipts, each redoing every single sum by hand, then comparing answers. Wildly redundant — and that redundancy *is the security*. To cheat, you wouldn't fool one accountant; you'd have to fool the *majority* of them simultaneously, all running identical, deterministic rules. Good luck.

But re-running everything has a cost, and it points straight at the next concept:

<Reveal client:visible summary="If every node redoes the work, isn't that wasteful?">
Yes — and deliberately so. The waste *is* the product. A normal cloud server is cheap because exactly one machine does the work and you trust it. The EVM is expensive because *thousands* of machines do the same work and you trust **none** of them individually — only their agreement. You're not paying for raw compute; you're paying for trustless verification.

That cost is also why every operation must be **metered and paid for**. If computation were free, a single attacker could submit a transaction with an infinite loop and force every node on Earth to spin forever. So the EVM charges for each step in a unit called **gas**, and a transaction that runs out of gas halts. Gas gets a full lesson of its own — for now, just hold the link in your head: *re-execution by everyone ⇒ computation must be priced.*
</Reveal>

## Bytecode and Being Turing-Complete

Nobody writes raw EVM instructions by hand (well, almost nobody, and they need a hug). Developers write contracts in high-level languages like **Solidity** or **Vyper** — readable code with functions, variables, and `if`-statements. A compiler then translates that down into **EVM bytecode**: a long string of low-level **opcodes**, the tiny primitive instructions the EVM actually executes (`ADD`, `SSTORE`, `JUMP`, and friends). Bytecode is to Solidity what machine code is to the C you'd write on your laptop — the same logic, ground down into steps a machine can chew.

The EVM is also **Turing-complete**. Turing-complete means it can, in principle, express *any* computation you can describe — loops, branches, arbitrary logic — given enough resources. That's powerful: it's why Ethereum can host genuinely complicated programs, not just "move coin from X to Y."

The catch with Turing-completeness is the **halting problem**: a sufficiently expressive machine can write programs that loop forever, and you can't always tell in advance whether one will. The EVM's fix is gas (again): every step costs gas, a transaction carries a finite gas budget, and when the budget runs dry, execution **halts**. So the EVM is Turing-complete *with a meter* — it can express anything, but it can't run anything *for free or forever*.

This is a deliberate contrast with **Bitcoin Script**, Bitcoin's on-chain language, which is intentionally **not** Turing-complete. Bitcoin Script has no unbounded loops on purpose — less expressive, but easier to reason about and harder to footgun. Ethereum chose the opposite trade: maximal expressiveness, fenced in by gas.

<MatchConcepts
  client:visible
  question="Match each term to what it actually is."
  pairs={[
    { term: "Solidity", definition: "A high-level language developers write contracts in" },
    { term: "Bytecode", definition: "The low-level instructions the EVM actually runs" },
    { term: "Opcode", definition: "A single primitive EVM instruction, e.g. ADD" },
    { term: "Deterministic", definition: "Same input + same start state ⇒ same result everywhere" },
    { term: "Gas", definition: "The meter that makes a runaway loop eventually halt" },
  ]}
  explanation="High-level source (Solidity) compiles to bytecode, a sequence of opcodes. Determinism makes every node agree; gas bounds execution so Turing-completeness can't loop forever."
/>

<Categorize
  client:visible
  question="Sort each statement: is it true of the EVM, or not?"
  buckets={["A property of the EVM", "NOT true of the EVM"]}
  items={[
    { text: "It is deterministic — same input always gives the same output", bucket: "A property of the EVM" },
    { text: "It is Turing-complete (bounded by gas)", bucket: "A property of the EVM" },
    { text: "Its state is replicated across every node", bucket: "A property of the EVM" },
    { text: "It runs on one central server Ethereum owns", bucket: "NOT true of the EVM" },
    { text: "Each node may compute a slightly different result", bucket: "NOT true of the EVM" },
    { text: "Programs can loop forever for free", bucket: "NOT true of the EVM" },
  ]}
  explanation="The EVM is deterministic, replicated, and Turing-complete with a gas-bounded halt. It has no central server, nodes never disagree on a correct result, and nothing runs for free or forever."
/>

## Blocks Put the Transactions in Order

One last piece. `newState = EVM(oldState, transaction)` is great for *one* transaction — but thousands fly in every minute, and order matters. (If A has 10 ETH and tries to send 8 ETH to both B and C, which one goes through depends entirely on which is processed first.) So the EVM needs a single, agreed **ordering**.

That's the job of **blocks**. Transactions are batched into a block; blocks are placed in a strict sequence, each one cryptographically linked to the one before it (the chain in "blockchain"). Apply the blocks in order, the transactions inside each block in order, and every node walks the *same* path from the genesis state to today's state — and lands on the *same* world state. Determinism handles each step; block ordering handles the *sequence* of steps.

<BlockchainChain
  client:visible
  caption="Each block bundles an ordered batch of transactions and links to the previous block. Same blocks, same order ⇒ same world state on every node."
/>

<MCQ
  client:visible
  question="Two nodes both have a deterministic EVM and the exact same set of pending transactions. What else must they share to be guaranteed the same final world state?"
  options={[
    { text: "The same order in which the transactions are applied — which blocks provide.", correct: true, explanation: "Determinism makes each step identical, but the outcome can still depend on sequence. Block ordering pins down the order so everyone agrees." },
    { text: "Nothing — determinism alone fully guarantees the same final state regardless of order.", explanation: "Determinism guarantees each individual step matches, but different orderings can yield different results. Order must also be agreed." },
    { text: "The same physical hardware.", explanation: "Hardware is irrelevant to a virtual machine — order is the missing ingredient, not silicon." },
    { text: "A trusted central coordinator to pick a winner.", explanation: "No central coordinator exists; the ordering comes from the agreed sequence of blocks." },
  ]}
/>

## The Big Picture

<MindMap
  client:visible
  title="The EVM at a glance"
  root={{
    label: "The EVM",
    children: [
      { label: "World state", children: [
        { label: "Accounts + balances" },
        { label: "Contract code + storage" },
        { label: "The save file" },
      ]},
      { label: "Transaction = state transition", children: [
        { label: "newState = EVM(oldState, tx)" },
        { label: "Old state in, new state out" },
      ]},
      { label: "Re-execution & determinism", children: [
        { label: "Every node re-runs everything" },
        { label: "Same input ⇒ same output" },
        { label: "Redundancy = security" },
      ]},
      { label: "Bytecode & Turing-complete", children: [
        { label: "Solidity → bytecode → opcodes" },
        { label: "Can express any computation" },
        { label: "Gas bounds the halt" },
      ]},
    ],
  }}
  caption="Four ideas: what it remembers, how it changes, why everyone agrees, and what it can run."
/>

<Quiz
  client:visible
  questions={[
    {
      question: "In one sentence, what is the EVM?",
      options: [
        { text: "A single virtual computer, replicated across every node, that runs Ethereum's programs.", correct: true },
        { text: "A cloud server farm operated by the Ethereum Foundation." },
        { text: "The wallet app you use to hold ETH." },
        { text: "The currency you pay transaction fees in." },
      ],
      explanation: "The EVM is one logical machine, defined by rules and copied across all nodes — there's no central operator. A wallet merely talks to Ethereum, and ETH/gas is the currency, not the machine that executes code.",
    },
    {
      question: "Which of these belong in the world state? (Pick all that apply.)",
      allowMultiple: true,
      options: [
        { text: "Account balances", correct: true },
        { text: "A contract's persistent storage", correct: true },
        { text: "A contract's deployed code", correct: true },
        { text: "Your local browser's cookies" },
      ],
      explanation: "The world state holds every account's data — balances, contract storage, and deployed contract code. Your browser's cookies live on your device, not in Ethereum's shared state.",
    },
    {
      question: "Why must computation on the EVM be metered with gas?",
      options: [
        { text: "Because every node re-executes it, an unbounded program would force the whole network to run forever.", correct: true },
        { text: "To make Ethereum slower on purpose." },
        { text: "Because the EVM is non-deterministic and needs a tiebreaker." },
        { text: "To replace the need for blocks." },
      ],
      explanation: "Re-execution by every node plus Turing-completeness means you must bound execution, or one infinite loop halts everyone. Gas is a meter, not a throttle or a tiebreaker — and blocks (which order transactions) are a different job.",
    },
    {
      question: "A developer writes a contract in Solidity. What does the network actually execute?",
      options: [
        { text: "EVM bytecode — the opcodes the Solidity was compiled down to.", correct: true },
        { text: "The raw Solidity source code, line by line." },
        { text: "An English description of what the contract should do." },
        { text: "A spreadsheet formula." },
      ],
      explanation: "High-level source is compiled to bytecode; the EVM understands bytecode, not Solidity, prose, or spreadsheet formulas (the spreadsheet was only an analogy for shared state).",
    },
    {
      question: "How does Bitcoin Script differ from the EVM, by design?",
      options: [
        { text: "Bitcoin Script is intentionally NOT Turing-complete — no unbounded loops, less expressive, easier to reason about.", correct: true },
        { text: "Bitcoin Script is more expressive than the EVM and can run any program." },
        { text: "Bitcoin Script also runs on the EVM." },
        { text: "There's no difference; both are identical languages." },
      ],
      explanation: "Bitcoin deliberately limited expressiveness for safety; Ethereum chose Turing-completeness fenced in by gas. They're separate systems with sharply different design philosophies.",
    },
    {
      question: "Determinism plus block ordering together give Ethereum what guarantee?",
      options: [
        { text: "Every node, applying the same blocks in the same order from the same start, reaches the same world state.", correct: true },
        { text: "Transactions can never fail." },
        { text: "Fees drop to zero." },
        { text: "Each node ends up with its own unique state." },
      ],
      explanation: "Determinism makes each step match; ordering makes the sequence match; together they make all nodes converge on one shared state. Transactions can still fail (e.g. out of gas) and gas still applies — the guarantee is about agreement, not success or cost.",
    },
  ]}
/>

## Key Takeaways

<Callout variant="success" title="What to remember">
- The **EVM** is one **virtual machine** — a computer defined by rules — whose **world state** is replicated identically across every node. One computer, a thousand copies.
- The **world state** is every account and its data (balances, contract code, contract storage): the network's save file.
- A **transaction is a state transition**: `newState = EVM(oldState, transaction)`. The EVM is **deterministic** — same start + same transaction ⇒ the same result everywhere, always.
- **Every node re-executes every transaction.** That redundancy is the security — and it's why computation must be metered and paid for with **gas**.
- Contracts are written in **Solidity/Vyper**, compiled to **EVM bytecode** (opcodes). The EVM is **Turing-complete** (it can express any computation) but **gas-bounded**, so nothing loops forever — unlike intentionally-limited Bitcoin Script.
- **Blocks** put transactions in an agreed order, so all nodes apply the same changes in the same sequence and converge on the same world state.
</Callout>

We've talked a lot about "accounts" holding balances — but how does an account actually *work*? Ethereum's answer (the **account model**) is strikingly different from Bitcoin's coins-in-a-jar approach (the **UTXO model**). Next up: accounts vs UTXO, and why Ethereum picked the model it did.
