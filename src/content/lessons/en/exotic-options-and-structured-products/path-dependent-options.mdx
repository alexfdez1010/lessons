---
title: "Path-Dependent Options: Asians & Lookbacks"
description: "Options that settle on the journey, not just the destination: Asian options that average the path to cut cost and curb manipulation, and lookbacks that pay off the best price you never had to time."
topic: "en/exotic-options-and-structured-products"
order: 3
minutes: 14
updated: 2026-06-14
---

import { Callout, MCQ, Quiz, FillBlank, MatchConcepts, Categorize, MindMap, Reveal, GbmPaths } from '@/components/react';

Every vanilla option you've met so far has a short attention span: it glances at the **final price** on expiry day, computes one payoff, and forgets everything that happened in between. The wild ride to get there — the spike, the crash, the recovery — is irrelevant. **Path-dependent options** are the opposite. They watch the *whole journey* and let it shape the payoff. That single change spawns two families that pull in opposite directions: **Asians**, which **average** the path to smooth it out, and **lookbacks**, which seize the **extremes** of the path to amplify it. One makes options cheaper and harder to rig; the other hands you perfect hindsight at a steep price. Let's walk both paths.

<MCQ
  client:visible
  pretest
  question="Two options on the same stock expire on the same day at the same final price. One pays off based only on that final price; the other paid attention to the entire price history along the way. What word best describes the second one?"
  options={[
    { text: 'Path-dependent — its payoff depends on the whole journey, not just the endpoint', correct: true },
    { text: 'American — it can be exercised early' },
    { text: 'In-the-money — it finished with positive intrinsic value' },
    { text: 'Deep out-of-the-money — it finished worthless' },
  ]}
  explanation="An option whose payoff reads the entire price path — not just the final tick — is path-dependent. 'American' is about WHEN you can exercise (early vs. only at expiry), not what the payoff looks at. In/out-of-the-money describe moneyness at a moment, not path dependence."
/>

## Asian options: averaging the journey

<MCQ
  client:visible
  pretest
  question="You're worried about manipulation on the single closing print that settles your option. A friend suggests settling on the AVERAGE price over the last 30 days instead. Compared with a vanilla that settles on the final price alone, what should you expect?"
  options={[
    { text: 'A smoother, less extreme settlement value that’s harder to push around on any single day', correct: true },
    { text: 'A wilder settlement that swings more than the final price' },
    { text: 'An identical payoff — averaging changes nothing' },
    { text: 'A payoff that ignores the strike entirely' },
  ]}
  explanation="Averaging many observations cancels out day-to-day noise, so the average is steadier and less extreme than any single endpoint — and far harder to rig, since no one day dominates. It still uses the strike; it just replaces 'final price' with 'average price' as the settlement input."
/>

**Analogy.** Think of dollar-cost averaging, but for your settlement price. Instead of betting everything on the price that happens to print on one specific afternoon, you sample the price on many dates and take the mean. Or picture a teacher grading you on your **term average** rather than your single final-exam score: one disastrous Tuesday can't sink you, and one lucky spike can't carry you. That's the whole spirit of an Asian option — the payoff is graded on the *average*, not the final day.

**The definitions.** "Asian" splits into two flavors depending on *what* gets averaged:

- **Average-price Asian** (the common one). The **average of the underlying** over a set of observation dates becomes the **settlement price**, compared against a **fixed strike**. An average-price call pays `max(average − K, 0)`; an average-price put pays `max(K − average, 0)`.
- **Average-strike Asian.** The **average becomes the strike**, and you settle against the **final price** `S_T`. An average-strike call pays `max(S_T − average, 0)`. This effectively locks in "did the stock end above its own typical level?" — handy when there's no natural strike to quote.

**Worked example.** Take a stock that prints these five observation dates over the option's life:

| Date | Stock price |
|---|---|
| 1 | \$100 |
| 2 | \$110 |
| 3 | \$90 |
| 4 | \$120 |
| 5 | \$100 |

The **arithmetic average** is `(100 + 110 + 90 + 120 + 100) / 5 = 520 / 5 = `**\$104**.

Now price an **average-price call struck at \$100**:

`payoff = max(average − K, 0) = max(104 − 100, 0) = `**\$4**.

Look at what just happened. The **final** tick was \$100 — a vanilla call struck at \$100 would expire **worthless** (`max(100 − 100, 0) = $0`). But the Asian remembers that the stock spent time up at \$110 and \$120, dragging the average to \$104, so it pays **\$4**. The journey, not the destination, wrote the check.

The fan of simulated paths below makes the point physical. Each path wanders, but its **average** lives somewhere in the calm middle of the cloud — it never chases the single most extreme tick. Crank the volatility slider up: the *endpoints* fly all over the place, yet the per-path averages stay far more huddled. That huddling is exactly why averaging tames a payoff.

<GbmPaths
  client:visible
  title="Asian intuition: the average of a path lives in the calm middle"
  caption="Each line is one simulated price journey. Raise volatility and the endpoints scatter violently — but each path's AVERAGE over the year sits near the middle of its own wandering, far less extreme than wherever it happened to finish. Averaging cancels noise; that's the Asian's whole trick."
/>

<FillBlank
  client:visible
  question="Fill in the average-price Asian arithmetic from the worked example."
  text="The five prints average to {{104|110|100}}, so an average-price call struck at 100 pays {{four dollars|forty dollars|zero}}. A plain vanilla call on the same final tick of 100 would instead expire {{worthless|deep in the money|at its maximum}}, because it only looks at the {{final price|whole average|highest print}}."
  explanation="Average = 520/5 = 104, so the Asian call pays max(104 − 100, 0) = $4. The vanilla reads only the final tick (100), which equals the strike, so it expires worthless. The Asian's edge here is that it remembers the journey, not just the last day."
/>

## Why Asians are cheaper (and harder to game)

<MCQ
  client:visible
  pretest
  question="An average of many noisy daily prices versus a single final price: which one swings around more from scenario to scenario — and what does that imply for the option's price?"
  options={[
    { text: 'The single final price swings more, so it has higher volatility — and higher vol means a pricier option', correct: true },
    { text: 'The average swings more, so the Asian is the more expensive of the two' },
    { text: 'They swing identically, so the two options cost the same' },
    { text: 'Volatility has nothing to do with option price' },
  ]}
  explanation="Averaging cancels noise, so the average is LESS volatile than any single endpoint. Lower volatility of the settlement input means a lower option value — which is why an Asian is cheaper than the otherwise-identical vanilla. (Yes, option value rises with the volatility of what it settles on.)"
/>

**The cheapness, in one line.** Option value rises with the volatility of whatever the payoff settles on. Averaging a series **cancels noise** — the up-days and down-days partly offset — so the **average is less volatile than the endpoint**. Less volatility in the settlement input means a **lower option value**. Therefore, all else equal, an **Asian is cheaper than the equivalent vanilla**. You're buying movement on a quantity that, by construction, moves less.

**A rule of thumb for the brave.** For *continuous* averaging, an arithmetic Asian behaves roughly like a vanilla with an **effective volatility around `σ/√3`** — call it about 58% of the underlying's vol. Don't take the number to the bank (it's an approximation that assumes continuous, evenly-spaced averaging), but the *direction* is exactly right and worth internalizing: averaging knocks the effective vol down by a meaningful chunk, and the option price falls with it. Fewer averaging dates means less cancellation, so the discount shrinks toward zero as the averaging window collapses to a single day (at which point you're just holding a vanilla again).

**The anti-manipulation superpower.** Here's the reason Asians dominate whole markets. A vanilla settles on **one** print — a single closing fix. If a large player can nudge the price for *one* afternoon (a practice charmingly known as "painting the close"), they can swing the entire payoff. Now make it settle on the **average of 30 daily fixes**: to move that average meaningfully, you'd have to manipulate the price *every single day*, against the whole market, for a month. That's wildly expensive and conspicuous. Spreading settlement across many observations makes the payoff **robust to a single rigged day** — which is precisely why Asians are the default in **commodities, FX, and any over-the-counter market** where one settlement print could otherwise be pushed around.

<Callout variant="warning" title="The pricing catch: no clean closed form">
There's a sharp edge hiding in the arithmetic average. Black–Scholes loves lognormals — but the **sum (and thus arithmetic average) of lognormal prices is not itself lognormal** (lognormals don't add up to another lognormal). So the **arithmetic** Asian has **no clean closed-form Black–Scholes price**; in practice it's priced by **analytic approximations** (Turnbull–Wakeman, Curran, moment-matching) or by **Monte Carlo** simulation. The cruel twist: the **geometric** Asian — which averages by multiplying and taking roots instead of adding — *does* have a tidy closed form, because the geometric mean of lognormals stays lognormal. But almost nobody trades geometric Asians; the arithmetic version is what the market actually uses. The clean math describes the product nobody wants.
</Callout>

<Categorize
  client:visible
  question="Sort each statement by whether it describes an Asian option's advantage or one of its complications."
  buckets={['Asian advantage', 'Asian complication']}
  items={[
    { text: 'Cheaper than the equivalent vanilla', bucket: 'Asian advantage' },
    { text: 'Hard to manipulate via a single closing print', bucket: 'Asian advantage' },
    { text: 'Lower effective volatility from averaging', bucket: 'Asian advantage' },
    { text: 'Arithmetic version has no clean closed-form price', bucket: 'Asian complication' },
    { text: 'Often needs Monte Carlo or approximations to value', bucket: 'Asian complication' },
    { text: 'The tidy closed-form (geometric) version isn’t what trades', bucket: 'Asian complication' },
  ]}
  explanation="The averaging that makes Asians cheap and manipulation-resistant (lower effective vol, robust to a single rigged fix) is also what breaks the math: arithmetic averages of lognormals aren't lognormal, so there's no neat Black–Scholes formula — only approximations or simulation, while the clean geometric version stays off the trading desk."
/>

## Lookback options: buying hindsight

<MCQ
  client:visible
  pretest
  question="Imagine an option that lets you buy the stock, retroactively, at the LOWEST price it touched over its whole life — then sell at today's price. What real-world wish does that grant?"
  options={[
    { text: 'Perfect timing in hindsight — you capture the best entry without ever having to call the bottom', correct: true },
    { text: 'A guaranteed loss, since you always buy high' },
    { text: 'The same payoff as a vanilla call struck at today’s price' },
    { text: 'Protection only if the stock never moves' },
  ]}
  explanation="Locking in the lowest price the stock visited is the dream of perfect hindsight timing — you 'bought the bottom' without having to predict it. That removes all entry-timing risk, which is exactly what makes lookbacks valuable (and, as we'll see, expensive)."
/>

**Analogy.** The lookback is the **"no regrets" option**. You know that maddening feeling of selling too early, or buying just before a dip? A lookback erases it. It lets you **buy at the lowest low** or **sell at the highest high** that occurred over the option's life — chosen *in hindsight*, after the fact, with the whole price history laid out in front of you. It's timing the market perfectly without the inconvenience of actually being able to time the market.

**The definitions.** As with Asians, there are two flavors — this time keyed to whether the **strike** floats or stays fixed:

- **Floating-strike lookback.** There's no fixed strike at all; the **best price over the life** *is* the strike. A floating-strike **call** pays `S_T − min(S)` — you "bought" at the lowest price and sell at the final price. A floating-strike **put** pays `max(S) − S_T` — you "sold" at the highest price. By construction these payoffs are **never negative**: the min is at most `S_T`, the max is at least `S_T`.
- **Fixed-strike lookback.** The strike `K` is fixed, but the payoff uses the path **extreme** instead of the final price. A fixed-strike call pays `max(max(S) − K, 0)` — it captures the **highest** price the stock ever reached versus your strike. A fixed-strike put pays `max(K − min(S), 0)` — the **lowest** price versus your strike.

**Worked example.** Reuse the same five-date path:

| Date | Stock price |
|---|---|
| 1 | \$100 |
| 2 | \$110 |
| 3 | \$90 |
| 4 | \$120 |
| 5 | \$100 |

Read off the extremes: `min(S) = `**\$90** (date 3), `max(S) = `**\$120** (date 4), and the final price `S_T = `**\$100**.

- **Floating-strike call** = `S_T − min(S) = 100 − 90 = `**\$10**. You retroactively bought the \$90 bottom and sell at \$100.
- **Floating-strike put** = `max(S) − S_T = 120 − 100 = `**\$20**. You retroactively sold the \$120 top and the stock finished at \$100.

Compare that to the Asian on the *identical* path, which paid only **\$4**, and the vanilla call, which paid **\$0**. Same journey, three radically different paychecks — because each option reads the path through a different lens: the vanilla sees only the last tick, the Asian sees the smoothed middle, the lookback grabs the juiciest extreme.

Flip back to the path fan and watch the **tops and bottoms** of each line, not the middles. As you raise volatility, those extremes stretch *further* from the start — the highest high climbs and the lowest low sinks. A lookback feeds on exactly that stretch, which is why it's the inverse of an Asian: averaging pulls toward the calm center, lookbacks reach for the wild edges.

<MatchConcepts
  client:visible
  question="Match each path-dependent payoff to what it actually reads off the price journey."
  pairs={[
    { term: 'Average-price Asian call', definition: 'Average of the path versus a fixed strike' },
    { term: 'Average-strike Asian call', definition: 'Final price versus the average of the path' },
    { term: 'Floating-strike lookback call', definition: 'Final price minus the lowest price reached' },
    { term: 'Floating-strike lookback put', definition: 'Highest price reached minus the final price' },
    { term: 'Fixed-strike lookback call', definition: 'Highest price reached versus a fixed strike' },
    { term: 'Vanilla call', definition: 'Final price versus a fixed strike — the journey is ignored' },
  ]}
  explanation="Asians replace one input with the AVERAGE (price- vs strike-flavored by which input it swaps). Lookbacks replace an input with a path EXTREME — floating uses the best price as the strike, fixed keeps the strike and uses the extreme as the settlement. The vanilla reads only the endpoint."
/>

## Why lookbacks are expensive

<MCQ
  client:visible
  pretest
  question="A floating-strike lookback hands you the best entry or exit the stock ever offered. Relative to a comparable vanilla, what should its price look like — and why?"
  options={[
    { text: 'More expensive, because the path’s max/min are more extreme than its endpoint, so the payoff is reliably larger', correct: true },
    { text: 'Cheaper, because hindsight reduces the option’s risk' },
    { text: 'Identical, since both settle on the same underlying' },
    { text: 'Free, because the payoff can never be negative' },
  ]}
  explanation="The maximum (or minimum) of a wandering path is, by definition, at least as extreme as wherever it ends up — usually much more. A payoff built on that extreme is reliably bigger than one built on the endpoint, so a lookback costs MORE than the comparable vanilla. A non-negative payoff isn't a free one — you pay up front for the privilege."
/>

**The expense, in one line.** A lookback pays off the **best point of a random walk**. The maximum of a path is always at least as high as its endpoint (and the minimum at least as low) — and over a volatile life, *much* more extreme. So a lookback's payoff is **systematically larger** than a vanilla's on the same underlying, which means it costs **more** — sometimes dramatically more. You're not buying exposure to where the stock ends; you're buying exposure to **how far it ever roamed**, and roaming is the one thing volatile assets do reliably.

What you're really purchasing is the **elimination of timing risk**. A vanilla makes you live with whatever price prints at expiry; a lookback lets you cherry-pick the best moment after the fact. That's an enormous luxury, and markets charge enormous luxury prices for it.

<Callout variant="warning" title="Gorgeous payoff, ugly price">
A lookback's payoff diagram is a thing of beauty — you literally cannot mistime your entry or exit. But beauty is expensive. Because they're built on path extremes, lookbacks are **richly priced and acutely sensitive to volatility and to the path itself**: more volatility means wider extremes, which inflates the price fast, and the value depends heavily on how the realized path behaves, not just where it lands. The blunt verdict: a lookback is rarely worth its premium **unless timing risk is genuinely your problem.** If you simply have a directional view, a plain vanilla expresses it for a fraction of the cost. Pay for hindsight only when not having it would actually hurt you — otherwise you're buying a Ferrari to fetch the mail.
</Callout>

<FillBlank
  client:visible
  question="Fill in why lookbacks command a high price."
  text="A lookback settles on the {{extreme|average|opening}} of the path, which is {{more|less|equally}} extreme than the endpoint — so its payoff is reliably {{larger|smaller|identical}} than a vanilla's, making it {{more|less|equally}} expensive. It's worth that premium mainly when {{timing risk|low volatility|a flat market}} is your real concern."
  explanation="Lookbacks read the max or min — a path extreme — which is more extreme than the endpoint, giving a reliably larger payoff and thus a higher price. The premium is justified only when removing entry/exit timing risk genuinely matters to you; for a plain directional view, a vanilla is far cheaper."
/>

## Asians vs lookbacks vs vanilla — when to use which

<MCQ
  client:visible
  pretest
  question="A corporate treasurer hedges the AVERAGE exchange rate they'll pay on steady monthly imports all year, and badly wants protection that a single bad fix can't distort. Which structure fits — and which would be overkill?"
  options={[
    { text: 'An Asian fits the averaged, manipulation-resistant exposure; a lookback would be expensive overkill', correct: true },
    { text: 'A lookback fits perfectly; an Asian can’t hedge currencies' },
    { text: 'A vanilla on the final-day rate fits best of all' },
    { text: 'Neither works — only forwards can hedge an average' },
  ]}
  explanation="The treasurer's real exposure IS an average (steady purchases all year), and they fear single-fix manipulation — that's the textbook Asian use case, and it's cheaper to boot. A lookback removes timing risk they don't actually have, so they'd massively overpay. A final-day vanilla ignores the averaged nature of the exposure entirely."
/>

Three options, three different relationships with the path. The vanilla **ignores** it, the Asian **smooths** it, the lookback **exploits its extremes**. Here's how to choose:

| | Vanilla | Asian | Lookback |
|---|---|---|---|
| Reads the path? | No — final price only | Yes — the **average** | Yes — the **max/min** |
| Effective volatility | The underlying's σ | **Lower** (averaging cancels noise) | **Higher** (extremes are wide) |
| Price vs vanilla | — | **Cheaper** | **More expensive** |
| What it's good at | Plain directional bets | Hedging **average** exposure; resisting manipulation | Removing **entry/exit timing** risk |
| Typical users | Everyone | Commodities, FX, treasury hedging of average rates | Traders who fear mistiming; payoff-juicing in notes |
| Pricing | Closed-form Black–Scholes | Approximation / Monte Carlo (arithmetic) | Path-extreme models; very vol-sensitive |

The mental shortcut: **Asian when you want cheaper and steadier**, because your real exposure is an average or you're guarding against a rigged single print. **Lookback when timing is your genuine enemy** and you'll pay a premium to delete it. **Vanilla when you just have a view** on where the price ends up and don't want to pay for path features you won't use.

Both exotics show up constantly as **ingredients inside structured products** — an Asian averaging feature to cheapen a note's embedded option, or a lookback to dangle a "best-entry" marketing hook in front of retail buyers. We'll crack those notes open and decompose them into their vanilla-plus-exotic building blocks in a later lesson; for now, just file away that these path-dependent payoffs are rarely sold naked — they're usually baked into something bigger.

<Categorize
  client:visible
  question="Match each scenario to the path-dependent option that fits it best."
  buckets={['Reach for an Asian', 'Reach for a lookback']}
  items={[
    { text: 'Hedging the average price of monthly commodity purchases', bucket: 'Reach for an Asian' },
    { text: 'Worried a single closing fix could be manipulated', bucket: 'Reach for an Asian' },
    { text: 'Want a cheaper option and your exposure is already an average', bucket: 'Reach for an Asian' },
    { text: 'Desperate not to mistime your single entry point', bucket: 'Reach for a lookback' },
    { text: 'Willing to pay a fat premium to capture the best exit', bucket: 'Reach for a lookback' },
    { text: 'Want a marketing hook promising the best price in hindsight', bucket: 'Reach for a lookback' },
  ]}
  explanation="Asians suit averaged exposure and manipulation-resistance, and they're cheaper — the commodity/FX/treasury world. Lookbacks suit one thing: deleting entry/exit timing risk, which you'll pay dearly for. If timing isn't your real problem, the lookback is overkill."
/>

## Putting it together

**Path-dependent options** judge the whole journey, not just the final tick — and they split into two opposing families. **Asians** replace a single price with the **average** of the path: that averaging cancels noise (effective vol roughly `σ/√3` for continuous sampling), making them **cheaper than vanillas** and **robust to single-fix manipulation**, which is why they rule commodities and FX. The catch is mathematical — the arithmetic average of lognormals isn't lognormal, so there's **no clean Black–Scholes formula**, only approximations and Monte Carlo (the tidy geometric version isn't what trades). **Lookbacks** do the reverse, settling on the path's **extremes** — buy the lowest low, sell the highest high in hindsight — which makes their payoffs reliably **larger** and therefore **expensive and very vol-sensitive**, worth it only when **timing risk** is your actual problem. On one identical five-date path (100, 110, 90, 120, 100), the vanilla paid **\$0**, the average-price call paid **\$4**, and the floating-strike lookbacks paid **\$10 and \$20** — same journey, three lenses, three paychecks. Both exotics live mostly **inside structured products**, the subject we'll take apart next.

<MindMap
  client:visible
  title="Path-dependent options at a glance"
  caption="Asians average the path (cheaper, manipulation-resistant, messy math); lookbacks grab the extremes (pricey, timing-risk killer); both judge the journey, not just the endpoint, and both hide inside structured notes."
  root={{
    label: 'Path-Dependent Options',
    children: [
      {
        label: 'The core idea',
        children: [
          { label: 'Payoff reads the WHOLE journey, not the endpoint' },
          { label: 'Asians average; lookbacks grab extremes' },
          { label: 'Opposite effects: smooth vs amplify' },
        ],
      },
      {
        label: 'Asian options',
        children: [
          { label: 'Average-price: average vs fixed strike' },
          { label: 'Average-strike: final price vs the average' },
          { label: 'Worked path averages to 104, call pays $4' },
        ],
      },
      {
        label: 'Why Asians win',
        children: [
          { label: 'Averaging cancels noise → lower effective vol (~σ/√3)' },
          { label: 'Cheaper than the equivalent vanilla' },
          { label: 'Robust to single-fix manipulation' },
          { label: 'Arithmetic: no closed form → Monte Carlo / approximations' },
        ],
      },
      {
        label: 'Lookback options',
        children: [
          { label: 'Floating-strike: best price IS the strike' },
          { label: 'Fixed-strike: extreme vs a fixed strike' },
          { label: 'Same path: floating call $10, put $20' },
        ],
      },
      {
        label: 'Why lookbacks cost',
        children: [
          { label: 'Settle on path extremes → bigger payoff than vanilla' },
          { label: 'Expensive and very vol-sensitive' },
          { label: 'Worth it only if timing risk is your real problem' },
        ],
      },
      {
        label: 'When to use which',
        children: [
          { label: 'Asian: cheaper, average exposure, anti-manipulation' },
          { label: 'Lookback: remove entry/exit timing risk' },
          { label: 'Vanilla: plain directional view' },
          { label: 'Both are common ingredients in structured notes' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: path-dependent options"
  questions={[
    {
      question: 'What fundamentally distinguishes a path-dependent option from a vanilla one?',
      options: [
        { text: 'Its payoff depends on prices observed along the way, not just the final price', correct: true },
        { text: 'It can be exercised at any time before expiry' },
        { text: 'It always finishes in the money' },
        { text: 'It has no strike price' },
      ],
      explanation: 'Path-dependent payoffs read the journey — an average, a maximum, a minimum — while a vanilla reads only the endpoint. Early exercise is the American feature (a different axis), and neither always finishes in the money nor lacks a strike.',
    },
    {
      question: 'A stock prints 100, 110, 90, 120, 100 on five averaging dates. An average-PRICE call struck at 100 pays:',
      options: [
        { text: '\$4 — the $104 average minus the $100 strike', correct: true },
        { text: '\$0 — the final price equals the strike' },
        { text: '\$20 — the highest print minus the final price' },
        { text: '\$10 — the final price minus the lowest print' },
      ],
      explanation: 'Average = 520/5 = 104, so the Asian pays max(104 − 100, 0) = $4. The $0 answer is the VANILLA (final tick = strike); $20 and $10 are the floating-strike lookback put and call on the same path — wrong instrument.',
    },
    {
      question: 'Why is an arithmetic Asian usually cheaper than the equivalent vanilla?',
      options: [
        { text: 'Averaging cancels noise, lowering the effective volatility it settles on', correct: true },
        { text: 'Asians can never finish in the money, so they’re nearly worthless' },
        { text: 'They expire earlier, leaving less time value' },
        { text: 'They carry no exposure to the underlying at all' },
      ],
      explanation: 'The average of many noisy observations is less volatile than a single endpoint, and lower settlement-input volatility means a lower option value — roughly an effective vol of σ/√3 for continuous averaging. Asians absolutely can finish in the money and keep full exposure; they just settle on a smoother quantity.',
    },
    {
      question: 'Why do market participants in commodities and FX favor Asian settlement over a single closing fix?',
      options: [
        { text: 'Averaging many observations makes the payoff robust to manipulation of any single day', correct: true },
        { text: 'A single fix is impossible to compute in those markets' },
        { text: 'Asians guarantee a larger payoff than vanillas' },
        { text: 'Averaging eliminates the need for any pricing model' },
      ],
      explanation: 'To distort an average you’d have to rig the price on many dates against the whole market — far harder than painting one close. Asians don’t guarantee bigger payoffs (they’re usually cheaper), and they actually make pricing HARDER, not easier (no clean closed form).',
    },
    {
      question: 'On the path 100, 110, 90, 120, 100, a floating-strike lookback PUT pays $20. Where does that come from?',
      options: [
        { text: 'The highest price reached ($120) minus the final price ($100)', correct: true },
        { text: 'The final price ($100) minus the lowest price ($90)' },
        { text: 'The average ($104) minus the strike ($100), scaled up' },
        { text: 'The highest price ($120) minus the lowest ($90)' },
      ],
      explanation: 'A floating-strike lookback put pays max(S) − S_T = 120 − 100 = $20 — you "sold" at the $120 top. The 100 − 90 = $10 is the floating-strike CALL; the others mix up the average and the range, neither of which defines this payoff.',
    },
    {
      question: 'When is paying up for a lookback actually justified rather than wasteful?',
      options: [
        { text: 'When eliminating entry or exit timing risk is genuinely your problem', correct: true },
        { text: 'Whenever you simply think the stock will rise' },
        { text: 'Whenever volatility is low, making lookbacks cheap' },
        { text: 'Whenever you want the cheapest possible option' },
      ],
      explanation: 'Lookbacks are expensive precisely because they delete timing risk by paying off the path’s best point — only worth it when that timing risk truly matters. A plain directional view is far cheaper with a vanilla, low vol makes them less valuable not bargain-priced, and "cheapest" points to an Asian, not a lookback.',
    },
  ]}
/>

Next — **structured products**: we'll pry open the notes that banks actually sell and watch them decompose into a bond plus a basket of vanilla and path-dependent options, including the very Asians and lookbacks you just met.
