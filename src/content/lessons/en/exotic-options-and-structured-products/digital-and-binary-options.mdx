---
title: "Digital & Binary Options"
description: "All-or-nothing payoffs: cash-or-nothing and asset-or-nothing digitals, why a digital is the limit of a tight call spread, and the violent discontinuous delta that makes them a hedging nightmare near the strike."
topic: "en/exotic-options-and-structured-products"
order: 1
minutes: 14
updated: 2026-06-14
---

import { Callout, MCQ, Quiz, FillBlank, MatchConcepts, Categorize, MindMap, Reveal, OptionPayoff } from '@/components/react';

You already know a vanilla call: above the strike its payoff ramps up dollar-for-dollar, and the Greeks behave like polite, continuous functions you can hedge. Now meet the **digital** — also called a **binary** option — the simplest exotic there is, and the one that quietly powers nearly everything else in this course. A digital doesn't ramp. It *flips*: you get a fixed payout if the stock finishes on the right side of a line, and nothing if it doesn't. That single yes/no payoff is the atom. **Autocall coupons, range accruals, the knock-in triggers in a structured note** — strip away the marketing and they're all just bundles of digitals. Learn this one cleanly and the rest of the course is mostly assembly. By the end you'll price a digital as a probability, build it out of a tight call spread, and understand exactly why its delta tries to go to infinity right when you least want it to.

<MCQ
  client:visible
  pretest
  question="A regular call option pays you more and more the higher the stock climbs above the strike. How does a cash-or-nothing digital call's payoff differ at expiry?"
  options={[
    { text: 'It pays one fixed amount if the stock finishes above the strike, and zero otherwise — no matter how far above', correct: true },
    { text: 'It pays the same ramp as a vanilla call but starts one strike higher' },
    { text: 'It pays twice the vanilla amount once the stock clears the strike' },
    { text: 'It pays nothing at the strike but ramps down as the stock rises' },
  ]}
  explanation="A digital is binary: a flat fixed payout if you finish in the money, zero if you don't. Whether the stock closes one cent over the strike or fifty dollars over, you collect the same fixed amount. That flat, all-or-nothing step is the whole personality of the instrument — and the source of all its weirdness."
/>

## A coin-flip payoff (cash-or-nothing vs asset-or-nothing)

**Analogy.** A vanilla call is a dimmer switch: turn the stock up and the payoff brightens smoothly. A digital is a plain **light switch** — off below the strike, on above it, with nothing in between. There's no "a little bit on." You either cleared the line or you didn't, and the prize is the same either way.

**The definitions.** There are two flavors, and the difference is *what* you collect:

- A **cash-or-nothing call** pays a **fixed cash amount** `Q` if the stock finishes above the strike `K`, and `$0` otherwise. The payout is $Q \times \mathbf{1}\{S_T > K\}$ — that indicator is just a fancy way of writing "1 if true, 0 if false."
- An **asset-or-nothing call** pays **one share of the stock itself** (worth $S_T$) if $S_T > K$, and `$0` otherwise. Same trigger, but the prize scales with where the stock lands.

(Puts are the mirror image — they pay when $S_T < K$.) Here's the elegant part that ties digitals back to the vanilla world you already know. A vanilla call's payoff, $\max(S_T - K, 0)$, decomposes *exactly* into these two atoms:

$$\text{vanilla call} = (\text{asset-or-nothing call}) - K \times (\text{cash-or-nothing call with } Q = 1)$$

Read it in words: above the strike a vanilla call hands you the stock ($S_T$, the asset-or-nothing piece) and charges you the strike ($K$ in cash, the cash-or-nothing piece) — and below the strike both pieces are zero. The vanilla you've used all along was secretly two digitals stapled together.

**Worked example.** Take a cash-or-nothing call with payout $Q$ of \$100 and strike $K = 50$, on a stock trading near 50 today. Walk the payoff:

| Stock at expiry | Above strike (50)? | Cash-or-nothing payoff | Asset-or-nothing payoff |
|---|---|---|---|
| 48 | No | \$0 | \$0 |
| 50 | No (not strictly above) | \$0 | \$0 |
| 51 | Yes | \$100 | \$51 |
| 70 | Yes | \$100 | \$70 |
| 120 | Yes | \$100 | \$120 |

Notice the cash-or-nothing column: \$100 the instant you clear 50, and stubbornly \$100 no matter how high the stock flies. The asset-or-nothing column climbs because you're being handed the share. That flat versus rising contrast is the entire taxonomy.

<MatchConcepts
  client:visible
  question="Match each digital piece to what it actually pays when it finishes in the money."
  pairs={[
    { term: 'Cash-or-nothing call', definition: 'A fixed cash amount Q, flat no matter how far above the strike' },
    { term: 'Asset-or-nothing call', definition: 'One share, worth the stock price S at expiry, so it scales' },
    { term: 'The indicator 1{S > K}', definition: 'Equals 1 if the stock finishes above the strike, else 0' },
    { term: 'Vanilla call decomposition', definition: 'Asset-or-nothing minus K cash-or-nothing units' },
  ]}
  explanation="Cash-or-nothing = flat fixed cash; asset-or-nothing = the share itself, so its value rises with S. The indicator is the on/off switch both share, and a vanilla call is exactly the asset-or-nothing leg minus K cash-or-nothing units — the stock you receive, less the strike you pay."
/>

## Pricing a digital: it's just a probability

<MCQ
  client:visible
  pretest
  question="A cash-or-nothing call pays $100 if the stock finishes above the strike. Before any math, what should its fair value today most resemble?"
  options={[
    { text: 'The \$100 payout scaled by the chance of finishing in the money, then discounted to today', correct: true },
    { text: 'Exactly \$100, since that is what it pays' },
    { text: 'Half of \$100, because there are two possible outcomes' },
    { text: 'The current stock price minus the strike' },
  ]}
  explanation="A digital is a bet that pays a fixed amount with some probability, so its value is that payout times the probability of winning, discounted for time. If there's a 42% chance of finishing in the money, you'd pay roughly 42% of the discounted $100 — not the full $100 (that's only if you were certain) and not a flat 50/50 (the odds aren't a coin flip)."
/>

**The mechanic.** Because a cash-or-nothing call pays a flat `Q` exactly when $S_T > K$, its value today is dead simple in spirit:

$$\text{price} = e^{-rT} \times Q \times \Pr{}^{\mathbb{Q}}(S_T > K)$$

That's just "payout × probability of winning, discounted back." The only subtlety is *which* probability. Under Black–Scholes the risk-neutral probability of finishing in the money is $N(d_2)$ — the same $d_2$ that already lives inside the vanilla pricing formula you met earlier. So the closed form is clean:

$$\text{cash-or-nothing call} = e^{-rT}\, Q\, N(d_2)$$

The asset-or-nothing call, by the same logic, prices to $S_0\, N(d_1)$. And — satisfyingly — subtract $K$ cash-or-nothing units from the asset-or-nothing leg and you recover $S_0 N(d_1) - K e^{-rT} N(d_2)$, the textbook vanilla call. The decomposition isn't a cute coincidence; it's baked into the prices.

**Worked example.** Suppose the payout $Q$ is \$100, the discount factor $e^{-rT} = 0.98$ (a touch under a year at a low rate), and your model spits out $N(d_2) = 0.42$ — a 42% risk-neutral chance of finishing above the strike. Then:

$$\text{price} = 0.98 \times 100 \times 0.42$$

Step through it: $100 \times 0.42 = 42$, i.e. \$42 of expected payout, and discounting by $0.98$ gives $42 \times 0.98 = 41.16$, i.e. \$41.16. So this digital is worth about **\$41.16** today. The price reads off as a probability almost literally — strip the discounting and the \$100, and you're staring at "42% likely."

<Callout variant="warning" title="The skew adds a slope the simple formula misses">
$N(d_2)$ is the *Black–Scholes* answer, which assumes one flat volatility. Real markets have a **volatility skew** — implied vol changes with strike. Because a digital is so sensitive to *exactly where* the strike sits, that skew tilts its price away from a naive $e^{-rT} Q\, N(d_2)$: the digital effectively inherits a **slope term from the skew**, nudging the value up or down depending on whether vol is rising or falling across nearby strikes. The intuition to keep: a digital isn't just betting on the probability of clearing the strike — it's also exquisitely exposed to how the *price of vol itself* changes right around that strike. We'll formalize this when we build it from a spread next.
</Callout>

<FillBlank
  client:visible
  question="Fill in the pricing logic for a cash-or-nothing digital."
  text="A cash-or-nothing call is worth its payout times the {{risk-neutral probability|expected stock return|historical average}} of finishing above the strike, discounted to today. In Black–Scholes that probability is exactly {{N(d2)|N(d1)|the delta}}, which is why the digital's price reads off almost directly as a {{probability|payout|premium}}. Once a vol skew is present, the simple formula is off by a {{slope term|rounding error|discount factor}}."
  explanation="Price = discounted payout × risk-neutral probability of finishing ITM, and under Black–Scholes that probability is N(d2) (N(d1) belongs to the asset-or-nothing / delta side). So the value reads as a probability — adjusted by a skew-driven slope term once implied vol varies across strikes."
/>

## The call-spread replication (and why it bounds the price)

<MCQ
  client:visible
  pretest
  question="You can't buy a 'digital' directly, but you can build something that behaves almost identically from two vanilla calls. Which combination, sized up, mimics a digital that pays a fixed amount above the strike?"
  options={[
    { text: 'Buy a call just below the strike, sell a call just above it, and scale the size up as the gap narrows', correct: true },
    { text: 'Buy a call and a put at the same strike (a straddle)' },
    { text: 'Buy two calls at the same strike to double the payoff' },
    { text: 'Sell a call below the strike and buy one above it' },
  ]}
  explanation="A tight bull call spread — long a call at K−ε, short a call at K+ε — pays a small ramp across the gap and a flat amount above it. Scale the quantity up as the gap ε shrinks and that ramp gets steeper and narrower until it's a near-vertical step: a digital. A straddle bets on movement either way; doubling calls at one strike just doubles a ramp; the reversed spread points the wrong way."
/>

**The construction.** Here's the trick desks actually use. Take a **bull call spread**: long a call struck at $K - \varepsilon$, short a call struck at $K + \varepsilon$, both at quantity $\tfrac{1}{2\varepsilon}$. Below $K-\varepsilon$ both expire worthless (payoff 0). Above $K+\varepsilon$ the two legs cancel to a flat payoff. *Across* the $2\varepsilon$ gap, the spread ramps linearly — and because you scaled the quantity by $\tfrac{1}{2\varepsilon}$, that ramp rises by exactly **1** across the gap. Now shrink $\varepsilon \to 0$: the ramp gets narrower and steeper while always rising a total of 1, until in the limit it's a vertical **step of height 1** sitting at $K$. That step is a cash-or-nothing digital with $Q = 1$. Want a \$100 payout? Multiply every quantity by 100.

The island below makes this concrete. It's a spread with a **\$2-wide gap** (long call at 99, short call at 101) at **quantity 50** each — so across the \$2 gap the payoff rises by $2 \times 50 = 100$, i.e. \$100, then flattens. Flip it to **Payoff** mode and look at the shape: a steep ramp from 99 to 101, then a flat plateau at \$100. Now imagine pinching that gap from \$2 down to \$0.50, scaling the quantity up to keep the plateau at \$100 — the ramp gets near-vertical. That's the digital emerging from the limit.

<OptionPayoff
  client:visible
  showProfit={false}
  title="A tight call spread replicating a $100 digital at strike 100"
  legs={[
    { type: 'call', position: 'long', strike: 99, premium: 0, quantity: 50, label: 'Long call 99 ×50' },
    { type: 'call', position: 'short', strike: 101, premium: 0, quantity: 50, label: 'Short call 101 ×50' },
  ]}
  caption="Across the $2 gap (99 → 101) at quantity 50, the payoff climbs by 2 × 50 = $100, then flattens — a $100 cash-or-nothing digital. Narrow the gap and scale the quantity up, and the ramp steepens toward a vertical step at the strike: that's the digital as a limit."
/>

**Worked example — how notional scales.** Hold the plateau fixed at \$100 and watch the quantity explode as the gap tightens:

| Spread gap (2ε) | Strikes | Quantity per leg | Payoff rise across gap | Plateau |
|---|---|---|---|---|
| \$2 wide | 99 / 101 | 50 | $2 \times 50 = 100$ | \$100 |
| \$1 wide | 99.5 / 100.5 | 100 | $1 \times 100 = 100$ | \$100 |
| \$0.50 wide | 99.75 / 100.25 | 200 | $0.5 \times 200 = 100$ | \$100 |
| $\to 0$ | $\to 100 / 100$ | $\to \infty$ | step of \$100 | \$100 |

Same \$100 prize, but to keep it you must trade *more and more* contracts as the gap narrows — the quantity is $\tfrac{Q}{\text{gap}}$, which runs off to infinity. That blow-up isn't a footnote; it's a preview of the delta nightmare in the next section.

This is also **how digitals get priced in practice**. A desk can't hedge a true step, so it hedges the digital with a *finite* call spread — say a \$0.50 gap — which **slightly overhedges**: the real digital is a vertical step, but the spread ramps just a hair early, paying out a touch more around the strike. To cover that extra, the desk **quotes the digital a little above** the theoretical $e^{-rT} Q\, N(d_2)$. That gap is the bid-side spread, and the call-spread replication is exactly *why* it exists — and, since the spread is sensitive to the two different strikes' implied vols, it's also where the **skew** sneaks into the price.

<Categorize
  client:visible
  question="As the replicating call spread's gap (2ε) shrinks toward zero, sort what happens to each quantity."
  buckets={['Grows toward infinity', 'Stays fixed', 'Approaches a vertical step']}
  items={[
    { text: 'The number of contracts you must trade', bucket: 'Grows toward infinity' },
    { text: 'The total payout Q on the plateau', bucket: 'Stays fixed' },
    { text: 'The shape of the payoff around the strike', bucket: 'Approaches a vertical step' },
    { text: 'The dollar height of the ramp across the gap', bucket: 'Stays fixed' },
    { text: 'The steepness (slope) of the ramp', bucket: 'Grows toward infinity' },
  ]}
  explanation="The payout and the ramp's dollar height stay pinned at Q; the shape converges to a vertical step at the strike. But to hold that fixed height across a vanishing gap, both the quantity and the slope must blow up toward infinity — which is exactly the source of the digital's exploding delta."
/>

## Discontinuous delta = a hedging nightmare

<MCQ
  client:visible
  pretest
  question="It's the final hour before expiry and the stock is sitting right on a digital's strike. How does the option's delta — its sensitivity to the stock price — behave there?"
  options={[
    { text: 'It spikes toward infinity, because a tiny price move flips the payoff between zero and the full payout', correct: true },
    { text: 'It settles near zero, because the option is about to expire' },
    { text: 'It stays at a calm 0.5, the same as an at-the-money vanilla' },
    { text: 'It turns negative, because the digital loses value at the strike' },
  ]}
  explanation="Right at the strike near expiry, an infinitesimal move in the stock swings the payoff across the entire gap from 0 to Q. Delta is value change per unit of stock move, so dividing a finite (huge) value jump by a vanishing price move sends delta exploding toward infinity. It's the opposite of a calm, near-zero or 0.5 delta — and it's why these things are a hedging nightmare."
/>

**The problem.** Delta is how much the option's value moves per \$1 move in the stock — for a vanilla it's a smooth number between 0 and 1. For a digital, the value has to travel the *entire* gap from 0 to `Q` across an *infinitesimal* sliver of price right at the strike. Near expiry, with no time left to smear out the uncertainty, that transition gets razor-sharp: the value is basically a cliff. Delta is the slope of that cliff, so **delta spikes toward infinity** right at $K$ and collapses back to near-zero on either side. The gamma — the rate of change of delta — does something even worse, swinging from a giant positive to a giant negative as the stock crosses the strike.

Now picture the poor trader who sold this digital and wants to **delta-hedge it with stock**. To stay neutral they'd need to hold a position proportional to delta — which means as the stock creeps toward the strike near expiry, they must buy a colossal amount of stock for a one-cent wiggle, then dump it all the instant the stock ticks back. The hedge demands trading enormous size on microscopic moves, racking up transaction costs and getting whipsawed to death. This is **pin risk** in its purest, most violent form: the stock "pinning" near the strike at expiry turns the hedge into an impossible game of slamming the brakes and flooring it on every tick.

<Callout variant="warning" title="Why nobody hedges a digital with stock">
The delta/gamma blow-up at the strike is exactly why desks **don't** try to delta-hedge a digital with the underlying — the required position is unbounded and flips sign across a hair of price. Instead they hedge with the **finite call spread** from the last section: a \$0.25 or \$0.50 spread has a large-but-*finite* delta, no infinite spike, and it's made of liquid vanilla options. The cost of that civility is the slight overhedge — and that overhedge is precisely why the digital is quoted with a **wider spread** than its theoretical price. The discontinuity doesn't vanish; the desk just trades a manageable approximation of it and charges you for the inconvenience.
</Callout>

<FillBlank
  client:visible
  question="Fill in why a digital is a hedging nightmare near the strike."
  text="Near expiry and right at the strike, a digital's value jumps from zero to its full payout over a tiny price move, so its delta {{spikes toward infinity|drops to zero|stays at one-half}}. A trader hedging with stock would have to trade an {{enormous amount|even amount|illiquid amount}} for a one-cent wiggle — the essence of {{pin risk|basis risk|credit risk}}. So desks hedge instead with a {{finite call spread|single short call|long straddle}}, which has a large but finite delta."
  explanation="The 0-to-Q jump over a vanishing move makes delta explode (and gamma flip sign) — pin risk. Because a stock hedge would demand unbounded size on microscopic moves, desks substitute a finite call spread: large but bounded delta, built from liquid vanillas."
/>

## When you'd use one

<MCQ
  client:visible
  pretest
  question="You're confident there's better than a 60% chance a stock finishes above $80 next quarter, and you want the cleanest possible way to monetize *that probability view*. Which instrument fits best?"
  options={[
    { text: 'A cash-or-nothing digital call struck at \$80 — its price is basically the market-implied probability', correct: true },
    { text: 'A vanilla call, since it also profits if the stock rises' },
    { text: 'A long straddle at \$80, to cover both directions' },
    { text: 'Selling a put, to collect premium while you wait' },
  ]}
  explanation="A digital pays a fixed amount on a yes/no event, so its price is (near enough) the market's probability of that event. If you think the true probability beats the implied one, buying the digital expresses that edge cleanly. A vanilla call mixes in a view on *how far* it rises; a straddle bets on movement either way; selling a put is a different bet entirely. The digital is the pure probability instrument."
/>

### When to use it

**Reach for a digital when your view is genuinely about a probability, not a magnitude.** Three classic uses:

1. **A clean probability bet.** "I think there's better than a 60% chance the stock is above \$80." A digital's price *is* roughly that probability, so if your number beats the market's, the digital is mispriced in your favor. A vanilla call would muddy this with a view on *how far* above \$80 — the digital strips that out.
2. **Cheap, defined leverage on a binary event.** Earnings, a court ruling, an FDA decision — an event with a yes/no outcome. A digital lets you size the bet exactly (you know the max payout and the max cost up front) without the open-ended, vol-crush-prone behavior of a vanilla.
3. **The building block for structured products.** This is the big one for this course. A coupon that pays *only if* the index is above a level on an observation date is a cash-or-nothing digital. **Autocallables** are stacks of these triggers; **range accruals** count up digitals day by day. Once you see digitals everywhere, structured notes stop looking like magic and start looking like Lego.

**The trade-offs, honestly.** The flat payoff cuts both ways: there's **no upside beyond `Q`** — if the stock rockets past the strike you collect the same fixed amount as if it squeaked over, so you give up the vanilla's open-ended tail. And for the *seller*, the near-strike delta/gamma blow-up makes a sold digital genuinely dangerous to manage at expiry. Clean to *express*, brutal to *hedge*.

<Reveal client:visible summary="So why not always use a digital instead of a vanilla call for a bullish view?">
Because they answer different questions. A digital is a bet on *whether* the stock clears a line — its payoff is capped at `Q`, so it ignores *how far* past the line you go. A vanilla call is a bet on *how far above* the strike you finish — its payoff keeps growing with the stock. If your edge is "this event is more likely than priced," the digital is the sharp tool. If your edge is "this could run a long way," you want the vanilla's uncapped upside. Using a digital for a big-run view throws away exactly the tail you were trying to capture.
</Reveal>

<MCQ
  client:visible
  allowMultiple
  question="Select every situation where a cash-or-nothing digital is a genuinely good fit."
  options={[
    { text: 'Monetizing a sharp probability view on a yes/no event', correct: true },
    { text: 'Building the coupon trigger inside an autocallable note', correct: true },
    { text: 'Taking a defined-cost, defined-payout bet on an earnings outcome', correct: true },
    { text: 'Capturing the full upside of a stock you expect to triple' },
    { text: 'Delta-hedging a position cheaply with the underlying near the strike' },
  ]}
  explanation="Digitals shine for probability views, structured-product triggers, and defined binary-event bets. They're a bad fit for capturing big open-ended upside (the payoff is capped at Q — a vanilla keeps that tail) and a nightmare to delta-hedge with stock near the strike (the delta spikes toward infinity)."
/>

## Putting it together

A **digital (binary) option** pays a fixed amount on a yes/no condition — the simplest exotic, and the atom behind autocall coupons, range accruals, and structured-note triggers. It comes in two flavors: a **cash-or-nothing** call pays a flat `Q` above the strike, an **asset-or-nothing** call pays the share itself, and a **vanilla call is exactly asset-or-nothing minus `K` cash-or-nothing units**. Pricing is just **discounted payout × probability of finishing in the money** — under Black–Scholes, $e^{-rT} Q\, N(d_2)$ — so a digital's value reads off as a probability, tilted by a **skew-driven slope** once implied vol varies across strikes. You build (and hedge, and price) a digital as the **limit of a tight bull call spread**, scaled up: as the gap shrinks the quantity blows up toward a vertical step, the finite spread slightly overhedges, and that's why quoted digitals sit a touch above theory. The cost of that step is a **discontinuous delta** that spikes toward infinity at the strike near expiry — pin risk so violent that nobody hedges it with stock. Use a digital for a **clean probability view, defined binary-event leverage, or as a structured-product building block** — just remember the upside is **capped at `Q`** and the near-strike risk is brutal for the seller.

<MindMap
  client:visible
  title="Digital & binary options at a glance"
  caption="A digital pays a fixed amount on a yes/no condition; its price is a discounted probability (N(d2)); it's the limit of a tight call spread; its delta blows up at the strike; and it's the atom behind structured products."
  root={{
    label: 'Digital / Binary Options',
    children: [
      {
        label: 'Two flavors',
        children: [
          { label: 'Cash-or-nothing: flat Q above the strike' },
          { label: 'Asset-or-nothing: the share itself, scales with S' },
          { label: 'Vanilla = asset-or-nothing − K cash-or-nothing' },
        ],
      },
      {
        label: 'Pricing = a probability',
        children: [
          { label: 'Discounted payout × Pr(finish ITM)' },
          { label: 'Black–Scholes: e^(−rT) Q N(d2)' },
          { label: 'Skew adds a slope term to the price' },
        ],
      },
      {
        label: 'Call-spread replication',
        children: [
          { label: 'Tight bull spread, quantity 1/(2ε)' },
          { label: 'Gap → 0 ⇒ vertical step of height Q' },
          { label: 'Finite spread overhedges ⇒ quoted above theory' },
        ],
      },
      {
        label: 'Discontinuous delta',
        children: [
          { label: 'Value jumps 0 → Q over a tiny move' },
          { label: 'Delta spikes to infinity at the strike (pin risk)' },
          { label: 'Hedge with a finite call spread, not stock' },
        ],
      },
      {
        label: 'When to use',
        children: [
          { label: 'Clean probability view on a yes/no event' },
          { label: 'Defined-cost leverage on a binary event' },
          { label: 'Building block for autocalls & range accruals' },
          { label: 'Trade-off: upside capped at Q; ugly seller risk' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: digital & binary options"
  questions={[
    {
      question: 'What distinguishes a cash-or-nothing call from an asset-or-nothing call when both finish in the money?',
      options: [
        { text: 'Cash-or-nothing pays a flat fixed amount; asset-or-nothing pays the share, so its value scales with the stock', correct: true },
        { text: 'Cash-or-nothing scales with the stock; asset-or-nothing pays a flat amount' },
        { text: 'They pay identically — the names are just jargon for the same thing' },
        { text: 'Cash-or-nothing pays only at the strike; asset-or-nothing pays only far above it' },
      ],
      explanation: 'Cash-or-nothing hands you a fixed Q no matter how far above the strike you land. Asset-or-nothing hands you one share (worth S), so its payoff rises with the stock. Same yes/no trigger, different prize.',
    },
    {
      question: 'A cash-or-nothing call pays \$100, the discount factor is 0.95, and the model gives a 40% risk-neutral chance of finishing in the money. Roughly what is it worth today?',
      options: [
        { text: 'About \$38', correct: true },
        { text: 'About \$100' },
        { text: 'About \$50' },
        { text: 'About \$40, with no discounting applied' },
      ],
      explanation: 'Price = discounted payout × probability = 0.95 × 100 × 0.40. First 100 × 0.40 = $40 of expected payout, then discount: 40 × 0.95 = $38. The full $100 assumes certainty; $50 assumes a coin flip; $40 forgets to discount.',
    },
    {
      question: 'In Black–Scholes, the price of a cash-or-nothing call is the discounted payout times which quantity, and why?',
      options: [
        { text: 'N(d2), because it is the risk-neutral probability of finishing in the money', correct: true },
        { text: 'N(d1), because it is the probability of finishing in the money' },
        { text: 'The option delta, because the digital tracks the stock one-for-one' },
        { text: 'The implied volatility, because the digital is a pure vol bet' },
      ],
      explanation: 'A cash-or-nothing call prices to e^(−rT) Q N(d2), and N(d2) is exactly the risk-neutral probability of finishing above the strike. N(d1) belongs to the asset-or-nothing / delta side, not the cash digital.',
    },
    {
      question: 'Why is a quoted digital usually priced a touch above its theoretical e^(−rT) Q N(d2) value?',
      options: [
        { text: 'Desks hedge it with a finite call spread that slightly overhedges the true step, and they charge for that cushion', correct: true },
        { text: 'A modeling error in Black–Scholes always overstates digital prices' },
        { text: 'Digitals pay dividends that must be added to the price' },
        { text: 'Regulators require a fixed markup on all exotic options' },
      ],
      explanation: 'A true step can\'t be hedged, so desks use a finite (e.g. $0.50) call spread, which ramps a hair early and pays out slightly more around the strike. To cover that overhedge they quote above theory — that gap is the bid-side spread.',
    },
    {
      question: 'Right at the strike near expiry, the value jumps from 0 to Q over a vanishing price move. What does this do to the digital\'s delta, and how do desks respond?',
      options: [
        { text: 'Delta spikes toward infinity (pin risk), so desks hedge with a finite call spread rather than stock', correct: true },
        { text: 'Delta drops to zero, so desks simply hold the position unhedged' },
        { text: 'Delta settles at 0.5, so desks hedge it like an ordinary at-the-money call' },
        { text: 'Delta turns sharply negative, so desks buy stock to offset it' },
      ],
      explanation: 'A finite value jump over an infinitesimal price move makes delta explode toward infinity (and gamma flip sign) — classic pin risk. A stock hedge would demand unbounded size on tiny moves, so desks substitute a finite call spread with a large-but-bounded delta.',
    },
    {
      question: 'Which view is a cash-or-nothing digital the WRONG tool to express?',
      options: [
        { text: 'You expect the stock could triple and you want to capture that full open-ended upside', correct: true },
        { text: 'You think a yes/no event is more likely than the market is pricing' },
        { text: 'You want a defined-cost, defined-payout bet on an earnings outcome' },
        { text: 'You\'re building the coupon trigger inside a structured note' },
      ],
      explanation: 'A digital\'s payoff is capped at Q, so it throws away exactly the open-ended tail you\'d want if you expect a big run — that\'s a vanilla call\'s job. Probability views, defined binary-event bets, and structured-product triggers are all where the digital shines.',
    },
  ]}
/>

Next — **barrier options** — where the payoff doesn't just depend on where you finish, but on whether the stock ever *touched* a level along the way, layering a path-dependent trigger on top of the all-or-nothing logic you just learned.
