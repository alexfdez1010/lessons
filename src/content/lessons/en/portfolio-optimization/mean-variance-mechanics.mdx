---
title: "Mean-Variance Mechanics"
description: "Solving for the weights: the minimum-variance portfolio, the maximum-Sharpe (tangency) portfolio, the Lagrangian and closed-form solutions that invoke the inverse covariance matrix, and a fully worked two- and three-asset example."
topic: "en/portfolio-optimization"
order: 3
minutes: 13
updated: 2026-06-07
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, MatchConcepts, MindMap, CapitalMarketLine, EfficientFrontier } from '@/components/react';

Now we compute. With the covariance matrix understood, this lesson turns the optimization problem into actual weights. Two portfolios sit at the heart of everything: the **minimum-variance portfolio** — the calmest possible mix, which needs *no return forecast at all* — and the **maximum-Sharpe (tangency) portfolio** — the best risk-adjusted bet, the point the capital market line touches the frontier. We'll see where their closed-form solutions come from (the Lagrangian, $\Sigma^{-1}$), and grind through a full two-asset example by hand so the formulas stop being symbols and start being numbers. This is the mechanical core of the course; the later lessons are all about why these clean formulas misbehave on real data.

<MCQ
  client:visible
  pretest
  question="Which of these two landmark portfolios requires you to forecast expected returns?"
  options={[
    { text: 'The maximum-Sharpe (tangency) portfolio needs μ; the minimum-variance portfolio needs only Σ', correct: true },
    { text: 'Both need μ — you can’t optimize anything without expected returns' },
    { text: 'Neither needs μ — both depend only on the covariance matrix' },
    { text: 'The minimum-variance portfolio needs μ; the tangency portfolio needs only Σ' },
  ]}
  explanation="The minimum-variance portfolio just minimizes wᵀΣw subject to the budget constraint — it never looks at returns, so it needs only Σ. The tangency portfolio maximizes the Sharpe ratio, which has μ in the numerator, so it needs both μ and Σ. This is why min-var is more robust: it sidesteps the hardest-to-estimate input."
/>

## The minimum-variance portfolio

**Analogy.** Forget about chasing returns for a moment. Imagine you just want the *smoothest ride* — the portfolio that wobbles least, whatever the assets happen to earn. That's the minimum-variance portfolio: the leftmost tip of the efficient frontier, the single point where risk bottoms out.

**The problem.** Minimize portfolio variance subject only to the budget constraint:
$$\min_w\ w^\top \Sigma\, w \quad \text{s.t.}\quad \mathbf{1}^\top w = 1.$$
Notice μ never appears. To solve it, form the **Lagrangian** (variance plus a multiplier $\lambda$ times the constraint):
$$\mathcal{L} = w^\top \Sigma\, w - \lambda(\mathbf{1}^\top w - 1).$$
Set the derivative to zero ($2\Sigma w = \lambda \mathbf{1}$), solve for $w$, and impose the constraint. The result is the **closed-form minimum-variance weights**:
$$w_{\min} = \frac{\Sigma^{-1}\mathbf{1}}{\mathbf{1}^\top \Sigma^{-1}\mathbf{1}}.$$
In words: apply $\Sigma^{-1}$ to a vector of ones, then rescale so the weights sum to one.

**Worked example (two assets).** Use $\sigma_1 = 20\%$, $\sigma_2 = 10\%$, $\rho = 0.3$, so
$$\Sigma = \begin{pmatrix} 0.04 & 0.006 \\ 0.006 & 0.01 \end{pmatrix}.$$
For two assets there's a tidy shortcut formula:
$$w_1 = \frac{\sigma_2^2 - \sigma_1\sigma_2\rho}{\sigma_1^2 + \sigma_2^2 - 2\sigma_1\sigma_2\rho}.$$
Plug in: numerator $= 0.01 - 0.006 = 0.004$; denominator $= 0.04 + 0.01 - 2(0.006) = 0.05 - 0.012 = 0.038$. So
$$w_1 = \frac{0.004}{0.038} \approx 0.105, \qquad w_2 = 1 - 0.105 = 0.895.$$
The min-variance portfolio holds about **10.5% in the volatile asset and 89.5% in the calm one** — sensible, since the low-vol asset deserves most of the weight. Its variance: $w^\top\Sigma w = 0.105^2(0.04) + 0.895^2(0.01) + 2(0.105)(0.895)(0.006) \approx 0.000441 + 0.008012 + 0.001127 \approx 0.00958$, so volatility $\approx \sqrt{0.00958} \approx 9.79\%$ — *below* even the calmer asset's 10%, thanks to diversification.

<EfficientFrontier
  client:visible
  title="The minimum-variance portfolio sits at the frontier’s tip"
  riskLabel="Risk (volatility)"
  returnLabel="Expected return"
  frontierLabel="Efficient frontier"
  minVarLabel="Minimum-variance portfolio"
  feasibleLabel="Feasible portfolios"
  inefficientLabel="Inefficient (dominated)"
  caption="The accent dot at the leftmost tip is the minimum-variance portfolio — the lowest risk any mix of these assets can achieve. It needs no return forecast: it falls out of the covariance matrix alone. Everything to its right on the upper arc trades extra risk for extra return."
/>

<Callout variant="tip" title="Why min-var is a practitioner favorite">
Because it ignores μ entirely, the minimum-variance portfolio dodges the single noisiest, hardest-to-estimate input in all of finance. Empirically, min-var and low-volatility portfolios have often *beaten* the cap-weighted market on a risk-adjusted basis — a big reason "low-vol" is a recognized factor. When in doubt about your return forecasts, leaning on Σ alone is a defensible move.
</Callout>

<FillBlank
  client:visible
  question="The minimum-variance portfolio."
  text="The minimum-variance portfolio minimizes {{wᵀΣw|wᵀμ|the Sharpe ratio}} subject to the weights summing to 1, and it depends on {{only Σ|only μ|both μ and Σ}}. Its closed form is Σ⁻¹𝟙 divided by {{𝟙ᵀΣ⁻¹𝟙|μᵀΣ⁻¹μ|the number of assets}}, which just rescales the weights to sum to one."
  explanation="Min-var minimizes variance wᵀΣw with no reference to returns, so it needs only Σ. The closed form is w = Σ⁻¹𝟙 / (𝟙ᵀΣ⁻¹𝟙): apply the inverse covariance to a ones-vector, then normalize so the weights sum to 1."
/>

## The maximum-Sharpe (tangency) portfolio

**Analogy.** Now bring reward back in. Of all the portfolios you could build, which gives the most return *per unit of risk*? Draw a line from the risk-free rate and pivot it upward until it just kisses the frontier — that touch point is the **tangency portfolio**, and the steepest such line is the **capital market line**. Its slope is the highest Sharpe ratio attainable.

**The problem.** Maximize the Sharpe ratio
$$\max_w\ \frac{w^\top\mu - r_f}{\sqrt{w^\top\Sigma w}} \quad \text{s.t.}\quad \mathbf{1}^\top w = 1.$$
Working through the optimization (define **excess returns** $\mu^e = \mu - r_f\mathbf{1}$), the **tangency weights** have the clean closed form
$$w_{\tan} = \frac{\Sigma^{-1}\mu^e}{\mathbf{1}^\top\Sigma^{-1}\mu^e}.$$
Compare it to min-var: the only change is that the ones-vector is replaced by the **excess-return vector** $\mu^e$. Min-var asks "what's calmest?"; tangency asks "what's calmest *per unit of expected reward*?" — so it tilts toward high-excess-return, low-risk assets.

**Worked example (two assets).** Keep $\Sigma = \begin{pmatrix}0.04 & 0.006\\ 0.006 & 0.01\end{pmatrix}$, let $\mu = (8\%, 4\%)$ and $r_f = 2\%$, so excess returns $\mu^e = (6\%, 2\%) = (0.06, 0.02)$.

First compute $\Sigma^{-1}$. The determinant is $0.04\times0.01 - 0.006^2 = 0.0004 - 0.000036 = 0.000364$. So
$$\Sigma^{-1} = \frac{1}{0.000364}\begin{pmatrix} 0.01 & -0.006 \\ -0.006 & 0.04 \end{pmatrix} \approx \begin{pmatrix} 27.47 & -16.48 \\ -16.48 & 109.89 \end{pmatrix}.$$
Now $\Sigma^{-1}\mu^e$:

- Row 1: $27.47(0.06) + (-16.48)(0.02) = 1.648 - 0.330 = 1.319$.
- Row 2: $-16.48(0.06) + 109.89(0.02) = -0.989 + 2.198 = 1.209$.

Sum $= 1.319 + 1.209 = 2.528$. Normalize:
$$w_{\tan} = \left(\frac{1.319}{2.528},\ \frac{1.209}{2.528}\right) \approx (0.522,\ 0.478).$$
The tangency portfolio holds about **52% in the high-return asset and 48% in the low-return one** — much more aggressive than min-var's 10.5/89.5 split, because now the optimizer is paying for the first asset's higher excess return.

<CapitalMarketLine
  client:visible
  title="The tangency portfolio and the capital market line"
  riskLabel="Risk (volatility)"
  returnLabel="Expected return"
  allocationLabel="Allocation to the market portfolio"
  cmlLabel="Capital Market Line"
  frontierLabel="Efficient frontier (risky only)"
  riskFreeLabel="Risk-free asset"
  marketLabel="Market (tangency) portfolio"
  sharpeLabel="Sharpe ratio (slope)"
  lendingLabel="Lending"
  borrowingLabel="Borrowing (leverage)"
  caption="The straight Capital Market Line runs from the risk-free rate and just touches the efficient frontier at the tangency portfolio — the maximum-Sharpe mix of risky assets. Slide the allocation: below the touch point you lend (hold some risk-free), above it you borrow to lever up. The line's slope is the best Sharpe ratio available."
/>

<MatchConcepts
  client:visible
  question="Match each portfolio or term to its defining feature."
  pairs={[
    { term: 'Minimum-variance portfolio', definition: 'Lowest possible risk; closed form Σ⁻¹𝟙 normalized; needs only Σ' },
    { term: 'Tangency portfolio', definition: 'Maximum Sharpe ratio; closed form Σ⁻¹μᵉ normalized; needs μ and Σ' },
    { term: 'Capital market line', definition: 'Straight line from the risk-free rate tangent to the frontier' },
    { term: 'Lagrangian', definition: 'Objective plus a multiplier × constraint, used to derive the closed form' },
    { term: 'Excess return μᵉ', definition: 'Expected return minus the risk-free rate, μ − r_f𝟙' },
  ]}
  explanation="Min-var: Σ⁻¹𝟙 normalized, only Σ. Tangency: Σ⁻¹μᵉ normalized, needs μ and Σ. The CML is the tangent line from r_f. The Lagrangian builds the constraint into the objective to derive both closed forms. μᵉ is return above the risk-free rate."
/>

## The Lagrangian and the structure of the solution

**Why a Lagrangian?** We're minimizing (or maximizing) subject to a constraint (weights sum to 1, maybe also hit a target return). The Lagrangian trick converts a *constrained* problem into an *unconstrained* one by adding a penalty term $-\lambda(\text{constraint})$; at the optimum, the gradient of the objective is parallel to the gradient of the constraint, which the multiplier $\lambda$ measures.

**The general efficient portfolio.** For a target return $r^*$ with both constraints ($\mathbf{1}^\top w = 1$ and $w^\top\mu = r^*$), the solution is a **linear combination of two fixed portfolios** — a beautiful result called **two-fund separation**:
$$w^*(r^*) = a + b\, r^*,$$
where $a$ and $b$ are fixed vectors built from $\Sigma^{-1}$, $\mathbf{1}$, and $\mu$. As you slide $r^*$, the weights move along a straight line in weight-space, sweeping out the entire efficient frontier. **Every** efficient portfolio is a blend of just two — say, the minimum-variance portfolio and the tangency portfolio. That's why those two are *the* fundamental objects.

**The unifying thread.** Min-var ($\Sigma^{-1}\mathbf{1}$), tangency ($\Sigma^{-1}\mu^e$), and the general efficient portfolio all route through $\Sigma^{-1}$. That shared dependence is the whole reason a noisy, ill-conditioned $\Sigma$ poisons every one of them — and why the fixes in later lessons (shrinking $\Sigma$, constraining $w$) help *all* of them at once.

<Reveal client:visible>
  <details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
    <summary className="cursor-pointer font-medium">Two-fund separation: why is every efficient portfolio just a mix of two?</summary>

    Because the efficient frontier is the solution set of a *quadratic* objective under *linear* constraints, and the math of such problems forces the solution to depend **linearly** on the target return r*. Linear dependence means: pick any two efficient portfolios (two different target returns), and every other efficient portfolio is a weighted average of those two. Concretely, if you know the minimum-variance portfolio and the tangency portfolio, you can reconstruct the entire frontier by blending them in different proportions — no further optimization needed. This is the portfolio-theory version of "two points determine a line." It also has a famous practical corollary: once a risk-free asset exists, *everyone* holds the same tangency portfolio of risky assets and simply dials their risk up or down by mixing it with cash — the foundation of the Capital Asset Pricing Model. The whole apparatus collapses to two funds: one risky (tangency) and one riskless.
  </details>
</Reveal>

<MCQ
  client:visible
  question="Both the minimum-variance and tangency portfolios share a common ingredient. Which is it, and why does it matter?"
  options={[
    { text: 'Both invert the covariance matrix (use Σ⁻¹), so a noisy or ill-conditioned Σ destabilizes both at once', correct: true },
    { text: 'Both ignore the covariance matrix and depend only on μ' },
    { text: 'Both require the risk-free rate, even the minimum-variance portfolio' },
    { text: 'Both set every weight equal, differing only in leverage' },
  ]}
  explanation="Min-var is Σ⁻¹𝟙 (normalized) and tangency is Σ⁻¹μᵉ (normalized) — both apply Σ⁻¹. So any pathology in Σ (noise, near-singularity, a high condition number) propagates into both portfolios' weights. That shared dependence is why fixing Σ via shrinkage helps the whole family of efficient portfolios."
/>

## A common trap: confusing the two portfolios

**Pitfall — "the optimal portfolio is the one with the lowest risk."** No. The *lowest-risk* portfolio is the min-variance one, but it's usually **not** the best risk-adjusted bet. Unless you happen to want minimum risk above all else, the tangency portfolio (highest Sharpe) is the one to combine with cash to hit your risk target. Min-var is a *corner* of the frontier; tangency is the *pivot point* of the capital market line. They coincide only in the degenerate case where all expected excess returns are equal.

**Pitfall — "more assets always lowers min-var risk."** Adding assets can lower min-variance risk *in theory*, but with estimation error a 500-asset min-var portfolio can be *worse* out of sample than a 30-asset one, because you've injected 125,000 noisy parameters. The clean formula assumes Σ is known; reality charges you for every parameter you estimate.

<MCQ
  client:visible
  question="An investor says: 'I'll just hold the minimum-variance portfolio — it's the optimal one.' What's the most precise correction?"
  options={[
    { text: 'Min-var is only optimal if your sole goal is least risk; for the best return per unit of risk you want the tangency (max-Sharpe) portfolio, then mix with cash to set your risk level', correct: true },
    { text: 'Min-var is never useful; always hold the tangency portfolio at full leverage' },
    { text: 'The minimum-variance portfolio is identical to the tangency portfolio, so the statement is fine' },
    { text: 'Optimal portfolios must hold every asset equally, so neither is right' },
  ]}
  explanation="The minimum-variance portfolio minimizes risk but generally has a lower Sharpe ratio than the tangency portfolio. If you care about risk-adjusted return, you hold the tangency portfolio and adjust overall risk by blending it with the risk-free asset (the capital market line). They coincide only when all excess returns are equal — a knife-edge case."
/>

## Putting it together

The **minimum-variance portfolio** ($w = \Sigma^{-1}\mathbf{1}$, normalized) is the calmest mix and depends on **Σ alone** — dodging the noisy μ estimate, which is why it's robust and empirically strong. The **tangency / maximum-Sharpe portfolio** ($w = \Sigma^{-1}\mu^e$, normalized) is the best risk-adjusted bet — the touch point of the **capital market line** — and needs both μ and Σ. Both come from a **Lagrangian** that builds the budget constraint into the objective, and both invoke **$\Sigma^{-1}$**. By **two-fund separation**, every efficient portfolio is a linear blend of these two, so they're the fundamental building blocks. The shared reliance on $\Sigma^{-1}$ is exactly why a noisy covariance matrix destabilizes the whole family — the problem we diagnose next.

<MindMap
  client:visible
  title="Mean-variance mechanics — the whole picture"
  caption="Two landmark portfolios — min-variance (Σ only) and tangency (μ and Σ) — both built from Σ⁻¹, and every efficient portfolio is a blend of the two."
  root={{
    label: 'Mean-variance mechanics',
    children: [
      {
        label: 'Minimum-variance',
        children: [
          { label: 'Min wᵀΣw s.t. weights sum to 1' },
          { label: 'Closed form: Σ⁻¹𝟙 normalized' },
          { label: 'Needs only Σ — robust to bad μ' },
          { label: 'Leftmost tip of the frontier' },
        ],
      },
      {
        label: 'Tangency (max-Sharpe)',
        children: [
          { label: 'Max Sharpe = excess return / risk' },
          { label: 'Closed form: Σ⁻¹μᵉ normalized' },
          { label: 'Needs μ and Σ' },
          { label: 'Touch point of the capital market line' },
        ],
      },
      {
        label: 'The Lagrangian',
        children: [
          { label: 'Builds the constraint into the objective' },
          { label: 'Multiplier λ measures the trade-off' },
          { label: 'Derives both closed forms' },
        ],
      },
      {
        label: 'Two-fund separation',
        children: [
          { label: 'Every efficient portfolio = blend of two' },
          { label: 'Weights move linearly with target return' },
          { label: 'All route through Σ⁻¹ → shared fragility' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: mean-variance mechanics"
  questions={[
    {
      question: 'Two assets have σ₁ = 30%, σ₂ = 10%, and ρ = 0. Using w₁ = (σ₂² − σ₁σ₂ρ)/(σ₁² + σ₂² − 2σ₁σ₂ρ), what is the minimum-variance weight in asset 1?',
      options: [
        { text: '0.1 — w₁ = 0.01/(0.09+0.01) = 0.01/0.10 = 0.1, so 10% in the volatile asset', correct: true },
        { text: '0.5 — equal weight, since correlation is zero' },
        { text: '0.9 — most weight goes to the volatile asset' },
        { text: '0.3 — proportional to the volatility' },
      ],
      explanation: 'With ρ = 0 the formula simplifies to w₁ = σ₂²/(σ₁²+σ₂²) = 0.01/(0.09+0.01) = 0.01/0.10 = 0.1. The min-variance portfolio puts most weight (90%) on the calmer asset — inverse to its variance when uncorrelated.',
    },
    {
      question: 'Which closed-form expression gives the tangency (maximum-Sharpe) portfolio?',
      options: [
        { text: 'Σ⁻¹μᵉ normalized to sum to 1, where μᵉ is the excess-return vector', correct: true },
        { text: 'Σ⁻¹𝟙 normalized to sum to 1 (the same as minimum-variance)' },
        { text: 'μᵉ divided by the number of assets, ignoring Σ' },
        { text: 'The equal-weight vector (1/N, …, 1/N)' },
      ],
      explanation: 'The tangency portfolio is w = Σ⁻¹μᵉ / (𝟙ᵀΣ⁻¹μᵉ): apply the inverse covariance to the excess-return vector, then normalize. Σ⁻¹𝟙 normalized is the minimum-variance portfolio (no μ). Tangency differs precisely by using μᵉ instead of 𝟙.',
    },
    {
      question: 'What does two-fund separation tell us about the efficient frontier?',
      options: [
        { text: 'Every efficient portfolio is a linear combination of just two efficient portfolios (e.g. min-variance and tangency)', correct: true },
        { text: 'The frontier requires a separate optimization for every target return, with no shortcut' },
        { text: 'Only two assets can ever be efficient at once' },
        { text: 'The frontier is a single point, not a curve' },
      ],
      explanation: 'Because the problem is quadratic with linear constraints, the optimal weights depend linearly on the target return. So any two efficient portfolios span all of them — pick min-variance and tangency, and every other efficient portfolio is a blend of those two. With a risk-free asset, this collapses to one risky fund (tangency) plus cash.',
    },
    {
      question: 'Cause → effect. You add 470 more stocks to a 30-stock minimum-variance portfolio. Why might out-of-sample risk get WORSE despite more diversification?',
      options: [
        { text: 'The 500-asset Σ requires estimating ~125,000 noisy parameters; the estimation error injected can outweigh the theoretical diversification benefit', correct: true },
        { text: 'More assets always increase variance by construction' },
        { text: 'The minimum-variance formula stops working above 30 assets' },
        { text: 'Adding assets forces equal weighting, which is riskier' },
      ],
      explanation: 'In theory more assets lower min-variance risk, but the closed form assumes Σ is known. With 500 assets you estimate ~125,000 parameters from thin history, and that noise feeds through Σ⁻¹ into unstable weights. The injected estimation error can swamp the diversification gain, making the bigger portfolio worse out of sample — the core failure we examine next.',
    },
  ]}
/>

Next — **estimation error and instability**: we've built the clean machinery, so now we break it. We'll see exactly why feeding estimated μ and Σ into these $\Sigma^{-1}$ formulas turns the optimizer into an "error maximizer", why the weights swing violently from sample to sample, and why the razor-sharp "optimal" portfolio so often loses to a dumb equal-weight one out of sample.
