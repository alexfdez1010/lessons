---
title: "From Frontier to Weights"
description: "Recap of mean-variance theory, the optimization problem in plain terms, the two inputs (expected returns μ and covariance Σ), what a portfolio of weights even is, and what 'solving for the weights' actually means."
topic: "en/portfolio-optimization"
order: 1
minutes: 11
updated: 2026-06-07
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, MatchConcepts, MindMap, PoWeightsBar, EfficientFrontier } from '@/components/react';

Portfolio theory gave you a gorgeous *picture* — an efficient frontier curving up and to the left, a tangency portfolio, a capital market line — and then waved its hands at the part that actually matters to a trader: **which assets do I buy, and how much of each?** That answer is a list of numbers called **weights**, and producing it from real data is the entire job of portfolio optimization. This lesson nails down the problem precisely: what you're solving for, what you feed in, and what "optimal" even means once you stop drawing curves and start computing vectors. Everything in this course is an answer to one question — *given my beliefs about the assets, what weights should I hold?* — so let's state that question with no hand-waving.

<MCQ
  client:visible
  pretest
  question="You've decided on the perfect efficient portfolio from a chart. What concrete object do you actually need to hand your broker to implement it?"
  options={[
    { text: 'A vector of weights — the fraction of your capital in each asset, which together describe the whole portfolio', correct: true },
    { text: 'The Sharpe ratio of the portfolio, which the broker uses to buy the assets' },
    { text: 'The expected return alone — the broker infers the holdings from it' },
    { text: 'The covariance matrix, since it already contains the portfolio' },
  ]}
  explanation="A portfolio IS its weights — w₁ in asset 1, w₂ in asset 2, and so on. The Sharpe ratio, expected return, and covariance matrix are inputs or summaries; the thing you trade is the weight vector. Portfolio optimization is the machinery that turns your beliefs (μ and Σ) into that vector."
/>

## What a portfolio actually is: a vector of weights

**Analogy.** Think of your capital as a pie and each asset as a slice. The recipe for the pie is just the list of slice sizes: 40% stocks, 30% bonds, and so on. That list — one number per asset — *is* the portfolio. Change the numbers and you have a different portfolio; nothing else about it is fundamental.

**Definition.** A **portfolio** of $N$ assets is a vector of **weights**
$$w = (w_1, w_2, \dots, w_N),$$
where $w_i$ is the fraction of total capital invested in asset $i$. The defining constraint is that the weights **sum to one**:
$$\sum_{i=1}^N w_i = 1.$$
That budget constraint says you invest all your money and no more. A weight can be **negative** — that's a **short position**, where you borrow and sell an asset, using the proceeds to buy more of something else (so the longs can sum to more than 100%). A **long-only** portfolio additionally requires every $w_i \ge 0$.

**Worked example.** You have \$100,000. You put \$60,000 in a stock fund, \$30,000 in bonds, and \$10,000 in gold. The weights are
$$w = \left(\tfrac{60\,000}{100\,000}, \tfrac{30\,000}{100\,000}, \tfrac{10\,000}{100\,000}\right) = (0.6,\ 0.3,\ 0.1),$$
and indeed $0.6 + 0.3 + 0.1 = 1$. Now suppose instead you short \$20,000 of gold and use the cash to buy \$120,000 of stock: $w = (1.2,\ 0.0,\ -0.2)$, which *still* sums to one ($1.2 + 0 - 0.2 = 1$). Same budget, very different bet.

<PoWeightsBar
  client:visible
  title="A portfolio is a vector of weights"
  assetLabels={['Stocks', 'Bonds', 'Gold', 'Cash']}
  tiltLabel="Tilt: defensive ↔ aggressive"
  shortLabel="Defensive: spread out, all long"
  concentratedLabel="Aggressive: concentrated, one short leg"
  sumLabel="Sum of weights"
  caption="The optimizer hands you one number per asset — the share of capital it gets. They must sum to 100%, but nothing stops a weight from going negative: that is a short position, funding a bigger bet elsewhere. Drag the tilt to see weights swing from a calm, spread-out mix to an aggressive, concentrated one — and watch the sum stay pinned at 100%."
/>

<Callout variant="info" title="Why weights, not dollars">
Working in weights instead of dollar amounts makes the math scale-free: the optimal *mix* for \$1,000 is the same as for \$1,000,000,000. It also lets us add the constraint "weights sum to 1" cleanly, and it's why every formula in this course is written in $w$, not in cash.
</Callout>

<MCQ
  client:visible
  question="A portfolio has weights w = (0.9, 0.5, −0.4) across three assets. Which statement is correct?"
  options={[
    { text: 'It is valid and self-financing — the weights sum to 1 — and it shorts the third asset to overweight the first two', correct: true },
    { text: 'It is invalid because one weight is negative; weights must all be positive' },
    { text: 'It is invalid because the weights sum to more than 1' },
    { text: 'It is long-only, since most of the weights are positive' },
  ]}
  explanation="0.9 + 0.5 − 0.4 = 1.0, so the budget constraint holds — it's a valid, fully invested portfolio. The −0.4 is a short position: you sell the third asset short and plough the proceeds into the first two. It is NOT long-only precisely because a weight is negative."
/>

## The two inputs: expected returns μ and covariance Σ

Every mean-variance optimizer eats exactly two things. Get these wrong and nothing downstream can save you — which is the running theme of this whole course.

**Input 1 — expected returns (μ).** A vector $\mu = (\mu_1, \dots, \mu_N)$ giving your forecast of each asset's average future return. This is the *reward* side. Crucially, these are **forecasts** — guesses about the future — and they are the hardest thing in finance to estimate accurately.

**Input 2 — the covariance matrix (Σ).** An $N \times N$ table capturing risk and co-movement. Its diagonal entry $\Sigma_{ii} = \sigma_i^2$ is asset $i$'s **variance** (volatility squared); its off-diagonal entry $\Sigma_{ij} = \sigma_i \sigma_j \rho_{ij}$ is the **covariance** between assets $i$ and $j$, where $\rho_{ij}$ is their correlation. This is the *risk* side, and it encodes the diversification that makes portfolios worth building in the first place.

From these two inputs, the portfolio's expected return and variance are compact matrix expressions:
$$\mathbb{E}[r_p] = w^\top \mu, \qquad \mathrm{Var}(r_p) = w^\top \Sigma\, w.$$

**Worked example (two assets).** Let $\mu = (8\%,\ 4\%)$, with volatilities $\sigma_1 = 20\%$, $\sigma_2 = 10\%$, and correlation $\rho = 0.3$. The covariance matrix is
$$\Sigma = \begin{pmatrix} 0.20^2 & 0.20\cdot 0.10\cdot 0.3 \\ 0.20\cdot 0.10\cdot 0.3 & 0.10^2 \end{pmatrix} = \begin{pmatrix} 0.04 & 0.006 \\ 0.006 & 0.01 \end{pmatrix}.$$
For an equal-weight portfolio $w = (0.5, 0.5)$:

- Expected return: $w^\top \mu = 0.5(8\%) + 0.5(4\%) = 6\%$.
- Variance: $w^\top \Sigma w = 0.5^2(0.04) + 0.5^2(0.01) + 2(0.5)(0.5)(0.006) = 0.01 + 0.0025 + 0.003 = 0.0155$.
- Volatility: $\sqrt{0.0155} \approx 12.45\%$.

Notice the volatility (12.45%) is *below* the simple average of the two asset vols ($\tfrac{20\% + 10\%}{2} = 15\%$). That gap is **diversification**, and it lives entirely in the off-diagonal covariance term. If the assets were perfectly correlated ($\rho = 1$), that term would be bigger and the gap would vanish.

<FillBlank
  client:visible
  question="The two inputs and what they carry."
  text="Mean-variance optimization needs two inputs: the vector {{μ of expected returns|of past prices|of weights}}, which carries the reward, and the {{covariance matrix Σ|Sharpe ratio|risk-free rate}}, which carries the risk and co-movement. The portfolio's expected return is {{wᵀμ|wᵀΣw|μᵀΣμ}} and its variance is {{wᵀΣw|wᵀμ|Σw}}."
  explanation="μ is the expected-return vector (reward); Σ is the covariance matrix (risk + co-movement). Portfolio return = wᵀμ (a weighted average of expected returns); portfolio variance = wᵀΣw (a quadratic form that includes every pairwise covariance)."
/>

## The optimization problem, stated plainly

Now we can write down what we're actually solving. Mean-variance optimization comes in a few equivalent flavors, but they all trade off the same two quantities: **reward ($w^\top\mu$)** and **risk ($w^\top\Sigma w$)**.

**Flavor 1 — minimize risk for a target return.** Pick the lowest-variance portfolio that achieves a required expected return $r^*$:
$$\min_w\ w^\top \Sigma\, w \quad \text{s.t.}\quad w^\top \mu = r^*,\ \ \mathbf{1}^\top w = 1.$$

**Flavor 2 — maximize the risk–reward trade-off.** Maximize a utility that rewards return and penalizes variance, where $\lambda$ is your **risk aversion**:
$$\max_w\ w^\top \mu - \tfrac{\lambda}{2}\, w^\top \Sigma\, w \quad \text{s.t.}\quad \mathbf{1}^\top w = 1.$$

**Flavor 3 — maximize the Sharpe ratio.** Find the portfolio with the best return per unit of risk (the **tangency** portfolio):
$$\max_w\ \frac{w^\top \mu - r_f}{\sqrt{w^\top \Sigma\, w}}.$$

As you sweep the target return $r^*$ (or the risk aversion $\lambda$) across all values, the solutions trace out the **efficient frontier** you already know. So "solving for the weights" means: *fix what you want (a target, a risk aversion, or the best Sharpe), then find the weight vector that achieves it with no waste.*

<EfficientFrontier
  client:visible
  title="Each target return picks one efficient weight vector"
  riskLabel="Risk (volatility)"
  returnLabel="Expected return"
  frontierLabel="Efficient frontier"
  minVarLabel="Minimum-variance portfolio"
  feasibleLabel="Feasible portfolios"
  inefficientLabel="Inefficient (dominated)"
  caption="Drag the marker up the efficient arc. Each position you stop at corresponds to one target return — and behind that single point sits a whole weight vector the optimizer computed. The leftmost tip is the minimum-variance portfolio; everything inside the cloud is a portfolio you could hold but shouldn't, because something on the frontier dominates it."
/>

<Callout variant="warning" title="The catch this whole course is about">
Notice all three problems take μ and Σ as if they were *known*. They are not — they're estimated from noisy history. Almost every disaster in portfolio optimization traces back to feeding a beautiful exact optimizer with garbage-y, error-laden inputs. Hold that thought; lessons 4–6 are entirely about it.
</Callout>

<MatchConcepts
  client:visible
  question="Match each piece of the optimization setup to what it means."
  pairs={[
    { term: 'Weight vector w', definition: 'The portfolio itself — the fraction of capital in each asset, summing to 1' },
    { term: 'Expected returns μ', definition: 'The reward input — a forecast of each asset’s average future return' },
    { term: 'Covariance matrix Σ', definition: 'The risk input — variances on the diagonal, co-movements off it' },
    { term: 'Budget constraint', definition: 'The requirement that the weights sum to 1 (fully invested)' },
    { term: 'Efficient frontier', definition: 'The set of solutions as you sweep the target return — best risk for each reward' },
  ]}
  explanation="w is the portfolio. μ (reward) and Σ (risk) are the two inputs. The budget constraint 1ᵀw = 1 keeps you fully invested. Sweeping the target return over the optimizer traces the efficient frontier."
/>

## What "solving" looks like — and why it's not just plug-and-play

For the simplest case (no short-sale or box constraints, just the budget constraint), the optimizer has a **closed-form solution**: a tidy formula involving the inverse of the covariance matrix, $\Sigma^{-1}$. You'll meet it in detail in lesson 3. The key structural fact — and the seed of every problem to come — is that the answer depends on **inverting Σ**.

**Why that matters.** Inverting a matrix is exquisitely sensitive to errors when the matrix is *ill-conditioned* — when some assets are nearly redundant (highly correlated). A near-singular Σ has an inverse with gigantic entries, and those gigantic entries multiply your estimation errors into gigantic, unstable weights. This is the mechanical reason the naive optimizer behaves so badly, and it's why the back half of this course is devoted to taming Σ (with shrinkage) and constraining $w$ (with bounds and penalties).

**When constraints enter**, the closed form usually disappears and you solve numerically with a **quadratic program** — a standard, fast, reliable optimization routine that handles "weights sum to 1", "no shorts", "no more than 5% in any one name", "turnover below 20%", and so on, all at once. Real desks almost always run the constrained, numerical version, never the raw closed form.

<Reveal client:visible>
  <details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
    <summary className="cursor-pointer font-medium">If the unconstrained problem has a clean formula, why would anyone bother with the slower numerical version?</summary>

    Because the clean formula will happily hand you a portfolio that is 340% long one stock and 290% short another — mathematically optimal for your (noisy) inputs, operationally insane, and probably illegal for a long-only mandate. The numerical quadratic program lets you bolt on the constraints that keep the answer tradeable: no shorting, position caps, sector limits, a turnover budget, a cap on how far you drift from a benchmark. Those constraints aren't a compromise of the theory — they're a crucial defense against estimation error, because they physically prevent the optimizer from taking the extreme positions that estimation noise tempts it into. In practice, sensible constraints often improve *out-of-sample* performance more than any clever tweak to the inputs. The closed form is a teaching tool and a sanity check; the constrained QP is what actually trades.
  </details>
</Reveal>

<MCQ
  client:visible
  question="Why does the dependence on Σ⁻¹ (the inverse of the covariance matrix) make the naive optimizer fragile?"
  options={[
    { text: 'When assets are highly correlated, Σ is nearly singular and its inverse has huge entries that amplify small estimation errors into wild weights', correct: true },
    { text: 'Matrix inversion is impossible when weights must sum to 1, so the optimizer always fails' },
    { text: 'Σ⁻¹ removes the diagonal, so all variance information is lost' },
    { text: 'The inverse makes the expected returns μ irrelevant, so the portfolio ignores reward' },
  ]}
  explanation="An ill-conditioned (near-singular) Σ — caused by near-redundant, highly correlated assets — has an inverse with enormous entries. Those entries multiply the errors in your estimated inputs, so a tiny mistake in μ or Σ becomes a giant, unstable swing in w. That sensitivity is the mechanical root of 'error maximization', covered in lesson 4."
/>

## Putting it together

A **portfolio is a vector of weights** $w$ summing to one — negative weights are shorts, all-nonnegative is long-only — and that vector is the concrete thing you trade. A mean-variance optimizer takes two inputs: **expected returns $\mu$** (reward) and the **covariance matrix $\Sigma$** (risk and co-movement), from which $\mathbb{E}[r_p] = w^\top\mu$ and $\mathrm{Var}(r_p) = w^\top\Sigma w$. **Solving for the weights** means minimizing risk for a target return, maximizing a return-minus-risk utility, or maximizing the Sharpe ratio — and sweeping the target traces the **efficient frontier**. The clean unconstrained solution depends on $\Sigma^{-1}$, which is exactly why a near-singular, error-laden $\Sigma$ makes the naive optimizer fragile — the headline problem this course will diagnose and then fix with constraints and shrinkage.

<MindMap
  client:visible
  title="From frontier to weights — the whole picture"
  caption="A portfolio is a weight vector; the optimizer turns the two inputs μ and Σ into that vector by trading off reward (wᵀμ) against risk (wᵀΣw)."
  root={{
    label: 'From frontier to weights',
    children: [
      {
        label: 'A portfolio = weights',
        children: [
          { label: 'w = (w1, ..., wN), one per asset' },
          { label: 'Budget: weights sum to 1' },
          { label: 'Negative weight = short; all ≥ 0 = long-only' },
        ],
      },
      {
        label: 'Two inputs',
        children: [
          { label: 'mu: expected returns (reward), forecasts' },
          { label: 'Sigma: covariance matrix (risk + co-movement)' },
          { label: 'Return = wᵀμ; Variance = wᵀΣw' },
        ],
      },
      {
        label: 'The problem',
        children: [
          { label: 'Min variance for a target return' },
          { label: 'Or max (return − risk penalty)' },
          { label: 'Or max Sharpe (tangency)' },
          { label: 'Sweep target → efficient frontier' },
        ],
      },
      {
        label: 'Solving',
        children: [
          { label: 'Unconstrained: closed form via Σ⁻¹' },
          { label: 'Σ⁻¹ amplifies input errors → fragility' },
          { label: 'Constrained: quadratic program' },
          { label: 'Constraints defend against estimation noise' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: from frontier to weights"
  questions={[
    {
      question: 'A two-asset portfolio has weights (0.7, 0.3) and expected returns μ = (10%, 5%). What is the portfolio’s expected return?',
      options: [
        { text: '8.5% — wᵀμ = 0.7×10% + 0.3×5% = 7% + 1.5%', correct: true },
        { text: '15% — you add the two expected returns' },
        { text: '7.5% — the simple average of the two returns' },
        { text: '5% — the lower of the two returns dominates' },
      ],
      explanation: 'Portfolio expected return is the weighted average wᵀμ = 0.7×10% + 0.3×5% = 7% + 1.5% = 8.5%. It is not the sum (that ignores the weights) nor the simple average (that ignores the 70/30 tilt toward the higher-return asset).',
    },
    {
      question: 'Which is TRUE about the covariance matrix Σ?',
      options: [
        { text: 'Its diagonal holds each asset’s variance and its off-diagonals hold pairwise covariances; the off-diagonals are where diversification lives', correct: true },
        { text: 'It holds only the expected returns of the assets' },
        { text: 'It is always diagonal, because assets never move together' },
        { text: 'It must be inverted to get the expected returns μ' },
      ],
      explanation: 'Σ has variances on the diagonal (σᵢ²) and covariances off it (σᵢσⱼρᵢⱼ). The off-diagonal terms capture co-movement, which is exactly what makes diversification possible — lower correlations shrink portfolio variance below the average of the individual variances.',
    },
    {
      question: 'Spot the trap. A colleague says: "An optimal portfolio can never have a weight above 100% in a single asset." Why is this wrong?',
      options: [
        { text: 'Shorting lets a long weight exceed 100% — e.g. (1.3, −0.3) sums to 1 by funding the overweight with a short', correct: true },
        { text: 'It is correct — weights are fractions, so none can exceed 1' },
        { text: 'Weights above 100% violate the budget constraint, so they are impossible' },
        { text: 'Only the minimum-variance portfolio can exceed 100% in one asset' },
      ],
      explanation: 'The budget constraint is that ALL weights sum to 1, not that each is below 1. A short position (negative weight) frees up cash, letting a long weight rise above 100%. (1.3, −0.3) sums to 1 and is perfectly valid unless a long-only constraint forbids the short.',
    },
    {
      question: 'Why do practitioners run a constrained quadratic program instead of the clean closed-form solution?',
      options: [
        { text: 'Constraints (no shorts, position caps, turnover limits) keep the portfolio tradeable AND defend against the extreme positions estimation error tempts the optimizer into', correct: true },
        { text: 'The closed form is mathematically wrong, so it must never be used' },
        { text: 'Quadratic programs ignore the covariance matrix, making them faster' },
        { text: 'Constraints always raise expected return, so they are free money' },
      ],
      explanation: 'The unconstrained closed form can return wildly leveraged, un-tradeable positions driven by noise. A quadratic program adds real-world constraints (long-only, caps, turnover, tracking error) that both make the portfolio implementable and physically block the extreme bets estimation error produces — often improving out-of-sample results.',
    },
  ]}
/>

Next up — **the covariance matrix** itself: how it's built, why it must be positive semidefinite, how its sheer size explodes with the number of assets, and what "ill-conditioned" really means. Σ is the beating heart of every risk calculation in this course, and understanding its structure is the prerequisite for understanding why optimization is so fragile — and how to fix it.
