---
title: "Risk Parity & Constraints"
description: "Risk-based allocation and real-world frictions: risk contribution and equal-risk-contribution (risk parity), why equal dollars aren't equal risk, long-only and box constraints, turnover penalties, and how transaction costs reshape the optimal portfolio."
topic: "en/portfolio-optimization"
order: 6
minutes: 13
updated: 2026-06-07
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, MatchConcepts, MindMap, PoRiskParityWheel, PoTurnoverCost } from '@/components/react';

We end where theory meets the trading desk. Two themes close the course. First, **risk parity** — a whole family of allocation methods that throw out return forecasts (the noisiest, most dangerous input) and instead balance how much *risk* each asset contributes, on the insight that a "balanced" 60/40 fund is secretly a ~90% equity-risk bet. Second, the **constraints and costs** that govern every real portfolio: you can't short in a long-only mandate, you can't hold 80% in one stock, and every trade pays the broker — frictions that quietly reshape what "optimal" even means. Master these and you can build a portfolio that survives not just the spreadsheet but the market.

<MCQ
  client:visible
  pretest
  question="A classic '60% stocks / 40% bonds' portfolio splits the DOLLARS 60/40. Roughly how is the RISK split, given stocks are far more volatile than bonds?"
  options={[
    { text: 'Around 90% of the risk comes from stocks — the volatile asset dominates risk even at 60% of the dollars', correct: true },
    { text: 'Exactly 60/40, the same as the dollar split' },
    { text: 'Around 40% stocks / 60% bonds — bonds dominate because there are more of them by risk' },
    { text: '50/50, because diversification always equalizes risk' },
  ]}
  explanation="Because stocks are ~3–4× as volatile as bonds, a 60/40 dollar split puts roughly 90% of the portfolio's RISK in stocks. Equal dollars are nowhere near equal risk — the whole motivation for risk parity, which reweights so each asset contributes the same risk."
/>

## Risk contribution: equal dollars aren't equal risk

**Analogy.** Three people push a car. If you measure their *effort* by where they stand, you'd say they contribute equally. But if one is a weightlifter and two are children, the weightlifter supplies almost all the force. A portfolio is the same: the dollar weights tell you where everyone stands, but the **risk contributions** tell you who's actually doing the pushing — and the volatile asset is the weightlifter.

**Definition.** The portfolio volatility is $\sigma_p = \sqrt{w^\top\Sigma w}$. Each asset's **marginal contribution to risk** is how much $\sigma_p$ rises if you nudge its weight: $\text{MCR}_i = \frac{(\Sigma w)_i}{\sigma_p}$. Its **total risk contribution** is weight times marginal contribution:
$$\text{RC}_i = w_i \cdot \text{MCR}_i = \frac{w_i(\Sigma w)_i}{\sigma_p}.$$
A beautiful fact: these **sum to the total volatility**, $\sum_i \text{RC}_i = \sigma_p$, so the *fractions* $\text{RC}_i / \sigma_p$ are a clean "who owns the risk" pie that adds to 100%.

**Worked example (60/40, ignoring correlation for clarity).** Stocks: weight 0.6, vol 18%. Bonds: weight 0.4, vol 5%. Treat them as uncorrelated, so each asset's risk contribution is proportional to $w_i^2\sigma_i^2$:

- Stocks: $0.6^2 \times 0.18^2 = 0.36 \times 0.0324 = 0.011664$.
- Bonds: $0.4^2 \times 0.05^2 = 0.16 \times 0.0025 = 0.0004$.
- Total $= 0.012064$.
- Stock share of risk: $0.011664 / 0.012064 \approx 96.7\%$.

The "balanced" 60/40 portfolio is really a **~97% stock-risk bet** (a bit under 90% once you add realistic positive correlation, but the point stands). Your bonds are along for the ride.

<PoRiskParityWheel
  client:visible
  title="Equal dollars ≠ equal risk"
  assetLabels={['Stocks', 'Bonds', 'Commodities']}
  vols={[0.18, 0.05, 0.15]}
  equalWeightLabel="Equal weight"
  riskParityLabel="Risk parity"
  capitalHeading="Capital share"
  riskHeading="Risk contribution"
  caption="Left donut: how the money is split. Right donut: how the risk is split. With equal dollars, the volatile asset quietly dominates the risk — your 'balanced' fund is anything but. Switch to risk parity and the risk donut evens out, at the cost of piling capital (and often leverage) into the calm asset."
/>

<FillBlank
  client:visible
  question="Risk contribution."
  text="An asset's risk contribution is its weight times its {{marginal contribution to risk|expected return|correlation}}, and the contributions {{sum to total portfolio volatility|sum to 1|cancel out}}. In a 60/40 portfolio, the volatile asset supplies {{far more|exactly 60%|less}} than its dollar share of the risk."
  explanation="RCᵢ = wᵢ × MCRᵢ = wᵢ(Σw)ᵢ/σₚ, and these sum to σₚ — so the fractions form a risk pie totalling 100%. Because stocks are much more volatile, in 60/40 they contribute ~90%+ of the risk, far above their 60% dollar share."
/>

## Risk parity: equalize the risk contributions

**The idea.** Instead of equalizing dollars (1/N) or maximizing a noisy Sharpe ratio, **risk parity** chooses weights so that **every asset contributes the same risk**:
$$\text{RC}_1 = \text{RC}_2 = \cdots = \text{RC}_N.$$
This is the **equal-risk-contribution (ERC)** portfolio. It requires no expected-return forecast at all — only the covariance matrix — so it sidesteps the optimizer's most dangerous input entirely, just like minimum-variance but with a different, often better-diversified, risk profile.

**The simple case.** If assets are uncorrelated, equal risk contribution means weighting **inversely to volatility**: $w_i \propto 1/\sigma_i$. The calm asset gets *more* dollars, the volatile asset *fewer*, until each pushes the car equally hard. With correlations, you solve a small numerical problem, but the inverse-vol intuition holds.

**Worked example (inverse-vol).** Stocks vol 18%, bonds vol 5%, commodities vol 15%. Inverse vols: $1/0.18 = 5.56$, $1/0.05 = 20.0$, $1/0.15 = 6.67$; sum $= 32.23$. Weights:
$$w = \left(\tfrac{5.56}{32.23},\ \tfrac{20.0}{32.23},\ \tfrac{6.67}{32.23}\right) \approx (0.172,\ 0.620,\ 0.207).$$
Risk parity puts **62% in the calm bond** and only 17% in stocks — the opposite tilt from 60/40. Each asset now contributes roughly a third of the risk.

**The leverage caveat.** Because risk parity piles capital into low-volatility assets, the *unlevered* portfolio has low total risk and therefore low expected return. To hit an equity-like return target, risk-parity funds typically **apply leverage** (borrow to scale the whole portfolio up). That's the famous "risk parity uses leverage" critique: the diversification is real, but reaching competitive returns means borrowing, which adds funding risk and can hurt badly when correlations spike and *everything* falls together (e.g. a rate shock hitting both stocks and bonds).

<Callout variant="warning" title="Risk parity's Achilles' heel">
Risk parity assumes you can diversify risk across assets, but in crises correlations rush toward 1 — stocks, bonds, and commodities can fall *together*, especially during inflation/rate shocks. When that happens the levered low-vol legs amplify losses. Risk parity isn't magic; it's a bet that diversification holds, scaled up by leverage. Size the leverage with that fragility in mind.
</Callout>

<MatchConcepts
  client:visible
  question="Match each allocation method to its defining rule."
  pairs={[
    { term: 'Equal weight (1/N)', definition: 'Equal dollars per asset; ignores both risk and return' },
    { term: 'Minimum variance', definition: 'Lowest total risk; uses Σ only, often concentrated in low-vol assets' },
    { term: 'Risk parity (ERC)', definition: 'Equal risk contribution per asset; uses Σ only, often needs leverage' },
    { term: 'Maximum Sharpe (tangency)', definition: 'Best return per unit risk; needs both μ and Σ, most input-sensitive' },
    { term: 'Inverse-volatility', definition: 'Weight ∝ 1/σ; the uncorrelated special case of risk parity' },
  ]}
  explanation="1/N equalizes dollars. Min-variance minimizes total risk (Σ only). Risk parity equalizes risk contributions (Σ only, often levered). Tangency maximizes Sharpe (needs μ, most fragile). Inverse-vol (w ∝ 1/σ) is risk parity when assets are uncorrelated."
/>

<MCQ
  client:visible
  question="Three uncorrelated assets have volatilities 10%, 20%, and 40%. Under inverse-volatility (risk parity), which asset gets the LARGEST weight and why?"
  options={[
    { text: 'The 10% asset — weight ∝ 1/σ, so the calmest asset gets the most dollars to equalize risk contributions', correct: true },
    { text: 'The 40% asset — the most volatile deserves the most capital' },
    { text: 'All three get equal weight regardless of volatility' },
    { text: 'The 20% asset — the middle volatility is always overweighted' },
  ]}
  explanation="Inverse-vol weights are proportional to 1/σ: 1/0.10 = 10, 1/0.20 = 5, 1/0.40 = 2.5. The calmest (10% vol) asset gets the biggest weight because you need MORE of a low-risk asset for it to contribute as much risk as a small slice of a high-risk one. Equalizing risk means overweighting the calm and underweighting the volatile."
/>

## Long-only and box constraints

**Why constrain at all.** As we saw, unconstrained optimizers produce wild, leveraged, un-tradeable positions. Constraints both make portfolios *implementable* and *defend against estimation error* by physically blocking the extreme bets noise tempts the optimizer into.

**The common constraints.**

- **Long-only**: every $w_i \ge 0$ — no shorting. Required for most mutual funds and many mandates. This single constraint dramatically tames the optimizer; in fact, **a long-only constraint acts a lot like shrinkage**, because it forbids the offsetting long/short noise trades that wreck naive optimization.
- **Box constraints**: lower and upper bounds per asset, $\ell_i \le w_i \le u_i$ (e.g. "no more than 5% in any one stock", "at least 1% in each"). Prevents concentration and forces a minimum of diversification.
- **Budget / fully invested**: $\sum w_i = 1$ (always present).
- **Group/sector constraints**: "no more than 30% in tech", "at least 20% in bonds" — risk-management overlays.

**The effect.** With constraints the closed form disappears and you solve a **quadratic program** numerically — fast and reliable. The constrained solution is *less* optimal on the (noisy) sample but typically *more* robust out of sample, because the constraints absorb the estimation error the optimizer would otherwise express as extreme positions.

**Worked example.** An unconstrained optimizer wants $w = (1.8, -0.9, 0.1)$ — a 180% long, 90% short bet. Add a long-only constraint ($w_i \ge 0$) and a 60% cap ($w_i \le 0.6$): the optimizer is forced toward something like $(0.6, 0.0, 0.4)$ — still tilted toward asset 1, but tradeable, diversified, and far less exposed to the noise that drove the −90% short. You gave up a sliver of sample-optimality for a huge gain in real-world sanity.

<MCQ
  client:visible
  question="Why does adding a long-only constraint often IMPROVE out-of-sample performance, even though it makes the portfolio less optimal on the historical sample?"
  options={[
    { text: 'It blocks the extreme offsetting long/short positions that estimation noise produces, acting like implicit shrinkage and stabilizing the weights', correct: true },
    { text: 'It increases the expected return of every asset' },
    { text: 'It removes the need to estimate the covariance matrix' },
    { text: 'Long-only portfolios always have higher Sharpe ratios mathematically' },
  ]}
  explanation="The biggest damage from estimation error comes through huge offsetting long/short bets on near-redundant assets. Forbidding shorts physically prevents those positions, so the constraint behaves like shrinkage — it sacrifices in-sample optimality but cuts the noise-driven extremes, usually improving out-of-sample results. Jagannathan and Ma (2003) showed long-only constraints can match Ledoit–Wolf shrinkage."
/>

## Transaction costs and turnover

**The friction.** Every trade costs money — commissions, the bid-ask spread, and **market impact** (your own buying pushes the price up). These costs scale with **turnover**: the total amount you trade to move from your current portfolio to the new target. An optimizer that ignores costs will happily rebalance aggressively every period, chasing noisy re-estimates, and surrender its entire edge to the broker.

**Definition.** **Turnover** over a rebalance is (roughly) the sum of absolute weight changes, $\text{TO} = \sum_i |w_i^{\text{new}} - w_i^{\text{old}}|$. Trading cost is approximately turnover times a per-unit cost $c$ (in basis points). To control it, add a **turnover penalty** to the objective:
$$\max_w\ w^\top\mu - \tfrac{\lambda}{2}w^\top\Sigma w - c\sum_i |w_i - w_i^{\text{old}}|.$$
The penalty creates a **no-trade region**: small signal changes don't justify the cost of trading, so the portfolio only moves when the expected benefit clearly exceeds the cost. This slashes turnover and stabilizes the portfolio over time.

**Worked example.** Your gross signal suggests a rebalance with turnover 200% (you'd trade twice your portfolio value over a year). At a round-trip cost of $c = 30$ bps, that's $2.0 \times 0.0030 = 0.006 = 0.6\%$ of assets lost to costs annually — and that's *modest*. A high-frequency signal with 800% turnover at the same cost bleeds $8.0 \times 0.0030 = 2.4\%$ per year, which can dwarf any alpha the signal generates. The fix: penalize turnover until **net** (after-cost) expected return is maximized, not gross.

<PoTurnoverCost
  client:visible
  title="Trading costs flip the optimal portfolio"
  penaltyLabel="Turnover penalty"
  noPenaltyLabel="No penalty: chase every signal"
  heavyPenaltyLabel="Heavy penalty: barely trade"
  grossLabel="Gross alpha"
  costLabel="Trading cost"
  netLabel="Net alpha"
  turnoverLabel="Turnover"
  caption="A cost-blind optimizer trades constantly, and the costs eat the edge — net alpha can sink below zero. Add a turnover penalty and trading collapses: costs fall faster than the gross edge, so net alpha climbs to a peak. Push the penalty too far and you trade so little the signal goes stale. The best portfolio depends on what trading it costs."
/>

<Callout variant="tip" title="Gross alpha is a fantasy; net alpha pays the bills">
A backtest that ignores transaction costs is fiction. Many 'profitable' strategies — especially fast, high-turnover ones — are net losers once realistic costs and market impact are subtracted. Always optimize and evaluate on NET returns, build a turnover penalty into the objective, and remember that a slightly worse gross portfolio that trades far less can easily win after costs.
</Callout>

<Reveal client:visible>
  <details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
    <summary className="cursor-pointer font-medium">How do constraints, shrinkage, and turnover penalties fit together into one robust optimizer?</summary>

    They're complementary defenses against the same enemy — estimation error — applied at different stages. **Shrinkage** cleans the *inputs*: Ledoit–Wolf stabilizes Σ and shrinking/Black–Litterman tames μ, so the optimizer starts from believable numbers. **Constraints** clean the *outputs*: long-only and box bounds physically forbid the extreme positions noise would otherwise produce, capping the damage any residual input error can do. **Turnover penalties** clean the *dynamics*: they stop the portfolio from whipsawing as estimates jitter period to period, preserving the after-cost edge. A real allocation pipeline uses all three at once: shrink the inputs, run a constrained quadratic program, and include a turnover/transaction-cost term — and often *resample* (average over many bootstrapped optimizations) on top. The output is a portfolio that's modestly tilted, well-diversified, slow-trading, and cost-aware — one that reliably beats 1/N, unlike the naive razor-sharp 'optimum'. The art of portfolio construction is precisely this stacking of humility about your inputs into every layer of the problem.
  </details>
</Reveal>

<MCQ
  client:visible
  question="A strategy has 500% annual turnover and a round-trip trading cost of 25 bps. Roughly how much does it lose to costs each year, and what's the design lesson?"
  options={[
    { text: 'About 1.25% per year (5.0 × 0.0025) — optimize on NET returns and add a turnover penalty so trading only happens when the benefit beats the cost', correct: true },
    { text: 'About 0.05% per year — costs are always negligible' },
    { text: 'About 5% per year — turnover and cost are added, not multiplied' },
    { text: 'Zero — turnover does not affect returns, only risk' },
  ]}
  explanation="Annual cost ≈ turnover × per-unit cost = 5.0 × 0.0025 = 0.0125 = 1.25% of assets, every year. That can swallow a real alpha. The lesson: evaluate and optimize on net (after-cost) returns and add a turnover penalty, which creates a no-trade region so the portfolio rebalances only when the expected gain clearly exceeds the cost."
/>

## Putting it together

**Risk parity** abandons return forecasts and equalizes each asset's **risk contribution** ($\text{RC}_i = w_i(\Sigma w)_i/\sigma_p$, summing to total volatility), correcting the fact that **equal dollars aren't equal risk** — a 60/40 fund is ~90% equity *risk*. The uncorrelated case is **inverse-volatility** weighting; reaching competitive returns usually needs **leverage**, whose Achilles' heel is correlations spiking to 1 in a crisis. **Constraints** — long-only ($w_i \ge 0$, which acts like implicit shrinkage), box bounds, sector limits — make portfolios tradeable *and* defend against estimation error by blocking extreme bets, solved via a quadratic program. **Transaction costs** scale with **turnover**; a turnover penalty creates a no-trade region so you rebalance only when the benefit beats the cost, and you must always optimize on **net**, not gross, returns. Stack shrinkage (clean inputs), constraints (clean outputs), and turnover penalties (clean dynamics) and you have a robust optimizer worth trading.

<MindMap
  client:visible
  title="Risk parity & constraints — the whole picture"
  caption="Balance risk not dollars (risk parity), make it tradeable and robust (constraints), and keep it after costs (turnover penalties) — the practitioner's toolkit."
  root={{
    label: 'Risk parity & constraints',
    children: [
      {
        label: 'Risk contribution',
        children: [
          { label: 'RCᵢ = wᵢ(Σw)ᵢ/σₚ, sums to σₚ' },
          { label: 'Equal dollars ≠ equal risk' },
          { label: '60/40 is ~90% equity risk' },
        ],
      },
      {
        label: 'Risk parity (ERC)',
        children: [
          { label: 'Equalize risk contributions' },
          { label: 'No μ needed — uses Σ only' },
          { label: 'Uncorrelated case: w ∝ 1/σ' },
          { label: 'Needs leverage; fragile if correlations → 1' },
        ],
      },
      {
        label: 'Constraints',
        children: [
          { label: 'Long-only (wᵢ ≥ 0) ≈ implicit shrinkage' },
          { label: 'Box bounds, sector limits' },
          { label: 'Solved via quadratic program' },
          { label: 'Block noise-driven extreme bets' },
        ],
      },
      {
        label: 'Transaction costs',
        children: [
          { label: 'Cost ∝ turnover × per-unit cost' },
          { label: 'Turnover penalty → no-trade region' },
          { label: 'Optimize NET, not gross, returns' },
          { label: 'Stack: shrink + constrain + penalize' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: risk parity & constraints"
  questions={[
    {
      question: 'Two uncorrelated assets have volatilities 10% and 30%. Under risk parity (inverse-volatility), what are the approximate weights?',
      options: [
        { text: '75% / 25% — weights ∝ 1/σ: (1/0.10, 1/0.30) = (10, 3.33), normalized to (0.75, 0.25)', correct: true },
        { text: '50% / 50% — risk parity always equal-weights' },
        { text: '25% / 75% — more weight to the volatile asset' },
        { text: '90% / 10% — proportional to the inverse variance, not volatility' },
      ],
      explanation: 'Inverse-vol weights ∝ 1/σ: 1/0.10 = 10 and 1/0.30 = 3.33, summing to 13.33. Normalized: 10/13.33 ≈ 0.75 and 3.33/13.33 ≈ 0.25. The calm asset gets 75% so that each asset contributes equal risk — overweight the low-vol, underweight the high-vol.',
    },
    {
      question: 'What is an asset’s risk contribution, and what useful property do the contributions have?',
      options: [
        { text: 'RCᵢ = wᵢ(Σw)ᵢ/σₚ — weight times marginal risk — and the contributions sum exactly to total portfolio volatility', correct: true },
        { text: 'It is just the asset’s weight, and the weights sum to 1' },
        { text: 'It is the asset’s expected return, and they sum to the portfolio return' },
        { text: 'It is the asset’s correlation, and they sum to zero' },
      ],
      explanation: 'Risk contribution RCᵢ = wᵢ × MCRᵢ = wᵢ(Σw)ᵢ/σₚ, and Euler’s theorem for the (homogeneous) volatility function guarantees ΣᵢRCᵢ = σₚ. So the fractions RCᵢ/σₚ form a clean risk pie summing to 100% — the basis for equalizing them in risk parity.',
    },
    {
      question: 'Cause → effect. You add a long-only constraint to a previously unconstrained optimizer. Why does out-of-sample performance often improve?',
      options: [
        { text: 'It forbids the extreme offsetting long/short positions that estimation noise creates, acting like implicit shrinkage and stabilizing the weights', correct: true },
        { text: 'It raises every asset’s expected return' },
        { text: 'It lets the optimizer ignore the covariance matrix' },
        { text: 'Shorting is always unprofitable, so removing it must help' },
      ],
      explanation: 'Most estimation-error damage flows through huge long/short bets on near-redundant assets. A long-only constraint physically blocks those, so it behaves like shrinkage — trading in-sample optimality for out-of-sample robustness. Jagannathan–Ma showed it can rival explicit covariance shrinkage.',
    },
    {
      question: 'Spot the trap. A backtest shows a high-turnover strategy earning 9% gross per year. The desk celebrates. Why hold the champagne?',
      options: [
        { text: 'Gross ignores transaction costs; with high turnover, realistic costs and market impact can consume much or all of the 9%, leaving little or negative NET alpha', correct: true },
        { text: 'Gross returns are always lower than net returns, so 9% is conservative' },
        { text: 'Turnover increases returns, so the strategy is even better than it looks' },
        { text: 'A 9% gross return is impossible, so the backtest must be wrong' },
      ],
      explanation: 'Gross returns are a fantasy until costs are subtracted. A high-turnover strategy can pay a large fraction of its edge to commissions, spreads, and market impact — e.g. 600% turnover at 30 bps costs 1.8%/year, and impact can be worse. Always evaluate on NET returns and penalize turnover; many "profitable" gross strategies are net losers.',
    },
  ]}
/>

That completes the toolkit: from the bare optimization problem, through the covariance matrix and the closed-form portfolios, to the estimation error that makes naive optimization an error maximizer, and finally the shrinkage, robust estimators, risk-based allocation, constraints, and cost-awareness that turn the fragile optimizer into one worth trading. Next is the **final exam** — a graded, one-shot run across the whole course to prove the whole structure stuck.
