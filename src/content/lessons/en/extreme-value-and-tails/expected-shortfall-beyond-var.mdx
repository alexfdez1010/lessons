---
title: "Expected Shortfall Beyond VaR"
description: "Why VaR is blind to tail size, Expected Shortfall (CVaR) as the average loss in the tail, the coherence axioms and subadditivity that VaR violates, EVT-based ES, and fully worked numbers."
topic: "en/extreme-value-and-tails"
order: 4
minutes: 13
updated: 2026-06-07
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, MatchConcepts, MindMap, EvEsVsVar } from '@/components/react';

VaR answers "how bad is a bad day?" with a single quantile — the loss you won't exceed 99% of the time. But it has a notorious blind spot you've already glimpsed: it tells you *where* the tail begins and nothing about *how deep it goes*. A \$10M VaR is consistent with a worst-case of \$11M or \$500M; VaR can't tell them apart. This lesson fixes that with **Expected Shortfall** (ES, a.k.a. CVaR) — the *average* loss given you're in the tail — and shows why ES isn't just "a bigger number" but a *better-behaved* one: it satisfies the **coherence** axioms, including **subadditivity** (it respects diversification), which VaR can violate. This is the number Basel moved bank capital onto, and now you'll see exactly why.

<MCQ
  client:visible
  pretest
  question="Two trading books each report a 99% one-day VaR of exactly $10M. Book A's losses beyond VaR are mild; Book B holds a portfolio of sold deep-out-of-the-money options that lose catastrophically in a crash. What does the shared VaR tell you about their relative tail risk?"
  options={[
    { text: 'Almost nothing — VaR is a single quantile and is blind to how deep the tail runs beyond it; B can be far more dangerous despite identical VaR', correct: true },
    { text: 'They have identical risk, since equal VaR means equal risk' },
    { text: 'Book A is riskier, because mild beyond-VaR losses are deceptive' },
    { text: 'VaR fully captures B\'s option risk, so no extra analysis is needed' },
  ]}
  explanation="VaR is the threshold where the worst 1% begins — it says nothing about the severity past it. Book B's sold options have a benign body but a catastrophic tail; its VaR can match A's while its expected shortfall is many times larger. This is VaR's defining blind spot, and exactly why Expected Shortfall exists."
/>

## VaR's blind spot: a line, not a depth

**Analogy.** VaR is a flood gauge that tells you the water will reach the second step 99 days out of 100 — but says nothing about whether the hundredth day brings ankle-deep water or a wall that swallows the house. It marks the threshold of disaster, then goes silent on its magnitude.

**Definition recap.** The **Value-at-Risk** at confidence $c$ is the loss quantile: $\text{VaR}_c = $ the smallest loss $\ell$ such that $P(\text{Loss} \le \ell) = c$. At $c = 99\%$ it's the loss you exceed only 1% of the time. By construction, it is *the boundary* of the tail — and it reports a single point on the distribution, full stop.

**Why that's dangerous.** Everything *beyond* VaR — the entire shape and depth of the tail — is invisible to it. Two portfolios can share a VaR while one has a gentle tail and the other a cliff. Worse, VaR's blindness *invites* manipulation: a desk can sell far-out-of-the-money options or take on hidden catastrophe exposure that leaves the 99th percentile untouched while loading the 99.9th percentile with ruin. VaR will bless the position. This isn't hypothetical — it's the structural reason a VaR-only risk regime can quietly accumulate tail bombs.

<MCQ
  client:visible
  question="Why can a trader 'game' a VaR limit by selling deep out-of-the-money options?"
  options={[
    { text: 'Such options rarely pay out, so they barely touch the 99% quantile (VaR), yet they create catastrophic losses in the far tail that VaR never sees', correct: true },
    { text: 'Selling options always reduces every risk measure simultaneously' },
    { text: 'Options have no effect on the loss distribution at all' },
    { text: 'It increases VaR so visibly that risk managers immediately catch it' },
  ]}
  explanation="Deep OTM options almost never trigger, so they leave the 99th-percentile loss (VaR) roughly unchanged while collecting premium. But in the rare crash they blow up, piling loss into the region BEYOND VaR — exactly where VaR is blind. ES, which averages that region, would flag the position instantly."
/>

## Expected Shortfall: averaging the disaster

**Analogy.** If VaR tells you the flood will reach the second step, ES tells you the *average depth of water on the days it does flood*. It stops marking the threshold and starts measuring the catastrophe itself.

**Definition.** The **Expected Shortfall** at confidence $c$ is the *average loss conditional on being in the tail beyond VaR*:
$$\text{ES}_c = E[\,\text{Loss} \mid \text{Loss} \ge \text{VaR}_c\,].$$
Equivalently, it's the average of all the VaRs at higher confidence levels:
$$\text{ES}_c = \frac{1}{1-c}\int_c^1 \text{VaR}_u\, du.$$
That second form is the key intuition: ES doesn't read one point off the tail — it *integrates over the whole tail*, weighting in every worse-than-VaR outcome. So ES is **always at least as large as VaR** (and strictly larger whenever the tail has any depth), because an average of things all $\ge$ VaR can't be below VaR.

**Names.** ES is also called **Conditional VaR (CVaR)**, **Tail VaR (TVaR)**, or **expected tail loss**. They're the same object for continuous loss distributions.

<EvEsVsVar
  client:visible
  title="VaR draws a line; ES averages what lies beyond it"
  confLabel="Confidence level"
  varLabel="VaR (the cut-off)"
  esLabel="ES (tail average)"
  varReadout="VaR (σ units)"
  esReadout="ES (σ units)"
  caption="VaR is the line where the shaded tail begins; ES is the average of that whole shaded region, so it always sits further out. Slide the confidence level and watch the gap. Two books can share a VaR yet have wildly different ES — and that gap is exactly the tail risk VaR refuses to measure."
/>

**Worked example — normal case.** For a standard normal loss, the closed forms are $\text{VaR}_c = z_c$ and $\text{ES}_c = \phi(z_c)/(1-c)$, where $\phi$ is the normal density and $z_c$ the quantile. At $c = 97.5\%$: $z_{0.975} = 1.96$, $\phi(1.96) \approx 0.0584$, so
$$\text{ES}_{0.975} = \frac{0.0584}{0.025} \approx 2.34.$$
So a 97.5% ES of 2.34σ is roughly the same magnitude as a 99% VaR of 2.33σ. **This is not a coincidence** — Basel III deliberately swapped the old 99% VaR for a 97.5% ES precisely because, under normality, they line up, so the switch isn't a stealth capital hike but a *shape-aware* upgrade: identical for thin tails, automatically more conservative for fat ones.

<FillBlank
  client:visible
  question="The relationship between VaR and ES."
  text="Expected Shortfall is the {{average|maximum|minimum}} loss given the loss exceeds VaR, so ES is always {{greater than or equal to|less than|unrelated to}} VaR. Unlike VaR, ES is sensitive to the {{depth and shape of the tail|center of the distribution|number of assets}} beyond the threshold."
  explanation="ES = E[Loss | Loss ≥ VaR] averages the entire tail, so it's always ≥ VaR. Crucially, ES depends on how DEEP the tail runs — the very information VaR discards. That's why ES distinguishes a mild tail from a catastrophic one when their VaRs match."
/>

## Coherence: why ES is the "right" kind of risk measure

There's a deeper reason to prefer ES than "it sees more of the tail." A risk measure should obey a few common-sense axioms — and ES does while VaR famously fails one.

**Definition — a coherent risk measure** (Artzner, Delbaen, Eber, Heath) satisfies four axioms:

1. **Monotonicity:** if portfolio A always loses at least as much as B, then $\rho(A) \ge \rho(B)$. (Worse outcomes → more risk.)
2. **Translation invariance:** adding \$$k$ of cash reduces the risk by exactly \$$k$. (Cash is a buffer.)
3. **Positive homogeneity:** doubling every position doubles the risk: $\rho(\lambda A) = \lambda\rho(A)$. (Scale linearly.)
4. **Subadditivity:** $\rho(A + B) \le \rho(A) + \rho(B)$. **Diversification can only help** — the risk of the combined book never exceeds the sum of the parts.

ES satisfies all four. **VaR can violate subadditivity** — and that's the deal-breaker.

**Why subadditivity matters.** Subadditivity is the mathematical statement of "diversification reduces risk." If a risk measure can *increase* when you combine two books — punishing diversification — it's pathological: it could push a firm to *split* a diversified portfolio to lower its reported risk, the opposite of prudence. A risk measure that doesn't respect diversification can't be used to aggregate risk across desks, which is the whole job of a firm-wide measure.

<Callout variant="info" title="VaR is coherent for nice distributions — but you can't count on nice">
For elliptical distributions (the normal, and the t with the same shape), VaR actually IS subadditive — which is why it survived so long. The violations appear with skewed, discrete, or heavy-tailed payoffs: credit portfolios, sold options, defaultable bonds. Precisely the tail-risky books where you most need a trustworthy measure are where VaR's subadditivity breaks. ES never has this problem.
</Callout>

## A worked subadditivity violation

Let's see VaR fail with the cleanest possible example — two independent defaultable bonds.

**Setup.** Each bond pays a small coupon but has a 4% chance of a large default loss. Specifically, each bond independently loses \$100 with probability 4% and gains \$2 with probability 96%. Consider 95% VaR (so we care about the worst 5%).

**Each bond alone.** The probability of the \$100 loss is 4%, which is *less than* the 5% tail. So the 95% quantile of each single bond's loss falls in the *no-default* region: the 95% VaR of one bond is just $-2$ dollars (a \$2 *gain*, i.e. negative loss). VaR says each bond alone is essentially riskless at 95%.

**The portfolio of both.** Now hold both. The chance that *at least one* defaults is $1 - 0.96^2 = 1 - 0.9216 = 0.0784$, i.e. **7.84%** — now *above* the 5% tail. So the 95% quantile lands in the default region: with one default you lose $100 - 2 = 98$ dollars. The 95% VaR of the combined book is about **\$98**.

**The violation.** Sum of individual VaRs: $(-2) + (-2) = -4$ dollars (a gain). Combined VaR: $+98$ dollars (a loss). So
$$\text{VaR}(A+B) = 98 \;>\; \text{VaR}(A) + \text{VaR}(B) = -4.$$
Diversifying made VaR *explode*. That's the subadditivity violation — VaR punished a perfectly sensible diversification. **ES would not do this:** because it averages over the whole tail (including both-default scenarios), it correctly registers each bond's default risk even when it sits just outside the 5% cut, and the combined ES stays below the sum. This single example is why theorists distrust VaR and regulators migrated to ES.

<MCQ
  client:visible
  question="In the two-bond example, why does 95% VaR judge each bond alone as nearly riskless but the combined book as very risky?"
  options={[
    { text: 'Each default (4%) sits just outside the 5% tail, so VaR ignores it; combining two bonds raises the chance of at least one default to 7.84%, pushing a default into the 5% tail', correct: true },
    { text: 'Because combining bonds always creates new risk out of nothing' },
    { text: 'Because ES and VaR are identical, so the example proves nothing' },
    { text: 'Because the bonds are perfectly correlated' },
  ]}
  explanation="VaR only 'sees' an event once it crosses into the tail. A single bond's 4% default is below the 5% cut, so its 95% VaR ignores it. Two independent bonds make 'at least one default' 7.84% — now inside the tail — so the combined VaR jumps. This non-monotone, diversification-punishing behavior is the subadditivity violation; ES, averaging the whole tail, avoids it."
/>

<MatchConcepts
  client:visible
  question="Match each coherence axiom to its plain-English meaning."
  pairs={[
    { term: 'Monotonicity', definition: 'A portfolio that always loses more is judged at least as risky' },
    { term: 'Translation invariance', definition: 'Adding cash reduces measured risk by exactly that amount' },
    { term: 'Positive homogeneity', definition: 'Scaling all positions by λ scales the risk by λ' },
    { term: 'Subadditivity', definition: 'The risk of a combined book never exceeds the sum of the parts — diversification helps' },
    { term: 'VaR\'s failure', definition: 'It can violate subadditivity for skewed/heavy-tailed books, punishing diversification' },
  ]}
  explanation="The four coherence axioms are monotonicity, translation invariance, positive homogeneity, and subadditivity. ES satisfies all four; VaR can break subadditivity (diversification should never increase risk, yet VaR sometimes says it does) — the central theoretical case for ES."
/>

## EVT-based Expected Shortfall

ES truly shines when fused with the GPD tail from the last lesson, because then you can compute ES *far past your data*.

**The neat GPD result.** If the tail above the threshold is GPD with shape $\xi$ (and $\xi < 1$, so the mean exists), then for a confidence level in the tail,
$$\frac{\text{ES}_c}{\text{VaR}_c} = \frac{1}{1-\xi} + \frac{\beta - \xi u}{(1-\xi)\,\text{VaR}_c}.$$
The dominant term is the clean ratio $\dfrac{1}{1-\xi}$: for a pure power-law tail, ES is just VaR scaled up by $1/(1-\xi)$. The heavier the tail (bigger $\xi$), the bigger the multiplier — exactly the right behavior.

**Worked example.** Take the EVT fit from last lesson: $\xi = 0.25$, and a 99.9% VaR we computed at about 7.3% (a \$7.3M loss on \$100M). The leading-order ES multiplier is
$$\frac{1}{1-\xi} = \frac{1}{1-0.25} = \frac{1}{0.75} \approx 1.33.$$
So $\text{ES}_{0.999} \approx 1.33 \times 7.3\% \approx 9.7\%$, i.e. about a **\$9.7M average loss** *given* you're in that worst-0.1% region. Notice the gap: VaR said \$7.3M, but the typical loss when you breach is \$9.7M — a third worse. If $\xi$ were a scarier 0.5, the multiplier would be $1/0.5 = 2$, doubling VaR to \$14.6M. The tail index directly controls how much worse ES is than VaR — a one-number summary of "how bad is bad, on average."

<Callout variant="warning" title="If ξ ≥ 1, Expected Shortfall is INFINITE">
The GPD mean — and therefore ES — only exists when ξ < 1 (equivalently tail index α > 1). For an ultra-heavy tail with ξ ≥ 1, the expected shortfall is literally infinite: the average tail loss does not converge. This isn't a glitch; it's the model honestly reporting that the catastrophe distribution has no finite average. Such regimes (some operational and catastrophe losses) need a different framing entirely — e.g. capping exposure rather than pricing an undefined average.
</Callout>

<Reveal client:visible>
  <details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
    <summary className="cursor-pointer font-medium">If ES is theoretically superior, why did VaR dominate for so long — and does ES have downsides?</summary>

    VaR won early for practical reasons: it's a single intuitive number ("we won't lose more than X, 99% of the time"), it's easy to *backtest* (just count breaches — did losses exceed VaR more than 1% of days?), and it predates the coherence framework. ES's drawbacks are real but mostly fixable. First, **backtesting ES is harder**: it's not "elicitable" in the simple way VaR is, so you can't validate it just by counting exceedances — you need conditional tests on the size of breaches, which the field has now developed. Second, **ES needs more data / more tail modeling** to estimate, since it depends on the whole tail shape, not one quantile — which is exactly why pairing it with EVT/GPD matters. Third, ES at level c and VaR at a higher level can give similar numbers, so the *practical* improvement is sometimes modest for thin-tailed books. Basel III's resolution was pragmatic: use 97.5% ES (calibrated to align with 99% VaR for normal cases) so the regime captures tail depth without an arbitrary capital jump, while keeping VaR around for backtesting. The verdict: ES is the better *risk measure*, VaR remains a useful *diagnostic*, and the modern stack uses both.
  </details>
</Reveal>

<MindMap
  client:visible
  title="Expected Shortfall beyond VaR — the whole picture"
  caption="VaR is a single quantile blind to tail depth; ES averages the whole tail, satisfies the coherence axioms VaR violates (subadditivity), and pairs with GPD for an EVT-based ES scaled by 1/(1−ξ)."
  root={{
    label: 'Expected Shortfall beyond VaR',
    children: [
      {
        label: "VaR's blind spot",
        children: [
          { label: 'A single quantile — the tail boundary' },
          { label: 'Says nothing about depth beyond it' },
          { label: 'Gameable with deep OTM options' },
        ],
      },
      {
        label: 'Expected Shortfall',
        children: [
          { label: 'ES = E[Loss | Loss ≥ VaR]' },
          { label: 'Average of the whole tail; always ≥ VaR' },
          { label: 'Also called CVaR / TVaR / expected tail loss' },
        ],
      },
      {
        label: 'Coherence',
        children: [
          { label: 'Monotonicity, translation, homogeneity, subadditivity' },
          { label: 'ES satisfies all four' },
          { label: 'VaR can violate subadditivity (punishes diversification)' },
        ],
      },
      {
        label: 'EVT-based ES',
        children: [
          { label: 'GPD tail → ES/VaR ≈ 1/(1−ξ)' },
          { label: 'Heavier tail (bigger ξ) → bigger multiplier' },
          { label: 'ξ ≥ 1 → ES infinite' },
        ],
      },
      {
        label: 'Practice',
        children: [
          { label: 'Basel III: 97.5% ES replaced 99% VaR' },
          { label: 'Aligned with normal VaR, conservative for fat tails' },
          { label: 'Harder to backtest; VaR kept as diagnostic' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: Expected Shortfall beyond VaR"
  questions={[
    {
      question: 'What does Expected Shortfall measure that VaR does not?',
      options: [
        { text: 'The average loss GIVEN you are in the tail — the depth of the tail beyond VaR, not just where it begins', correct: true },
        { text: 'The probability of any loss occurring' },
        { text: 'The most likely everyday return' },
        { text: 'The standard deviation of the whole distribution' },
      ],
      explanation: 'ES = E[Loss | Loss ≥ VaR] averages the entire region beyond VaR, capturing how deep the tail runs. VaR reports only the threshold; ES reports the typical severity once that threshold is breached.',
    },
    {
      question: 'Which coherence axiom does VaR fail, and why does it matter?',
      options: [
        { text: 'Subadditivity — VaR can rise when two books are combined, perversely punishing diversification, which a firm-wide risk measure must never do', correct: true },
        { text: 'Monotonicity — VaR ignores which portfolio loses more' },
        { text: 'Translation invariance — VaR mishandles cash' },
        { text: 'VaR fails all four axioms simultaneously' },
      ],
      explanation: 'VaR can violate subadditivity: VaR(A+B) can exceed VaR(A)+VaR(B), implying diversification increased risk. That is pathological for aggregating risk across desks. ES is subadditive (and coherent overall), which is the central theoretical reason to prefer it.',
    },
    {
      question: 'An EVT fit gives a tail shape ξ = 0.5. Using the leading-order GPD result, roughly how does ES compare to VaR in the deep tail?',
      options: [
        { text: 'About twice VaR, since the multiplier 1/(1−ξ) = 1/0.5 = 2', correct: true },
        { text: 'About equal to VaR, since ξ does not affect ES' },
        { text: 'About half of VaR' },
        { text: 'ES is infinite at ξ = 0.5' },
      ],
      explanation: 'For a GPD tail the leading ES/VaR ratio is 1/(1−ξ). At ξ = 0.5 that is 1/0.5 = 2, so ES ≈ 2 × VaR. The heavier the tail (larger ξ), the bigger the gap. ES becomes infinite only at ξ ≥ 1.',
    },
    {
      question: 'Spot the trap: "We replaced 99% VaR with 97.5% ES, so management secretly doubled our capital requirement." What\'s the nuance?',
      options: [
        { text: '97.5% ES was deliberately calibrated to roughly match 99% VaR under normality, so for thin-tailed books capital is similar — ES only bites harder when tails are genuinely fat', correct: true },
        { text: 'Correct — ES is always exactly double VaR' },
        { text: 'ES is always smaller than VaR, so capital actually fell' },
        { text: 'The two measures are unrelated and cannot be compared' },
      ],
      explanation: 'Basel III chose 97.5% ES because it aligns with 99% VaR for normal distributions, so the switch is not a blanket capital hike. It captures tail DEPTH, so it raises capital specifically for fat-tailed, tail-risky books — a shape-aware upgrade, not an arbitrary doubling.',
    },
  ]}
/>

Next up — **copulas and tail dependence** — we leave single-asset tails and confront how assets crash *together*. Correlation, it turns out, is a fair-weather friend: assets can look mildly linked day-to-day yet plunge in lockstep during a crisis. Copulas separate "how each asset behaves" from "how they move together," and tail dependence captures the "everything goes to one" clustering that destroys diversification exactly when you need it.
