---
title: Estimating Factor Exposures
description: The econometrics of factor models — time-series regression for one asset's loadings, alpha and R², the difference between R² and average return, cross-sectional regression for whether a factor is actually priced, and the Fama–MacBeth two-pass procedure with a worked t-stat.
topic: "en/factor-models"
order: 4
minutes: 9
updated: 2026-06-06
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, Categorize, MatchConcepts, MindMap, BetaScatter, CrossSectionScatter } from '@/components/react';

So far you've met the *cast* of factors — market, size, value, momentum, profitability, investment — and the idea that a factor is a portfolio whose return is a reward for bearing some shared risk. But naming factors is the easy part. The hard, money-deciding part is **measurement**: given an actual fund's monthly returns, *how much* of each factor does it carry? Is its leftover return real skill or just statistical noise? And do these factors even earn a premium, or did someone backtest themselves into a mirage?

That's all econometrics, and it splits cleanly into **two regressions that ask two different questions**. A **time-series regression** looks at *one asset over time* and asks "what are *this* asset's exposures, and is its alpha real?" A **cross-sectional regression** looks at *many assets at one moment* and asks "across the whole market, does loading on this factor actually pay?" Confuse them and nothing in this field makes sense; keep them straight and the famous **Fama–MacBeth** procedure falls out naturally. Let's measure.

<MCQ
  client:visible
  pretest
  question="A mutual fund's monthly returns are regressed on the market, size and value factors. The regression's R² comes back at 0.99. What does that high R² tell you about the fund?"
  options={[
    { text: 'Almost all of its return movement is explained by its factor tilts — it behaves like a leveraged blend of those factors, which says nothing good about its alpha', correct: true },
    { text: 'The fund has enormous alpha — 99% of its return is manager skill' },
    { text: 'The fund beat the market 99% of the time' },
    { text: 'The fund is a great investment, since high R² means high returns' },
  ]}
  explanation="R² is the fraction of return VARIANCE the factors explain, not the fraction that is skill. R² = 0.99 means the fund moves almost entirely with its factor exposures — it is essentially a factor portfolio in disguise (a closet index fund). High R² is the OPPOSITE of alpha, not evidence of it. R² and average return are different things entirely."
/>

## Time-series regression: one asset's exposures

**Analogy.** Picture a single sailboat over a season. Some days it's pushed by the wind (the market), some days by the tide (the value factor), some days by a current (size). A time-series regression watches *that one boat* across many days and asks: how strongly does it respond to each force, and is there any leftover motion the known forces can't explain?

**Definition.** For a single asset $i$, you regress its **excess return** (return minus the risk-free rate, $R_i - R_f$) on the contemporaneous factor returns:

$$R_i - R_f = \alpha_i + \sum_k \beta_{i,k} F_k + \varepsilon_i.$$

Each $\beta_{i,k}$ is a **loading** (also called a factor exposure): the slope on factor $k$, telling you how many units of that factor the asset carries. The intercept $\alpha_i$ is the **model alpha** — the average return left over *after* every factor has been paid its due. The residual $\varepsilon_i$ is the wiggle no factor explains.

**Reading the picture.** With a single factor this is just a scatter of the asset's excess return (vertical) against the factor's return (horizontal), with a fitted line through it. The **slope is the loading** and the **intercept is alpha** — exactly the geometry below. Toggle the presets to watch a defensive (low-loading) asset's line lie flat, a market-tracking asset sit at slope one, and an aggressive asset swing hard; flip the alpha control to lift the whole line by adding skill on top.

<BetaScatter
  client:visible
  title="A time-series regression for one asset"
  marketAxisLabel="Factor return"
  portfolioAxisLabel="Asset excess return"
  betaLabel="Loading (slope)"
  alphaLabel="Alpha (intercept)"
  defensiveLabel="Low loading"
  marketNeutralLabel="Loading = 1"
  aggressiveLabel="High loading"
  caption="Each dot is one period: the factor's return across, the asset's excess return up. The fitted slope is the loading (factor exposure); the intercept is alpha — the average return left over once the factor is accounted for. Steeper line = more exposure; lifting the line = real skill on top."
/>

**What R² measures here.** Alongside the loadings and alpha, the regression spits out an $R^2$ — the **fraction of the asset's return variance the factors explain**. A broad, diversified equity portfolio is almost nothing *but* factor exposure, so its $R^2$ on a good multi-factor model is routinely **0.90 or higher**. A single stock, dragged around by company-specific news, has far more idiosyncratic wiggle, so its $R^2$ is much lower — often 0.3 to 0.5. Diversification literally *raises* $R^2$ by averaging away the idiosyncratic part.

**Is the alpha real?** A point estimate of $\alpha_i = 2\%$ per year is meaningless without a **t-statistic** — the alpha divided by its standard error. As a rough bar, you want $|t| \gtrsim 2$ before treating an alpha as distinguishable from luck. And the standard errors can't be naive: monthly returns are **heteroskedastic** (volatility clusters) and **autocorrelated** (this month relates to last), which deflates ordinary standard errors and makes junk alphas look significant. Practitioners use **Newey–West** standard errors, which inflate the error bars to account for that structure, so the t-stat you trust is the honest, larger-error one.

<Callout variant="info" title="Loadings and alpha, in one breath">
A time-series regression of one asset's excess return on the factors gives you, per asset: the **loadings** (slopes = how much of each factor it carries), the **alpha** (intercept = average return no factor explains), and **R²** (how much of its variance the factors account for). Loadings and alpha are *return* quantities; R² is a *variance* quantity. Keep that distinction — the next section is built on it.
</Callout>

<FillBlank
  client:visible
  question="Fill in the anatomy of a time-series factor regression."
  text="Regressing one asset's {{excess return|nominal price|trading volume}} on the factor returns, the slope on each factor is its {{loading|residual|premium}}, the intercept is the model {{alpha|beta|variance}}, and the fraction of the asset's variance the factors explain is the {{R-squared|t-statistic|standard error}}. Because returns cluster and autocorrelate, the honest standard errors are the {{Newey–West|ordinary least squares|equal-weighted}} ones."
  explanation="You regress excess return (return minus the risk-free rate). Slopes are loadings, the intercept is alpha, R² is the variance share explained, and Newey–West standard errors correct for heteroskedasticity and autocorrelation so a noisy alpha can't masquerade as significant."
/>

## What R² does and doesn't tell you

Here is the single most expensive confusion in factor analysis, and it deserves its own section: **R² measures variance explained, not average return earned**. The two are *orthogonal* — almost completely unrelated. Knowing a fund's $R^2$ tells you essentially nothing about whether it makes money beyond its factors.

**The vivid case.** Imagine a fund that quietly holds the exact same basket as the value factor. Its returns track that factor tick for tick, so its time-series $R^2$ is **0.99** — gorgeous fit. But because it *is* the factor, the regression's intercept is $\alpha = 0$: there's no return left over once you account for the factor it's secretly mimicking. That is a **closet index fund** — high R², zero alpha, charging active fees for a passive tilt. The high R² isn't evidence of skill; it's evidence the fund has *no* independent skill, just exposure.

**The opposite case.** Now imagine a genuinely skilled, eclectic strategy whose bets don't line up neatly with any known factor. Its $R^2$ might be a modest **0.40** — the factors explain less than half its variance — yet its **alpha is large and significant**. Low R², real alpha. The alpha lives in the *unexplained* part, which is exactly the part R² is small *because of*.

So flip your intuition: **high R² is the opposite of alpha, not a sign of it.** A high R² says "this asset is well described by the factors," which means "there's little room left for skill." Alpha is what survives in the residual; R² rising means the residual shrinking.

<MCQ
  client:visible
  question="Two funds are each regressed on the same factor model. Fund A has R² = 0.97 and α = 0.0% (not significant). Fund B has R² = 0.45 and α = +3.5% per year (t = 2.6). Which statement is correct?"
  options={[
    { text: 'Fund B shows real, factor-adjusted skill; Fund A is essentially a factor portfolio in disguise with no skill', correct: true },
    { text: 'Fund A is the better fund — its higher R² means it explains more return' },
    { text: 'Fund A has more skill because 97% beats 45%' },
    { text: 'Neither can have skill, because alpha requires a high R²' },
  ]}
  explanation="R² is variance explained, not skill. Fund A's 0.97 R² with zero alpha is a closet index fund — all exposure, no edge. Fund B's lower R² with a large, significant alpha (t = 2.6 > 2) is the one with real factor-adjusted skill, sitting in the part of its return the factors can't explain. High R² is the opposite of alpha."
/>

## Cross-sectional regression: are loadings priced?

The time-series regression answered "what does *this one* asset carry, and is *its* alpha real?" It cannot answer a different, equally vital question: **across the whole universe of assets, does loading more heavily on a factor actually earn you more return?** A factor only deserves the name if its premium is positive — if the assets that load on it are *paid* for it. That's a statement about the **cross-section**, so it needs a cross-sectional regression.

**Analogy.** Forget the single boat over a season. Line up *every* boat in the marina on one afternoon and plot, for each, its sail area against its average speed. If bigger sails systematically go faster, "sail area" is priced; the *slope* of that cloud tells you how many extra knots one extra unit of sail buys. That slope is a **premium**.

**Definition.** Using the loadings $\hat\beta_{i,k}$ already estimated from the first-pass time-series regressions, you regress assets' **average excess returns** on those loadings — *across assets*:

$$\bar R_i = \lambda_0 + \sum_k \lambda_k \hat\beta_{i,k} + u_i.$$

Now the unit of observation is an **asset** (or a test portfolio), not a date. The slopes $\lambda_k$ estimate the **factor risk premia** — the extra average return earned per unit of loading on factor $k$. The intercept $\lambda_0$ is the **pricing error**: the average return common to all assets that *isn't* explained by any loading (it should be near zero if the model prices the cross-section well).

This is a genuinely different regression from the time-series one, even though both involve "betas." Time-series: *one asset, many dates, asks for exposure and alpha.* Cross-section: *many assets, the loadings as the X variable, asks whether those loadings are rewarded.*

**Reading the picture.** Each dot below is one test portfolio: its factor loading runs across, its average realised excess return runs up. The best-fit line's **slope is the estimated premium** (how much average return one extra unit of loading earns) and its **intercept is the pricing error**. A priced factor tilts the line clearly upward — high-loading portfolios genuinely earn more.

<CrossSectionScatter
  client:visible
  title="Pricing a factor across portfolios"
  xLabel="Factor loading"
  yLabel="Average excess return (% / yr)"
  slopeLabel="Estimated premium (slope)"
  interceptLabel="Pricing error (intercept)"
  points={[
    { label: 'P1', x: 0.2, y: 1.4 },
    { label: 'P2', x: 0.5, y: 3.6 },
    { label: 'P3', x: 0.7, y: 3.0 },
    { label: 'P4', x: 0.9, y: 5.3 },
    { label: 'P5', x: 1.1, y: 6.4 },
    { label: 'P6', x: 1.3, y: 5.9 },
    { label: 'P7', x: 1.5, y: 8.1 },
    { label: 'P8', x: 1.6, y: 8.6 },
  ]}
  caption="Each dot is a portfolio: factor loading across, average excess return up. The upward slope is the estimated factor premium — higher loading is rewarded with higher average return. The intercept is the pricing error (return unexplained by loading). Two points sit off the line: that scatter is the regression's noise."
/>

<MatchConcepts
  client:visible
  question="Match each regression ingredient to which of the two passes it belongs to and what it means."
  pairs={[
    { term: 'Time-series slope on a factor', definition: 'One asset\'s loading — how much of that factor it carries' },
    { term: 'Time-series intercept', definition: 'The asset\'s alpha — average return no factor explains' },
    { term: 'Time-series R²', definition: 'Fraction of that asset\'s return variance the factors explain' },
    { term: 'Cross-sectional slope on a loading', definition: 'The factor risk premium — return earned per unit of loading' },
    { term: 'Cross-sectional intercept', definition: 'The pricing error — average return unexplained by any loading' },
  ]}
  explanation="The time-series pass (one asset over time) yields loadings, alpha and R². The cross-sectional pass (many assets at a moment) regresses average returns on those loadings: its slopes are the premia and its intercept is the pricing error. Same word 'beta' appears in both, but as an output of the first and an input to the second."
/>

## Fama–MacBeth in two passes

In 1973 Eugene Fama and James MacBeth turned the two regressions into a clean, two-pass recipe that's still the workhorse for estimating factor premia and their significance. The trick solves a thorny statistical problem — assets' returns are correlated *with each other* at any moment, which wrecks naive cross-sectional standard errors — and it does so almost elegantly.

**Pass 1 — time series (estimate the loadings).** For each asset, run the time-series regression from the first section to estimate its loadings $\hat\beta_{i,k}$. After this pass, every asset has a fixed set of factor exposures.

**Pass 2 — cross section, *one regression per period*.** Here's the clever bit. For **each month** $t$ separately, run a cross-sectional regression of *that month's* returns across assets on the loadings:

$$R_{i,t} = \lambda_{0,t} + \sum_k \lambda_{k,t}\,\hat\beta_{i,k} + u_{i,t}.$$

That gives you not one premium estimate but a whole **time series** of monthly premium estimates $\hat\lambda_{k,t}$ — one number per factor per month.

**Combine.** The estimated premium is simply the **time-series average** of those monthly slopes,

$$\bar\lambda_k = \frac{1}{T}\sum_{t=1}^{T} \hat\lambda_{k,t},$$

and — this is the elegant part — its **t-statistic comes from the standard error of that time series of monthly estimates**. By treating each month's cross-sectional slope as one observation and averaging over months, cross-sectional correlation *within* a month is automatically absorbed: you never have to model it, because you only ever average *across* independent-ish months.

**Worked example — testing a value (HML) premium.** Suppose Pass 2 gives just four monthly HML slopes (a real study uses hundreds; four keeps the arithmetic visible):

| Month | Monthly HML slope $\hat\lambda_t$ | Deviation from mean | Squared deviation |
|---|---|---|---|
| 1 | +0.4% | +0.2% | 0.04 |
| 2 | −0.2% | −0.4% | 0.16 |
| 3 | +0.6% | +0.4% | 0.16 |
| 4 | 0.0% | −0.2% | 0.04 |
| **Avg** | **+0.2%** | — | **sum = 0.40** |

Walk the arithmetic (working in units of percent):

- **Mean premium:** $\bar\lambda = (0.4 - 0.2 + 0.6 + 0.0)/4 = 0.8/4 = 0.2\%$ per month.
- **Sample variance:** divide the sum of squared deviations by $T - 1$: $0.40 / (4 - 1) = 0.1333$.
- **Standard deviation:** $\sqrt{0.1333} = 0.365\%$ per month.
- **Standard error of the mean:** $\mathrm{SE} = 0.365 / \sqrt{4} = 0.365 / 2 = 0.183\%$.
- **t-statistic:** $t = \bar\lambda / \mathrm{SE} = 0.2 / 0.183 \approx 1.1$.

A t-stat of about **1.1 is well below the rough bar of 2**, so on this (deliberately tiny) sample the value premium is **not statistically significant** — the +0.2%/month average is swamped by month-to-month noise. The premium *might* be real, but four months can't prove it. Run the same procedure over 600 months and the $\sqrt T$ in the denominator shrinks the standard error dramatically, which is exactly why the canonical Fama–French studies use decades of data.

<Callout variant="info" title="Why divide by √T">
The standard error of an average is the data's standard deviation divided by $\sqrt{T}$. That $\sqrt{T}$ is why **more months are the cheapest way to buy significance**: a real premium with steady month-to-month noise will eventually clear the t = 2 bar simply because the denominator keeps shrinking. A premium that *can't* clear the bar even with hundreds of months is genuinely indistinguishable from noise.
</Callout>

<MCQ
  client:visible
  question="In the Fama–MacBeth procedure, where does the t-statistic for a factor premium come from?"
  options={[
    { text: 'From the standard error of the time series of monthly cross-sectional slope estimates', correct: true },
    { text: 'From the R² of the first-pass time-series regressions' },
    { text: 'From a single cross-sectional regression run on the full-period average returns' },
    { text: 'From the standard error of one asset\'s alpha' },
  ]}
  explanation="Fama–MacBeth runs a cross-sectional regression each month, producing a time series of premium estimates. The premium is their average and its t-stat is that average divided by the standard error of the time series. Averaging across months absorbs the within-month cross-sectional correlation that would otherwise corrupt the standard errors."
/>

## Reading the t-stats honestly

The whole machine exists to separate **signal from noise**, and the t-statistic is the verdict. A factor premium isn't "real" because its average is positive — it's credible only when that average is **large relative to its month-to-month wobble**. A premium of +0.2%/month sounds like a clean win until you notice the months range from −0.2% to +0.6%; that spread is the noise the t-stat measures against.

**Why short windows mislead.** With a small $T$, the $\sqrt{T}$ in the standard error is small, so the error bars are wide and even a genuine premium can fail the t = 2 bar (as our four-month example did). The reverse trap is just as dangerous: data-mine enough candidate factors over a short window and *some* will clear t = 2 by pure luck. This is why the field now demands **higher hurdles** (some argue t &gt; 3) for *newly discovered* factors — you're implicitly testing hundreds of them, so the bar for "not luck" must rise.

**The misconception to bury.** Tying the whole lesson together: a **high R² does *not* mean you found alpha or a good investment.** R² says the factors explain the asset's *variance*; it is silent on average return, and a perfect-fit closet index fund has R² near 1 and alpha of zero. Skill lives in a *significant alpha* (time series) or a *significant, positive premium* (cross section) — never in R². Measure the right thing, demand a real t-stat, and don't let a pretty fit fool you.

<Categorize
  client:visible
  question="Sort each statement: a sign of genuine, measurable edge — or a statistical mirage / misread?"
  buckets={['Genuine measurable edge', 'Mirage or misread']}
  items={[
    { text: 'A time-series alpha of +3% with a Newey–West t-stat of 2.8', bucket: 'Genuine measurable edge' },
    { text: 'A cross-sectional premium that stays positive and significant over 50 years', bucket: 'Genuine measurable edge' },
    { text: 'An R² of 0.98, taken as proof the fund has skill', bucket: 'Mirage or misread' },
    { text: 'A factor that clears t = 2 once in a four-month window', bucket: 'Mirage or misread' },
    { text: 'A +0.2%/month average premium with a t-stat of 1.1', bucket: 'Mirage or misread' },
    { text: 'One of two hundred mined factors that happens to look significant', bucket: 'Mirage or misread' },
  ]}
  explanation="Edge shows up as a significant, robust alpha or premium — large relative to its (properly corrected) standard error and stable over long samples. Mirages are high R² mistaken for skill, fragile significance from tiny windows, premia that can't clear the t-bar, and data-mined winners that ignore the multiple-testing penalty."
/>

<Reveal client:visible>
  <details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
    <summary className="cursor-pointer font-medium">If the time-series and cross-sectional regressions both use "betas," why not just run one?</summary>

    Because they answer different questions and use the betas in opposite roles. In the **time-series** pass, beta is an **output**: you regress one asset's returns *on the factor returns over time*, and the slope you estimate *is* the loading. In the **cross-sectional** pass, those estimated loadings become the **input** — the X variable — and you regress assets' *average returns on their loadings*, with the new slope being the factor's *premium*. One asks "how exposed is this asset?"; the other asks "is that exposure rewarded?" Fama–MacBeth chains them precisely because you need the first pass's loadings before the second pass can price them — and running the second pass month by month is what makes its standard errors trustworthy.
  </details>
</Reveal>

## Putting it together

Estimating factor exposures is two regressions doing two jobs. The **time-series regression** ($R_i - R_f = \alpha_i + \sum_k \beta_{i,k} F_k + \varepsilon_i$) takes one asset over many dates and returns its **loadings** (slopes), its **alpha** (intercept — return no factor explains), and its **$R^2$** (variance explained, *not* average return — diversified portfolios sit at 0.90+, single stocks far lower). A high $R^2$ is the *opposite* of alpha: a closet index fund fits perfectly and earns nothing extra. The **cross-sectional regression** ($\bar R_i = \lambda_0 + \sum_k \lambda_k \hat\beta_{i,k} + u_i$) takes many assets at a moment, uses the loadings as inputs, and returns the **premia** (slopes) and the **pricing error** (intercept) — answering whether loading on a factor is actually *rewarded*. **Fama–MacBeth** chains them: estimate loadings (Pass 1), run a cross-sectional regression every month to get a time series of premium estimates (Pass 2), then take their average and judge it by the t-stat from that series' standard error. Our four-month value example gave a +0.2%/month premium but only $t \approx 1.1$ — not significant, because the $\sqrt{T}$ in the standard error punishes short samples. Always demand an honest, Newey–West-corrected t-stat, never a pretty $R^2$.

<MindMap
  client:visible
  title="Estimating factor exposures — the whole pipeline"
  caption="Two regressions, two questions. Time series gives loadings, alpha and R²; cross section gives premia; Fama–MacBeth chains them and judges everything by the t-stat — never by R²."
  root={{
    label: 'Estimating factor exposures',
    children: [
      {
        label: 'Time-series regression',
        children: [
          { label: 'One asset, many dates' },
          { label: 'Excess return on factor returns' },
          { label: 'Slopes = loadings (exposures)' },
          { label: 'Intercept = alpha (unexplained return)' },
          { label: 'Newey–West standard errors' },
        ],
      },
      {
        label: 'R² ≠ average return',
        children: [
          { label: 'R² = variance explained' },
          { label: 'Diversified portfolios 0.90+' },
          { label: 'High R² is the opposite of alpha' },
          { label: 'Closet index fund: R² ≈ 1, alpha = 0' },
        ],
      },
      {
        label: 'Cross-sectional regression',
        children: [
          { label: 'Many assets, one moment' },
          { label: 'Average return on loadings' },
          { label: 'Slopes = factor risk premia (λ)' },
          { label: 'Intercept = pricing error' },
        ],
      },
      {
        label: 'Fama–MacBeth two passes',
        children: [
          { label: 'Pass 1: estimate loadings' },
          { label: 'Pass 2: cross-section each month → λ̂ over time' },
          { label: 'Premium = average of monthly λ̂' },
          { label: 't-stat from that series\' SE' },
        ],
      },
      {
        label: 'Read t-stats honestly',
        children: [
          { label: 'Need |t| ≈ 2 (higher for new factors)' },
          { label: 'SE shrinks like 1 / √T' },
          { label: 'Short windows mislead both ways' },
          { label: 'High R² ≠ alpha ≠ good investment' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: estimating factor exposures"
  questions={[
    {
      question: 'In a time-series factor regression of one asset, what do the slope and the intercept represent?',
      options: [
        { text: 'The slope on each factor is the asset\'s loading (exposure); the intercept is its alpha — the average return no factor explains', correct: true },
        { text: 'The slope is alpha and the intercept is the loading' },
        { text: 'The slope is R² and the intercept is the premium' },
        { text: 'Both are the factor risk premium, estimated two different ways' },
      ],
      explanation: 'Regressing one asset\'s excess return on the factor returns over time, each slope is a loading (how much of that factor the asset carries) and the intercept is alpha (return unexplained by any factor). Premia come from the cross-sectional regression, not this one.',
    },
    {
      question: 'Spot the trap. A fund advertises an R² of 0.99 against a three-factor model as proof of its quality. What is the honest interpretation?',
      options: [
        { text: 'The fund moves almost entirely with its factor tilts — likely a closet index fund with little or no alpha; high R² is the opposite of skill', correct: true },
        { text: '99% of the fund\'s return is manager skill' },
        { text: 'The fund will outperform 99% of peers' },
        { text: 'A high R² guarantees a high, significant alpha' },
      ],
      explanation: 'R² is variance explained, not average return. An R² of 0.99 means the factors describe nearly all the fund\'s movement, leaving almost no residual for skill — the hallmark of a closet index fund. Alpha lives in the unexplained part, so high R² is the opposite of evidence for it.',
    },
    {
      question: 'How does a cross-sectional regression differ from a time-series regression in a factor model?',
      options: [
        { text: 'It regresses many assets\' average returns on their loadings at a moment, estimating factor premia; the time-series regression regresses one asset\'s returns on factors over time, estimating its exposure and alpha', correct: true },
        { text: 'They are the same regression with different variable names' },
        { text: 'The cross-sectional regression estimates a single asset\'s alpha; the time-series one estimates R²' },
        { text: 'The cross-sectional regression uses one date and ignores returns entirely' },
      ],
      explanation: 'Time series: one asset over many dates, outputs loadings and alpha. Cross section: many assets at one moment, with the loadings as the X variable, outputs the premia (slopes) and pricing error (intercept). They answer "how exposed?" versus "is exposure rewarded?"',
    },
    {
      question: 'Lay out the two passes of the Fama–MacBeth procedure in order.',
      options: [
        { text: 'Pass 1: time-series regressions estimate each asset\'s loadings. Pass 2: a cross-sectional regression each period yields a time series of premium estimates, whose average and standard error give the premium and its t-stat', correct: true },
        { text: 'Pass 1: average all returns. Pass 2: regress that average on the market once' },
        { text: 'Pass 1: compute R² for every asset. Pass 2: pick the highest-R² factor' },
        { text: 'Pass 1: run one cross-sectional regression. Pass 2: confirm it with a single time-series regression' },
      ],
      explanation: 'Fama–MacBeth first estimates loadings via time-series regressions, then runs a separate cross-sectional regression every month to build a time series of premium estimates. The premium is that series\' average and the t-stat comes from its standard error, which neatly absorbs within-month cross-sectional correlation.',
    },
    {
      question: 'A Fama–MacBeth run over four months gives monthly premium estimates of +0.4%, −0.2%, +0.6%, and 0.0%, for a mean of +0.2%/month, a standard error of about 0.18%, and a t-stat near 1.1. Conceptually, what should you conclude — and what fixes it?',
      options: [
        { text: 'The premium is not statistically significant (t ≈ 1.1 is below ≈ 2); a much longer sample shrinks the standard error via √T and could establish significance if the premium is real', correct: true },
        { text: 'The premium is significant because its average is positive' },
        { text: 'The premium is significant because four months is plenty of data' },
        { text: 'Significance is impossible to assess without the R² of the regression' },
      ],
      explanation: 'A positive average is not enough — t ≈ 1.1 means the average is swamped by month-to-month noise and falls short of the t = 2 bar. Because the standard error scales as 1/√T, lengthening the sample to hundreds of months shrinks it and can reveal a genuine premium. R² is irrelevant to this judgment.',
    },
    {
      question: 'Why do practitioners use Newey–West standard errors when testing a time-series alpha?',
      options: [
        { text: 'Because return residuals are heteroskedastic and autocorrelated, which deflates ordinary standard errors and makes noisy alphas look significant; Newey–West widens the error bars to correct for that', correct: true },
        { text: 'Because they make the alpha estimate itself larger' },
        { text: 'Because they raise R² toward 1' },
        { text: 'Because they convert the time-series regression into a cross-sectional one' },
      ],
      explanation: 'Monthly returns have clustered volatility (heteroskedasticity) and serial correlation (autocorrelation). Ordinary standard errors ignore both and understate the true uncertainty, inflating t-stats. Newey–West standard errors account for that structure, giving a larger, more honest error bar and a t-stat you can trust.',
    },
  ]}
/>

Next, we'll put these estimates to work — turning measured loadings and a thin, hard-won alpha into a verdict on whether a strategy's returns are *skill* or merely repackaged factor beta, and watching how published factor premia decay once the whole market piles into them.
