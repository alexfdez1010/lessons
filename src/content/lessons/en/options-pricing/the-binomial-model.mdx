---
title: "The Binomial Model in Full"
description: "The full Cox–Ross–Rubinstein tree — up/down factors, the risk-neutral probability q, backward induction over many steps, calibrating u and d to volatility, convergence to Black–Scholes, and early exercise."
topic: "en/options-pricing"
order: 2
minutes: 10
updated: 2026-06-05
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, Categorize, MatchConcepts, MindMap, BinomialTree } from '@/components/react';

Last lesson you built an option out of stock and cash, watched the real-world probabilities cancel out of the algebra, and saw a strange new number — a *risk-neutral probability* $q$ — appear from nowhere to do the pricing. That was the magic trick. This lesson is the instruction manual: we'll write $q$ down as a clean formula, roll it across a multi-step tree, calibrate the whole thing to a real stock's volatility, and watch it quietly turn into Black–Scholes as we slice time finer. By the end you'll be able to price an option with a pen, a single exponential, and some patient arithmetic.

First, a sanity check on what you carried over from lesson 1.

<MCQ
  client:visible
  pretest
  question="In the one-step replication argument, the real-world probability that the stock goes up turned out to:"
  options={[
    { text: 'Cancel out — it never appears in the option price', correct: true },
    { text: 'Be the only thing that determines the price' },
    { text: 'Equal the risk-free rate' },
    { text: 'Be replaced by the option premium' },
  ]}
  explanation="The whole shock of lesson 1 was that the option's fair price does not depend on how likely the stock is to rise. Replication pins the price to the cost of a stock-and-cash recipe, and the real probability washes out. What's left is q — a pricing weight, not a forecast."
/>

## The one-step formula, stated cleanly

Set up the simplest possible future: one period of length $T$, at the end of which the stock has either gone **up** to $S_0 u$ or **down** to $S_0 d$. Here $u$ is the **up-factor** and $d$ is the **down-factor**, with $d < 1 < u$ (down means shrink, up means grow). The risk-free rate over the step is $r$, so a dollar in the bank becomes $e^{rT}$.

The option is worth $V_u$ if the stock goes up and $V_d$ if it goes down. Lesson 1's replication argument collapses to two lines. Define the **risk-neutral up-probability**:

$$q = \frac{e^{rT} - d}{u - d}$$

Then today's fair price of the option is the discounted, $q$-weighted average of the two outcomes:

$$V = e^{-rT}\,\big[\,q\,V_u + (1 - q)\,V_d\,\big]$$

Read each piece slowly, because every later step is just this sentence repeated:

- $q\,V_u + (1-q)\,V_d$ is an **expected payoff** — but weighted by $q$, not by the real odds.
- $e^{-rT}$ **discounts** that expected payoff back to today's money (a dollar at expiry is worth less than a dollar now).
- $q$ itself is built only from $u$, $d$, and the risk-free growth $e^{rT}$. Notice what's *missing*: the real probability of an up-move, the stock's expected return, your opinion. None of it is in the formula.

<Callout variant="info" title="The whole model in one breath">
**Expected payoff under $q$, discounted at the risk-free rate.** That's it. Everything else in this lesson — extra steps, calibration, American options — is this one rule applied more times or with one extra comparison bolted on.
</Callout>

### The no-arbitrage bound

For $q$ to behave like a probability it must sit between 0 and 1, and that forces a condition on the factors:

$$d < e^{rT} < u$$

This isn't a technicality — it's the law that keeps the market honest. Suppose it broke. If $e^{rT} \le d$, then even the *down* outcome ($S_0 d$) grows at least as fast as cash: the stock can only beat or tie the bank, never lose to it. Borrow money, buy the stock, and you've got a guaranteed free lunch — **arbitrage**. If $e^{rT} \ge u$, the stock can never even keep up with the bank, so you short it, park the proceeds at the risk-free rate, and again pocket a riskless profit. Only when the risk-free growth sits *strictly between* the down and up outcomes does the stock genuinely have an upside and a downside — and only then is $q$ a number between 0 and 1.

<FillBlank
  client:visible
  question="Fill in the no-arbitrage condition and what it guarantees."
  text="The factors must satisfy {{d < e^{rT} < u|u < e^{rT} < d|d < e^{rT} = u}}. If this fails, a riskless {{arbitrage|premium|dividend}} appears, and the risk-neutral probability q falls {{outside|inside}} the range 0 to 1."
  explanation="Risk-free growth must sit strictly between the down and up outcomes. Otherwise one side dominates cash, free money appears, and q is no longer a valid probability (it goes negative or above 1)."
/>

## Why q is not the real probability

This is the idea people trip over for years, so let's nail it. Rearrange the definition of $q$ and ask what it makes true. Plug $q$ back into the expected *stock* price next period:

$$q\,(S_0 u) + (1 - q)\,(S_0 d) = S_0\,e^{rT}$$

In words: **$q$ is precisely the probability that makes the stock's own expected growth equal the risk-free rate.** Under the real odds, a risky stock should be expected to out-grow cash (that extra return is your reward for stomaching the risk). Under $q$, that risk premium is surgically removed — every asset, risky or not, is "expected" to grow at exactly $r$. That's why $q$ is called the *risk-neutral* probability: it describes a make-believe world where nobody demands extra return for taking risk.

So $q$ is a **pricing device, not a forecast**. If you genuinely believe the stock will rise 70% of the time, that's a fine trading opinion — but it does *not* go into the option price. The price uses $q$, and $q$ is fixed by $u$, $d$, and $r$ alone. The market prices the option "as if" everyone were risk-neutral, not because they are, but because that's the only weighting under which the replicating recipe and the option must cost the same. Confuse $q$ with your forecast and you'll mis-price every contract you touch.

<Callout variant="warning" title="q ≠ your probability of an up-move">
$q$ feels like "the chance the stock rises," and it is *not*. It's the weight that makes the stock grow at the risk-free rate on average. Two stocks with identical $u$, $d$, $r$ share the same $q$ even if one is a meme coin and the other a utility — because $q$ encodes *no view at all*. Keep your forecast; the price doesn't want it.
</Callout>

<MatchConcepts
  client:visible
  question="Match each symbol to what it actually represents."
  pairs={[
    { term: 'u', definition: 'The up-factor — what the stock multiplies by in an up-move' },
    { term: 'd', definition: 'The down-factor — the shrink multiplier, less than 1' },
    { term: 'q', definition: 'The risk-neutral weight that makes the stock grow at the risk-free rate' },
    { term: 'e^{rT}', definition: 'Risk-free growth of cash over one step of length T' },
    { term: 'e^{-rT}', definition: 'The discount factor pulling a future value back to today' },
  ]}
  explanation="u and d shape the tree; e^{rT} grows cash forward and e^{-rT} discounts it back; q is the pricing weight (not a forecast) that equates the stock's expected growth with the risk-free rate."
/>

## A fully worked two-step example

Numbers make it concrete. Take a stock at $S_0 = 100$, with $u = 1.2$ and $d = 0.8$ over each step, a European **call** struck at $K = 100$, and — to keep the arithmetic clean — a risk-free rate of essentially **zero** for now, so $e^{rT} = 1$. We'll run two steps.

**Step A — grow the tree forward.** Each node either multiplies by 1.2 or by 0.8:

| Path | Price | |
|---|---|---|
| up, up | $100 \times 1.2 \times 1.2$ | **144** |
| up, down (= down, up) | $100 \times 1.2 \times 0.8$ | **96** |
| down, down | $100 \times 0.8 \times 0.8$ | **64** |

So the three terminal prices are **144, 96, 64**. The call's payoff at expiry is $\max(S - K, 0)$ with $K = 100$:

- $V_{uu} = \max(144 - 100, 0) = 44$
- $V_{ud} = \max(96 - 100, 0) = 0$
- $V_{dd} = \max(64 - 100, 0) = 0$

**Step B — compute $q$.** With $e^{rT} = 1$:

$$q = \frac{1 - 0.8}{1.2 - 0.8} = \frac{0.2}{0.4} = 0.5$$

A tidy fifty-fifty. (That's a coincidence of our zero-rate, symmetric-ish factors — don't expect 0.5 in general.)

**Step C — backward-induct, node by node.** Now we walk the tree *backwards*, applying $V = e^{-rT}[q V_u + (1-q) V_d]$ at every interior node. With $e^{-rT} = 1$ and $q = 0.5$ it's just the average of the two children.

*The upper node after one up-move* (stock at 120) has children 44 and 0:

$$V_u = 0.5 \times 44 + 0.5 \times 0 = 22$$

*The lower node after one down-move* (stock at 80) has children 0 and 0:

$$V_d = 0.5 \times 0 + 0.5 \times 0 = 0$$

*The root (today)* has children $V_u = 22$ and $V_d = 0$:

$$V_0 = 0.5 \times 22 + 0.5 \times 0 = 11$$

The call is worth **\$11 today**. Not the average payoff under your gut feeling — the discounted $q$-weighted roll-back of the whole tree. Every interior node is the same formula; we just applied it three times, right-to-left.

<MCQ
  client:visible
  question="In that two-step tree, what is the value at the upper interior node (the stock at 120, one step from expiry)?"
  options={[
    { text: '22 — the q-weighted average of its children 44 and 0', correct: true },
    { text: '44 — it inherits the best terminal payoff' },
    { text: '11 — the same as the root' },
    { text: '0 — the option is out of the money there' },
  ]}
  explanation="That node's children are V_uu = 44 and V_ud = 0. With q = 0.5 and no discounting, V = 0.5×44 + 0.5×0 = 22. The root then averages 22 and 0 to get 11."
/>

Now drive the tree yourself. This island uses the same spot and factors but a small positive rate, so $q$ won't be a clean 0.5 — watch the readout. Move the sliders and the values roll back from the terminal column to the root in real time.

<BinomialTree
  client:visible
  title="Two-step tree: roll the call back to today"
  spot={100}
  up={1.2}
  down={0.8}
  rate={0.03}
  strike={100}
  steps={2}
  type="call"
  caption="Terminal nodes pay max(S − K, 0). Every node before them is the discounted, q-weighted average of its two children. Roll back to the root and you have the fair price — with no real-world probability anywhere in sight. (This tree uses r = 3%, so q is a touch above 0.5.)"
/>

<Callout variant="tip" title="Same machine, the other flavour">
Flip the same tree to a **put** and only the terminal payoffs change — they become $\max(K - S, 0)$. The roll-back rule, $q$, and the discounting are identical. One engine prices both; the payoff column is the only thing that knows whether it's a call or a put.
</Callout>

<BinomialTree
  client:visible
  title="The same tree, now pricing a put"
  spot={100}
  up={1.2}
  down={0.8}
  rate={0.03}
  strike={100}
  steps={2}
  type="put"
  caption="Identical structure and identical q — only the terminal payoffs flip to max(K − S, 0). Backward induction doesn't care which option it's pricing."
/>

## Calibrating u and d to a real stock

So far $u = 1.2$ and $d = 0.8$ were plucked from thin air. To price a *real* option you need the tree to match the *real* stock's wobbliness — its **volatility** $\sigma$ (the annualised standard deviation of its returns). The Cox–Ross–Rubinstein (CRR) recipe sets the factors directly from $\sigma$ and the length of one time step $\Delta t$:

$$u = e^{\sigma \sqrt{\Delta t}}, \qquad d = \frac{1}{u} = e^{-\sigma \sqrt{\Delta t}}$$

Two things to notice. First, $d = 1/u$, so an up-move followed by a down-move lands you exactly back where you started — the tree **recombines**, which keeps the number of nodes small (a 50-step tree has 51 terminal prices, not $2^{50}$). Second, the size of each move scales with $\sqrt{\Delta t}$: chop a year into more steps, each $\Delta t$ shrinks, and each up/down jump gets *smaller*. A jittery, high-$\sigma$ stock gets a wide tree (big $u$); a sleepy bond-like stock gets a narrow one.

**Worked calibration.** Say $\sigma = 20\%$ per year and you want monthly steps, so $\Delta t = 1/12$. Then $\sqrt{\Delta t} = \sqrt{0.0833} \approx 0.2887$, and

$$u = e^{0.20 \times 0.2887} = e^{0.0577} \approx 1.0594, \qquad d = 1/u \approx 0.9439$$

Each month the stock either rises about 5.9% or falls about 5.6% in this model — a believable monthly wobble for a 20%-vol stock. More steps means finer time slices, smaller jumps, and a more realistic spread of outcomes.

<FillBlank
  client:visible
  question="Fill in the Cox–Ross–Rubinstein calibration."
  text="To match a stock with volatility σ over a step of length Δt, set u = {{e^{σ√Δt}|e^{rΔt}|σ√Δt}} and d = {{1/u|u−1|−u}}. Using more steps makes each Δt {{smaller|larger}} and each up/down jump {{smaller|larger}}."
  explanation="CRR sets u = e^{σ√Δt} and d = 1/u so the tree recombines. More steps shrink Δt, which shrinks each jump and gives a finer, more realistic model."
/>

<MCQ
  client:visible
  question="Why does the CRR choice d = 1/u matter in practice?"
  options={[
    { text: 'The tree recombines, so an up-then-down move returns to the start and node count grows linearly, not exponentially', correct: true },
    { text: 'It guarantees q is always exactly 0.5' },
    { text: 'It removes the need to discount at the risk-free rate' },
    { text: 'It makes the option worth the same as the stock' },
  ]}
  explanation="With d = 1/u, up·down = 1, so up-then-down lands back at S₀. A 50-step recombining tree has 51 terminal nodes instead of 2⁵⁰ — the difference between a pen-and-paper job and a supercomputer one."
/>

## Convergence: the tree is Black–Scholes in slow motion

Here's the payoff for all that calibration. Take a fixed option and price it with a 2-step tree, then 10 steps, then 50, then 500. The prices don't wander off — they **converge**, homing in on a single limiting number. That limit is the **Black–Scholes price** (the subject of the next lesson). As the number of steps $\to \infty$, each move shrinks toward zero, the jagged binomial distribution of final prices smooths into the bell-shaped lognormal distribution Black–Scholes assumes, and the discrete roll-back becomes the continuous formula.

So the binomial model isn't a rough cousin of Black–Scholes — it's **Black–Scholes in slow motion**, frozen into discrete frames you can actually compute by hand. The convergence isn't perfectly smooth, either: as you add steps the binomial price tends to **oscillate** around the limit (even step counts and odd step counts approach from slightly different sides) before settling down. Doubling the steps roughly halves the error. A few hundred steps is usually plenty for a price you'd trade on.

<MatchConcepts
  client:visible
  question="Match each number of steps to what it buys you."
  pairs={[
    { term: '1 step', definition: 'The bare replication argument — one up/down, easy to reason about, crude price' },
    { term: '2 steps', definition: 'A hand-computable tree showing genuine backward induction' },
    { term: '~500 steps', definition: 'A price visually indistinguishable from Black–Scholes' },
    { term: 'Infinitely many steps', definition: 'The exact Black–Scholes formula, the continuous-time limit' },
  ]}
  explanation="More steps = finer time slices = closer to the continuous lognormal world. In the limit the tree *is* Black–Scholes; a few hundred steps already lands on essentially the same number."
/>

## American options: the one thing the tree can do that the formula can't

Everything so far priced a **European** option (exercise only at expiry). Trees earn their keep on **American** options, where the holder may exercise *any time*. The plain Black–Scholes formula can't handle that early-exercise choice; the tree handles it with a one-line tweak.

At **every node** of the tree, instead of just taking the rolled-back continuation value, you take the larger of two things:

$$V_{\text{node}} = \max\Big(\underbrace{e^{-rT}[q V_u + (1-q)V_d]}_{\text{hold / continuation value}},\ \underbrace{\text{payoff}(S_{\text{node}})}_{\text{exercise now}}\Big)$$

At each step backward you ask: *is exercising right now worth more than holding on?* If yes, you exercise (and the node takes the immediate payoff); if no, you hold (and it keeps the continuation value). That single $\max$ is the entire difference between American and European pricing in a tree — and it's exactly why an American option is worth *at least* as much as its European twin: the extra right to bail early can only help.

When does that early-exercise right actually have value? Almost always for American **puts**: deep in the money, a put's payoff $K - S$ is capped (the stock can't fall below zero), so it can be worth grabbing the cash now and earning interest on it rather than waiting. For American **calls** on a *non-dividend* stock, early exercise is never optimal — you'd throw away time value and the interest on the strike — so they price the same as European calls. The exception is calls on a **dividend-paying** stock, where exercising just before a dividend can pay off.

<Reveal client:visible>
  <details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
    <summary className="cursor-pointer font-medium">Worked node: when an American put exercises early</summary>

    Take a node where the stock has dropped to **70**, on an American put struck at **100**, with $q = 0.5$ and (for simplicity) no discounting. Suppose its rolled-back **continuation value** — the $q$-weighted average of holding on — works out to **28**. The **immediate-exercise payoff** is $\max(100 - 70, 0) = 30$.

    Take the max: $\max(28,\ 30) = 30$. **Exercise now.** Holding is worth only 28, but selling at the struck 100 today nets 30, and with a positive rate you'd also start earning interest on that cash. A European put would be *forced* to wait and would carry the lower value of 28 at this node — which is exactly why the American put is worth more.
  </details>
</Reveal>

<Categorize
  client:visible
  question="Sort each option by whether early exercise can ever be worth it."
  buckets={['Early exercise can pay off', 'Early exercise never optimal']}
  items={[
    { text: 'American put, deep in the money', bucket: 'Early exercise can pay off' },
    { text: 'American call on a dividend-paying stock', bucket: 'Early exercise can pay off' },
    { text: 'American call on a non-dividend stock', bucket: 'Early exercise never optimal' },
    { text: 'Any European option', bucket: 'Early exercise never optimal' },
    { text: 'American put when rates are high', bucket: 'Early exercise can pay off' },
  ]}
  explanation="Early exercise lives mainly in American puts (and calls just before a dividend). Calls on non-dividend stocks should never be exercised early — you'd forfeit time value and the interest on the strike. European options can't be exercised early at all, by definition."
/>

<MCQ
  client:visible
  question="Which statements about pricing American options on a tree are correct? (Select all that apply.)"
  allowMultiple
  options={[
    { text: 'At each node you take the max of the continuation value and the immediate-exercise payoff', correct: true },
    { text: 'An American option is worth at least as much as its European twin', correct: true },
    { text: 'Early exercise is generally most relevant for American puts', correct: true },
    { text: 'American calls on a non-dividend stock should always be exercised immediately' },
    { text: 'The basic Black–Scholes formula handles early exercise directly' },
  ]}
  explanation="The max(hold, exercise) rule at every node is the whole trick, and it makes American ≥ European. Early exercise matters mostly for puts (and dividend-paying calls). Non-dividend American calls should *never* be exercised early, and plain Black–Scholes can't price early exercise at all — that's exactly the gap the tree fills."
/>

## Putting it together

You can now price an option from scratch: grow a recombining tree with $u = e^{\sigma\sqrt{\Delta t}}$ and $d = 1/u$, stamp the payoff on every terminal node, compute $q = (e^{rT} - d)/(u - d)$, and roll backward with $V = e^{-rT}[qV_u + (1-q)V_d]$ — taking a $\max$ against immediate exercise at every node if the option is American. Add steps and the price marches toward Black–Scholes.

<MindMap
  client:visible
  title="The binomial model — the whole machine"
  caption="One rule (discounted q-weighted average) applied across a calibrated, recombining tree. Add steps to reach Black–Scholes; add a max to handle American options."
  root={{
    label: 'Binomial Model',
    children: [
      {
        label: 'One-step rule',
        children: [
          { label: 'q = (e^{rT} − d) / (u − d)' },
          { label: 'V = e^{−rT}[qV_up + (1−q)V_down]' },
          { label: 'No-arbitrage bound d < e^{rT} < u' },
        ],
      },
      {
        label: 'q is a pricing device',
        children: [
          { label: 'Makes stock grow at the risk-free rate' },
          { label: 'Not your forecast of an up-move' },
        ],
      },
      {
        label: 'Multi-step tree',
        children: [
          { label: 'Grow prices forward' },
          { label: 'Stamp payoffs at terminal nodes' },
          { label: 'Backward-induct node by node' },
        ],
      },
      {
        label: 'Calibration (CRR)',
        children: [
          { label: 'u = e^{σ√Δt}, d = 1/u' },
          { label: 'd = 1/u so the tree recombines' },
          { label: 'More steps = finer time slices' },
        ],
      },
      {
        label: 'Convergence',
        children: [
          { label: 'Steps → ∞ gives Black–Scholes' },
          { label: 'Price oscillates then settles' },
        ],
      },
      {
        label: 'American options',
        children: [
          { label: 'max(continuation, exercise now) at each node' },
          { label: 'American ≥ European' },
          { label: 'Matters mostly for puts' },
        ],
      },
    ],
  }}
/>

<Quiz
  client:visible
  title="Recap: the binomial model"
  questions={[
    {
      question: 'The risk-neutral up-probability q is defined as:',
      options: [
        { text: '(e^{rT} − d) / (u − d)', correct: true },
        { text: '(u − e^{rT}) / (u − d)' },
        { text: 'The real-world chance the stock rises' },
        { text: 'u / (u + d)' },
      ],
      explanation: 'q = (e^{rT} − d)/(u − d). It is built only from u, d and the risk-free growth — never from any real-world probability.',
    },
    {
      question: 'A one-step tree has S₀ = 100, u = 1.2, d = 0.8 and r = 0 (so e^{rT} = 1). What is q?',
      options: [
        { text: '0.5', correct: true },
        { text: '0.2' },
        { text: '0.6' },
        { text: '0.8' },
      ],
      explanation: 'q = (1 − 0.8)/(1.2 − 0.8) = 0.2/0.4 = 0.5. With these symmetric factors and a zero rate, q lands exactly at one half.',
    },
    {
      question: 'Why is q called the *risk-neutral* probability?',
      options: [
        { text: 'It is the weight under which the stock is expected to grow at the risk-free rate', correct: true },
        { text: 'Because risk-free bonds have no volatility' },
        { text: 'Because it equals the real probability of an up-move' },
        { text: 'Because it makes every option worth zero' },
      ],
      explanation: 'Under q, q·S₀u + (1−q)·S₀d = S₀e^{rT}: the risk premium is stripped out and every asset "grows" at r. That make-believe world is what risk-neutral means.',
    },
    {
      question: 'In the Cox–Ross–Rubinstein calibration, the up-factor is set to:',
      options: [
        { text: 'u = e^{σ√Δt}, with d = 1/u', correct: true },
        { text: 'u = e^{rΔt}, with d = 1 − u' },
        { text: 'u = σ·Δt, with d = −u' },
        { text: 'u = 1 + r, with d = 1 − r' },
      ],
      explanation: 'CRR sets u = e^{σ√Δt} and d = 1/u so the tree recombines and matches the stock\'s volatility. The jump size scales with √Δt, so more steps mean smaller moves.',
    },
    {
      question: 'What is the one change needed to price an American option on the tree?',
      options: [
        { text: 'At every node take the max of the continuation value and the immediate-exercise payoff', correct: true },
        { text: 'Replace q with the real-world probability' },
        { text: 'Use a different discount rate for every node' },
        { text: 'Stop discounting the terminal payoffs' },
      ],
      explanation: 'American pricing bolts a max(hold, exercise now) onto every node of the same backward induction. That single comparison — which plain Black–Scholes can\'t do — is what lets the tree value early exercise.',
    },
  ]}
/>

Next up — **The Black–Scholes Formula** — we let the steps go to infinity. The tree's discrete roll-back smooths into a single closed-form equation, the lognormal bell curve replaces the jagged terminal distribution, and that mysterious $q$ reappears wearing a new costume: $N(d_2)$, a probability hiding inside the most famous formula in finance.
