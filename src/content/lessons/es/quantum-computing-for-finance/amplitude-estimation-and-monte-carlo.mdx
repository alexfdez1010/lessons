---
title: "Estimación de amplitud y la aceleración de Monte Carlo"
description: "La estimación de amplitud cuántica convierte el error 1/sqrt(N) de Monte Carlo en 1/N — una aceleración cuadrática para la valoración de opciones y el riesgo VaR/CVaR, la victoria cuántica más limpia y demostrable de las finanzas."
topic: "es/quantum-computing-for-finance"
order: 2
minutes: 18
updated: 2026-06-23
---

import { Callout, MCQ, Quiz, Reveal, ThinkReveal, FillBlank, MatchConcepts, Categorize, MindMap, AmplitudeEstimationConvergence, MonteCarloConverge } from '@/components/react';

Cualquier otra afirmación sobre finanzas cuánticas que oigáis es, o bien un quizás, o bien un algún día, o bien una mentira piadosa. Esta no es ninguna de las tres. **La estimación de amplitud cuántica (QAE) coge la ley más tozuda de todas las finanzas computacionales — que el error de Monte Carlo encoge como uno partido por la raíz cuadrada del número de muestras — y la sustituye por uno partido por el número de muestras.** Eso es una aceleración *cuadrática*, y es demostrable, nada de andarse por las ramas. Es la joya de la corona de todo este curso: el único lugar donde las matemáticas dicen sin ambigüedad que la máquina cuántica hace menos preguntas para obtener la misma respuesta.

Así que dedicaremos esta lección a ganarnos el derecho a estar emocionados — y luego, al final del todo, a ganarnos el derecho a ser escépticos. Porque "menos consultas en teoría" y "menos segundos en un chip real con ruido real y una factura real de carga de datos" son afirmaciones muy distintas, y la brecha entre ambas es el resto de este curso.

Si vuestro recuerdo de *Monte Carlo para finanzas* está borroso, desempolvadlo ahora. Toda la recompensa de QAE solo se siente sobre el telón de fondo de por qué el Monte Carlo clásico converge tan frustrantemente despacio.

## El muro del Monte Carlo clásico

<MCQ
  client:visible
  pretest
  question="El Monte Carlo clásico estima una esperanza promediando muestras aleatorias. Si queréis reducir a la mitad vuestro error de estimación, ¿cuántas muestras más necesitáis aproximadamente?"
  options={[
    { text: "Unas 4 veces más — el error encoge como 1/sqrt(N), así que reducirlo a la mitad necesita 4 veces las muestras", correct: true },
    { text: "Unas 2 veces más — el error es proporcional a 1/N" },
    { text: "Unas 10 veces más, sin importar la ley del error" },
    { text: "Ninguna muestra más; el error no depende del número de muestras" },
  ]}
  explanation="El error estándar escala como sigma/sqrt(N). Reducirlo a la mitad significa que sqrt(N) debe duplicarse, así que N debe cuadruplicarse. Esa castigadora ley de la raíz cuadrada es exactamente lo que QAE ataca."
/>

**Analogía.** El Monte Carlo clásico es un encuestador. Para estimar qué fracción de votantes prefiere a un candidato, llamáis a gente al azar y promediáis. Encuestad a 100 personas y vuestro margen de error tiene cierta anchura; para *reducir a la mitad* ese margen no llamáis a 200 personas — llamáis a 400. Cada dígito extra de precisión os cuesta cien veces más llamadas telefónicas. El dolor del encuestador es el dolor del quant.

**Definición.** Para estimar una esperanza $\mu = \mathbb{E}[f(X)]$, el Monte Carlo clásico extrae $N$ muestras independientes y las promedia. Por el teorema central del límite, el **error estándar** de ese promedio es

$$ \text{SE} = \frac{\sigma}{\sqrt{N}}, $$

donde $\sigma$ es la desviación típica de $f(X)$. Por tanto, el error de estimación encoge como $1/\sqrt{N}$ — pendiente $-1/2$ en una gráfica log-log. Invirtiéndolo: para alcanzar un error objetivo $\varepsilon$ necesitáis

$$ N \approx \frac{\sigma^2}{\varepsilon^2} \;\propto\; \frac{1}{\varepsilon^2}. $$

El coste crece con el *cuadrado* de la precisión que exigís. Este es el muro.

**Ejemplo resuelto.** Fijad $\sigma = 1$ por limpieza. Para alcanzar un error de $\varepsilon = 10^{-2}$ (uno por ciento) necesitáis unas $N = 1/(10^{-2})^2 = 1/10^{-4} = 10^{4}$ muestras — diez mil. Ahora queréis un dígito más, $\varepsilon = 10^{-3}$:

$$ N \approx \frac{1}{(10^{-3})^2} = \frac{1}{10^{-6}} = 10^{6}. $$

Un millón. **Un dígito extra de precisión os ha costado un aumento de 100 veces en muestras** — de diez mil a un millón. Ese factor de 100 no es coincidencia; es $(10^{-2}/10^{-3})^2 = 10^2$. Elevar al cuadrado la razón de precisión es toda la historia.

<MonteCarloConverge client:visible caption="Monte Carlo clásico: la estimación en curso deambula hacia la verdad, pero la banda de error solo se estrecha como 1/sqrt(N). Los últimos dígitos de precisión son agónicamente caros — ese lento gateo de raíz cuadrada es el muro que QAE rompe." />

<Callout variant="warning" title="El impuesto de la raíz cuadrada">
La ley 1/sqrt(N) no es una manía de una mala implementación — es el teorema central del límite, y ninguna astucia clásica cambia el *exponente*. Los trucos de reducción de varianza (variantes antitéticas, variables de control, muestreo por importancia) encogen la constante sigma, lo cual ayuda, pero el error sigue cayendo como 1/sqrt(N). Podéis hacer el muro más fino; no podéis moverlo. Solo cambiar cómo se *generan y combinan* las muestras — que es lo que hace un ordenador cuántico — cambia el exponente mismo.
</Callout>

### Cuándo usarlo

Recurrid al Monte Carlo clásico siempre que una esperanza no tenga forma cerrada y la dimensión sea demasiado alta para una rejilla — valorar opciones dependientes de la trayectoria, agregar riesgo de cartera, propagar incertidumbre. Es el caballo de batalla por defecto precisamente porque es general y vergonzosamente paralelizable. Solo presupuestad con honestidad: cada dígito extra de precisión es una factura de cuatro veces (por medio dígito) o cien veces (por dígito completo) en muestras, y esa factura es lo que QAE promete recortar.

<FillBlank
  client:visible
  question="Enunciad la ley de convergencia clásica con vuestras propias palabras."
  text="El error del Monte Carlo clásico encoge como 1 partido por la {{raíz cuadrada de N|cuadrado de N|logaritmo de N}}, así que el número de muestras necesarias para un error objetivo crece como 1 partido por {{epsilon al cuadrado|epsilon|la raíz cuadrada de epsilon}}."
  explanation="El error estándar es sigma/sqrt(N), así que el error cae como 1/sqrt(N) y el presupuesto de muestras crece como 1/epsilon^2. Cuadrático en la precisión que exigís."
/>

## El resultado central de QAE: 1/sqrt(N) se vuelve 1/N

<MCQ
  client:visible
  pretest
  question="La estimación de amplitud cuántica estima una amplitud (probabilidad) desconocida 'a' con error epsilon. ¿Cómo escala su coste en consultas con epsilon, comparado con el muestreo clásico?"
  options={[
    { text: "QAE necesita O(1/epsilon) consultas al oráculo frente a las O(1/epsilon^2) muestras clásicas — una aceleración cuadrática", correct: true },
    { text: "QAE necesita O(1/epsilon^2), igual que el clásico — sin ganancia asintótica" },
    { text: "QAE necesita O(log(1/epsilon)) — una aceleración exponencial sobre el clásico" },
    { text: "QAE necesita O(1/epsilon^3), estrictamente peor que el clásico" },
  ]}
  explanation="QAE compra una aceleración cuadrática: O(1/epsilon) consultas en vez de O(1/epsilon^2) muestras. Cuadrática, no exponencial — esa distinción es todo el partido, y la machacamos al final."
/>

**Analogía.** Imaginad dos árbitros cronometrando con qué frecuencia una moneda trucada cae cara. El árbitro clásico lanza la moneda muchas veces y cuenta — y queda atrapado en el impuesto de raíz cuadrada del encuestador. La árbitra cuántica no lanza y cuenta; pone la moneda en una especie de *rotación resonante* cuyo ángulo codifica el sesgo directamente, y luego lee el ángulo. Leer un ángulo con precisión cuesta un esfuerzo proporcional a la precisión que queréis, no a su cuadrado. La misma pregunta, una medición fundamentalmente más barata.

**Definición.** Supongamos que un operador $\mathcal{A}$ prepara un estado en el que el resultado "bueno" que os importa tiene amplitud $\sqrt{a}$, de modo que la probabilidad de medirlo es exactamente $a$. Clásicamente estimaríais $a$ muestreando: preparar, medir, repetir $N$ veces, contar — error $\propto 1/\sqrt{N}$. La **estimación de amplitud** en cambio usa amplificación de amplitud (el motor tras el algoritmo de Grover) para estimar $a$ con error aditivo $\varepsilon$ usando un número de aplicaciones de $\mathcal{A}$ (consultas al "oráculo") que escala como

$$ N_{\text{quantum}} = O\!\left(\frac{1}{\varepsilon}\right), $$

frente al clásico

$$ N_{\text{classical}} = O\!\left(\frac{1}{\varepsilon^2}\right). $$

La razón es $N_{\text{classical}} / N_{\text{quantum}} \approx 1/\varepsilon$ — el número de consultas cuánticas es la *raíz cuadrada* del número de muestras clásicas. En una gráfica log-log de error frente a coste, el clásico tiene pendiente $-1/2$ y el cuántico tiene pendiente $-1$. La brecha vertical creciente entre esas dos líneas **es** la aceleración.

**Ejemplo resuelto.** Exigid un error objetivo $\varepsilon = 10^{-3}$. El muestreo clásico necesita

$$ N_{\text{classical}} \approx \frac{1}{(10^{-3})^2} = 10^{6} \;\text{muestras}, $$

mientras que QAE solo necesita

$$ N_{\text{quantum}} \approx \frac{1}{10^{-3}} = 10^{3} \;\text{consultas}. $$

Un millón frente a mil. La razón de aceleración es $10^{6}/10^{3} = 10^{3} = 1/\varepsilon$. **Apretad el objetivo a $\varepsilon = 10^{-4}$ y la brecha explota a $10^{8}$ frente a $10^{4}$ — una ventaja de diez mil veces.** Cuanta más precisión exigís, más desigual se vuelve la contienda, porque estáis corriendo pendiente $-1$ contra pendiente $-1/2$.

<AmplitudeEstimationConvergence
  client:visible
  title="Convergencia: Monte Carlo clásico frente a estimación de amplitud cuántica"
  classicalLabel="Monte Carlo clásico"
  quantumLabel="Estimación de amplitud cuántica (QAE)"
  targetErrorLabel="Error objetivo (ε)"
  classicalCountLabel="Muestras clásicas"
  quantumCountLabel="Consultas cuánticas"
  speedupLabel="Aceleración"
  animateLabel="Barrer N"
  xAxisLabel="Muestras / consultas al oráculo N"
  yAxisLabel="Error de estimación"
  caption="Arrastrad el deslizador de error objetivo: el número de muestras clásicas (línea de pendiente -1/2) crece como 1/epsilon^2 mientras que el número de consultas cuánticas (línea de pendiente -1) crece solo como 1/epsilon. La etiqueta de aceleración marca alrededor de 1/epsilon, así que en epsilon = 1e-3 el cuántico necesita ~1.000 consultas donde el clásico necesita ~1.000.000 muestras. Pulsad Barrer N para ver cómo la brecha entre las dos leyes se ensancha al apretar la precisión."
/>

<Callout variant="success" title="Por qué esta sí es real">
La mayoría de las afirmaciones de ventaja cuántica descansan sobre una estructura del problema que nadie puede garantizar que exista en vuestros datos. La aceleración cuadrática de QAE es un *teorema sobre el coste de estimar una amplitud* — se cumple siempre que podáis construir el operador de preparación de estado. Por eso es la victoria más limpia de las finanzas: no es "quizás los datos tengan estructura oculta", es "así escala demostrablemente la estimación de amplitud". Los asteriscos van todos sobre el coste por consulta y la carga, no sobre si la aceleración es real.
</Callout>

### Cuándo usarlo

QAE es la herramienta siempre que vuestra cantidad de interés sea una **esperanza o una probabilidad** que de otro modo haríais por Monte Carlo, y necesitéis *muchos dígitos* de precisión. La razón de aceleración es $1/\varepsilon$, así que la ganancia es insignificante con precisión gruesa ($\varepsilon = 0{,}1$ os compra solo unas 10x) y enorme con precisión fina ($\varepsilon = 10^{-4}$ os compra 10.000x). Si solo necesitáis respuestas de dos dígitos, la aceleración cuadrática apenas importa; si vais detrás de números de riesgo ajustados o probabilidades de cola, es exactamente donde la palanca es más larga.

<MatchConcepts
  client:visible
  pairs={[
    { term: "Error estándar", definition: "Dispersión del promedio de Monte Carlo; igual a sigma/sqrt(N), lo que da la ley de error 1/sqrt(N)" },
    { term: "Aceleración cuadrática", definition: "El coste en consultas cuánticas crece como 1/epsilon en vez del 1/epsilon^2 clásico" },
    { term: "Consulta al oráculo", definition: "Una aplicación del operador de preparación de estado A, la unidad en la que QAE cuenta su coste" },
    { term: "Amplitud a", definition: "La probabilidad del resultado bueno marcado, igual a la amplitud al cuadrado que QAE lee" },
    { term: "Razón de aceleración", definition: "Muestras clásicas divididas por consultas cuánticas, aproximadamente 1/epsilon a un error objetivo epsilon" },
  ]}
/>

## Cómo un pago se convierte en una amplitud

<MCQ
  client:visible
  pretest
  question="Para que QAE valore una opción, el pago esperado E[f(X)] debe primero codificarse como algo que el ordenador cuántico pueda leer. ¿Como qué?"
  options={[
    { text: "La probabilidad (amplitud al cuadrado) de medir un qubit auxiliar marcado en el estado 1", correct: true },
    { text: "El número de qubits del circuito" },
    { text: "El tiempo de ejecución total del algoritmo en nanosegundos" },
    { text: "La semilla aleatoria clásica usada para generar muestras" },
  ]}
  explanation="La esperanza se carga en la amplitud de un qubit auxiliar marcado, de modo que la probabilidad de medir ese qubit como 1 sea igual a E[f(X)] (debidamente normalizada). QAE entonces estima esa probabilidad."
/>

**Analogía.** Pensad en construir una diana a medida cuya geometría está amañada para que la *probabilidad de que un dardo al azar caiga en el centro* sea exactamente el número que queréis — el pago esperado de la opción. Una vez construida la diana, no lanzáis miles de dardos y contáis (eso es muestreo clásico). Le entregáis la diana a QAE, que estima la probabilidad del centro directamente y de forma mucho más barata. La astucia vive en construir la diana; la aceleración vive en cómo la leéis.

**Definición.** Construís, en dos etapas conceptuales, un operador $\mathcal{A}$ que actúa sobre un registro de qubits más un auxiliar:

1. **Cargar la distribución.** Un suboperador $\mathcal{P}$ prepara una superposición sobre los posibles resultados $x$ con amplitudes fijadas de modo que medir el registro dé el resultado $x$ con probabilidad $p(x)$ — la distribución de vuestro factor de riesgo $X$. Formalmente construye $\sum_x \sqrt{p(x)}\,\lvert x \rangle$.
2. **Codificar el pago.** Un segundo suboperador rota el qubit auxiliar un ángulo que depende del pago $f(x)$, de modo que condicionado al resultado $x$ el auxiliar es $\lvert 1 \rangle$ con probabilidad proporcional a $f(x)$.

Juntos, la probabilidad de medir el auxiliar en $\lvert 1 \rangle$ es

$$ a = \sum_x p(x)\, \tilde f(x) = \mathbb{E}\big[\tilde f(X)\big], $$

donde $\tilde f$ es $f$ reescalada a $[0,1]$. Así que $a$ — la amplitud que QAE estima — *es* la esperanza (normalizada). Estimad $a$ con error $\varepsilon$ y habréis estimado la esperanza con error $\varepsilon$ (por el factor de reescalado), en $O(1/\varepsilon)$ consultas.

**Ejemplo resuelto.** Supongamos que un pago toma solo dos valores en un modelo de juguete de dos resultados: $f = 0$ con probabilidad $0{,}7$ y $f = 1$ con probabilidad $0{,}3$. Tras la carga, el auxiliar es $\lvert 1 \rangle$ con probabilidad $a = 0{,}7 \times 0 + 0{,}3 \times 1 = 0{,}3$. El pago esperado verdadero es $0{,}3$. Clásicamente prepararíais-y-mediríais $N$ veces y vuestra estimación de $0{,}3$ llevaría un error $\sqrt{0{,}3 \times 0{,}7}/\sqrt{N} \approx 0{,}458/\sqrt{N}$ — el familiar $1/\sqrt{N}$. QAE en cambio lee $a = 0{,}3$ con error $\varepsilon$ en $O(1/\varepsilon)$ consultas. El mismo $0{,}3$; precisión cuadráticamente más barata.

**La maquinaria canónica, con honestidad.** El QAE original envuelve esto en **estimación de fase cuántica (QPE)**: trata el operador de amplificación de amplitud al estilo Grover como una rotación, y QPE lee su ángulo de rotación — del cual sale $a$. QPE es potente pero cara: necesita muchos qubits auxiliares extra y circuitos largos y coherentes, que el hardware ruidoso real detesta. Así que ahora existe una familia de variantes de **estimación de amplitud sin QPE / iterativa** (QAE iterativa, QAE por máxima verosimilitud y parientes) que recuperan el mismo escalado $O(1/\varepsilon)$ con circuitos mucho más superficiales — a costa de más rondas de medición, más simples. La aceleración titular es la misma; estas variantes son las que la hacen remotamente ejecutable en máquinas a corto plazo (NISQ).

<Callout variant="info" title="La diana es el cuello de botella">
Fijaos dónde se ha movido el trabajo. Clásicamente, generar cada muestra es barato y solo necesitáis muchas. En QAE, el operador de preparación de estado por consulta A — especialmente cargar la distribución P — puede ser *caro*, y lo incorporáis a cada consulta individual. Mantened un ojo en ese coste: es la bisagra sobre la que pivota toda la aceleración del mundo real, y volvemos a ella en la sección del asterisco (y le dedicamos una lección posterior entera).
</Callout>

### Cuándo usarlo

Este paso de codificación es exactamente donde QAE se encuentra con vuestro problema concreto: cualquier cantidad expresable como $\mathbb{E}[f(X)]$ para una distribución que podáis cargar y un pago que podáis calcular dentro del circuito es candidata. Elegid QAE cuando (a) podáis escribir vuestro objetivo como tal esperanza, (b) podáis construir un cargador $\mathcal{P}$ razonablemente barato, y (c) necesitéis suficiente precisión para que la brecha $1/\varepsilon$ frente a $1/\varepsilon^2$ pague el sobrecoste cuántico. Si no podéis cargar la distribución de forma barata, parad — la aceleración puede no sobrevivir, como advierte la penúltima sección.

<ThinkReveal
  client:visible
  prompt="Antes de seguir leyendo: si el pago esperado que queréis es una cifra en dólares como 4,20, pero la probabilidad del auxiliar 'a' debe vivir entre 0 y 1, ¿qué debéis hacer al pago antes de codificarlo — y cómo recuperáis la respuesta en dólares al final?"
  text="Reescalad (normalizad) el pago a [0,1] antes de cargarlo — p. ej. dividir por una cota superior del pago para que f-tilde quede en [0,1]. QAE entonces estima a = E[f-tilde(X)] en ese mundo escalado. Recuperáis la esperanza real multiplicando la a estimada de vuelta por el mismo factor de escala (y cualquier factor de descuento). El escalado 1/epsilon de las consultas es sobre la amplitud escalada; el reescalado solo convierte unidades al final."
/>

## Valorar una opción europea con QAE

<MCQ
  client:visible
  pretest
  question="Valorar una opción call europea se reduce a ¿qué objeto matemático?"
  options={[
    { text: "Un pago descontado esperado bajo una distribución neutral al riesgo — exactamente una esperanza que QAE puede atacar", correct: true },
    { text: "Un problema de ordenación sobre precios históricos" },
    { text: "Una inversión de matriz sin esperanza alguna" },
    { text: "Una tarea de clasificación que una red neuronal maneja mejor" },
  ]}
  explanation="El precio de una opción europea es la esperanza descontada de su pago bajo la medida neutral al riesgo. Cualquier esperanza que haríais por Monte Carlo es un objetivo de QAE."
/>

**Analogía.** El precio de una opción europea es un promedio ponderado de "lo que cobráis al vencimiento", ponderado por cuán probable es cada precio futuro (en el mundo neutral al riesgo) y encogido por un factor de descuento. Ese es el promedio del encuestador otra vez — y el impuesto de raíz cuadrada del encuestador otra vez — lo que significa que es precisamente el tipo de promedio que QAE fue construido para estimar de forma más barata.

**Definición.** Una call europea con strike $K$ sobre un subyacente con precio terminal $S_T$ tiene precio

$$ C = e^{-rT}\, \mathbb{E}^{\mathbb{Q}}\!\big[\max(S_T - K,\, 0)\big], $$

la esperanza neutral al riesgo descontada del pago $\max(S_T - K, 0)$. Para llevarlo a QAE, (1) cargáis la distribución neutral al riesgo de $S_T$ en el registro vía $\mathcal{P}$, (2) codificáis el pago reescalado $\max(S_T - K, 0)$ en la amplitud del auxiliar, y (3) ejecutáis la estimación de amplitud para leer $a = \mathbb{E}^{\mathbb{Q}}[\widetilde{\max(S_T - K,0)}]$ con error $\varepsilon$ en $O(1/\varepsilon)$ consultas, luego deshacéis el reescalado y aplicáis $e^{-rT}$.

**Ejemplo resuelto.** Digamos que queréis el precio de la opción a una precisión relativa de $\varepsilon = 10^{-3}$ (tres dígitos, típico para una mesa a la que le importan los puntos básicos de un libro). Lado a lado:

| Error objetivo $\varepsilon$ | Muestras clásicas $\approx 1/\varepsilon^2$ | Consultas QAE $\approx 1/\varepsilon$ | Razón de aceleración $\approx 1/\varepsilon$ |
|---|---|---|---|
| $10^{-1}$ | $10^{2}$ (100) | $10^{1}$ (10) | 10x |
| $10^{-2}$ | $10^{4}$ (10.000) | $10^{2}$ (100) | 100x |
| $10^{-3}$ | $10^{6}$ (1.000.000) | $10^{3}$ (1.000) | 1.000x |
| $10^{-4}$ | $10^{8}$ (100.000.000) | $10^{4}$ (10.000) | 10.000x |

En el objetivo de $10^{-3}$ de la mesa, la valoración clásica quema un millón de evaluaciones del pago mientras que QAE hace mil consultas — una reducción de mil veces en el *número de veces que debéis evaluar el escenario del pago*. Empujad a cuatro dígitos y la razón es de diez mil veces. La tabla es solo las dos leyes de escalado hechas concretas; fijaos en que la columna de aceleración es literalmente el inverso del error objetivo.

<Callout variant="info" title="La ganancia se compone a lo largo de un libro">
Una sola opción rara vez es el punto — las mesas valoran y revaloran miles de instrumentos y los pasan por rejillas de escenarios. Como la aceleración por instrumento es multiplicativa a lo largo de todo el libro, una mejora cuadrática en cada valoración es, en principio, una mejora cuadrática en toda la corrida nocturna de valoración. Por eso la valoración de opciones es la aplicación de QAE de manual: es una esperanza, necesita precisión y se ejecuta enormemente a menudo. (En principio. La sección del asterisco viene de camino.)
</Callout>

### Cuándo usarlo

La valoración con QAE es más atractiva para instrumentos donde el Monte Carlo clásico *ya* es el método elegido y la precisión es cara: pagos dependientes de la trayectoria y exóticos (asiáticas, barrera, cesta) donde no existe forma cerrada, y donde necesitáis muchos dígitos. Para una europea vainilla con una forma cerrada de Black–Scholes no haríais Monte Carlo en absoluto, mucho menos echaríais mano de un ordenador cuántico — no hay nada que acelerar. La regla general: QAE ayuda exactamente donde el MC clásico más duele.

<Categorize
  client:visible
  question="Clasificad cada situación de valoración según si la aceleración cuadrática de QAE encaja de forma natural."
  buckets={["Buen objetivo para QAE", "Mal objetivo para QAE"]}
  items={[
    { text: "Opción asiática dependiente de la trayectoria sin forma cerrada, que necesita precisión de 4 dígitos", bucket: "Buen objetivo para QAE" },
    { text: "Opción sobre cesta grande valorada por Monte Carlo con tolerancia ajustada", bucket: "Buen objetivo para QAE" },
    { text: "Número de riesgo de cola donde los dígitos extra realmente importan", bucket: "Buen objetivo para QAE" },
    { text: "Europea vainilla con una fórmula exacta de Black-Scholes", bucket: "Mal objetivo para QAE" },
    { text: "Una cantidad que solo necesitáis a uno o dos dígitos", bucket: "Mal objetivo para QAE" },
    { text: "Un problema donde cargar la distribución cuesta más que el muestreo que reemplaza", bucket: "Mal objetivo para QAE" },
  ]}
  explanation="QAE brilla en esperanzas que el MC clásico debe calcular a alta precisión y que no tienen forma cerrada. Se desperdicia en instrumentos con forma cerrada, necesidades de precisión gruesa o casos donde cargar la distribución se come la aceleración."
/>

## Riesgo: VaR y CVaR como objetivos de QAE

<MCQ
  client:visible
  pretest
  question="El valor en riesgo (VaR) y el VaR condicional / déficit esperado (CVaR) son cantidades de cola. ¿Por qué son objetivos naturales de QAE?"
  options={[
    { text: "Son probabilidades de cola y esperanzas condicionales — exactamente el tipo de esperanza que QAE estima con una aceleración cuadrática", correct: true },
    { text: "Requieren ordenación y los ordenadores cuánticos ordenan exponencialmente más rápido" },
    { text: "Son deterministas, así que no hace falta estimación en absoluto" },
    { text: "No están relacionados con esperanzas y no pueden ser objetivos de QAE" },
  ]}
  explanation="El VaR se define a través de una probabilidad de cola; el CVaR es la pérdida esperada más allá del VaR — una esperanza condicional. Ambos son esperanzas/probabilidades, el pan de cada día de la estimación de amplitud."
/>

**Analogía.** Medir el riesgo de cola es como estimar con qué frecuencia un río rebasa un muro de contención, y cuán grave se pone cuando lo hace. "Con qué frecuencia" es una *probabilidad* de cola (el cuantil del VaR); "cuán grave cuando lo hace" es una *esperanza* condicional (el CVaR). Ambos son promedios sobre eventos raros — los peores para el encuestador, porque los eventos raros exigen muestras enormes para precisarse. Por tanto, la ventaja cuadrática de QAE es más amplia exactamente aquí, donde el muestreo clásico está más hambriento.

**Definición.** Para una pérdida $L$ a nivel de confianza $\alpha$ (digamos $99\%$):

$$ \text{VaR}_\alpha = \inf\{\,\ell : \mathbb{P}(L \le \ell) \ge \alpha\,\}, \qquad \text{CVaR}_\alpha = \mathbb{E}\big[\,L \mid L \ge \text{VaR}_\alpha\,\big]. $$

El VaR es el umbral de pérdida bajo el cual se sitúa la masa de probabilidad $\alpha$; el CVaR (déficit esperado) es la pérdida media *dado que* habéis superado ese umbral. Para hallar el VaR con QAE estimáis la probabilidad de cola $\mathbb{P}(L \le \ell)$ — una amplitud — para umbrales candidatos $\ell$ (una búsqueda por bisección sobre umbrales, cada uno evaluado por QAE). Para obtener el CVaR estimáis la esperanza condicional de la pérdida en la cola — de nuevo una amplitud. Ambos heredan el escalado $O(1/\varepsilon)$ en consultas frente al clásico $O(1/\varepsilon^2)$.

**Ejemplo resuelto.** Queréis la probabilidad de cola al $99\%$ de una pérdida de cartera con error $\varepsilon = 10^{-3}$. El Monte Carlo clásico necesita unas $N_{\text{classical}} \approx 1/(10^{-3})^2 = 10^{6}$ simulaciones de escenario para clavar esa probabilidad — y la estimación de cola rara es aún más hambrienta en la práctica porque pocos de esos millones de escenarios caen en la cola del $1\%$. QAE estima la misma amplitud de cola en $N_{\text{quantum}} \approx 1/10^{-3} = 10^{3}$ consultas — mil frente a un millón, la misma razón $1/\varepsilon = 1000\times$. Para el CVaR, ejecutáis la misma maquinaria sobre la esperanza condicional; la aceleración cuadrática se traslada sin cambios porque eso también es solo una amplitud.

| Cantidad de riesgo | Qué estimáis | Coste clásico a $\varepsilon=10^{-3}$ | Coste QAE a $\varepsilon=10^{-3}$ |
|---|---|---|---|
| VaR$_{99\%}$ | Probabilidad de cola $\mathbb{P}(L \le \ell)$ | $\approx 10^{6}$ sims | $\approx 10^{3}$ consultas |
| CVaR$_{99\%}$ | Esperanza condicional $\mathbb{E}[L \mid L \ge \text{VaR}]$ | $\approx 10^{6}$ sims | $\approx 10^{3}$ consultas |

<Callout variant="tip" title="Las colas son donde el MC clásico sangra">
Cuanto más profundo en la cola vais (99,9 %, 99,99 %), más brutal es la factura de muestras clásica — estáis intentando estimar una probabilidad que es ella misma minúscula, así que la mayoría de las simulaciones no aportan nada. Este es exactamente el régimen donde ansiáis más precisión y donde la aceleración cuadrática es más valiosa. Si QAE alguna vez se gana el sueldo en finanzas de producción, el cálculo regulatorio de riesgo de cola (con su apetito por muchos dígitos sobre eventos raros) es un candidato destacado.
</Callout>

### Cuándo usarlo

Apuntad QAE a números de riesgo cuando necesitéis estimaciones de cola *ajustadas* y el MC clásico esté forzándose — VaR/CVaR de cola profunda, conjuntos de netting grandes, cálculos de capital regulatorio reejecutados constantemente. Como con la valoración, la palanca es la precisión: un VaR al $95\%$ de servilleta a dos dígitos no justificará una máquina cuántica, pero un déficit esperado al $99{,}9\%$ a cuatro dígitos, calculado de noche a lo largo de un libro enorme, es precisamente la forma de problema donde la pendiente $-1$ aplasta a la pendiente $-1/2$. Solo recordad que el cargador aún tiene que existir y ser barato.

<FillBlank
  client:visible
  question="Conectad las medidas de riesgo con la maquinaria de QAE."
  text="El VaR se halla estimando una {{probabilidad|varianza|duración}} de cola, mientras que el CVaR es una {{esperanza|cuantil|histograma}} condicional de la pérdida más allá del VaR — y como ambos son amplitudes, QAE los estima en aproximadamente 1 partido por {{epsilon|epsilon al cuadrado|N}} consultas."
  explanation="El VaR funciona a través de una probabilidad de cola; el CVaR es la esperanza condicional pasado el umbral del VaR. Ambos son amplitudes, así que QAE hereda el escalado O(1/epsilon) en consultas."
/>

## El asterisco: cuadrática, no mágica

<MCQ
  client:visible
  pretest
  question="La aceleración de QAE sobre el Monte Carlo clásico se describe mejor como:"
  options={[
    { text: "Cuadrática — convierte 1/epsilon^2 en 1/epsilon, valiosa pero acotada, y no hace triviales los problemas intratables", correct: true },
    { text: "Exponencial — convierte problemas de Monte Carlo intratables en instantáneos" },
    { text: "Incondicional — siempre gana en tiempo de reloj sin importar el hardware" },
    { text: "Irrelevante — no hay diferencia asintótica real frente al muestreo clásico" },
  ]}
  explanation="QAE es una aceleración CUADRÁTICA: de 1/epsilon^2 a 1/epsilon. Eso es significativo pero no transformador como lo sería una aceleración exponencial — y es una victoria en número de consultas, no aún una victoria en tiempo de reloj ni en PnL."
/>

**Analogía.** Una aceleración cuadrática es como pasar de andar a ir en bici — genuinamente más rápido, llegaréis mucho antes, y en distancias largas la brecha es grande. Pero no es teletransporte. Una aceleración *exponencial* (lo que el bombo cuántico insinúa constantemente) sería teletransporte: convertiría un problema que tarda la edad del universo en uno que tarda un segundo. QAE no hace eso. No puede convertir una esperanza clásicamente intratable en una trivial; puede hacer una clásicamente *cara-pero-factible* más barata — *si* todos los demás costes cooperan.

**Definición y la trampa.** El enunciado limpio es sobre el **número de consultas**: QAE usa $O(1/\varepsilon)$ aplicaciones de $\mathcal{A}$ frente a las $O(1/\varepsilon^2)$ muestras clásicas. Pero el coste en *tiempo de reloj* (o en dólares) es

$$ \text{coste total} \approx (\text{número de consultas}) \times (\text{coste por consulta}) \;+\; (\text{carga} + \text{lectura} + \text{sobrecoste de manejo de errores}). $$

La victoria cuadrática vive enteramente en el primer factor. Puede ser devorada por el resto:

- **La carga es la asesina silenciosa.** Construir $\mathcal{A}$ requiere cargar la distribución $\mathcal{P}$ en el estado cuántico. Si esa carga cuesta $O(2^n)$ o $O(N)$ puertas — como puede hacer un cargador general ingenuo — entonces *cada consulta* paga un coste comparable al muestreo clásico que intentabais evitar, y la ventaja $1/\varepsilon$ frente a $1/\varepsilon^2$ se evapora. La aceleración supone un cargador *barato*, que no es gratis para distribuciones arbitrarias.
- **Ruido.** En hardware NISQ real, los circuitos profundos y coherentes que el QAE canónico quiere acumulan errores rápido; podríais necesitar tantas repeticiones o tanta mitigación de errores que el escalado práctico no se parezca en nada al $O(1/\varepsilon)$ de manual.
- **Lectura y constantes.** La O grande oculta factores constantes y la velocidad de reloj comparativamente glacial de las puertas cuánticas de hoy frente a un FLOP clásico.

**Ejemplo resuelto.** Recordad que a $\varepsilon = 10^{-3}$ la victoria en número de consultas es $10^{6}$ frente a $10^{3}$ — una ventaja de $1000\times$ en *consultas*. Ahora supongamos que cada consulta cuántica, incluida la carga, es $10^{4}\times$ más lenta (en tiempo de reloj) que extraer una muestra clásica — totalmente plausible en hardware a corto plazo. Entonces el coste en tiempo de reloj es aproximadamente $10^{3} \times 10^{4} = 10^{7}$ "unidades de tiempo" para QAE frente a $10^{6} \times 1 = 10^{6}$ para el clásico. **La máquina cuántica es ahora 10x *más lenta* en tiempo de reloj pese a hacer 1000x menos preguntas.** La aceleración en consultas era real; simplemente no sobrevivió al contacto con el coste por consulta. Esa aritmética — ventaja en consultas frente a la penalización por consulta y por carga — es toda la pelea, y es lo que el resto de este curso dirime.

<Callout variant="warning" title="Un teorema en número de consultas no es una partida de PnL">
Interiorizad la distinción o exageraréis como cada nota de prensa: la aceleración cuadrática de QAE es un enunciado *demostrado* sobre cuántas consultas al oráculo necesitáis. *No* es, por sí mismo, un enunciado de que un ordenador cuántico valore vuestro libro más rápido, más barato o en absoluto hoy. La carga + lectura + ruido se interponen entre el teorema y la mesa de operaciones. Las lecciones 5 y 6 de este curso están dedicadas a esos muros — el problema de carga de datos / preparación de estado y la realidad del ruido / corrección de errores. Sostened la emoción de esta lección en una mano y ese escepticismo en la otra; ambos son correctos.
</Callout>

### Cuándo usarlo

Invocad la aceleración cuadrática como un argumento *real pero condicional*: es el caso más fuerte de las finanzas cuánticas precisamente porque la victoria algorítmica está demostrada, pero siempre debéis emparejarlo con la salvedad de la carga y el ruido. Usadlo para justificar interés investigador y apuestas de largo horizonte; *no* lo uséis para reclamar una ventaja de producción a día de hoy. La frase honesta de una línea: "QAE necesita demostrablemente cuadráticamente menos consultas — y si eso se convierte en menos segundos depende del cargador, la lectura y el ruido del hardware, que hoy normalmente dicen que no".

<Reveal client:visible>
<details className="rounded-card border border-ink-100 bg-surface px-4 py-3">
<summary className="cursor-pointer font-medium">¿Por qué una aceleración cuadrática no convierte un problema de Monte Carlo intratable en uno trivial?</summary>

**Respuesta.** Porque elevar al cuadrado el número de dígitos no es lo mismo que colapsar una exponencial. Si un problema clásico necesita $10^{12}$ muestras para alcanzar la precisión que queréis, QAE necesita unas $\sqrt{10^{12}} = 10^{6}$ consultas — un millón, aún muchas, y posiblemente aún inviable si cada consulta es lenta o el cargador es caro. La aceleración *reduce el exponente del coste de precisión de 2 a 1*; no cambia el hecho de que una precisión muy ajustada es intrínsecamente exigente. Una aceleración exponencial (como la de Shor para factorizar) convertiría $10^{12}$ en aproximadamente $\log_2(10^{12}) \approx 40$ — un universo distinto. QAE vive en el mundo más suave y acotado de las ganancias cuadráticas, que es exactamente por qué es la historia *creíble* de las finanzas cuánticas y no la fantástica.

</details>
</Reveal>

## Resumen

Vinisteis queriendo una victoria cuántica honesta, y aquí está: la estimación de amplitud lee una probabilidad desconocida $a$ con error $\varepsilon$ en $O(1/\varepsilon)$ consultas, demoliendo el $O(1/\varepsilon^2)$ del Monte Carlo clásico — una aceleración *cuadrática*, pendiente $-1$ contra pendiente $-1/2$. Mapead cualquier esperanza a una amplitud de auxiliar y QAE valora opciones (pagos esperados descontados) y mide riesgo (probabilidades de cola del VaR, esperanzas condicionales del CVaR) con esa misma reducción de raíz cuadrada en consultas. Luego el asterisco: es cuadrática, no exponencial; presupone un cargador de distribución barato; y un teorema en número de consultas solo se vuelve una victoria en tiempo de reloj o en PnL una vez pagados la carga, la lectura y el ruido — el tema de las lecciones que vienen.

<MindMap
  client:visible
  title="Estimación de amplitud y la aceleración de Monte Carlo"
  caption="Construid el mapa: el muro clásico, el resultado cuántico, las dos aplicaciones y el importantísimo asterisco."
  root={{
    label: 'Aceleración QAE de Monte Carlo',
    children: [
      { label: 'Muro del MC clásico', children: [
        { label: 'Error estándar = sigma / sqrt(N)' },
        { label: 'El error encoge como 1/sqrt(N)' },
        { label: 'Muestras necesarias ~ 1/epsilon^2' },
        { label: 'Un dígito más = 100x muestras' },
      ]},
      { label: 'Resultado central de QAE', children: [
        { label: 'Estimar amplitud a con error epsilon' },
        { label: 'Consultas ~ 1/epsilon (pendiente -1)' },
        { label: 'Aceleración cuadrática, razón ~ 1/epsilon' },
      ]},
      { label: 'De pago a amplitud', children: [
        { label: 'Cargar distribución con operador P' },
        { label: 'Codificar pago en amplitud del auxiliar' },
        { label: 'QAE basada en QPE vs iterativa / sin QPE' },
      ]},
      { label: 'Aplicaciones', children: [
        { label: 'Precio de opción = pago esperado descontado' },
        { label: 'VaR = probabilidad de cola' },
        { label: 'CVaR = esperanza condicional de cola' },
      ]},
      { label: 'El asterisco', children: [
        { label: 'Cuadrática, NO exponencial' },
        { label: 'Supone cargador P barato' },
        { label: 'Carga + lectura + ruido pueden comérsela' },
        { label: 'La victoria en consultas no es aún victoria en PnL' },
      ]},
    ],
  }}
/>

<Quiz
  client:visible
  title="Repaso mixto: ¿es la joya de la corona realmente una joya?"
  questions={[
    {
      question: "Una mesa necesita un precio de opción con error 1e-4 en vez de 1e-2. ¿Por qué factor crece el número de muestras del Monte Carlo CLÁSICO?",
      options: [
        { text: "Unas 10.000x — las muestras escalan como 1/epsilon^2, y (1e-2 / 1e-4)^2 = (100)^2 = 10.000", correct: true },
        { text: "Unas 100x — las muestras escalan como 1/epsilon" },
        { text: "Unas 2x — el error se reduce a la mitad cada vez que duplicáis las muestras" },
        { text: "Sin cambio — el número de muestras no depende del error objetivo" },
      ],
      explanation: "El coste clásico es 1/epsilon^2. Pasar de 1e-2 a 1e-4 es un error 100x más ajustado, elevado al cuadrado da 10.000x más muestras. Ese crecimiento castigador es lo que QAE ataca.",
    },
    {
      question: "A un error objetivo de 1e-3, ¿cuántas consultas al oráculo de QAE se necesitan aproximadamente, y cuál es la razón de aceleración sobre el muestreo clásico?",
      options: [
        { text: "Unas 1.000 consultas (1/epsilon), frente a ~1.000.000 muestras clásicas — una aceleración de ~1.000x", correct: true },
        { text: "Unas 1.000.000 consultas, igual que el clásico — sin aceleración" },
        { text: "Unas 30 consultas (log de 1/epsilon) — una aceleración exponencial" },
        { text: "Unas 1.000.000.000 consultas — más lento que el clásico" },
      ],
      explanation: "Las consultas de QAE escalan como 1/epsilon = 1.000; las muestras clásicas como 1/epsilon^2 = 1.000.000. La razón es 1/epsilon = 1.000x.",
    },
    {
      question: "¿Cómo se hace legible para QAE el pago esperado descontado de una opción europea?",
      options: [
        { text: "Se codifica como la probabilidad (amplitud al cuadrado) de medir un qubit auxiliar marcado, tras cargar la distribución neutral al riesgo y el pago reescalado", correct: true },
        { text: "Se almacena como el número de qubits usados en el circuito" },
        { text: "Se calcula clásicamente y solo lo verifica la máquina cuántica" },
        { text: "Se lee del tiempo de ejecución de las puertas cuánticas" },
      ],
      explanation: "Cargad la distribución con P, rotad el auxiliar por el pago reescalado, y la probabilidad de auxiliar-igual-a-1 se vuelve E[f-tilde(X)]. QAE estima esa amplitud.",
    },
    {
      question: "¿Qué afirmación sobre la aceleración de QAE es la honesta?",
      options: [
        { text: "Es una reducción CUADRÁTICA demostrada en número de consultas, pero la carga, la lectura y el ruido pueden borrar cualquier victoria en tiempo de reloj", correct: true },
        { text: "Es una aceleración exponencial que hace instantánea la valoración intratable" },
        { text: "Siempre vence al clásico en tiempo de reloj con el hardware de hoy" },
        { text: "Solo se aplica a la valoración de opciones, nunca a medidas de riesgo" },
      ],
      explanation: "Cuadrática, no exponencial; un teorema en número de consultas, no una victoria garantizada en tiempo de reloj ni en PnL. La salvedad del cargador y el ruido es esencial y es el foco de lecciones posteriores.",
    },
    {
      question: "¿Por qué el VaR y el CVaR de cola profunda son objetivos de QAE especialmente atractivos comparados con un VaR grueso al 95 %?",
      options: [
        { text: "Las colas profundas exigen alta precisión sobre eventos raros, donde el MC clásico más sangra y la brecha 1/epsilon frente a 1/epsilon^2 es más amplia", correct: true },
        { text: "Las colas profundas tienen soluciones en forma cerrada, así que el cuántico es innecesario" },
        { text: "Un VaR grueso no puede expresarse como esperanza en absoluto" },
        { text: "Los ordenadores cuánticos solo pueden estimar probabilidades por debajo del 1 %" },
      ],
      explanation: "La razón de aceleración es 1/epsilon, así que crece con la precisión que exigís. El riesgo de cola profunda necesita muchos dígitos sobre eventos raros, exactamente donde el muestreo clásico está más hambriento y la ventaja de QAE es mayor.",
    },
  ]}
/>
