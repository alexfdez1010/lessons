---
title: "La EVM: el ordenador mundial de Ethereum"
description: "Cómo funciona el ordenador compartido de Ethereum: la EVM como máquina de estados determinista, el estado mundial, las transacciones como transiciones de estado, por qué cada nodo lo reejecuta todo, y qué significan realmente «bytecode» y «Turing-completo»."
topic: "es/ethereum"
order: 2
minutes: 9
updated: 2026-06-03
---

import { Callout, MCQ, Quiz, Reveal, FillBlank, Categorize, MatchConcepts, MindMap, EvmStateMachine, BlockchainChain } from '@/components/react';

En la lección anterior dijimos que Ethereum no solo mueve dinero de un lado a otro: *ejecuta programas*. Contratos inteligentes, tokens, aplicaciones enteras, todo viviendo en la cadena. Lo cual plantea una pregunta de seguimiento incómoda que la mayoría de las explicaciones se saltan convenientemente:

**¿Dónde se ejecutan realmente esos programas?**

Vuestro portátil ejecuta programas en su CPU. Una web se ejecuta en un servidor de algún sitio. Pero un contrato inteligente no está en la máquina concreta de nadie: pertenece a toda la red. Así que Ethereum necesita un ordenador que *todos* ejecuten, que *nadie* posea y que *siempre* esté de acuerdo consigo mismo. Ese ordenador es la **EVM**, y esta lección es la visita guiada.

## Un ordenador, mil copias

Antes de explicarlo, una comprobación de intuición.

<MCQ
  client:visible
  question="Hay miles de nodos de Ethereum por todo el mundo. ¿En cuántos «ordenadores» se ejecuta efectivamente un contrato inteligente?"
  options={[
    { text: "Uno: un único ordenador virtual compartido del que cada nodo ejecuta una copia idéntica.", correct: true, explanation: "Exacto. La EVM es una sola máquina lógica; cada nodo mantiene su propia copia y todas coinciden." },
    { text: "Miles: cada nodo ejecuta su propio programa separado e independiente, con sus propios resultados.", explanation: "Si los resultados pudieran diferir de un nodo a otro no habría una única verdad. Ejecutan la misma máquina y llegan a la misma respuesta." },
    { text: "Cero: los contratos son solo datos, nada los ejecuta.", explanation: "Los contratos son código ejecutable; la EVM lo ejecuta. No son datos inertes." },
    { text: "Un servidor físico que Ethereum alquila en la nube.", explanation: "No hay servidor central. El «único ordenador» es virtual y está replicado en todos los nodos." },
  ]}
  checkLabel="Comprobar"
  retryLabel="Reintentar"
  correctLabel="¡Correcto!"
  incorrectLabel="Incorrecto"
  correctAnswerLabel="Respuesta correcta"
  explanationLabel="Explicación"
/>

La **EVM (máquina virtual de Ethereum)** es una **máquina virtual**: un ordenador definido por *reglas*, no por hardware. Una máquina virtual es un trozo de software que finge ser una CPU —tiene su propio conjunto de instrucciones, su propio modelo de memoria, su propia noción de qué es un «paso»— y cualquier ordenador real puede ejecutarla siguiendo esas reglas. Java funciona así; también los emuladores de videoconsolas. La gracia es la portabilidad: escribe las reglas una vez y toda máquina que las siga se comporta *de forma idéntica*.

Ethereum coge esa idea y la hace social. Hay conceptualmente **una sola EVM**, pero su estado está **replicado en todos los nodos** de la red. Imaginadla como una hoja de cálculo compartida gigantesca —un Google Doc del tamaño del planeta— que miles de personas tienen abierta a la vez. Nadie edita una celda a mano. El documento solo cambia cuando se envía una edición que cumple las reglas y *todos aplican esa misma edición*. Como todos parten del mismo documento y aplican las mismas ediciones con las mismas reglas, todos acaban viendo el mismo documento. Un ordenador, mil copias idénticas.

<Callout variant="info" title="Virtual ≠ falso">
«Virtual» no significa de mentira. Significa *definido por una especificación en lugar de por el silicio*. La EVM es tan real como cualquier ordenador: simplemente vive en un libro de reglas acordado que miles de máquinas obedecen simultáneamente.
</Callout>

## Estado mundial: lo que el ordenador recuerda

Un ordenador sin memoria es un pisapapeles muy caro. La memoria de la EVM se llama **estado mundial**.

El **estado mundial** es el conjunto completo de todo lo que la EVM recuerda en un momento dado: cada **cuenta** y todos sus datos. Para cada cuenta eso significa:

- su **saldo** (cuánto ETH tiene),
- para las cuentas de contrato, el **código** que se ejecuta cuando la llamas,
- y el **almacenamiento** de ese contrato (sus variables privadas y persistentes: la tabla de «quién posee cuánto» de un contrato de tokens vive aquí).

Si la EVM es una videoconsola, el estado mundial es la **partida guardada**. Pulsa el botón (envía una transacción) y la partida se actualiza. Reinicia un nodo desde cero, reproduce cada guardado hasta ahora y aterrizas exactamente en la misma partida guardada que todos los demás.

<FillBlank
  client:visible
  question="Rellena los huecos sobre qué contiene el estado mundial."
  text="El {{estado mundial|recompensa de bloque|límite de gas}} es el conjunto de todas las cuentas y sus datos —saldos, código de contratos y el {{almacenamiento|cotilleo|firma}} persistente de cada contrato— en un instante del tiempo. Una analogía útil es la {{partida guardada|cable de alimentación|mando}} de la videoconsola."
  explanation="Estado mundial = todas las cuentas + sus datos. El almacenamiento de un contrato es su memoria persistente en cadena. La analogía de la partida guardada captura «todo el estado recordado ahora mismo»."
  checkLabel="Comprobar"
  retryLabel="Reintentar"
  explanationLabel="Explicación"
  correctResultLabel="✓ Correcto"
/>

## Una transacción es una transición de estado

Aquí va la frase más importante de toda esta lección, así que la pondremos en código:

`newState = EVM(oldState, transaction)`

Ese es el modelo entero. Una **transacción** es una instrucción enviada a la red («envía 3 ETH de A a B», o «llama a la función `mint` de este contrato»). La EVM coge el **estado mundial antiguo** más esa transacción y calcula un **estado mundial nuevo**. Nada más místico que eso: una transacción es una **transición de estado**, una receta para convertir una partida guardada en la siguiente.

Y la EVM es una **máquina de estados**: un sistema que está en algún estado y pasa a un estado nuevo, completamente determinado, cuando llega una entrada. (Un interruptor de la luz: estados «encendido» y «apagado», entrada «pulsar». La misma idea, con algunas cuentas más.)

La palabra mágica es **determinista**. Determinista significa *cero sorpresas, jamás*: el mismo estado de partida más la misma transacción produce el **mismo** estado nuevo, **exactamente** igual, en todos los nodos, todas las veces, para siempre. Sin aleatoriedad, sin «depende del tiempo que haga», sin «en mi máquina funciona». Esa propiedad es la base que permite que miles de desconocidos se pongan de acuerdo sin fiarse los unos de los otros.

Juega con una abajo. Pasa unas cuantas transacciones predefinidas por la máquina y observa cómo el estado mundial va de *antes* a *después*. Prueba primero una transferencia normal de ETH (mira cómo cambian dos saldos), luego una llamada a contrato (mira cómo cambia el número almacenado del contrato), y luego pulsa reiniciar y ejecuta la *misma* secuencia otra vez: fíjate en que obtienes el resultado idéntico. Esa repetibilidad es el determinismo, en directo.

<EvmStateMachine
  client:visible
  caption="Mismo inicio + misma transacción ⇒ mismo resultado, todas las veces. Eso es el determinismo, y es la razón por la que todos los nodos de la Tierra coinciden en el estado mundial."
  worldStateLabel="Estado mundial"
  balanceLabel="saldo"
  storageLabel="contador almacenado"
  inputLabel="Transacción (entrada)"
  outputLabel="Nuevo estado (salida)"
  contractLabel="Contrato Contador"
  nextLabel="Siguiente transacción"
  resetLabel="Reiniciar"
  steps={["A envía 1 ETH a B", "B llama a increment() en el Contador", "A envía 0,5 ETH al Contador y llama a increment()"]}
/>

<MCQ
  client:visible
  question="Un nodo en Tokio y un nodo en São Paulo aplican ambos la transacción T al mismo estado mundial de partida S. ¿Qué garantiza el determinismo sobre sus resultados?"
  options={[
    { text: "Calculan exactamente el mismo estado mundial nuevo, byte a byte.", correct: true, explanation: "Misma entrada + mismo estado de partida ⇒ misma salida, siempre. Eso es lo que hace posible «un único ordenador compartido»." },
    { text: "Probablemente obtienen estados parecidos, pero cabe esperar pequeñas diferencias.", explanation: "No: «probablemente parecidos» rompería el consenso. Determinismo significa idéntico, no aproximado." },
    { text: "Gana el resultado de Tokio porque probablemente ejecutó primero.", explanation: "El orden de ejecución entre nodos no cambia las matemáticas. Ambos alcanzan el mismo estado sin importar quién lo ejecutó antes." },
    { text: "El resultado depende de la CPU y el sistema operativo de cada nodo.", explanation: "La EVM está especificada con precisión para que el hardware no pueda cambiar la respuesta. Esa es la gracia de una máquina virtual." },
  ]}
  checkLabel="Comprobar"
  retryLabel="Reintentar"
  correctLabel="¡Correcto!"
  incorrectLabel="Incorrecto"
  correctAnswerLabel="Respuesta correcta"
  explanationLabel="Explicación"
/>

### Una transferencia, paso a paso

Supón que el estado mundial tiene la cuenta **A** con 10 ETH y la cuenta **B** con 2 ETH. A firma y difunde la transacción «envía 3 ETH a B». La EVM hace la aritmética obvia contra el estado *antiguo*:

| Cuenta | Antes | Cambio | Después |
|---|---:|---:|---:|
| A | 10 ETH | −3 | 7 ETH |
| B | 2 ETH | +3 | 5 ETH |

El nuevo estado mundial es «A: 7, B: 5». Cambiaron dos celdas de la hoja de cálculo gigante; nada más se movió. (Ignoramos la pequeña comisión por ahora: eso es la próxima lección.) Todo nodo que aplique esta transacción a ese mismo estado antiguo aterriza en A: 7, B: 5. Sin excepciones, porque no hay margen para ninguna.

## Por qué cada nodo lo reejecuta todo (avance del gas)

Aquí va la parte que suena absurda hasta que encaja: **cada nodo reejecuta de forma independiente cada transacción.** Tu nodo no se fía de la palabra de otro nodo sobre los nuevos saldos. Descarga la transacción, la ejecuta en *su propia* EVM, calcula el resultado por sí mismo y comprueba que todos los demás obtuvieron lo mismo.

Imagina miles de contables meticulosos, a cada uno se le entrega el mismo montón de recibos, cada uno rehace cada suma a mano y luego comparan respuestas. Tremendamente redundante, y esa redundancia *es la seguridad*. Para hacer trampa no engañarías a un contable; tendrías que engañar a la *mayoría* de ellos simultáneamente, todos ejecutando reglas idénticas y deterministas. Suerte con eso.

Pero reejecutarlo todo tiene un coste, y apunta directamente al siguiente concepto:

<Reveal client:visible summary="Si cada nodo rehace el trabajo, ¿no es eso un desperdicio?">
Sí, y deliberadamente. El desperdicio *es* el producto. Un servidor en la nube normal es barato porque exactamente una máquina hace el trabajo y te fías de ella. La EVM es cara porque *miles* de máquinas hacen el mismo trabajo y no te fías de **ninguna** de ellas individualmente: solo de su acuerdo. No estás pagando por cómputo en bruto; estás pagando por verificación sin confianza.

Ese coste es también por lo que cada operación debe estar **medida y pagada**. Si el cómputo fuera gratis, un solo atacante podría enviar una transacción con un bucle infinito y obligar a cada nodo de la Tierra a girar eternamente. Así que la EVM cobra por cada paso en una unidad llamada **gas**, y una transacción que se queda sin gas se detiene. El gas tiene una lección entera para él solo; por ahora, simplemente guarda el enlace en tu cabeza: *reejecución por parte de todos ⇒ el cómputo debe tener precio.*
</Reveal>

## Bytecode y ser Turing-completo

Nadie escribe instrucciones EVM en crudo a mano (bueno, casi nadie, y los que lo hacen necesitan un abrazo). Los desarrolladores escriben contratos en lenguajes de alto nivel como **Solidity** o **Vyper**: código legible con funciones, variables y sentencias `if`. Un compilador luego lo traduce a **bytecode de la EVM**: una larga cadena de **opcodes** de bajo nivel, las diminutas instrucciones primitivas que la EVM ejecuta realmente (`ADD`, `SSTORE`, `JUMP` y compañía). El bytecode es a Solidity lo que el código máquina es al C que escribirías en tu portátil: la misma lógica, triturada en pasos que una máquina puede masticar.

La EVM también es **Turing-completa**. Turing-completa significa que puede, en principio, expresar *cualquier* cómputo que puedas describir —bucles, ramificaciones, lógica arbitraria— con suficientes recursos. Eso es potente: es por lo que Ethereum puede alojar programas genuinamente complicados, no solo «mover moneda de X a Y».

La pega de la Turing-completitud es el **problema de la parada**: una máquina suficientemente expresiva puede escribir programas que se repiten para siempre, y no siempre puedes saber de antemano si uno lo hará. La solución de la EVM es el gas (otra vez): cada paso cuesta gas, una transacción lleva un presupuesto finito de gas, y cuando el presupuesto se agota, la ejecución **se detiene**. Así que la EVM es Turing-completa *con un contador*: puede expresar cualquier cosa, pero no puede ejecutar nada *gratis ni para siempre*.

Esto es un contraste deliberado con **Bitcoin Script**, el lenguaje en cadena de Bitcoin, que intencionadamente **no** es Turing-completo. Bitcoin Script no tiene bucles sin límite a propósito: menos expresivo, pero más fácil de razonar y más difícil de pegarte un tiro en el pie. Ethereum eligió el intercambio opuesto: máxima expresividad, vallada por el gas.

<MatchConcepts
  client:visible
  question="Empareja cada término con lo que realmente es."
  pairs={[
    { term: "Solidity", definition: "Un lenguaje de alto nivel en el que los desarrolladores escriben contratos" },
    { term: "Bytecode", definition: "Las instrucciones de bajo nivel que la EVM ejecuta realmente" },
    { term: "Opcode", definition: "Una única instrucción primitiva de la EVM, p. ej. ADD" },
    { term: "Determinista", definition: "Misma entrada + mismo estado de partida ⇒ mismo resultado en todas partes" },
    { term: "Gas", definition: "El contador que hace que un bucle desbocado acabe por detenerse" },
  ]}
  explanation="El código fuente de alto nivel (Solidity) se compila a bytecode, una secuencia de opcodes. El determinismo hace que todos los nodos coincidan; el gas acota la ejecución para que la Turing-completitud no pueda repetirse para siempre."
  checkLabel="Comprobar"
  retryLabel="Reintentar"
  explanationLabel="Explicación"
  allCorrectLabel="✓ Todo emparejado"
/>

<Categorize
  client:visible
  question="Clasifica cada afirmación: ¿es cierta para la EVM o no?"
  buckets={["Una propiedad de la EVM", "NO es cierto de la EVM"]}
  items={[
    { text: "Es determinista: la misma entrada siempre da la misma salida", bucket: "Una propiedad de la EVM" },
    { text: "Es Turing-completa (acotada por el gas)", bucket: "Una propiedad de la EVM" },
    { text: "Su estado está replicado en todos los nodos", bucket: "Una propiedad de la EVM" },
    { text: "Se ejecuta en un servidor central propiedad de Ethereum", bucket: "NO es cierto de la EVM" },
    { text: "Cada nodo puede calcular un resultado ligeramente distinto", bucket: "NO es cierto de la EVM" },
    { text: "Los programas pueden repetirse para siempre gratis", bucket: "NO es cierto de la EVM" },
  ]}
  explanation="La EVM es determinista, replicada y Turing-completa con una parada acotada por el gas. No tiene servidor central, los nodos nunca discrepan sobre un resultado correcto y nada se ejecuta gratis ni para siempre."
  checkLabel="Comprobar"
  retryLabel="Reintentar"
  explanationLabel="Explicación"
  allCorrectLabel="✓ Todo ordenado"
/>

## Los bloques ponen en orden las transacciones

Una última pieza. `newState = EVM(oldState, transaction)` está muy bien para *una* transacción, pero entran miles cada minuto, y el orden importa. (Si A tiene 10 ETH e intenta enviar 8 ETH tanto a B como a C, cuál sale adelante depende enteramente de cuál se procese primero.) Así que la EVM necesita un único **orden** acordado.

Ese es el trabajo de los **bloques**. Las transacciones se agrupan en un bloque; los bloques se colocan en una secuencia estricta, cada uno enlazado criptográficamente con el anterior (la cadena de «blockchain»). Aplica los bloques en orden, las transacciones dentro de cada bloque en orden, y cada nodo recorre el *mismo* camino desde el estado génesis hasta el estado de hoy, y aterriza en el *mismo* estado mundial. El determinismo se ocupa de cada paso; el orden de los bloques se ocupa de la *secuencia* de pasos.

<BlockchainChain
  client:visible
  caption="Cada bloque empaqueta un lote ordenado de transacciones y enlaza con el bloque anterior. Mismos bloques, mismo orden ⇒ mismo estado mundial en todos los nodos."
/>

<MCQ
  client:visible
  question="Dos nodos tienen ambos una EVM determinista y exactamente el mismo conjunto de transacciones pendientes. ¿Qué más deben compartir para tener garantizado el mismo estado mundial final?"
  options={[
    { text: "El mismo orden en el que se aplican las transacciones, que proporcionan los bloques.", correct: true, explanation: "El determinismo hace que cada paso sea idéntico, pero el resultado aún puede depender de la secuencia. El orden de los bloques fija el orden para que todos coincidan." },
    { text: "Nada: el determinismo por sí solo garantiza plenamente el mismo estado final sin importar el orden.", explanation: "El determinismo garantiza que cada paso individual coincide, pero distintos órdenes pueden dar resultados distintos. El orden también debe acordarse." },
    { text: "El mismo hardware físico.", explanation: "El hardware es irrelevante para una máquina virtual: el ingrediente que falta es el orden, no el silicio." },
    { text: "Un coordinador central de confianza que elija un ganador.", explanation: "No existe coordinador central; el orden viene de la secuencia acordada de bloques." },
  ]}
  checkLabel="Comprobar"
  retryLabel="Reintentar"
  correctLabel="¡Correcto!"
  incorrectLabel="Incorrecto"
  correctAnswerLabel="Respuesta correcta"
  explanationLabel="Explicación"
/>

## La imagen global

<MindMap
  client:visible
  title="La EVM de un vistazo"
  root={{
    label: "La EVM",
    children: [
      { label: "Estado mundial", children: [
        { label: "Cuentas + saldos" },
        { label: "Código + almacenamiento de contratos" },
        { label: "La partida guardada" },
      ]},
      { label: "Transacción = transición de estado", children: [
        { label: "newState = EVM(oldState, tx)" },
        { label: "Entra el estado antiguo, sale el nuevo" },
      ]},
      { label: "Reejecución y determinismo", children: [
        { label: "Cada nodo lo reejecuta todo" },
        { label: "Misma entrada ⇒ misma salida" },
        { label: "Redundancia = seguridad" },
      ]},
      { label: "Bytecode y Turing-completo", children: [
        { label: "Solidity → bytecode → opcodes" },
        { label: "Puede expresar cualquier cómputo" },
        { label: "El gas acota la parada" },
      ]},
    ],
  }}
  caption="Cuatro ideas: qué recuerda, cómo cambia, por qué todos coinciden y qué puede ejecutar."
/>

<Quiz
  client:visible
  questions={[
    {
      question: "En una frase, ¿qué es la EVM?",
      options: [
        { text: "Un único ordenador virtual, replicado en todos los nodos, que ejecuta los programas de Ethereum.", correct: true },
        { text: "Una granja de servidores en la nube operada por la Ethereum Foundation." },
        { text: "La aplicación de monedero que usas para guardar ETH." },
        { text: "La moneda con la que pagas las comisiones de transacción." },
      ],
      explanation: "La EVM es una sola máquina lógica, definida por reglas y copiada en todos los nodos: no hay operador central. Un monedero simplemente habla con Ethereum, y el ETH/gas es la moneda, no la máquina que ejecuta el código.",
    },
    {
      question: "¿Cuáles de estos pertenecen al estado mundial? (Marca todas las que correspondan.)",
      allowMultiple: true,
      options: [
        { text: "Los saldos de las cuentas", correct: true },
        { text: "El almacenamiento persistente de un contrato", correct: true },
        { text: "El código desplegado de un contrato", correct: true },
        { text: "Las cookies de tu navegador local" },
      ],
      explanation: "El estado mundial contiene los datos de cada cuenta: saldos, almacenamiento de contratos y código de contratos desplegado. Las cookies de tu navegador viven en tu dispositivo, no en el estado compartido de Ethereum.",
    },
    {
      question: "¿Por qué el cómputo en la EVM debe medirse con gas?",
      options: [
        { text: "Porque cada nodo lo reejecuta, un programa sin límite obligaría a toda la red a ejecutarse para siempre.", correct: true },
        { text: "Para hacer Ethereum más lento a propósito." },
        { text: "Porque la EVM no es determinista y necesita un desempate." },
        { text: "Para sustituir la necesidad de bloques." },
      ],
      explanation: "La reejecución por cada nodo más la Turing-completitud implica que debes acotar la ejecución, o un solo bucle infinito detiene a todos. El gas es un contador, no un acelerador ni un desempate, y los bloques (que ordenan las transacciones) son otro trabajo distinto.",
    },
    {
      question: "Un desarrollador escribe un contrato en Solidity. ¿Qué ejecuta realmente la red?",
      options: [
        { text: "Bytecode de la EVM: los opcodes a los que se compiló el Solidity.", correct: true },
        { text: "El código fuente Solidity en crudo, línea a línea." },
        { text: "Una descripción en español de lo que el contrato debería hacer." },
        { text: "Una fórmula de hoja de cálculo." },
      ],
      explanation: "El código fuente de alto nivel se compila a bytecode; la EVM entiende bytecode, no Solidity, prosa ni fórmulas de hoja de cálculo (la hoja de cálculo era solo una analogía del estado compartido).",
    },
    {
      question: "¿En qué se diferencia Bitcoin Script de la EVM, por diseño?",
      options: [
        { text: "Bitcoin Script intencionadamente NO es Turing-completo: sin bucles sin límite, menos expresivo, más fácil de razonar.", correct: true },
        { text: "Bitcoin Script es más expresivo que la EVM y puede ejecutar cualquier programa." },
        { text: "Bitcoin Script también se ejecuta en la EVM." },
        { text: "No hay diferencia; ambos son lenguajes idénticos." },
      ],
      explanation: "Bitcoin limitó deliberadamente la expresividad por seguridad; Ethereum eligió la Turing-completitud vallada por el gas. Son sistemas separados con filosofías de diseño marcadamente distintas.",
    },
    {
      question: "El determinismo más el orden de los bloques juntos dan a Ethereum, ¿qué garantía?",
      options: [
        { text: "Cada nodo, aplicando los mismos bloques en el mismo orden desde el mismo inicio, alcanza el mismo estado mundial.", correct: true },
        { text: "Las transacciones nunca pueden fallar." },
        { text: "Las comisiones caen a cero." },
        { text: "Cada nodo acaba con su propio estado único." },
      ],
      explanation: "El determinismo hace que cada paso coincida; el orden hace que la secuencia coincida; juntos hacen que todos los nodos converjan en un estado compartido. Las transacciones aún pueden fallar (p. ej. quedarse sin gas) y el gas sigue aplicándose: la garantía va sobre el acuerdo, no sobre el éxito ni el coste.",
    },
  ]}
  questionLabel="Pregunta"
  ofLabel="de"
  correctCountLabel="correctas"
  completeLabel="Completado"
  scoreLabel="Tu puntuación"
  checkLabel="Comprobar"
  retryLabel="Reintentar"
  correctLabel="¡Correcto!"
  incorrectLabel="Incorrecto"
  correctAnswerLabel="Respuesta correcta"
  explanationLabel="Explicación"
  nextLabel="Siguiente"
  seeResultsLabel="Ver resultados"
  backLabel="Atrás"
  restartLabel="Reiniciar"
  gateLabel="Comprueba tu respuesta para continuar."
/>

## Ideas clave

<Callout variant="success" title="Qué recordar">
- La **EVM** es una sola **máquina virtual** —un ordenador definido por reglas— cuyo **estado mundial** está replicado de forma idéntica en todos los nodos. Un ordenador, mil copias.
- El **estado mundial** es cada cuenta y sus datos (saldos, código de contratos, almacenamiento de contratos): la partida guardada de la red.
- Una **transacción es una transición de estado**: `newState = EVM(oldState, transaction)`. La EVM es **determinista**: mismo inicio + misma transacción ⇒ el mismo resultado en todas partes, siempre.
- **Cada nodo reejecuta cada transacción.** Esa redundancia es la seguridad, y es por lo que el cómputo debe medirse y pagarse con **gas**.
- Los contratos se escriben en **Solidity/Vyper**, se compilan a **bytecode de la EVM** (opcodes). La EVM es **Turing-completa** (puede expresar cualquier cómputo) pero **acotada por el gas**, así que nada se repite para siempre, a diferencia del intencionadamente limitado Bitcoin Script.
- Los **bloques** ponen las transacciones en un orden acordado, de modo que todos los nodos aplican los mismos cambios en la misma secuencia y convergen en el mismo estado mundial.
</Callout>

Hemos hablado mucho de «cuentas» que guardan saldos, pero ¿cómo funciona realmente una cuenta? La respuesta de Ethereum (el **modelo de cuentas**) es sorprendentemente distinta del enfoque de monedas-en-un-tarro de Bitcoin (el **modelo UTXO**). Lo próximo: cuentas frente a UTXO, y por qué Ethereum eligió el modelo que eligió.
